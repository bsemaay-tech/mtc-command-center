# KVM2 repair cycle 2 round 2 — Stage A: execution companion only

Work in `C:\LAB\Tradingview_LAB_CLEAN`. Read and obey `AGENTS.md`. Preserve all
unrelated dirty-worktree changes. Do not run Git mutations or access any VPS,
runtime, broker, exchange, secret, or network system.

Edit only:

`MTC_COMMAND_CENTER/11_TRIAGE/KVM2_AI_LAB_AND_BRIDGE_EXECUTION_TASKS_2026-07-25.md`

This is Stage A of the focused repair defined in
`KVM2_MASTER_PLAN_REPAIR_CYCLE2_ROUND2_PROMPT_2026-07-26.md`. Read that repair
prompt, but in this stage apply only its execution-companion changes. Do not
edit the master plan or audit prompt yet.

Close all companion-side requirements:

1. Prove `HL_LIVE_ACK` absent from unit, EnvironmentFile, and running process
   environment at P4-03, P4-07, P4-08B, P5-05A and crosswalk item 4; presence
   is a Stop; never expose unrelated environment values.
2. Add a non-circular owner-controlled pre-P5-06 ARMED crash-recovery task:
   one attempt per authorization, restart DISARMED, raw exchange reconcile,
   open-position/order handling, P5-04 window reclassification, hashed injected
   staging crash drill. Require it closed at P5-05A/P5-06. Require P2-10 applied
   and verified before the counter. Never authorize automatic re-ARM/retry.
3. Phase 8: immutable design manifest, then distinct Barış owner admission and
   any separate network/security gate, then bounded P8-02 build/start, then
   independent separation proof. Builder cannot self-authorize.
4. Split Option A and B preparation-only facts from destructive
   reprovision/purchase action facts. Add distinct Barış one-shot authorization
   gates naming target/decision, manifest, recovery proof, executor, and one
   bounded action. Option selection alone authorizes nothing.
5. Add persistent-log rotation/retention/compression policy + hash at P2-04 and
   forced-rotation proof at P4-07. Restore all canonical item-8 requirements in
   crosswalk row 8 with real closing tasks.
6. P4-05 and crosswalk row 6 must require two timestamped raw exchange-side
   positions/orders responses bracketing old-host authority revocation.
7. P2-12 and P3-05 must disclose and accept P2-09's exact VERIFIED or
   BLOCKED/UNVERIFIED verdict. P0-04 must own restricted-evidence retention and
   deletion policy before Phase 1.
8. Add `/tmp`, `/dev/shm`, journald group access, abstract AF_UNIX sockets,
   kernel keyrings, and `PrivateTmp=yes` where appropriate to P5-10(c), P6-01,
   and P6-02 with executable negative evidence.

Hard structural rules:

- Final file must be below 60,000 bytes. Compress only redundant wording; do not
  weaken an authority, safety, evidence, dependency, or Stop gate.
- Every task definition has exactly one line beginning `  - Evidence:` and one
  line beginning `  - Stop:`.
- Task IDs are unique. Compute and report the new exact task count.
- Crosswalk rows 1–10 remain exactly once.
- Keep the program preparation-only. Do not claim an audit verdict.
- Do not update hashes or other files in Stage A.

Return changed task IDs, final task count, exact structure checks, and byte size.
