# KVM2 repair cycle 2 round 2 — Stage B: master synchronization and hashes

Work in `C:\LAB\Tradingview_LAB_CLEAN`. Read and obey `AGENTS.md`. Preserve all
unrelated dirty-worktree changes. Do not run Git mutations or access any VPS,
runtime, broker, exchange, secret, or network system.

Read the repaired execution companion:

`MTC_COMMAND_CENTER/11_TRIAGE/KVM2_AI_LAB_AND_BRIDGE_EXECUTION_TASKS_2026-07-25.md`

It is the detailed task authority and has independently verified Stage-A
structure: 81 standard-shape unique task IDs, exact Evidence/Stop pairs,
crosswalk rows 1–10 once each, and size below 60,000 bytes.

Edit only:

- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_AI_LAB_AND_BRIDGE_MASTER_PLAN_2026-07-25.md`
- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_MASTER_PLAN_MULTI_MODEL_AUDIT_PROMPT_2026-07-25.md`

Synchronize the master with the repaired companion:

1. Update every stale detailed-task count from 77 to 81.
2. Replace the false “no task ID appears” claim with: no task *definition*
   appears in the master; shorthand task references are cross-references only;
   the companion governs task text; any conflict is a BLOCK until reconciled.
   Keep zero full `KVM2-P...` IDs in the master.
3. Extend Invariant 7 / isolation text to cover shared `/tmp` and `/dev/shm`,
   journald access through `systemd-journal`/`adm`, abstract AF_UNIX sockets,
   kernel keyrings, and `PrivateTmp=yes` where appropriate.
4. Synchronize Phase 5 and authority text with the P5-05B owner-controlled
   ARMED crash/recovery procedure and staging drill before P5-05A/P5-06; no
   automatic re-ARM/retry; P2-10 applied before the monitoring counter.
5. Synchronize Phase 8 with immutable design manifest P8-01, owner admission
   P8-01A, and separate network/security gate before any bounded build/start.
   Phase 7 closure alone authorizes nothing.
6. Synchronize clean-image Option A/B authority with preparation-only P10-03A/B
   and one-shot owner/action gates P10-03C/D. P10-02 selection authorizes
   neither wipe nor purchase. Name P10-03C/D in the authority matrix.
7. Reflect the repaired companion’s log-rotation proof, raw exchange-side
   cutover reconfirmation, P2-09 verdict carry-forward, and P0-04 restricted
   evidence-retention policy wherever the master summarizes those contracts.
8. Preserve preparation-only status and all install/deploy/cutover/first-start/
   ARM/lab/network/reprovision/purchase/mainnet authorization separations.

Then:

- Keep master below 60,000 bytes.
- Verify companion remains unchanged and below 60,000 bytes.
- Verify 81 companion task definitions, unique standard-shape IDs, exact
  Evidence/Stop fields, and crosswalk rows 1–10 once each.
- Verify zero full `KVM2-P...` IDs in master and no stale `77 task` phrase.
- Compute fresh byte-level SHA-256 for master and companion.
- Update the audit prompt’s current immutable hashes and round label. Move the
  prior current hashes to clearly superseded entries.
- Do not edit the companion, claim an audit verdict, or authorize execution.

Return concise validation with sizes, task count, structure, and fresh hashes.
