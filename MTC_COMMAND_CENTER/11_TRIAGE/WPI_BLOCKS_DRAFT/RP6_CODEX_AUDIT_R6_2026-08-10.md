VERDICT: REQUEST_CHANGES

| ID | Checked item | Evidence from the frozen candidate and independent execution | Result |
|---|---|---|---|
| A1 | Audit contract and scope | Classified **T0** because this is a staging/host preflight. Reviewed locally and read-only; no host, SSH, network, service, broker, deployment, backtest, Pine, parity, MTC, or trading action was run. `RP6-P0.sh` was not edited. | PASS |
| A2 | Candidate identity | `HEAD=8fcab4d490c5699c6ce87e498d5e1d2ca7e2a33a`; target is clean against `HEAD`. Independent Windows and Git Bash measurements agree: SHA-256 `75db028e76438bc88caba19b9c3b6411e5f573f7b6c2bd13c3883d24e4389570`, `93421` bytes, zero CR bytes, first three bytes `23 20 3d` (no BOM). | PASS |
| A3 | R5-F1 — omitted `python3` pin must STOP | The post-loop requirement is present at `RP6-P0.sh:541-553`. Anchored execution of the R5-F1 body returned `rc=0` with `R5_F1_QA_SUMMARY cases=6 pass=6 fail=0 result=PASS`; omission is rc 3 and a filled matching `python3` pin is admitted. | PASS |
| A4 | R5-F2 — prerequisite names must be shell functions, not PATH files/aliases | The two checks at `RP6-P0.sh:359-362` reject ordinary PATH-file and alias resolution, and the original R5-F2 body passes 4/4. But the report calls this an “exact builtin type assertion” while the code invokes overridable `type`, not `builtin type`. Independent falsification defined `type(){ printf 'function\n'; }`: output was `FAKE_TYPE=[function] BUILTIN_TYPE=[]`; both guards passed, the missing `rp0_require_safe_component` invocation reached `command_not_found_handle`, and the case ended `RC=0 … PREREQ_GATE_ACCEPTED`. Even with `builtin type`, function type alone cannot prove the function came from RP0-LIB, so `P0_prereq lib=sourced` at line 375 also outruns the predicate. | **FAIL** |
| A5 | R5-F3 — forbidden-GID grammar and noglob | Whole-value grammar precedes splitting at `RP6-P0.sh:435-438`; both relevant splits are under `set -f` at lines 440-445 and 941-947. Anchored R5-F3 execution returned `R5_F3_QA_SUMMARY cases=5 pass=5 fail=0 result=PASS`. `*` STOPs in both tested working directories; `0 988` remains accepted. | PASS |
| A6 | R6-F1 — retract false claim that the child self-check contains hostile site startup | The target comment is corrected at `RP6-P0.sh:1543-1559`, and the real launch remains `-I -S -c` at line 1561. Anchored R6-F1 execution returned `R6_F1_QA_SUMMARY cases=3 pass=3 fail=0 result=PASS`: delivered launch left the marker absent; the no-`-S` mutant let the hostile `.pth` create the marker and forge version `9.9` without a STOP. The separately disclosed stale sentence in `RP6_REPAIR_R4_REPORT.md` is outside the target and was not silently claimed fixed. | PASS for the listed target-file repair |
| A7 | R6-F2 — raw `id -G` response grammar and noglob | The complete raw `gids` value is checked before expansion at `RP6-P0.sh:918-921`, and the per-item split is bracketed by `set -f`/`set +f` at lines 922-929. Anchored R6-F2 execution returned `R6_F2_QA_SUMMARY cases=10 pass=10 fail=0 result=PASS`, including the malformed `*`, `0*`, and `?` cases and the honest forbidden-root-group case. | PASS |
| A8 | R6-F3 — pin-path glob refusal and `p0_lookup` noglob | `p0_lookup` itself now disables globbing at `RP6-P0.sh:245-252`, and the pin-path gate rejects `*`, `?`, `[` at lines 521-524. The repair is incomplete because the outer parser is still `for p0_pin in $P0_TOOL_PINS` at line 488 with globbing enabled, so pathname expansion happens **before** the new gate. Executing the delivered lines 484-539 with `P0_TOOL_PINS='stat=/usr/bin/sta* python3=/opt/py312/bin/python3.12'` gave `clean cwd -> rc 3`, but a cwd containing `stat=/usr/bin/stat` gave `crafted cwd -> rc 0, PIN_PARSE_ACCEPTED count=2 … trusted=yes`. The R6-F3 harness used a cwd with no whole-token match, so its 7/7 PASS did not falsify this pre-validation expansion. | **FAIL** |
| A9 | Exit-code truthfulness — all explicit branches | Static enumeration found all ordinary error exits go through `p0_stop`/the ERR trap (rc 3) except eight object-state sites using `p0_fail` (rc 1). Two rc-1 routes can be reached without a genuine object-state observation. (1) `p0_probe_kind` sanitizes an rc-0 `stat` response before checking its shape (`RP6-P0.sh:1428-1454`); a simulated successful response `directory\nwarning_from_probe\n` became `kind=other` and produced `RC=1 P0_FAIL … venv_root_kind_unexpected`, although the probe was malformed and unevaluable. (2) `p0_assert_venv_root` does not validate a successful canonicalization response before comparing it (`1491-1498`); an rc-0 empty `readlink -f` response produced `RC=1 P0_FAIL … canonical=[]`. Both must be rc 3 STOPs. | **FAIL** |
| A10 | Comments and printed claims do not exceed the predicates | Several claims are not established. `P0_TOOL_PINS` is called optional at line 478 although R5-F1 makes a `python3` entry mandatory. Lines 354 and 375 claim builtin checking / RP0-LIB provenance that `type -t` does not establish (A4). The manager line and terminal claim say `pinned_timeout` (`1381`, `1636`), but no timeout pin is required; R5-F1 itself admits a one-entry, python-only pin set. Also GNU `timeout` rc 124 is ambiguous: `timeout 10s bash -c 'exit 124'` returned rc 124 in `elapsed_s=0`, yet lines 1355-1357 would print `manager_query_deadline_exceeded`. Finally, lines 1607 and 1636 assert site startup / `.pth` non-execution while simultaneously admitting the candidate interpreter binary’s behaviour is unattested; passing `-I -S` is established, but an arbitrary executable at that path can forge the output. | **FAIL** |
| A11 | Recorded QA transcripts reproduce literally | The six R5/R6 harness bodies pass when extracted with anchored marker lines: R5 summaries are 6/6, 4/4, 5/5; R6 summaries are 3/3, 10/10, 7/7, all rc 0. The commands actually recorded in the reports are unanchored (`/BEGIN/,/END/`) and every one returned rc 2: after the valid summary, `sed` re-entered a range at the later Markdown invocation line containing both marker strings and fed prose to Bash. Of the five legacy line-range commands, `952,1035`, `1678,2068`, and `2286,2319` reproduced their PASS summaries; stale `2545,2989` returned rc 2; stale `3353,3518` ended without the 27-case summary. Current `3379,3544` produced `C13_R4B_ARM_QA_SUMMARY cases=27 result=PASS`. Current `2553,3007` emitted `RP6_R4_D026_SUMMARY … result=PASS`, but the process retained an open descendant/output handle and did not terminate within the 60-second audit bound, so the recorded rc-0 transcript was not independently reproduced. This confirms and extends the fragility already admitted in `STATUS_RP6_P0.md:35-44`. | **FAIL** |
| A12 | Syntax | Git Bash `bash -n MTC_COMMAND_CENTER/11_TRIAGE/WPI_BLOCKS_DRAFT/RP6-P0.sh` returned rc 0. | PASS |

## Required corrections

1. **Make R5-F2 a real builtin check and make its claim honest.** Use `builtin type -t` (or an equivalently non-overridable builtin invocation) so a caller-defined `type()` cannot forge the result. Separately, either bind the function definitions to the accepted RP0-LIB identity or rename `lib=sourced` / related comments to the narrower fact actually established: required shell functions were present and exercised. Add D026 RED/GREEN cases for an overridden `type` and for an unrelated same-name shell function.

2. **Close R6-F3 before the first split.** Disable pathname expansion, preserving/restoring the caller’s prior noglob state, around the outer `for p0_pin in $P0_TOOL_PINS` parse (or parse without unquoted expansion). Keep the downstream charset gate and `p0_lookup` defense in depth. Extend R6-F3 with the exact whole-token crafted-cwd case: a directory tree matching `stat=/usr/bin/sta*`; the current bytes must be RED (`rc 0`), and repaired bytes must STOP identically in clean and crafted cwds.

3. **Adjudicate successful producer shape before any rc-1 object verdict.** In `p0_probe_kind`, reject CR/LF, non-printable, empty, and otherwise invalid rc-0 producer shapes as reasoned rc 3 before sanitizing/classifying. Apply the same status-then-shape rule to successful `readlink -f` in `p0_assert_venv_root`; empty/multiline/non-printable/unparseable output is STOP, while only a valid, complete canonical path differing from the preregistered literal may be FAIL. Add D026 cases for both reproduced arms.

4. **Narrow or enforce the printed claims.** At minimum: document that `P0_TOOL_PINS` requires `python3`; do not call the prerequisite check builtin/provenance-bound unless it is; require the timeout pin before printing `pinned_timeout` or print its actual resolution mode; do not label rc 124 uniquely as a deadline unless the wrapper can distinguish a child’s own rc 124; and express interpreter isolation as requested flags plus child-reported state unless interpreter behaviour/provenance is independently bound.

5. **Make the evidence commands literal and bounded.** Replace all line ranges with unique anchored markers whose invocation text cannot reopen the range (for example `^# R6_F1_HARNESS_BEGIN$` / `^# R6_F1_HARNESS_END$`). Give the five legacy fences the same treatment. Repair the R4 fence so all descendants close and the command itself returns within its documented bound. Re-run every exact command from a clean Git Bash and record command, rc, summary, and stderr. Existing PASS summaries are supplemental until their recorded commands exit cleanly.

Until these corrections receive D026 RED/GREEN evidence and a fresh T0 review, `RP6-P0.sh` must remain draft, not frozen and not dispatchable.
