Round 6 implementation complete. All three Claude re-audit findings repaired; only the four allowed files were touched.

## Repaired identity (REAL, computed in-session; sha256sum/wc/tr not gated)

| file | SHA-256 | bytes | CR |
|---|---|---|---|
| `RP6-P0.sh` | `75db028e76438bc88caba19b9c3b6411e5f573f7b6c2bd13c3883d24e4389570` | 93421 (was 89029, +4392) | 0 |
| `SELF_QA_RP6.md` | `15ffc11ea312fa278e7a7f5ae1a88d1d75190eab4a6ae58fdf1786ee51263ed9` | 278028 | 0 |
| `STATUS_RP6_P0.md` | `dd879eb7180e3eade417456236eb1a6f0d0370d24d768ab5b94be7ca71dc2ba9` | 34164 | 0 |
| `RP6_REPAIR_R6_REPORT.md` (new) | `aef7511335096c238d7203b6a779ecd582ac83885a2cc36c8725fbbf0b095e96` | 11065 | 0 |

Baseline `490e3e4e…` / 89029 B was verified before the first edit. All LF-only.

## What was repaired

- **F1 (carried, MEDIUM)** — retracted the false "`-S` cannot be silently deleted → named STOP" claim at every in-scope site (block comment, SELF_QA R4 prose + arm note, STATUS round-4 bullet) and replaced the cooperating `.pth` evidence with an **adversarial `.pth`** (`os._exit(0)` after forging `P0PY 9.9`). Honest claim now stated: the `sys.flags` self-check guards only accidental flag-word loss; `-S` is the load-bearing control. F1 is comment-only in the block — the `-I -S -c` launch line and all executable arms are byte-identical.
- **F2 (new, MEDIUM)** — applied the F3 pattern to `gids`: grammar-gate the raw `id -G` capture (`*[!0-9[:space:]]*`) before any expansion + `set -f` around the split. Kills all three wrongs (cwd-dependent verdict, false `form=numeric_only`, laundered whole-word intersection); `HONEST_ROOT_GROUP` still STOPs `capability_wider_than_ledger gid=0`.
- **F3 (new, LOW/MEDIUM)** — refuse `*`/`?`/`[` at the pin charset gate + run `p0_lookup`'s split under `set -f` + corrected the "deliberate and safe" comment to cover pathname expansion.

## Evidence status — PENDING (honest, not fabricated)

This GLM-5.2 session gates `bash -n` and script execution (same blocker as C13/round-5), so per the kickoff and D026/Pattern 10 the evidence is recorded **PENDING**. `SELF_QA_RP6.md` §R6 has three marker-delimited, self-contained harnesses (`R6_F1`/`R6_F2`/`R6_F3`) with expected-polarity tables, driving the auditor's own fixtures so they flip. **The Lead must execute `bash -n`, the three R6 harnesses, and re-run the five prior mandated fences against `75db028e…`** — exactly as it did for round 5.

## Two things to flag

1. **Out-of-scope site disclosed.** The audit named `RP6_REPAIR_R4_REPORT.md:88` as a fourth F1 site, but that file is outside the four-file allowlist, so I left it untouched/stale and flagged it for the Lead in `STATUS_RP6_P0.md`, `SELF_QA_RP6.md` §R6, and the R6 report.
2. **Concurrent changes that are NOT mine.** `git status` shows `remote_close_tree_wpi.sh`, `remote_extract_verify_wpi.sh`, `remote_setup_wpi.sh`, `run_p0.sh` modified, plus new `KICKOFF_TRANSPORT_REPAIR_R4.md` / `TRANSPORT_R4_RUN_2026-08-10.log` — these appeared during my session from a parallel transport-R4 workstream. I never opened or edited them; my four files are the only ones I touched.

Per the kickoff's strict "touch only four files," I did **not** update `GLOBAL_HANDOFF.md` — you or the Lead should log the round-6 completion there. Nothing was committed (no commit was requested).
