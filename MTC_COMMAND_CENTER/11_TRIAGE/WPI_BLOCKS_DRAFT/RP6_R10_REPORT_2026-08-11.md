# RP6-P0 round 10 — repair report (rounds 10a + 10b), four Codex R9 findings

**Verdict on the scope: all four findings UPHELD, all four closed, every closure
executed.** No finding is contested and nothing is recorded as a non-repair.

| field | value |
|---|---|
| block | `WPI_BLOCKS_DRAFT/RP6-P0.sh` |
| audit under repair | `RP6_CODEX_T0_AUDIT_R9_2026-08-11.md` — REQUEST_CHANGES, 4 findings |
| tier | **T0** (host/execution-domain preflight) |
| auditor of record | Codex `gpt-5.6-sol`, xhigh — re-audit pending |
| implementer, round 10a | Claude Pro — **died on its weekly cap mid-round** |
| implementer, round 10b | Claude Max, `claude-opus-5`, effort xhigh (this report) |
| entry bytes (round 9) | `08e0a93562bb04f4f78bac77d973a26da5b609aa305491d3bfa51743adbcf10c`, 104683 B, commit `9bc25721` |
| exit bytes (round 10) | `a090ae736cbecd9973e8ae948b052504b21cbe8b61602f4b5ac592394fad0617`, 107252 B |
| `bash -n` | rc 0 (GNU bash 5.2.37(1)-release, x86_64-pc-msys) |
| CR bytes | 0 in every written file |
| commit | **none** — the Lead commits |

## Why this report has two authors

Round 10 was dispatched to Claude Pro, which hit its **weekly** cap partway
through and stopped. It left `RP6-P0.sh`, `SELF_QA_RP6.md` and the preregistration
draft modified, with **no report, no status update, and no executed evidence** —
twelve `@…@` placeholders sat where the transcripts belong. The Lead preserved
that as commit `da78d99c`, labelled **round 10a, explicitly not round 10**, and
dispatched round 10b to Claude Max under a standing instruction:

> **Confirm or complete, never assume.** For each of F1–F4: verify against the
> partial bytes with executed evidence (never credit a comment or an existing diff
> as proof). Where the repair is present and provable, record the proof. Where
> absent or broken, repair from the partial and prove it.

This report states, per finding, **what the partial already contained** and **what
round 10b added**. That split is the point of the round, so it is never blurred.

## What round 10b found on entry

The partial was **materially complete on the block side and empty on the evidence
side**. Both halves were established by execution, not by reading the diff:

| element | state on entry | established by |
|---|---|---|
| F3 repair in `RP6-P0.sh` | present, lines 1600-1612 | ran `R10_F3`; read the bytes |
| F4 repair in `RP6-P0.sh` | present, line 676 | ran `R10_F4`; read the bytes |
| prereg §8.1.1 grammar | present, 89 forms | ran `R10_GRAMMAR` against the live draft |
| `R10_GRAMMAR` / `R10_F3` / `R10_F4` fences | present and passing | ran all three |
| **every published-command transcript** | **absent — 12 placeholders** | `grep '@[A-Z0-9_]\{3,\}@'` |
| **`RP6_R10_REPORT_2026-08-11.md`** | **absent** | — |
| **`STATUS_RP6_P0.md`** | **absent** (a listed deliverable) | `git diff e46fbdfc da78d99c` |

Round 10a's own header claimed "every transcript is real captured output". At the
moment it was written that was **not true**, and it is withdrawn as written in
`SELF_QA_RP6.md`. It is true now, because round 10b ran everything.

The block change is confined to two hunks — `git diff 9bc25721 da78d99c --
RP6-P0.sh` reports exactly `@@ -648 @@` (F4) and `@@ -1577 @@` (F3), 55 insertions
and 18 deletions — which matches the "F3 and F4 only" claim.

---

# F1 (HIGH) — the published command did not run the harness

**Disposition: UPHELD. Closed. Reproduced independently before repair was credited.**

## The finding, reproduced by round 10b against the round-9 blob

The round-9 RED command ended `... | bash --noprofile --norc "$mutant"`. With a
filename after the options, Bash executes **that file** and discards piped stdin.
The round-9 bytes and the round-8 mutant blob were materialised with
`git cat-file blob` (never `git checkout`) and the documented command run verbatim:

```text
DOCUMENTED_RED_RC=3
P0_SECTION header candidate=2ce41e34bceb599d80af24c5c33d835820ec321b block=RP6-P0 stage=p0
P0_SECTION prerequisites
P0_STOP reason=rp0_lib_not_sourced predicate=rp0_require_safe_component detail=not_a_shell_function
R9_GRAMMAR_SUMMARY count in output = 0
```

That is the audit's transcript, character for character. The command ran the RP6
block, not the harness.

## What the partial already contained

- The `-s --` repair on the `R9_GRAMMAR` invocation, so the harness runs and the
  mutant is its `$1`.
- Anchored `^# X_HARNESS_BEGIN$` / `^# X_HARNESS_END$` markers on the six R5/R6
  fences and the R9 pair, which round 7 had applied to the other fences and never
  extended to these.
- An own-status guard (`[ "$COUNTER" = 0 ] || exit 1`) appended to the ten fences
  that previously printed `result=FAIL` and exited 0.
- The §1b/§1c prose describing all of the above.

## What round 10b added

**1. The sweep itself — the deliverable F1 actually names.** Every published
command run **verbatim from a clean shell** *and* **as the Lead-style extraction
to a file**, per the addendum, with disagreement between the two forms treated as
a finding in itself. `BASH_ENV` and `ENV` were confirmed unset so no startup file
could inject into either form.

**Result: 19 published commands plus the RED twin — all twenty agree on rc and on
summary line.** The published `R9_GRAMMAR` command emits `R9_GRAMMAR_SUMMARY` in
its real recorded output, which is what the addendum required. Full per-command
evidence is `SELF_QA_RP6.md` §1e; the closing rows:

```text
$ sed -n '/^# R9_GRAMMAR_HARNESS_BEGIN$/,/^# R9_GRAMMAR_HARNESS_END$/p' SELF_QA_RP6.md | bash --noprofile --norc -s -- RP6-P0.sh
    verbatim_rc=0   extracted_rc=0   forms_agree=yes
    R9_GRAMMAR_SUMMARY cases=5 pass=5 fail=0 result=PASS

$ R9_GRAMMAR RED twin (mutant construction as published)
    verbatim_rc=1   extracted_rc=1   forms_agree=yes
    R9_GRAMMAR_SUMMARY cases=5 pass=2 fail=3 result=FAIL
```

The RED twin exits **1**, not 0. The own-status defect the audit named is closed
on the fence it named.

**2. Falsification of the own-status guards, because a grep is not evidence.**
Round 10a asserted ten fences had been given a guard. Confirming the text would
establish nothing about behaviour — precisely the error class this package keeps
re-finding. Each fence was therefore extracted, its guard line located by exact
text, its fail counter forced nonzero on the line immediately before the guard,
and the mutant run. The injection count is asserted so a mis-targeted injection
cannot pass:

```text
Each fence is extracted, its own-status guard line is located by exact text,
the fail counter is forced nonzero on the line IMMEDIATELY BEFORE that guard,
and the mutant is run. A guard that exists and is reached must exit nonzero.

R5_F1       guard_at_line=59   injections=1  forced R5_F1_FAIL=7 -> rc=1  GUARD_HOLDS
R5_F2       guard_at_line=71   injections=1  forced R5_F2_FAIL=7 -> rc=1  GUARD_HOLDS
R5_F3       guard_at_line=63   injections=1  forced R5_F3_FAIL=7 -> rc=1  GUARD_HOLDS
R6_F1       guard_at_line=75   injections=1  forced R6_F1_FAIL=7 -> rc=1  GUARD_HOLDS
R6_F2       guard_at_line=107  injections=1  forced R6_F2_FAIL=7 -> rc=1  GUARD_HOLDS
R6_F3       guard_at_line=81   injections=1  forced R6_F3_FAIL=7 -> rc=1  GUARD_HOLDS
R7_F2       guard_at_line=43   injections=1  forced R7_F2_BAD=7 -> rc=1  GUARD_HOLDS
R7_F3       guard_at_line=48   injections=1  forced R7_F3_BAD=7 -> rc=1  GUARD_HOLDS
R7_C3       guard_at_line=117  injections=1  forced R7_C3_BAD=7 -> rc=1  GUARD_HOLDS
R9_GRAMMAR  guard_at_line=31   injections=1  forced R9_FAIL=7 -> rc=1  GUARD_HOLDS
```

**3. Reproduction of the other six defects** round 10a claimed, against the
round-9 published patterns (which had no `# ` prefix, so the range reopened on the
invocation line's own text):

```text
unanchored-range over-extraction, round-9 published patterns:
  R5_F1 1368   R5_F2 1287   R5_F3 1196   R6_F1 986   R6_F2 891   R6_F3 757
  -> all six reproduce round 10a's line counts exactly

R9_GRAMMAR GREEN, documented command verbatim, both files present:
  rc=124 under a 30 s bound; unbounded self-recursion confirmed
```

**4. One correction to round 10a's evidence.** Round 10a published "57
`R9_GRAMMAR_SUMMARY` lines" for the GREEN self-recursion. Round 10b measured
**172** on the same bytes under the same 30-second bound. The rc (124) and the
mechanism reproduce; the iteration count is a wall-clock artefact of an unbounded
loop, not a reproducible measurement, and publishing it as one repeats the
round's own lesson in miniature. §1b now states the rc and the mechanism and
marks the count machine-dependent, recording both observations.

**5. Marker uniqueness, verified rather than asserted.** All 18 fence marker pairs
are unique whole lines (`grep -cxF` = 1 for every BEGIN and every END), so no
range can reopen on prose or on invocation text.

---

# F2 (HIGH) — the declared and executable grammars were not closed

**Disposition: UPHELD. Closed. Verified against the live working-tree draft.**

## What the partial already contained

- **Preregistration §8.1.1**, a new subsection carrying the complete P0 result
  grammar as a machine-readable declaration between `# P0_RESULT_GRAMMAR_BEGIN` /
  `# P0_RESULT_GRAMMAR_END`: one line per form, `<site_count> <PREFIX> <reason>
  <field>={<value-class>,…}`, fields in emission order.
- **Four §8.1 row corrections** where the draft, not the block, was wrong.
- **One new §8.1 paragraph** explaining why P0 has `P0_FAIL` forms at all.
- The **`R10_GRAMMAR` fence**, which re-derives the grammar from the block bytes
  and `diff`s it against the declaration, closing both directions at once.

## What round 10b added

Execution, and an accurate account of the edits.

```text
R10_GRAMMAR_DECLARED forms=89 sites=161 source=../WPI_PREREG_DRAFT_ROUND1/WPI_PREREGISTRATION_DRAFT.md
R10_GRAMMAR_DERIVED  forms=89 sites=161 source=RP6-P0.sh
ASSERT_MET declaration_present forms=89
ASSERT_MET grammar_closed declared==derived forms=89 sites=161
ASSERT_MET site_total_independent expected=161 derived=161 wrapper_sites=160 direct_sites=1
ASSERT_MET no_unparseable_emitter
ASSERT_MET err_trap_printf_arguments=rc,BASH_LINENO0,BASH_COMMAND
ASSERT_MET mutant=relabel_f4_site killed delta_lines=3
ASSERT_MET mutant=drop_field killed delta_lines=2
ASSERT_MET mutant=retoken_detail killed delta_lines=2
ASSERT_MET mutant=new_emitter killed delta_lines=1
ASSERT_MET mutant=declaration_line_removed killed
R10_GRAMMAR_SUMMARY cases=10 pass=10 fail=0 result=PASS
PUBLISHED_COMMAND_RC=0
```

89 declared forms, 89 derived, **161 emit sites on both sides**, and the site
total independently cross-checked against a literal grep (160 wrapper + 1 direct
ERR-trap emitter) so a derivation that silently dropped sites cannot pass by
agreeing with a declaration built the same way. All five mutants — a relabelled
reason, a dropped field, a changed literal `detail=` token, a brand-new undeclared
emitter, and a removed declaration line — are killed.

This also settles the audit's central F2 example. The generic
`input_pin_freeze_unfilled` form is now declared for all twelve tool/constant
pairs, and round 9's attempt to close it by asserting the emitter's own variable
name (`name=$P0_FROZEN_CONST_NAME`) is **withdrawn** in both the draft and the QA
file. The audit's site count is confirmed and the round-9 report's is withdrawn:
**159 on the entry bytes (158 wrapper + 1 ERR trap), 161 on the round-10 bytes**
after F3 adds two.

### Every preregistration edit, enumerated

Required by the scope fence. Established by diffing the round-10a commit against
its own parent, not from the QA file's summary:

| # | location | edit |
|---|---|---|
| 1 | §8.1 row 1 | `input_pin_freeze_unfilled` declared **generic over all twelve tools**; `input_pin_count_unexpected` gains `detail=exactly_one_frozen_pin_per_preregistered_tool`; `tool_pin_unpinned` gains `detail=every_tool_requires_a_frozen_pin`. Round 9's variable-name closure withdrawn |
| 2 | §8.1 row 3 | `identity_unexpected` / `state_account_resolution_unexpected` declare `expected_numeric=<P0_STATE_UID>:<P0_STATE_GID>` — the operator-supplied preregistered input — instead of the literal `999:988` the block never emits. Residual named in-place |
| 3 | §8.1 row 8 | `execution_domain_unattested` gains `detail=<d>`; the `field=`-only form was never emitted by any of the 34 sites |
| 4 | §8.1 row 9 | the manager's `budget_s=`/`elapsed_s=`/`text=` fields declared for **every** nonzero wrapper status, not rc 124 alone |
| 5 | §8.1, new paragraph | why P0 has eight `P0_FAIL` forms: the venv root and the interpreter are objects P0 can observe completely, so deviance there is rc 1, while an unreadable producer answer is rc 3 |
| 6 | §8.1.1, new subsection | the exhaustive declaration (89 forms / 161 sites), its derivation rule, and an explicit statement of what a static source grammar does **not** establish |

Round 10a's own summary said "six narrow §8.1 edits". The diff is four row
rewrites plus one added paragraph plus the new subsection; the QA file is
corrected. `§8.2` rows 20-22 also differ from the round-9 blob, but they belong to
the **RP7 round-8** session (commit `bb8546e6`) and are not this round's.

### A note on the parallel session

`WPI_PREREG_DRAFT_ROUND1/` carries uncommitted edits from the parallel Max session
that owns `pathscope_prover.py`. Those edits sit at draft lines 181-709; §8.1 and
§8.1.1 occupy 774-967 — **no overlap**. `R10_GRAMMAR` was run against the
working-tree draft as it actually stands, not a private copy, so closure is
verified against the bytes the Lead will commit. Round 10b wrote nothing in that
directory.

---

# F3 (HIGH) — a malformed followed-target response reached rc 1

**Disposition: UPHELD. Closed.**

## What the partial already contained

The repair, at `RP6-P0.sh` lines 1600-1612. Correction 3 (round 7) gated the shape
of the **leaf** rc-0 `stat -c %F` response but not the response read after an
allowed interpreter symlink is **followed**; that second capture was sanitised
first and classified second, so `regular file\nwarning_from_follow_probe\n`
folded its newlines to spaces, matched no recognised kind, fell through to
`other`, and the caller turned it into
`P0_FAIL reason=interpreter_target_kind_unexpected kind=other` at rc 1.

`p0_probe_kind` now adjudicates the **raw** followed-target response for empty,
multi-line and non-printable shape **before** `P0_FKIND` is assigned, identically
to the leaf gate. `link_target_probe_empty` moves to the raw check;
`link_target_probe_multiline` and `link_target_probe_nonprintable` are new. The
partial also contained the `R10_F3` fence.

## What round 10b added

Execution, and confirmation by reading the bytes that the gate is genuinely ahead
of the assignment rather than a comment claiming so.

```text
CASE_OK GREEN_multiline_rc got=[3]
CASE_OK GREEN_multiline_exact_line got=[P0_STOP reason=link_target_probe_multiline path=/fixture/python rc=0 detail=regular file warning_from_follow_probe]
CASE_OK GREEN_nonprintable_rc got=[3]
CASE_OK GREEN_nonprintable_exact_line got=[P0_STOP reason=link_target_probe_nonprintable path=/fixture/python rc=0 detail=[non_printable_detail_suppressed]]
CASE_OK GREEN_empty_rc got=[3]
CASE_OK GREEN_empty_exact_line got=[P0_STOP reason=link_target_probe_empty path=/fixture/python rc=0]
CASE_OK GREEN_regression_directory_target_rc got=[1]
CASE_OK GREEN_regression_directory_exact_line got=[P0_FAIL reason=interpreter_target_kind_unexpected kind=dir path=/fixture/python expected=regular]
CASE_OK GREEN_regression_regular_target_rc got=[0]
CASE_OK GREEN_regression_regular_target_binding got=[KIND=link_live FKIND=regular]
CASE_OK RED_prefix_multiline_reaches_rc1 got=[1]
CASE_OK RED_prefix_multiline_exact_line got=[P0_FAIL reason=interpreter_target_kind_unexpected kind=other path=/fixture/python expected=regular]
CASE_OK RED_prefix_nonprintable_reaches_rc1 got=[1]
CASE_OK RED_prefix_nonprintable_exact_line got=[P0_FAIL reason=interpreter_target_kind_unexpected kind=other path=/fixture/python expected=regular]
R10_F3_QA_SUMMARY cases=14 pass=14 fail=0 result=PASS
PUBLISHED_COMMAND_RC=0
```

The GREEN side drives the **real** round-10 functions, extracted from the block by
function anchor and called through the real `p0_assert_interpreter_executable`, so
the verdict under test is the block's own. The RED side is a faithful pre-round-10
replica and reproduces the audit's exact rc-1 line. Every result assertion is an
**exact whole line**, because the defect class this package keeps re-finding is a
missing field that a substring assertion cannot see.

Both regression directions hold, which is what makes this a repair rather than a
loosened expectation: a **complete** observation of a non-regular target
(`kind=dir`) is still `P0_FAIL` at rc 1, and an honest regular target still binds
at rc 0. The repair moves malformed answers off rc 1; it does not move host-state
divergence off rc 1.

---

# F4 (MEDIUM) — the round-9b relabelling was convenient, not established

**Disposition: UPHELD, and the round-9 claim is WITHDRAWN. Closed.**

## What the partial already contained

Round 9b had relabelled line 668 `input_pin_omitted tool=python3` and offered a
static grep as evidence. The audit's objection stood on both counts: the predicate
is `P0_TRUSTED_PYTHON_BOUND != yes`, which is not an observation of omission, and
a grep is not D026 evidence for a branch nothing can reach.

The partial takes the audit's second option — declare it as an internal-binding
invariant naming the predicate it actually tests — at `RP6-P0.sh` line 676:

```text
p0_stop "internal_invariant_unmet invariant=trusted_python_pin_bound predicate=P0_TRUSTED_PYTHON_BOUND_eq_yes observed=$P0_TRUSTED_PYTHON_BOUND detail=an_upstream_input_gate_stopped_detecting_its_condition"
```

and adds the `R10_F4` fence.

## What round 10b added

Execution of the falsification the audit demanded.

```text
CASE_OK ARM_BUILD_COMPLETE got=[built]
CASE_OK A1_omitted_python3_rc got=[3]
CASE_OK A1_consumed_by_the_omission_loop got=[P0_STOP reason=input_pin_omitted tool=python3 detail=every_preregistered_tool_requires_one_frozen_pin]
CASE_OK A2_unfilled_placeholder_rc got=[3]
CASE_OK A2_consumed_by_the_freeze_gate got=[P0_STOP reason=input_pin_freeze_unfilled tool=python3 name=P0_FIXED_TRUSTED_PYTHON detail=deploy_channel_value_never_derived_here]
CASE_OK A3_disagreeing_pin_rc got=[3]
CASE_OK A3_consumed_by_the_frozen_python_gate got=[P0_STOP reason=input_pin_not_frozen_trusted_python tool=python3 pinned=/fixture/bin/python3.other frozen=/fixture/bin/python3.real]
CASE_OK A4_complete_pin_set_rc got=[0]
CASE_OK A4_binding_established got=[ARM_END bound=yes count=12]
CASE_OK A5_invariant_unreached_on_unmutated_bytes got=[unreachable]
CASE_OK B0_mutation_applied got=[applied]
CASE_OK B0_MUTANT_ARM_BUILD_COMPLETE got=[built]
CASE_OK B1_invariant_reached_rc got=[3]
CASE_OK B1_invariant_exact_line got=[P0_STOP reason=internal_invariant_unmet invariant=trusted_python_pin_bound predicate=P0_TRUSTED_PYTHON_BOUND_eq_yes observed=no detail=an_upstream_input_gate_stopped_detecting_its_condition]
CASE_OK C1_single_invariant_site got=[1]
CASE_OK C2_no_input_deficiency_label_at_the_invariant got=[0]
R10_F4_QA_SUMMARY cases=16 pass=16 fail=0 result=PASS
PUBLISHED_COMMAND_RC=0
```

The arm is the block's own top-level pin parser, sliced between unique landmark
lines, with a build-completeness guard that fails loudly if the slice references a
frozen literal the preamble does not define. Nothing is replicated.

- **A1–A3** show, by running the real parser, that every input class leaving the
  binding unset is consumed by its own upstream gate first — omission by the
  omission loop, an unfilled deploy literal by the freeze gate, a disagreeing pin
  by the frozen-python gate — each with its exact emitted line.
- **A4** shows a complete valid pin set reaching the end of the parser at rc 0
  with the binding set.
- **A5** concludes unreachability on the unmutated bytes **from those runs**, not
  from inspection.
- **B** neutralises the two consuming gates and reaches the line, recording its
  exact emitted result line and rc 3 — the executable falsification a static grep
  could never supply.
- **C** confirms the site is singular and carries no input-deficiency label.

The `R9_GRAMMAR` fence's fifth case, which asserted the withdrawn
`input_pin_omitted tool=python3` token, now tracks the internal-invariant reason.
That is a fixture following a preregistered grammar that changed in the direction
the audit required — not an expectation lowered to make a test pass.

---

# The thread the kickoff drew through F1 and F4

Both findings were the same failure: **evidence that looks conclusive and
establishes nothing.** F1 produced output from the wrong program; F4 labelled a
branch nobody could reach.

Round 10b hit the same class twice in its own work, and both are recorded rather
than quietly fixed:

1. The first guard-falsification harness used a mis-escaped `awk` regex. The
   injection landed ahead of each counter's initialisation and produced ten
   confident `GUARD_ABSENT` results. **The generator was wrong, not the fences.**
   It was caught by asserting *where* the injection landed — an assertion now
   permanently in that harness.
2. The first command-table generator ran with the wrong working directory. Every
   command failed identically, and the table therefore reported `forms_agree=yes`
   on twenty rows of nothing. A "both forms agree" check agrees perfectly when
   both forms are broken. The generator now aborts if the subject files are absent
   and if any command yields an empty summary line.

Neither reached the QA file. They are reported because the round's subject is
exactly this failure mode, and a report that showed only the clean run would be
making the same mistake it is adjudicating.

---

# Deliverables and scope

**Written this round (10a + 10b):**

- `WPI_BLOCKS_DRAFT/RP6-P0.sh` — F3 and F4 only (10a; **unchanged in 10b**)
- `WPI_BLOCKS_DRAFT/SELF_QA_RP6.md` — round-10 section (10a), all twelve
  transcripts, six corrections and the round-10B section (10b)
- `WPI_BLOCKS_DRAFT/STATUS_RP6_P0.md` — **10b only**; 10a never wrote it
- `WPI_BLOCKS_DRAFT/RP6_R10_REPORT_2026-08-11.md` — this file, new (10b)
- `WPI_PREREG_DRAFT_ROUND1/WPI_PREREGISTRATION_DRAFT.md` — six edits enumerated
  under F2 (10a; **untouched in 10b**)

**Not touched:** `pathscope_prover.py`, `PATHSCOPE_*`, `SEC10*`, anything else in
`WPI_PREREG_DRAFT_ROUND1/`, and every `RP7-*` / `TRANSPORT_*` / `remote_*` /
`run_*` artefact owned by other sessions. No `git checkout`, `git reset` or
`git stash` was run on any tracked file. **Nothing was committed — the Lead
commits.** No host was contacted and no network command was run.

## Final measurements, executed

```text
RP6-P0.sh    sha256=a090ae736cbecd9973e8ae948b052504b21cbe8b61602f4b5ac592394fad0617
             bytes=107252   CR=0   bash -n rc=0
emit sites   161 = 160 p0_stop/p0_fail wrapper sites + 1 direct ERR-trap printf
declared     89 forms in preregistration §8.1.1, closed against the block bytes
CR bytes     RP6-P0.sh=0  SELF_QA_RP6.md=0  STATUS_RP6_P0.md=0
             RP6_R10_REPORT_2026-08-11.md=0  WPI_PREREGISTRATION_DRAFT.md=0
```

`shellcheck` is not installed in this environment and was not run.

## Explicit local limits

The complete P0 block still was not run end to end: it needs the accepted RP0
library and bootstrap, Linux `/proc` namespace objects, the preregistered per-SHA
venv, `getent`/`systemctl`/`ss`/`curl` on the host, and a reachable system manager
— none of which exist in this Git Bash environment. All **seventeen** frozen
deploy-channel literals (twelve tool pins plus five namespace/root-mount
attestation values) remain `<PIN-AT-FREEZE>`, so no end-to-end `P0 PASS` is
reachable and nothing here is dispatchable.

Three limits bound what this round establishes, and they are carried into the
re-audit as named residuals rather than closed:

1. **`R10_GRAMMAR` is a static source grammar.** It constrains prefixes, reasons,
   field names, field order, every literal value and every literal `detail=`
   token. It does not constrain what a `<name>` class evaluates to at run time.
   The runtime shape of the four forms this round touches is evidenced by `R10_F3`
   and `R10_F4`, which assert exact whole lines; the other 85 forms are evidenced
   only where an existing fence already drives them.
2. **The `P0_STATE_UID` / `P0_STATE_GID` / `P0_EXPECT_UID` input-integrity
   residual is named, not closed.** The block constrains these to positive
   decimals and cannot establish that the prelude carried the preregistered
   numerics; §2 preregisters no numeric for `P0_EXPECT_UID` at all. Closing it
   would mean freezing new literals into the block — a new control this round was
   not asked to add and did not add. Freeze-gate/owner band.
3. **`R10_F4`'s reachability result is about *this* control flow.** It shows the
   invariant is unreachable while the three upstream gates stand, and reachable
   when the two consuming gates are removed. It does not prove no future edit
   could reach it by another route — which is exactly why the assertion is kept
   rather than deleted.

## Recommendation to the Lead

Reproduce independently before accepting. The two commands that carry this round
are:

```text
sed -n '/^# R10_GRAMMAR_HARNESS_BEGIN$/,/^# R10_GRAMMAR_HARNESS_END$/p' SELF_QA_RP6.md | bash --noprofile --norc
sed -n '/^# R9_GRAMMAR_HARNESS_BEGIN$/,/^# R9_GRAMMAR_HARNESS_END$/p' SELF_QA_RP6.md | bash --noprofile --norc -s -- RP6-P0.sh
```

Both must print their summary line with `result=PASS` at rc 0, and the published
RED twin must print `result=FAIL` at rc **1**. Status stays
**ROUND-10-REPAIRED-PENDING-T0-REAUDIT** until Codex `gpt-5.6-sol` re-audits these
bytes; RP6-P0 is not re-submitted by this report, and the block is not frozen,
accepted, dispatchable, or authorised for host execution.
