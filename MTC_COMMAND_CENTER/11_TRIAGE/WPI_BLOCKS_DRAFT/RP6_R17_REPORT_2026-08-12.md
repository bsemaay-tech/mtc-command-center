# RP6-P0 round 17 - RP6-11 dynamic-target census closure

Author: Codex **`gpt-5.5`**, xhigh, round-17 implementer
*(Lead correction 2026-08-12 ~17:30: this line as authored said `gpt-5.6-sol`. The dispatch
ran on `-Account free`, whose session header records `model: gpt-5.5` — see
`RP6_R17_CODEX_RUN_2026-08-12.log`. Corrected because implementer identity is load-bearing.)*  
Date: 2026-08-12  
Audit tier: T0, pending different-flagship audit by Claude Pro `claude-opus-5` xhigh  
Scope: `SELF_QA_RP6.md`, `STATUS_RP6_P0.md`, this report only

No host, SSH, network, deployment, backtest, broker, trading, or Git mutation
occurred. `RP6-P0.sh` was not edited.

## Identity

`RP6-P0.sh` identity was re-derived before and after the R17 fence:

```text
bytes=110817
sha256=5132bacde24cbff8c9267a82f6ac6e3b0cebe3d3c82b092518efac1245103330
```

This matches the kickoff identity exactly.

## Finding Addressed

RP6-11 was open because the round-16 acceptance did not include an executed
RED/GREEN pair for a dynamically resolved inventory-mutation target. The round-16
Codex PASS-WITH-NITS was scoped to constructs in the published detection
vocabulary; it did not make missing D026 evidence appear.

Important source-state note: the exact committed R16 fence already refuses
`eval`, `source`, and `.` as `UNMODELED kind=indirect_execution_builtin:*`. The
kickoff/GLM read said the current R16 fence would admit bare `eval`; that is not
true of the checked-in `SELF_QA_RP6.md`. Round 17 therefore uses the D026-allowed
equivalent falsification path: it removes only that indirect-execution refusal
from a temporary extracted R16 fence and proves that the weakened fence certifies
the dynamic-target mutants clean. R17 then refuses the same mutants with explicit
`UNMODELED` records.

## Repair Construction

R17 does not add `eval` to a mutating-builtin list. It adds a closed effect model
over the tokenizer stream:

- A bare command word is accepted only if it is a span-dispositioned block
  function, `rp0_require_safe_component`, a builtin with no named-variable target
  surface, or a variable-mutating builtin whose target grammar is already modeled
  by `VARTARGET` / `dynamic_variable_target`.
- `eval`, `source`, `.`, and any future observed bare word outside that model are
  opaque shell-language execution surfaces and fail with an unmodeled record.
- `dynamic_targets` is now measured as
  `dynamic_variable_target + indirect_execution_builtin + effect_model_unmodeled`.

Clean-byte measured count:

```text
variable_targets=113 inventory_targets=0 dynamic_targets=0 dynamic_variable_targets=0 opaque_mutators=0 effect_unmodeled=0 nonfunction_bare=10
```

Pass-format audit result: six literal-zero fields across three R16 success lines
could be read as measurements (`expand_aliases_enabled=0`, the three shadow
counts, `inventory_targets=0`, `dynamic_targets=0`). R17 publishes measured
fields or omits those details; `r17_literal_zero_measurements=0`.

## Published Command

Run from `MTC_COMMAND_CENTER/11_TRIAGE/WPI_BLOCKS_DRAFT`:

```bash
sed -n '/^# R17_DYNAMIC_TARGETS_HARNESS_BEGIN$/,/^# R17_DYNAMIC_TARGETS_HARNESS_END$/p' SELF_QA_RP6.md | bash --noprofile --norc
```

## Executed Evidence

The published command returned rc 0:

```text
R17_ASSERT_MET carried_r16_grammar cases=50 pass=50 fail=0 rc=0
R17_ASSERT_MET r17_dynamic_targets_measured variable_targets=113 inventory_targets=0 dynamic_targets=0 dynamic_variable_targets=0 opaque_mutators=0 effect_unmodeled=0 nonfunction_bare=10
R17_ASSERT_MET D026_RED_WEAKENED_R16 mutant=eval rc=0 summary=PASS
R17_ASSERT_MET D026_GREEN_R17 mutant=eval refused rc=1 report=[variable_targets=113 inventory_targets=0 dynamic_targets=1 dynamic_variable_targets=0 opaque_mutators=1 effect_unmodeled=0 nonfunction_bare=10]
UNMODELED kind=indirect_execution_builtin:eval line=1567 raw=[eval]
R17_ASSERT_MET D026_RED_WEAKENED_R16 mutant=dot_source rc=0 summary=PASS
R17_ASSERT_MET D026_GREEN_R17 mutant=dot_source refused rc=1 report=[variable_targets=113 inventory_targets=0 dynamic_targets=1 dynamic_variable_targets=0 opaque_mutators=1 effect_unmodeled=0 nonfunction_bare=10]
UNMODELED kind=indirect_execution_builtin:. line=1567 raw=[.]
R17_DYNAMIC_TARGETS_SUMMARY cases=15 pass=15 fail=0 result=PASS
```

The two class members are structurally different:

- `eval`: builds the protected inventory variable name by concatenation and
  assigns through `eval`.
- `dot_source`: writes a constructed assignment into a runtime path and sources it
  through `.`.

Both are certified by the weakened R16 control and refused by R17.

## Residuals

R17 remains a static source fence. It refuses opaque shell-language execution and
unmodeled command effects; it does not resolve runtime text or external sourced
file contents. That is intentional: unresolved mutation target surfaces stop the
verdict instead of being guessed.

The block is still not frozen, accepted, dispatchable, or authorised for host
execution. The next required step is the T0 different-flagship audit.
