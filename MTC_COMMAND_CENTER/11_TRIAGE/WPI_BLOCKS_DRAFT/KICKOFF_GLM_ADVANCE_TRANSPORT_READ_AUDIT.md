# KICKOFF — GLM-5.2 ADVANCE read-audit: the WP-I transport set

You are GLM-5.2 via the Z.AI route. **You are running UNATTENDED — do not ask for approval, do
not write a plan and stop. Execute directly and write your verdict file.** Working dir
`C:\LAB\Tradingview_LAB_CLEAN`. Read-only: create nothing except your verdict file, no git
mutation, no host, no network, no WSL fixture execution.

## What this dispatch is, and honestly is not
An **ADVANCE, SUPPLEMENTAL read-audit**, not the second-flagship audit. Transport holds a Codex
flagship acceptance (`TRANSPORT_CODEX_R6B_CONFIRM`); the required second flagship is Claude
`claude-opus-5`, running tonight. **Your verdict cannot close that slot and must not claim to.**
Prefix your verdict line `ADVANCE-SUPPLEMENTAL`. If you cannot execute the fixture harness, mark
those steps `PENDING-LEAD-EXECUTION` and keep your opinion source-level — **never fabricate a
green run.**

## Bytes
The nine-file transport set in `WPI_BLOCKS_DRAFT` — exact current identities are tabulated in
`AUDIT2_READINESS_PACKAGE/AUDIT2_ACCEPTANCE_MATRIX_2026-08-12.md`. Re-derive at least
`transport_runner.ps1`, `TRANSPORT_PLAN.tsv` and `remote_close_tree_wpi.sh` and confirm they
match. Evidence: `STATUS_TRANSPORT.md`, `SELF_QA_TRANSPORT.md`, and the Codex audit chain.

## Binding context you must respect
**F1 (the outer SSH account-shell boundary) is honestly OPEN and OWNER-RATIFIED 2026-08-12 as
ACCEPT-WITH-DISCLOSURE** — an inherent SSH-trust-model limit, not a freeze blocker. Do not demand
its closure. DO verify that no text presents the cleared inner-child `env -i` domain as an
end-to-end F1 closure, and that the disclosure is carried at every site that could mislead.

## Priority reading targets
1. **First-mismatch semantics and per-branch cleanup prerequisites** (07←04, 08←05, 09←07,
   10←08, 11←07+09, 12←08+10). An unrelated branch's failure must never demote a genuine marked
   rc 1 to not-evaluable. Try to construct a case where it does.
2. **Per-operation provenance binding** — an `ssh_stdin` operation's marker must come from the
   family belonging to the stdin artifact that row sends, not from a global union of every
   program's prefixes. Look for a path back to the global union.
3. **The close-script contract.** Today's freeze-input ledger
   (`WPI_FREEZE_INPUT_LEDGER_2026-08-12.md` §"Blocker 8") found that plan rows 07/08 and
   `remote_close_tree_wpi.sh` now AGREE on the three-argument form (`EV_DIR RUNID WORK_ROOT`),
   that execution currently stops earlier on unfilled `EXPECT_UID`/`EXPECT_GID`, and that the
   residual contradiction is documentary (a stale two-arg record in
   `RUNID_MINTING_REVIEW_CODEX_2026-08-11.md:167`). **Independently verify that reading** — it
   was produced by a weaker model (gpt-5.5) and has not yet been cross-checked.
4. **Marker discipline** — `<ALLOCATE-AT-DISPATCH>`/`<PIN-AT-FREEZE>` stay literal and the
   preflight marker gate STOPs the runner on the first unfilled constant.

## Verdict
Grammar: `PASS / PASS-WITH-NITS / REQUEST_CHANGES / BLOCK`, prefixed `ADVANCE-SUPPLEMENTAL`.
State plainly what you executed and what you did not.

Write ONE new file: `TRANSPORT_GLM_ADVANCE_READ_AUDIT_2026-08-12.md` in `WPI_BLOCKS_DRAFT`.
