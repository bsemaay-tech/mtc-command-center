# KICKOFF — RP6 round 19: token-layer assigning-effect model (the real inversion)

Codex `-Account free`, xhigh. Working dir `C:\LAB\Tradingview_LAB_CLEAN`. No git mutation. No
sub-delegation. State your session-header model in the report. **Block `RP6-P0.sh` stays
byte-identical: 110817 B / `5132bacd…` — harness/evidence work only.**

## Work order

`MTC_COMMAND_CENTER/11_TRIAGE/WPI_BLOCKS_DRAFT/RP6_CLAUDE_T0_2NDFLAG_AUDIT_2026-08-12.md`
(fresh Claude second-flagship verdict on the r18 bytes: REQUEST_CHANGES). Its "Minimum
required repairs" are the contract. Read it in full first. The Lead endorses its central
diagnosis: r16→r17→r18 each extended a command-word enumeration and each reopened; **the
repair is moving the effect model to the token layer**, so any token whose raw text carries an
assignment-capable expansion gets a terminal disposition regardless of the command word.

1. **R1 (HIGH)** — token-layer terminal dispositions for assignment-capable word expansions:
   `${name=…}`, `${name:=…}`, indirect `${!name…}` forms, and arithmetic assignment operators
   inside `$(( ))`, `(( ))`, array subscripts, and `let`. Executed D026 RED against the
   delivered r18 bytes (must include the indirect runtime-named form) and GREEN after repair.
2. **R2 (HIGH)** — the `waittarget` grammar must live in the SHIPPED policy inside
   `SELF_QA_RP6.md` so the document's own published R17 command refuses the wait mutant with
   no harness-side injection. Re-run the published command and paste the result. Correct
   `STATUS_RP6_P0.md:19-33` and `SELF_QA_RP6.md:16527-16531` (they claim a closure the
   delivered bytes did not have) — locate by quoted content.
3. **R3 (MEDIUM)** — widen the measured pass-format scan grammar per the verdict (it detects
   2 of 8 tested literal shapes; the verdict's finding section lists the missed shapes). Add
   executed falsification mutants for at least the two-level indirection shape.
4. Adopt the verdict's nits where cheap; list each as done/skipped.

## Rule 8 boundary — binding, now sharpened

If after building the token-layer model an assignment-capable class STILL survives that the
token grammar cannot refuse without unbounded enumeration: **STOP. Do not iterate.** Write an
accept-with-disclosure recommendation naming the residual class. The owner decision point is
already queued for the morning either way.

## Discipline

- Every RED/GREEN line produced by executing the shipped policy path — **no harness-injected
  policy standing in for delivered bytes** (that is exactly finding R2).
- Rule 9b authoring rules; re-derive all identities after the final edit; repo-wide grep on
  changed values; loudly declare the new `SELF_QA_RP6.md` identity.
- Output hygiene: narrow bands, scratch files outside the repo, summary lines only.

## Files you own

`SELF_QA_RP6.md`, `STATUS_RP6_P0.md`, `RP6_R19_REPORT_2026-08-13.md` (new). Nothing else.
