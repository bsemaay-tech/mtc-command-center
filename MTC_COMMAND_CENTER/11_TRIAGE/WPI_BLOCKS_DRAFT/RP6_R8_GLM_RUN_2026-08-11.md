Round 8 complete. Summary for the Lead:

## What I did

**Evidence-only round.** Touched only the three writable files. `RP6-P0.sh` is **unchanged** — SHA-256 `fa852d7e0a984f977a489bd565834c1ced32eab4fd81221388a25a6bad6483cd`, 103071 B, 0 CR bytes (confirmed three times, including after the last edit). No commit, no host, no network.

## Root cause (verified by source-reading, not execution)

Both failing arms (`build_f4_arm` in FULLBLOCK, `build_pin_arm` in R4) `sed`-slice the block between landmarks. Correction 7 put the `P0_FIXED_*` literals at `RP6-P0.sh:266-299`, outside those slices; the extracted pin loop reads them under `set -u` → abort rc 1. Static tracing confirmed **only these two landmark-slice arms break** — every other arm extracts whole functions (`exfn`) whose bodies read env inputs, not `P0_FIXED_*`.

The tracing also surfaced **two consequences the unbound abort had masked**:
- the pin-arm slice also reads `P0_TOOL_COUNT_EXPECTED` (`:634`, also correction 7, also above the slice);
- the R4 GREEN case supplied 10 pins / asserted `count=10`, but correction 7 requires 12.

## The two repairs

1. **Arm construction that survives growth.** Both arms now define every `P0_FIXED_*` their slice references (the pin arm also mirrors `P0_TOOL_COUNT_EXPECTED`), each with a **build-time assertion** that scans the slice and fails LOUDLY (`ARM_BUILD_INCOMPLETE …`) if a future round adds a new frozen literal — so the next break is at build time, not a silent rc 1.
2. **F7_TOOL_POST — classified and fixed.** The block is **correct**; the **fixture was stale**. Correction 7 deleted the unpinned fallback, so the arm's empty `P0_TOOL_PINS` now STOPs at `tool_pin_unpinned` before the `[ -x ]` check that emits `tool_not_evaluable`. Prereg §8.1 row 1 still carries `tool_not_evaluable tool=getent … rc=<n|na>` and its round-7 amendment defines `tool_pin_unpinned` for the unpinned case — so the fixture must pin `getent` to reach the intended token. (I also fixed the R4 count staleness, `10 → 12`, justified against the same row.)

## Two things to flag

- **QA is PENDING-LEAD-EXECUTION, not fabricated.** This session gates `bash` (every `bash <script>`/`bash -n`/`sed|bash` returned *requires approval*) — the same blocker round-7 Claude/GLM hit. I did not invent transcripts. The existing FULLBLOCK/R4 transcripts are labelled as the **round-6** captures (the shape the repair restores). The report gives the exact anchored commands and the expected rc/summary per fence.
- **One self-caught bug, fixed.** The inert attested literals I added to `build_f4_arm` originally used unquoted `<PIN-AT-FREEZE>`; bash parses `<`/`>` there as redirections, which would have aborted the arm. Caught it before finalizing and single-quoted them (matching the block's own convention at `:289`).

Deliverables: `SELF_QA_RP6.md` (4 code fixes + ROUND 8 section), `STATUS_RP6_P0.md` (round-8 layer), `RP6_REPAIR_R8_REPORT.md` (new, full disposition). The Lead's unhindered-Git-Bash re-run of the two fences by anchored marker is the remaining step before this counts as closure for corrections 5 and 7.
