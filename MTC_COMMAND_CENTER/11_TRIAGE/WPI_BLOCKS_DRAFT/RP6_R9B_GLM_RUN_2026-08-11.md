Round 9b complete. Here's the handoff.

## What I did (IMPLEMENTER, no host/network/commit)

Closed the three things 9a (`ab53a012`) left open: the second `input_pin_freeze_unfilled` emit site, the emit-site sweep, and the report/QA/status layer.

**The repair** — one executable-line change plus a comment rewrite in `RP6-P0.sh`:
- The post-loop python3 backstop (`:668`) emitted an undeclared second shape of `input_pin_freeze_unfilled` (`detail=trusted_python_pin_omitted_freeze_gate_load_bearing`, a round-5 relic). It now emits the **declared** `input_pin_omitted tool=python3 detail=every_preregistered_tool_requires_one_frozen_pin`, matching the omission loop at `:635` verbatim.
- `input_pin_freeze_unfilled` now has exactly one declared shape, at one live site (`:616`, fixed in 9a). No draft byte touched.

**The adjudication** (the kickoff's "say which, and why"): the two sites are distinct conditions, but `:668`'s condition is *already declared under `input_pin_omitted`* (added round 7). Round 7's omission loop fires first, making `:668` an unreachable defense-in-depth backstop — proven by a static trace (`python3 ∈ P0_PIN_SEEN ⟹ P0_TRUSTED_PYTHON_BOUND=yes`). So neither "declare a second form" (redundant + unreachable → unprovable under D026) nor "emit the `:616` line" (the deploy value *is* filled there — a lie). The honest fix is to make the backstop conform to the draft as it already stands.

**Sweep:** all 174 `p0_stop`/`p0_fail` sites (`:399`–`:1753`) cross-checked against draft §8.1 rows 1–9. Exactly one deviation — `:668` — now fixed. Full table in the report.

## Artefact

```
RP6-P0.sh  sha256=08e0a93562bb04f4f78bac77d973a26da5b609aa305491d3bfa51743adbcf10c
           bytes=104683  cr_bytes=0  (was e7ca9ff1…/103808 in 9a)
```

## PENDING-LEAD-EXECUTION (this session gates `bash`, per kickoff clause — not fabricated)

1. `bash -n RP6-P0.sh` → expect rc 0
2. The new `R9_GRAMMAR` harness (marker-delivered in `SELF_QA_RP6.md` §R9, reads the real bytes): GREEN on `08e0a935…`, RED on a relic-restored mutant — the D026 falsifiable proof.
3. The eight 9a fences re-run (unchanged by this edit — none asserts the `:668` token; confirmed by grep).

Then the Codex (`gpt-5.6-sol`, xhigh) T0 re-audit of `08e0a935…`.

**Files written:** `RP6-P0.sh`, `SELF_QA_RP6.md` (§R9 + R5-F1 supersession note), `STATUS_RP6_P0.md` (R9 layer), `RP6_REPAIR_R9_REPORT.md` (new), and a `GLOBAL_HANDOFF.md` pointer. The draft, all `RP7-*`/transport/pathscope files, and everything outside the writable list are untouched. Nothing committed.

I did **not** dispatch the Codex audit — that's the Lead's Gate 5. Say the word if you want me to launch it via `Invoke-CodexForClaude.ps1`.
