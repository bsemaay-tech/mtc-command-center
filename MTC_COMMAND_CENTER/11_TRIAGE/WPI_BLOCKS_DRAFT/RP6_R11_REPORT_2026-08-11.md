# RP6-P0 round 11 — repair report (Codex round-10 T0 audit, four findings)

Implementer: **Claude Max, `claude-opus-5`, effort xhigh**, fresh session.
Auditor of record: **Codex `gpt-5.6-sol`**. Tier: **T0**. Dispatched by
`KICKOFF_RP6_REPAIR_R11.md`. Working dir `C:\LAB\Tradingview_LAB_CLEAN`.

No commit, no host, no network. Every command below was executed in this session
in a local Git Bash `--noprofile --norc` process; every transcript quoted in
`SELF_QA_RP6.md` §ROUND 11 was re-run against the final bytes and reproduces
byte-for-byte.

## Verdict on the four findings

| # | audit finding | disposition |
|---|---|---|
| 1 | HIGH — grammar coverage fail-open; 89/161 normalisation lossy | **CLOSED.** Fail-closed census + correlated-tuple declaration; both named mutants executed and both make the fence nonzero; the round-10 mechanism executed on the same mutants to prove it does not |
| 2 | HIGH — F3 maps an unrecognised producer token to host-state FAIL | **CLOSED at both classification sites.** RED reproduces the audit's rc-1 line; GREEN is rc 3 with a declared reason; the recognised-kind rc-1 regression is asserted for all 17 tokens |
| 3 | HIGH — the published RED recipe masks its own status; guards are prose only | **CLOSED.** `R11_R9RED` exits with the RED status (rc 1); `R11_GUARDS` is an executable self-checking falsification fence over 15 fences |
| 4 | MEDIUM — F4 evidence prose outruns the executed predicate | **CLOSED by narrowing, claim by claim** — no claim was widened by extending a harness |

## Identity

```text
input   RP6-P0.sh  sha256=a090ae736cbecd9973e8ae948b052504b21cbe8b61602f4b5ac592394fad0617  107252 B
                   (commit 71a62cc8 — verified against the kickoff before the first edit)
output  RP6-P0.sh  sha256=5132bacde24cbff8c9267a82f6ac6e3b0cebe3d3c82b092518efac1245103330  110817 B
        SELF_QA_RP6.md    sha256=96e9920ee99399b9a258be97ce8df71abea6ef7bb67e63d31df9d5a8fb7b293f  467356 B
        STATUS_RP6_P0.md  sha256=aad774947702851716c1fd5f224fdc5590c888e5731fe808806043bab3bfef56   60499 B
        prereg §8.1.1 declaration block only
                          sha256=31f8315c5028f5ee3a7ada2a2690000e7b490685c6fe0e9c6117ab33b6da59e5
bash -n RP6-P0.sh rc=0    (GNU bash 5.2.37(1)-release, x86_64-pc-msys)
CR bytes: RP6-P0.sh 0 · SELF_QA_RP6.md 0 · STATUS_RP6_P0.md 0 · prereg draft 0
emit sites 161 -> 163     (162 wrapper + 1 direct ERR-trap printf)
declared    89 forms/161 sites -> 149 correlated tuples/163 sites
```

## Finding 1 — grammar coverage fails closed, and the declaration keeps its correlations

### What was wrong

Two independent fail-open paths, both demonstrated by the audit:

1. **Coverage measured with the restriction it was meant to check.** The
   derivation reads `p0_stop "…"` / `p0_fail "…"` plus one exact single-quoted
   direct-`printf` spelling; the "independent" site total at
   `SELF_QA_RP6.md:5782-5787` used that same lexical restriction. An emitter in
   any other valid spelling vanished from both counts. The audit inserted one
   reachable `p0_stop 'r10_alt_syntax detail=single_quoted'` and the fence
   returned 10/10 PASS at rc 0.
2. **Per-field unions destroyed field correlation.** One line per *form* with an
   independent sorted value set per field admits a Cartesian product: 12 forms
   admitted 65 combinations no site emits. `account=gatea` → `account=mtc-bridge`
   at one of the three `identity_unexpected` sites left every union byte-identical.

### Byte changes

**`RP6-P0.sh`:** none for this finding. The defect was in the fence and the
declaration, not in the block.

**`SELF_QA_RP6.md`:**

- `R10_GRAMMAR` gains a **SUPERSEDED** banner and leaves the mandated set. Its
  bytes stay: they are the round-10 record, and `R11_F1_RED` extracts its
  derivation from them in order to execute what it could not see.
- new `R11_GRAMMAR` fence — 15 assertions:
  - assertions 1-5 are the round-10 five, **carried forward unchanged**
    (`declaration_present`, `grammar_closed`, `site_total_independent`,
    `no_unparseable_emitter`, `err_trap_printf_arguments`). The round-10 narrow
    site total is retained rather than replaced, so nothing it caught is lost;
  - assertion 6 `census_no_unmodeled_syntax` — **fail closed**: any censused line
    the modelled parser cannot read fails the fence;
  - assertion 7 `census_covers_every_emitter` — the broad census total must equal
    the derived site total;
  - assertion 8 `correlation_preserved_one_value_per_field` — no `{}` may contain
    a comma, so per-field unions cannot silently return;
  - mutants (a)-(e) are the round-10 five, carried forward unchanged, plus
    (f) `alt_quoting` and (g) `correlated_relabel` — the two the audit named.
    Each mutant now runs the **whole verdict**, not just the diff, and the fence
    records which sub-check killed it.
- new `R11_F1_RED` fence — 17 cases, the D026 RED half.

**Prereg §8.1.1** — see the enumerated draft edits below.

### The census rule, stated exactly

Every line that uses `p0_stop`/`p0_fail` as a shell word, **or** writes the
`P0_STOP`/`P0_FAIL` result literal, minus exactly two declared exclusions: the
two wrapper definitions and whole-line comments. It never mentions the modelled
quoting. On the round-11 bytes it returns 163 lines against 163 derived sites and
zero unmodelled lines.

### D026 evidence — executed

`R11_GRAMMAR`, real output (full transcript in `SELF_QA_RP6.md` §F1):

```text
R11_GRAMMAR_DECLARED tuples=149 sites=163 source=../WPI_PREREG_DRAFT_ROUND1/WPI_PREREGISTRATION_DRAFT.md
R11_GRAMMAR_DERIVED  tuples=149 sites=163 source=RP6-P0.sh
R11_GRAMMAR_CENSUS   emitter_lines=163 unmodeled=0
ASSERT_MET mutant=alt_quoting killed_by=census_no_unmodeled_syntax,census_covers_every_emitter(164!=163)
ASSERT_MET mutant=correlated_relabel killed_by=grammar_closed
R11_GRAMMAR_SUMMARY cases=15 pass=15 fail=0 result=PASS
```

`R11_F1_RED` executes **the round-10 mechanism** — extracted from the superseded
`R10_GRAMMAR` harness in the same file, not paraphrased — over the same mutants:

```text
CASE_OK RED_r10_derivation_blind_to_alt_quoting got=[invariant]
CASE_OK RED_r10_independent_total_blind_to_alt_quoting got=[163]
CASE_OK RED_r10_normalisation_blind_to_correlated_relabel got=[invariant]
CASE_OK GREEN_r11_census_flags_alt_quoting got=[1]
CASE_OK GREEN_r11_census_total_moves got=[164]
CASE_OK GREEN_r11_tuples_see_correlated_relabel got=[differs]
RELABEL_DIFF 2
58c58
< 1 P0_STOP identity_unexpected observed_numeric={<live_uid>:<live_gid>} expected_numeric={<P0_PW_UID>:<P0_PW_GID>} account={gatea}
---
> 1 P0_STOP identity_unexpected observed_numeric={<live_uid>:<live_gid>} expected_numeric={<P0_PW_UID>:<P0_PW_GID>} account={mtc-bridge}
CASE_OK BOUNDARY_r11_parser_alone_still_blind_census_is_what_catches_it got=[invariant]
R11_F1_RED_SUMMARY cases=17 pass=17 fail=0 result=PASS
```

The last case is deliberate and is **not** a hidden weakness: the round-11
*parser* is as blind to the alternate quoting form as round 10's was; the
*census* is what fails closed on it. Asserting that stops the next round from
believing the parser got broader than it did.

### No carried fence weakened

Every round-10 assertion and every round-10 mutant is present in `R11_GRAMMAR`
with its original text. Nothing was dropped, so no discriminating-power proof is
owed for a removal. `R10_GRAMMAR` itself is superseded rather than deleted, and
its current status against the round-11 declaration is published:

```text
R10_GRAMMAR_DECLARED forms=149 sites=163
R10_GRAMMAR_DERIVED  forms=91 sites=163
ASSERT_UNMET grammar_closed declared!=derived diff_lines=106
R10_GRAMMAR_SUMMARY cases=10 pass=9 fail=1 result=FAIL
SUPERSEDED_R10_GRAMMAR_RC=1
```

## Finding 2 — an unrecognised `%F` token is rc 3, at both sites

### What was wrong

`RP6-P0.sh:1618-1622` (round-10 numbering) sent every remaining printable token
through `*) P0_FKIND="other"`, so arbitrary producer text became
`P0_FAIL reason=interpreter_target_kind_unexpected kind=other` at rc 1 — a
completed observation of deviant host state asserted from an answer the block
could not read (patterns 1, 5, 6).

**The audit named the followed-target site; the leaf classifier at
`RP6-P0.sh:1636` carried the identical catch-all**, feeding
`venv_root_kind_unexpected` / `interpreter_kind_unexpected` at rc 1 from the same
unreadable input. Repairing one and leaving the other would have left the finding
live under a different reason token, so both are repaired. This is inside the
audit's own required repair, which says `P0_FKIND=other` may be assigned only for
explicitly recognised complete kinds — the leaf assigns it too.

### Byte changes — `RP6-P0.sh`

| site | change |
|---|---|
| followed target, `p0_probe_kind` | `*) P0_FKIND="other" ;;` replaced by an explicit 17-token recognised-kind arm binding `P0_FKIND="other"`, plus `*) p0_stop "link_target_kind_unrecognized path=$p rc=0 detail=$P0_SAFE expected=complete_gnu_stat_percent_F_token"` |
| leaf, `p0_probe_kind` | `*) P0_KIND="other"; P0_FKIND="other"; return 0 ;;` replaced by an explicit 16-token recognised-kind arm (no `symbolic link` — the arm above consumes it) binding `P0_KIND/P0_FKIND="other"`, plus `*) p0_stop "path_probe_kind_unrecognized path=$p rc=0 detail=$P0_SAFE expected=complete_gnu_stat_percent_F_token"` |
| both | the surrounding comment states the producer, the rule, and which direction is fail-closed |

The accepted set is the complete return set of GNU coreutils `file_type()`
(`src/stat.c`) — the pinned producer this block already depends on for its
C-locale failure shapes in `p0_classify_stat_shape`: `regular file`,
`regular empty file`, `directory`, `symbolic link`, `block special file`,
`character special file`, `contiguous data`, `door`, `fifo`, `message queue`,
`multiplexed file`, `named file`, `network special file`, `port`, `semaphore`,
`shared memory object`, `socket`, `typed memory object`, `weird file`,
`whiteout`.

Two new declared forms, both in §8.1.1:

```text
1 P0_STOP link_target_kind_unrecognized path={<p>} rc={0} detail={<P0_SAFE>} expected={complete_gnu_stat_percent_F_token}
1 P0_STOP path_probe_kind_unrecognized path={<p>} rc={0} detail={<P0_SAFE>} expected={complete_gnu_stat_percent_F_token}
```

### D026 evidence — executed

`R11_F3`, 85 cases, all PASS. The load-bearing ones:

```text
CASE_OK GREEN_followed_unrecognised_rc got=[3]
CASE_OK GREEN_followed_unrecognised_exact_line got=[P0_STOP reason=link_target_kind_unrecognized path=/fixture/python rc=0 detail=made up stat kind expected=complete_gnu_stat_percent_F_token]
CASE_OK GREEN_leaf_unrecognised_rc got=[3]
CASE_OK GREEN_leaf_unrecognised_exact_line got=[P0_STOP reason=path_probe_kind_unrecognized path=/fixture/python rc=0 detail=made up stat kind expected=complete_gnu_stat_percent_F_token]
CASE_OK RED_followed_unrecognised_reaches_rc1 got=[1]
CASE_OK RED_followed_unrecognised_exact_line got=[P0_FAIL reason=interpreter_target_kind_unexpected kind=other path=/fixture/python expected=regular]
CASE_OK RED_leaf_unrecognised_reaches_rc1 got=[1]
CASE_OK RED_leaf_unrecognised_exact_line got=[P0_FAIL reason=interpreter_kind_unexpected kind=other path=/fixture/python expected=regular_or_live_symlink]
R11_F3_QA_SUMMARY cases=85 pass=85 fail=0 result=PASS
```

RED is a faithful replica of the round-10 classifier at **both** sites — every
round-10 shape gate kept, only the two catch-alls restored — and it reproduces
the audit's `AUDIT_UNRECOGNISED rc=1 last=[P0_FAIL reason=interpreter_target_kind_unexpected kind=other …]`
observation exactly.

**Regressions the repair must not move, all executed:** every one of the 17
recognised non-regular tokens still reaches rc 1 with `kind=other` on the
followed side; the 16 leaf tokens still reach rc 1 with `kind=other`;
`directory` still reaches rc 1 with `kind=dir`; `regular file` /
`regular empty file` still bind at rc 0. `R10_F3` (multiline / non-printable /
empty / directory / regular) passes unchanged on the round-11 bytes and stays in
the mandated set.

## Finding 3 — the RED recipe returns its own status; the guards are falsified executably

### The recipe

Round 10 published the RED twin as loose shell lines ending in `rm -f "$mutant"`.
`rm` succeeds, so the recipe's process status was 0 while the harness it exists
to demonstrate had failed with 1 — in the same file that claimed "every command's
status agrees with its verdict". **That claim is withdrawn as written**, and the
round-10 recipe block now carries a SUPERSEDED banner saying so.

`R11_R9RED` is a marker-delimited fence: cleanup moved into an `EXIT` trap so it
can no longer be the last command, and the fence exits **with** the RED harness's
status. It is self-checking in both directions — rc 9 if the RED twin ever goes
GREEN, rc 2 if the mutation does not apply.

```text
R9_RED_MUTANT_APPLIED delta_lines=2
R9_GRAMMAR_SUMMARY cases=5 pass=2 fail=3 result=FAIL
R9_RED_RC=1
R9_RED_VERDICT status_preserved_across_cleanup exit=1
```

Process status of the published command: **rc 1**. This is the one member of the
mandated set whose PASS condition is nonzero, and it is labelled as such in both
`SELF_QA_RP6.md` and `STATUS_RP6_P0.md`.

### The guards

Round 10b's ten-guard claim was a prose algorithm plus a transcript, which under
D026 is supplemental. `R11_GUARDS` is the executable command: for each fence it
extracts the fence by marker pair, locates the own-status guard by **exact
whole-line text**, requires exactly one match, injects the counter assignment on
the line immediately before it, **asserts where the injection landed** (the
round-10b generator was once wrong in exactly this way and produced ten false
results), and requires a nonzero status from the mutant.

It covers **15** fences, not 10 — the three round-10 fences and the two new
round-11 counting fences are included, so a guard added this round is falsified
by the same mechanism. `R11_R9RED` is excluded and the reason is stated in the
harness: it has no pass/fail counter, because its purpose is to exit with a RED
harness's status.

```text
R5_F1 guard_at_line=59  … R9_GRAMMAR guard_at_line=31 … R11_F1_RED guard_at_line=114
R11_GUARDS_SUMMARY fences=15 pass=15 fail=0 result=PASS
```

The ten line numbers reproduce round 10b's transcript exactly, which is
independent corroboration that the round-10b hand-run was honest — it was just
not published as a command.

## Finding 4 — prose narrowed to the executed predicate, claim by claim

Resolution is **narrowing** in every case. Extending `R10_F4` to cover the
parser's other early-stop classes would be a new control this round was not asked
to add, and the audit explicitly allowed either, per claim.

| claim | where | what actually executes | resolution |
|---|---|---|---|
| the fence "deletes the three upstream gates" | `RP6-P0.sh:671-674` | the mutant neutralises **two** sites — omission loop, pin-count check | **narrowed** in the block comment; the two gates that are *not* neutralised (freeze-unfilled, disagreement) are now named |
| "every input class that leaves the binding unset" | `SELF_QA_RP6.md:6145-6153` | three classes: omitted pin, unfilled deploy placeholder, disagreeing pin | **narrowed** to "three input classes"; the unexecuted early-stop classes (malformed entry, unknown tool, duplicate, non-absolute, whitespace, glob metacharacter, non-python frozen path) are named as not executed |
| the same phrasing in the status layer | `STATUS_RP6_P0.md` | — | **checked, not found.** `grep -in 'input class\|binding unset' STATUS_RP6_P0.md` returns nothing on the round-10 bytes; its F4 bullet already said "a mutant whose two consuming gates are neutralised". The round-11 status states the three-class bound explicitly anyway |
| the same phrasing in `RP6_R10_REPORT_2026-08-11.md:362-369` | round-10 report | as above | **corrected here, not rewritten there** — see below |

### The round-10 report line, corrected rather than edited

`RP6_R10_REPORT_2026-08-11.md:362-364` says A1–A3 show "that **every** input class
leaving the binding unset is consumed by its own upstream gate first". **That is
an overclaim and it is corrected here:** A1–A3 execute exactly three classes — an
omitted pin, an unfilled deploy-channel placeholder, and a pin that disagrees
with the frozen literal. The pin parser has further early-stop classes that
`R10_F4` does not run.

The round-10 report is **not** edited. It is a delivered audit-round record of
what that round claimed, and the kickoff scope fence does not list it as
writable. The correction is published here, in `SELF_QA_RP6.md` §F4, and as
residual 5 in `STATUS_RP6_P0.md`. If the Lead prefers the round-10 report
amended in place, that is a one-line edit and is the Lead's call, not mine.

The block comment at `RP6-P0.sh:653-662` is unchanged and remains accurate: it
describes which gate consumes which condition in the live control flow, which is
a statement about the code and not about the mutant.

## Preregistration draft edits — every one enumerated

File: `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/WPI_PREREGISTRATION_DRAFT.md`,
§8.1.1 only. The kickoff permits this "if a finding requires"; finding 1 requires
it, because a correlation-preserving fence needs a correlation-preserving
declaration to diff against.

1. **"What a declared form is" → "What a declared tuple is."** The format line
   changes from `<field>={<value-class>[,<value-class>...]}` to
   `<field>={<value-class>}`; the sentence "The value set of a field is the
   sorted union over every site sharing the form" is deleted, because that
   sentence *is* the defect.
2. **New paragraph, "Round 11 …: one value class per field, never a union."**
   States what round 10 did, why it was lossy (12 forms / 65 synthetic
   combinations / the `account=gatea` relabel), what replaces it, and that the
   fence asserts no `{}` contains a comma.
3. **New paragraph, "Coverage is fail-closed (round 11)."** States the census
   rule and its two declared exclusions, and that an unreadable censused line is
   a coverage error rather than a skip.
4. **Header line rewritten** — "89 forms, 161 emit sites, derived from
   `RP6-P0.sh` at the round-10 bytes" → "149 correlated tuples, 163 emit sites …
   round-11 bytes", naming the two `*_kind_unrecognized` additions and that the
   rest of the growth is round-10 unions splitting into the tuples that exist.
5. **The marker-delimited declaration regenerated**, 89 lines → 149 lines, by the
   published derivation. Block-only sha256
   `31f8315c5028f5ee3a7ada2a2690000e7b490685c6fe0e9c6117ab33b6da59e5`.

Nothing outside §8.1.1 was touched. §8.1 rows are unchanged.

### One thing the Lead must know about this file

**The draft is shared, and the round-11 §8.1.1 edits are already committed —
by another lane's commit, not by me.** Sequence, measured:

```text
this session splices §8.1.1        -> draft sha256 18797f03…
15:14 transport lane writes lines 343-359 and 580-591 (F1 OPEN wording)
commit 008d2dde (Lead, transport r5) commits the WHOLE draft file
                                  -> HEAD blob == worktree == ce6e2762…
```

So `008d2dde` carries this round's §8.1.1 declaration under a transport-lane
commit message. I committed nothing and ran no `git checkout`/`reset`/`stash`.
The round-11 commit therefore has **no draft delta left to make** — its content
is `RP6-P0.sh`, `SELF_QA_RP6.md`, `STATUS_RP6_P0.md` and this report. The
provenance mismatch is recorded here rather than tidied away; if the Lead wants
the declaration attributed to round 11 in history, that needs a commit-message
note, not a re-edit.

I also verified the two lanes do not overlap: the transport edits sit at draft
lines 343-359 and 580-591; §8.1.1's declaration block is lines 932-1082.
`R11_GRAMMAR` was re-run **after** the transport write and still closes, which is
the executable form of that claim.

## Mandated harness set after round 11 — executed sweep

Twenty-three published commands, run verbatim from `WPI_BLOCKS_DRAFT`:

```text
bash -n RP6-P0.sh                       -> rc=0
C13_R3_BACKSTOP                    -> rc=0  C13_R3_BACKSTOP_QA_SUMMARY inputs=2 mutations=2 cases=4 result=PASS
RP6_FULLBLOCK_D026                 -> rc=0  RP6_FULLBLOCK_D026_SUMMARY findings=7 round3_residuals=3 real_lstat_arms=2 execution_domain_cases=9 readlink_stop_arms=3 result=PASS
F2_FREEZE_GATE                     -> rc=0  F2_FREEZE_GATE_QA_SUMMARY placeholder_rc=3 filled_fixture_rc=0 result=PASS
RP6_R4_D026                        -> rc=0  RP6_R4_D026_SUMMARY findings=4 pth_forge=real_venv manager_bound=real_timeout inventory_basis=23e55667@d6a976aa result=PASS
C13_R4B                            -> rc=0  C13_R4B_ARM_QA_SUMMARY cases=27 result=PASS
R5_F1                              -> rc=0  R5_F1_QA_SUMMARY cases=6 pass=6 fail=0 result=PASS
R5_F2                              -> rc=0  R5_F2_QA_SUMMARY cases=4 pass=4 fail=0 result=PASS
R5_F3                              -> rc=0  R5_F3_QA_SUMMARY cases=5 pass=5 fail=0 result=PASS
R6_F1                              -> rc=0  R6_F1_QA_SUMMARY cases=3 pass=3 fail=0 result=PASS
R6_F2                              -> rc=0  R6_F2_QA_SUMMARY cases=10 pass=10 fail=0 result=PASS
R6_F3                              -> rc=0  R6_F3_QA_SUMMARY cases=7 pass=7 fail=0 result=PASS
R7_F2                              -> rc=0  R7_F2_QA_SUMMARY cases=4 pass=4 fail=0 result=PASS
R7_F3                              -> rc=0  R7_F3_QA_SUMMARY cases=4 pass=4 fail=0 result=PASS
R7_C3                              -> rc=0  R7_C3_QA_SUMMARY cases=8 pass=8 fail=0 result=PASS
R9_GRAMMAR (-s -- RP6-P0.sh)       -> rc=0  R9_GRAMMAR_SUMMARY cases=5 pass=5 fail=0 result=PASS
R10_F3                             -> rc=0  R10_F3_QA_SUMMARY cases=14 pass=14 fail=0 result=PASS
R10_F4                             -> rc=0  R10_F4_QA_SUMMARY cases=16 pass=16 fail=0 result=PASS
R11_GRAMMAR                        -> rc=0  R11_GRAMMAR_SUMMARY cases=15 pass=15 fail=0 result=PASS
R11_F1_RED                         -> rc=0  R11_F1_RED_SUMMARY cases=17 pass=17 fail=0 result=PASS
R11_F3                             -> rc=0  R11_F3_QA_SUMMARY cases=85 pass=85 fail=0 result=PASS
R11_GUARDS                         -> rc=0  R11_GUARDS_SUMMARY fences=15 pass=15 fail=0 result=PASS
R11_R9RED                          -> rc=1  R9_RED_VERDICT status_preserved_across_cleanup exit=1
```

`R10_GRAMMAR` is **out** of the set (superseded, rc 1 by construction) and the
round-10 RED recipe is replaced by `R11_R9RED`. Both removals are banner-marked
in place in `SELF_QA_RP6.md` rather than deleted.

## Residuals — named, not closed

1. `R11_GRAMMAR` is a **static source** grammar. The census makes coverage fail
   closed over emitter *syntax*; it still cannot constrain what a `<name>` class
   evaluates to at run time.
2. The round-11 **parser** is as blind to alternate quoting as round 10's — the
   census is what catches it. Asserted executably rather than left implicit.
3. The `%F` token set is GNU coreutils' complete `file_type()` return set. On a
   non-GNU producer an out-of-set token now STOPs at rc 3 instead of being
   reported as host deviation. That is the intended fail-closed direction, not a
   claim that this block can classify another producer's vocabulary.
4. `R11_F3` drives the block's real functions through a `stat` shim: it
   establishes what the block does with a given producer answer, not which
   answers a real host produces.
5. `P0_STATE_UID` / `P0_STATE_GID` / `P0_EXPECT_UID` input integrity — unchanged
   from round 10, freeze-gate/owner band.
6. `R10_F4`'s reachability result covers the **three** input classes it executes.
7. `RP6_R10_REPORT_2026-08-11.md:362-364` still carries the "every input class"
   wording; corrected here, not edited there (scope fence).
8. The complete P0 block still was not run end to end — seventeen
   `<PIN-AT-FREEZE>` literals, no RP0 library/bootstrap, no Linux `/proc`, no
   host. Nothing here is dispatchable.
9. `shellcheck` is not installed in this environment and was not run.
10. `git` warns `LF will be replaced by CRLF the next time Git touches it` for
    `RP6-P0.sh` in this worktree. The file on disk is LF-only (CR bytes = 0,
    measured). The Lead should confirm the committed blob stays LF.

## Scope and safety

Files written: `RP6-P0.sh`, `SELF_QA_RP6.md`, `STATUS_RP6_P0.md`, this report,
and `WPI_PREREG_DRAFT_ROUND1/WPI_PREREGISTRATION_DRAFT.md` §8.1.1 only.

One extra one-line correction inside a file already in scope, disclosed rather
than slipped in: `STATUS_RP6_P0.md`'s round-7 section header read "the current
bytes — round 8 changed no block byte", which stopped being true at round 9. It
now reads "superseded — rounds 9, 10 and 11 changed block bytes". No other
content in that section was touched.

No other file was opened for writing. No `git checkout`, `git reset` or
`git stash` was run on any tracked file. Nothing was committed — the Lead
commits. No host was contacted, no network command was run, no credential, ARM,
broker, exchange, trading, deployment or freeze authority is implied or granted.
