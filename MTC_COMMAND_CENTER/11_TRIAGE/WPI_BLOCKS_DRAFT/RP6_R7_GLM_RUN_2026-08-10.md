Round 7 implementation is complete. All seven corrections are applied to `RP6-P0.sh`, the evidence/docs are written, and the artefact identity is real.

## Summary

**Repaired `RP6-P0.sh`** — SHA-256 `fa852d7e0a984f977a489bd565834c1ced32eab4fd81221388a25a6bad6483cd`, 103071 bytes (+9650), LF-only (CR=0 verified). Block is internally consistent — all new tokens present, all stale tokens absent.

**The seven corrections:**
1. **C1/A4 (R5-F2)** — `type -t` → **`builtin type -t`** (matches accepted `RP7-WPI-RO.sh:646-647`), defeating a caller-defined `type(){…}`. Comment + `P0_prereq` line narrowed to "required functions present and exercised" (not RP0-LIB provenance).
2. **C2/A8 (R6-F3)** — outer pin parse wrapped in a **caller-noglob save/restore**, so a crafted cwd holding `stat=/usr/bin/stat` can no longer rewrite `stat=/usr/bin/sta*` before the charset gate.
3. **C3/A9** — `p0_probe_kind` and `p0_assert_venv_root` adjudicate **rc-0 producer shape** (empty/multiline/non-printable/non-absolute) as rc 3 STOP *before* any rc-1 verdict (pattern 1/6).
4. **C4/A10** — rc 124 relabelled `manager_query_rc124_timeout_reached_or_child_exit_124` (timeout can't distinguish a child's own 124); interpreter isolation restated as requested `-I -S` + child-reported state (binary provenance unbound); `pinned_timeout` honest via C7.
5. **C5/A11** — all eight fences now carry **unique anchored marker pairs** (`^# NAME_HARNESS_BEGIN$`/`END$`) whose invocation text can't reopen the range; R4 D026 POST assertions updated for the renamed tokens.
6. **C6** — `RP6_REPAIR_R4_REPORT.md` stale "`-S` cannot be silently undone" replaced with the round-6 truth.
7. **C7** — exactly one frozen pin required per tool (12 total), each equal to its frozen literal; omissions/extras/mismatches rejected; **unpinned `command -v` fallback deleted**. Freeze-gate literals **6 → 17**.

Prereg §8.1 rows 1 and 9 amended narrowly.

**QA status: PENDING-LEAD-EXECUTION** (the kickoff's accepted path). My session gates `bash`/`bash -n`/`bash -c` (execution approval), so I did **not** fabricate transcripts. Real SHA/bytes/CR were captured via stdin-redirect (`sha256sum <`, `wc -c <`, `tr -cd | wc -c`); `bash -n` remains pending. `SELF_QA_RP6.md` §R7 carries three self-contained marker-delimited D026 harnesses (`R7_F2`, `R7_F3`, `R7_C3`) plus the marker migration of the five legacy fences.

**Lead must run** (clean Git Bash, from `WPI_BLOCKS_DRAFT`):
```
bash -n RP6-P0.sh
sed -n '/^# R7_F2_HARNESS_BEGIN$/,/^# R7_F2_HARNESS_END$/p' SELF_QA_RP6.md | bash --noprofile --norc
sed -n '/^# R7_F3_HARNESS_BEGIN$/,/^# R7_F3_HARNESS_END$/p' SELF_QA_RP6.md | bash --noprofile --norc
sed -n '/^# R7_C3_HARNESS_BEGIN$/,/^# R7_C3_HARNESS_END$/p' SELF_QA_RP6.md | bash --noprofile --norc
# + the five legacy fences by anchored marker; confirm the R4 fence returns within bound
```

**No commit, no host contact, no network.** Six files touched (all within the writable list); no other-session files modified. Full record in `RP6_REPAIR_R7_REPORT.md`; status bumped to `ROUND-7-REPAIRED-PENDING-T0-REAUDIT` in `STATUS_RP6_P0.md`.

One honest note for the auditor: the R4 fence "open handle" (A11) is a harness/fixture concern (a lingering background `timeout`/venv child), not a block defect — every block path exits via `p0_stop`/`p0_fail`/`exit`; the Lead's marker-extracted re-run will confirm whether it returns within bound.
