# Owner decision — ACCELERATED FULL COMPLETION approved — 2026-08-16 afternoon

Recorded by the Fable 5 Lead from the owner's chat answer on 2026-08-16
(afternoon, after the midday handoff's §3 question was put to him in the exact
corrected form). This record answers the open question flagged in
`NEW_CHAT_HANDOFF_2026-08-16_MIDDAY.md` §3, `GLOBAL_HANDOFF.md` (midday
section), and `SESSION_LOCK.md` (11:05 log entry).

## The owner's answer

Barış answered, verbatim (chat, 2026-08-16):

> "APPROVE ACCELERATED FULL COMPLETION"

together with a model-routing instruction:

> "Fable5 lead, codex and claude sonnet 5 coder audit, GLM 5.3 coder auditor"

## The approved contract (as put to the owner)

1. **Objective unchanged** — assemble and accept the current Bridge release,
   deploy that exact accepted release to Hostinger KVM2 initially masked and
   DISARMED, and verify startup safety, logs, monitoring, backup, recovery and
   rollback.
2. **Full existing T0 standard kept** for host-touching code/scripts,
   credentials, keys, broker/exchange access, ARM, orders, TESTNET/mainnet and
   live-money paths.
3. **Normal repository T2/T3 rules only** for non-load-bearing documentation
   and ordinary plumbing. No extra flagship reviews, no duplicate proof
   systems, no repair cycles beyond the tier policy. A reproduced required
   defect affecting the selected finish line must still be repaired.
4. **Sequence by delivery value:** current-release integration and tests →
   required release acceptance → read-only KVM2 inventory → exact host-bound
   plan and estimate → masked, DISARMED KVM2 installation → operational
   verification. Non-load-bearing documentation deferred until the system is
   observable.
5. **Read-only inspection of the existing Hostinger KVM2 VPS is authorized**
   for inventory and planning, using existing safe access routes, without
   displaying or copying secrets. This does NOT authorize reprovisioning,
   deletion, replacement, paid purchase, secret creation, TESTNET/mainnet
   access, broker connection, ARM, orders or live trading.
6. **One exact deployment action plan** is presented after the read-only KVM2
   inventory and current-release acceptance complete. Routine decisions are not
   re-asked, but the separately required authorization is obtained before the
   first KVM2 configuration/install/start action.

## Model-routing interpretation of record

- **Lead:** Fable 5 (this session; the owner set the model explicitly).
- **T0 audit slots are unchanged** by the routing sentence: contract clause 2
  keeps the full existing T0 standard, so T0 remains exact `claude-opus-5` +
  `gpt-5.6-sol` at xhigh per `AGENTS.md` §CANONICAL AUDIT ROSTER. The owner's
  "coder audit" list is read as the **T1/T2 auditor pool**: Codex
  (`gpt-5.6-sol`), Claude Sonnet 5, and GLM-5.3 for code-level reviews at
  their tier-controlled efforts.
- If the owner intended Sonnet 5 to REPLACE `claude-opus-5` in T0 slots, that
  is a roster waiver and needs his explicit sentence; it is NOT assumed here.
  This interpretation was surfaced to him in chat at recording time.
- GLM-5.3 verdicts remain supplemental wherever execution matters
  (`LESSONS.md` L-008 class); it may fill T1 second-opinion / T2 reviewer
  slots.

## What this supersedes and what it does not

- Supersedes: the open §3 question (options A / B+C) in the midday handoff.
  The accelerated contract is effectively "B and C with the money gate kept at
  full standard", plus an explicit KVM2 read-only inventory grant.
- Does NOT supersede: owner decisions §1–§6 of
  `OWNER_DECISIONS_2026-08-16_MORNING.md` (all stand); the GATEA-STAGING-only
  host grant in `HOST_CHANNEL_AUTHORIZATION_2026-08-16.md`; RP7 rows 1–9 T0
  acceptance at `80cbed46`; the Pathscope closure (no sixth cycle); D026; the
  audit tier policy; single-writer locks; exact-model rules for invoked slots.
- Hard boundaries unchanged: no KVM2 configuration/install/start without the
  separate authorization in clause 6; no secret display/copy/creation; no
  broker/exchange, ARM, orders, TESTNET/mainnet activation, mainnet, live
  money; no merge to master; no destructive git.

## Immediate consequences applied

1. The remaining-hours figures (10–20, 15–25, 41.5–77.5, 60–70) are all
   retired as mixed-host artifacts; a new range is published only after the
   host-labelled register rebuild and the KVM2 read-only inventory.
2. The critical path starts at current-release integration: refresh the merge
   runbook for repaired tip `7d4e9a96`, execute the prescribed merge in an
   isolated worktree, run the full integrated suite, obtain tier-required
   acceptance.
3. The privileged mutation-denial channel design gets a load-bearing decision
   under this contract before any audit or build of it (midday handoff §6).
