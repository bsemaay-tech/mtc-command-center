# Pathscope final additional T1 repair — Claude implementer

You are the counterpart flagship IMPLEMENTER in a fresh session. The exact
model is `claude-opus-5`, effort `high`, with no session persistence. Do not
resume or continue any prior session. No sub-delegation and no Git mutation.

Working directory: `C:\LAB\Tradingview_LAB_CLEAN`.

## Authority and tier

- Gate-1 classification: **T1** — local non-economic Pathscope prover and its
  evidence harness; no host or deployment execution.
- Owner authority:
  `MTC_COMMAND_CENTER/11_TRIAGE/WPI_OWNER_DECISION_PATHSCOPE_FINAL_OVERRIDE_2026-08-14.md`.
- This is exactly one final repair. It is followed, if the Lead freezes an
  eligible candidate, by exactly one fresh `gpt-5.6-sol` high execution audit.
  No further cycle is authorized.

Read in order and in full:

1. root `AGENTS.md` and `MTC_COMMAND_CENTER/_AI_MEMORY/START_HERE.md`;
2. `MTC_COMMAND_CENTER/11_TRIAGE/DESIGN_DEFECT_PATTERNS_2026-08-10.md`, especially Patterns 10, 12, and 13;
3. the owner-decision file above;
4. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/PATHSCOPE_CAP_OVERRIDE_CODEX_T1_AUDIT_2026-08-13.md`;
5. `pathscope_prover.py`, `SELF_QA_PATHSCOPE.md`, `STATUS_PATHSCOPE.md`, and
   `PATHSCOPE_CAP_OVERRIDE_REPAIR_REPORT_2026-08-13.md`.

Before editing, reproduce every required finding against the committed
pre-repair candidate `2fb3eac05f8da716609549179a7961aa692eae6b`. Do not infer
old behavior from source text. Keep all scratch outside the repository and do
not execute fixture bodies as shell.

## Binding repairs

Close C-3 with terminal conservation across the complete admitted assignment
member grammar and all applicable call sites. At minimum, the following must
never silently disappear or produce zero terminal accounting:

- a whitespace list with a later relative pathname member;
- a URI-shaped loader list with a later absolute member;
- a colon-bearing whole pathname reading;
- an empty-only loader list such as `LD_LIBRARY_PATH=:`; and
- executable command text without `/`, such as `GIT_SSH_COMMAND="ssh -v"`.

Close C-4 by making quoted declaration assignments reach the same accounting
grammar, including both `export "LD_PRELOAD=/etc/escape.so"` and
`export 'X=/safe dir/escape'`. Re-audit prefix, `env`, and `export` reachability
as one grammar, not as isolated examples.

Close the literal-harness portability defect. The published PowerShell harness
must reproduce its recorded transcript and determinism hashes under its literal
command in the Turkish Windows profile without manual path substitutions or
post-run edits. The repair must not merely make two broken runs agree.

For every claimed closure, provide D026 RED against the exact committed
pre-repair blob and GREEN against repaired bytes, through the real prover, with
the commands and real output recorded. Preserve and execute benign controls,
the existing quoted-space regression guard, and carried fences. Any carried
fence change requires the repository's per-change discriminating-power proof.
Zero facts plus PASS is red; every admitted member must reach exactly one
terminal disposition or an explicit fail-closed coverage record.

## Owned files only

- `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/pathscope_prover.py`
- `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/SELF_QA_PATHSCOPE.md`
- `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/STATUS_PATHSCOPE.md`
- new `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/PATHSCOPE_FINAL_OVERRIDE_REPAIR_REPORT_2026-08-14.md`

These paths may contain current committed work from prior agents. Do not run
`git checkout`, `git reset`, or `git stash` on any tracked file. Do not touch
the separate uncommitted `WPI_BLOCKS_DRAFT/RP7-WPI-RO.sh` partial.

Run the published QA literally, proportionate direct fixtures, Python 3.12
feature-compatible AST parsing, and `git diff --check` on owned files. Re-derive
byte counts and SHA-256 identities. Report all commands and results honestly.
Do not claim acceptance, commit, push, contact a host, use credentials, or
perform deployment, service, broker, ARM, order, TESTNET/mainnet, Pine, parity,
MTC, or trading action. Stop after the owned files and new report are complete.
