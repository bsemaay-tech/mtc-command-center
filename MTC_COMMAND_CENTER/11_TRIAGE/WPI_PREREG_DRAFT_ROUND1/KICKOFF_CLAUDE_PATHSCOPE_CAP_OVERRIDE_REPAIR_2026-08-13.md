# Pathscope cap-override repair - Claude implementer

You are the counterpart flagship IMPLEMENTER in a fresh session. Model must be
`claude-opus-5`; effort `high`. This is the single owner-authorized T1 cycle
beyond the normal cap. No sub-delegation and no git mutation.

Working directory: `C:\LAB\Tradingview_LAB_CLEAN`.

Read, in order:

1. root `AGENTS.md` and `MTC_COMMAND_CENTER/_AI_MEMORY/START_HERE.md`;
2. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_OWNER_DECISIONS_2026-08-13.md`, section 4;
3. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/PATHSCOPE_CODEX_T1_EXEC_AUDIT_R3_2026-08-13.md` in full;
4. the current `pathscope_prover.py`, `SELF_QA_PATHSCOPE.md`,
   `STATUS_PATHSCOPE.md`, and the prior r3 repair report.

The binding work order is finding C-2. Reproduce it before editing. Repair the
construct boundary without a variable-name allowlist: a statically known
assignment value containing one path, a path list, command text, or an ordinary
relative pathname must never silently disappear. Parse the complete supported
value grammar and terminally account for every relevant component, or emit a
specific fail-closed coverage record where the grammar is not modeled. Preserve
the disclosed bare-soname boundary only if it is accurate after the repair.

Add literal D026 evidence against the actual pre-repair bytes for at least:

- a loader list whose later member is an out-of-allowlist absolute path;
- the same shape at prefix, `env`, and `export` call sites;
- a whitespace-separated later absolute member; and
- an ordinary relative pathname containing `/` with an out-of-allowlist pinned
  `PWD`.

Each claimed closure needs executed pre-fix RED and repaired GREEN through the
real prover, with commands and real output. Include benign controls. Do not
execute fixture text as shell. Keep all scratch outside the repository.

Also close the cheap documentary items if still applicable: U-3 citation,
ENDPOINT `ALLOW-LEXICAL` wording, and stale determinism prose.

Owned files only:

- `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/pathscope_prover.py`
- `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/SELF_QA_PATHSCOPE.md`
- `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/STATUS_PATHSCOPE.md`
- new `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/PATHSCOPE_CAP_OVERRIDE_REPAIR_REPORT_2026-08-13.md`

Run proportionate self-QA, re-derive exact byte/SHA identities, run
`git diff --check` on owned files, and report every command/result honestly.
Do not claim acceptance; the Lead and a fresh auditor decide it. Stop after the
owned files and report are complete.
