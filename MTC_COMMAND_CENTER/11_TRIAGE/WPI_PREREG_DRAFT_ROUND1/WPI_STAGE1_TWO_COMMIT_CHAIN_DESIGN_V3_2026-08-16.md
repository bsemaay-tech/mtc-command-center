Status: TWO-COMMIT CHAIN DESIGN V3 — 2026-08-16 — SUPERSEDES V2 — NOT ACCEPTED — implements `TWO_COMMIT_CHAIN_R2_DISPOSITION_2026-08-16.md` — V2 source pin: 79100 bytes, sha256 `1fe9e5cb5ef9c266b819c3ca63d4273e3c45ef21493fac04c2c492dbf5dc36d4`

# Finding-closure table

| Finding | V3 section | Closure mechanism | Review cite |
|---|---|---|---|
| REQ-1 | §§5.1–5.2, 6; §DISCLOSURE | The governing disposition severs the privileged channel. The surviving walk proceeds directly from 7C/8/8A to Step 9, and no surviving check consumes mutation-denial evidence; therefore no second dependency edge remains to produce a cycle. | `TWO_COMMIT_CHAIN_R2_DISPOSITION_2026-08-16.md` |
| REQ-2 | §§1.3, 2.2, 3.2, 7; K15, K16, K43 | Adds the accepted-build venv executable path/digest to stable expectations and K16; binds `UNIT_ACTIVE_STATE` and `UNIT_NRESTARTS` to independent A-4/A-5 terminal dispositions; labels MainPID, start time, and net namespace producer-internal where no independent expectation exists; corrects the table heading and closing claim. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:48-58` |
| REQ-3 | §3.4; K40 | Rebuilds every parameter token in `ATT01_REMOTE_COMMAND` and `ATT01_OUTER_ARGV_B64` from the committed 1a allocation row and 1b candidate row, byte-for-byte; any divergence is STOP before socket open. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:60-70` |
| REQ-4 | §§1.2, 3.4, 4; K34, K41, K42 | Names every producer and recorder parameter and its exact source field; adds an allocation-derived K33-record path; derives `COMMIT_1B` once from the sealed K33 record; forbids runtime reference resolution; compares the closed record’s first line with that independently frozen OID. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:72-82` |
| REQ-5 | §§1.2, 2.2, 5.1; K10, K13, K14 | Adds allocation-derived `GATEA_FINAL_RECORD_PATH` to 1a; Step 8A requires exactly one record there, no second record naming the 1a OID in the enumerated universe, and authority-identity/use-ordinal equality with 7C. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:84-94` |
| REQ-6 | §§1.5–1.7, 3.3; K18, K28 | Keeps exact tool paths, no-PATH resolution, and immediate rehash rules in 1a; moves the four measured digests to 1b root-channel bindings. The cost analysis states that an operator patch changes a 1b input and does not imply a new Gate-A run. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:96-106` |
| NIT-1 | §§1.1, 1.6; K06 | Replaces the name blacklist with a source whitelist: every 1a data value must be derivable from a named pre-run source. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:114` |
| NIT-2 | §1.8; K04, K05, K07, K12 | Moves the four nonexistent contracts into an honest `UNKNOWN` register; their checks cannot PASS until independent accepted sources exist. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:115` |
| NIT-3 | §§1.6, 7; K08 | Labels object/freeze agreement as identity bookkeeping, not independent content correctness; K31 owns independent exact-byte/D026 review. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:116` |
| NIT-4 | §1.9 | Carries the V2 category-1/category-2 split and Lead adjudication verbatim, including the rule that bounded native infrastructure records are disclosed, not blockers, while crossovers remain category 1. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:117` |
| NIT-5 | §1.4; K35 | States at the field definition that `OPERATOR_RECORD_PARENT_STATE` is a local live preflight observation, not a standing fact, and must be rechecked immediately before capture. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:118` |
| NIT-6 | §§5.2, 6.1 | Cites the runbook-V2 review as the provenance for Step 14A, the documentary-not-interlock scope, and truthful post-create quarantine states. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:119` |
| NIT-7 | §§2.1, 5.2; K10 | Step 7C records whether Gate-A uses the D2-pinned SSH identity. Reuse requires an owner sentence that deliberately widens D2; absence or inference is STOP. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:120` |
| F6 enforcement gap | §§2.2, 5.2, 6.2; K43 | Restores the seal-to-capture MainPID/start-time/executable/netns liveness comparison as an explicit check rather than half-state prose. | `C:\tmp\lane_out\AUD_TC1_VERDICT.md:137,147` |

# V2 repair choices and change log

2. **Volatile-six choice.** V2 uses outside sources where the review names them: accepted-build venv identity, A-4 `active`, and A-5 `NRestarts=0`. It labels MainPID, start-time ticks, and net namespace `producer-internal, no independent expectation exists`; K43 proves later continuity against a different producer but does not retroactively make their initial production independent. `MTC_COMMAND_CENTER/11_TRIAGE/BRIDGE_RELEASE_INTEGRATION_DESIGN_2026-08-15.md:239-250`; `C:\tmp\lane_out\AUD_TC1_VERDICT.md:54-58`
3. **Gate-A record path choice.** The fixed operator-side create-once path is `C:\WPI_ARTIFACTS\WPI_TRANSPORT_<BASE>\gatea\STAGE1_GATEA_FINAL_RECORD.tsv`, derived from the allocation `BASE`/`OPERATOR_RECORD_ROOT`. It predates the run in 1a and gives create-once semantics one collision point.
4. **Recorder provenance choice.** The recorder has one parameter, the allocation-derived path to the sealed K33 binding record. It derives one `COMMIT_1B` variable from that record and never resolves `HEAD`, a branch, a tag, or another reference for object identity. This directly carries the accepted F7 repair into the two-object chain. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/WPI_STAGE1_COMMIT1_PREREG_DRAFT_V2_2026-08-15.md:19,752-777`
5. **Tool volatility choice.** Only paths are procedure inputs in 1a. Digests are measured and bound in 1b immediately before use. This preserves no-PATH lookup and rehash-before-use without making a workstation security patch destroy the already completed staging evidence. `C:\tmp\lane_out\AUD_TC1_VERDICT.md:96-106`
6. **Carried sound controls.** Candidate binding still anchors on a fresh record that names the exact independently verified 1a OID; 1b remains the direct first-parent child; the 1a→1b delta remains allowlisted and blob-compared; grant #6 remains barred from 1a; K16 remains an independent cross-check and is strengthened, not replaced. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_DEPENDENCY_CYCLE_ANALYSIS_2026-08-16.md:118-128`

# Scope and authority boundary

This is a T1-classified documentation/design repair because its subject is a host/security-sensitive preregistration contract. This lane itself performs no host, network, SSH, deployment, service, credential, broker/exchange, ARM, order, TESTNET/mainnet, Pine, parity, MTC, trading, merge, push, Git-write, or economic action. It creates no acceptance, host-contact authority, staging authority, grant-#6 binding, credential permission, deployment permission, or economic permission. The owner authorized design, drafting, and review only. `MTC_COMMAND_CENTER/11_TRIAGE/OWNER_DECISIONS_2026-08-16_MORNING.md:3-12,28-38`

The design replaces the single-Commit-1 contract with two Git objects and one separately authorized pre-1b host stage:

- **Commit 1a** is a mechanically verified procedure commit. It freezes stable candidate expectations, allocation, exact procedure bytes, schemas, exact tool paths, fixed evidence paths, and explicit no-authority fences.
- A separately authorized fresh candidate-bound A-0..A-9 run produces one immutable final checkpoint record.
- **Commit 1b** is the direct first-parent child of 1a. It adds the fresh candidate binding and independently checked non-candidate bindings without changing procedure bytes.
- Grant #6 may be bound only to verified Commit 1b.

The original single-commit cycle remains correctly diagnosed: old Step 8 required verified Commit 1 while old Step 10 required Step-8-derived final-checkpoint values. Option A breaks that candidate-binding edge. With the privileged channel severed, no surviving check consumes mutation-denial evidence, so no second evidence dependency edge remains. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_DEPENDENCY_CYCLE_ANALYSIS_2026-08-16.md:102-128`

## 1. Commit 1a — procedure commit

### 1.1 Content and provenance-whitelist rule

Commit 1a contains exact procedure and schema bytes, but it is intentionally not executable as the grant-#6 capture subject. The procedure refuses socket open unless it is operating against a verified Commit 1b whose direct parent is this exact 1a and whose binding files pass §§3, 4, and 7.

Commit 1a contains **no data value** for MainPID, process start time, active state, restart count, running-process executable path or digest, running-process network namespace, or another live final-checkpoint observation. Field names may appear in schemas; no data row for them may appear. This preserves the stable/volatile split selected by Option A. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_DEPENDENCY_CYCLE_ANALYSIS_2026-08-16.md:118-128`

The controlling K06 property is a whitelist, not a field-name blacklist. Every 1a data value must be derivable byte-for-byte from exactly one named pre-run source class:

1. an owner-authored accepted design literal;
2. the independently frozen K02 candidate/T0 package or a formula whose only variable is a K02 field;
3. the independently verified append-only allocation event or a fixed formula whose only variable is an allocation field;
4. a predeclared exact procedure/schema/path universe independent of the manifest being checked;
5. an accepted operator-tool **path** inventory, never a measured tool digest; or
6. an explicit schema-state literal such as `UNKNOWN-until-admin-review`, which grants no authority and cannot appear in 1b.

Any 1a data value that lacks one of those named sources, depends on a run-time observation, or is derived from the object/manifest that it is supposed to check makes K06 STOP. The design-time candidate values remain `UNKNOWN-until-frozen-T0-candidate` outside the eventual 1a; they must be concrete before creation. The eight admin schema slots intentionally contain the literal `UNKNOWN-until-admin-review` in 1a and remain non-executable.

### 1.2 `COMMIT1A_CHAIN_CONTROL.tsv`

One ASCII, LF-only, final-LF header and one data row. Every literal comes from the accepted owner-selected design and independently verified allocation; the builder is not its own policy source.

| Field | Commit-1a content | Independent source | Mechanical verification |
|---|---|---|---|
| `CHAIN_SCHEMA_VERSION` | `WPI_OPTION_A_TWO_COMMIT_V2` | Owner Option A plus accepted V2 design | Parse one row and require literal equality. |
| `COMMIT_ROLE` | `PROCEDURE_1A` | Accepted design | Require literal equality and reject change in 1b. |
| `FINAL_COMMIT_ROLE` | `FINAL_PREREGISTRATION_1B` | Owner Option A | Require literal equality. |
| `FINAL_PARENT_POLICY` | `DIRECT_FIRST_PARENT_IS_1A` | Accepted design | Require `1b^1` to equal independently frozen 1a; extra parent or intervening commit blocks. |
| `PROCEDURE_IMMUTABILITY_POLICY` | `ALL_1A_PROCEDURE_BLOBS_IDENTICAL_IN_1B` | Option A anti-self-confirmation rule | Compare every procedure-member blob OID 1a↔1b. |
| `ALLOWED_1B_DELTA_PATHS_B64` | Canonical base64 of the sorted NUL-terminated §1.7 list | Accepted design/path universe, not 1b manifest | Decode/re-encode against the fixed accepted list and compare actual diff. |
| `CANDIDATE_BINDING_SOURCE_POLICY` | `FRESH_GATEA_FINAL_RECORD_ONLY` | Owner Option A | Candidate provenance may name only the one K14-sealed record. |
| `ALLOCATION_SOURCE_PATH` | `MTC_COMMAND_CENTER/11_TRIAGE/WPI_STAGE1_ALLOCATION/STAGE1_ALLOCATION_RECORD.tsv` | Existing exact allocation contract | Resolve in 1a and compare its blob to the procedure manifest. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/WPI_STAGE1_COMMIT1_PREREG_DRAFT_V2_2026-08-15.md:89-110` |
| `STABLE_EXPECTATIONS_PATH` | `MTC_COMMAND_CENTER/11_TRIAGE/WPI_STAGE1_COMMIT_CHAIN/STAGE1_STABLE_CANDIDATE_EXPECTATIONS.tsv` | Accepted design | Resolve in 1a and compare blob. |
| `GATEA_FINAL_RECORD_PATH` | `C:\WPI_ARTIFACTS\WPI_TRANSPORT_<BASE>\gatea\STAGE1_GATEA_FINAL_RECORD.tsv` | Fixed accepted formula plus allocation `BASE`/`OPERATOR_RECORD_ROOT` | Rebuild from the committed allocation row; independently typed path is forbidden. Require absence before the authorized run and create-once at this exact path. |
| `GATEA_FINAL_RECORD_USE_ORDINAL` | `1` | Accepted one-use policy | Compare 7C authority binding, Step-8 record, and K13 use census to canonical decimal `1`. |
| `GRANT6_BINDING_RECORD_PATH` | `C:\WPI_ARTIFACTS\WPI_TRANSPORT_<BASE>\authority\GRANT6_CAPTURE_AUTHORITY_BOUND.tsv` | Fixed accepted formula plus allocation `BASE`/`OPERATOR_RECORD_ROOT` | Rebuild from 1a allocation; this is the recorder’s sole path parameter source. |
| `FINAL_CANDIDATE_BINDING_PATH` | `MTC_COMMAND_CENTER/11_TRIAGE/WPI_STAGE1_COMMIT_CHAIN/STAGE1_CANDIDATE_BINDING.tsv` | Candidate schema plus split design | Require absent in 1a and present only in 1b. |
| `FINAL_ROOT_BINDING_PATH` | `MTC_COMMAND_CENTER/11_TRIAGE/WPI_STAGE1_COMMIT_CHAIN/STAGE1_ROOT_CHANNEL_BINDING.tsv` | Root-channel schema plus split design | Require absent in 1a and present only in 1b. |
| `COMMIT1A_HOST_AUTHORITY` | `NONE` | Owner Option A design-only sentence | Require literal equality and scan all authority fields for conflict. |
| `COMMIT1A_GRANT6_BINDING_STATE` | `PROHIBITED` | Owner Option A | Any grant record naming 1a blocks. |
| `GATE_A_AUTHORITY_REQUIREMENT` | `SEPARATE_OWNER_RECORD_REQUIRED` | Existing authority separation | K10 must bind an independent owner sentence before Step 8. |
| `UNKNOWN_POLICY` | `STOP_NEVER_FAIL` | Defect Pattern 1 | Absent/unreadable/unevaluable input is STOP, never host-state FAIL. `MTC_COMMAND_CENTER/11_TRIAGE/DESIGN_DEFECT_PATTERNS_2026-08-10.md:48-107` |

### 1.3 `STAGE1_STABLE_CANDIDATE_EXPECTATIONS.tsv`

Every value is fixed from the exact frozen candidate/T0 package before 1a. Today candidate-dependent values are `UNKNOWN-until-frozen-T0-candidate`; no value is guessed, and that token cannot survive into a created 1a data row. Expected identity comes from the frozen package, never the later Gate-A producer. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_DEPENDENCY_CYCLE_ANALYSIS_2026-08-16.md:118-128`

| Field | Required 1a content | Independent source | Mechanical verification |
|---|---|---|---|
| `STAGING_HOST` | `GATEA-STAGING` | Owner host boundary/integration contract | Literal comparison and `PRODUCTION_HOST_EXCLUSION=KVM2`. |
| `PRODUCTION_HOST_EXCLUSION` | `KVM2` | Disposable-host rule | Pre-run host identity must not equal production KVM2. |
| `WPI_CANDIDATE_SHA` | Exact 40-lowercase-hex frozen candidate commit | Independently T0-accepted candidate record | Read frozen source manifest and compare exact bytes. |
| `CANDIDATE_TREE_OID` | Exact Git tree OID | Frozen candidate object | Resolve `<candidate>^{tree}` and compare; inability is STOP. |
| `CANDIDATE_ARTIFACT_SHA256` | Exact 64-lowercase-hex artifact digest | Reproducible build/T0 evidence | Independently hash the frozen artifact and compare. |
| `CANDIDATE_MANIFEST_SHA256` | Exact manifest digest | Reproducible build/T0 evidence | Hash force-enumerated manifest bytes. |
| `CANDIDATE_MANIFEST_BLOB_OID` | Exact Git blob OID | Frozen candidate commit | Compare Git object and separately hashed on-disk form. |
| `UNIT_NAME` | `mtc-bridge-first-start.service` | Accepted integration/Gate-A contract | Literal comparison to frozen unit/package input. |
| `EXPECTED_MAINPID_CWD` | `/opt/mtc-bridge/releases/<WPI_CANDIDATE_SHA>/IBKR_PAPER_BRIDGE` | Candidate SHA plus accepted path formula | Rebuild from K02 SHA. |
| `EXPECTED_MAINPID_CMDLINE_B64` | Canonical base64 of `/opt/mtc-bridge/venvs/<sha>/bin/python\0-m\0bridge.app\0` | Candidate SHA plus accepted command contract | Independently rebuild NUL bytes and canonical base64. |
| `EXPECTED_MAINPID_CMDLINE_SHA256` | SHA-256 of exact command-line bytes | Independently rebuilt bytes | Recompute; do not copy candidate-row digest. |
| `EXPECTED_MAINPID_EXE_PATH` | Exact accepted `/proc/<MainPID>/exe` link identity for the candidate venv interpreter | Frozen candidate SHA plus accepted build/venv manifest | Derive the venv interpreter object/link identity from accepted build evidence; do not observe it from the Gate-A runner. |
| `EXPECTED_MAINPID_EXE_SHA256` | Exact 64-lowercase-hex digest of the accepted venv interpreter bytes | Accepted build/T0 artifact evidence independent of Gate-A runner | Hash the accepted build member and bind its path/member identity; K16 compares the running-process observation to this value. |
| `GATE_A_RUN_KIT_BLOB_OID` | Exact accepted run-kit identity | Independent accepted run-kit manifest | Resolve every run-kit member; manifest-only universe is insufficient. |
| `T0_ACCEPTANCE_RECORD_BLOB_OID` | Exact two-flagship accepting record identity | Independent T0 auditors/frozen source | Verify exact models, efforts, execution, and verdicts. |

No MainPID, start time, active state, restart count, or network namespace appears here. Executable identity is stable build identity, not a live process observation. The independent A-4/A-5 terminal records for `active` and `NRestarts=0` are produced after 1a and are compared by K16; they are not inserted into 1a.

### 1.4 Concrete allocation record included in 1a

Allocation moves before 1a because it is local and not intrinsically derived from A-0..A-9. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_DEPENDENCY_CYCLE_ANALYSIS_2026-08-16.md:35-39`

| Field | Source | Mechanical verification |
|---|---|---|
| `ATTESTATION_RECORD_ID` | Append-only allocator over independent collision universe | Require `WPI-[0-9]{8}T[0-9]{6}Z-[0-9a-f]{8}-ATT-01`; prove absent from every independently enumerated source before mint. |
| `BASE` | Same allocator event | Require `WPI-[0-9]{8}T[0-9]{6}Z-[0-9a-f]{8}` and exact record-ID prefix. |
| `P0_RUNID` | Allocation `BASE` | Require `<BASE>-P0`. |
| `RO_RUNID` | Allocation `BASE` | Require `<BASE>-RO`. |
| `REMOTE_BASE` | Accepted formula plus `BASE` | Require `/home/gatea/wpi_staging_<BASE>`. |
| `CONFIRM_TOKEN` | Accepted formula plus `BASE` | Require `<BASE>-EXECUTE`. |
| `OPERATOR_RECORD_ROOT` | Accepted formula plus `BASE` | Require `C:\WPI_ARTIFACTS\WPI_TRANSPORT_<BASE>` and literal non-reparse resolution. |
| `OPERATOR_RECORD_PATH` | Root, record ID, accepted child rule | Rebuild once as `<root>\attestation\<record-id>.record`; independent typing forbidden. |
| `OPERATOR_RECORD_PARENT_STATE` | Local filesystem preflight | Require `EXISTS_EMPTY_CREATE_ONCE` from independently captured preflight. **This is a local live observation at allocation time, not a standing fact. The 1a→capture interval is longer than the former contract; K35 must recheck the parent/path immediately before capture, and drift is STOP.** |
| `COLLISION_RESULT` | Independent ledger/root/remote allocation universe | Require `NO_COLLISION_ALL_CANONICAL_SOURCES` only after all accepted source classes reconcile. The completeness policy is currently `UNKNOWN`. |
| `ALLOCATION_DISPOSITION` | Enforced append-only transition | Require `RESERVED`, predecessor-prefix proof, and an independently accepted durable burn path. That burn path is currently `UNKNOWN`, so minting is presently blocked. |

Collision completeness and append-only enforcement require discriminating evidence; declarations are not checks. `MTC_COMMAND_CENTER/11_TRIAGE/SELF_CONFIRMING_CHECK_PATTERN_2026-08-15.md:16-23,45-63`

### 1.5 Frozen operator-tool paths; digests deferred to 1b

Commit 1a freezes exact absolute paths and the execution rule. It does **not** freeze maintenance-volatile digests.

| Field | Required 1a content | Independent source | Mechanical verification |
|---|---|---|---|
| `POWERSHELL_EXE_PATH` | `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe` | Accepted operator-tool path inventory | Exact normalized absolute path; no PATH lookup. |
| `OPERATOR_PYTHON_PATH` | `C:\Python314\python.exe` | Accepted operator-tool path inventory | Exact absolute path; no PATH lookup. |
| `GIT_EXE_PATH` | `C:\Program Files\Git\cmd\git.exe` | Accepted operator-tool path inventory | Exact absolute path; no PATH lookup. |
| `SSH_EXE_PATH` | `C:\Windows\System32\OpenSSH\ssh.exe` | Accepted operator-tool path inventory | Exact absolute path; no PATH lookup. |

`COMMIT1A_OPERATOR_TOOLS.tsv` retains these path rows and the rules `PATH_LOOKUP=PROHIBITED`, `REHASH_IMMEDIATELY_BEFORE_EACH_USE=REQUIRED`, and `DIGEST_BINDING_LOCATION=COMMIT1B_ROOT_CHANNEL_BINDING`. The four digests are measured into 1b and rehashed again immediately before their corresponding use. An absent, reparse-resolved, wrong-kind, or unreadable path is STOP.

### 1.6 Procedure member manifest and schemas

The force-inclusive 1a member universe comes from an independently accepted design, not the manifest it checks. It contains chain control, stable expectations, allocation, `COMMIT1A_OPERATOR_TOOLS.tsv`, the parameterized target producer, parameterized operator recorder, launcher, candidate schema, root-channel schema, preregistration text, and procedure manifest. The no-authority fields live in chain control; no second authority record may disagree.

Each non-manifest row contains:

| Field | Source and verification |
|---|---|
| `ordinal` | Accepted force-inclusive list; canonical positive decimal and exact order. |
| `path` | Accepted list; compare to force-inclusive `git ls-tree`; extra/hidden/missing is STOP. |
| `bytes` | Actual member bytes; recount. This is identity bookkeeping, not independent correctness. |
| `sha256` | Freeze record of the same actual bytes; recompute. This is identity bookkeeping, not an independent content expectation. |
| `git_blob_oid` | Git read-back; compare 1a:path with `hash-object --no-filters` and retain dual forms. |
| `role` | Accepted member-role map; exactly one role and terminal disposition. |

K08 therefore answers **No for content correctness**. It can prove only that the created object matches a pre-create byte freeze, and only if the freeze is made before creation and controlled by a reviewer independent of the object creator. K31, not K08, owns independent content review and D026. `MTC_COMMAND_CENTER/11_TRIAGE/SELF_CONFIRMING_CHECK_PATTERN_2026-08-15.md:16-23,45-63`

The candidate schema lists all §3.2 fields but has no data row in 1a. The root-channel schema carries the eight unresolved admin slots:

| Admin fact | Exact 1a schema value | Concrete 1b fields |
|---|---|---|
| SSH principal | `UNKNOWN-until-admin-review` | `SSH_TARGET`, `SERVER_AUTHORIZED_KEY_BINDING` |
| Account shell | `UNKNOWN-until-admin-review` | `ACCOUNT_UID_GID`, `ACCOUNT_SHELL_PATH`, `ACCOUNT_SHELL_SHA256` |
| Forced command | `UNKNOWN-until-admin-review` | `FORCED_COMMAND_MODE`, `FORCED_COMMAND_EXACT_ARGV_B64` or proved `NONE` |
| Wrapper mapping | `UNKNOWN-until-admin-review` | `ROOT_WRAPPER_PATH`, `ROOT_WRAPPER_SHA256`, `ATT01_TARGET_PROCESS_CHAIN`, `ATT01_REMOTE_COMMAND` |
| Pre-`env` environment | `UNKNOWN-until-admin-review` | `ATT01_PRE_ENVIRONMENT_B64` |
| Initial cwd | `UNKNOWN-until-admin-review` | `ATT01_INITIAL_CWD` |
| Descriptor set | `UNKNOWN-until-admin-review` | `ATT01_STDIN_MAPPING`, `ATT01_FD_MAPPING` |
| Mutation-denial control | `UNKNOWN-until-admin-review` | `TARGET_MUTATION_DENIAL_CONTROL` |

All eight remain unestablished. Under §DISCLOSURE, the mutation-denial slot is resolved only as `NO_MUTATION_DENIAL_EVIDENCE`; the former adjacent crossover-evidence input is not part of 1b. Agreement among a launcher, recorder, and TSV cannot establish the other facts. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/ROOT_CHANNEL_RECORD_GAP_2026-08-15.md:172-195,197-214`

### 1.7 Exact 1b delta allowlist and tool-volatility cost analysis

Only these paths may be added or changed from 1a to 1b:

1. `STAGE1_GATEA_FINAL_RECORD.tsv` — sealed copy/provenance of the unique fresh record.
2. `STAGE1_CANDIDATE_BINDING.tsv` — concrete final candidate row.
3. `STAGE1_ROOT_CHANNEL_BINDING.tsv` — concrete independently checked root/tool/channel row, including tool digests.
4. `COMMIT1B_CHAIN_BINDING.tsv` — 1a/1b ancestry, delta, and input identities.
5. `COMMIT1B_FINAL_PREREGISTRATION.md` — final exact human-readable contract.
6. `COMMIT1B_PACKAGE_MANIFEST.tsv` — final complete package identity.

No producer, recorder, launcher, schema, allocation, stable-expectations, tool-**path**, 1a authority-scope, or procedure-manifest byte may change. A procedure/path change requires a new 1a and, where applicable, a new staging authority, fresh A-0..A-9 run, and 1b.

The four tool **digests are not 1a procedure bytes**. They are 1b root-channel binding values measured from the 1a-pinned absolute paths. If an OS patch replaces a tool after Gate-A, K28 binds the new digest to 1b and immediately rehashes it before use; the completed fresh Gate-A record remains eligible for the same 1a/candidate chain, and no new Gate-A run is implied. If a tool changes after 1b or between rehash and use, K28/K34 STOP before socket and a new valid 1b/grant binding is needed. This design trades a maintenance-volatile 1b input for preserved staging evidence; it does not weaken executable identity because the use-time digest remains exact. `C:\tmp\lane_out\AUD_TC1_VERDICT.md:96-106`

Commit 1a verification remains: clean exact source state; K06 source whitelist; force-inclusive member reconciliation; exact candidate/T0 and tool-path checks; D026 for executable checks; exact-byte review; create 1a; read it back; prove dual forms and clean current state. Pre-create failure is `COMMIT1A_NOT_STARTED_STOP`. Possible creation followed by non-PASS read-back is `COMMIT1A_CREATED_UNVERIFIED_QUARANTINE`; preserve the observed object/reference and do not amend, reset, delete, rewrite, or use it.

### 1.8 Honest `UNKNOWN` contract register

The following contracts do not yet exist and cannot be described as accepted:

| Contract needed by | Current state | Consequence |
|---|---|---|
| K04 allocation completeness policy/authority | `UNKNOWN` | Allocation cannot be minted. |
| K05 durable append-only burn path | `UNKNOWN` | A post-mint failure has no accepted closure; minting cannot start. |
| K07 independently accepted force-inclusive 1a path/role universe | `UNKNOWN` | 1a member conservation cannot PASS. |
| K12 independently accepted Gate-A C11 safe-close contract | `UNKNOWN` | Step 7C and Step 8 are unreachable. |
| K10 exact `GATE_A_STAGE_AUTHORITY` | `UNKNOWN` | Step 8 is unreachable. |
| Eight admin facts | `UNKNOWN` | Step 9/1b creation is unreachable until the independently sourced facts are concrete; the mutation-denial slot resolves only under §DISCLOSURE. |

These are honest STOP states, not inferred policy. The runbook review records the allocation completeness authority/evidence as operationally open. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_TO_AUDIT2_RUNBOOK_V2_REVIEW_2026-08-15.md:90-99`

### 1.9 Controlling category split and Lead adjudication — carried verbatim

**REJECTED IN PART under the Lead adjudication; retained in part.** The review merged category 1 target mutation with category 2 inherent infrastructure records. Category 2 is disclosed and is not a blocker. Category 1 remains strict.

#### Category 1 — target mutation

For this preregistration, category 1 means any effect, other than the narrowly enumerated category-2 records below, that creates, deletes, renames, modifies, truncates, changes permissions/ownership/xattrs, rotates, moves, or reconfigures content or persistent configuration on `GATEA-STAGING`.

Category 1 must be impossible for each command and must be demonstrated before Commit 1. Source inspection alone proves only that the visible producer requests read-only operations; it does not prove the interpreter, loader, account shell, root wrapper, PAM hooks, audit rules, or failure paths cannot mutate. The defect catalogue requires the claim sentence to stay within the predicate and requires the executed instrument, not merely a declared instrument, to be bound. `MTC_COMMAND_CENTER/11_TRIAGE/DESIGN_DEFECT_PATTERNS_2026-08-10.md:547-613,817-849`

Current category-1 status:

| Surface | Required demonstration | Current fact |
|---|---|---|
| Target producer source | No write/create/delete/rename/chmod/chown/xattr/mount/service/socket/subprocess API; file opens are read-only; only fd 1/2 are written. | Demonstrated by the normative producer source, but source inspection is not chain execution proof. |
| Python, loader, and standard-library runtime before and during the producer | Exact runtime identity plus an enforced control that denies content/configuration mutation on success and every failure path while allowing inherited stdout/stderr. | `UNKNOWN`. No source establishes the exact enforcement control. |
| Account shell, forced command, sudo/root wrapper, or equivalent pre-`env` chain | Exact chain and a demonstrated no-mutation contract for each process before `env -i`. | `UNKNOWN`. The grant names a root session but not its mechanism. |
| Native auth/audit record rollover | The bounded category-2 append must not trigger log rotation, journal vacuum/rotation, audit rule action outside its native store, or another content/configuration mutation. | `UNKNOWN`. This is a plausible category-2-to-category-1 crossover and therefore blocks 1b. |
| PAM/root-wrapper hooks | No hook may create a home object, refresh credentials, update an unrelated database, execute a profile, or invoke another side-effecting program. | `UNKNOWN`; blocks 1b. |

No operator may convert an `UNKNOWN` in this table into a claim of impossibility. A final 1b must cite the exact committed root-channel binding/proof record that closes every row.

#### Category 2 — inherent infrastructure records

The following records are inherent to the owner's authorized use of the pinned SSH identity. They are disclosed, not claimed absent, and are not blockers by themselves:

| Subsystem | Expected record from an authorized read-only session | Exact host implementation |
|---|---|---|
| `sshd` / system journal or auth log | Connection attempt and success/failure, authenticated principal/key identity or fingerprint as configured, source endpoint, session open/close, and possibly the requested command. | Exact backend, fields, path, retention, and verbosity are `UNKNOWN`. |
| PAM | Authentication/account/session module records and session-open/session-close accounting when the configured stack emits them. | Exact module stack and stores are `UNKNOWN`. |
| `auditd` / kernel audit | Login/session/user-command/syscall records selected by installed audit rules. | Whether enabled, exact rules, store, and record set are `UNKNOWN`. |
| Root wrapper / `sudo` / forced command | Invocation and policy decision records produced by the exact root-channel mechanism. | The mechanism itself is `UNKNOWN`. |
| Login accounting | `wtmp`, `btmp`, `lastlog`, journal equivalents, or other native login accounting when enabled. | Which stores are enabled is `UNKNOWN`. |
| Filesystem metadata | Access-time metadata that the active mount/filesystem policy may update because the interpreter, libraries, `/proc` objects, executable, and observation inputs are ordinarily read. | Mount atime policy and which reads update it are `UNKNOWN`. |

These bounded records are consistent with the permission because the permission expressly authorizes use of the pinned identity for the exact capture on `GATEA-STAGING`; they are evidence that the authorized login/read occurred, not producer-directed host content/configuration work. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_OWNER_DECISIONS_2026-08-15_NIGHT.md:35-58`

The disclosure is not a waiver for crossovers. A native log append is category 2; a rollover/rotation/vacuum caused by it, an audit rule writing outside its native audit store, a PAM hook changing unrelated state, or a wrapper updating configuration is category 1 and must be denied or proven impossible before 1b. This section carries the controlling V2 split and Lead adjudication. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/WPI_STAGE1_COMMIT1_PREREG_DRAFT_V2_2026-08-15.md:14,31-66`

## 2. The staging run

### 2.1 Separate Gate-A authority

Fresh A-0..A-9 is covered only by a separately owner-authored, mechanically bound `GATE_A_STAGE_AUTHORITY`. **No such exact sentence currently exists.** It must name exact candidate, disposable `GATEA-STAGING`, A-0..A-9, credential/channel, permitted mutations, mandatory close/safe-state contract, time/use limit, and exclusions. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_TO_AUDIT2_EXECUTION_RUNBOOK_V2_2026-08-15.md:83-92`

Option A is design authority only. Grant #6 is a different one-command-set read-only authority. Gate-A includes install, service start, SIGKILL, and explicit restart activity and cannot inherit grant #6. `MTC_COMMAND_CENTER/11_TRIAGE/OWNER_DECISIONS_2026-08-16_MORNING.md:28-38`; `MTC_COMMAND_CENTER/11_TRIAGE/BRIDGE_RELEASE_INTEGRATION_DESIGN_2026-08-15.md:239-250`

Step 7C must surface and bind this owner question: **Does Gate-A use the same pinned SSH identity named by D2?** If yes, D2’s “solely for” wording does not cover Gate-A; the new owner sentence must deliberately widen that identity’s use to the exact A-0..A-9 action. If no, the new sentence must bind the different exact identity/channel. Missing evidence or inference is STOP. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_OWNER_DECISIONS_2026-08-15_NIGHT.md:30-38,54-60`

### 2.2 Unique fresh Gate-A final record

The staging runner must create one immutable record after fresh A-0..A-9. No historical PASS transfers. `MTC_COMMAND_CENTER/11_TRIAGE/BRIDGE_RELEASE_INTEGRATION_DESIGN_2026-08-15.md:229-255`

The only record path is the 1a field `GATEA_FINAL_RECORD_PATH`. Step 8A requires:

1. exactly one regular create-once record at that literal non-reparse path;
2. the record’s `COMMIT1A_OID` equals the independently frozen verified 1a OID;
3. its `GATE_A_STAGE_AUTHORITY_IDENTITY` equals the exact sentence bound at 7C;
4. its `GATE_A_STAGE_AUTHORITY_USE_ORDINAL` equals both 1a’s canonical `1` and the 7C bound ordinal;
5. a force-inclusive independent enumeration over the accepted allocation ledger, literal `OPERATOR_RECORD_ROOT`, retained Gate-A evidence roots, and declared remote-allocation evidence contains no second record naming this 1a OID; and
6. inability to enumerate any source class is STOP, not evidence of uniqueness.

The final record contains:

- schema/version, run ID, byte count, SHA-256, exact create-once path, close/fsync result, and one terminal disposition;
- exact verified 1a OID and procedure-manifest blob;
- exact Gate-A authority identity and use ordinal;
- exact `GATEA-STAGING` identity and KVM2 exclusion;
- exact candidate commit/tree, artifact digest, manifest identity, and run-kit identity;
- one terminal disposition/evidence identity for A-0..A-9 with no historical reuse;
- safe-final-host-state predicate/result;
- `CHECKPOINT_ID=STAGE1_EXACT_CANDIDATE_A0_A9_FINAL`;
- all §3.2 fields and raw observation evidence captured at the final checkpoint; and
- an explicit staging-only verdict with no implication of grant #6, WP-I, production, merge, credential expansion, ARM, broker, order, TESTNET/mainnet, or economic authority.

`UNIT_ACTIVE_STATE=active` must trace to the independently contracted A-4 terminal disposition; `UNIT_NRESTARTS=0` must trace to A-5. Executable path/digest must compare to §1.3 accepted-build identity. MainPID, start-time ticks, and network namespace remain staging-runner-produced facts with no producer-independent initial expectation. The record must say so.

K43 later compares the sealed MainPID/start-time/cwd/cmdline/executable/netns tuple with the separately executed ATT-01 capture before socket consumption can be called continuous. It establishes seal-to-capture continuity relative to the capture producer; it does not make the Gate-A producer’s initial observation independent.

## 3. Commit 1b — final exact preregistration

### 3.1 Entry gate and immutable relationship to 1a

Commit 1b may be attempted only after:

1. 1a is mechanically verified and independently frozen by OID.
2. Gate-A authority/preflight/safe-close passed before Step 8.
3. Fresh A-0..A-9 produced the unique K14-sealed record at the 1a path and K10/K13 use-count checks passed.
4. Allocation remains reserved and unchanged.
5. All eight admin facts are concrete and independently sourced.
6. Tool paths/digests, exact argv/environment/cwd/FD chain, review, and all remaining checks pass.

Candidate binding comes only from the unique fresh record. Every other input has its own source/check. The final object is the direct first-parent child of exact 1a, and the actual diff equals §1.7. Grant #6 remains barred from 1a. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_DEPENDENCY_CYCLE_ANALYSIS_2026-08-16.md:118-128`

### 3.2 `STAGE1_CANDIDATE_BINDING.tsv`

Every value is copied byte-for-byte from the sealed record; there is no fallback source. The final column is deliberately named **Verification / independence classification**, not “Independent verification.”

| Field | Sole producer source | Verification / independence classification |
|---|---|---|
| `STAGING_HOST` | Fresh final record | Independent expectation: 1a literal `GATEA-STAGING`; reject KVM2. |
| `CHECKPOINT_ID` | Fresh final record | Independent policy literal `STAGE1_EXACT_CANDIDATE_A0_A9_FINAL`. |
| `WPI_CANDIDATE_SHA` | Fresh final record | Independent expectation: 1a/K02 T0 candidate. |
| `UNIT_NAME` | Fresh final record | Independent expectation: frozen unit/run kit. |
| `UNIT_ACTIVE_STATE` | Fresh final record | Independent expectation: A-4 terminal disposition must say `active`; final manager observation alone is not the expectation. |
| `UNIT_NRESTARTS` | Fresh final record | Independent expectation: A-5 terminal disposition must say canonical `0`; final manager observation alone is not the expectation. |
| `WPI_MAINPID` | Fresh final record | **Producer-internal, no independent expectation exists.** Require canonical positive decimal and raw checkpoint binding; K43 later checks continuity against a different producer. |
| `WPI_MAINPID_STARTTIME_TICKS` | Fresh final record | **Producer-internal, no independent expectation exists.** Parse field 22 from complete recorded `/proc/<pid>/stat`; K43 later checks continuity. |
| `WPI_MAINPID_CWD` | Fresh final record | Independent expectation: 1a `EXPECTED_MAINPID_CWD`. |
| `WPI_MAINPID_CMDLINE_SHA256` | Fresh final record | Independent expectation: rebuild exact NUL bytes from 1a and hash. |
| `WPI_MAINPID_EXE_PATH` | Fresh final record | Independent expectation: 1a accepted-build `EXPECTED_MAINPID_EXE_PATH`; raw link consistency is supplemental. |
| `WPI_MAINPID_EXE_SHA256` | Fresh final record | Independent expectation: 1a accepted-build `EXPECTED_MAINPID_EXE_SHA256`; recomputation over producer-sealed bytes is supplemental and is not its own expectation. |
| `WPI_MAINPID_NET_NS` | Fresh final record | **Producer-internal, no independent expectation exists.** Require `net:[<positive-canonical-decimal>]` and raw-link consistency; K43 later checks continuity. |

K15 proves only record→TSV byte-copy fidelity. K16 independently compares host, candidate, unit, cwd, cmdline, accepted-build executable path/digest, A-4 active state, and A-5 restart count. It does not claim an independent initial expectation for MainPID, start time, or net namespace.

### 3.3 Non-candidate inputs and `STAGE1_ROOT_CHANNEL_BINDING.tsv`

| Input | Independent source | Required verification before 1b |
|---|---|---|
| Allocation | Verified 1a allocation | Blob unchanged; K04/K05 contracts must exist and pass. |
| Procedure/recorder/launcher/schemas | Verified 1a manifest/object | Blob-for-blob identity; zero procedure delta. |
| Stable candidate expectations | Frozen T0 package | 1a blob unchanged; K16 comparisons pass. |
| Operator tool paths | 1a path inventory | Exact path/no-PATH rule unchanged. |
| Operator tool digests | Immediate independent rehash of exact 1a paths for 1b | Store measured digests in root binding and rehash again immediately before each use. |
| SSH principal/key mapping | Admin decision plus authoritative server configuration | Compare selected route and key-to-principal evidence. |
| Account shell | Authoritative account/provisioning record | Exact path, numeric identity, kind, owner/mode, digest. |
| Forced command | Effective sshd/authorized-key mapping | Byte-exact argv or independently proved `NONE`. |
| Wrapper/process chain | Admin-selected route plus configuration proof | Complete ordered exec chain; no ellipsis or unbound program. |
| Pre-`env` environment | Independent first-process/config record | Canonical sorted `name=value\0`; missing observation STOP. |
| Initial cwd | Independent first-process/config record | Exact cwd before shell startup/import/`chdir`. |
| Descriptor set | Independent first-process FD inventory | Exact 0/1/2 and no extra inherited writable FD. |
| Mutation-denial | Owner-approved accelerated contract and §DISCLOSURE | 1b attests `NO_MUTATION_DENIAL_EVIDENCE`; no control or independently proven read-only capture is claimed. |
| Final review/evidence | Reviewer independent of builder | Exact bytes, executable RED/GREEN, accepting required verdict. |

The root binding is one ASCII/LF/final-LF row. Agreement with the recorder is never sufficient by itself.

| Field | Expected source | Required 1b verification |
|---|---|---|
| `POWERSHELL_EXE_PATH` | Unchanged 1a tool path | Exact bytes; no PATH. |
| `POWERSHELL_EXE_SHA256` | Immediate read-only hash of exact path | Store in 1b; K28 rehash before launcher use. |
| `OPERATOR_PYTHON_PATH` | Unchanged 1a tool path | Exact bytes; no PATH. |
| `OPERATOR_PYTHON_SHA256` | Immediate read-only hash | Store in 1b; K28 rehash before recorder use. |
| `GIT_EXE_PATH` | Unchanged 1a tool path | Exact bytes; no PATH. |
| `GIT_EXE_SHA256` | Immediate read-only hash | Store in 1b; K28 rehash before Git use. |
| `SSH_EXE_PATH` | Unchanged 1a tool path | Exact bytes; no PATH. |
| `SSH_EXE_SHA256` | Immediate read-only hash | Store in 1b; K28 rehash immediately before socket. |
| `SSH_TARGET` | Admin-selected principal/address plus server config | Exact mapping and K19 key binding. |
| `SERVER_AUTHORIZED_KEY_BINDING` | Authoritative server key mapping | Pinned identity maps only to selected route. |
| `ACCOUNT_UID_GID` | Authoritative account/escalation config | Exact numeric identity for each hop. |
| `ACCOUNT_SHELL_PATH` | Authoritative account record | Absolute path, kind, numeric owner/mode, selected use. |
| `ACCOUNT_SHELL_SHA256` | Independently read shell bytes | Recompute; launcher copy is insufficient. |
| `FORCED_COMMAND_MODE` | Effective sshd/key mapping | Exactly `NONE` or `EXACT`; prove `NONE`. |
| `FORCED_COMMAND_EXACT_ARGV_B64` | Effective mapping | Canonical NUL argv; empty only for proved `NONE`. |
| `ROOT_WRAPPER_PATH` | Admin route/config | Exact absolute path or proved `NONE`; no PATH. |
| `ROOT_WRAPPER_SHA256` | Independently read wrapper | Recompute or literal `NONE`. |
| `ATT01_TARGET_PROCESS_CHAIN` | Verified account/sshd/wrapper mapping | Complete ordered chain through `/usr/bin/env` and Python. |
| `ATT01_INITIAL_CWD` | Independent first-process record | Exact absolute cwd before startup/import/`chdir`. |
| `ATT01_PRE_ENVIRONMENT_B64` | Independent first-process environment | Canonical sorted bytes; reject hooks/loaders. |
| `ATT01_POST_ENVIRONMENT_B64` | Accepted procedure | Rebuild `HOME=/root\0LC_ALL=C\0PATH=/usr/bin:/bin\0PYTHONDONTWRITEBYTECODE=1\0` in sorted order. |
| `ATT01_STDIN_MAPPING` | Procedure plus independent channel mapping | Exact `producer_blob_to_python_stdin_no_prefix_or_suffix`. |
| `ATT01_FD_MAPPING` | Independent complete FD inventory | Exact `0=ssh-stdin,1=ssh-stdout,2=ssh-stderr,all_other_fds=closed`. |
| `ATT01_REMOTE_COMMAND` | Effective forced/wrapper mapping plus committed rows | K40 reconstructs complete bytes; no placeholder/metavariable/independent expansion. |
| `ATT01_OUTER_ARGV_B64` | Exact committed 1a/1b fields plus fixed procedure/tool paths | K40 reconstructs canonical NUL-separated Windows argv with final NUL. |
| `ATT01_OUTER_ARGV_SHA256` | K40 reconstructed bytes | Recompute; recorder digest is not the expectation. |
| `TARGET_MUTATION_DENIAL_CONTROL` | Owner-approved accelerated contract and §DISCLOSURE | Exact literal `NO_MUTATION_DENIAL_EVIDENCE`; no mutation-denial control is claimed. |

### 3.4 Exact parameter provenance, argv equality, and single commit variable

The target producer’s post-`env` argv remains exactly:

```text
/usr/bin/python3 -I -S -B - --record-id <ATTESTATION_RECORD_ID> --candidate <WPI_CANDIDATE_SHA> --allocation-parent /home/gatea --main-pid <WPI_MAINPID> --main-starttime <WPI_MAINPID_STARTTIME_TICKS> --producer-sha256 <PRODUCER_SHA256>
```

Every producer parameter has one source:

| Producer token | Exact committed source |
|---|---|
| `--record-id` | 1a allocation `ATTESTATION_RECORD_ID` |
| `--candidate` | 1b candidate `WPI_CANDIDATE_SHA` |
| `--allocation-parent` | Exact parent `/home/gatea` derived from 1a allocation `REMOTE_BASE=/home/gatea/wpi_staging_<BASE>`; no independently typed copy |
| `--main-pid` | 1b candidate `WPI_MAINPID` |
| `--main-starttime` | 1b candidate `WPI_MAINPID_STARTTIME_TICKS` |
| `--producer-sha256` | 1a procedure-manifest `sha256` row for exact `capture_wpi_attestation.py` blob |

The fixed executable/flag tokens `/usr/bin/python3 -I -S -B -` come only from the unchanged 1a producer procedure and are additionally bound through the root-channel executable chain. `ATT01_REMOTE_COMMAND` is serialized from those fixed tokens plus the table fields. `ATT01_OUTER_ARGV_B64` is serialized from the unchanged launcher/recorder procedure, 1a absolute tool paths/allocation row, 1b candidate row, and root-channel route fields. K40 decodes, parses, rebuilds, re-encodes, and requires byte-for-byte equality for **every** token in both representations. A duplicate, omitted, reordered, transformed, shell-expanded, stale, or independently typed token is STOP before any socket.

The operator recorder has exactly one parameter:

| Recorder token | Exact source |
|---|---|
| `--grant6-binding-record` | 1a chain-control `GRANT6_BINDING_RECORD_PATH`, itself derived from 1a allocation `BASE`/`OPERATOR_RECORD_ROOT` |

No recorder parameter may supply an object OID directly. The recorder opens that exact create-once K33 record, verifies its schema, close/hash, authority source, `GRANT6_CAPTURE_AUTHORITY_BOUND=PASS`, exact 1a OID, host/action/identity/exclusions/use ordinal, and then assigns exactly once:

```text
COMMIT_1B := K33_RECORD.COMMIT1B_OID
```

Every later Git object expression is `<COMMIT_1B>` or `<COMMIT_1B>:<path>`. Runtime resolution of `HEAD`, a branch, tag, symbolic ref, upstream, current object, or launcher-supplied OID for object identity is forbidden. Clean/current-package checks compare raw worktree/index/untracked state and exact package bytes directly against `COMMIT_1B`; they do not derive identity from current references. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_PREREG_DRAFT_ROUND1/WPI_STAGE1_COMMIT1_PREREG_DRAFT_V2_2026-08-15.md:19,752-777`

Before socket, K41 proves this provenance and K34 proves the package equals the exact object. After close, K42 requires first line `attestation_prereg_commit=<COMMIT_1B>\n` byte-for-byte against the independently frozen K33 OID.

### 3.5 `COMMIT1B_CHAIN_BINDING.tsv` and final verification

The chain row contains `COMMIT1A_OID`, `COMMIT1A_PROCEDURE_MANIFEST_BLOB`, `COMMIT1B_PARENT_OID`, `ALLOWED_DELTA_PATHS_SHA256`, `ACTUAL_DELTA_PATHS_SHA256`, `GATEA_FINAL_RECORD_SHA256`, `GATEA_AUTHORITY_IDENTITY`, `GATEA_AUTHORITY_USE_ORDINAL`, `CANDIDATE_BINDING_BLOB`, `ROOT_BINDING_BLOB`, `ALLOCATION_BLOB`, `FINAL_PACKAGE_MANIFEST_PATH`, `PROCEDURE_BLOBS_UNCHANGED`, and `GRANT6_ELIGIBILITY`.

- Expected 1a OID comes from pre-staging frozen 1a, never 1b.
- Parent OID comes from Git object read-back and equals expected 1a.
- Expected allowlist comes from 1a; actual list from force-inclusive 1a..1b diff.
- Gate-A identity/digest/use ordinal come from unique K14 record and 7C binding.
- All blobs come from 1b read-back and reconcile to independent sources.
- `FINAL_PACKAGE_MANIFEST_PATH` comes from accepted universe; its blob OID is recorded externally after 1b to avoid a hash cycle.
- `PROCEDURE_BLOBS_UNCHANGED=PASS` requires one-to-one comparison.
- `GRANT6_ELIGIBILITY=READY_FOR_SEPARATE_BINDING`, never `BOUND`.

Pre-create non-PASS is `COMMIT1B_NOT_STARTED_STOP`. Possible creation followed by non-PASS read-back/parent/delta/identity is `COMMIT1B_CREATED_UNVERIFIED_QUARANTINE`; preserve actual object/reference, no amend/reset/delete/rewrite/continue.

## 4. Grant #6 binding

Grant #6 binds only exact verified 1b. Step 11/K33 is local and opens no socket. It consumes the owner D2 source plus verified K32 object/package and creates exactly one sealed record at 1a `GRANT6_BINDING_RECORD_PATH` with at least:

`GRANT6_CAPTURE_AUTHORITY_BOUND`, `COMMIT1A_OID`, `COMMIT1B_OID`, `HOST`, `ACTION_ID`, `PINNED_IDENTITY`, `EXCLUSIONS_SHA256`, `USE_ORDINAL`, `SOURCE_AUTHORITY_IDENTITY`, `RECORD_BYTES`, `RECORD_SHA256`, and `CLOSE_RESULT`.

K33 PASS requires:

- target is 1b, not 1a or another descendant;
- direct parent is exact verified 1a;
- allowlist and unchanged-procedure checks pass;
- candidate source is only unique K14 record, with correct authority identity/use ordinal;
- candidate/root/tool/allocation fields are concrete with the independence classifications in §3;
- host/action/pinned identity/exclusions equal D2; and
- record path is create-once, allocation-derived, closed, hashed, and use ordinal is one.

D2’s exact sentence authorizes only preregistered/committed read-only grant-#6 capture and later WP-I operations; it excludes other credential/host/write actions. `MTC_COMMAND_CENTER/11_TRIAGE/WPI_OWNER_DECISIONS_2026-08-15_NIGHT.md:30-60`

K33 fails closed as `GRANT6_AUTHORITY_UNBOUND_STOP` for 1a/different object, dirty/different bytes, different host/action/identity, broad scope, changed procedure, missing/unknown input, non-unique Gate-A record, or wrong use ordinal. K41 derives `COMMIT_1B` once from the sealed record. K42 independently checks the closed attestation’s first line against it. Commit creation and record creation do not create owner authority.

## 5. Runbook reordering

### 5.1 Old cycle and V2 replacement

| Existing location | Existing dependency | V2 replacement |
|---|---|---|
| Step 7 | Cycle resolution `UNKNOWN` | Accept/pin reviewed two-commit contract; this draft is not acceptance. |
| Old Step 9 | Allocation waits on Step 8 | **7A:** local allocation before 1a; independent collision/append-only checks. |
| New | None | **7B:** create/verify 1a; it grants no host or grant-#6 authority. |
| New | None | **7C:** bind separate Gate-A authority and safe close; explicitly answer pinned-SSH/D2 scope question. |
| Old 8/H-A | Required final Commit 1 | **8/H-A:** require verified 1a, candidate T0, Gate-A authority, disposable host, safe-close; run exact A-0..A-9. |
| New | Step-8 record lacked anchored transition | **8A:** seal/verify the unique record at 1a path; derive candidate TSV only from it; check authority identity/use ordinal and universe uniqueness. |
| Old 9/10 join | Inputs mixed in one commit step | **9:** local join of allocation, admin/config, tools, exact argv, review. |
| Old 10 | Creates single Commit 1/no prior host | **10:** create/read back direct-child 1b; only the separately authorized Step 8 host action may precede it. |
| 11 | Grant binds Commit 1 | Bind only exact 1b and seal K33 record. |
| 12/H-6 | Commit-bound capture | Require verified 1b, K33, K34/K40/K41/K43, and create-once close preflight. |

The original candidate cycle is removed: Step 8 waits on 1a, and Step 10 consumes Step-8 output. The surviving walk has no evidence cycle because no surviving check consumes privileged-channel output: 7C/8/8A flow directly to Step 9.

### 5.2 Full ordered-path walk

This is a **documentary transition contract for a compliant operator, not a mechanical interlock**. That wording carries the runbook-V2 review’s required R2 repair. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_TO_AUDIT2_RUNBOOK_V2_REVIEW_2026-08-15.md:65-73`

| Step | Walk and host guard |
|---|---|
| 1 | Local source freeze/review. Unaccepted integrated runbook → STOP. No host edge. |
| 2 | Record owner-ratified cumulative plan. No host edge. |
| 3 | Locally verify common KVM2 Phase-2 close record. Missing identity STOP. |
| 4 | Record Pathscope off-critical-path disposition; infer no repair/host action. |
| 5 | Owner-bound branch-local integration decision plus dry local applicability/recovery preflight. Local partial is quarantined. |
| 6 | Local integration/tests/build/D026 and two T0 flagship acceptances for exact candidate. No staging host action. |
| 7 | Accept/pin Option-A contract and revised runbook. Owner selection remains design-only. |
| 7A | Local allocation; K04/K05 must have accepted independent sources. No host. |
| 7B | Local create/read-back 1a. 1a grants no authority. |
| 7C | Bind separately owner-authored Gate-A authority and safe close to exact 1a/candidate/run kit/host; compare its credential/channel with D2 pinned identity and require explicit widening if reused. Missing input → STOP before socket. |
| 8/H-A | **First possible host action.** Only after 7C PASS; disposable `GATEA-STAGING`; exact A-0..A-9; branch-safe close only. |
| 8A | Local unique-record seal/verification at 1a path; authority identity/use ordinal and no-second-record census; candidate TSV copy. No host. |
| 9 | Local final-input join including the eight admin facts and K40 parameter reconstruction. K19–K25 and K30 consume their independently sourced values here; any `UNKNOWN` STOPs. |
| 10 | Local create/read-back 1b direct child; allowlisted data/provenance/manifest paths only. |
| 11 | Local K33 grant binding/sealed record; cannot bind 1a; no socket. |
| 12/H-6 | **Second possible host action.** Requires exact 1b from K33, no runtime ref resolution, K34/K40/K41/K43, root/mutation boundary, and create-once close preflight. Exact read-only capture only. |
| 13 | Local targeted fills and consumer conservation. Procedure change discards capture and restarts a new chain. |
| 14 | Local Commit-2 create/read-back; truthful pre/post-create states. |
| 14A | Local D2 WP-I-clause and always-close binding to exact Commit-2 run kit. No socket. This carries runbook-review R1’s required producing transition. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_TO_AUDIT2_RUNBOOK_V2_REVIEW_2026-08-15.md:28-30` |
| 15/H-WPI | **Third possible host action.** Only after 14A PASS, verified Commit 2, accepted P9-15, and WP-I close. No inherited Gate-A authority. |
| 16 | Local Packet-9 closure. Non-PASS host run writes only P9-17 STOP; no Step 17/quarantine clearing. |
| 17 | Local pre-WP-A freeze, only from Packet-9 PASS. |
| 18 | Local full-suite/runtime preflight and RED arms. No host edge. |
| 19 | Local suite execution/anomaly adjudication. Inability STOP; observed deviation FAIL. |
| 20 | Local Packet-11 completion plus owner ratification of measured actual. |
| 21 | Local authoritative dispatch bundle/conservation. |
| 22 | External model dispatch, not staging/production host action; paired preflight before either launch. |

No host node is reachable in this documentary state machine before its own distinct authority and preconditions. The current path in fact stops before Step 8 because Gate-A authority and C11 are `UNKNOWN`. This proof claims ordering only, not mechanical enforcement or present executability.

## 6. Failure and stop semantics

### 6.1 Per-step outcomes

| Step | Failure/STOP behavior and half-state closure |
|---|---|
| 1 | Missing/changed/unevaluable source → `STEP_1_STOP`; no mutation. |
| 2 | Missing/ambiguous owner plan → `STEP_2_STOP`; never infer. |
| 3 | Missing/wrong close identity → `STEP_3_STOP`. |
| 4 | Missing owner disposition → STOP; no inferred repair. |
| 5 | Preflight problem → `INTEGRATION_NOT_STARTED_STOP`; possible local mutation → `LOCAL_PARTIAL_QUARANTINED`, no improvised reset. |
| 6 | Test/T0 non-acceptance → `CANDIDATE_LOCAL_OR_T0_STOP`; partial auditor launch invalid/supplemental. |
| 7 | Unaccepted contract → `STAGE1_PREHOST_ORDERING_CONTRACT_STOP`. |
| 7A | Missing completeness/burn contract → `ALLOCATION_UNIVERSE_STOP`; post-mint failure uses accepted burn path or mint was ineligible. |
| 7B | Pre-create non-PASS → `COMMIT1A_NOT_STARTED_STOP`; post-create uncertainty → `COMMIT1A_CREATED_UNVERIFIED_QUARANTINE`, preserve object/reference. |
| 7C | Missing/mismatched/broad/expired authority, pinned-identity scope, or close → `GATEA_AUTHORITY_OR_CLOSE_UNBOUND_STOP`. |
| 8 | Pre-start non-PASS means no socket. After start, only preregistered close. Safe PASS → `HOST_SAFE_STOP`; unproved state → `HOST_STATE_UNKNOWN_QUARANTINE`; no 8A PASS. |
| 8A | Wrong path/count/1a/candidate/host/authority/use ordinal, second record, incomplete enumeration, or unreadable record → `GATEA_FINAL_RECORD_STOP`; preserve raw evidence. |
| 9 | Any admin `UNKNOWN`, tool/argv/provenance/review gap → `COMMIT1B_INPUT_JOIN_STOP`. |
| 10 | Pre-create non-PASS → `COMMIT1B_NOT_STARTED_STOP`; post-create uncertainty → `COMMIT1B_CREATED_UNVERIFIED_QUARANTINE`. |
| 11 | Authority/object/scope/path/use mismatch → `GRANT6_AUTHORITY_UNBOUND_STOP`; verified 1b remains safe idle. |
| 12 | Pre-start non-PASS means no socket. K40/K41/K43 must PASS first. After start preserve immutable PASS/STOP record; unproved host state → quarantine; no retry/wider command. |
| 13 | Missing/duplicate/unresolved consumer → `TARGETED_FILL_CONSERVATION_STOP`; preserve capture. |
| 14 | Pre-create non-PASS → `COMMIT2_NOT_STARTED_STOP`; post-create uncertainty → `COMMIT2_CREATED_UNVERIFIED_QUARANTINE`. |
| 14A | Missing/mismatched authority/close → `WPI_PREHOST_AUTHORITY_OR_CLOSE_STOP`. |
| 15 | Non-PASS executes only accepted `always` actions. Safe close → `HOST_SAFE_STOP`; unproved close → quarantine; only Step-16 closure remains. |
| 16 | PASS path reaches 17 only after Step-15 PASS. Non-PASS writes immutable P9-17 STOP and ends. |
| 17 | Missing/extra/duplicate/unresolved or single-form identity → `PRE_WPA_FREEZE_STOP`. |
| 18 | Missing runtime/universe or nondiscriminating RED → `MANDATED_SUITE_CONTRACT_STOP`. |
| 19 | Tool/runtime inability STOP; completed unauthorized deviation FAIL; both block 20. |
| 20 | Missing owner ratification → `PACKET11_OWNER_STOP`. |
| 21 | Bundle conservation/identity failure → `DISPATCH_BUNDLE_STOP`. |
| 22 | Failed paired preflight → `AUDIT2_NOT_DISPATCHED_STOP`; one-of-two launch → supplemental `AUDIT2_PARTIAL_INVALID_STOP`. |

The truthful post-create quarantine states for 1a, 1b, and Commit 2 carry runbook-review R3: preserve actual object/current-reference state without destructive recovery. `MTC_COMMAND_CENTER/11_TRIAGE/STAGE1_TO_AUDIT2_RUNBOOK_V2_REVIEW_2026-08-15.md:79-86`

### 6.2 Named chain half-states

| Half-state | Allowed closure |
|---|---|
| Verified 1a; no Gate-A authority | Safe idle; obtain exact authority or stop. |
| Gate-A authority bound; run not started | Safe idle; unspent and time/use limited. |
| Gate-A started; no final PASS | Accepted close only; safe STOP or quarantine. |
| 1b possibly created but unverified | `COMMIT1B_CREATED_UNVERIFIED_QUARANTINE`; no grant. |
| Verified 1b; K33 absent | Safe idle; no socket. |
| K33 bound; capture not started | Safe idle; exact object/one use only. |
| Capture started; no closed PASS | Preserve create-once STOP/partial; safe/unknown classification; no retry. |

K43 makes the former prose continuity condition executable: immediately before capture, the live subject’s MainPID, start time, cwd, cmdline, executable path/digest, and net namespace must equal the sealed Gate-A tuple and remain stable under the capture producer’s before/after pidfd checks. A changed process is observed deviance/STOP as specified; inability to observe is STOP. The sealed record is never rewritten.

An inability to evaluate is always STOP. FAIL is reserved for a completed observation of deviant state. `MTC_COMMAND_CENTER/11_TRIAGE/DESIGN_DEFECT_PATTERNS_2026-08-10.md:48-107`

## 7. Full renumbered check inventory

For each check, “Independent?” answers whether the expected value is controlled by a source other than the producer whose output is checked. Identity bookkeeping and producer-internal consistency are labelled as such. Every executable check records input identities, exact command/procedure, stdout, stderr, rc, first divergence, and PASS/FAIL/STOP. Unreadable/unevaluable input is STOP.

| ID | Check | Expected-value source | Independent? | What makes it fail / go red |
|---|---|---|---|---|
| K01 | Governing-source freeze | Independently accepted source/authority manifest | Yes | Omitted/duplicate/substituted/changed/single-form source → STOP. |
| K02 | Exact T0 candidate selection | Frozen T0 records and candidate Git object | Yes | Candidate/tree/artifact/manifest differs; auditor non-execution/non-acceptance; identity mismatch → STOP. |
| K03 | Stable-expectations row | K02 plus accepted cwd/cmdline/executable formulas/build manifest | Yes | Grammar/derivation failure or value from Gate-A producer → STOP. |
| K04 | Allocation collision completeness | Independent ledger/roots/sources plus accepted completeness policy | Yes if source exists; currently `UNKNOWN` | Collision, omitted/unreadable source class, canonical duplicate, reparse redirect, or absent accepted policy → STOP before mint. |
| K05 | Allocation append-only/burn | Pinned predecessor chain plus accepted durable burn path | Yes if source exists; burn path currently `UNKNOWN` | Overwrite/truncate/insert/reorder/hash break/duplicate or absent closure path → STOP/quarantine. |
| K06 | 1a provenance whitelist | Accepted six-class source whitelist in §1.1 | Yes | Any 1a data value lacks exactly one named pre-run derivation, depends on live output, or is unparseable → STOP. |
| K07 | 1a member conservation | Independently accepted force-inclusive path/role universe | Yes if source exists; currently `UNKNOWN` | Extra/hidden/missing/duplicate/member without role; manifest defines universe; absent accepted universe → STOP. |
| K08 | 1a object read-back identity | Pre-create byte freeze plus actual 1a object | **No for content correctness; yes only for freeze↔object identity if freezer is independent and precedes creation** | Blob/byte/hash/dual-form mismatch → STOP/quarantine. Content defect is not red here; K31 owns it. |
| K09 | 1a authority fence | Owner Option-A sentence | Yes | Anything other than no host authority, grant #6 prohibited, and separate Gate-A authority required → STOP. |
| K10 | Gate-A authority binding/use scope | Exact owner sentence plus 1a candidate/path/use policy and D2 source | Yes | Missing/expired/broad; candidate/host/A0..A9/mutations/close/exclusions/use ordinal mismatch; record authority identity differs; channel equals D2 pinned identity without explicit widening, or identity question unresolved → STOP before socket. |
| K11 | Gate-A subject/host preflight | 1a expectations, run kit, disposable-host contract | Yes | Wrong candidate/1a/run kit, KVM2, or unavailable host identity → STOP. |
| K12 | Gate-A safe-close preflight | Independently accepted C11 close contract | Yes if source exists; currently `UNKNOWN` | Missing branch close, unsafe/unevaluable final predicate, producer supplies expectation, or absent accepted contract → STOP. |
| K13 | Fresh A-0..A-9 conservation/use count | Independent A-step contract, candidate, 7C authority identity/use ordinal | Yes | Missing/duplicate/unresolved step, historical reuse, wrong bytes/state, record authority mismatch, use count not exactly one → STOP/FAIL as observed. |
| K14 | Unique fresh final-record path/seal | 1a `GATEA_FINAL_RECORD_PATH`, accepted universe, OS create-once, frozen 1a OID | Yes for path/schema/universe | Not exactly one record at path, second record naming 1a, incomplete census, collision/write/fsync/close/reopen/hash/raw-evidence failure → immutable STOP. |
| K15 | Candidate TSV provenance/copy | Sealed K14 record | Yes relative to TSV builder; **No relative to Gate-A producer’s truth** | Byte difference, alternate source, mutable/unsealed record → STOP. It cannot go red for a false producer-internal observation repeated consistently. |
| K16 | Candidate independent cross-check | 1a/K02 expectations, accepted build, A-4 and A-5 terminal records | Yes | Host/candidate/unit/cwd/cmdline/executable path or digest differs; active not A-4 `active`; restart count not A-5 `0` → FAIL if observed, STOP if unevaluable. |
| K17 | 1b direct-parent ancestry | Independently frozen 1a OID | Yes | `1b^1` differs, extra parent, unreadable ancestry → STOP/quarantine. |
| K18 | Allowed delta/procedure immutability | 1a allowlist and procedure blob map | Yes | Nonallowlisted path/field, missing delta, changed producer/recorder/launcher/schema/allocation/stable/tool-path/procedure byte → STOP. Tool digest values are valid 1b inputs, not 1a blobs. |
| K19 | SSH principal/key mapping | Admin decision plus authoritative server mapping | Yes | Target/key differs or config absent → STOP; current slot `UNKNOWN`. |
| K20 | Account shell/identity | Authoritative account/provisioning record | Yes | UID/GID, shell path/kind/owner/mode/digest differs/unavailable → STOP. |
| K21 | Forced-command binding | Effective sshd/authorized-key config | Yes | Exact bytes differ, duplicate/ambiguous rule, unproved `NONE` → STOP. |
| K22 | Wrapper/process chain | Selected route plus independent config/tool identities | Yes | Missing hop, unbound executable, PATH lookup, ellipsis, different chain → STOP. |
| K23 | Pre-`env` environment | Independent first-process/config record | Yes | Unexpected/missing entry, bad encoding, hook/loader variable, unreadable record → STOP. |
| K24 | Initial cwd | Independent first-process/config record | Yes | First command starts elsewhere or pre-start cwd cannot be established → STOP. |
| K25 | Descriptor set | Independent first-process FD inventory | Yes | Mapping differs, extra writable FD, incomplete enumeration → STOP. |
K26 — REMOVED (privileged channel severed by owner decision 2026-08-16; see §DISCLOSURE).
K27 — REMOVED (privileged channel severed by owner decision 2026-08-16; see §DISCLOSURE).
| K28 | Operator-tool identity | 1a absolute paths plus independent 1b/use-time hashes | Yes for exact bytes at use | PATH resolution, wrong path/kind, 1b digest differs from immediate rehash, or bytes change before use → STOP. |
| K29 | Final 1b member conservation | 1a allowlist plus accepted final universe | Yes | Extra/hidden/missing/duplicate member or manifest-defined universe → STOP. |
| K30 | No unresolved final input | Accepted schemas/source register | Yes | Any `UNKNOWN`, placeholder, metavariable, unparsed byte, absent field/contract → STOP. |
| K31 | Executable D026/exact-byte review | Predeclared mutations plus independent reviewer/auditor | Yes | Test not RED against pre-fix/equivalent mutation and GREEN on exact bytes, non-execution, or required finding → STOP/BLOCK. |
| K32 | 1b object/read-back/clean state | Independent 1a/delta/input identities plus actual object | Yes | Parent/object/raw bytes/manifest/dual forms/worktree/index/untracked differs → pre-create STOP/post-create quarantine. |
| K33 | Grant-#6 binding/sealed record | Owner D2 source plus verified K32 object and 1a record path | Yes | Target 1a/different object; host/action/identity/exclusions/use/path/close mismatch; input unevaluable → `GRANT6_AUTHORITY_UNBOUND_STOP`. |
| K34 | Immediate pre-H-6 exact-object/package check | `COMMIT_1B` derived once from sealed K33 record | Yes | Worktree/index/untracked/package/recorder/tool bytes differ from exact object, or any lookup derives identity from `HEAD`/ref/launcher OID → STOP before socket. |
| K35 | Capture create-once/close | 1a allocation path/parent preflight plus accepted close contract | Yes for path/contract; allocation-time parent state is stale until rechecked | Parent/path drift, collision, overwrite, fsync/close/reopen/streams/rc/host-safe observation failure → immutable STOP/quarantine. |
| K36 | Targeted-fill/component conservation | Independent consumer inventory/canonical packet IDs | Yes | Dropped/duplicated/overwritten/unresolved admitted value/member → STOP. |
| K37 | Full-suite/runtime contract | Owner-fixed suite, frozen inventory, independent collection/runtime identities | Yes | Narrowed command, missing test, altered runtime/dependency/tool/fixture, anomaly, nondiscriminating RED → STOP/FAIL. |
| K38 | Paired flagship dispatch readiness | Canonical roster plus frozen bundle/worktrees/routes | Yes | Model/effort/route/suite unavailable, bundle/SHA differs, worktree dirty, or one starts alone → no dispatch/partial-invalid STOP. |
K39 — REMOVED (privileged channel severed by owner decision 2026-08-16; see §DISCLOSURE).
| K40 | Executed argv/committed-row equality | 1a allocation + procedure/tool paths and 1b candidate/root rows | Yes relative to argv constructor | Any parameter token in `ATT01_REMOTE_COMMAND`/decoded `ATT01_OUTER_ARGV_B64` is omitted, duplicated, reordered, transformed, stale, independently typed, or differs byte-for-byte from its exact source → STOP before socket. |
| K41 | Recorder parameter provenance/single commit derivation | 1a `GRANT6_BINDING_RECORD_PATH` plus sealed K33 fields | Yes | Recorder has extra/missing/different parameter; K33 schema/hash/authority invalid; `COMMIT_1B` not assigned exactly once from K33; any runtime ref or launcher OID supplies identity → STOP before record/socket. |
| K42 | Closed-record first-line binding | Independently frozen `COMMIT_1B` from K33 | Yes relative to recorder | After close/reopen, first bytes are not exactly `attestation_prereg_commit=<COMMIT_1B>\n`, duplicate line exists, or complete bytes unreadable → immutable STOP; grant is spent but record non-consumable. |
| K43 | Seal-to-capture MainPID liveness continuity | Unique K14 Gate-A tuple versus independent ATT-01 capture producer/pidfd observations | Yes relative to capture producer; not an independent initial-truth check on Gate-A producer | MainPID/start-time/cwd/cmdline/executable path/digest/netns differs, pidfd readable, before/after drift, process exit/reuse/exec, or observation inability → STOP before accepting capture continuity. |

The check design follows the governing rule that a real correctness check needs an expectation outside the checked producer and a concrete red world. `MTC_COMMAND_CENTER/11_TRIAGE/SELF_CONFIRMING_CHECK_PATTERN_2026-08-15.md:30-63` K08 content comparison, K15’s Gate-A truth, and the producer-internal MainPID/start-time/netns consistency checks are expressly **not** independent correctness evidence. Where no producer-independent expectation exists, this design says so; it does not treat a producer-supplied hash, count, manifest, PASS token, raw-evidence copy, or scope universe as independent proof of its own claim.

## DISCLOSURE — no mutation-denial evidence

Commit 1b attests **NO mutation-denial evidence**. The mutation-denial control does not exist on `GATEA-STAGING` (observed 2026-08-16). The owner-approved accelerated contract and the channel load-bearing decision removed the privileged channel from the critical path.

The residual risk is bounded by `GATEA-STAGING` being a local, disposable Hyper-V VM; the retained Hyper-V checkpoint, which is unreachable from inside the VM, including by in-VM root; and operator-attested observation records. Those observation records are not mechanism-enforced and must not be presented as independently proven read-only captures.

The money gate is unaffected and keeps the full T0 standard for credentials, keys, broker/exchange access, ARM, orders, TESTNET/mainnet, and live paths.

If any future owner decision revives the privileged channel, the round-2 verdict’s R-1 through R-5 **MUST** be reopened before any 8B-class sentence is put to the owner.

## V3 change log and self-QA

This V3 implements `TWO_COMMIT_CHAIN_R2_DISPOSITION_2026-08-16.md` against the pinned V2 source of 79100 bytes and SHA-256 `1fe9e5cb5ef9c266b819c3ca63d4273e3c45ef21493fac04c2c492dbf5dc36d4`. No round-2 NIT was closed: n-1 through n-8 remain untouched. No round-2 NIT finding text sat inside the removed V2 §2.3 content, so no NIT text disappeared with that removal.

### Required source-hit accounting

- V2 line 7 (`8B`, `K26`, `K27`, `K39`): replaced the REQ-1 closure row with the owner-disposition removal and no-surviving-consumer cycle result.
- V2 line 16 (`K26`, `K27`): removed only the deleted check references; retained the NIT-4 text unchanged.
- V2 line 24 (`8B`): removed the obsolete route-(i) repair-choice entry.
- V2 line 43 (`8B`): replaced the former distinct-authority cycle explanation with the no-surviving-consumer explanation.
- V2 line 87 (`PRIVILEGED_EVIDENCE_AUTHORITY_REQUIREMENT`, `K26`, `K27`, `8B`): removed the entire chain-control field row.
- V2 line 88 (`PRIVILEGED_EVIDENCE_USE_ORDINAL`): removed the entire chain-control field row.
- V2 line 191 (`8B`): removed the evidence-run phrase from the procedure/path-change cost sentence.
- V2 line 208 (`8B`, `PRIVILEGED_EVIDENCE_AUTHORITY`): removed the entire obsolete `UNKNOWN` contract row.
- V2 line 292 (`8B`): removed the §2.3 heading and its whole step block.
- V2 line 296 (`8B`, `PRIVILEGED_EVIDENCE_AUTHORITY`): removed with the whole §2.3 step block.
- V2 line 309 (`K39`): removed with the whole §2.3 step block.
- V2 line 311 (`8B`, `PRIVILEGED_EVIDENCE_AUTHORITY`, `K26`, `K27`): removed with the whole §2.3 step block.
- V2 line 324 (`8B`, `K26`, `K27`, `K39`): removed the obsolete 1b entry-gate item and renumbered the surviving following item from 7 to 6.
- V2 line 367 (`8B`, `K26`, `K39`): replaced the evidence-source row with the explicit no-mutation-denial-evidence source and attestation.
- V2 line 368 (`8B`, `K27`, `K39`): removed the obsolete infrastructure-crossover evidence row.
- V2 line 401 (`8B`, `K26`, `K39`): replaced the root-binding field source with the accelerated-contract disclosure and exact `NO_MUTATION_DENIAL_EVIDENCE` literal.
- V2 line 402 (`8B`, `K27`, `K39`): removed the obsolete `INFRASTRUCTURE_CROSSOVER_RESULT` root-binding field row.
- V2 line 443 (`PRIVILEGED_EVIDENCE_RECORD_SHA256`): removed the field from the chain-row member list.
- V2 line 449 (`8B`, `K26`, `K27`, `K39`): removed the privileged-evidence digest provenance bullet.
- V2 line 469 (`K26`, `K27`, `K39`): removed the obsolete K33 PASS prerequisite bullet.
- V2 line 476 (`8B`): removed the obsolete evidence prerequisite from K33 fail-closed causes.
- V2 line 490 (`8B`, `K26`, `K27`, `K39`): removed the ordered-replacement row for the deleted host step.
- V2 line 491 (`8B`): removed evidence from the Step 9 join row.
- V2 line 492 (`8B`): changed the Step 10 row to name only the separately authorized Step 8 host action.
- V2 line 496 (`8B`): replaced the old evidence-cycle paragraph with the direct no-consumer ordering statement.
- V2 line 516 (`8B`, `K26`, `K27`, `K39`): removed the ordered-walk row.
- V2 line 517 (`8B`): changed Step 9 to consume the eight admin facts through K19–K25 and K30.
- V2 line 533 (`8B`): removed the obsolete stop-at-deleted-step clause.
- V2 line 553 (`8B`, `PRIVILEGED_EVIDENCE`): removed the per-step outcome row.
- V2 line 554 (`K26`, `K27`, `K39`): removed the deleted checks from Step 9 STOP causes.
- V2 line 579 (`8B`): removed the obsolete absent-authority half-state row.
- V2 line 580 (`8B`): removed the obsolete authority-bound half-state row.
- V2 line 581 (`8B`): removed the obsolete started/not-closed half-state row.
- V2 line 582 (`8B`): removed the obsolete PASS/1b-absent half-state row.
- V2 line 606 (`8B`): removed the deleted authority from K09 while retaining the Gate-A authority fence.
- V2 line 623 (`8B`, `K26`): replaced the deleted check with its required K26 tombstone; the surviving K-ID sequence was not renumbered.
- V2 line 624 (`8B`, `K27`): replaced the deleted check with its required K27 tombstone; the surviving K-ID sequence was not renumbered.
- V2 line 630 (`8B`): removed the deleted evidence prerequisite from K33.
- V2 line 636 (`8B`, `K26`, `K27`, `K39`): replaced the deleted check with its required K39 tombstone; the surviving K-ID sequence was not renumbered.

### Other authorized edits

- Bumped the status/header from V2 to V3, dated it 2026-08-16, named the governing disposition, and recorded the exact V2 byte/SHA-256 pin.
- Removed the deleted channel authority from the scope boundary, changed the pre-1b host-stage count from two to one, removed the corresponding scope bullet, and changed the §2 heading from plural channel runs to the single staging run.
- Reconciled the eight-admin-fact register and schema text with the §DISCLOSURE-controlled `NO_MUTATION_DENIAL_EVIDENCE` value and removed the former crossover-evidence input from 1b.
- Restored host-action ordinals after deletion: H-6 is now the second possible host action and H-WPI the third; no surviving step number or K-ID was renumbered.
- Added exactly one `## DISCLOSURE — no mutation-denial evidence` section with the required residual-risk, checkpoint, operator-attestation, money-gate, and future-revival statements.

### Self-QA results

**Required grep.** A whole-file UTF-8 grep for `8B`, `PRIVILEGED_EVIDENCE`, `K26 `, `K27 `, and `K39 ` found no surviving design requirement or field. Every remaining hit is confined to the three required REMOVED tombstone lines, §DISCLOSURE, or this change log.

**V1 REQ-1 dependency-cycle check.** The surviving walk order is: “1–7 → 7A → 7B → 7C → 8/H-A → 8A → 9 → 10 → 11 → 12/H-6 → 13 → 14 → 14A → 15/H-WPI → 16 → 17 → 18 → 19 → 20 → 21 → 22.” Removing 8B does not recreate the V1 dependency cycle: Step 8 still waits only on verified 1a, Step 10 still consumes the Step-8-derived sealed candidate record to create 1b, and no surviving check consumes privileged-evidence output. Therefore no deleted-channel output needs an earlier producer edge.

**Eight-admin-facts flow.** With 8B gone, Step 9 consumes the eight admin facts. K19–K25 consume the independently sourced SSH principal/key mapping, account shell/identity, forced command, wrapper/process chain, pre-`env` environment, initial cwd, and descriptor set; the mutation-denial slot is carried only as the §DISCLOSURE-controlled `NO_MUTATION_DENIAL_EVIDENCE` attestation. K30 rejects any unresolved remaining input. No surviving step presupposes output from 8B.
