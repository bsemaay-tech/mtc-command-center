# Bridge lane-J proposals — exact evidence and narrowly scoped recommendations (2026-09-07)

**Class:** T3 recommendation record (no code changed). Source: overnight read-only lane J
(`OVERNIGHT_LANE_J_BRIDGE_STATIC_HUNT_2026-09-07.md`), re-read on the Windows host against
`IBKR_PAPER_BRIDGE` at `master` `afe52ea`. Owner instruction 2026-09-07: do not implement the wider
lane-J proposals yet; prepare exact evidence and separate, narrowly scoped recommendations. Every
item below is owner-gated (T0, broker/order/risk surface) and needs its own authorization sentence.

| # | Location (master `afe52ea`) | Evidence | Classification | Narrow recommendation | What it would NOT touch |
|---|---|---|---|---|---|
| J-1 | `bridge/broker/mock.py:349,428,569,817` | Four `datetime.now()` calls without `UTC`; the rest of the module and the engine use `datetime.now(UTC)`. Naive timestamps flow into `FillEvent.ts` / `OrderUpdateEvent.ts` and reach `reconcile.py:826` (`observed_ts.astimezone(UTC)`), where a naive value is read as host-local time. `MockBroker` is wired only when `dry_run=True` (`bridge/app.py:203-207`). | Latent: wrong only on non-UTC hosts, dry-run path only; never live/paper. | **Recommend a single T0 package "mock broker UTC timestamps":** change the four call sites to `datetime.now(UTC)` (the last one drops `.astimezone()`), plus one regression test that runs a dry-run fill on a mocked non-UTC local zone and asserts `ts.tzinfo is UTC`. RED/GREEN required (D026). | No `HyperliquidBroker`, no reconcile logic, no economics. |
| J-2 | `bridge/store/db.py:3174-3177` | `new_submitted_ts` is assigned in both branches of a duplicate-intent legacy-migration identity check and never compared (ruff F841); `new_state`, `origin_run_id`, `origin_decision_uid` are compared. Reads as a dropped `submitted_ts` equality check. | Real defect candidate in fail-closed migration validation (order/trade identity). | **Two-step recommendation, both owner-gated:** (a) Lead census: enumerate every migration fixture in `tests/` that exercises this branch and record whether adding `existing["submitted_ts"] != new_submitted_ts` would change any recorded outcome (read-only, no code). (b) Only if (a) shows no legitimate mismatch: one T0 package adding the comparison with a RED test (fixture whose `submitted_ts` differs must raise `MigrationError`). If (a) finds a legitimate mismatch, document it and rename the variable to `_new_submitted_ts` instead (behaviour-preserving). | No live migration run; no schema change; no reconcile/order code. |
| J-3 | `bridge/store/db.py:4989,5067,6297-6298,7015,9960`; `bridge/engine/orders.py:1453`; `bridge/engine/reconcile.py:1126-1127` | Seven exact float `==`/`!=` comparisons on qty/px inside collision detection, fill-reconciliation matching, and equity integrity checks. Values round-trip through SQLite/JSON. | Known-risky pattern; changing it changes collision/reconciliation semantics. | **Do not change comparison semantics.** Recommend a read-only T0 study first: for each site, state the producer of both operands (same-process float vs SQLite REAL vs JSON), whether a representation difference is reachable, and what a tolerance would mask (a genuine qty mismatch is a fail-closed signal). Only a site with a demonstrated reachable representation drift gets a package, and that package must use the venue's lot/price quantum (`InstrumentMetadata`/lot quantisation already in the engine), never an arbitrary epsilon. | No blanket `math.isclose`; no economics. |
| J-4 | `bridge/config_contract.py:1880` (B007) | `for variable in sorted(set(expected_bindings) - observed_bindings)` emits a `SourceCensusFinding` that omits `variable`; the count/location stay correct. | Diagnostic quality only (self-check tool). | **Recommend a T1 one-line package:** include `variable` in the finding code (`f"ENGINE_OPTIONS_NOT_BOUND_OBJECT_INPUT:{variable}"`) with a test asserting the name appears; check first whether any consumer string-matches the exact code (grep `ENGINE_OPTIONS_NOT_BOUND_OBJECT_INPUT` across `bridge/`, `tools/`, `tests/`, docs) — if one does, append the name in a separate field instead. | No engine behaviour. |
| J-5 | `bridge/engine/orders.py:67` (`OrderCollisionError` imported, never caught by name), `bridge/store/db.py:81` (`RISK_DAY_STATE_MALFORMED` imported, literal used at `:8496-8499`) | Latent: broad `except Exception` already routes the collision; the literal equals the constant's value today. | Latent, owner-gated (fail-closed error codes). | **Recommend one T1 package "reference the imported constant":** replace the literal at `db.py:8496-8499` with `RISK_DAY_STATE_MALFORMED`, add a test that the raised `ReconcileConflictError` code equals the constant; leave the `OrderCollisionError` import as-is (removing it changes nothing at runtime and a future explicit `except` is plausible). | No control-flow change. |
| J-6 | `bridge/engine/engine.py:23,30`, `bridge/engine/orders.py:63`, `tools/smoke_fill.py:28,74`, `db.py:4993,5076` (B904), `orders.py:4266` (B009) | Dead imports, `raise` without `from`, `getattr` with constant. No runtime effect. | Style. | **Recommend bundling into the next Bridge T1 hygiene package, not a standalone PR**; the two B904 sites should use `from exc` (keeps the SQLite `IntegrityError` chain explicit). | Nothing behavioural. |

**Cross-cutting evidence already on record:** ~50 `except Exception` blocks reviewed by lane J with two
nested examples traced (`orders.py:2403-2417`, `db.py:5071-5075`); no silent discard of an
order/fill/money result found. No mutable default arguments, no `shell=True`, no unguarded
`.get()` on money-critical keys (`db.py:2988-2999` fails closed via `MigrationError`).

**Suggested authorization order if the owner opens any of these:** J-1 (dry-run only, cheapest, clean
RED/GREEN) → J-4/J-5 (T1, diagnostic) → J-2 step (a) census → J-3 study. None is on the P0 critical
path; none should pre-empt P0-12.

- **NEXT ACTION:** none until the owner authorizes a numbered item above.
- **WAITING FOR OWNER:** authorization sentence per item (J-1 … J-6), or "park all".
