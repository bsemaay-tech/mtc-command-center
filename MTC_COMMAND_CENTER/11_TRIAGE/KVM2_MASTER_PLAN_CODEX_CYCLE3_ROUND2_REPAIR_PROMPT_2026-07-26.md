# KVM2 Codex-only audit cycle 3 — repair round 2

Work in `C:\LAB\Tradingview_LAB_CLEAN`. Read and obey `AGENTS.md` and
`MTC_COMMAND_CENTER/_AI_MEMORY/START_HERE.md`. Barış explicitly authorized a
new KVM2 documentation audit cycle, reset the three-round cap for this artifact
only, prohibited Claude-credit use, and required Codex repairs plus fresh Codex
audits. This is Cycle 3, R2 after the fresh exact `gpt-5.6-sol`/`xhigh`
read-only R1 verdict `REQUEST_CHANGES`.

Do not invoke Claude, Cline, DeepSeek, Grok, OpenRouter, or another provider.
The future bridge-candidate gate still requires exact `claude-opus-5`/`xhigh`
because D023 waives Claude only for this documentation audit cycle; document
that future requirement but do not run it.

Preserve every unrelated dirty-worktree change. Do not run Git mutations,
access the VPS/runtime, install/deploy, use secrets, contact brokers/exchanges,
or perform network/trading actions.

Edit only:

- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_AI_LAB_AND_BRIDGE_MASTER_PLAN_2026-07-25.md`
- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_AI_LAB_AND_BRIDGE_EXECUTION_TASKS_2026-07-25.md`
- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_MASTER_PLAN_MULTI_MODEL_AUDIT_PROMPT_2026-07-25.md`
- `MTC_COMMAND_CENTER/_AI_MEMORY/DECISIONS.md`

Apply all nine required R1 audit repairs:

1. **No-lab route to Phase 10.** Phase 10 is mandatory even when Phases 6–9
   are rejected/not admitted for instability, headroom, or security. Add an
   acyclic route from accepted Phase-5 bridge evidence and monitoring history
   to Phase 10, recording Phases 6–9 as `NOT_ADMITTED`. Preserve both branches:
   lab-admitted → Phase 9 → Phase 10; no-lab → Phase 10. Neither bypasses the
   Option A/B authorization/execution gates or P10-04.

2. **Remove P3-02/P3-05 cycle.** Delete P3-02's generic “missing independent
   verdict” Stop condition. Keep exact-candidate acceptance at P3-05. Make the
   executable trace explicit: P3-01 → P3-02 → P3-03 → P3-04 → P3-05.

3. **Restore canonical bridge item 10.** P3-05 must require the lower-level
   pre-deploy audit contract: fresh exact `claude-opus-5`, effort `xhigh`,
   no fallback, no resume/continue, on the exact repair diff and tests, unless
   a later explicit owner decision amends it. This is a future task, not an
   instruction to invoke Claude now. Crosswalk item 10 must close its
   pre-deploy portion at P3-05 before P4-01 while separately preserving deploy
   authorization, ARM authorization, and the monitoring counter starting only
   at final approved ARM.

4. **Canonical rollback identity.** The lower-level bridge task always requires
   a rollback SHA. For first deployment, require a named immutable
   rollback-state artifact/manifest SHA plus a hash-recorded tested rollback
   procedure: service stopped/disabled, state/risk artifacts preserved, writer
   count zero, no Windows authority re-enabled. If a prior accepted VPS release
   exists, record its rollback release SHA too. Synchronize P4-08, crosswalk
   item 8, master Phase 4, and authority wording. Do not weaken the canonical
   lower-level bridge task.

5. **Authorize P6-04.** Extend P6-00A to one bounded campaign covering P6-01
   through P6-04. The owner sentence cites both P5-09 contract and P5-10 design
   hashes, names the executor, authorizes exactly one P6-04 dummy-workload/
   kill-switch attempt, and requires a new P6-00A after any failure/retry.
   P6-04 must block without P6-00A and record the same pre/post bridge PID,
   unit hash, loopback listener, restart count, reconciliation freshness, and
   exchange-connectivity assertions; regression invokes P5-10(e) rollback and
   halts Phase 6. Synchronize master Phase 6 and authority matrix.

6. **Split Option A/B authorization from execution without adding tasks.**
   Repurpose the existing four IDs:
   - P10-03A: Barış-only Option A immutable execution-packet acceptance and
     pre-action authorization. It closes before action and authorizes exactly
     one wipe, one bootstrap/first-boot sequence, and one verified restore,
     each named separately with target, executor, hashes, stop conditions, and
     no retry.
   - P10-03C: named executor performs only the already-authorized Option A
     bounded actions and records post-action evidence. It cannot self-authorize.
   - P10-03B: Barış-only Option B immutable execution-packet acceptance and
     pre-action authorization. It closes before action and separately
     authorizes exactly one purchase, one provisioning/bootstrap sequence, and
     one verified restore, with provider/target, executor, hashes, stop
     conditions, and no retry.
   - P10-03D: named executor performs only the already-authorized Option B
     bounded actions and records post-action evidence. It cannot self-authorize.
   Preserve DISARMED, raw exchange flat proof, WAL-consistent capture, isolated
   restore verification, zero writers, agent-authority revocation, provenance,
   credential rotation, no-lab/no-KVM2-artifact proofs, Gate 5/6, and separate
   P10-04 mainnet authority. Update the master dependency and authority rows.

7. **P4-04 is tabletop-only.** State explicitly that rehearsal is a document
   walkthrough/tabletop exercise with no process, service, scheduler, listener,
   network, secret, exchange, or writer mutation. Any live rehearsal needs a
   separate future bounded owner authorization. Preserve the actual P4-04A/P4-05
   live cutover gates.

8. **Fresh admission for controlled coding.** P7-04 requires a distinct
   P6-05-style owner decision ID and immutable coding-manifest hash; it cannot
   reuse the Hermes or prior workload authorization. Record permissions,
   resource budget, credential scope, observation duration, rollback, and
   start/end conditions. Every Phase-7 workload gets its own owner record.

9. **Create a real Phase-8 execution chain without adding tasks.** Reorder or
   repurpose existing IDs into:
   P8-01 immutable design → P8-02 Linux path/readiness validation with no
   build/start → P8-01A owner admission plus any network gate → P8-03 exactly
   one bounded build/start → P8-04 network/bridge verification.
   P8-03 must require the design/readiness hashes, named executor, exact attempt
   count, unit/config hashes, listener/firewall diff, unchanged pre/post bridge
   assertions, and rollback on regression. P8-04 verifies no 8790 exposure.
   Synchronize master Phase 8 and authority matrix.

Also apply both safe optional nits:

- Remove the duplicated P9-02 roster phrase.
- If an admitted Phase 8/9 service uses a domain/certificate, extend the
  existing off-host monitor with expiry threshold, owner/renewal contact, and a
  tested synthetic expiry alert. Do not create another monitoring stack.

Cycle/integrity requirements:

- Update the identical cycle map in both headers and D023 to current Cycle 3,
  R2, while preserving the R1 `REQUEST_CHANGES` history.
- Update the audit prompt to Cycle 3/R2, exact fresh Codex-only
  `gpt-5.6-sol`/`xhigh`, ephemeral/read-only, no fallback, and move R1 hashes to
  superseded history.
- Keep exactly 84 unique standard-shape IDs; repurpose, do not add/delete IDs.
- Every task retains exactly one `  - Evidence:` and one `  - Stop:` line.
- Crosswalk rows 1–10 appear exactly once.
- Keep master and companion below 60,000 bytes. Compress redundant prose only;
  never remove gates or evidence.
- Keep zero full `KVM2-P...` IDs in the master.
- Preserve preparation-only status and all authorization separations.
- Use valid UTF-8, persist no runtime-only private source path, and claim no
  audit verdict.
- Compute fresh byte-level SHA-256 for master and companion and update expected
  hashes only after all edits are final.

Return concise validation: changed findings, 84/84 structure, crosswalk, sizes,
fresh hashes, and confirmation that no Claude or external provider was invoked.
