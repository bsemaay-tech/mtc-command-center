# WP-L P2 proposal-repair specification — independent audit (2026-08-09)

## Verdict

**PASS-WITH-NITS.** Exact audited commit:
`9ac60ac652f4a221316465cdbc24516aa391f5ce`. No required repair remains in
`WPL_P2_COMMAND_GAP_PROPOSALS_REPAIR_SPEC_2026-08-09.md`.

This accepts the specification as a contract for a future one-file documentation repair. It does **not**
accept rejected proposal `779bd038`, reopen the exhausted `C:\PGRK` design, authorize implementation,
or grant any host/trading/deployment authority.

## Independent execution

- **GLM-5.2 via Z.AI Coding Plan:** `PASS-WITH-NITS`; completed read-only source checks against candidate
  `2ce41e34`; final `git status --short` empty.
- **Codex `gpt-5.6-sol` xhigh via secondary subscription:** timed out at 604 seconds, no verdict.
- **Claude Opus 5 xhigh via alternate CLI account:** consumed the review window then reported session
  limit (reset 13:50 Europe/Chisinau), no verdict.

The GLM audit is the executing cross-model review for this documentation contract. No shell/host test is
mandated by the spec-audit task; GLM reproduced the exact candidate source anchors and inspected the
frozen diff. Non-verdict runs are recorded as supplemental non-execution, not acceptance.

## Lead reproduction

The Lead independently confirmed:

- RP0 rejects object/link reuse, separates operator bootstrap evidence, maps `pgrep` and
  `systemctl is-enabled` outcomes three ways, and does not declare the separate `C:\PGRK` gap closed.
- RP1 requires exact `0555`, candidate `/222` any-write semantics, manifest binding, fail-closed reads,
  and RED/GREEN fixtures for `0444`, `0020`, `0002`, wrong hashes, unreadability, and partial `find`.
- RP2 supplies no runnable C1 route while either exact exit-tuple or safe active-writer baseline gap is
  open; no partial PASS exists.
- RP3 keeps two preregistered reboot branches and requires genuine invariant baselines/equality.
- RP4 uses `sqlite3.Connection`, `collect_invariants(restored_connection)`, candidate
  `invariants_hash`, SQLite backup semantics, artifact preservation, identity, sidecar, and hash checks.
- RP5 requires rollback-manifest no-clobber, dry-run, exact mask, fresh invariant equality, and no rebind.
- RP6 keeps C5 blocked and provides no executable credential/broker/network procedure.
- One-file future scope, three-round cap, and all authority boundaries are explicit.

Candidate anchors reproduced: `wal_state_bundle.py:212-213,417,561-562`; `verify.sh` exact `0555` and
manifest-binding checks; `common.sh` `/222`; `rollback.sh` dry-run, symlink-only guard and unconditional
manifest write; first-start template no `[Install]`, timeout and final signal.

## Optional nits — accepted, not required

1. Add one ancillary-path mode/owner drift fixture to RP1 for symmetry.
2. State explicitly that stop+mask-only C4 requires empty no-rebind fields in the rollback manifest.
3. When C1's two gaps eventually close, add its own explicit falsification list.
4. Name the restore primitive as `src_conn.backup(dst_conn)` with read-only source/fresh destination.

These do not create a false PASS under the current acceptance floor and may be folded into a future
counterpart repair without becoming required scope.

## Routing record

```text
Classification          : Tier 4 protected Bridge safety/evidence specification audit
Protected               : yes — persistence, stop/reboot, rollback and broker boundaries
Model/provider          : GLM-5.2 via Z.AI Coding Plan (executing); Codex/Claude supplemental no-verdict
Cheaper-model rationale : exact owner routing request and protected cross-cutting contract
Exact paths             : repair spec + proposal audit + rejected proposal + candidate source anchors
Context/tool budget     : 231-line spec; compact references; 20-minute GLM ceiling
Fallback                : Lead reproduction; no secondary model may implement protected scope
External API credits    : no
```

## Next steps

1. Checkpoint this accepting verdict in all continuity files.
2. Prepare the exact one-file counterpart implementation prompt; do not implement with a secondary model.
3. Dispatch only when an exact counterpart flagship CLI account is available.
4. Freeze the repaired proposal commit and run fresh protected-scope audit before any script extraction.
5. Keep all host/budget/authority holds unchanged.
