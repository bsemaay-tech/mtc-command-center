# MCC Command Center end-to-end audit — 2026-05-31

Tam audit: backend Python (mcc_readonly, 17 modul), web app, testler, HTTP sunucu, controlled writer gate, ve dün geceki overnight backtest entegrasyonu.

## Skor kart

| Boyut | Skor | Not |
|---|---|---|
| Test geçişi | 29/29 ✅ | tüm modüller covered |
| Type hint kapsamı | 271/272 fonksiyon ✅ | optimization_reader'da 1 eksik (önemsiz) |
| Bare except sayısı | **0** ✅ | tek anti-pattern yok |
| TODO/FIXME | 1 (sabit string) ✅ | yorum-olarak FIXME yok |
| HTTP güvenliği | ✅ | path traversal protected, read-only enforced |
| Yazma yolu güvenliği | ✅ | atomic temp+rename, lock file |
| Schema validation | ✅ | health OK, schema_validation_ok=true |
| Overnight veri entegrasyonu | ✅ | MEGA tanındı, audit yeni URL'leri yakaladı |

## Kritik bulgular

### Mükemmel ✅

1. **HTTP Read-only Enforcement** ([server.py:50-60](08_DASHBOARD_APP/apps/api/mcc_readonly/server.py:50-60))
   POST/PUT/PATCH/DELETE 405 dönüyor. Test dosyası `test_readonly_core.py` bunu doğruluyor.

2. **Path traversal koruması** ([server.py:74-82](08_DASHBOARD_APP/apps/api/mcc_readonly/server.py:74-82) ve [server.py:111-117](08_DASHBOARD_APP/apps/api/mcc_readonly/server.py:111-117))
   `_send_static_file` resolved path'in web_root altında olduğunu kontrol ediyor. `_send_report` ise 04_REPORTS dışındaki dosyaları engelliyor.

3. **Writer atomic write pattern** ([writer.py:255-278](08_DASHBOARD_APP/apps/api/mcc_readonly/writer.py:255-278))
   temp file + rename ile partial write riski yok. Lock file `open("x")` ile atomic-create-or-fail.

4. **Schema'lı task validation** ([writer.py:VALID_TASK_STATES](08_DASHBOARD_APP/apps/api/mcc_readonly/writer.py:20))
   Whitelist tabanlı state machine; bilinmeyen state'ler reddediliyor.

5. **Test coverage breadth** — Her ana modülün dedicated test dosyası var:
   - audit_reader: 280 satır test (1231 satır source)
   - pipeline_reader: 197 satır test (946 satır source)
   - backtest, parity, registry, optimization, liveops, pine_builder, writer hepsi covered.

6. **Overnight data integration doğrulandı**:
   - `/api/snapshot` 128 eligible candidate gösteriyor (dün 109 → manual_backfill source map URL'leri otomatik yakalandı)
   - `backtest_status.last_successful_run = "MEGA_walk_forward"` — dün geceki run tanınıyor
   - `quantlens_result_runs: 14` — QuantLens lab JSON'ları okunuyor

### Orta öncelik ⚠️ (refactor adayları)

1. **`pipeline_reader.build_candidate_pipeline` = 264 satır, tek fonksiyon** ([pipeline_reader.py:624](08_DASHBOARD_APP/apps/api/mcc_readonly/pipeline_reader.py:624))
   En büyük tek fonksiyon. Pipeline aggregation logic'i parçalanmalı: stage_classification, source_lookup, metric_extraction ayrı fonksiyonlara. Test edilebilirliği artar.

2. **`audit_reader.py` = 1231 satır, 43 fonksiyon, 0 sınıf** (heavily procedural)
   En uzun 5 fonksiyon: `_audit_row_base` (97), `_finalize_audit_rows` (73), `_split_candidate_analysis` (64), `_source_record` (64), `_blocked_reason` (58).
   Öneri: `CandidateAuditBuilder` sınıfı ile group + state encapsulation. Şu an her fonksiyon birbirine `source_index` dict'i pas ediyor — sınıf bunları field yapar, signature'lar küçülür.

3. **`backtest_reader` UX gap** ([backtest_reader.py](08_DASHBOARD_APP/apps/api/mcc_readonly/backtest_reader.py))
   MEGA matrix run'ları için `symbols`, `timeframes`, `trade_count` alanları None geliyor (dashboard'da boş görünüyor). Reader status=COMPLETED'i doğru çıkarıyor ama matrix metadata'sını (17 sembol × 5 TF × N strateji) extract etmiyor.
   **Fix**: MEGA results JSON'unun `config` ve `results` array'ini parse edip aggregate'le.

4. **Audit reader file scanning her snapshot'ta** ([audit_reader.py:_load_source_index](08_DASHBOARD_APP/apps/api/mcc_readonly/audit_reader.py:71))
   `/api/snapshot` her çağrıldığında tüm glob'lar yeniden yürünüyor (research/**/*.jsonl, 12_LLM_WIKI/**, vs). 177 candidate × multiple file walks. Mevcut data setiyle hızlı ama 1000+ candidate'a ölçeklenmez.
   **Fix**: TTL-based memoization (file mtime hash key).

### Düşük öncelik 🟡 (cosmetic)

1. **Writer lock file stale-detection yok** ([writer.py:206-224](08_DASHBOARD_APP/apps/api/mcc_readonly/writer.py:206-224))
   Process mid-write crash olursa lock file kalır, sonraki yazma manuel temizleme gerektirir. Lock age check eklenebilir (e.g., > 5 dk eski lock'u stale say).

2. **Server'da CORS header yok** — Yalnızca lokal kullanım için OK ama farklı origin'den çağrı yapmak istenirse engellenir.

3. **Static file resolve `strict=False`** ([server.py:76,95,111-112](08_DASHBOARD_APP/apps/api/mcc_readonly/server.py:76))
   Var olmayan path'i resolve etmeye çalışıyor. Existence check var ama önce `strict=True` yapılabilir.

4. **Windows case-sensitivity** ([server.py:77](08_DASHBOARD_APP/apps/api/mcc_readonly/server.py:77))
   `str(requested).startswith(str(root))` Windows'ta path case farkıyla yanlış POSITIVE veya NEGATIVE üretebilir. `requested.is_relative_to(root)` daha güvenli.

### Test gap'leri 🧪

1. **HTTP integration test yok**: Server start → request → response döngüsü test edilmiyor. Sadece reader fonksiyonları unit-tested. `test_server_integration.py` eklemek değerli.

2. **Performans bütçesi yok**: `/api/snapshot` cevap süresi için baseline yok. Eklemek için: snapshot < 500ms hedefi ile gating test.

3. **audit_reader test ratio**: 280 satır test / 1231 satır source = %22. Yeni split_candidate_analysis ve source_quality_classification fonksiyonlarına focused test eklenebilir.

## Overnight backtest entegrasyon doğrulaması

| Veri | Reader | Dashboard'da görünüyor? |
|---|---|---|
| Yeni manual_backfill URL'leri | audit_reader ✅ | has_url=True doğru gösteriyor |
| MEGA_walk_forward.json (dün gece) | backtest_reader ⚠️ | tanındı ama matrix metadata boş |
| 19 yeni candidate prototip dosyası | pipeline_reader ⚠️ | spec dosyaları var ama dashboard'da aday olarak görünmüyor |
| FOCUSED_VALIDATION subdir | backtest_reader ⚠️ | subdir taranıyor mu? — manuel inceleme gerekli |

## Önerilen Eylem Listesi (öncelik sırası)

1. **backtest_reader matrix metadata fix** (1-2 saat) — MEGA dashboard'da bilgili görünsün
2. **pipeline_reader.build_candidate_pipeline refactor** (3-4 saat) — 264 satırı 5-6 alt fonksiyona böl
3. **HTTP integration test ekle** (1 saat) — gerçek server start+stop+request döngüsü
4. **audit_reader memoization** (2 saat) — TTL cache ile snapshot hızlandır
5. **Writer stale lock detection** (30 dk) — kaybolmuş lock'ları otomatik tespit et
6. **server.py Windows case-safety** ([str.startswith → Path.is_relative_to](08_DASHBOARD_APP/apps/api/mcc_readonly/server.py:77)) (15 dk) — küçük güvenlik sıkılaştırma

## Genel değerlendirme

**8.5/10** — Çok temiz codebase. Güvenlik tertemiz, type hints kapsamlı, atomic write pattern doğru, tests passing. Refactor adayları daha çok "ileride büyüme için" iyileştirmeler, acil değil. Read-only philosophy disipline edilmiş — dashboard tarafından hiçbir yazma yolu yok.

Asıl gözlem: **Bu codebase production-ready durumda**. İlerideki ölçeklenme için audit_reader/pipeline_reader refactor'u, kısa vadeli için ise backtest_reader UX bug'ı (MEGA metadata) en görünür olan.
