# Owner decision — Option A: two prescribed repairs + confirmation-only pass — 2026-08-16

Recorded from the owner's chat message after the round-4 stop-and-report.
Authorizes ONLY: (1) one authoritative installation sentence in Plan V6 §3
with the complete admitted object list (incl. operator-side backup
directories, current payload path, full unshortened annex hash), the annex's
duplicate draft sentence explicitly subordinated; (2) failed-attempt cleanup
repair — missing unit never aborts cleanup; partial installation still fully
removed; a genuine stop failure of an EXISTING unit remains fail-closed.
Focused RED/GREEN for the cleanup branch (missing unit → continues; existing
unit with failing stop → stops and reports). NO candidate/payload/launcher/
Bridge-code/dashboard changes; NO full-suite rerun (candidate bytes
unchanged); confirmation-only reviews — Codex confirms both exact closures,
Claude confirms the sentence closure + no unrelated bytes changed; no fresh
adversarial search, no new findings in scope. Both confirmed → record
acceptance + present the corrected sentence. Either fails → stop, report only
that. KVM2 untouched until the owner approves the sentence.
