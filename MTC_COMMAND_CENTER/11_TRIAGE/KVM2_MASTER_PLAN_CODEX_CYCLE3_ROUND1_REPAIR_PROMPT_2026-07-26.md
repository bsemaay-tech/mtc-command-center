# KVM2 Codex-only audit cycle 3 — repair round 1

Work in `C:\LAB\Tradingview_LAB_CLEAN`. Read and obey `AGENTS.md`, with this
explicit latest owner override for this artifact cycle:

> Barış explicitly authorized a new KVM2 documentation audit cycle, waived/reset
> the three-round repair cap for this artifact only, prohibited Claude-credit
> use, and required Codex repairs plus fresh Codex audits.

This is therefore a **Codex-only** cycle. Do not invoke Claude, Cline, DeepSeek,
Grok, OpenRouter, or another provider. Root Codex remains lead and independent
acceptance authority; this Codex CLI session is the bounded implementer under
the explicit owner waiver.

Preserve every unrelated dirty-worktree change. Do not run Git mutations,
access the VPS/runtime, install/deploy, use secrets, contact brokers/exchanges,
or perform network/trading actions.

Edit only:

- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_AI_LAB_AND_BRIDGE_MASTER_PLAN_2026-07-25.md`
- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_AI_LAB_AND_BRIDGE_EXECUTION_TASKS_2026-07-25.md`
- `MTC_COMMAND_CENTER/11_TRIAGE/KVM2_MASTER_PLAN_MULTI_MODEL_AUDIT_PROMPT_2026-07-25.md`
- `MTC_COMMAND_CENTER/_AI_MEMORY/DECISIONS.md`

The final Claude audit of the preceding cycle returned `REQUEST_CHANGES`.
Apply these six required repairs:

1. **Kodee trust model**
   - Reclassify Hostinger Kodee as an out-of-band provider control-plane actor,
     not a resident `ai-lab` Unix-user workload.
   - Require panel-account MFA.
   - Forbid Kodee-initiated snapshot, restore, reboot, firewall, or service
     action while the bridge is deployed.
   - Any such action is a P5-04 monitoring reset/reclassification event; an
     unexplained action triggers the master stop rule.
   - P2-11 must include an unexpected-provider-panel-action incident branch.
   - Remove false assurance that guest Unix permissions constrain Kodee.

2. **Safe Option-A wipe**
   - Before P10-03C wipe evidence: bridge DISARMED; raw exchange-side
     positions/orders flat proof; final WAL-consistent state capture;
     isolated restore verified under P2-06/P5-03; agent authority revoked;
     writer count exactly zero.
   - Stop if wipe is attempted ARMED, non-flat, unreconciled, with a writer
     remaining, or without the verified final restore.
   - Synchronize the master authority row.

3. **P3-03 Ubuntu staging host**
   - Require a named expendable Ubuntu 24.04 environment using the P2-09
     environment classes; explicitly forbid active KVM2.
   - If unavailable, record `BLOCKED/UNVERIFIED` and carry the exact environment
     plus verdict into P3-05 evidence.
   - Stop any live-KVM2 staging install or undeclared environment.

4. **P6-02 live-host authorization**
   - Extend P6-00A's one-shot campaign scope to P6-01, P6-02, and P6-03.
   - P6-02 must explicitly implement its user/workspace and execute denial tests
     only under P6-00A.
   - Add the same pre/post bridge PID, unit hash, loopback listener, restart
     count, reconciliation freshness, and exchange-connectivity assertions as
     P6-01/P6-03; regression invokes P5-10(e) rollback and halts Phase 6.
   - Synchronize master Phase 6 and authority rows.

5. **Normalize audit cycles and record owner waiver**
   - Add `D023 | 2026-07-26` at the top of `DECISIONS.md`: Barış explicitly
     authorizes this new KVM2 documentation audit cycle, waives/resets the
     three-round cap for this artifact only, and requires Codex-only
     repair/audits with no Claude credits. It authorizes documentation work only,
     no Git/runtime/VPS/deploy/trading action.
   - Use one identical cycle map in both plan headers:
     - Cycle 1: original rounds 1–3, closed non-accepting at the cap.
     - Cycle 2: R1 = round-5 focused repair; R2 = staged round-6/7 repair;
       R3 = final-repair-sync/slices; closed by the final non-accepting Opus
       verdict and STOP.
     - Cycle 3: owner-authorized by D023; current state is R1, Codex-only.
   - State that D023 acknowledges and ratifies the legacy cycle-label history
     and governs the new reset. Cite D023 in master §11.
   - Update the audit prompt to require only the canonical fresh Codex auditor
     for Cycle 3: exact `gpt-5.6-sol`, `xhigh` because this is a security/
     architecture re-audit, fresh ephemeral read-only session, no fallback.
     This is an explicit owner waiver of the Claude auditor for Cycle 3 only.

6. **Define first-deploy rollback**
   - P4-08 must prove exactly zero writers while the service is stopped/disabled;
     no Windows task/process/listener/exchange authority is re-enabled.
   - For first deployment, rollback target is stopped/disabled with state
     artifacts preserved.
   - Re-establishing a Windows writer requires new P4-03-class secret
     authorization and is outside P4-08.
   - Crosswalk row 8 must say rollback procedure tested at P4-08; rollback SHA
     applies only when a prior accepted VPS release exists.

Final constraints:

- Do not add a task. Keep 84 unique standard-shape IDs.
- Every task retains exactly one `  - Evidence:` and one `  - Stop:` line.
- Crosswalk rows 1–10 appear exactly once.
- Keep master and companion below 60,000 bytes. The companion has little
  headroom; compress redundant prose/table wording only, never gates.
- Keep zero full `KVM2-P...` IDs in master.
- Preserve preparation-only status and every authorization separation.
- Compute fresh byte-level SHA-256 for master and companion; update the audit
  prompt current expected hashes and Cycle-3/R1 label; move prior values to
  superseded history.
- Do not claim an audit verdict.

Return concise validation: changed sections/tasks, D023, 84/84 structure,
crosswalk, sizes, and fresh hashes.
