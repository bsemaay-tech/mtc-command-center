# P020 interface contract — revision1, pending independent fixture seal
Authority: owner implementation approval in this task. Base42f99571e6abb5222744d9345e1c8a9e19c23c15. Proposed semantic2.1.0 is currently absent from _SUPPORTED_VALUES. P012 owner confirms no semantics.py collision. No product edit until the Lead seals this contract and independent fixture expectations in FREEZE_RECEIPT.json.

## Immutable boundaries
Preserve existing public signatures and behavior for1.0.0/2.0.0, all original schemas/hash member sets and production economic-record admission. No old core/economics.py,instrument.py,runner.py or record-file edits. New2.1.0 path is explicit, research-only and non-accepting. No automatic fallback to it, synthetic records, legacy sizing or default policies. No signal/strategy/grid alteration. Constructor/optional keyword additions below apply only to explicitly selected new paths.

## Successor and allocator seam
NEW core/p020_economics.py contains P020EconomicsAdapter, a version-specific ExecutionEconomics with semantics_id2.1.0. Keep resolve(state,intent,market_event,records)->EconomicTransition exactly. Immutable per-decision P020 binding/context is supplied to its constructor (BoundSizingIntent, AccountSnapshot, AllocationPolicy, RiskBucket, explicit AllocationPolicyCaps, optional observed-volatility value+estimator identity). No shared/global monkeypatch, no mutation of old adapter, no pre-sizing at the caller. Reuse CorrectedEconomicsAdapter exit/funding behavior and current validated record loading without changing their source. Override the new OPEN path so fill/slippage/alignment is resolved first, the shared allocator computes exactly once inside resolve, caps reject in full and one immutable transition carries final quantity/cash. Existing old adapters stay independently selected and unchanged.

Add a four-method dispatcher in root shared_risk_calculator.py, preserving all existing seed functions/signatures. It consumes the existing SizingRequest plus bound snapshot/policy facts, final aligned fill price, authoritative metadata, existing gross exposure and explicit method-specific observations. Reuse its deterministic Decimal context, quantity-floor and full-rejection helpers where contracts match. Methods: risk-at-stop=equity*risk_fraction/(absolute final-fill-to-stop distance*multiplier); fixed quantity=requested constant; fixed notional=requested quote notional/(final fill*multiplier); volatility target=(equity*target/observed volatility)/(final fill*multiplier), with finite positive observation and exact matching frozen estimator identity and convention. No estimated-vol fallback or silent annualization. Require applicable independent stop-risk ceiling, leverage and gross-exposure caps; inability to calculate a required cap refuses even for fixed/vol sizing. Method/source provenance must not change arithmetic. Check stop direction, nonfinite/nonpositive inputs, step/minimum quantities/notional; equal-to-cap is allowed, breach refuses. Never trim to cap. Do not infer stop ceiling from requested risk.

OPEN binding validation: exact2.1.0, package/instrument metadata/record IDs, policy version, bucket membership, account snapshot digest and timestamp, positive explicit freshness deadline, nonfuture/nonstale snapshot; decision timestamp/bar and request direction must agree with economic intent. Bind account equity/exposure to that same snapshot and actual portfolio state. allocator_reference_qty is audit-only; it never sets quantity. Input structs are not silently coerced to make identity checks pass.

## Research integration and portfolio
NEW portfolio_simulator.py owns mutable research account/position/cash state and immutable snapshots; NEW p020_simulator.py adapts research signals/intents to the successor and existing contracts. One portfolio commits each accepted transition exactly once, applies each cash event once, advances sequences/lifecycles and preserves partial positions. It never recomputes quantity. Refusals leave position/equity unchanged while latching the same-bar decision key. Key account,bucket,package,instrument,bar,frozen decision slot remains stable even when caller supplies new intent/snapshot IDs; only bar advance permits retry. Decision slots are not an arbitrary caller escape hatch.

Consume existing RiskBucket/AllocationPolicy/GuardianPolicy identities; any additional research guard settings are ordinary implementation configuration in these approved new modules, not a new external schema. Six guards: positive daily-loss fraction>=cap; trades>=max; positive drawdown fraction>=cap; consecutive losses>=max; equity<SMA once enough samples; previous closed-trade MAE>cap (equality allowed). Preserve observed GROSS-MINUS-FEES basis, daily reset and full-close counters. Enabled guard with absent required values refuses. Keep daily/peak equity references explicit. Required account-wide exposure checks cannot be replaced by only current-position notional. Missing real values remain unset; tests supply labelled synthetic constants.

Canonical mega_walk_forward.simulate_slice gains an explicit optional P020 context/profile keyword; old calls remain old behavior. New path consumes existing sig/stop/exit_mode arrays unchanged and returns its established result shape plus recorded semantic/ledger identity where supported. An explicit corrected request lacking required context refuses instead of running legacy. Consumers must carry the parent's explicit mode/profile and preserve immutable source artifacts. Old two independent simulators are retired/frozen as decided, with refusal at corrected/acceptance use. Patcher refuses before mutation on corrected path. Reference trades project the actual corrected transition ledger, never its old manual replay under a new label.

## Identity/evidence contracts
Add helpers only to contracts/mtc_contracts/identity.py: compute_account_snapshot_id(snapshot)->SHA256(canonical_json(snapshot without snapshot_id)); raw original manifest digest remains dataset_manifest_sha; selected dataset manifest hash covers original raw digest plus entries sorted by dataset_id/path with ID,path,raw CSV digest,symbol,timeframe,row_count,interval. Preserve first matching PASS selection from original manifest BEFORE sorting selected entries. Validate unique IDs, fields and deterministic types. Original package/evaluation/deployment/trial formulas remain byte-identical. Exact helper names may be aligned once across builder and tests before the receipt seal; no schema fields added.

Independent E/X/V/M proof uses the exact control vocabulary/carrier adapter in the approved preparation IMPLEMENTATION_HANDOFF. E derives enabled controls from frozen config, X records actual transition execution, V checks independently authored relations, M joins validated claims; claimed names alone cannot satisfy promotion. Root ControlEntry maps to shared UnsimulatedControl without changing its fields. Future acceptance requires applicable full P012 admission/reviews; synthetic success remains a separate evidence class.

Expected values are authored and reviewed before use of implementation output. Economic relations use exact Decimal values; the retained P012 float boundary is tested using explicitly frozen binary/serialized expectations, not a broad guessed epsilon or invented production rounding. Mutations must break meaningful relations. Missing/extra events, fee/funding double application, skipped controls, identity tampering and stamped legacy outputs refuse. All expected artifacts are frozen with digests; fixes require reasoned versioned expectation corrections, never output-based resealing.

## Measurement-only harness
NEW p020_benchmark_runner.py may measure a bounded caller-supplied synthetic trial callable and journal identities/results; it must not launch market workloads or dynamically import arbitrary modules from ledger data. Record individual wall times, median/p90, total elapsed and successful/failed/eligible/executed counts separately. No feasibility PASS inferred from latency percentiles. Whole-grid/heavy run is disabled absent separate later authority. Resume must exactly reproduce trial-ID/result sets, skip completed IDs, refuse changed inputs/duplicates/tamper, and handle a torn final record without pretending it completed. Timing is diagnostic. Candidate/acceptance status remains blocked where required producers/limits are absent.

## Write ownership and completion
Economic flagship: shared_risk_calculator.py + core/p020_economics.py + additive core/semantics.py only.
Integration flagship: root portfolio_simulator.py,p020_simulator.py and tools/mega_walk_forward.py only, after economic seam frozen.
Identity writer: contracts/mtc_contracts/identity.py + contracts/tests/test_p020_identity.py.
Consumer writer: the exact eight tools plus probabilistic_pbo.py named by the approved handoff; exclusive from canonical mega writer.
Evidence writer: exact root checklist/manifest/evidence/cost/battery/checker files listed by handoff and NEW research_verification/p020/projections.py,test_execution_proofs.py.
Benchmark writer: root p020_benchmark_runner.py + NEW research_verification/p020/test_restart.py.
Lead owns external state/specs, verification and exact local commits. All lanes use feature/p020-successor-20260912 at C:/P020_IMPL_20260912; no live/scheduled dependencies or foreign writers. CI wiring only the approved research-gates.yml when a tested scoped command exists. Required role and review rules remain binding.

First complete example: synthetic account1000, LONG entry100/stop95, risk1%, multiplier1, step1, explicit permissive fixture caps yieldsquantity2, thenexit110; 2bps entry+exit fees total0.084, net gain19.916, one position closes, all cash applied once. Repeat SHORT, other methods, partial exits, cap refusal and snapshot/guard tamper. This is research correctness evidence, never production acceptance.
