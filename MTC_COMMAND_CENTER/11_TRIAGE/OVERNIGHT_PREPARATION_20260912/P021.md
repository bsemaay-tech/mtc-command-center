# P021 bounded preparation report — objective eligibility/readiness (editorial close)

Prepared under overnight control run 01a0927b-2a2a-7160-b1ba-c75f9baed952. Pin `42f99571e6abb5222744d9345e1c8a9e19c23c15` (42f9957).
Scope: nonbinding implementation-decision preparation. Nothing here is implemented, accepted, or executable authority. No product implementation is authorized this window. Pure code is still implementation.
Evidence root: `inputs/source/` is a Git archive **materialization** of 42f9957: **120 raw blob matches + 418 CRLF-only**. Source content is equivalent; **not all raw blobs are identical**. Separate `qa/source_lf` contains **538 exact raw Git blobs**. Do not overwrite historical original inputs or logs.
Design inputs: `inputs/P021_DESIGN_DRAFT_V1.md`, `inputs/P021_FIFTEEN_NUMBERS_PACKET.md`, `inputs/P021_ENGINEERING_DESIGN_V2.md` (v2.1), `inputs/P021_EVIDENCE_POLICY_PROFILES.md`, `inputs/P021_HISTORICAL_MEASUREMENTS.json`, `inputs/OWNER_ENGINEERING_AUTHORITY_20260906.md`, `inputs/OWNER_PROVISIONAL_POLICY_20260906.md`. Absence claims are scoped to that inventory.
New fixtures remain `PROPOSED_NOT_EXECUTED`.

## 0. Executed component evidence (not package acceptance)

Lead executed `p021_readiness_rules.py --self-check` on Python 3.12.12 (`qa/P021_existing_check.log`; same result on the `qa/source_lf` tree): exit 0; **7 checks REFUSED**; **12 closed / 4 open** numbers; two modified-copy detections (gap_ratio_max wiring removed; swing_forward_period closure removed); `READY=False`. This is the **existing catalogue self-check only**. It is not a readiness evaluation, not execution of any §4 proposed fixture, and not package acceptance. `ready` stays false; `implementation_authorized` stays false.

Fresh independent measurement (`qa/P021_FRESH_DATA_VERIFICATION.json`, exact scope used here): **17** Binance Futures 5m research CSVs; **4,105,966** rows; all 17 SHA256 hashes match the historical records; all 17 row counts match; **internal_missing_slots=0**, duplicates=0, out_of_order=0, off_grid_intervals=0. Limits of that scan: historical Binance research series only; no OHLCV-value verification; not Hyperliquid venue truth; **gap_ratio_max remains unset**. Measured internal-gap 0 is a **historical corpus observation**, not an eligibility policy limit.

Source producer still distinct from that measurement: `inputs/source/MTC_COMMAND_CENTER/01_MTC_PROJECT/tools/create_optimization_data_bundle.py` `TIMEFRAME_SECONDS` (:51) has **no `5m`** entry (`15m/1h/2h/4h/1D` only); gap estimates use **`int` truncation** (`:317`, `:324` `int(delta / expected_step) - 1`; `:354` `int((t_last-t_first)/step)+1`). Unknown timeframe silently skips the gap scan. Do not treat producer output as the independent 17-series measurement, and do not infer a ratified zero gap limit from either.

## 1. Current effective contract versus history

### 1.1 Current effective contract (source implementation verified)

Live module: `inputs/source/MTC_COMMAND_CENTER/03_QUANTLENS/tools/p021_readiness_rules.py` (422 lines).

- **Policy object** (`POLICY_SET`, :52-68): `policy_version="v1"`, `status="PROVISIONAL"`, `profile="balanced"`, `P021_DECISION_1="A"`, `P021_DECISION_2="balanced"`, `decided_at="2026-09-07"`, `owner_decision=186` (:315). Reversibility: new hashed `strategy_type_policy_set` version plus fresh evidence (decisions 129/130) (:64-67).
- **Twelve closed numbers** (`CLOSED_NUMBERS`, :73-159) = balanced column of `P021_EVIDENCE_POLICY_PROFILES.md` §3, including `day_forward_period=21` days and `stop_loss_ceiling_equity_fraction=0.005`. Position-class values carry `extrapolated=True`.
- **Four open numbers** (`OPEN_NUMBERS`, :165-170): `gap_ratio_max` (B-02), `divergence_tolerance` (B-06), `divergence_window_length` (B-07), `divergence_min_paired_observations` (B-07). Methods exist in design v2.1; **values remain unset**.
- **Seven rules** (`RULES`, :174-235) all permanently `REFUSED` in this catalogue (`readiness_record`, :298-337: `"ready": False`).
- **Fail-closed validation** (`validate_catalog`, :253-296) and **self-check** (`self_check`, :340-377).
- Diagnostic `check_set_version: 2` (:320). **Preserve current source value 2.** Mapping this diagnostic int onto contract `EligibilityVerdictSet.check_set_version: NonEmptyStr` is an **engineering adapter decision**. No owner ratification of the bump is requested; no change to the diagnostic reporter is proposed.

### 1.2 Provenance (source commit trail; no artifact-retrieval question)

Source git history of the catalogue: **`a63bd4b053648e4a7008e5f2fe5f999b94947650`** original catalogue → **`380a26aacf960ee33fd610ec699dcc2180e4eb8a`** records `P021_DECISION_1=A` / `P021_DECISION_2=balanced`. Preserve that provenance distinction.

`inputs/OWNER_PROVISIONAL_POLICY_20260906.md` records A/balanced as **provisional recording authorized in prior scope**. `inputs/OWNER_ENGINEERING_AUTHORITY_20260906.md` lets engineering define taxonomy, gap formula, divergence metric, and policy-version contracts, and forbids re-asking answered decisions. **No blanket new implementation authority** is taken from those historical documents. Decision 186's full chat-reply artifact is not required to complete preparation; do not ask the owner to supply it.

Draft v1.12 VALUE-PENDING tags do not control the module's recorded closure of the twelve numbers. The fifteen-numbers packet is historical input. Four measure-first numbers stay open.

### 1.3 Two differently labelled day-forward metrics (no silent numeric repair)

`day_forward_period = 21` days is the **applied minimum** in source (`CLOSED_NUMBERS`, :103-111; profiles Rule 3 clocks → 21 d). The same profile also reports **4 weeks as an expected median wait** (owner-facing summary / Decision 2 median-wait row). These are **different-labelled metrics**, not a rounding error and not a universal discrepancy to close by rewriting 21→28. Preserve both labelled meanings. A future policy version must explicitly label `min_period_days` versus `expected_median_wait`. Application source remains **21 days, not 28**.

### 1.4 Module self-declared limitation

Docstring :1-16: evaluates no candidate, admits nothing, engine prerequisite unmet, refuses when limits/rules unset. `accepted_engine_precondition = "NOT_MET_OVERRIDDEN_FOR_THIS_BOUNDED_BUILD"` (:317).

## 2. Material implemented / missing claims

### 2.1 Source implementation verified (in supplied source)

1. Readiness-rule catalogue, seven checks, all REFUSED — :174-235, 298-337. Implemented ≠ ready (:318).
2. Fail-closed catalogue validation + modified-copy detection — :253-296, 340-377 (self-check executed, §0).
3. WP-P0-04 eligibility shapes — `contracts/mtc_contracts/admission.py:28-66`.
4. Identity formulae — `identity.py` six-member `compute_evaluation_run_hash` :76-95 (unchanged); `compute_deployment_identity_hash` hashes **12 named members** (`package_hash`, `allocator_code_sha`, `allocation_policy_version`, `guardian_code_sha`, `guardian_policy_json`, `risk_bucket_policy_json`, `economic_policy_json`, `runtime_policy_json`, `protection_semantics_json`, `broker_adapter_id`, `broker_adapter_version`, `cost_lineage_json`; environment lineage excluded) :98-129; `compute_package_hash` :54-73; `canonical_json` :31-44.
5. Eligibility state ladder — `admission.py:21-25`; admitted-environment sets :81-90.
6. Unsimulated-control shape — `lineage.py:29-32`.
7. `DurableRiskPolicy` hashed-policy construction pattern — `IBKR_PAPER_BRIDGE/bridge/engine/types.py:1327` exists: create validates finite numeric fields and hashes canonical JSON containing `float.hex` strings, prefix `rpol-v1`. **Reusable design pattern only**; do not import or alter the protected Bridge, and do not force a P020 carrier.

### 2.2 Engineering-designed, not implemented (no code in `inputs/source/`)

Keep these distinct from §2.1:

1. **B-22 taxonomy + hashed policy set** — specified: series unit `(strategy_id, asset, timeframe)`; holding-time-axis typing; four named refusals; `p021pol-v1:` hashed object. Not built.
2. **B-05/B-07/B-20 divergence machinery** — specified (R units, residual-only tolerance, pairing key). Cannot run: no accepted engine, no completed forward window, no populated `dataset_hash`.
3. **B-17 gap machinery (M2)** — formula specified with `round`, not the producer's `int` truncation. 5m missing from producer `TIMEFRAME_SECONDS`. Unknown timeframe must raise.
4. **B-13 `ds-v1:` dataset hash** — construction defined; field exists (`BacktestResult.dataset_hash`) and is never computed. This is a **third** identity, never silently assigned to P020 `dataset_manifest_sha` or selected-dataset digest.

### 2.3 Still open (unchanged)

1. No eligibility module constructing `EligibilityCheckResult`/`EligibilityVerdictSet` for the seven checks.
2. B-01 accepted corrected engine — synthetic-only milestone only; every rule lists `accepted_corrected_engine.B01`. P012 production remains NONACCEPTED.
3. B-08/B-19, B-12/B-21/B-16, B-09 testnet leg remain open.
4. Four measure-first numeric limits remain unset.

### 2.4 Module conflicts, reframed (no owner field-naming)

1. **`check_set_version` int 2 vs contract `NonEmptyStr`.** Diagnostic JSON, not a live `EligibilityVerdictSet`. Adapter maps `2` → string using the **current source value 2**. Not a reproduced runtime failure; not an owner ratification item.
2. **`policy_version` vs allocation/guardian versions.** Separate domains. P021 readiness-policy hash **references** frozen allocation/guardian identities; **no new P020 policy carrier**. Shared versioning mechanism ≠ identical version strings.
3. **3-week applied minimum vs 4-week expected median wait** — §1.3; both preserved.
4. Name mapping `day_trade_count_min` → `day_trade_count_starting_rule` (value 30) is design v2.1 §a.5 rule 2. Prefix/path terminology is an engineering adapter decision.

## 3. Cross-package interfaces

| Interface | Producer | Consumer | Mapping / rule |
|---|---|---|---|
| `EligibilityCheckResult.dataset_hash` | Future bundle pass (`ds-v1:` of §f.2) | P021 check results | Selected-dataset recipe; **distinct** from P020 `dataset_manifest_sha`. Never silently assigned. |
| `EligibilityVerdictSet.evaluation_run_hash` | P020 six named members (`identity.py:88-95`) | P021 verdict set | Consume, never re-derive. Six-member hash **unchanged**. P020 freeze independently verified (`qa/P020_FREEZE_VERIFICATION.json`, SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5`, `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`). Full integration **`WAIT_P020_ACCEPTANCE`**. No new P020 policy carrier. |
| `EligibilityVerdictSet.deployment_identity_hash` | `compute_deployment_identity_hash` (**12 named members**, `identity.py:116-129`) | P021 verdict + per-check | Per-check identity must match set identity. |
| `strategy_type` | Classifier §a.4 over frozen backtest record | P021 / P013 / P020 | Input, never inferred by independent sides. |
| `policy_set.version` | Proposed `p021pol-v1:` object | Readiness module + every result | No defaults; different version → `EXPIRED`. References allocation/guardian versions; does not replace them. |
| Method identities | `data_gap_ratio_m2_v1`, `p021_divergence_v1`, `rule_based_market_regime_v2` | The numbers they limit | Travel inside the policy set. |

P020 2.1.0 successor stays read-only here. P012 remains NONACCEPTED; no P021 build step may assume the corrected engine.

P030 OPEN-N12/N13: inherit method `data_gap_ratio_m2_v1` after a separately authorized producer 5m/`round` fix; **no number crosses the seam** until decision rules run on real scans. Measured corpus gap 0 is not that number.

## 4. Independently derived tests (five existing-catalogue checks executed; rest proposed)

Lead executed T-P021-01, T-P021-02, T-P021-03 short, T-P021-03 duplicate, and T-P021-04 against the **existing catalogue** on Python 3.12.12 (`qa/P021_LITERAL_FIXTURE_CHECK.json`). Those five checks are `EXECUTED_EXISTING_CATALOGUE_ONLY` — not helper or evaluator execution. T-P021-05…09, F-1…F-3, and all new helper/eligibility files remain `PROPOSED_NOT_EXECUTED`. Standalone fixture-bank 24 target cases stay proposed.

**T-P021-01 — refusal-wiring mutation.** Input: copy `RULES` with index 3 (`P021.DATA_QUALITY`) replaced by `replace(RULES[3], missing_numbers=())`; `OPEN_NUMBERS` and `CLOSED_NUMBERS` unchanged. Call `validate_catalog(modified_rules, OPEN_NUMBERS, CLOSED_NUMBERS)`. Expected: `ValueError` `open-number refusal wiring changed; missing=['gap_ratio_max']; extra=[]` (`wired_names != EXPECTED_OPEN_NUMBER_NAMES`, :282-286). Failure mutation: a `validate_catalog` that returns normally on the unwired copy. Status: `EXECUTED_EXISTING_CATALOGUE_ONLY`.

**T-P021-02 — closed-number removal.** Input: `CLOSED_NUMBERS` filtered to drop `name == "swing_forward_period"` (11 closed). Call `validate_catalog(RULES, OPEN_NUMBERS, dropped)`. Expected: `ValueError` `closed-number catalogue changed; missing=['swing_forward_period']; extra=[]` (`closed_declared != EXPECTED_CLOSED_NUMBER_NAMES`, :272-276). Failure mutation: accept the shortened closed set. Status: `EXECUTED_EXISTING_CATALOGUE_ONLY`.

**T-P021-03 — catalogue count mutation.** Input A: `RULES[:-1]` (6 rules). Expected: `ValueError("the designed seven-check catalogue is incomplete")` (:263-264). Input B: `RULES + (RULES[0],)` duplicate `check_id`. Expected: `ValueError("duplicate check id")` (:261-262). Failure mutation: accept 6 or 8 rules. Status: `EXECUTED_EXISTING_CATALOGUE_ONLY` (both arms).

**T-P021-04 — extra closed name (first error is closed-set, not overlap).** Input: `CLOSED_NUMBERS + (ClosedNumber("gap_ratio_max", 0.0, "ratio", "fixture-only"),)`. Actual first `ValueError` against the existing catalogue: `closed-number catalogue changed; missing=[]; extra=['gap_ratio_max']`. Validation stops at the closed-number set check **before** the overlap branch (`:272-276` then `:278-280`). Do **not** expect `a number is both closed and open` as the first error. Fixture-only 0.0; does **not** set policy `gap_ratio_max`. Status: `EXECUTED_EXISTING_CATALOGUE_ONLY`.

**T-P021-05 — contract BLOCKED enforcement.** Input: `EligibilityCheckResult` with `outcome=BLOCKED`, `blocked_reason=None`, otherwise valid `check_id`, hashes, UTC `timestamp`, `environment_lineage` (`admission.py:28-46`). Expected: `ValueError("BLOCKED check must name what is missing")`. Control: same object with `blocked_reason="p021.data_quality.gap_ratio_max_open.B02"` validates. Failure mutation: accept BLOCKED with empty reason.

**T-P021-06 — timestamp predicate.** Input: `EligibilityCheckResult` / `EligibilityVerdictSet` with naive `datetime(2026, 9, 7, 12, 0, 0)` (no tzinfo). Expected: `require_utc` refusal (`admission.py:43, 65-66`). Control: same instant with `tzinfo=timezone.utc` accepted. Failure mutation: accept naive timestamps.

**T-P021-07 — adapter-mapping probe.** Input: pass `readiness_record()["check_set_version"]` (int `2`) into `EligibilityVerdictSet.check_set_version` (`NonEmptyStr`). Expected per current contract: validation failure. Engineering adapter (no owner question, no reporter edit): at future wire-up, map diagnostic `2` to string `"2"` using the **current source value 2**. Later S8b path (not S1): `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p021_eligibility.py`. `PROPOSED_NOT_EXECUTED`.

**T-P021-08 — lookahead acceptance shape.** Catalogue rule `P021.LOOKAHEAD_PREFIX` (:181-186): `intent_mismatch_count_max=0`, `tolerance=0`; missing_rules `accepted_corrected_engine.B01`, `decision_domain.B12`, `intent_hash_contract.B21`. Input: bars `[t0, t1, t2]`; a fixture decision at `t1` that reads `t2`; prefix physically truncated after `t1`. Expected mismatch compares **same** lookahead series: full lookahead vs prefix lookahead. Causal control compares full causal vs prefix causal. Do **not** define mismatch as lookahead vs causal. Expected **today**: BLOCKED (named missing rules), never PASS. Later S8b path (not S1): `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p021_eligibility.py`. `PROVISIONAL-ON-P012`. Failure mutation: emit PASS while B-01/B-12/B-21 remain missing. `PROPOSED_NOT_EXECUTED`.

**T-P021-09 — readiness monotonicity.** Input: DATA_QUALITY with `gap_ratio_max` filled in `limits` but `missing_rules` still `("accepted_corrected_engine.B01", "gap_ratio_formula.B17", "dataset_hash_contract.B13")`. Expected: `readiness_record()["ready"] is False`; that check stays `REFUSED`. Further: `validate_catalog` raises if any rule has empty `missing_numbers` **and** empty `missing_rules` (`:293-295` "could be presented as ready"). Closing one open number must not emit `ready=True` while named missing rules remain. Failure mutation: `ready=True` after filling a single number.

**F-1 — classifier worked example (design §a.4; n<10 refuses).** Full-helper **positive** fixture uses **12** holds `[2, 1.5, 3, 2.5, 1, 2, 2, 1.5, 3, 2.5, 1, 2]` → all `day`; n=12; dominance=1; median=2 → `strategy_type="day"`. Mixed **12** holds `[2, 2, 2, 2, 30, 30, 30, 30, 800, 800, 800, 800]` → `BLOCKED_MIXED_HOLDING_PROFILE`. Six-hold `[2, 1.5, 3, 2.5, 1, 2]` is `BLOCKED_INSUFFICIENT_SAMPLE` on the full helper — **not** `day`. Do not use n=6 as a positive `day` case. Hand-computed; `PROPOSED_NOT_EXECUTED`. S1 path: `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_strategy_type_classifier.py`.

**F-2 — gap metric M2 (design §d.4).** `s=3600`; timestamps `0, 3600, 7200, 14400, 18000`. Missing bars `Σ max(0, round(Δ/s)−1)` = 1. `expected_bars = round((18000−0)/3600)+1 = 6`. **M2 = 1/6 ≈ 0.1667**. Contrast: M1 = 1/4 = 0.25; M3 = 3600/18000 = 0.20. Producer `int` truncation is a different formula; this fixture pins M2. Does not set `gap_ratio_max`.

**F-3 — divergence (design §c.2/§c.3).** One paired observation, decision divergence 0. Replay +1.20R, forward +0.95R → `d = −0.25R`. Decomposition: slippage −0.10R + fee −0.05R + timing −0.05R + residual −0.05R; tolerance applies to residual only. No declared risk unit → REFUSE, no fallback. Later S8b (not S1): `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p021_eligibility.py`. `PROPOSED_NOT_EXECUTED`.

## 5. Minimal proposed implementation (not authorized)

**No unsolicited repair to `p021_readiness_rules.py`.** Reuse it unchanged.

**Reused unchanged:** `p021_readiness_rules.py`; `canonical_json`; `EligibilityCheckResult`/`EligibilityVerdictSet`/`require_utc`; `DurableRiskPolicy` pattern only (Bridge prefix `rpol-v1`; P021 instance would use `p021pol-v1:`; not a new P020 carrier; do not import/alter Bridge). Standalone `create_optimization_data_bundle.py` **5m / unknown-timeframe measurement repair** needs its **own explicit code authority**; it does **not** require P012 production acceptance or the engine. Full eligibility still waits.

**Proposed new files** (exclusive writer: P021 build lane, only after a new bounded authorization naming these paths):

| Path | Role | Acceptance |
|---|---|---|
| `MTC_COMMAND_CENTER/03_QUANTLENS/tools/strategy_type_policy_set.py` | §a.5 object; `p021pol-v1:` hash; 14 numbers null-able | Stable hash; any closed-number/taxonomy edit changes version; unset = refuse |
| `MTC_COMMAND_CENTER/03_QUANTLENS/tools/strategy_type_classifier.py` | §a.4 holds → buckets → dominance → median; four named refusals; n<10 refuses | F-1 12-hold `day` / mixed 12 / n=6 `INSUFFICIENT_SAMPLE`; malformed timestamps refuse |
| `MTC_COMMAND_CENTER/03_QUANTLENS/tools/data_gap_ratio.py` | M2 (+ publish M1/M3); raise on unknown timeframe | F-2 exact; never silent on missing `5m` |
| `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_strategy_type_policy_set.py` | S1 helper policy-set only | Stable hash; null refuse |
| `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_strategy_type_classifier.py` | S1 F-1 | 12-hold `day`; mixed 12 MIXED; n=6 INSUFFICIENT_SAMPLE |
| `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_data_gap_ratio.py` | S1 F-2 | M2=1/6; unknown timeframe raises |

Lookahead / divergence / contract-adapter fixtures (T-P021-07/08, F-3) stay on later `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p021_eligibility.py` (map S8b). No extra S1 file. Do not use `test_p021_helpers.py`.

S1→S2→S3 as listed. **These files are `READY_FOR_SEPARATE_BOUNDED_AUTHORIZATION`.** They are **not** authorized now, **not** inside historical decision-186 implementation authority, and **not** "build-ready / no gate". Decision 186 authorized the existing refusal catalogue only. Full package still depends on P012 admission, P020 accepted evidence (`WAIT_P020_ACCEPTANCE`), B-13 ratification, and the four unset limits.

Blocked exactly: eligibility skeleton (`WAIT_P020_ACCEPTANCE` + P012); standalone bundle 5m/unknown-timeframe measurement repair (own explicit code authority; **not** P012 production acceptance or engine; B-13 still separate); divergence measurement (B-01 + forward window + populated `dataset_hash`); preimage contracts (B-08/B-12/B-16/B-19/B-21). Full eligibility still waits.

## 6. Remaining facts / dependencies — one recommendation each

1. **Four measure-first limits remain unset** (`gap_ratio_max`, `divergence_tolerance`, `divergence_window_length`, `divergence_min_paired_observations`). Measured 17-series internal-gap 0 is observation, not a limit. Recommendation: keep them unset until the named mechanical rules run on real scans after a **separately authorized** bundle 5m/unknown-timeframe measurement repair (own code authority; not P012/engine).
2. **P012 NONACCEPTED; B-01 unmet.** Recommendation: no eligibility PASS path; helpers stay refusals-only.
3. **P020 interface freeze independently verified** (`qa/P020_FREEZE_VERIFICATION.json`, SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5`, `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`); full integration still **`WAIT_P020_ACCEPTANCE`**. Six-member evaluation hash unchanged; no new P020 carrier. Recommendation: consume the frozen surface; do not treat freeze as acceptance; do not pre-commit to unfrozen P020 internals.
4. **Classifier / policy-set / M2 helper files are unbuilt.** Recommendation: if a morning packet authorizes work, authorize **only** the three helper files + `test_strategy_type_policy_set.py` / `test_strategy_type_classifier.py` / `test_data_gap_ratio.py` as an isolated engineering slice; leave the diagnostic reporter and four limits unchanged.
5. **`ds-v1:` vs raw manifest vs selected-dataset digest** remain three identities. Recommendation: P021 adapter maps explicitly; never assign silently.
6. **21-day applied minimum vs 4-week expected median wait** remain different labels. Recommendation: future policy version names both fields; do not rewrite 21→28.
7. **Owner-facing choices that actually remain:** (a) exact bounded build authorization for the helper slice; (b) later genuine risk/time choices once measurements exist. **Removed:** decision-186 artifact supply; `check_set_version` bump ratification; field naming; old-decision retrieval.

**Net owner-facing questions: none for completing this preparation.** New build scopes require separate authorization.

## 7. Authorization-state summary

Label for the helper slice and every new fixture: **`READY_FOR_SEPARATE_BOUNDED_AUTHORIZATION`**. This window authorizes no product implementation. Standing refusals survive: every check REFUSED; `ready=False`; four numbers unset; nothing live-ready from backtest evidence (decisions 129/130). Source implementation of the refusal catalogue is verified; no global accepted or production claim.

**Remaining facts:** 7 REFUSED / 12 closed / 4 open; 17 CSVs / 4,105,966 rows / 0 internal missing slots as historical observation; producer lacks 5m and uses `int` gaps; `compute_deployment_identity_hash` has **12** named members; P012 NONACCEPTED; P020 freeze verified non-accepting (`FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`) and full integration **`WAIT_P020_ACCEPTANCE`**; helpers unbuilt; bundle 5m/unknown-timeframe repair needs own code authority (not P012/engine); full eligibility still waits.
**One recommendation:** authorize isolated P021 engineering helpers (`strategy_type_policy_set.py`, `strategy_type_classifier.py`, `data_gap_ratio.py`, and S1 tests `test_strategy_type_policy_set.py` / `test_strategy_type_classifier.py` / `test_data_gap_ratio.py`) with synthetic fixtures only, leaving `p021_readiness_rules.py` and the four limits unchanged; treat any bundle 5m/unknown-timeframe measurement repair as a separate explicit code-authority slice that does not admit eligibility.
