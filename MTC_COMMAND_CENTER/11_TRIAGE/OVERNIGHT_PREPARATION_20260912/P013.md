# P013 preparation report — TrialRecord catalogue writer (bounded, nonbinding)

Lane: P013 included-subscription preparation worker.
Inputs: `inputs/P013_DESIGN_DRAFT_V1.md` (effective v2.3); `inputs/source/` is a Git archive **materialization** of `42f99571e6abb5222744d9345e1c8a9e19c23c15` (**120 raw blob matches + 418 CRLF-only**; source content equivalent, **not all raw blobs identical**; master work-package plan / architecture brief and registry are present — no missing-input blocker where supplied); separate `qa/source_lf` contains **538 exact raw Git blobs**. Do not overwrite historical original inputs or logs. `inputs/P013_BRANCH/` (exact git archive of commit `3e704c2d` from `feature/wp-p0-13-catalogue-s13b`: `03_QUANTLENS/tools/trial_catalog.py` + `tools/tests/test_trial_catalog.py`, with the `contracts/` tree copied underneath as the master `42f9957` dependency), `inputs/P020_IMPLEMENTATION_HANDOFF.md`, `inputs/P021_ENGINEERING_DESIGN_V2.md`, `RUN_STATE.md`.
All source citations are archive-relative `path:symbol/line`, verified this session by reading the archives.
Branch evidence status: the branch archive contains a real PyArrow reader (`trial_catalog.py:139` `read_catalog_view`, `:197` `search_catalog`, `:209` `search_rejection_reason`, `:221` `search_parameter`, plus `TRIAL_RECORD_SCHEMA` :41 and `CATALOG_VIEW_SCHEMA` :94). The lead independently ran the branch's existing pytest suite with Python 3.14.2 (pytest/pyarrow/pydantic available there; main Python 3.12.12 lacks those packages and none were installed): **3 passed, exit 0** (`qa/P013_existing_reader.log`; rerun against the `qa/source_lf` / branch-reader tree). This is **component evidence, not package acceptance** — **branch ownership and acceptance were NOT acquired; nothing here claims the branch is merged or production-accepted.**
No product implementation is authorized this window. Pure code is still implementation. No code, test, script, shell, Git, or implementation was run or produced by this lane beyond reading. `PROPOSED_NOT_EXECUTED` marks every proposed fixture; the three branch reader tests were executed separately by the lead as component evidence only.
Constraints held: P012 production NONACCEPTED — no P012 artifact cited as authority; P020 has a separate owner-approved 2.1.0 successor implementation owned elsewhere — not recreated here; E/X/V/M proof independent.

## 1. Current effective contract versus history

Current controlling design version is v2.3 (`inputs/P013_DESIGN_DRAFT_V1.md:1`).
Header stack retains v2.1 and v2.2 titles as explicitly superseded (:3-7).
Later change logs fold onto earlier text in place; retained superseded history is not controlling.
Effective rules, in order of authority:

1. Selection (decision 128, v2.2, supersedes decision 87): all safety/quality checks first, then overall-robustness ranking, best **20** separately per owned strategy TYPE, every cut-off tie retained (`:1484-1506`).
2. Strategy TYPE list `day`/`swing`/`position` is **illustrative, not closed** (addendum 32 "for example", v2.3; `:11-20,35,455`).
3. v1.8 corrections to v1.7 closures are controlling:
   - Commit preimage = six declared members **plus inherited `contract_version`** (seven serialized); omission of the inherited member is filled-not-refused, ACCEPTED not DETECTED (`:807-837`).
   - Type-vocabulary collapse rule: `dictionary VARCHAR`, `canonical-JSON VARCHAR`, `canonical decimal VARCHAR`, `VARCHAR(64)` all collapse to physical `VARCHAR` (`:1029-1046`).
   - `document_version` and `publication_version` are design-coined, type `NonEmptyStr`, literal `"1"`, named sole producers, change rule requires new type name and literal (`:987-1005,1077-1093`).
   - SQL-whitespace row of the publication matrix is NOT DETECTED: `DuckDBViewPublisher` produces both SQL bytes and their digest; no reader recomputes (`:1095-1128`).
4. v1.9/v2.0 widen the B-15 reach: while B-15 is open no part path may be derived; F-1, F-4, F-5, F-7, F-10, F-11, F-12, F-13 and R-1..R-8 are all blocked; only F-2/F-3/F-8 refuse before any path exists (`:1247-1268`).
5. Reader refusal taxonomy: closed eight-reason system taxonomy (`:1186-1192`); `PATH_RECEIPT_CELL_MISMATCH` not executable while B-15 or B-17 open (`:1215-1220`).

Current **row contract** is the repository model; WP-P0-04 owns it, P013 consumes and may not redefine (`:378-380`).
Verified archive state:

- `MTC_COMMAND_CENTER/contracts/mtc_contracts/trials.py:14-74` `TrialRecord` — 48 top-level fields exactly as design §3.2: identities :17-24, search/family :27-33, fold/lockbox/benchmark/statistics :35-55, lineage/classification :57-60, flags :62-67, nested `environment_lineage` :68; `modules_enabled_count` equality validator :70-74.
- `mtc_contracts/identity.py:31-41` `canonical_json` — json.dumps, sort_keys, separators `(",",":")`, ensure_ascii=False, allow_nan=False; Decimal via `format(value,"f")` :23-24. Design's "recipe — cited, not invented" (`:784-790`) matches the archive byte-for-byte.
- `identity.py:43-44` `_hash_named_parts` — SHA256(canonical_json(parts).encode("utf-8")).
- `identity.py:76-95` `compute_evaluation_run_hash` — exactly six hashed members (`package_hash, dataset_manifest_sha, cost_model_json, simulator_class, simulator_version, evaluation_config_json`); `environment_lineage` accepted as parameter but **excluded** from the digest (:90-95 call omits it).
- `mtc_contracts/base.py:12` `CONTRACT_VERSION="0.1.0"`; :15-16 `Sha256`/`NonEmptyStr`; :59-89 `ContractModel` with `ConfigDict(extra="forbid", frozen=True, str_strip_whitespace=True, validate_default=True)` :62-67, inherited `contract_version` default :69, validator :71-78, deep-freeze validator :80-89. This is the evidence base for the v1.8 seven-serialized-member consequence (`:807-827`).
- `mtc_contracts/lineage.py:18-26` `EnvironmentLineage` — 6 declared fields; serialized as 7 with inherited `contract_version`.
- `mtc_contracts/package.py:18-24,27-45` `InstrumentMetadata.symbol` present; `StrategyPackage` carries no strategy-name or timeframe field — B-14/B-15 premise confirmed.
- Design §3.4 view contract: 48 top-level fields + 5 view-only columns (`lineage/strategy/symbol/timeframe/catalog_commit_hash`), frozen schema document, `environment_lineage` never flattened (`:590-631,1007-1013`).
- DuckDB required query: rejection lookup via `list_contains` on native Parquet list; `parameters` queried through JSON extraction from its canonical-JSON VARCHAR column (`:937-946`).
- Artifact addressing: per trial `artifacts/<package_hash>/<trial_id>/`; `ArtifactManifest` currently lacks `trial_id`/`param_hash` (B-09/D-04); two selected trials can share `package_hash`, so package-only address collides (`:743-765,1866-1868`).

Branch (`inputs/P013_BRANCH/.../03_QUANTLENS/tools/trial_catalog.py`) — read-side exists, writer does not:

- `TRIAL_RECORD_SCHEMA` :41-92 — real PyArrow schema, 48 columns; `CATALOG_VIEW_SCHEMA` :94-103 = record schema + 5 view-only columns (`lineage/strategy/symbol/timeframe/catalog_commit_hash`) — matches design §3.4.
- `ENVIRONMENT_LINEAGE_TYPE` :28-37 — nested 7-field struct, never flattened.
- `read_catalog_view(catalog_root, covered_commit_hashes)` :139 — globs `lineage=*/run_id=*/strategy=*/symbol=*/timeframe=*/part-*.parquet`, validates part name against `_PART_NAME` regex, checks schema equality, path/row `run_id` and path/row `lineage` consistency, appends the five query-only columns. **`covered_commit_hashes` is a caller-supplied input**: the reader verifies physical/path consistency of parts the caller already claims are covered — it does NOT independently verify commit completeness or commit identity.
- `search_catalog` :197 (exact-match filters, unknown filter refused), `search_rejection_reason` :209 (`list_contains` shape, `_REJECTION_RESULT_FIELDS`), `search_parameter` :221 (parses canonical-JSON `parameters` VARCHAR; refuses non-JSON/non-object; explicitly does not define the parameter identity hash).
- Module docstring :1-6: "deliberately has no writer" — row assembly blocked on `param_hash`/`preregistered_space_hash` owner recipes. Existing tests: `test_schema_matches_the_accepted_contract` :114, `test_made_up_parquet_rows_are_searchable` :122, `test_modified_copy_with_wrong_lineage_is_refused` :157 — all 3 passed under the lead's run.

Build/acceptance truth: design exists; build and acceptance do not (`:1984-1990`). Branch read-side exists and runs; branch acceptance does not.
Open items **14**: blockers B-01, B-03, B-04, B-05, B-06, B-07, B-08, B-09, B-12, B-15, B-17 and discrepancies D-02, D-03, D-04 (`:1851-1854,1984-1990`).

## 2. Material implemented / missing claims, grounded in source

Implemented (present in archive `inputs/source/`):

1. `canonical_json` + `_hash_named_parts` identity encoding — `identity.py:31-44`.
2. Six-member evaluation hash, environment excluded — `identity.py:76-95`.
3. Shipped identity functions: `make_candidate_id` :47, `compute_package_hash` :54, `compute_deployment_identity_hash` :98, `make_trial_id` :132, `make_run_id` :138, `compute_family_id` :144. **No** `compute_param_hash`, **no** `compute_preregistered_space_hash` (grep over `mtc_contracts/`: only the field declaration `trials.py:28`).
4. `TrialRecord` row model, 48 fields, required-vs-nullable per design §3.2 — `trials.py:14-74`.
5. `ArtifactManifest` 10 fields, no `trial_id`, no `param_hash` — `trials.py:77-87` (B-09/D-04 premise confirmed).
6. `ContractModel` strict base with inherited `contract_version` — `base.py:59-89`.
7. `EnvironmentLineage` — `lineage.py:18-26`; `UnsimulatedControl` :29-32; `EvidenceIdentity` :35-40.
8. `StrategyPackage`/`InstrumentMetadata` — `package.py:18-45`; exported via `contracts/__init__.py` along with `TrialRecord`, `ArtifactManifest`, `EnvironmentLineage`.
9. `unsimulated_controls.py` at repository root (not `03_QUANTLENS/tools/`) — computed control-manifest module (P020 adjacent, re-derivation not stamp), available for the `ControlManifestHasher` input side.
10. Contracts test trio exists: `test_trial_record_carries_all_identity_search_gate_and_environment_groups`, `test_blocked_check_is_distinct_and_names_what_is_missing`, `test_eligibility_check_result_requires_its_own_environment_lineage` (`contracts/tests/test_trials_and_eligibility.py:27,58,89`).

Missing (absent from archive — scoped to supplied inventory):

11. **No catalogue writer pieces exist; the read side does.** Grep of every `.py`/`.md` under `inputs/source/` for `TrialCatalog`, `CatalogCommit`, `ParquetCommitter`, `DuckDBView`, `CompletedRunInput`, `RunCellDimensions`, `TrialRecordAssembler`, `ArtifactCommitVerifier`, `LineageClassifier`, `SelectedArtifact` returned **0 hits**. Design §2.3's claim of no existing Parquet/DuckDB/SQLite TrialRecord writer (`:493`) is confirmed for the writer only — **the branch archive supplies a real reader/schema module** (`tools/trial_catalog.py`, §1 above). P013's remaining work is the writer; the reader/schema is an existing asset to **reuse at its exact proposed path, not recreate**.
12. No staged-selection policy, no strategy-TYPE or policy-version member anywhere (decision-128 binding target; design adds no TrialRecord field, `:574-576`).
13. No `P020CanonicalPathReceiptVerifier`, no canonical-receipt issuer/shape (B-01 half, `:1441-1445`).
14. No `CatalogCommitPreimageV1`, `PublishedViewSchemaDocumentV1`, `ViewPublicationPreimageV1`, `RunCellDimensionsReceipt` types — design-coined (`:1799-1827`; v1.9 measured 0-hit grep over `MTC_COMMAND_CENTER/`).
15. `03_QUANTLENS/tools/mega_walk_forward.py` (1856 lines) writes JSON/MD/checkpoint only (`:1536-1850` `_atomic_write_text`, `json.dumps`); no Parquet/DuckDB emission. It is the designated sole direct caller after migration (`:486`), not a reusable writer.
16. No completion receipt for the run envelope members `cost_model_json` / `evaluation_config_json` as committed bytes anywhere — they gain their committed home only through the P013-owned preimage design (`:843,846`).

## 3. Cross-package interfaces — producer / consumer / identity, and specific conflicts

1. **WP-P0-04 (schema owner)** — producer of `TrialRecord`, `ArtifactManifest`, all identity functions; consumer: P013 writer. Identity: `contract_version 0.1.0` (`base.py:12`). Conflicts: B-04 (`trials.py:24` requires `param_hash`; no function), B-12 (`trials.py:28` requires `preregistered_space_hash`; no function), B-09 (design requires per-trial manifest/address binding `:1512-1514`, model lacks it `trials.py:77-87`), B-03/D-02/D-03 (partition dimensions and parameter lowering are not model fields; canonical-JSON physical lowering is the design's choice pending schema-owner ratification, `:1447-1449,1856-1868`).
2. **P020** — producer of accepted evaluation/battery artifacts and the canonical-path receipt; consumer: P013 full-kernel branch. **Separate approved 2.1.0 successor implementation owned elsewhere; do not recreate its work.** From `inputs/P020_IMPLEMENTATION_HANDOFF.md:41-42`: `snapshot_id` = SHA256 of UTF-8 canonical_json(AccountSnapshot **without** snapshot_id); `dataset_manifest_sha` = raw original manifest-file digest; `ArtifactManifest.dataset_hash` = digest of the **selected** dataset manifest — two distinct digests. P013's commit envelope carries `dataset_manifest_sha` (member 2 of the evaluation formula, `:842`), never the selected-dataset hash. **P020 interface freeze independently verified** (`qa/P020_FREEZE_VERIFICATION.json`): receipt SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5`; status `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`; `acceptance: false`. Frozen surface preserves the original six-member evaluation hash and the emitter's row `strategy`/`symbol`/`timeframe` coordinates in `mega_walk_forward.py`; **no new exported P020 policy carrier**. Conflict: design is PROVISIONAL-ON-P020 with B-01 gate (`:1441-1445`) while the successor already runs elsewhere — P013 must consume **accepted** receipts and must not mint battery/threshold/evaluator code. Full writer/publication integration remains **`WAIT_P020_ACCEPTANCE`**. **Six-member evaluation hash unchanged**; the freeze adds no seventh member, consistent with `identity.py:90-95`. **P022 package-mapping / access observations are a downstream contract of P020, not extra P020 work** — P013 records them as consumer obligations only. The P013 catalogue freeze/enumeration store remains unbuilt and is distinct from this P020 interface freeze.
3. **WP-P0-08 (migration owner)** — producer of the migrated `mega_walk_forward.py` sole direct caller and of the strategy × symbol × timeframe cell persistence unit (`:486,1717`); consumer: P013 `CompletedRunInput`. Conflict **B-15**: declared field list (`:396`) carries no strategy/symbol/timeframe member; `RunCellDimensionsFactory` has no declared input; no Hive `strategy=/symbol=/timeframe=` path may be derived (`:1706-1732`). **Correction per the frozen P020 interface: the mega emitter already emits row `strategy`/`symbol`/`timeframe`** — the proposed `CompletedRunInput` is a wrapper that binds those existing emitted coordinates to independent source evidence (exact contract + producer adapter named); if the adapter/evidence is unavailable, the writer refuses — it does not invent coordinates.
4. **P021** — producer of per-strategy-TYPE versioned minimum/forward-evidence rules; consumer: P013 selection-policy binding (NEW-BY-ADDENDUM-32 item, `:443-453`). **Engineering design is `inputs/P021_ENGINEERING_DESIGN_V2.md`, with newer values in the current readiness source — the old draft's "all taxonomy unknown" state must not be carried forward.** Sibling values remain OWNER-ANSWERED-SHAPE, VALUE PENDING; must not be defaulted. **Shared taxonomy is conditional compatibility, not a hard-coded exhaustive enum: the owner may add future categories via a new version rather than silently extending the current list.**
4a. **Version-mechanism separation**: P020's allocation/guardian versions and P021's readiness policy may share a common versioning mechanism, but a shared mechanism does **not** force the same version string and does **not** justify a new P020 schema. Each package keeps its own version values; P013 asserts no coupling between them.
5. **P022** — sibling of B-12 via X-1: shared canonical `preregistered_space_hash` engineering contract (`:78-82`); P013 must not close it unilaterally.
6. **P012** — its §5.1 exact-file recipe was considered and **not** picked (`:958-980`); P012 production NONACCEPTED, so nothing P012 is authority here.
7. **P026 (OPSA)** — watchdog/backup only; no `TrialRecord` emission; no overlap with the catalogue tree.
8. **Internal writer conflict (B-17)**: expected cell coordinates exist only in the write-time `RunCellDimensionsFactory` receipt; `AcceptanceEvidenceReader` may use committed bytes only; item 2's envelope marks the coordinates absent by design and item 3's `path_slot` is the observed operand, so R-8 cannot be performed from committed bytes (`:1734-1749`).
9. **Identity rule held throughout**: each of the 48 fields has exactly one sole producer (`:506-555`); envelope produced solely by `EvaluationIdentityFactory` receiving the `LineageClassifier`-locked class (`:844`); comparers never produce operands (conservation `:417`, schema-fingerprint `:948`, run-id `:757`, cell `:1198-1203`).
10. **WP-P0-08 inventory mapping** confirmed against the draft's own table (`:484-491`): post-migration `mega_walk_forward.py` = sole direct caller; CPCV/PBO/robustness/BH-DSR/gate-scoring = identity-keyed value producers feeding the completed-run receipt, never independent catalog writers; `reference_producer.py` and prototype debug writers feed Tier-2 selected artifacts only; `run_quantlens_overnight_research.py` routes through `LegacySignalScreenRunAdapter`; run plans/progress emitters never emit `TrialRecord` rows; no legacy writer is deleted by this design (`:1760`).
11. **Two-adapter seam is the boundary, not an optimizer hook** (`:473-480`): `MigratedCanonicalRunAdapter` can obtain `FullEvidenceSinkCapability` only from a successful P020 receipt; `LegacySignalScreenRunAdapter` has no method that can request it and maps unconditionally to `SIGNAL_SCREEN_ONLY`; grid/TPE/random vary upstream while the writer interface stays fixed (`:518`).
12. **Two proof producers at D026** (`:1175-1180`): production row producer (`LegacySignalScreenRunAdapter → TrialCatalogWriter`) and a separate admissibility-verdict producer (`AcceptanceEvidenceReader → P020CanonicalPathReceiptVerifier`); the verdict producer is forbidden to import the writer or the classifier.
13. **Lineage structural rule**: the model field is only `NonEmptyStr` (`trials.py:57`), so the capability seal, locked class binding into the evaluation preimage, and independent acceptance receipt are all required — the field string alone is convention, not structure (`:1169-1173`).

## 4. Independently derived proposed test inputs — PROPOSED_NOT_EXECUTED

Concrete inputs and exact expected outputs, derived from the archive contracts and the design's closed byte formulas, not copied from the draft's own arm text:

1. **T1 canonical_json shape**: input dict `{"b": Decimal("1.500"), "a": "ü"}` → expect exact string `{"a":"ü","b":"1.500"}` (non-ASCII retained, decimal `1.500` not `1.5`, keys sorted). Mutation: `Decimal("NaN")` → exception (allow_nan=False). Fails if implementation uses json.dumps defaults (escapes `ü`, renders `1.5`).
2. **T2 six-member evaluation hash**: `compute_evaluation_run_hash(package_hash="a"*64, dataset_manifest_sha="b"*64, cost_model_json='{"fee_bps":"10"}', simulator_class="SIGNAL_SCREEN_ONLY", simulator_version="1", evaluation_config_json='{"folds":3}')` → SHA256 of `canonical_json` over exactly those six named parts. Mutations: pass any `environment_lineage` → hash **unchanged** (excluded member, `identity.py:90-95`); change `simulator_class` → hash changes.
3. **T3 seventh serialized member**: `CatalogCommitPreimageV1` (proposed `ContractModel` subclass, six declared members) → `canonical_json` output contains `"contract_version":"0.1.0"` among keys (7 total, `base.py:69`, `identity.py:16-17` model_dump). Mutation: same six members on a plain non-ContractModel model → **different digest**. Pins the v1.8 member contract; distinguishes the two honest implementations.
4. **T4 member-omission refusal (R-7 shape)**: stored receipt JSON omitting exactly `package_hash`, digest recomputed over shortened preimage → typed parse refuses with `COMMIT_PREIMAGE_MEMBER_MISSING` **before** rehash; rehash-only reader accepts the same bytes and is RED. Omission of only `contract_version` → parse **fills** `"0.1.0"` and accepts (documented ACCEPTED limit, `:829-837`).
5. **T5 conservation multiset**: terminal receipts ordered `["t1","t2","t3"]`; staged parts carry `["t1","t2"]` → `TrialConservationComparer` refuses. Degenerate implementation reading both operands from staged parts accepts → RED.
6. **T6 boundary reserved key**: `CompletedRunInput` invoked with `simulator_class="FULL_KERNEL_SIMULATION"` → refuse at parse (explicit reserved-key refusal; `extra="forbid"`), before any `TrialRecord` construction (F-2 shape, `:1280`).
7. **T7 schema fingerprint matrix**: document = 48 top-level entries in §3.2 table order + 5 view-only columns in §3.4.1 order; `environment_lineage` nested with 7 `fields`. Expected fingerprint = `SHA256(canonical_json(document))`. Mutations: flatten struct → `SCHEMA_FINGERPRINT_MISMATCH` (DETECTED); reorder two columns → DETECTED; nullability flip → DETECTED; type change across vocabulary (BIGINT→DOUBLE) → DETECTED; `VARCHAR(64)`→`VARCHAR` on a Sha256 column → **accepted** (collapse rule); `dictionary VARCHAR`→`VARCHAR` → **accepted**; schema-document whitespace/indentation → **accepted** (canonical_json re-serializes).
8. **T8 rejection query**: one row with `rejection_reasons=("NO_LOCKBOX_DATA",)` → `SELECT trial_id, candidate_id, classification, rejection_reasons, simulator_class, deployment_identity_hash FROM trial_catalog WHERE list_contains(rejection_reasons,'NO_LOCKBOX_DATA')` returns exactly that row; renamed or absent reason returns zero rows.
9. **T9 parameters lowering**: `parameters={"b": 1, "a": "x", "c": Decimal("0.100")}` → stored VARCHAR must equal the canonical JSON string of that mapping (sorted keys, decimal `"0.100"`); non-canonical or non-JSON value → writer refuses (`:500`). Query round-trip: `json_extract(parameters,'$.a')` returns `"x"`.
10. **T10 module-count consistency limit**: `modules_enabled=("m1","m2")`, `modules_enabled_count=3` → model validator raises (`trials.py:70-74`). Same-producer consistency only, as design records (`:524`): both operands come from `StrategyPackageProjector`, so this is not independent completeness proof — the test pins only the model check, not a completeness claim.
11. **T11 path/row run_id sub-arm pre-check**: valid committed package with mixed row `run_id` values inside one part → `PathRowRunIdComparer` refuses with `PATH_ROW_RUN_ID_MISMATCH`, never `PATH_ROW_LINEAGE_MISMATCH` (F-12 sub-arm d, `:1290`); requires B-15 closed like all path-based arms.

Independently specified additions (refine directive; all `PROPOSED_NOT_EXECUTED`; the branch's existing 3 reader tests are the separately executed baseline):

12. **T12 same package, two trial IDs → distinct artifact directories**: two selected trials sharing one `package_hash` but different `trial_id` → staged addresses `artifacts/<package_hash>/<trial_id_a>/` and `artifacts/<package_hash>/<trial_id_b>/` differ; package-only addressing would collide (`:765`) → refused by the ratified scheme.
13. **T13 missing part on covered commit refuses**: commit receipt covers parts `[p1, p2]`; `p2` absent on disk → reader/writer-side check refuses (`COMMIT_PART_MISSING` shape) instead of reading a partial view.
14. **T14 rewritten payload with self-consistent local hash still rejected**: mutate persisted part bytes and rewrite its local hash so staged bytes and local hash agree → independent commit receipt digest (computed over the original preimage) no longer matches → refuse. Pins receipt-vs-bytes independence; a self-consistent rewrite alone must never pass.
15. **T15 path/row mismatch**: part under `run_id=X/` containing rows with `run_id=Y` → refuse (mirrors the branch's existing path/row check, exercised through the writer path once B-15 closed).
16. **T16 idempotent commit vs conflicting repeated ID**: re-invoking the writer with the identical completed run → no-op, same receipt; same `trial_id` with different payload bytes → refuse, no overwrite.
17. **T17 ties at 20 per type do not merge groups**: two strategy TYPEs each with ≥20 candidates tied at the cut-off → selection keeps 20 per TYPE (40 total), never a merged 20 across TYPEs (decision 128).
18. **T18 stop before writing on incomplete P020/P021 fields**: `CompletedRunInput` missing any P020-required field or any P021-bound selection-policy field → refuse at parse, before any staged byte or path exists (F-2/F-3/F-8 shape).

## 5. Ordered minimal implementation steps, exact proposed files, exclusive writer roles, dependencies, acceptance

Precondition: B-04, B-12, B-15, B-17, B-09, B-07, B-01 owner acts (§6). Each step names one exclusive writer; no shared write surface.

1. **Ratify owner contracts** (owner acts, not code):
   - B-04/B-12 preimages into `mtc_contracts/identity.py` — writer: WP-P0-04 owner.
   - B-15 cell-coordinate member name/type — writer: P013 build owner jointly with WP-P0-08.
   - B-09 `ArtifactManifest` additions (`trial_id`, `param_hash`) and address scheme — writer: WP-P0-04 owner.
   - B-07 versioned selection-policy encoding + lifecycle/pin record bindings — writer: P013 build owner with named contract owners.
   - Dependencies: every later step. Acceptance: functions importable; blocker "required resolution" clauses satisfied; no invented formula, no approximate default.
2. **Boundary + preimage types module** — proposed file `MTC_COMMAND_CENTER/contracts/mtc_contracts/trial_catalog.py` — writer: P013 build owner.
   Contents: `CompletedRunInput` (closed; reserved-key refusal; includes B-15 cell-coordinate member once ratified), `SelectedArtifactPayloadReceipt` (`{trial_id, artifact_kind, producer_id, staged_handle, byte_length, sha256}`, `:471`), `RunEnvelopeV1` (six declared members; explicitly a `ContractModel` subclass so `contract_version` serializes, `:807-827`), `CatalogCommitPreimageV1` (four items, `:782-902`), `PublishedViewSchemaDocumentV1`, `ViewPublicationPreimageV1` (five members, `:1073-1093`), `RunCellDimensionsReceipt`.
   Dependencies: step 1. Acceptance: T3/T4/T6/T7 pass; `extra="forbid"` verified; no new `TrialRecord` column.
3. **Conservation + identity stages** — proposed `MTC_COMMAND_CENTER/01_MTC_PROJECT/00_PYTHON/mtc_v2/trial_catalog/stages_conservation_identity.py` — writer: P013 build owner.
   Contents: `CompletedRunTrialUniverseProjector`, `CommittedTrialRowInventory`, `TrialConservationComparer`, `LineageClassifier`, `IdentityValidator`, `EvaluationIdentityFactory` wrapper receiving the locked class.
   Dependencies: step 2. Acceptance: T5; boundary refusals F-2/F-3/F-8; identity refusal refuses rather than rewrites the row (`:419`).
4. **Selection + artifact pipeline** — proposed `.../trial_catalog/stages_selection_artifacts.py` — writer: P013 build owner.
   Contents: `ArtifactSelector` (decision-128 policy consumer; strategy TYPE via owned binding; type list illustrative), `TopKSelector`, `ParetoSelector`, `RobustnessGate`, `LifecycleDecisionProjector`, `UserPinProjector` (row facts only), `SelectedArtifactManifestBuilder`, `SelectedArtifactCommitter` (byte-only; refuses second write to committed address), `ArtifactCommitVerifier` (compares committed identities against independent stage-3 row identity; never reuses upstream receipt as both operands, `:458`).
   Dependencies: step 2 receipts + B-09 ratified manifest. Acceptance: F-9 baseline `true,true,false` and all six sub-arms refused before assembly (`:1287`).
5. **Lowering + commit + publication** — proposed `.../trial_catalog/parquet_commit_view.py` — writer: P013 build owner.
   Contents: `ParquetCommitter` (hash-free staging outside glob; `parameters` canonical-JSON VARCHAR; `fee_bps_used` canonical decimal VARCHAR; atomic move to hash-bearing paths; idempotent commit; same trial_id with different bytes refuses), `CatalogCommitIdentity`, `TrialCatalogViewSchemaProjector`, `CommittedDuckDBSchemaInspector`, `DuckDBSchemaFingerprintComparer`, `DuckDBViewPublisher` (sole SQL-byte producer; strict-superset replacement rule only; wrong glob refuses).
   **Reader/schema reuse (not recreation):** the Parquet shapes and reader functions already exist on the branch (`tools/trial_catalog.py`: `TRIAL_RECORD_SCHEMA` :41, `CATALOG_VIEW_SCHEMA` :94, `read_catalog_view` :139, `search_catalog` :197, `search_rejection_reason` :209, `search_parameter` :221). The writer module imports those exact objects from the branch module at its existing path; no schema or reader is redefined. The branch module stays the sole reader; the writer stays the sole writer.
   Dependencies: steps 2-4. Acceptance: T1/T7; F-10 modified copy RED; re-publish identical preimage is no-op; representation-only SQL change acknowledged as digest-changing (gap recorded per §6.9).

   **Proposed minimal writer transaction (one sequence, exact immutable paths, sole writer roles):**
   1. Independent identities resolved (B-04/B-12 owner functions) for every row.
   2. Staged immutable artifacts/parts written to `artifacts/<package_hash>/<trial_id>/` and staged Parquet parts under a hash-free staging area (outside the `part-*` glob).
   3. Persisted bytes verified: recompute digests of staged bytes against the preimage members (`ArtifactCommitVerifier`); T14 semantics.
   4. Publish commit receipt: atomic move of parts to `catalog/lineage=<L>/run_id=<R>/strategy=<S>/symbol=<Y>/timeframe=<T>/part-<catalog_commit_hash>-<seq>.parquet`, then atomically write `commits/<catalog_commit_hash>.json` (preimage + `RunCellDimensionsReceipt`).
   5. Readers admit only complete covered commits: `read_catalog_view(catalog_root, covered_commit_hashes)` admits a commit hash only when its receipt exists and every covered part is present (T13); `covered_commit_hashes` remains a caller-supplied input — the reader checks physical/path consistency, not independent commit completeness.
   Crash at any point reveals no partial commit: staging is invisible to the glob until the atomic move; receipts are written after parts; no overwrite ever (T16). Sole writers: `SelectedArtifactCommitter` (artifacts), `ParquetCommitter` (parts), `DuckDBViewPublisher` (view SQL), one receipt writer — no shared write surface.
6. **Acceptance reader** — proposed `.../trial_catalog/acceptance_reader.py` — writer: P013 build owner; forbidden to import writer or classifier.
   Sequence: typed parse → rehash → path derivation → evaluation recompute from six committed envelope members → dependent `trial_id` → lineage / path-run-id / path-cell comparers → canonical receipt. Emits only the closed eight-reason taxonomy; bare `REFUSED` forbidden (`:1186-1192`).
   Dependencies: steps 2-5. Acceptance: R-1..R-7 one-check-deleted RED; R-8 only after B-17 committed operand exists.
7. **Caller integration** — migrated `mega_walk_forward.py` invokes `write_completed_run(completed_run) -> CatalogCommitReceipt` once at finalization — writer: WP-P0-08 owner.
   Dependencies: steps 3-6. Acceptance: one row per trial in the published view; T8 query works; conservation proven across the writer seam.

## 6. Specific missing facts, dependencies, owner choices — one recommended resolution each

1. **B-04 `param_hash` preimage** — owner: WP-P0-04. Recommendation: ratify canonical preimage over the canonicalized parameter mapping (canonical_json of frozen `parameters` plus explicit version tag) and add `compute_param_hash` to `identity.py`; single member set, no alternatives.
   **Concrete proposed P0-04-owned change list (no invented approved schema — proposed only, owner ratifies):**
   - `mtc_contracts/identity.py` — add `compute_param_hash(parts)` reusing `_hash_named_parts` (:43-44); no change to existing six-member function.
   - `mtc_contracts/trials.py` — add `trial_id` + `param_hash` fields to `ArtifactManifest` (:77-87) only because design §B-09 requires the per-trial binding (`:1512-1514`); no other model field added.
   - No schema document or enum is minted by P013; every added field keeps exactly one sole producer (`:506-555`).
2. **B-12 `preregistered_space_hash` preimage** — owner: WP-P0-04 / search-family owner. Recommendation: ratify a closed registered-space member list (module-combination set, adaptive budget, repeated-iteration membership) before any hashing; explicitly do **not** hash `evaluation_config_json` (collision with evaluation hash, `:1589-1591`). Coordinate with P022 OQ-4 through one shared contract.
3. **B-15 cell-coordinate channel** — owner: P013 build owner + WP-P0-08. Recommendation: proposed `CompletedRunInput` **wraps the existing emitter `strategy`/`symbol`/`timeframe` fields** (Lead-confirmed; no new P020 export; prefix/path naming is an engineering adapter decision — do not ask the owner to name fields). `RunCellDimensionsFactory` reads no other source; corroborate `symbol` against `StrategyPackage.instrument_metadata.symbol` (`package.py:19`).
4. **B-17 committed expected coordinate** — owner: P013 build owner + cell-coordinate contract owner. Recommendation: store the `RunCellDimensionsReceipt` inside the run-level commit receipt document at `commits/<catalog_commit_hash>.json` beside the preimage, and re-point R-8 at those bytes; not a second projection of the Hive path.
5. **B-07 engineering half** — owner: P013 build owner + lifecycle/pin contract owners. Recommendation: encode decision 128 as a versioned policy document consumed by `ArtifactSelector`; bind "market" to an existing owned dimension rather than minting one; treat Pareto/robust/promoted/pinned as independent row facts only. Do not re-ask the owner — decision 128 is final.
6. **B-09 manifest binding** — owner: WP-P0-04. Recommendation: extend `ArtifactManifest` (or owned sibling) with `trial_id` + `param_hash` and ratify `artifacts/<package_hash>/<trial_id>/`; reject the package-only address because two selected trials can share `package_hash` (`:765`). Change surface exactly as the B-04 list above: two added fields in `trials.py:77-87`, nothing else; T12 pins the distinct-directory behavior.
7. **B-01 / P020 sequencing** — owner: P013 build owner (awaits). P020 freeze receipt is independently verified (`qa/P020_FREEZE_VERIFICATION.json`, SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5`, `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`). Recommendation: consume that frozen surface; full writer still **`WAIT_P020_ACCEPTANCE`**; re-audit entry condition per §9 (`:1886-1948`) before any build acceptance; do not recreate evaluator/battery code.
8. **B-05/B-06/B-08** — vocabulary enum, rejection taxonomy, legacy deployment preimage — owner: WP-P0-04/P0-20 jointly. Recommendation: finite `simulator_class` enum, finite reason taxonomy with per-reason producer and PASS/FAIL/BLOCKED mapping, honest non-accepting legacy preimage; no sentinel pretending the shared allocator exists.
9. **Publication-hash independence gap (G102-F06)** — recommendation: add a reader-side recompute of `view_publication_hash` from the stored `ViewPublicationPreimageV1` inside the publication receipt, using the two-operand comparer pattern already used for both schema fingerprints; otherwise the whitespace class remains undetected (`:1117-1128`).

## 7. Authorization-state and blocked slices

**READY_FOR_SEPARATE_BOUNDED_AUTHORIZATION** (not "build-ready now", not "no gate"; this window authorizes no product implementation; pure type modules remain implementation):

- Pure type definitions of step 2 and their serialization tests: T1, T3, T4, T6, T7 operate on proposed types and the shipped `canonical_json` — they need no row, no path, no view — **and still require a new explicit bounded authorization naming those files**.
- Conservation comparer trio skeleton with T5 — same authorization limit.
- Acceptance reader's typed-parse-before-rehash logic (T4), testable against stored-receipt fixtures — same authorization limit.
These are specified against the archive contracts and the design's closed byte formulas; they produce no repository artifact in this window. Full writer start is **not** in this slice.

**Full P013 writer / publication / caller integration requires accepted P020 plus exact contracts** (`WAIT_P020_ACCEPTANCE`; B-04/B-12/B-15/B-17/B-09/B-07/B-05/B-06/B-08). Prefix/path terminology is an engineering adapter decision; do not ask the owner to name fields. Proposed `CompletedRunInput` wraps existing mega `strategy`/`symbol`/`timeframe`; no new P020 policy carrier; six-member evaluation hash unchanged. P020 interface freeze is independently verified (`FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`); it is not P020 acceptance.

**Blocked slices and their gates:**

1. Any `TrialRecord` assembly or Parquet byte — B-04 + B-12 (no emittable `param_hash` / `preregistered_space_hash`) (`:421,1236-1237`).
2. Any `strategy=/symbol=/timeframe=` path derivation, hence any staged part, any commit receipt, any published view — B-15 (`:1247-1268`). Reviewer question kept open by the design (`:1926-1928`): whether F-9's selected-artifact tree lies inside B-15's reach (its bytes carry no routing segment) and, if the wiring reading holds, whether B-15 must close ahead of B-04/B-12 in the closing order.
3. Full-kernel branch, canonical receipt, real-run gates — B-01 / P020 acceptance; successor implementation owned elsewhere.
4. Selection execution — B-07 engineering policy + lifecycle/pin records.
5. Selected-artifact commit/verification — B-09.
6. Legacy screening rows — B-08; stable reason queries — B-06; `simulator_class` vocabulary enum — B-05.
7. Reader cell-coordinate check (R-8) — B-15 then B-17, sequential and independent (`:1938-1948`).
8. Gate rows closable today — none (standing B-04/B-12/B-01 reason plus B-15 reach, `:1359-1383`); the "One row per trial" gate uses the v1.6 controlling conservation trio, not the withdrawn single `TerminalTrialConserver` (`:1411-1412`).

**Marginal notes on ordering:**

- The view-preimage inheritance/member-count ambiguity from GM66-F01 remains open (B-16 deliberately not opened because the supplied patch hunk stopped, `:86-95,1838-1844`); step 2's choices are downstream of whatever base-class ruling the next re-audit makes — pinning T3 before that ratification is provisional on it.
- Retention/replay direction and retirement of legacy JSON/CSV authorities are separate authorities (`:1756-1761`); this report proposes no deletion.

**Decisive findings:**

1. Zero existing writer pieces in the scoped archive — the writer module is new, confirming design §2.3 (`:493`); the branch archive separately supplies a real reader/schema module (`tools/trial_catalog.py`) that P013 reuses, not recreates.
2. The shipped identity code confirms both "cited, not invented" recipes and the v1.8 seven-serialized-member correction — commit-hash interoperability therefore has exactly one honest baseline.
3. The writer cannot emit a single row or path today: B-04/B-12 (identities), B-15 (path channel), B-01 (P020 acceptance) are the three gates in dependency order; every fixture except the writer-boundary probes F-2/F-3/F-8 is blocked behind them.
4. Branch read-side independently verified (3 existing tests passed, `qa/P013_existing_reader.log`, Python 3.14.2); branch ownership/acceptance not acquired — no merged or production-accepted claim.

**Readiness statement:** this report is **READY_FOR_SEPARATE_BOUNDED_AUTHORIZATION** only. The full writer depends on P020 acceptance (B-01, `WAIT_P020_ACCEPTANCE`) and the unresolved protected-contract approvals (B-04/B-12/B-15/B-17/B-09/B-07/B-05/B-06/B-08). Bound: 15 minutes; no audit loop. No duplicate audit demand beyond package review policy. Source implementation of the shipped identity/row contracts is verified; no global accepted or production claim.

**Scope limit of this report:** absence claims are scoped to the supplied archive inventory; the design draft cites named external lane-prompt and owner files not present in this root, and those are reported as design citations, not independently re-verified here. Nothing in this report claims implementation, execution, or acceptance; every probe is a build requirement. New fixtures remain `PROPOSED_NOT_EXECUTED`. Branch reader 3 tests (Python 3.14.2) remain component evidence.

## 8. Remaining facts / one recommendation

**Remaining facts:** no catalogue writer in archive; branch reader exists (3 tests, Python 3.14.2, component evidence only, unmerged); B-04/B-12 preimages absent; B-15/B-17 cell/receipt channel unfrozen; B-09 manifest lacks `trial_id`/`param_hash`; `unsimulated_controls.py` is at repo root; P020 interface freeze independently verified (`qa/P020_FREEZE_VERIFICATION.json`, SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5`, `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`) but full integration **`WAIT_P020_ACCEPTANCE`**; no new P020 policy carrier; six-member evaluation hash unchanged; `CompletedRunInput` must wrap existing `strategy`/`symbol`/`timeframe`; P013 catalogue freeze/enumeration store unbuilt; P012 NONACCEPTED; all new fixtures `PROPOSED_NOT_EXECUTED`.
**One recommendation:** do not start the full writer this window; wait for accepted P020 evidence plus P0-04 exact `param_hash`/`preregistered_space_hash` preimages and per-trial manifest fields. Any type-only module still needs a separate bounded authorization naming exact files.
