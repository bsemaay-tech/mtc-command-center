# P022 — Family lineage and observation-leakage control — preparation report

Worker: bounded included subscription preparation lane P022. Scope: nonbinding
implementation-decision preparation. DESIGN ONLY. Nothing here claims implemented; every
claim cites the supplied archive. Absence statements are scoped to `inputs/source/`, a Git archive **materialization** of
`42f99571e6abb5222744d9345e1c8a9e19c23c15`: **120 raw blob matches + 418 CRLF-only**.
Source content is equivalent; **not all raw blobs are identical**. Separate `qa/source_lf`
contains **538 exact raw Git blobs**. Do not overwrite historical original inputs or logs.
Existing component checks remain component evidence, not package acceptance.
All proposed fixtures are PROPOSED_NOT_EXECUTED.

**Startability:** this lane authorizes preparation only. No product implementation is
authorized this window. Pure code (the resolver, the interval math, the proposed diff) is
still implementation and is **not authorized** under this task. The correct label for
R1–R3 is **READY_FOR_SEPARATE_BOUNDED_AUTHORIZATION**. Full P022 ledger/leakage integration
keeps its P013 freeze/enumeration dependency (`WAIT_P020_ACCEPTANCE` transits through P013).
Early helper scope (R2 resolver / R3 interval math only) needs an **explicit isolated
build-order exception** to that P013 integration dependency, naming the exact files; without
that exception the helpers stay queued behind P013. Each exact contract still needs a
separate owner-approved scope. No "build-ready now" / "no gate" claim is made.

Design input: `inputs/P022_DESIGN_DRAFT_V1.md` (v1.11 title, v1.12 fold; superseded history
retained in-draft is not controlling; current effective text = live prose + latest tags).
Source input: `inputs/source/` (contracts package, QUANTLENS tools, protocols, OPSA,
**now including** `05_REGISTRY/STRATEGY_RESEARCH_REGISTRY.json` + `TAG_DICTIONARY.json`,
the master architecture brief, the master work-package plan and
`11_TRIAGE/DESIGN_DEFECT_PATTERNS_2026-08-10.md`). Verified directly this round.

---

## 1. Current effective contract versus history

Effective, source-verified (supplied archive):

- `compute_family_id(source_provenance, producer, parameter_neighbourhood)` returns
  `FAM-<sha256[:16]>`; hashes the three named parts exactly, canonical JSON, key-sorted
  (`MTC_COMMAND_CENTER/contracts/mtc_contracts/identity.py:144-154` + `:31-44`).
- `TrialRecord.family_id: NonEmptyStr` — no allowed-value set, no `FAM-UNKNOWN` literal,
  no LIVE_CANDIDATE binding (`.../mtc_contracts/trials.py:22`). Same class shape on
  `StrategyPackage` (`.../mtc_contracts/package.py:27-31`: `candidate_id`, `family_id`,
  `package_hash` together).
- `package_hash: Sha256` (`trials.py:19`, `package.py:32`) — produced by
  `compute_package_hash` whose preimage is spec/kernel/params/modules/substitutes/instrument
  metadata, environment_lineage explicitly excluded (`identity.py:54-73`). The design
  draft's historical claim "no stated preimage, no named producer" (OQ-1, DS66-F02) is FALSE
  against the supplied archive; the current effective contract supersedes that history.
  **Recommendation (ratify instead of invent): reuse the exact existing
  `compute_package_hash` recipe — no new or replacement hash — plus a typed, named
  freeze receipt carried by P013** (the authoritative freeze/enumeration store). The
  existing recipe resolves the draft OQ1 preimage assertion; it does **not** by itself
  provide an authoritative freeze/enumeration store — that store is the P013 receipt
  surface, not anything P022 mints.
- `EligibilityState.LIVE_CANDIDATE` exists; `EligibilityCheckResult` BLOCKED requires
  `blocked_reason` (`.../mtc_contracts/admission.py:21-46`). Nothing in the contracts binds
  `family_id == "FAM-UNKNOWN"` to refusal — OQ-5 gap confirmed in source.
- `AdmissionDecision.evidence_references: tuple[NonEmptyStr, ...]` +
  `leakage_record_id: NonEmptyStr | None` (`admission.py:104-106`) — the existing sink a
  family/observation control would feed. Unused by any family code today.
- `Environment` enum: FORWARD_SHADOW, INTERNAL_PAPER, EXCHANGE_TESTNET, LIMITED_LIVE,
  MAINNET (`.../mtc_contracts/lineage.py:10-15`) — matches §2.1 `environment` field set.
- Contracts are frozen `contract_version 0.1.0`, `extra=forbid`, deep-frozen containers
  (`.../mtc_contracts/base.py:12-16,59-89`) — P022 cannot extend `TrialRecord` without a
  versioned contract change (WP-P0-04 surface).

Effective in the design draft (current text, not superseded history): §1.3 resolution order
with cross-producer conflict check; §2.1 row schema; §2.4 single serialising appender +
access/atomicity contract; §3.2 recompute-only untouched window; §3.3 whole-window
fail-closed completeness incl. OQ-16 conjunct; §4.1 time/linkage tests with δ owner decision;
§5 fixtures b1–b4 + 5(a) two-producer + 5(c) RED/GREEN; OQ-1..OQ-16 blockers.

**Decision 98 is binding and changes the family-key semantics:** "approved search-space
version per strategy," no bins. The adaptation required — normalize **strategy identity**
and **frozen search-space identity** together as the exact neighbourhood preimage — is
specified concretely below (§4, T14 adapter spec). Historical `family_id`/`package_hash`
values must continue to verify: the adapter keys by version pair and preserves all
previously emitted hashes under version separation. **No arbitrary bucketing anywhere.**

Master-doc gate wording now verified in supplied archive (previously NOT VERIFIABLE):

- **D-15** (brief:216): "Family-lineage and human-observation leakage must be controlled,
  with objective evidence, **before the first `LIVE_CANDIDATE` decision**." §6.6's five
  rules close package-level leakage only; the family-level gap is the O-12 residual.
- **A-7d** (brief:3075): family lineage, append-only observation ledger and computed
  untouched window exist per **§6.6 rules 6–8**, with a D026 fixture in which a deliberately
  contaminated sibling is RED without the control and GREEN with it. Required before the
  first `LIVE_CANDIDATE` decision.
- **A-18 promotion precondition** (brief, WP-V3-03 wording): no promotion decision recorded
  without a referenced, accepted family/human-observation leakage record (§6.6 rules 6–8);
  a promotion artifact whose `leakage_record_id` is absent or `UNKNOWN` is **refused by the
  Promotion Authority**, not merely flagged.
- **D026** verified in `11_TRIAGE/DESIGN_DEFECT_PATTERNS_2026-08-10.md` ("The rule." block):
  decide before writing QA what observation would have proved the claim false; record the
  exact executable command, its real red output against the prior artefact, its real green
  output against the current one. A command needing an edit/substitution/undeclared shell
  state is not closure evidence.
- **R6** row (brief:195): WP-P0-22 is the carrier; WP-V2A-10, WP-V3-03, WP-V4-01 depend on it.

Superseded history NOT controlling: v1–v1.11 change logs; "enforced structurally" (v1.1);
"known-reader" completeness (v1.1); bin-assignment half of §1.2 (owner decision 98 removed
it); unratified-δ state for now-ratified results; "is checked today" (now "designed but not
live today").

## 2. Material implemented / missing claims (numbered, source-grounded)

Implemented (present in supplied archive):

1. `compute_family_id` — `identity.py:144-154`, deterministic, exported
   (`mtc_contracts/__init__.py:32,134`), pinned by `tests/test_identity.py:103-113`.
2. `family_id` / `candidate_id` / `package_hash` recorded on both `TrialRecord`
   (`trials.py:17-22`) and `StrategyPackage` (`package.py:30-32`) — the record-once
   carrier fields exist.
3. `preregistered_space_hash: Sha256` on TrialRecord (`trials.py:28`) — the field owner
   decision 98 names as the family-key half.
4. `EligibilityState.LIVE_CANDIDATE` + BLOCKED-with-reason machinery (`admission.py`).
5. `Environment` enum matching ledger §2.1 (`lineage.py:10-15`).
6. Identity formulas exclude environment lineage from all three hashes — pinned test
   (`tests/test_identity.py:82-100`).

**Registry census — now independently verified (supplied
`05_REGISTRY/STRATEGY_RESEARCH_REGISTRY.json`, schema_version 1.0, generated 2026-06-06,
generator `build_strategy_research_registry.py`):**

7. Exactly **63 strategies**, strategy_id `STG001`..`STG063` contiguous; **all 63 carry
   `source_folder`** (`03_QUANTLENS/strategies/STGxxx_...`). Draft's "63" VERIFIED.
8. The confidence split "46/17" is real and verified — but the field is
   **`classification_confidence`** with values **`heuristic_auto` = 46** and
   **`explicit_metadata` = 17** (`build_strategy_research_registry.py:703,733`), not a
   HIGH/MEDIUM label. Draft's "46/17 confidence split" VERIFIED under the correct field name.
9. **`strategy_family` is NOT a field of the registry JSON** (0 occurrences in the file;
   absent from all 63 entries). The tool at
   `build_strategy_research_registry.py:590` only *reads* `spec.get("strategy_family")`
   from `producer_spec.json` into a scoring `signal_text` — it writes no registry strategy
   field. Consequence: the registry `strategy_family` tool field is NOT `family_id`, and
   it does not even exist as a registry output. Conflation is impossible at the data level.
10. `TAG_DICTIONARY.json` present adjacent to the registry.

Missing from supplied archive (absence scoped to `inputs/source/`):

11. No `FAM-UNKNOWN` sentinel anywhere; no refusal binding family_id to eligibility.
    OQ-5 confirmed missing.
12. No observation **ledger**, ledger writer, appender, or hash chain. There is **no
    ledger-specific `observation_id` implementation**. Do not say "no `observation_id`
    anywhere": P030 `MarketBar` already carries `observation_id`. §2 ledger is entirely
    unbuilt.
13. No `FAMILY_OBSERVED` marking, no untouched-window computation, no siblings() resolver.
    §1.3/§3/§4 unbuilt.
14. No `evidence_window_start` or `frozen_at` field on any contract
    (`grep "evidence_window|frozen_at"` → only a test name in
    `test_risk_admission_execution.py:147`). The §3.1 window inputs have no carrier.
15. No DuckDB/Parquet catalog read path, access log, `atime` config, or optimizer
    warm-start provenance field in the supplied tools. Producer-2 channels unbuilt;
    OQ-3 NOT VERIFIED confirmed.
16. `mega_walk_forward.py` exists and exports `simulate_slice` (`:648`; call sites
    `:1310-1313, :1354`) but contains no prior-package-record read instrumentation
    (`grep p020|provenance|warm_start|prior_package|package_hash` → 0 hits) — §2.3 class 3
    hook site present as a file, absent as a hook.

## 3. Cross-package interfaces (producer / consumer / identity) and conflicts

| Interface | Producer | Consumer | Identity key | Conflict / status |
|---|---|---|---|---|
| `family_id` | P022 resolver (§1.3) at freeze | `StrategyPackage` + `TrialRecord` + P013 catalog + §4 sibling logic | `FAM-<16hex>` or `FAM-UNKNOWN` | Contract carries field, no literal/binding (OQ-5). `compute_family_id` can never emit `FAM-UNKNOWN` (`identity.py:154` always `FAM-<hex>`) — resolver must be a NEW component that may refuse and emit sentinel; do not reuse `compute_family_id` as the resolver. |
| decision-98 adapter | P022 proposed (this report, T14) | `compute_family_id` caller path | (normalized strategy identity, frozen search-space identity) | Normalizes strategy + frozen search-space identity as the exact neighbourhood; preserves historical hashes under version separation; no arbitrary bucketing. P0-04 owns any contract reshaping. |
| `parameter_neighbourhood` | caller (P013 trials) | `compute_family_id` | object hashed exactly (`identity.py:149-154`) | Exact-hash behavior now conforms once the adapter feeds version identity. Contract change is WP-P0-04's (OQ-4 engineering half). Strategy id has no carrier field in the triple — named gap, resolved by adapter (T14), not contract edit. |
| `package_hash` | `compute_package_hash` (identity.py:54-73) | TrialRecord, StrategyPackage, AdmissionDecision, ledger §2.1 rows | 64-hex Sha256 | **Reuse exact existing recipe; add typed named freeze receipt from P013 as the authoritative freeze/enumeration store. No new replacement hash.** Residual = freeze-record store only (P013). |
| `candidate_id` | `make_candidate_id` (identity.py:47-51) | same sinks | `QLC-YYYYMMDD-<8hex>` | §1.3 steps 1–2 join `candidate_id → family_id`; join store does not exist (P013). |
| eligibility / admission | `EligibilityVerdictSet`, `AdmissionDecision` | WP-V2A-10 / V3-03 / V4-01 gates | `package_hash` + `deployment_identity_hash` | `evidence_references` + `leakage_record_id` are the natural inputs for `confirmation_evidence_duration` and `FAMILY_OBSERVED`; `leakage_record_id` absent-or-UNKNOWN → refusal at Promotion Authority (verified brief wording). OQ-13 marking schema feeds `leakage_record_id`. |
| observation ledger | 5 writer classes → serialising appender | §3 recompute, §4 marking, gates | `observation_id` (OQ-15), `row_hash` (OQ-12) | Entirely new package/store. **P013/P022 boundary (concrete, below §3A).** |
| registry seed | P022 resolver consumes registry census | none | STGxxx | Now VERIFIED: 63 strategies present; **no `strategy_family` registry field exists** (`classification_confidence` 46 heuristic_auto / 17 explicit_metadata is the only confidence split, `builder:703,733`). Do not conflate with `family_id`. PR #140 **is verified** by source inspection (`qa/PR140_REGISTRY_VERIFICATION.json`): merge `2e8a649a9052429b60b236dbab8b8376d8d5b065` is an ancestor of `42f9957`. `build_strategy_research_registry.py` `_promotion_maturity` `:571`, `build_strategy_entry` `:625-639` refuses ungranted promotion tokens. That merged promotion-label guard is **not** the P013 immutable package freeze/enumeration store. Code was not executed; inspection only. |
| P020 successor | P020 handoff names canonical path (`p020_simulator.py`, `portfolio_simulator.py`, `mega_walk_forward.py`; `simulate_slice` delegates to `p020_simulator`) | P022 consumes the *migrated canonical path* as OQ-3's read-site location | — | **OQ-3 is a transitive P020/P013 dependency, not a "no P020 dependency" claim** (draft §2.3.3: "the exact read site is in the not-yet-migrated canonical path"). P020's current seam = `simulate_slice` + `p020_simulator` surfaces; P022 does NOT touch them and does NOT demand warm-start observation read hooks from P020 — those remain future scope. P022 consumes nothing from P020's 1.0/2.0 identity surfaces; P020 interface freeze independently verified (`FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`); full integration **`WAIT_P020_ACCEPTANCE`**; no new P020 policy carrier; six-member evaluation hash unchanged; no recreation of P020 work. |

Specific conflicts found:

A. **P013/P022 boundary for the independent access observer and the authoritative
   completeness anchor (concrete):**
   - Shared namespace: ledger rows and freeze receipts live under one namespace keyed by
     `package_hash` (+ `observation_id` rows), so the §3.2 recompute parses FROZEN freeze
     receipts and ledger rows through the same identity path.
   - Row transaction binding: each ledger row binds
     `observation_id + package_hash + row_hash + prev_hash` in one canonical-JSON preimage
     recommended to be `canonical_json` itself (`identity.py:31-44`) — closes OQ-12 with a
     ratified encoder; writer_seq monotonicity refuses non-monotonic rows (draft §2.4,
     DS47-F06 fold); the freeze receipt carries `frozen_at` and is the completeness anchor
     upstream of every positive-duration assertion.
   - P022 owns: resolver, interval math, proposed diff, ledger appender + verifier design
     (after P013 accepts). P013 owns: freeze-record store, `package_hash ↔ candidate_id`
     join, catalog. Neither side writes the other's namespace.
B. Draft OQ-1 "no stated preimage, no named producer" vs `identity.py:54-73` — draft stale;
   source wins (C-2 discipline). Residual OQ-1 narrows to the P013 freeze-receipt store;
   recipe reuse, no replacement hash.
C. `FAM-UNKNOWN` sentinel satisfies `NonEmptyStr` (`base.py:16`) so it can flow through
   `StrategyPackage`/`TrialRecord` validation today — enforcement gap only (D-4).
D. `AdmissionDecision` has no family precondition field; adding one is a 0.1.0-pinned
   contract break (`base.py:69` pattern `^0\.1\.0$`) — needs versioned change, not P022 prose.
E. OQ-3 read-site claim vs supplied tools — both true: read site belongs to the not-yet-
   migrated P020 canonical path; `mega_walk_forward.py` in the archive is the pre-migration
   instrument without the hook. Not a contradiction, a statement of sequence.

## 4. Independently derived proposed tests (PROPOSED_NOT_EXECUTED)

Derived from source formulas, not copied from the draft's §5. Inputs are concrete; every
expected output is computed by hand from the cited source lines.

T1 — family-id determinism + exact-neighbourhood hash (proposed against existing shipped contracts; a new test file remains PROPOSED_NOT_EXECUTED and is still implementation).
Input: `compute_family_id(source_provenance={"source":"transcript-7"}, producer="normalizer-v2",
parameter_neighbourhood={"period":[18,22]})` twice, and once with key order reversed in the
neighbourhood dict. Expected: identical `FAM-<16 hex>` all three times (canonical_json
sorts keys, `identity.py:31-44`); changing one hex char of the neighbourhood changes the
digest. Mutation (must FAIL): replace `parameter_neighbourhood` with the string
`"approved-space-v1"` — hash changes → confirms exact-object hashing, i.e. no bucket
semantics live in the contract.

T2 — FAM-UNKNOWN flows through validation but binds nothing (proposed against existing shipped contracts; a new test file remains PROPOSED_NOT_EXECUTED).
Input: build `TrialRecord` as in `tests/test_trials_and_eligibility.py:27-53` but
`family_id="FAM-UNKNOWN"`. Expected: validates (NonEmptyStr, `trials.py:22`) — no error.
Then request a LIVE_CANDIDATE verdict set containing that record's identity:
Expected: no contract-level refusal exists today (documents OQ-5 gap). Mutation for the
future enforcement change: same verdict set with `family_id="FAM-UNKNOWN"` must yield
`outcome=BLOCKED`, `blocked_reason` naming family sentinel — PROPOSED_NOT_EXECUTED until
the WP-P0-04 change lands.

T3 — **untouched-window decomposition with complete proof (statute: 0 unless proven).**
Fixture inputs: window `[0, 100)`; direct reads `[10, 25)`; sibling reads `[20, 40)`.
Expected: observed union `[10, 40)`, untouched `[0, 10) ∪ [40, 100)`,
`duration = 70` — **but only when** the ledger + coverage proof is complete and
independently bound (freeze receipt present, coverage rows bound to `coverage_key`,
completeness anchor acceptable). **Any missing ingredient — absent ledger, missing
observer coverage, absent/unverifiable anchor — yields duration = 0** (fail-closed). It is
explicitly NOT the case that mathematically empty observations imply 100: missing evidence
= 0 confirmation evidence. All fixtures PROPOSED_NOT_EXECUTED.

T4 — FAM-UNKNOWN zeroes the window (b4 analog).
Input: ledger absent-or-present, `family_id(B)="FAM-UNKNOWN"`. Expected:
`untouched(B)=∅`, duration 0 regardless of direct rows. Mutation: same store,
`family_id="FAM-0123456789abcdef"` with no sibling rows → duration = full window only if
positively complete; without completeness attestation the expected stays 0 — documents why
the mutation arm needs the OQ-16 conjunct.

T5 — resolver conflict refusal (§1.3 step 0; needs new resolver + catalog stub).
Input: fixture freeze record parent → `F1`; catalog candidate → `F2`. Expected:
`FAM-UNKNOWN` (both refused; list order irrelevant). Mutation: catalog result flipped to
`F1` → expected shared `F1`. PROPOSED_NOT_EXECUTED (resolver and catalog do not exist).

T6 — row-hash chain (needs appender; OQ-12 closed by `canonical_json` recommendation).
Input: 3 rows, edit `window_end` of row 2 without recomputing. Expected: verifier DETECTS
via row 2 recompute. Mutation: recompute row 2 only → DETECTED by row 3 `prev_hash`;
recompute 2..3 + manifest → DETECTED only by the independent anchor. PROPOSED_NOT_EXECUTED.

T7 — out-of-order writer_seq. Input: same strictly-monotonic appender, rows 1,3 then 2.
Expected: appender refuses row 2 (non-monotonic), no partial write. PROPOSED_NOT_EXECUTED.

T8 — duplicate observation identity / conflicting bytes. Input: two rows sharing
`observation_id` with different payload bytes. Expected: second committed row truncated/
refused at the store boundary (identity uniqueness, OQ-15), verifier DETECTS if present in
an older segment. PROPOSED_NOT_EXECUTED.

T9 — graph-store ancestor / reversed edge. Input: lineage graph where candidate B cites
ancestor A; then edge reversed. Expected: store rejects the reversed-edge mutation
(fail-closed OQ-14), resolution unchanged. PROPOSED_NOT_EXECUTED.

T10 — missing strategy/search-space version. Input: freeze receipt lacking either the
normalized strategy identity or the frozen search-space identity. Expected: decision-98
adapter refuses (cannot form family key), duration 0. PROPOSED_NOT_EXECUTED.

T11 — wrong environment/deployment. Input: trustworthy ledger rows tagged
`EXCHANGE_TESTNET` fed to a `LIMITED_LIVE` window. Expected: recompute refuses
(environment lineage excluded from all hashes `test_identity.py:82-100`, so it is visible
only at row level → rows must be environment-stamped and checked). PROPOSED_NOT_EXECUTED.

T12 — validly hashed truncated ledger caught by the independent anchor. Input: ledger
clone missing final segment, all row hashes internally valid. Expected: P013 freeze
receipt / enumeration store anchor count mismatch → detection via the **independent**
anchor, not the chain (proves anchor independence, OQ-8). PROPOSED_NOT_EXECUTED.

T13 — concurrent precommit fail-closed. Input: two writers race one serialising appender.
Expected: one wins, one exits, store intact; no torn row. PROPOSED_NOT_EXECUTED.

T14 — decision-98 adapter (proposed; the "precise proposed adapter" demanded by decision 98;
PROPOSED_NOT_EXECUTED → for P0-04/P013 action).
Input normalization (concrete, no owner vacuum):
`compute_family_id(source_provenance={"strategy_identity": <registry STGxxx id
(e.g. "STG001")> | candidate lineage from P013 catalog}, producer=<unchanged producer>,
parameter_neighbourhood={"approved_search_space_version": <frozen search-space version
string bound to `preregistered_space_hash`>, "strategy": <the same normalized
strategy identity>})`. Version separation: `approved_search_space_version` rotates the key
without any bucketing; every former (strategy, version-1) `FAM-<hex>` stays verifiable
because the preimage continues to hash exactly and historical outputs are recomputed at
their recorded version. Registry STGxxx id is the natural strategy identity carrier —
verified present on all 63 entries; no registry write is proposed (P022 touches no
registry file).

Boundary/representation note applying to every window fixture above and to b1–b4: the
proposals use **half-open `[start, end)`** representation. This is a PROPOSED
representation; any unresolved legacy artifacts employing closed `[start, end]` endpoints
must be normalized before join — touching-boundary semantics (a direct read ending exactly
where a sibling read begins) must be preserved deterministically across the translation,
i.e. the union remains connected and the untouched count is invariant under the
conversion. Which legacy artifacts (if any) use closed endpoints is a remaining material
decision (below).

## 5. Ordered minimal implementation steps (exclusive writers)

Each step is independently acceptable; nothing before P013 freezes keys. **None of it is
authorized by this lane; every step here is a proposal awaiting a separate bounded
authorization naming the exact file, writer and lines.**

1. **R1 (P022 lane, prose deliverable in this report's successor)** proposed contract diff
   for P0-04: add `FAM_UNKNOWN: Final = "FAM-UNKNOWN"` + eligibility validator. Not in any
   slice: contract change is P0-04's. Accept: diff names exact file/lines it would touch
   (`trials.py:22`, `admission.py`). Writer: P022 (proposed text only).
2. **R2 (P022 lane, proposed)** `MTC_COMMAND_CENTER/03_QUANTLENS/tools/family_resolver.py`
   — pure resolver implementing §1.3 order 0–4 with injected inputs (freeze-receipt,
   catalog, triple, confidence, decision-98 adapter input per T14). No ledger, no registry
   reads. Writer: P022 exclusive.
3. **R3 (P022 lane, proposed)** `MTC_COMMAND_CENTER/03_QUANTLENS/tools/untouched_window.py`
   — pure interval math incl. whole-window fail-closed rule, `coverage_key` binding,
   half-open representation, "missing evidence = 0" statute. Writer: P022 exclusive.
4. **R4 (P013 owner, their file)** freeze-receipt store (typed, named; carries
   `package_hash ↔ candidate_id` join + `frozen_at` + authoritative enumeration anchor)
   — resolves OQ-1/OQ-2 residuals via the recommended reuse of the existing hash recipe.
   Writer: P013.
5. **R5 (P0-04 owner, their file)** contract change: sentinel literal + LIVE_CANDIDATE
   binding + decision-98 neighbourhood-input adaptation. Writer: P0-04.
6. **R6 (P022 build lane, separately authorized)** observation ledger appender + row-hash
   verifier + FAMILY_OBSERVED marking per §2/§4. Writer: P022 exclusive, after P013 accepts
   and OQ-10 re-audit.

## 6. Remaining material decisions (one recommended resolution each)

1. ~~Registry/tag dictionary absent~~ — **resolved**: verified this round (63 STGxxx,
   all with source_folder; `classification_confidence` 46/17 = heuristic_auto/
   explicit_metadata; no `strategy_family` registry field exists at all).
2. ~~Plan/brief/A-7d/D026 text absent~~ — **resolved**: verified this round (D-15,
   A-7d §6.6 rules 6–8, A-18 promotion refusal for absent/UNKNOWN `leakage_record_id`,
   D026 rule text in DESIGN_DEFECT_PATTERNS).
3. `package_hash` freeze-record store does not exist. Recommendation: owner picks the
   **P013 freeze receipt** as the single authoritative freeze/enumeration store; reuse the
   exact existing `compute_package_hash` recipe inside it; **no new or replacement hash**.
4. Legacy endpoint semantics: proposed fixtures use half-open `[a,b)`; whether any legacy
   window evidence uses closed `[a,b]` endpoints — and the exact deterministic conversion —
   is an owner choice before T-fixtures are ratified. Draft is silent; touched-boundary
   semantics must survive the conversion unchanged.
5. **OQ-7 cross-producer tolerance** (decisions 100+101 still leave an unstated time-based
   tolerance). Recommendation: **do not mint a zero tolerance if it is not ratified.**
   Recommended architecture independent of the δ decision: an **exact event-identity join**
   (same event id → reconcile by identity first), with any genuinely time-based tolerance
   remaining UNSET pending the ratified decision and evidence. No δ number is proposed here.
6. P020 interface freeze independently verified (`qa/P020_FREEZE_VERIFICATION.json`,
   SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5`,
   `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`); canonical path still not
   migrated and full integration remains **`WAIT_P020_ACCEPTANCE`**. Recommendation:
   OQ-3 is tracked as a transitive P020→P013→P022 dependency on the migrated canonical
   path, not eliminated; no extra P020 scope demanded (warm-start observation read hooks
   stay future); no new P020 policy carrier; six-member evaluation hash unchanged.
7. PR #140 promotion-label guard **verified** (source inspection only, not executed):
   merge `2e8a649a9052429b60b236dbab8b8376d8d5b065` ancestor of `42f9957`
   (`qa/PR140_REGISTRY_VERIFICATION.json`); `_promotion_maturity` `:571`,
   `build_strategy_entry` `:625-639` refuses ungranted promotion tokens. Remaining
   unbuilt store is the **P013** immutable package freeze/enumeration receipt — PR140
   is not that store. No PR140 retrieval blocker.

## 7. Authorization-state summary

READY_FOR_SEPARATE_BOUNDED_AUTHORIZATION (proposals, this lane authorized preparation only;
pure code here is still implementation and separately needs authorization naming the exact
file, writer, and lines; no product implementation is authorized this window):

- R2 family_resolver with injected inputs + decision-98 adapter input (T5/T14 shape) —
  **early helper only if an explicit isolated build-order exception to the P013 integration
  dependency is granted in that future scope**; otherwise queued behind P013 freeze/enumeration.
- R3 untouched_window pure interval math + decomposition fixtures (T3/T4 + T7–T13 shapes,
  zero-arm only without completeness attest input) — same isolated-exception rule as R2.
- R1 proposed contract-diff text for P0-04 (prose only; contract change is P0-04's).
Full ledger/leakage integration waits for P013 freeze/enumeration and independent
completeness/anchor producers (`WAIT_P020_ACCEPTANCE` transits through P013). Prefix/path
terminology is an engineering adapter decision; do not ask the owner to name fields.
Reuse existing `compute_package_hash`; no replacement hash. P020 interface freeze is
independently verified (`FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`); the **P013
catalogue freeze/enumeration store** remains unbuilt and is distinct from that P020
receipt. Full ledger/leakage integration still **`WAIT_P020_ACCEPTANCE`**.

Blocked slices and their named blockers:
- Ledger appender/store (§2.4): OQ-2 (store layout, P013), OQ-8 (anchor unbuilt),
  OQ-11 (access/atomicity), OQ-12 (closed by `canonical_json` recommendation, pending
  owner stroke).
- Positive-duration fixtures beyond the zero-arm: COMPLETENESS-UNBLOCK =
  OQ-8+OQ-9+OQ-11+OQ-16; OQ-9 attestation emitter unbuilt.
- OPTIMIZER linkage branch: OQ-3 (transitive P020/P013 — read site in not-yet-migrated
  canonical path; no warm-start provenance field in supplied pre-migration tools).
- LIVE_CANDIDATE refusal: OQ-5 (P0-04 contract change).
- Marking row storage: OQ-13 (schema/store); graph-store enforcement: OQ-14.

Decisive findings: (1) draft's OQ-1 "no package_hash preimage/producer" is stale against
the supplied snapshot — `compute_package_hash` exists with preimage and exclusion rule;
resolution is recipe reuse + P013 typed freeze receipt, no replacement hash; (2) the
registry census is now independently verified (63/46/17 correct under
`classification_confidence` with values heuristic_auto/explicit_metadata; strategy_family
absent from registry JSON), and master-brief gate wording (D-15, A-7d, A-18 §6.6, D026) is
verified — five previously unverifiable evidence blocks are now source-grounded; (3)
`compute_family_id` can never emit `FAM-UNKNOWN` — resolver must be a new refusing
component, not a call into the existing function; (4) decision 98 binds the family key to
(approved search-space version, strategy) — the proposed adapter normalizes both
identities concretely with version separation and no bucketing; (5) OQ-3 is a transitive
P020/P013 dependency through the migrated canonical path (P020 seam = `simulate_slice` /
`p020_simulator`), not a no-dependency or extra-scope claim; (6) untouched-window duration
70 in the T3 decomposed fixture requires a COMPLETE ledger + coverage + anchor proof;
missing evidence yields 0, never implied 100.

## 8. Remaining facts / one recommendation

**Remaining facts:** `compute_family_id` / `compute_package_hash` exist (source implementation verified); no observation ledger or ledger-specific `observation_id` (P030 `MarketBar.observation_id` is a different field); no FAM-UNKNOWN eligibility binding; P013 catalogue freeze/enumeration store unbuilt; registry census 63 / 46 heuristic_auto / 17 explicit_metadata, no `strategy_family` field; OQ-3 is transitive P020→P013→P022; P020 interface freeze independently verified (`qa/P020_FREEZE_VERIFICATION.json`, SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5`, `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`) but full integration **`WAIT_P020_ACCEPTANCE`** through P013; no new P020 policy carrier; six-member evaluation hash unchanged; all new fixtures `PROPOSED_NOT_EXECUTED`; no package acceptance.
**One recommendation:** authorize isolated R2/R3 helpers (`family_resolver.py`, `untouched_window.py`) only with an explicit isolated build-order exception naming those two files; otherwise keep P022 queued behind P013 freeze/enumeration. Do not start the ledger/leakage package from this window.
