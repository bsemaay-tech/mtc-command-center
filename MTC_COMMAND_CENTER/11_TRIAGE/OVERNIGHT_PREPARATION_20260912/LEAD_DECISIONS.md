# Lead decisions and evidence limits

This packet is nonbinding preparation, checked by the Lead. It grants no implementation, schema, numeric-policy, operational, publication or acceptance authority. Start: 2026-09-11T21:59:28Z. Deadline: 2026-09-12T05:59:28Z.

## Verified facts

Baseline: `42f99571e6abb5222744d9345e1c8a9e19c23c15`. The original 538-file archive has 120 raw matches and 418 CRLF-only materializations. It is preserved; CRLF equivalence is not byte identity. A separate LF archive has 538 exact Git blob matches. Four additional, targeted raw sources are recorded in `qa/ADDITIONAL_SOURCE_VERIFICATION.json`.

- P013 has a foreign, unmerged schema/reader/search branch at `3e704c2d`; its three existing tests pass on Python 3.14.2. The reader trusts a caller-supplied covered-commit list. It is not a complete writer or independent admission verifier. Adoption requires an owned reconciliation and subsequent checks. TrialRecord has 48 fields including inherited version; ArtifactManifest lacks trial/parameter binding. Parameter and search-space hash functions are absent.
- P021 has seven deliberately REFUSED diagnostic checks, 12 closed provisional fields and four open numerical fields; `ready=false`. Its self-check and two modified-copy detections pass on Python 3.12.12. Commits `a63bd4b053648e4a7008e5f2fe5f999b94947650` and `380a26aacf960ee33fd610ec699dcc2180e4eb8a`, plus supplied historical owner records, explain the catalogue and version 2. No new owner retrieval or version-ratification task is needed. Preserve the applied 21-day minimum and the separately labelled four-week median wait.
- P021's actual bundle producer is `MTC_COMMAND_CENTER/01_MTC_PROJECT/tools/create_optimization_data_bundle.py`. Its timeframe map lacks 5m and its gap estimate truncates. Independently re-reading 17 historical CSVs reproduced 4,105,966 rows, all hashes/counts and zero internal cadence faults. This does not verify OHLCV truth or make Binance data Hyperliquid admission evidence. The four limits remain unset.
- P022 has existing identity functions and 63 registry rows: 46 heuristic and 17 explicit classifications. `strategy_family` is absent from those rows. A family hash is not a resolver, observation ledger, completeness proof or leakage gate. Ledger-specific observation identifiers are absent; P030 bar identifiers do exist.
- P030's offline checker passes, including mutants and zero network attempts. An independent probe shows archive replay NOOP versus identical WS_LIVE ingest refusal. Only one row is stored. This is the concrete next repair candidate.
- P026's existing local watchdog/backup/restore checks pass 33 tests on Python 3.12.12. This proves local components, not installed monitoring, phone delivery or a consistent snapshot of a live collector.
- Additional source inspection confirms DurableRiskPolicy's canonical float.hex hashing pattern; existing backtest I/O, cache and ccxt downloader utilities also exist. The cache's truncated MD5 key lacks venue/provenance, and its miss path downloads. `validate_dataset` is not a strict collector admission validator. These are reuse candidates with limits, not permission to invoke or modify them.

## Settled preparation interfaces

1. **P020:** its receipt SHA256 `a0a94105dea818f152453eadfcd05be5b91a3bfde70f64192de947ce2e95b6c5` and all three members were independently matched. Status: `FROZEN_FOR_NONACCEPTING_CANDIDATE_IMPLEMENTATION`; see `qa/P020_FREEZE_VERIFICATION.json`. The frozen document's older pending-seal heading is qualified by this later receipt. Full downstream integration remains `WAIT_P020_ACCEPTANCE`.
2. **Identity:** preserve the original six-member evaluation hash and all existing formulas. Deployment hash has 12 named members. Allocation, guardian and readiness policies remain distinct domains. The P020 Lead confirmed no new policy carrier is needed. P013 wraps existing strategy/symbol/timeframe fields with independent evidence. Original raw manifest digest, selected dataset manifest digest and P021's proposed ds-v1 recipe must never be silently interchanged.
3. **P013/P022:** P004 owns concrete parameter/search-space preimages and protected artifact binding. Serialize identity.py after P020 releases it. Reuse package hashing; a future family adapter binds one approved search-space version and one strategy without rewriting historical IDs. Full P022 integration needs P013 receipts and independently produced access enumeration plus an exact-head anchor. A self-consistent hash chain alone cannot prove completeness.
4. **P030/P026:** leave OPSA heartbeat payload unchanged: schema, id, seq, emitted_at, pid, optional note. P030 owns a separate proposed health sidecar for closed-market-bar freshness and reconciliation progress. A fresh process beat cannot imply a fresh feed. Back up closed partitions or a proven stable snapshot with a committed high-water mark; file-copy hashes alone are insufficient.
5. **P012:** the supplied intake report and all three production-record hashes were independently matched. The final acquisition closure arrived before delivery: SHA256 `62347dff777e1d9463779a8aa4699641f8b250ed03a5a52bac86183336601e81`; all 44 listed files and the three live production-record hashes were rechecked (`qa/P012_LATEST_CLOSURE_VERIFICATION.json`). Its public-source interpretations remain attributed to the P012 Lead; no admitted interface change resulted. Its latest research adds no admitted economic/schema change. Full production remains NONACCEPTED, including missing production funding events. Active research or synthetic success does not clear this dependency.

## Recommended build decisions

1. First authorize only root `market_data_collector.py` and `check_market_data_collector.py` for the P030 duplicate-ingest correction. Inspect replay without writing; a new incoming bar must pass validation and required gap fill before persistence. Identical replay no-ops, conflicting replay refuses, missing-gap refusal leaves the incoming bar unwritten, and existing fences remain intact.
2. Optionally authorize P021's three isolated helper modules and exact tests in `IMPLEMENTATION_MAP.md`. Keep its diagnostic reporter and four open limits unchanged. A separate bundle-timeframe repair needs its own bounded code authority, but does not depend on production engine admission. Full eligibility does.
3. Queue P013's contract prerequisite until P020 releases overlapping files and P004 receives protected scope. Full writer integration waits for accepted P020 evidence. P022's full integration follows P013; early pure helpers require an explicitly isolated scope, not an implied build-order waiver.
4. Treat P030 health/backup adapters as later configuration-and-contract slices. No P026 implementation, host change, network collector or production admission starts from this packet.

All new target fixtures remain `PROPOSED_NOT_EXECUTED`. Independently calculated literal expected hashes/arithmetic are recorded separately in `qa/FIXTURE_EXPECTED_VALUES.json`. Existing tests are component evidence, not full-package acceptance. This T2 prose check does not downgrade applicable T0 implementation review for binding identity, refusal or recovery semantics.

NEXT ACTION: present the exact P030 unit and optional P021 helper scope for authorization.
WAITING FOR OWNER: Nothing to finish preparation; new implementation and retained operational/policy decisions require separate authority.
