# HANDOFF — P0 Claude Lead, session 6 → session 7 — written Sat 2026-09-19 09:0x UTC+3 by session 6 (`tradingview-lab-clean-be` / `4a8233`)

Supersedes `HANDOFF_20260917_SESSION7.md` (keep it for the Wed/Thu detail). Copies: this file, `%TEMP%\CLAUDE_HANDOFF_20260919_P0_SESSION7.md`, CT13 `MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/`. Owner-facing times in UTC+3; records may keep Z. The machine-stamped lane ledger is `C:/tmp/OPUS_QUEUE_20260916/QUEUE_LOG.txt` — trust it over prose times.

## 0. Read first (in this order)
1. Memory `MEMORY.md` head + `p0-claude-lead-session-2026-09-16-day` (Wed→Sat sections) + the three permanent lessons of this week: `review-lane-mutant-armed-a-live-tool`, `partial-read-is-not-a-measurement`, `red-evidence-on-the-pinned-interpreter`.
2. `C:/tmp/CLAUDE_P0_RUN_20260913/RUN_STATE.md` tail (Fri/Sat lines) and `OPUS_QUEUE_20260916/QUEUE_LOG.txt` tail.
3. Before touching anything: `ListAgents` + mtimes (a live peer session kills shared-`.git` Gemini calls); probe every route before use (Codex/Sol reset Sat 18:47 local; Grok weekly Fri; Claude PRO profile `~/.claude` 5-hour windows; Gemini `bsemaay` mailbox).

## 1. Standing rules (binding, unchanged)
No host contact except an owner-worded testnet action; no deploy; no trading (the owner opens/closes positions); no mainnet ACTIONS (public read-only Info reads are allowed); no spend beyond subscriptions; workflow files merge only after a T1 verdict; no Claude lanes for audits; **no git AND no shell cwd inside any worktree while `agy.exe` runs**; lane evidence ASCII-only before Gemini staging; hash `git show` blobs, never CRLF checkouts; guard PASS before every commit, exact staged paths; `HL_API_WALLET_KEY` never printed; owner address short-form in prose; mailboxes by local part only; never bare `codex`; **the Lead never accepts its own code**; Bridge pytest `--basetemp` ASCII outside `C:/tmp`; a sleeping host kills timed waiters; two flagships of different families + Gemini + Lead reproduction before any merge; **DD-06 lane SAFETY RULE** (scratch mutants only, fixture suite only, never `main()`/real SDK — the workstation registry holds a live agent-wallet credential). New this week: RED evidence on the PINNED interpreter (lesson above).

## 2. Where every package stands (Sat 09:0x)
| Lane | Package | Worktree @ pin | Exact-Opus | Gemini | Next |
|---|---|---|---|---|---|
| 1 | P0-31 M1 | `C:/tmp/P031_M1_20260913` @ `61c56148` | PASS-WITH-NITS (3 reads; N-14..N-20) | PASS ×2 | **Sol tonight on `61c56148`**; then the semantic NIT slice (N-5 B, N-10 yes, N-13 yes, **N-16 B**, N-14/N-15 fences, N-11 blob-OID pins) → both flagships → merge; M2 build after (owner Q7-Q13 all A) |
| 2 | P0-12 capture tool | `C:/tmp/P1CAP_20260914` @ `af921d75` (untouched) | PASS-WITH-NITS (7 NITs) | PASS | **Sol tonight on `af921d75`**; the NIT slice is DRAFTED, NOT APPLIED: `P1CAP_NIT_SLICE_PREP_20260918/p1cap_nit_patch.py` + README — apply/test/RED on the pinned interpreter, then Gemini + Opus re-read |
| 3 | P0-30 exporter | `C:/tmp/P030_INTEGRATION_20260913` @ **`d426e79f`** (= `45a7f50e` + NIT slice N1-N5/F8) | `45a7f50e` PASS-WITH-NITS; **slice read REQUEST_CHANGES** (F-1: the N4 walk-guard fence is interpreter-dependent — untested on the pinned 3.12; my RED came from 3.14; F-2 third adapter parse site unguarded; F-3/4/5 NITs) `P030/LEAD_ADJUDICATION_P030_NIT_T0.md` | R1 PASS-WITH-NITS; NIT delta PASS | **Task 1 of session 7: the repair spelled out in the adjudication** (derive the depth at test time, one arm per layer, RED on BOTH interpreters, guard the third site, receipt-side race arm, docstring trim; fix the record) → Gemini → lane 3 attempt 5 → Sol. If not repaired before 18:47, the Sol P030 lane reads `45a7f50e` |
| 4 | P0-26 OPS-A | `C:/tmp/P026_REPAIR_20260915` @ **`505af399`** (= `e114ed31` + NIT slice 1/2(doc)/4/5; NIT-3 = P0-30 F8 on that branch) | `e114ed31` PASS-WITH-NITS; **slice read PASS-WITH-NITS 0 REQUIRED (Sat 09:21; `P026/LEAD_ADJUDICATION_P026_NIT_T0.md`)** — NIT-1 four sole guards unfenced, NIT-2 non-UTF-8 read, NIT-3 one `--latest` unmarked, NIT-4 authority (evidence doc outside the ceiling → owner `P26-EVID A|B`), NIT-5 docs 900 s vs ratified 300 s | PASS; NIT delta PASS | **Sol tonight on `505af399`**; next P0-26 slice = NIT-1/2/3/5 (+ the evidence note per the owner); merge PR after Sol |
| 5 | P0-27 CI | merged | T1 ×2 | — | Sol retrospective optional, last |
| 6 | P0-21 S1 | `C:/tmp/P021_S1_20260915` @ `eada65ed` | PASS-WITH-NITS (2 reads; NIT-3/4/5/6) | PASS | **Sol tonight on `eada65ed`**; NIT slice (3 carrier field, 5 tuple fold, 6 `s2_scope` wording) after; then S2 ratification (B-22) |
| 7 | DD-06 probe | `C:/tmp/P029_DD06_20260915` @ **`a46da0aa`** (round 4: INCONCLUSIVE fund arm measured+stopped, `..._UNCLASSIFIED`; arm order B withdraw3 last) | **fifth read PASS-WITH-NITS, 0 REQUIRED** (`DD06/LEAD_ADJUDICATION_DD06_R4_T0.md`); rounds: `1af85067`→`a46b2a9d`→`2128352b`→`acd79b52`→`a46da0aa` | R1..R4 all PASS | **Sol tonight under the SAFETY RULE** (make the Sol brief carry it); execution blockers before any r4 run: N-22 (step packet + `kvm2_dd06_probe_run_r3.sh` stale: order B + `DD06_PROBE_RUN_TOKEN="$RUN_ID"`) — re-issue the packet WITH the owner's word; NIT slice later (N-16/N-20 amount table + zero threshold, N-19/N-26 abort discards the record, first) |
| 8 | P0-12 intake half | `C:/tmp/P012_INTAKE_20260915` @ `a871e429` | PASS-WITH-NITS (2 reads; NITs 1-9 + NIT-A) | PASS | **Sol tonight on `a871e429`**; NIT slice with **D6-START B** (window start exclusive) after |
| — | P0-20 | derived plan `d84b043a`; **measurement EXECUTED ONCE Fri 08:17** (`P020_MEASUREMENT_20260915/LEAD_TERMINAL_P020_MEASUREMENT.md`: `MEASUREMENT_COMPLETE_NO_ACCEPTANCE`, 15/15 both ledgers, semantic equal, base plan restored; Grok pre-screen SATISFIED `laneGKDERIV_grok/LEAD_ADJUDICATION_GROK.md`) | — | Sol PASS-WITH-NITS + Gemini SATISFIED on the plan | the measurement RECORD goes to Sol + Gemini before any acceptance discussion; Grok NITs 1-3 carried (driver validation depth; `tool_sha256` self-hash; `DERIVATION_RULE.md` digest drift = doc fix) |
| — | P0-12 r2 real capture | `P012_PATH1_REAL_CAPTURE_20260917/r2/` (owner-signed, tool-run at `af921d75`, verified; first real D-1..D-6 candidate in a hand-filled dry run) | — | — | **O-1 wait** (owner) — nothing to do until he says `O-1 build` |

## 3. Owner decisions this week (all recorded in CT13 `DECISIONS.md`)
Thu: `OD-20260917-P031-M2-Q7-Q13-1` (all A), `-P031-M1-N5-N10-N13-1` (B/yes/yes), `-P012-ORACLE-O1-O3-1` (wait / 0.05 % / now). Fri: `OD-20260918-DD06-CAP-A-1` (one extra round), then the one line `N-16 B  D6-START B  DD06-ORDER B  P26-N2 doc  O-1 wait  DD06-R4 A` → six rows. **One owner one-liner open: `P26-EVID A|B`** (keep the evidence-doc note in the merge PR with a row, or drop that hunk to the follow-up slice; default B). Plus the registry-key hygiene note (his machine). Sat 08:5x the owner said: finish unfinished work, start nothing new, update memory, prepare the handoff — done here.

## 4. Tonight — Sol (Codex Plus reset Sat 18:47 local)
`make_sol_lane.ps1 -Pkg P031|P1CAP|P030|P026|P027CI` reads the CURRENT pins from each Wednesday launcher (`OPUS_QUEUE_20260916/<lane>/opus/run.ps1`) and refuses on drift — so: **re-pin lane 3 to `45a7f50e` unless the repair landed** (the launcher now says `d426e79f`), keep lane 4 at `505af399` (its Opus read passed), and add P021S1 (`eada65ed`), DD06 (`a46da0aa`, brief must carry the SAFETY RULE), P012INTAKE (`a871e429`) by hand. Order: P031 → P1CAP → P030 → P026 → P021S1 → DD06 → P012INTAKE → P0-20 record → P027CI retrospective. Sol is documentary-only (no shell); ~3-4 lanes per bucket, weekly cap after ~2 days — prioritise. Every Sol PASS + the existing Opus PASS + Gemini + a Lead reproduction = merge-ready (PR-only serial merges to master; NIT slices that changed bytes need BOTH flagships on the final bytes).

## 5. What is in flight / armed right now
- Lane 4 finished and is adjudicated (above); archived `P026/ATTEMPT2_PASS_WITH_NITS_505af399/`.
- Nothing else runs. No Gemini call. No timed waiter.
- CT13 last push **wave 24 `a20f9629`**; everything through Sat 09:1x is in it (wave 25 adds lane 4's result).

## 6. Routes (probe, never recall)
Claude PRO profile (`~/.claude`, `claude --print --model claude-opus-5 --effort xhigh`) for exact-Opus lanes, one at a time, `api_error_status 429` = 5-hour cap; Gemini `gemini-3.8-flash-high` via `Invoke-GeminiProReadOnly.ps1` on the `bsemaay` mailbox (retry chain `gemini_retry_chain_v2.py <root>`; each root needs its own runner clone with the packet boundary — `run_<lane>_g38.ps1`); Grok `Invoke-GrokSubscription.ps1` (weekly reset Fri; used Fri for GKDERIV); Codex/Sol Sat 18:47; `uvx ruff` 0.16.8 for parity checks (per-file counts vs HEAD, by rule text); the pinned Bridge/P020 interpreter is Python **3.12** (`C:/tmp/P020_IMPL_20260912/.../.venv/Scripts/python.exe`), the shell's `python` is 3.14 — name the interpreter in every record.

Recorded by Claude Opus 5 Lead (session 6, `4a8233`).
