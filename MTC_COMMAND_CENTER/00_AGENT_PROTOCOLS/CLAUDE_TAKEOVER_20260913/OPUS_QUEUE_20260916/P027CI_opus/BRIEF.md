# Exact T1 review slot: claude-opus-5, xhigh — WP-P0-27 CI workflow additions, PR #193 (a3325836) and PR #194 (63b7bbe0)

Read and execute `C:/tmp/OPUS_QUEUE_20260916/P027CI/REVIEW_BRIEF.md` exactly. You are the exact `claude-opus-5` xhigh T1 reviewer of that brief. Write your single report to `C:/tmp/OPUS_QUEUE_20260916/P027CI/opus/OPUS_T1_REPORT.md` (create it early with a skeleton and update it as you go; save after every section). Write nothing else outside `C:/tmp/OPUS_QUEUE_20260916/P027CI/opus/` except scratch copies under `C:/tmp/OPUS_P027CI_SCRATCH/`. Never run git status/diff-with-working-tree/add/commit/checkout/push in any repository (`git -c safe.directory=* rev-parse HEAD`, `git -c safe.directory=* show <rev>:<path>` and `git -c safe.directory=* diff <sha> <sha> -- <paths>` only); `gh` GET-only (`gh pr view`, `gh run view`, `gh api` GET) with every call and output in the report; never merge, comment, close, re-run or touch the ruleset. The evidence records are under `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/P027/`, `.../P027_OPSA_CI_20260915/` and `.../P027_CI_GEMINI/`; verify every claim against the workflow bytes and the GitHub state you read, not against a record's prose. If a HEAD is not the pinned one, your verdict for that PR must be BLOCK. End the report with the exact per-PR VERDICT lines and the T1 MERGE RECOMMENDATION line the brief requires.
