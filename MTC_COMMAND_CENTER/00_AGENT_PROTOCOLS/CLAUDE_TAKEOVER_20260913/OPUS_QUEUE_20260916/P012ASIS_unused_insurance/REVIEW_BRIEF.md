# Review brief — WP-P0-12 funding intake, slice 4: the ACCEPTING half of `export_mtc_funding.py` under D-1..D-6, candidate `a871e42965322d4fc66e317ef7067d6fca28972c` (exact `claude-opus-5` xhigh; Wednesday 2026-09-16 slot, OPTIONAL lane — only if allowance remains after lanes 1-5; also the source brief for the Friday Sol lane)

You are a fresh, independent reviewer. Your exact role (model and effort) is fixed by the launcher that started you; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory (scratch copies under `C:\tmp\OPUS_P012INTAKE_SCRATCH` only). The candidate is LEAD-AUTHORED (Claude Opus 5 Lead, session 6, disclosed); the Lead never accepts its own code — you are the first exact flagship read. Tier: T1 by the Lead's Gate-1 classification (offline tool + tests, no Bridge runtime module, no host, no key), reviewed at this slot's rigor because the tool is Bridge-adjacent and its subject is venue evidence.

## Context (settled facts; verify identities, do not re-decide)
- Package WP-P0-12 (funding evidence). Base `origin/master` `fcac0ac6`. Branch `feature/p012-funding-intake-adapter-20260915`: `004a0711` → `65c4bc40` → `4c802e9b` (the NON-accepting adapter, reviewed by Gemini ×4, counted PASS-WITH-NITS) → **`b667dbcc`** (format-only: `ruff format` reflow of the pre-existing `export_mtc_funding.py`, NO semantic change — verify mechanically, see item 7) → **`9ef072a8`** (the slice: four files under `IBKR_PAPER_BRIDGE/`: `tools/export_mtc_funding.py`, `tools/funding_intake_adapter.py`, `tests/test_mtc_funding_export_real_capture.py` (new, 57 tests), `tests/test_funding_intake_adapter.py` (+4 tests)).
- Authority: `OD-20260916-P012-INTAKE-D1-D6-R-1` (owner `D all R`, 2026-09-16 08:05 UTC+3): D-1 `source_event_digest` = sha256 over the exact captured funding-row bytes, tagged `HL_USERFUNDING_ROW_V1:<hex>`; D-2 `funding_event_id` = `hl-funding:<account-short>:<coin>:<time_ms>`; D-3 venue stamp verbatim as `event_timestamp` + `interval_hour_utc` (floor); D-4 production requires an oracle capture at each funding instant, a payment-derived price is never admitted; D-5 `REAL_CAPTURE_READ_ONLY` admitted only with D-1 and a witness identity naming the ownership-signature record; D-6 two byte-identical funding passes + hour-aligned window + 1-second end tolerance + a fills-based expected-count check. The row says: "explicit `--evidence-kind` admission, never a `--mode` toggle"; "this row admits NO production evidence: `OD-20260914-P012-ADMISSION-Q3` Wait and the gate order Q6 stand". Read the row yourself in `C:/CT13/DECISIONS.md`.
- What the slice claims: a second, explicitly named evidence profile (`--evidence-kind REAL_CAPTURE_READ_ONLY` / keyword-only `evidence_kind=`) whose packet must declare the same kind everywhere (`CANDIDATE_EVIDENCE_KIND_MISMATCH` otherwise); `ACCEPTED_EVIDENCE_KINDS` unchanged `(SYNTHETIC_FIXTURE,)`; the synthetic candidate's bytes unchanged; every binding fact re-derived from the exact captured venue bytes carried in the packet (`coverage.funding_witness_passes`, `coverage.fills_witness`, `coverage.source_witnesses[event_id]`); the real candidate labelled `REAL_CAPTURE_FUNDING_CANDIDATE_V1` / `synthetic_only: false` / `REFUSED_REAL_CAPTURE_READ_ONLY_NOT_A_PRODUCTION_RECORD`, structurally non-consumable (no `events`/`schedule_id`/`settlement_currency` at the root). On the real r1 capture the adapter now also writes `binding_packet_real_capture.json`, and the exporter REFUSES it with `CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE` (D-4: r1 captured no oracle read) — the Lead calls that the honest outcome.
- Reviews so far: Gemini 3.8 detection of `9ef072a8` (`P012_INTAKE_S4_GEMINI`; see the record folder for its verdict, or the absence of one); Lead verification `LEAD_VERIFICATION_P012_INTAKE_S4.md` with RED 57/57 on the pre-slice bytes, four mutants, full Bridge suite 1696 passed. The Lead disclosed two corrections inside the slice: the adapter's `retained_rows_expected.json` `payload_digest` label `UNRESOLVED:D-1` → `UNAVAILABLE:BRIDGE_OBSERVATION_REQUIRED` (with the two adapter assertions), and the sharpening of one D-1 test after a first mutant run. Owner Q3 "Wait" stands; nothing here admits production. Lead = author, never acceptor.

## Subject (read-only worktree; verify before reading anything else)
- `C:/tmp/P012_INTAKE_20260915`, HEAD must be `a871e42965322d4fc66e317ef7067d6fca28972c` (`git -c safe.directory=* -C C:/tmp/P012_INTAKE_20260915 rev-parse HEAD`; NO other git command that touches the working tree — never status/add/commit/checkout/stash; `git -c safe.directory=* show <rev>:<path>`, `git -c safe.directory=* diff b667dbcc 9ef072a8 -- <paths>` and `git -c safe.directory=* diff 4c802e9b b667dbcc` for byte comparisons only, and say so). If HEAD differs, STOP and write `VERDICT: BLOCK` with the observed HEAD.
- Tests: from `C:/tmp/P012_INTAKE_20260915/IBKR_PAPER_BRIDGE`, Bridge interpreter `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe -m pytest tests/test_mtc_funding_export_real_capture.py tests/test_funding_intake_adapter.py tests/test_mtc_funding_export.py -q -p no:cacheprovider --basetemp C:/bt_opus_intake` (expect 229 passed, 1 skipped). Ruff: `C:/tmp/wp_p0_04_tooling_20260825/Scripts/ruff.exe check` on the four files (expect exactly the 5 findings that already exist on `4c802e9b`'s exporter: UP035, 3× ISC004, SIM101; 0 new). The r1 capture directory is `C:/tmp/CLAUDE_P0_RUN_20260913/P012_PATH1_REAL_CAPTURE_20260915/r1/` (read-only; the launcher grants it).
- Records: `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/P012_FUNDING_INTAKE_20260915/S4_ACCEPTING_HALF/` (verify every closure against bytes, never against a report's description).

## What you must decide
1. **Explicit admission, no toggle, fences intact.** From the bytes: how the real profile is selected; that a real packet under the default profile refuses (`CANDIDATE_EVIDENCE_KIND_MISMATCH`), a synthetic packet under the real profile refuses, one foreign provenance kind refuses, an unknown kind refuses `CANDIDATE_PRODUCTION_EVIDENCE_UNAVAILABLE`; `ACCEPTED_EVIDENCE_KINDS` unchanged; `tests/test_mtc_funding_export.py` byte-identical between `4c802e9b` and `9ef072a8` (`git diff 4c802e9b 9ef072a8 -- IBKR_PAPER_BRIDGE/tests/test_mtc_funding_export.py` empty) and its pinned synthetic candidate bytes still pass. Any code path inferring the kind from the packet = REQUIRED.
2. **D-1..D-6 fidelity, each with your own failing input.** For each rule: the owner's row text, the implementing lines, producer A (packet field) and producer B (tool re-derivation and its byte source), and ONE mutant or malformed packet OF YOUR OWN that the tool must refuse — run it (pure function `build_funding_candidate(..., evidence_kind="REAL_CAPTURE_READ_ONLY")` with the fixtures of the new test module). Specifically try: a digest over a canonical re-serialization of the row (must refuse D-1); an id built from the hour instead of the stamp (D-2); a rounded stamp (D-3); `oracle_price_source = "DERIVED_FROM_PAYMENT"` (D-4); an identity without the ownership token or for another scope (D-5); a window ending mid-hour, one pass only, a captured row missing from the inventory, fills that leave the position open at an hour with no payment, a row `szi` the fills do not reach (D-6). Report VERIFIED / DECLARED-NOT-CHECKED / DIVERGES-FROM-RULING per rule.
3. **The 1-second end tolerance.** Reproduce with a `[15:00, 18:00)` window on the r1 bytes (the 18:00:00.060Z stamp): accepted with tolerance; refused `CANDIDATE_BINDING_OUT_OF_INTERVAL` at +1.060 s. Then decide whether the grid (`start..end` inclusive of the end hour) and the tolerance are exactly the owner's D-6 (i), and whether the start side (a payment stamped `15:00:00.041Z` settling the 14-15 interval is inside the window by stamp) is stated anywhere — it is NOT an owner decision; say if it needs one.
4. **Real r1 dry run.** Run the adapter yourself into `C:/tmp/OPUS_P012INTAKE_SCRATCH/r1_s4` (`python IBKR_PAPER_BRIDGE/tools/funding_intake_adapter.py --run-dir <r1 dir> --out ...`), compare digests with the record folder's `intake_r1_s4/*.sha256`, then feed `binding_packet_real_capture.json` to the exporter's pure function with FIXTURE retained rows built as the record's `LEAD_EXPORTER_r1_real_packet.txt` describes: expect `CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE` as emitted; with a fixture oracle locator, `REAL_CAPTURE_CANDIDATE_BUILT`. Confirm the full mainnet address appears in no output (grep `0x` + 40 hex; only the venue's zero hash may match) and that the capture directory is unchanged.
5. **Overclaim and honest limits.** `REAL_CAPTURE_EVIDENCE_LIMITATIONS`, `REAL_CAPTURE_NOT_A_PRODUCTION_RECORD`, the report's `production_mode`, the adapter gap report's `real_capture_packet` block: does anything read as accepted or production evidence? Are the Lead's stated limits (no schema-v10 store with the owner's events → CLI refuses at the snapshot step; D-4 and D-5 are naming checks; `PRODUCTION_MODE_UNAVAILABLE` wording now stale for the real kind) visible in the artifacts themselves?
6. **Tests and RED evidence.** Reproduce the RED arm on the pre-slice bytes (scratch copy of the worktree with `git show 4c802e9b:IBKR_PAPER_BRIDGE/tools/export_mtc_funding.py` swapped in: expect 57 failed) and at least two of the four mutants (`LEAD_MUTANT_*.txt` name the edits). For each new test say what wrong implementation it catches; name any test that could pass for the wrong reason. Judge the two changed adapter assertions (`payload_digest` label) as a disclosed correction or a silently changed fence, against the exporter's own `CANDIDATE_DIGEST_DOMAIN_CONFLATION` rule and docstring.
7. **Format-only commit.** `git diff 4c802e9b b667dbcc` must be reflow only: run `ruff format --check` on `git show 4c802e9b:…export_mtc_funding.py` (expect "would reformat") and on `git show b667dbcc:…` (expect "already formatted"); run `python -m pytest tests/test_mtc_funding_export.py` at `b667dbcc` bytes in a scratch copy (expect 161 passed). Any non-whitespace change in that diff = REQUIRED.
8. **Scope and safety.** Exactly four files; no `bridge/` runtime module; stdlib-only additions; no env read, no network, no key in either tool (prove by imports and a grep); the tool still never writes a database or copies a live one.

## Report format
Markdown with: verified identities (COMPUTED HEAD and parent, blob OIDs of the four files); the D-1..D-6 table with your own failing inputs and their outcomes; the tolerance reproduction; the r1 dry-run transcript and digests; the format-only proof; findings REQUIRED/NIT with file:line; NOT VERIFIED; final line exactly `VERDICT: PASS` / `VERDICT: PASS-WITH-NITS` / `VERDICT: REQUEST_CHANGES` / `VERDICT: BLOCK`.

Written by Claude Opus 5 Lead (session 6, `4a8233`) 2026-09-16 09:5x UTC+3, replacing the 2026-09-15 brief for `4c802e9b`; the Lead is the author of the candidate and is not a reviewer here.


## ADDENDUM 2026-09-17 14:4x UTC+3 — SECOND exact-Opus read (T0 repair round 1 of 3)
- The first read (13:55-14:23 UTC+3, `ATTEMPT1_REQUEST_CHANGES_9ef072a8/OPUS_REPORT.md`, 335 lines; it hit the launcher's turn cap right after writing its complete report) returned **REQUEST_CHANGES** on one REQUIRED finding: inside `_real_completion` the accepting `opening is None` branch (no fill for the symbol in the fills witness) takes the position from the payments' own `szi` yet emits `completion_check.position_source = "coverage.fills_witness"` — a false statement about the artifact's own evidence source, on a branch no test reached (three mutants survived the suite). Eight NITs (stale `ACCEPTED_EVIDENCE_KINDS` message; tolerance boundary docstring; stale `production_mode`/`report_kind`/`synthetic_schedule_id` labels on a real run; adapter D-1 gloss; the `"derived"` substring blacklist in D-4; the payment-stamp window needs one owner sentence; stale `UNRESOLVED:D-n` labels in the draft packet; small consistency items) are carried, not part of this commit.
- The candidate is now **`a871e429`** = `9ef072a8` + the fix (exporter: `REAL_POSITION_SOURCE_FILLS` / `REAL_POSITION_SOURCE_PAYMENTS`, the payments position resolved once before the grid walk, `REAL_CAPTURE_EVIDENCE_LIMITATIONS[3]` qualified; no refusal code or message changed; synthetic bytes untouched) + two tests (`test_d6_the_completion_check_names_the_fills_witness_as_the_position_source`, `test_d6_without_a_fill_for_the_symbol_the_position_source_is_the_payments[empty-fills-witness|another-coin-only]`). Lead evidence (disclosed builder): the reviewer's three mutants each fail exactly the new tests on the patched bytes (2/232, 3/232, 1/232); GREEN 232 passed / 1 skipped on the three modules; full Bridge suite 1669 passed / 1 skipped (ASCII basetemp); records CT13 `QUEUED_PACKAGES_20260915/P012_FUNDING_INTAKE_20260915/S4_ACCEPTING_HALF/REPAIR_R1_20260917/`.
- Decide with your OWN mutants of the patched block (in a scratch copy): (a) the no-fills branch returns `Decimal(0)`; (b) `position_source` relabelled; (c) `opening_position` always `None` — each must turn at least one of the two new tests RED and nothing else; restore → 232 passed. Re-run your §2 failing inputs and the r1 dry run (unchanged: refused at D-4). Confirm the diff touches only the two files (`git -c safe.directory=* show a871e429 --stat`) and that the synthetic candidate bytes are byte-identical (the carried fence). NITs 1-8 may be re-raised as NITs; re-raise any you judge REQUIRED. Final line `VERDICT: …` as before. Round 1 of the T0 cap 3.


## NOTE 2026-09-19 12:1x UTC+3 - AS-IS candidate lane (owner ruling OD-20260919-P012-INTAKE-ASIS-1)
This lane reads the accepting half AS IT STANDS at `a871e429` (worktree `C:/tmp/P012_INTAKE_20260915`, branch `feature/p012-funding-intake-asis-20260919` = the same commit). The NIT slice `8cbf4f1a` is a separate candidate with its own lane. Everything above this note stands for `a871e429`.
