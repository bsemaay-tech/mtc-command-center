# T0 review brief — WP-P0-29 DD-06 testnet falsification probe (script + fixture tests; ran three times on testnet (r1 aborted at the control arm, r2 INCONCLUSIVE-by-rule, r3 the agent moved its own 1 USDC)), candidate `a46b2a9db861433e1128dba21fc18362867b5d7f` (exact `claude-opus-5` xhigh; Wednesday 2026-09-16 slot, OPTIONAL lane — only if allowance remains after lanes 1-5; also the source brief for the Friday Sol lane)

You are a fresh, independent T0 reviewer. Your exact role (model and effort) is fixed by the launcher that started you; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory (scratch copies under `C:\tmp\OPUS_DD06_SCRATCH` only). The candidate is LEAD-AUTHORED (Claude Opus 5 Lead, disclosed); the Lead never accepts its own code — you are the first exact flagship read.

## Context (settled facts; verify identities, do not re-decide)
- Package WP-P0-29 (custody / venue due diligence), item DD-06 (agent-wallet withdrawal restriction, undocumented by the venue). Base `origin/master` `fcac0ac6`. Branch `feature/p029-dd06-testnet-probe-20260915`: `71cde24e` (slice 1) → `69377d6b` (slice 2: Gemini F-1/F-2 REQUIRED — withdraw3 stated as leaving the venue; refusal-text classification AUTHORIZATION/VALIDATION/UNCLASSIFIED) → `1af85067` (slice 3 after the real testnet run r1 of 2026-09-15 18:33Z aborted at the control arm: tick-valid price via the Bridge's own `round_hl_price`; `ok` envelope with `statuses[].error` = REFUSED for that request; S0 records spot USDC). Two new files: `IBKR_PAPER_BRIDGE/tools/dd06_agent_withdraw_probe.py`, `IBKR_PAPER_BRIDGE/tests/test_dd06_agent_withdraw_probe.py` (16 tests).
- Authority: `OD-20260915-P029-DD06-TESTNET-PROBE-PREP-1` (prepare) and `OD-20260915-P029-DD06-PROBE-STEPS-APPROVED-1` (execute §3 of the step packet once); r1 ran on Hyperliquid TESTNET from KVM2-P4-03 with the Bridge's AGENT key and aborted before any transfer arm; a corrected re-run r2 waits for the owner's word. Records: `QUEUED_PACKAGES_20260915/P029/DD06_TESTNET_PROBE_20260915/` (step packet, r1 console + record, slices' evidence) and `.../P029/DD06_PROBE_GEMINI/` (two voided reads + COUNTED attempt 3 PASS-WITH-NITS, tasks 1-11).
- Lead = author, never acceptor. You do NOT run the probe (no credential, no network): fixture tests + code reading only.

## Subject (read-only worktree; verify before reading anything else)
- `C:/tmp/P029_DD06_20260915`, HEAD must be `acd79b52efbbf8847621cabe1797cc6fc0567f17` (`git -c safe.directory=* -C C:/tmp/P029_DD06_20260915 rev-parse HEAD`; NO other git command — never status/diff-with-working-tree/add/commit/checkout; `git -c safe.directory=* show <rev>:<path>` and `git -c safe.directory=* diff fcac0ac6 1af85067 -- <paths>` for byte comparisons only, and say so). If HEAD differs, STOP and write `VERDICT: BLOCK` with the observed HEAD.
- Tests: from the worktree root, Bridge interpreter `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe -m pytest IBKR_PAPER_BRIDGE/tests/test_dd06_agent_withdraw_probe.py -q -p no:cacheprovider --basetemp C:/bt_opus_dd06` (expect 16 passed). Never set HL_* variables; never call the network.
- Records: `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/P029/DD06_TESTNET_PROBE_20260915/` (verify every closure against bytes, never against a report's description).

## What you must decide
1. **No mainnet path, no key leak:** `TESTNET_URL` hard-coded; `refuse_unless_testnet`; `HL_LIVE_ACK` refusal; master-key refusal; `del api_key`; redaction of 40/64-hex before any record; `write_record` refuses a surviving hex — prove each with the test that pins it and with one mutant of your own (e.g. delete the `del api_key` line and show which test fails, or show none does and file REQUIRED).
2. **Funds cannot leave silently:** `STOP_ON_NOT_REFUSED`; withdraw3 amount 6 USDC and its destination; the packet's plain statement that a non-refused withdraw3 moves test-USDC off the venue.
3. **Classification honesty:** `classify_response` (err envelope; ok envelope with per-status error; clean ok), `classify_exception`, `classify_refusal_text` — can a VALIDATION refusal ever be counted as DD-06 evidence? Show the code path that makes the run INCONCLUSIVE.
4. **Control arm:** `control_order_size` via `round_hl_price` — verify 5-significant-figure / `6 - szDecimals` bounds for real BTC mids (76974.0 → 69270.0) and that the r1 rejection (`Price must be divisible by tick size`, 69276.6) cannot recur for any mid ≥ 10 000.
5. **Fixture tests vs reality:** the r1 defect passed 14 fixture tests and two Gemini reads — say what other venue-shaped facts the fixtures still do not carry (e.g. `statuses` shapes for withdraw3/usdSend, unified-account balances) and whether any of them could make r2 misreport.
6. **Scope and suite:** exactly two files; full Bridge suite green with them (`LEAD_PYTEST_full_bridge_slice3.txt`: 1614 passed) — re-run the focused tests yourself (`python -m pytest IBKR_PAPER_BRIDGE/tests/test_dd06_agent_withdraw_probe.py -q -p no:cacheprovider --basetemp C:/bt_opus_dd06` with the Bridge interpreter `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe`).

## Report format
Markdown with: verified identities (COMPUTED HEAD, sha256 of both files); per-item table with file:line; your own mutant(s) with commands/cwd/output; findings REQUIRED/NIT; NOT VERIFIED (you did not run the probe); final line exactly `VERDICT: PASS` / `VERDICT: PASS-WITH-NITS` / `VERDICT: REQUEST_CHANGES` / `VERDICT: BLOCK`.

Written by Claude Opus 5 Lead (session 5, `03c6c8`) 2026-09-15 20:2xZ; the Lead is the author of the candidate and is not a reviewer here.


## ADDENDUM 2026-09-17 21:5x UTC+3 — SECOND exact-Opus read (T0 repair round 1 of 3) — READ THE SAFETY RULE FIRST
- **SAFETY RULE FOR THIS LANE (new, binding):** this workstation's user registry holds a live agent-wallet credential (`HL_API_WALLET_KEY`, `HL_ACCOUNT_ADDRESS`). You may mutate the tool ONLY in a scratch copy, and you may run ONLY the fixture suite against a mutant — never `main()` or any `run_probe` with real SDK objects, never with a venue URL. The fixture suite's `main()` tests now run under an `offline` fixture (tripwires in place of the credential resolver and both SDK constructors); do not remove or bypass that fixture. If any command of yours could reach a venue, do not run it and say so in NOT VERIFIED. No network call of any kind.
- The first read (18:5x-19:04 UTC+3, `ATTEMPT1_REQUEST_CHANGES_1af85067/OPUS_T0_REPORT.md`, 229 lines) returned **REQUEST_CHANGES** on four REQUIRED findings and disclosed (§0) that its mutant M8 (the `HL_LIVE_ACK` refusal removed) turned the `main()`-calling fixture test into one live TESTNET run (control order placed and cancelled; fund arms refused; nothing moved — Lead public reads confirm the venue clean). R-1 four guards unfenced (`TESTNET_URL`, the `refuse_master_key` call, `del api_key`, the write_record hex guard); R-2 the suite calls the real `main()` with nothing stubbed and one refusal keeps it offline; R-3 `"does not exist"` counted as AUTHORIZATION-class evidence; R-4 the `NOT_REFUSED` finding text claims "for an agent wallet" without reading the agent's own balance. NITs N-1..N-9.
- The candidate is now **`a46b2a9d`** = `1af85067` + the repair (Lead = disclosed builder; +349/−13, two files): `refuse_without_run_token` (`DD06_PROBE_RUN_TOKEN` must equal `--run-id`; checked after the network/HL_LIVE_ACK gate and before any credential read or SDK object); the `offline` test fixture (tripwires + cleared env) around every `main()` test; fences for the testnet host + gates in `plan_text`, the master-key call in `main` (a key deriving to the account address → exit 3, no SDK object), the write_record hex guards, the token gate; R-3 markers narrowed (`"user or api wallet"` AUTHORIZATION; `"does not exist"` VALIDATION) with two failing inputs; R-4 `_balances`/`_moved`, `S0_agent_balances`, post-arm re-reads of BOTH wallets, results `DD06_FINDING_MASTER_FUNDS_MOVED` / `DD06_FINDING_OWN_FUNDS_MOVED` / `DD06_FINDING_NOT_REFUSED` (with readings); `run_probe(..., agent_address=)`; N-1 `FUND_MOVING_ARMS` used (+ `usdClassTransfer`); N-2 KeyboardInterrupt/SystemExit re-raised. `del api_key` kept without a claim (the reviewer's reading: a gesture, not a control). Lead evidence: eight mutants each fail exactly their fence, the two removed-gate mutants hit the tripwires instead of dialling; GREEN 25 passed (16 → 25); full Bridge suite 1623 passed / 1 skipped with `HL_*` removed and an ASCII basetemp; Ruff = the 8 pre-existing findings, both files ruff-formatted; records CT13 `QUEUED_PACKAGES_20260915/P029/DD06_TESTNET_PROBE_20260915/REPAIR_R1_20260917/`.
- Decide with your OWN mutants under the safety rule above: (a) remove the token gate from `main` and run the suite — the token test must fail on the resolver tripwire, never on a network error; (b) remove the `HL_LIVE_ACK` refusal — the unit test must fail and NO test may reach the resolver (the token gate stops it first; state which line stopped it); (c) `_moved` → always False — the two attribution cases must fail; (d) `"does not exist"` back in AUTHORIZATION — the classification test must fail. Confirm the diff touches only the two files. N-3..N-9 may be re-raised as NITs; re-raise any you judge REQUIRED (N-9 arm order is the owner's). Final line `VERDICT: …` as before. Round 1 of the T0 cap 3.


## ADDENDUM 2026-09-17 22:0x UTC+3 — THIRD exact-Opus read (T0 repair round 2 of 3) — THE SAFETY RULE ABOVE STILL BINDS
- The second read (21:15-21:32 UTC+3, `ATTEMPT2_REQUEST_CHANGES_a46b2a9d/OPUS_T0_REPORT.md`, 453 lines; no incident) returned **REQUEST_CHANGES** on two REQUIRED findings: R-1 the round-1 attribution asked "changed?" not "decreased?" and consulted the master first, so replaying r3's real readings (agent 14.0→13.0, master 983.987457→984.987457) printed "DD-06 falsified on testnet"; sub-cases: an unreadable master read as "unchanged", a mark-to-market tick counted as movement. R-2 a fund-moving arm ending in ERROR was neither measured nor stopped. NITs N-3..N-9 re-raised, N-10 (`len(digits) <= 5` is not an invariant of `control_order_size`), N-11 (the dry-run test lacked the offline fixture), N-12 (this lane's agent brief still pinned `1af85067`).
- The candidate is now **`2128352b`** = `a46b2a9d` + round 2 (Lead = disclosed builder; +262/−82): `_paid(before, after, amount)` — funds LEFT a wallet only on a readable DECREASE of at least half `ARM_AMOUNT_USDC[name]`; `_attribute` — MASTER_FUNDS_MOVED needs a measured master decrease, OWN_FUNDS_MOVED the agent's, else NOT_REFUSED with the readings; an unreadable side is written UNREADABLE, never unchanged; `_reread_both_wallets` shared by the NOT_REFUSED and the new ERROR branch (`DD06_INCONCLUSIVE_FUND_ARM_ERROR`, later arms `SKIPPED_AFTER_ERROR`); N-11 applied; N-12 fixed in `opus/BRIEF.md`; N-8 fixed in this brief's Subject. Lead evidence: eleven mutants each fail their fence (the two removed-gate mutants still hit the tripwires); GREEN 28 (25→28); full Bridge suite 1626 passed / 1 skipped; Ruff 7 (all pre-existing); records CT13 `QUEUED_PACKAGES_20260915/P029/DD06_TESTNET_PROBE_20260915/REPAIR_R2_20260917/`.
- Decide with your OWN mutants under the safety rule: (a) `_paid` back to "changed" — the r3-replay case must fail; (b) `None` rendered as "did not pay" — the unreadable case must fail; (c) the ERROR branch removed — the transport-error test must fail; (d) confirm `_paid`'s threshold arithmetic (half the arm amount; withdraw3 6 USDC → 3; usdSend/spotSend 1 → 0.5) and that a master INCREASE can never produce the breach label. Re-raise N-3..N-7, N-9, N-10 as NITs or REQUIRED per your reading. Final line `VERDICT: …` as before. Round 2 of the T0 cap 3 — a third REQUEST_CHANGES ends the Lead's repair authority on this candidate (owner's call then).

## ADDENDUM 2026-09-18 09:0x UTC+3 — FOURTH exact-Opus read (T0 repair round 3 = the ONE extra round the owner authorized, `OD-20260918-DD06-CAP-A-1`) — THE SAFETY RULE ABOVE STILL BINDS
- The third read (attempt 4, 23:00-23:17 UTC+3 on 09-17, `ATTEMPT4_REQUEST_CHANGES_2128352b/OPUS_T0_REPORT.md`, 423 lines; no incident) returned **REQUEST_CHANGES** on one REQUIRED finding, R-1: `_paid` counted a key as readable per key; the unified account keeps every dollar in spot USDC while its perp `accountValue` is structurally `0.0` and always parses, so a failed SPOT read after a fund-moving arm still counted as "measured" — the record said "account did not pay" with no UNREADABLE marker and the OWN branch printed "not the DD-06 breach" (sub-case R-1(b): the exclusion also printed with the account wholly unreadable). The Lead's own unit test pinned that partial case as `False`. New NITs N-13 (a drift larger than half the arm amount counts as a payment), N-14 (single post-arm snapshot), N-15 (the ERROR finding embeds `_attribute`'s sentence), N-16 (`ARM_AMOUNT_USDC` unpinned); N-11/N-12 closed; N-3..N-10 carried. The T0 cap (3) was reached; the owner authorized ONE extra round.
- The candidate is now **`acd79b52`** = `2128352b` + round 3 (Lead = disclosed builder; +94/−18, two files): `_paid` returns True on any readable DECREASE of at least half the arm amount (a measured decrease is a fact beside an unreadable key), False ONLY when every key parsed on both sides and none decreased, None when any key was unreadable/unparsable and no readable key decreased, None for an empty reading; `_attribute` draws the r3 exclusion ("not the DD-06 breach") only from a completely read, unchanged account (`master_paid is False`) — an agent decrease with `master_paid is None` is the new label `DD06_FINDING_OWN_FUNDS_MOVED_MASTER_UNREADABLE` (the agent's decrease reported as a fact, nothing concluded about the master, "re-read the account by hand"); the NOT_REFUSED text names an unmeasured wallet ("NOT measured (UNREADABLE): re-read it by hand"). Module docstring updated. Tests: three new parametrized cases (the reviewer's shape spot-None/perp-0.0 → MASTER_UNREADABLE and never "did not pay"; a measured spot decrease with the perp unreadable is still the breach; nothing moves with the spot unreadable → NOT_REFUSED naming the unmeasured wallet), the whole-unreadable case now expects MASTER_UNREADABLE with no exclusion, the `_paid` unit pins partial → None, the decoy shape → None, a readable decrease beside an unreadable key → True, "n/a" → None, {} → None. Lead evidence (`P029/DD06_TESTNET_PROBE_20260915/REPAIR_R3_20260918/`): the third reader's own probe file (asserting the old behaviour) fails 3/3 on the patched code; readable-per-key restored → 4 failed; MASTER_UNREADABLE branch removed → 2 failed; unparsable counted as readable → 1 failed; unmeasured note removed → 1 failed; GREEN 31; full Bridge 1629/1; ruff 7 (pre-existing); guard PASS; Gemini `DD06_R3_GEMINI` (see the queue log for its verdict).
- Decide with your OWN mutants under the safety rule (scratch copies, fixture suite only, never `main()`/real SDK): (a) `_paid` back to readable-per-key — the spot-unreadable cases and the `_paid` unit must fail; (b) the MASTER_UNREADABLE branch removed — the two unreadable-master cases must fail on "not the DD-06 breach"; (c) confirm the decoy shape (before `{"accountValue": "0.0", "usdc_total": "983.987457"}`, after `{"accountValue": "0.0", "usdc_total": None}`) yields None for any amount; (d) confirm the complete-reading semantics of round 2 are unchanged (r3 replay → OWN, master decrease → MASTER, tick → NOT_REFUSED). Re-raise N-3..N-10, N-13..N-16 as NITs or REQUIRED per your reading (N-9 arm order is the owner's). Final line `VERDICT: …` as before. This is the owner-authorized extra round: a REQUEST_CHANGES here goes back to the owner, not to a Lead repair.
