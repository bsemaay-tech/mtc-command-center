# T0 review brief — WP-P0-12 Path 1 read-only own-account evidence capture tool, candidate `af921d75226932b11c5f0279b2cbf0b0de5be9e0` (exact `claude-opus-5` xhigh; Wednesday 2026-09-16 slot)

You are a fresh, independent T0 reviewer. Your exact role is fixed by the launcher; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory (scratch copies under the scratch dir your launcher names). **You must not make any network call**: the tool's tests are fully offline (monkeypatched SDK); the real capture already exists as a record and is NOT to be re-run.

## Context (settled; verify identities, do not re-decide)
- WP-P0-12 production admission needs REAL own-account fee/funding evidence (owner answers `OD-20260914-P012-ADMISSION-Q1..Q7`; Q1 = the owner self-attests his own account; Q3 "Wait" = no fee tier declared yet). "Path 1" = the owner's own Hyperliquid mainnet account: on 2026-09-14 he made three declared BTC fills plus a duplicate (16:03-16:14Z), held 0.00058 BTC through the 17:00Z and 18:00Z funding settlements and closed at 18:08Z — his hands; the Lead only watched. The commissioning promise is `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/P012_PATH1_REAL_CAPTURE_PACKET_20260914.md` section 5; the intake rules are `…/P012_PATH1_CAPTURE_TOOL_REVIEW_20260915/`'s brief sources (`REAL_OBSERVATION_INTAKE.md`, half-open interval rule at line 31).
- Commits on `feature/p012-path1-capture-20260914` (worktree `C:/tmp/P1CAP_20260914`, base `fcac0ac6` = master after PR #191): **`e77af1c8`** candidate (Codex gpt-5.5 lane P1CAP; three new files: `IBKR_PAPER_BRIDGE/tools/capture_own_account_evidence.py`, `IBKR_PAPER_BRIDGE/tools/path1_sign_ownership.html`, `IBKR_PAPER_BRIDGE/tests/test_capture_own_account_evidence.py`); **`af921d75`** correction P1FIX — **LEAD-AUTHORED** (owner `OD-20260915-P012-P1FIX-LEAD-1` "A", every Codex route capped; disclosed): Gemini F-1 (`--run-id` required with a signature; signature verified before any network object or file), F-2 (half-open `[start, end)` window with range refusals and manifest semantics), F-4 coin in the funding identity, F-5 exclusive create, F-6 error response bytes kept as `<name>_ERROR.json`, F-7 `raw_bytes_source` provenance, F-8 `CapturingInfo.post` unit test, F-9 multi-page + wrong-signature arms, F-10 lint/format, F-11 content-level re-query; Lead L-1..L-6 folded in.
- Reviews so far: Gemini 3.8 detection of `e77af1c8` REQUEST_CHANGES (F-1, F-2 REQUIRED); Gemini 3.8 delta of `af921d75` PASS-WITH-NITS (all F/L RESOLVED; one NIT on two RED arms failing pre-fix by detail assertion only). Lead evidence on Python 3.12: focused 21 passed; RED on the pre-fix tool 12 failed / 9 passed; full Bridge suite 1619 + 30 passed, 0 failed; Ruff/format clean; guard PASS. Records: `…/P012_PATH1_CAPTURE_CANDIDATE_20260914/`, `…/P012_PATH1_CAPTURE_TOOL_REVIEW_20260915/`, `…/P012_PATH1_CAPTURE_TOOL_FIX_20260915/`.
- **A real capture with this exact tool exists** (`…/P012_PATH1_REAL_CAPTURE_20260915/`, run `p012-path1-20260914T1500Z-1900Z-r1`, 2026-09-15 07:15Z): five read-only Info calls, ownership VERIFIED against the owner's `personal_sign` signature, both query passes byte-identical, derived view matching the declared fills and settlements (net +0.2839 USDC). Use it as fixture evidence of what the tool produces (open the manifest, the sidecars, the derived view); do not re-run any capture. **You are the first exact flagship on this tool; exact Sol follows after the Codex reset.**

## Subject (read-only worktree; verify before reading anything else)
- `C:/tmp/P1CAP_20260914`, HEAD must be `af921d75226932b11c5f0279b2cbf0b0de5be9e0` (`git -c safe.directory=* -C C:/tmp/P1CAP_20260914 rev-parse HEAD`; no other git command except `git -c safe.directory=* show <rev>:<path>`).
- Interpreter with the Bridge dependencies (SDK 0.24.0, eth_account): `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe` (3.12.12). Tests from `C:/tmp/P1CAP_20260914/IBKR_PAPER_BRIDGE`: `python -m pytest tests/test_capture_own_account_evidence.py -q -p no:cacheprovider --basetemp <dir under your user temp>` (expect 21 passed). Run the tests with `HL_API_WALLET_KEY` removed from your process environment (the tool refuses if it is visible; never print any environment variable value).
- SDK reference (read-only): `C:/tmp/P012_FUNDING_PY312_20260911/Lib/site-packages/hyperliquid/{info,api}.py` (`user_fills_by_time`, `user_funding_history`, `user_state`, `API.post`, `_handle_exception`); the accepted broker's usage of the same calls: `IBKR_PAPER_BRIDGE/bridge/broker/hyperliquid.py` (`HL_FILLS_PAGE_LIMIT` 2000, `HL_INFO_PAGE_LIMIT` 500, mainnet/testnet URL selection).

## What you must decide (T0 — this tool produces evidence the owner's own money-account admission would rest on)
1. **Read-only by construction:** no `hyperliquid.exchange` import, no signing of anything but the offline ownership check, no path that could place/cancel/transfer; key refusal is the first statement; `make_info` builds only an `Info` subclass. Any way to reach a write endpoint = REQUIRED.
2. **Bytes are original:** `CapturingInfo.post` vs `API.post` line by line (URL, session, json, timeout, capture before `_handle_exception`, before JSON parsing); the manifest's request body equals the posted payload; `raw_bytes_source` tells the truth; the real capture's page files hash to their sidecars and manifest entries (verify with the interpreter, read-only).
3. **Window and pagination:** half-open `[start, end)` per the intake vs the API's inclusive `endTime`: walk `paged_query` for rows at `start`, `end-1`, `end`, `end+1`, before `start`, and across a full page whose last rows share a millisecond with the next page; the cursor restart, dedup by identity, `page_max` advancing past an excluded `end` row, the stop condition counting raw rows, `CAPTURE_REFUSED_TRUNCATED` on a stalled cursor. Design RED arms in a scratch copy for anything the tests do not cover.
4. **Identities and re-query:** `fill_identity` (`tid`, else hash+oid+time) collision analysis; `funding_identity` with coin; the re-query compares `{identity: row}` content; two passes byte-identical in the real capture — say what a divergence would look like and that it refuses.
5. **Ownership evidence:** `ownership_message` text vs the HTML page character by character; EIP-191 recovery via `eth_account`; `--run-id` mandatory with a signature; verification before any network object or file (prove with the tests: `fill_calls == 0`, out dir absent); NOT_PROVIDED recorded, never invented; the real capture's `ownership_evidence` VERIFIED for the owner's address.
6. **Write discipline and errors:** exclusive create for every file and sidecar; `verify_sidecars`; `<name>_ERROR.json` path on failures; nothing deleted; nothing written outside `--out`.
7. **D026 evidence:** for each of F-1..F-11 / L-1..L-6, the test that is RED on `e77af1c8` for the RIGHT reason (the Lead's scratch-tree run: 12 failed / 9 passed; Gemini noted two fail pre-fix only by detail assertion) — reproduce the RED run yourself (`git show e77af1c8:…` into a scratch tree with the new tests and a minimal conftest).
8. **Lead authorship of `af921d75`:** the Lead wrote the correction under the owner's ruling and does not accept its own code; say whether anything in it is beyond the eleven findings and whether your verdict needs anything beyond your and Sol's review.
9. **Scope:** exactly three files in the branch; nothing else in the Bridge touched (diff `fcac0ac6..af921d75 --stat`); no protected scope.

## Report format
Markdown with: verified identities (COMPUTED: HEAD, sha256 of the three files, sha256 of the real-capture manifest and page files vs their sidecars); promise-conformance table (packet section 5 promise | file:line | EQUAL/DIFFERENT); F/L disposition table with the RED-on-pre-fix judgement per test; your own RED arms with commands, cwd and exact output; the window walk table; findings numbered with severity REQUIRED / NIT and exact file:line; NOT VERIFIED (non-empty: no network call by you; the real API's pagination behaviour beyond one page is unobserved); final line exactly `VERDICT: PASS` or `VERDICT: PASS-WITH-NITS` or `VERDICT: REQUEST_CHANGES` or `VERDICT: BLOCK` (BLOCK if you could not execute the mandated commands). Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. Your report alone accepts nothing; the Lead assembles the roster.
