# LEAD_ADJUDICATION - lane 7 (DD-06 testnet probe), fifth exact-Opus read of `a46da0aa` (attempt 6, 11:08-11:26 UTC+3, 2026-09-18) = round 4 (`OD-20260918-DD06-R4-A-1` + order B `OD-20260918-DD06-ORDER-B-1`)

**Verdict as written:** **PASS-WITH-NITS, 0 REQUIRED.** 420 lines, 128 assistant turns, launcher exit 0, no cap, no incident (section 7: no credential read, `main()` only through the `offline` fixture, mutants on scratch, network-shape scan clean).
**What the reviewer established (its own work, not the Lead's claims):** HEAD `a46da0aa`; scope exactly two files, `+92 -29`; the four authorized checks - (a) INCONCLUSIVE dropped from the condition -> the unclassifiable test fails; (b) the stop removed -> both undetermined-outcome tests fail; (c) withdraw3 back to first -> the order-pinning tests fail; (d) an INCONCLUSIVE on `approveAgent` still ends in the ordinary end-of-run `DD06_INCONCLUSIVE` handling, and `_paid` / `_attribute` / the gates / the `offline` fixture are byte-identical to `acd79b52` (round 3 untouched, still fenced under the reviewer's own mutants). Arm order B applied and pinned for the default configuration, verified by the reviewer for the full one; "funds cannot leave silently" strictly stronger; N-9 closed. Gemini `DD06_R4_GEMINI` PASS on the same bytes (1 NIT).
**Standing:** DD-06 probe candidate `a46da0aa` = exact-Opus PASS-WITH-NITS + Gemini PASS. Next: Sol (Sat 2026-09-19) on these bytes under the BINDING SAFETY RULE; nothing merges before both flagships; the probe runs again only on a separate owner word; the Lead never accepts its own code.

## Execution blockers named by the reviewer (documents, not candidate defects) - the Lead's disposition
- **N-22** the owner-approved step packet (`DD06_TESTNET_PROBE_STEP_PACKET_20260915.md:22`) lists the pre-order-B arm sequence, and neither it nor the newest runner (`kvm2_dd06_probe_run_r3.sh:15`) sets `DD06_PROBE_RUN_TOKEN` (the token gate was added in round 1 after they were written) - run as written the probe exits 3 (`PROBE_REFUSED: DD06_PROBE_RUN_TOKEN must equal the run id`). **Must be fixed before any r4 run:** an addendum row to the packet (order B; `DD06_PROBE_RUN_TOKEN="$RUN_ID"` in the runner). Filed as the first task of the r4 preparation; NOT done now (the probe's next execution needs the owner's word; the documents will be re-issued with the addendum when that word comes, so the owner approves the current sequence, not the stale one).
- **N-25** the brief's addendum cites `P029/DD06_TESTNET_PROBE_20260915/REPAIR_R4_20260918/` in CT13, which was not yet pushed when the reviewer looked (the round-4 records were written 10:5x and CT13 wave 22 predates them). Copied in the next wave. The reviewer re-derived every code claim itself, so its verdict does not rest on that path.

## NITs (carried into the lane record; the Lead builds none without a slice)
Re-raised: N-3, N-4, N-5, N-6, N-7, N-8 (residue), N-10, N-12 (`opus/BRIEF.md:3` still names the old candidate hash in one place - record fix), N-13, N-14, N-15, N-16, N-17; new: N-19/N-26 (every abort path discards the whole record, including after a fund arm that was not refused - the reviewer's first pick), N-20 (a zero threshold makes an unchanged wallet "pay"; `ARM_AMOUNT_USDC` unpinned - with N-16 the reviewer's second pick), N-21, N-23, N-24. The DD-06 NIT slice (after Sol) should take N-16/N-20 and N-19/N-26 first.

## NOT VERIFIED by the reviewer (as stated)
The probe never run; full Bridge suite / ruff / guard / Gemini from the Lead's records; mainnet parity not claimed; DD-06 stays BLOCK (whether an agent wallet can withdraw from a FUNDED perp balance is still not established - the open venue question).

Recorded by Claude Opus 5 Lead (session 6, `4a8233`); machine stamps in the queue log.
