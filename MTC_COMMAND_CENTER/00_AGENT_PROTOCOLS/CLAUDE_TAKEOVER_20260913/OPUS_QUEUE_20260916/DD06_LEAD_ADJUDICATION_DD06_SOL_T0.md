# LEAD_ADJUDICATION - exact-Sol T0 read of the DD-06 testnet probe `a46da0aa` (Codex PRO `free`, gpt-5.6-sol xhigh; Sat 2026-09-19 14:50-15:04 UTC+3; adjudicated 15:1x)

**Verdict as written:** **REQUEST_CHANGES** - two REQUIRED (R-1, R-2), four NITs (N-1..N-4). 105 lines, 14 min, exit 0; worktree clean after the lane. Identities computed by the reviewer match the pin (HEAD `a46da0aa`; scope = exactly the tool and its test file since `fcac0ac6`). **The SAFETY RULE held**: "The real probe was not run. No credential, registry, SDK construction, network, testnet, or mainnet action was attempted." The reviewer read no Opus/Gemini/Lead report and worked from fixture-suite scratch runs only.

## Lead grep-verification (blob `a46da0aa:IBKR_PAPER_BRIDGE/tools/dd06_agent_withdraw_probe.py`)
- **R-1 CONFIRMED by reading.** `AUTHORIZATION_MARKERS` (`:212-220`) contains context-free tokens `"not allowed"` and `"agent"`; `classify_refusal_text` (`:244-256`) lets authorization markers win over validation markers; `:677-681` increments `authorization_refusals` for every `refusal_class == "AUTHORIZATION"`; `:738-747` emits `DD06_REFUSALS_OBSERVED` when every arm was refused with that class. So a venue VALIDATION message that merely contains "agent" or "not allowed" (the reviewer produced two such counterexamples in scratch) is counted as DD-06 authorization evidence. This is the fifth-read R-3 family (markers narrowed once already) with a residual: the remaining tokens are still context-free. Required repair (reviewer's wording, Lead agrees): signer-specific phrases or structure; ambiguous overlap → `VALIDATION`/`UNCLASSIFIED` (conservative); negative tests for the two phrases plus a whole-run case ending `DD06_INCONCLUSIVE`.
- **R-2 CONFIRMED by reading.** The three S0 identity blocks (`:490-541`: `extra_agents`, `user_state`, `spot_user_state`) `except BaseException` WITHOUT the `except (KeyboardInterrupt, SystemExit): raise` guard that `_attempt` (`:307-308`) and `_balances` carry; an operator abort during S0 is recorded as a step and the run proceeds to the signed arms. Required repair: re-raise before each broad S0/control-plan catch; fence with a test asserting no exchange call after an S0 abort.
- N-1 `del api_key` gesture unpinned; N-2 one snapshot + half-amount threshold can misattribute unrelated drift (the round-2/3 design; known trade-off, recorded); N-3 ERROR/INCONCLUSIVE findings embed "was NOT refused" text; N-4 non-fund `approveAgent` INCONCLUSIVE has no finding text (= Gemini R4 NIT). All plausible on reading; carried.

## Reproduction status
Not reproduced by the Lead today (the P0-12 close took the day; DD-06 mutants must run under the SAFETY RULE on a scratch copy with the offline fixture). Both findings are confirmed by direct reading of the cited lines, which is sufficient to record REQUEST_CHANGES; the reproduction belongs to the repair session.

## Standing - OWNER WORD NEEDED
DD-06 `a46da0aa`: exact-Opus PASS-WITH-NITS (fifth read) + Gemini PASS, but **exact-Sol REQUEST_CHANGES** → not merge-ready and not ready for the r4 probe run. **The T0 repair cap is reached** (rounds 1-4 + the owner's extra round `DD06-CAP A`), so the Lead may not repair. Owner one-liner: **`DD06-R5 A`** = authorize ONE repair round for R-1 + R-2 (two small, test-first changes; done by the DD-06 package session on Opus 5 under the SAFETY RULE, then Gemini delta + exact-Opus + exact-Sol re-reads on the final bytes; recommended) or **`DD06-R5 B`** = park DD-06 as it stands (no merge, no probe run until later). N-22 execution-document blockers (step packet, runner) remain for any r4 run regardless.

Recorded by Claude Fable 5.1 Lead (session 7, `18c1b8`).
