# CODEX-NEUTRAL VARIANT of `REVIEW_BRIEF.md` for the exact-Sol reviewer (2026-09-19 14:5x). Same candidate, same mandated commands, same acceptance rules; only vocabulary differs (regression-test quality review of the owner's own repository: does each declared refusal actually get exercised by a test?). Report facts as file, field, before value, after value, observed result - no runnable modification recipes. Write the full report to the lane file; keep the final chat message to one neutral sentence.

# T0 review brief — WP-P0-30 Shape-B archive exporter, candidate `45a7f50eac009646bde1d80bcf66b1d14e6d72e0` (re-pinned 2026-09-16 15:2x UTC+3 by the session-6 Lead after the T0 repair round 1; previously `153edee9`, re-pinned 2026-09-15 night by the session-5 Lead after the Lead-built O9FIX commit `153edee9` closed the Gemini NITs K-01..K-05 under owner `OD-20260915-BUILD-ABC-1` item a; the candidate is now daf6a43b + O9FIX; the Lead is the author of the O9FIX delta and does not accept it; records `laneO9FIX_build/DISPOSITION_O9FIX.md`) (exact `claude-opus-5` xhigh; Wednesday 2026-09-16 slot)

You are a fresh, independent T0 reviewer. Your exact role is fixed by the launcher; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory (scratch copies for expected-failure runs under the scratch dir your launcher names).

## Context (settled; verify identities, do not re-decide)
- WP-P0-30 = the market-data collector / archive line at the repository root of the worktree `C:/tmp/P030_INTEGRATION_20260913` (branch `feature/p030-integrated-20260913`, base `f42fd540`). The owner chose **Shape B** for the producer/exporter bridge (`OD-20260914-P030-BRIDGE-SHAPE-1`; design note `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/P030_BRIDGE_DESIGN_CHOICE_20260914.md`: the collector keeps writing its raw partitions; a separate EXPORTER re-identifies rows through `p030_market_data_contracts` and writes canonical, contract-shaped JSONL that `p030_closed_partition_backup_adapter` can consume; the D-13 drill's GREEN half) and authorized the build (`OD-20260914-P030-EXPORTER-GO-1`).
- Candidate **`153edee9`** = `daf6a43b` (Codex gpt-5.5 lane O9EXP, Lead-committed) + the O9FIX commit (Lead-built, disclosed: K-01 overflow wrapped, K-02 slice-level `ContractRefused` wrapped, K-03 whole-second `exported_at_utc`, K-04 16/16 refusal-code tests, K-05 citation corrected in the disposition; review the O9FIX delta with the same rigour as the base — it is Lead-authored): two NEW files at the worktree root — `p030_archive_exporter.py` (323 lines: `export_partition(source_jsonl, target_jsonl, *, source_root) -> ExportReceipt`; single byte read; strict JSON decode with duplicate-key and non-finite refusals; re-identification through the contracts module; canonical sorted-key LF JSONL; exclusive-create target with byte re-read; receipt sidecar; `ExportRefused.code`) and `check_p030_archive_exporter.py` (263 lines: refusal cases + the D-13 expected-failure→GREEN pair). `market_data_collector.py` untouched.
- Reviews so far: Gemini 3.8 read-only design-conformance review PASS-WITH-NITS (K-01 non-finite float inside a line must surface as `ExportRefused("source_line_invalid")` not a raw `ValueError`; K-02 contract refusals must surface as `ExportRefused("contract_refused")`; K-03 `_utc_z` must refuse sub-second timestamps like the adapter; K-04 11 of 16 refusal codes have no negative test; K-05 a report citation) — the correction O9FIX was then BUILT by the Lead (disclosed; Codex capped) as commit `153edee9` and closes K-01..K-05 per `laneO9FIX_build/DISPOSITION_O9FIX.md`; the Gemini 3.8 DELTA review of `daf6a43b..153edee9` (2026-09-15 16:04-16:15Z) read PASS-WITH-NITS by content — K-01..K-05 CLOSED, 16/16 refusal codes with named negative tests — but was VOIDED by the CLI 503 envelope, so it is SUPPLEMENTAL (`P012_S16_REVIEWS_20260913/P030_O9FIX_GEMINI/LEAD_ADJUDICATION.md`); its two NITs are OPEN and known: K-06 `check_p030_archive_exporter.py:362` uses one alternation regex `non-finite|not parseable` across the overflow/NaN arms (code assertion exact, message check loose); K-07 the lane `SHA256SUMS.txt` hashed the CRLF checkout of the checker while the blob is LF (blob authoritative). You may ask for K-06 as a change; nothing else is expected. Lead verification on the pinned Python 3.12.12: `check_p030_archive_exporter.py` OK + `D-13 expected-failure RAW REFUSAL: prefix record is not canonical JSONL` / `D-13 GREEN EXPORTED CAPTURE: PASS`; `check_market_data_collector.py` PASS (NETWORK ATTEMPTS 0); `check_p030_market_data_contracts.py` PASS; `check_p030_closed_partition_backup_adapter.py` OK; Ruff clean; guard PASS. Records: `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/P030_EXPORTER_CANDIDATE_20260914/` (lane report, Lead note, evidence outputs, Gemini review + adjudication, the O9FIX brief). **You are the first exact flagship on this candidate; exact Sol follows after the Codex reset.**

## Subject (read-only worktree; verify before reading anything else)
- `C:/tmp/P030_INTEGRATION_20260913`, HEAD must be `d426e79f4c7813c6cdf40b6a1049ced6a58a5de5` (`git -c safe.directory=* -C C:/tmp/P030_INTEGRATION_20260913 rev-parse HEAD`; no other git command except `git -c safe.directory=* show <rev>:<path>`).
- Files: `p030_archive_exporter.py`, `check_p030_archive_exporter.py` (the candidate); `p030_market_data_contracts.py`, `p030_closed_partition_backup_adapter.py`, `market_data_collector.py` and their `check_*.py` (the consumers/producers the exporter must agree with; unchanged).
- Pinned interpreter: `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (3.12.12). Run each `check_*.py` from the worktree root and paste the output.

## What you must decide (T0 — this exporter produces the archive rows a later production adapter would trust)
1. **Design conformance to Shape B:** the exporter never mutates or re-reads the source partition after its single byte read; re-identification goes through the contracts module (no home-grown ids); output is canonical (sorted keys, LF, one row per line, exclusive create, byte re-read equals what was written); the receipt binds source bytes digest → target bytes digest → row counts; the adapter's rules (whole-second UTC `Z` timestamps, canonical JSONL, id domains) are what the exporter enforces — cite the adapter/contract lines.
2. **Refusal completeness (refusing by default):** for EVERY `ExportRefused` code, the input that triggers it and whether a negative test exists (Gemini counted 11 of 16 missing at `daf6a43b`; at `153edee9` the Lead-built `ArchiveExporterRefusalCodeTests` claims 16 of 16 — reproduce BOTH counts from the bytes, do not take either from the records); any path where a malformed source line, a duplicate key, a non-finite number, a contract refusal, a wrong timestamp precision, a pre-existing target, or a partial write could surface as a raw exception or, worse, as a silently exported row. K-01..K-03 are known and OPEN: confirm or refute each with a expected-failure check in a scratch copy (`1e999` row, `NaN` literal, a contracts-refused row, a sub-second timestamp) and state whether any of them can produce an EXPORTED row (that would be REQUIRED); a raw exception instead of `ExportRefused` is a NIT unless the exception path leaves a partial target.
3. **D-13 drill expected-failure/GREEN:** `check_p030_archive_exporter.py` — the expected-failure half must be the real adapter refusing a raw collector partition (`prefix record is not canonical JSONL`), the GREEN half the adapter accepting the exported partition; verify the check really goes through the adapter (not a re-implementation) and that GREEN depends on the export (mutate the exported bytes in a scratch → adapter must refuse).
4. **Boundary:** no network, no subprocess, no writes outside the target path + receipt; the collector is untouched (diff `f42fd540..153edee9`; the O9FIX commit touches exactly `p030_archive_exporter.py` and `check_p030_archive_exporter.py`).
5. **Report honesty:** the lane report and Lead note vs the bytes; counts reproduced.

## Report format
Markdown with: verified identities (COMPUTED: HEAD, sha256 of both candidate files); conformance table (Shape-B promise | file:line | EQUAL/DIFFERENT); refusal-code table (code | trigger | test present? | your check result); D-13 evidence with commands, cwd and exact output; findings numbered with severity REQUIRED / NIT and exact file:line (mark K-01..K-05 as CONFIRMED / REFUTED / EXTENDED); NOT VERIFIED (non-empty: no real archive, no host); final line exactly `VERDICT: PASS` or `VERDICT: PASS-WITH-NITS` or `VERDICT: REQUEST_CHANGES` or `VERDICT: BLOCK`. Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. Your report alone accepts nothing; the Lead assembles the roster.

## ADDENDUM 2026-09-16 15:2x UTC+3 — this is the SECOND exact-Opus read (repair round 1 of 3)
- The first read (2026-09-16 11:21-12:07Z, `ATTEMPT1_REQUEST_CHANGES_153edee9/OPUS_T0_REPORT.md`) returned **REQUEST_CHANGES** with two REQUIRED findings: (1) a refusal after the target existed left a consumable/truncated partition that the backup adapter accepted; (2) `source_root` containment accepting an input it should refuse (`..` parent-directory (`..`) path component, junction/symlink components). Plus NITs 3-8.
- The candidate is now **`45a7f50e`** = `153edee9` → `53b43c21` (format-only: `ruff format` reflow of the two files, verifiable with `ruff format --check`; the old 20 tests pass at those bytes) → `0be0a8aa` (the repair) → `45a7f50e` (parity tweak from the Gemini delta review: percent-escaped source segments refused like the adapter, one test; Lead-built as the same implementer, disclosed; commit message lists the closures and the evidence; records under `CT13 QUEUED_PACKAGES_20260915/P030_O9FIX_20260915/REPAIR_R1_20260916/`).
- Read the semantic change with `git -c safe.directory=* show 0be0a8aa` and `git -c safe.directory=* show 45a7f50e` (the repair and the parity tweak, each commit's own diff; read-only) and, if you want the reflow-only proof, `git -c safe.directory=* show 53b43c21` — the git rules of your launch brief stand (no status/diff-against-working-tree/add/commit/checkout). Decide, with your OWN checks, whether findings 1 and 2 are CLOSED: (1) reproduce the mid-write ENOSPC check (half write then `OSError(28)`) and a receipt-write refusal and a verify refusal — after each, NOTHING may exist under the target name, the leftover must sit under `<target>.p030partial`, and a second run must refuse `target_exists` on it; also confirm the publish order (receipt first, then target) and that `os.replace` is the only publication path (no delete code path anywhere); (2) reproduce the `..` parent-directory (`..`) path component and the NTFS junction check — both must refuse `source_outside_root` and the adapter must refuse the same path; add a symlink component if your host allows it; confirm the resolved-form check. Then re-run the checker (expect `Ran 26 tests ... OK`) and the sibling P0-30 checkers, and confirm the expected-failure runs the Lead recorded (7/25 on the pre-repair exporter).
- Judge NITs 3 (K-06 split), 5 (LF-normalised `exporter_sha256`), 7 (tightened unwritable tests) as applied or not; NITs 4, 6, 8 were documentary/out-of-scope and are not part of this commit.
- Everything else in this brief stands. Final line `VERDICT: PASS` / `PASS-WITH-NITS` / `REQUEST_CHANGES` / `BLOCK` as before.

## ADDENDUM 2026-09-18 11:3x UTC+3 — THIRD exact-Opus read: the NIT slice (not a repair round; 0 REQUIRED stood at `45a7f50e`)
- The second read (attempt 3, 2026-09-17 09:07-09:22 UTC+3, `ATTEMPT3_PASS_WITH_NITS_45a7f50e/OPUS_T0_REPORT.md`) returned **PASS-WITH-NITS, 0 REQUIRED** with NITs N1-N5 (`LEAD_ADJUDICATION_P030_R1_T0.md`); Gemini `P030_R1` PASS-WITH-NITS. The owner chose to build the NIT slices BEFORE the Sol read so both flagships judge the final bytes on 2026-09-19.
- The candidate is now **`d426e79f`** = `45a7f50e` + the NIT slice (Lead = disclosed builder; +157/−18, FOUR files: `p030_archive_exporter.py`, `check_p030_archive_exporter.py`, `p030_closed_partition_backup_adapter.py`, `check_p030_closed_partition_backup_adapter.py`): **N1** `_publish()` — Windows `os.rename` refuses an existing destination (no-clobber, nothing unlinked), POSIX keeps `os.replace` behind an exists-check with the residual window DOCUMENTED not closed; a destination that appeared is refused `target_exists` / `receipt_exists` naming the staging file; **N2** the staging comment claims only what the bytes support; **N3** the D-13 GREEN arm binds the adapter's capture to the receipt (`prefix_sha256 == exported_sha256 == sha256(published bytes)`); **N4** a line nested deeper than the recursion limit is refused `source_line_invalid` — on this interpreter `json.loads` accepts 3000 levels and the recursive non-finite walk is where the error arises, so the walk is guarded (parser clause = defence in depth) in the exporter and, for parity, at both parse sites of the adapter; **N5/F8** the adapter checker's config anchor uses `ensure_ascii=False` like the writer (expected-failure at HEAD under the long-form non-ASCII TEMP `0 != 1`, GREEN after). Adapter files carry only the semantic lines (not reformatted). Lead evidence: `P030_NIT_SLICE_20260918/LEAD_VERIFICATION_P030_NIT.md` (modified copies: publish by `os.replace` → 2 failed; receipt hash over other bytes → the D-13 binding fails; exporter walk guard removed → 1 error; adapter walk guard removed → 1 error; a first parser-clause modified copy SURVIVED and is recorded as superseded; exporter checker 28 OK, adapter checker 44 OK under both TEMPs; ruff findings per file identical to HEAD by rule; guard PASS); Gemini `P030_NIT_GEMINI` (see the queue log).
- Decide with your OWN modified copies (scratch copies; the checkers are plain unittest files run with `python <checker>.py` from the worktree root, ASCII `TEMP` for the sibling — its F8 arm needs the long-form non-ASCII `TEMP` to show expected-failure at `45a7f50e`): (a) `_publish` back to `os.replace` on Windows — the race test must fail; (b) the D-13 binding assertion removed — a receipt hash computed over other bytes must pass again (state which test still catches it); (c) either walk guard removed — the nesting arm must error; (d) confirm the refusal codes, staging/verify/publish order, receipt fields and `exporter_sha256` LF-form, and the adapter's canonical-prefix rules are unchanged from `45a7f50e`; (e) say whether the POSIX residual window is honestly documented or over-claimed. Re-raise anything you judge REQUIRED. Final line `VERDICT: …` as before. A REQUEST_CHANGES here is a Lead repair round (this is the first read of the slice).

## ADDENDUM 2026-09-19 09:5x UTC+3 - FOURTH exact-Opus read: repair round 1 of the slice read (the slice read's own cap of 3 starts here)
- The third read (attempt 4, 2026-09-18 11:50-12:10 UTC+3, `ATTEMPT4_REQUEST_CHANGES_d426e79f/OPUS_T0_REPORT.md`) returned **REQUEST_CHANGES**: **F-1 REQUIRED** - the N4 nesting arms hard-coded depth 3000, where on the pinned Python 3.12 `json.loads` raises first, so the walk guards at all three sites were exercised by nothing and the slice's D026 record (made on Python 3.14) was inverted on the pinned interpreter; NITs F-2 (a THIRD adapter parse site `_decode_json_object` unguarded), F-3 (receipt-side "appeared before publication" arm untested), F-4 (the exporter's "nested too deeply" message unreachable at 3000 on 3.12), F-5 (`_publish` POSIX docstring over-states the alternatives). Adjudicated `LEAD_ADJUDICATION_P030_NIT_T0.md`; the Lead reproduced the interpreter split.
- The candidate is now **`b4df1413`** = `d426e79f` + repair round 2 (Lead = disclosed builder, same four files, +315/-34): both checkers derive one nesting depth per layer on the running interpreter (`nesting_arms()`: search for the smallest depth where anything raises and the smallest where the parser raises, through one fixed frame path; the parser raised first iff they coincide; `_STACK_MARGIN = 64`; each arm asserts its layer's exact message); arms at all three adapter sites (capture `_prefix_facts`, manifest `_decode_strict_jsonl`, NEW config+receipt `_decode_json_object`) and in the exporter; F-2 the third site guarded at parser and walk; F-3 the receipt-side race test; F-5 the docstring names `os.link`'s real cost. Exporter checker 28->29, adapter checker 44->46, GREEN on the pinned 3.12 AND 3.14, ASCII and non-ASCII TEMP. Lead D026 on scratch copies on BOTH interpreters: nine modified copies (walk guard / parser clause per site, receipt-side arm) each error exactly their own arm - `CLAUDE_P0_RUN_20260913/P030_NIT_SLICE_20260918/REPAIR_R2_20260919/LEAD_VERIFICATION_P030_NIT_R2.md` (+ CT13 copy); the d426e79f record now carries a CORRECTION header naming the interpreter. Gemini delta `P030_R2_GEMINI` (see the queue log).
- Read the repair with `git -c safe.directory=* show b4df1413` (the git rules of the launch brief stand). Decide with your OWN modified copies ON THE PINNED INTERPRETER (and, if you wish, on 3.14 = `C:/Python314/python.exe`): (a) delete each walk guard and each parser clause at the four sites (exporter; adapter x3) - the matching arm must error on the pinned interpreter; (b) delete the receipt-side `FileExistsError` arm - the F-3 test must fail; (c) confirm the derived depths land on the intended layer on the pinned interpreter (the arms print `layer=`/`depth=` on failure; `nesting_arms()` can be called directly) and that the parser arm's depth is not pathological for the checker's runtime; (d) judge whether the searching helper itself is honest (monotone predicates, the margin, the skip-by-name path) and whether any of the three adapter sites is still reachable unguarded; (e) F-4/F-5 closed or not. Everything else in this brief stands. Final line `VERDICT: ...` as before. A REQUEST_CHANGES here is Lead repair round 2 of the slice read's cap 3.

## ADDENDUM 2026-09-19 14:2x UTC+3 - Ruff tooling (applies to every reviewer of this lane)
The pinned interpreter venv has NO `ruff` module; any sentence above that implies `python -m ruff` runs there is wrong. Use the standalone `C:/tmp/wp_p0_04_tooling_20260825/Scripts/ruff.exe` (0.16.4) or `uvx ruff@0.16.8` if your sandbox allows it, otherwise read the Lead's ruff parity record for this candidate and mark Ruff NOT VERIFIED by you. A missing linter is NOT a reason to BLOCK; BLOCK only when the mandated TESTS/checkers cannot run.
