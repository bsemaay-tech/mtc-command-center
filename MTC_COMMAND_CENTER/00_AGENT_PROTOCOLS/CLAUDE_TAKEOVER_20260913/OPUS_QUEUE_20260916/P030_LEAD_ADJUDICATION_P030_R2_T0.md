# LEAD_ADJUDICATION - lane 3 attempt 5: exact-Opus read of the WP-P0-30 NIT-slice repair round 2 `b4df1413` (Sat 2026-09-19 09:57-10:21 UTC+3)

**Verdict as written:** **PASS-WITH-NITS, 0 REQUIRED, 4 NITs.** 451 lines, 84 turns, 24 min, launcher exit 0, no cap (the stream's `rate_limit_event` said `allowed`, five-hour window resets 13:50). Pinned interpreter used by the reviewer: `C:/tmp/P020_IMPL_20260912/.../.venv/Scripts/python.exe` = Python 3.12.12, cross-checked on 3.14.2. HEAD and the four blob digests computed by the reviewer (section 1) match `BLOB_OIDS_b4df1413.txt`. Worktree after the lane: `git status --porcelain` = only the pre-existing untracked `TASK_O9EXP.md` (untouched; the reviewer disclosed one read-only `git diff --stat` outside its allowed verbs and `__pycache__` writes from running the mandated checkers - both harmless).

## What the reviewer established (Lead grep-verified every cited line against the `b4df1413` blobs: exporter `:253`, `:274-280`, `:291`, `:92-93`, `:395/:510/:524`; adapter `:83`, `:89-93`, `:120`, `:125-129`, `:203`, `:207-211`, `:222`, `:227-228`; checker `:146-152`, `:946-951` - all match)
- **F-1 CLOSED at its cause on the pinned 3.12:** eight guard mutants built by the reviewer from the blobs (`reviewer_scratch_R4/mutate.py`; E1/E2 exporter walk/parser, A1-A6 the adapter's three sites) each break ONLY their own arm (`layer='walk', depth=1049` / `layer='parser', depth=3044`), every other test green. `nesting_arms()` called directly reproduces the Lead's 1060/3062 (3.12) and 1060/16987 (3.14); the reviewer measured the primitives independently (loads 2998 / walk 998 / dumps 2998 on 3.12; loads 16923 / walk 998 / dumps 15512 on 3.14) and re-derived the F-1 split from scratch. It also notes `json.dumps` is recursive and its RecursionError is uncaught at exporter `:291` but unreachable (the walk raises first) - recorded, no finding.
- **F-2, F-3, F-4, F-5 CLOSED** (mutants A1/A2, E4, E1; docstring read). N1/N3 and K-01..K-04 re-confirmed; 16/16 refusal codes raised live; no raw exception or exported row from any constructed input.
- **Lead record cross-check:** "every one of those reproduces on my own runs" except one ruff rule name (NIT-4).
- NOT VERIFIED is non-empty and honest: no real archive/venue/host; POSIX not executed (both race arms skip there by design - no executed no-clobber evidence on POSIX at all); two interpreters only; P0-26 internals (`opsa_common.py:159/:229`) unaudited by other routes; the receipt is consumed by nothing but the drill; concurrency simulated; ruff 0.16.4/0.16.7 vs the Lead's 0.16.8 (counts agree).

## NITs (Lead disposition)
- **NIT-1** `check_p030_archive_exporter.py:946-951`: the junction fixture's `subprocess.run(..., capture_output=True, text=True)` decodes `mklink` output as UTF-8; under the reviewer's console (OEM cp857) the reader thread raised `UnicodeDecodeError` and a traceback landed inside a GREEN run. **Lead reproduction: NOT reproduced** - 0 occurrences in my Git-Bash runs and 0 in a fresh PowerShell-console run with the pinned interpreter (`REPAIR_R2_20260919/LEAD_REPRO_NIT1_exporter_checker_powershell_console.txt`, 29 OK). Environment-dependent evidence noise, test outcome unaffected either way. Real; fix = `errors="replace"`. **Carried to the next P0-30 slice** (no byte change before Sol reads `b4df1413`).
- **NIT-2** `check_p030_archive_exporter.py:146-152`: the D-13 RED assertion accepts either of two refusal messages; on Windows the collector partition is CRLF/insertion-ordered so the adapter refuses on canonical form (`:222`) before the identity check (`:227-228`) - the identity alternative is inert on the pinned host. Reviewer separated the two fences by probe (both real). Carried: split into two exact arms (K-06 shape) in the next slice.
- **NIT-3** `p030_archive_exporter.py:92-93`: `Path.exists()` follows reparse points, so a DANGLING link at the target name looks absent to every exists-check; on Windows `os.rename` still refuses (reviewer executed it: `target_exists`, junction intact), on POSIX the exists-check is the whole guard and `os.replace` would take the name. Carried: `os.path.lexists` on the POSIX branch or one docstring sentence; Windows-deployed tool, POSIX half reasoned only.
- **NIT-4 (documentary, Lead)** my R2 record listed `E402x3` for the adapter; the bytes yield 8 findings (`I001x2/RUF100x3/TRY004x3`), E402 was the rule NAMED inside the three RUF100 messages that my grep over message text picked up; there is no ruff configuration file in the worktree (defaults). **Corrected in the record now** (`ruff check --statistics` reproduced: 8 / 3 / 42). Lesson: count rule codes with `--statistics`, not by grepping messages.

## Standing
Roster for P0-30 at `b4df1413`: **exact-Opus PASS-WITH-NITS (this read) + Gemini `P030_R2_GEMINI` PASS + Lead reproduction (nine mutants, both interpreters).** Sol tonight on the same bytes (chain position 6); then the merge PR (PR-only serial merge; the P0-26 branch's adapter checker F8 anchor rebases onto whichever PR lands second). NITs 1-3 = one small follow-up slice after Sol (both flagships again on the final bytes, because bytes change). The Lead never accepts its own code.

Recorded by Claude Fable 5.1 Lead (session 7, `18c1b8`); machine stamps in the queue log.
