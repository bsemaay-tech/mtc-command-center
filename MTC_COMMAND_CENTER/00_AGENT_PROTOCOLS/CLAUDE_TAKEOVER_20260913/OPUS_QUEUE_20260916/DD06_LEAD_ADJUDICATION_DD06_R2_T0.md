# LEAD_ADJUDICATION - lane 7 (DD-06 testnet probe), third exact-Opus read of `2128352b` (attempt 4, 23:00-23:17 UTC+3, 2026-09-17) = round 3 of the T0 cap 3

**Verdict as written:** REQUEST_CHANGES (one REQUIRED R-1; NITs N-3..N-10 carried, N-13..N-16 new; N-11/N-12 closed). 423 lines, 99 assistant turns, launcher exit 0, no cap.
**Safety rule:** held. The reviewer's section 0: no incident, no network call, every mutant on the scratch copy `C:/tmp/OPUS_DD06_SCRATCH/mutroot3/`, `main()` never run, tripwires intact (the two `hyperliquid-testnet` matches in the mutant output were the tripwire printing its own argument and an assertion source line - the reviewer opened both).
**Attempt 3** (21:49-21:53) was the Pro 5-hour cap, skeleton only, not counted; attempt 4 ran on the 23:00 window and is the counted third read.

## R-1 (REQUIRED) - reproduced by the Lead, offline
Claim: `_paid` (`tools:355-377`) counts a key as `readable` when that ONE key parses on both sides; the unified account's perp `accountValue` is structurally `0.0` and always parses, so a master wallet whose SPOT read fails after the arm (the only balance that can pay on this account shape) is still "measured" and rendered by `_attribute` (`tools:380-412`) as the flat words "account did not pay" with no UNREADABLE marker, and the OWN branch prints the exclusion "not the DD-06 breach" (`tools:403`). Sub-case R-1(b): with the master fully unreadable the sentence still says "not the DD-06 breach" - an exclusion drawn from no measurement.
Reproduction: the reviewer's probe `test_reviewer_partial3.py` (copied to `ADJUDICATION_R3_20260917/reviewer_test_reviewer_partial3.py`) run by the Lead on a fresh scratch copy built from the `2128352b` blobs (`c4f55256` tool, `2291d5aa` tests - `hash-object` equal), fixture objects only, `HL_*`/`DD06_PROBE_RUN_TOKEN` removed from the environment, basetemp `C:/bt_s4/dd06r3`: **3 passed** - `RESULT: DD06_FINDING_OWN_FUNDS_MOVED`, finding "[account did not pay, agent wallet paid]" while `post_spotSend_account_balances.after.usdc_total` is `None` (`LEAD_REPRO_R3_R1_reviewer_partial_probe_on_unmodified_2128352b.txt`). The third probe shows a real 6-USDC master decrease behind the failed spot read is invisible and still labelled OWN + "not the DD-06 breach".
Citations grep-verified against the blobs: `tools:355-356` (`_paid` signature), `:374` (`readable += 1`), `:377` (`return False if readable else None`), `:380` (`_attribute`), `:393` (readings), `:400-406` (the OWN sentence incl. `:403`), `:486` (the unifiedAccount comment); `tests:677-684` (the Lead's own unit test pins the partial case as `False`), `:595-598` (the parametrized unreadable case blanks BOTH endpoints), `:522`.
Lead's reading: the finding is correct and it is mine. Round 2 made an unreadable side honest only when EVERY key failed; I pinned the partial case as `False` in `test_paid_is_directional_amount_aware_and_unreadable_honest` (a test whose name promises the opposite). On this account shape the decoy `0.0` guarantees the partial case is the one that occurs. Error direction: flattering (the record says the master did not pay about money it did not read).

## The cap
D028 / `OWNER_DECISION_AUDIT_TIERS_2026-08-09.md`: T0 = two flagships, xhigh, **max 3 rounds**. This candidate has consumed three: read of `1af85067` -> repair `a46b2a9d`; read of `a46b2a9d` -> repair `2128352b`; read of `2128352b` -> REQUEST_CHANGES. The Lead's repair authority on the DD-06 probe ends here (recorded in the handoff and the queue file before the read ran). No repair was built. Nothing was changed in the worktree (`C:/tmp/P029_DD06_20260915` still `2128352b`, clean).

## Owner decision `DD06-CAP` (defaults: nothing moves; the candidate stays parked at `2128352b`)
- **A - one extra round, Lead repairs** (rec.): the remedy is small and local - `_paid` returns `None` when any key is unreadable and no readable key decreased (`True` on a readable decrease stays), `tests:677-684` flips to `None`; `_attribute` prints no exclusion and no OWN conclusion when `master_paid is None` -> new label `DD06_FINDING_OWN_FUNDS_MOVED_MASTER_UNREADABLE` ("the agent paid; the account was NOT measured"). Then a fourth exact-Opus read (next Pro window) and Sol on Saturday judge the same bytes. Cost: one Pro lane + one Gemini delta; no venue contact.
- **B - park; Sol reads `2128352b` as-is on Saturday**, the owner decides after both flagships (the defect stays open; nothing runs on the venue meanwhile).
- **C - accept `2128352b` with R-1 carried as a known limitation** plus the reviewer's operator rule in the step packet: never accept an attribution whose `post_*_balances` step contains a `null` on either side - re-read by hand before recording any conclusion. Cheapest; the tool's sentence stays wrong in that case and the human check carries it.
The Lead never accepts its own code; A still needs both flagships before any merge.

## NITs (carried into the lane record; none built)
N-3 `_resting_oid`, N-4 `_HEX40`, N-5 min-notional (re-measured), N-6 host binding, N-7 `usd_class_transfer` test, N-8 brief residue, N-9 arm order (owner `DD06-ORDER A|B`), N-10 `len(digits) <= 5`, **N-13 new** a mark-to-market drift larger than half the arm amount still counts as a payment, **N-14 new** single post-arm snapshot with no settlement allowance, **N-15 new** the ERROR branch embeds `_attribute`'s sentence verbatim, **N-16 new** `ARM_AMOUNT_USDC` pinned by no test. N-11, N-12 closed.

## NOT VERIFIED by the reviewer (as stated)
The probe itself never run; full Bridge suite and ruff taken from the Lead's evidence; venue-side truth of r1-r3 from the recorded files; mainnet neither probed nor claimed.

Recorded by Claude Opus 5 Lead (session 6, `4a8233`), 23:4x UTC+3.
