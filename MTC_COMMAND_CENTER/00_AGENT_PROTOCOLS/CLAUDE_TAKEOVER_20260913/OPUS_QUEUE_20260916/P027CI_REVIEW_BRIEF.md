# T1 review brief — WP-P0-27 CI workflow additions: PR #193 (`research-gates.yml`, `a3325836`) and PR #194 (`opsa-tests.yml`, `63b7bbe0`) (exact `claude-opus-5` xhigh; Wednesday 2026-09-16 slot, lane 5 — started BY HAND after lanes 1-4, owner word `we'd opus` = "Wed Opus" 2026-09-15 18:2xZ)

You are a fresh, independent T1 reviewer of two GitHub Actions workflow changes. Your exact role (model and effort) is fixed by the launcher; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory. You never merge, never touch GitHub state, never push. `gh` is read-only for you (`gh pr view`, `gh run view`, `gh api` GET only).

## Context (settled facts; verify identities, do not re-decide)
- WP-P0-27 (CI home) is **ACCEPTED at day-one scope** by the owner (`OD-20260915-P027-DAYONE-ACCEPT-1`; `CI_POLICY.md` "Acceptance (2026-09-15)" paragraph in `C:/CT13/MTC_COMMAND_CENTER/11_TRIAGE/WP_P0_27_CI_HOME_2026-08-25/CI_POLICY.md`). The ruleset 21444962 on `master` requires exactly `Bridge suite (Python 3.12)` + `pine-alert-guard`, strict, zero bypass. Neither PR changes the ruleset; neither adds a required check.
- **CI_POLICY: the workflow package is audit tier T1** — a workflow file change merges only after a T1 verdict. That is YOUR verdict. The merge itself is a separate act under the standing git delegation after your PASS; a REQUEST_CHANGES or BLOCK keeps both PRs open.
- Both PRs are **LEAD-AUTHORED (Claude Opus 5 Lead, disclosed)**; the Lead never accepts its own change. Gemini 3.8 read PR #193 three times (T2 corroboration of the diff + the acceptance packet: PASS-WITH-NITS ×2 each time, 0 REQUIRED; all three attempts wrapper-voided → SUPPLEMENTAL) — findings: Ruff installed by version pin without `--require-hashes` (`research-gates.yml:170` vs the Bridge lock install at `:162`); `git config --system core.longpaths true` (system scope, ephemeral runner). PR #194 has had no reviewer yet.
- PR #193 `feature/p027-research-gates-additions-20260915` @ `a3325836` (base `fcac0ac6`): adds to `.github/workflows/research-gates.yml` three jobs/steps — shared contracts tests + Ruff, the three P0-30 checkers on a Windows runner (`core.longpaths` set BEFORE checkout — the intermediate commit `55ab90b8` failed the Windows checkout, that is the RED evidence), Research gate checkers. Runs: GREEN `34963602576` (and the checks on the PR head), RED demo `34961383292` on `55ab90b8`. Evidence: `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/P027/` (`P027_ACCEPTANCE_PACKET_20260915.md`, `RED_DEMO_EVIDENCE_20260915.md`, `GH_QUERIES_20260915_NIGHT.txt`, `CI_PR193/`).
- PR #194 `feature/p027-opsa-tests-ci-20260915` @ `63b7bbe0` (base `fcac0ac6`): adds ONE new file `.github/workflows/opsa-tests.yml` — job `OPS-A tests (Python 3.12, Windows)` on `windows-2025`, `git config --system core.longpaths true` before checkout, `actions/checkout` with `persist-credentials: false`, setup-python 3.12, `python -m unittest test_opsa -v` in `MTC_COMMAND_CENTER/tools/opsa`, `permissions: contents: read`, concurrency group, timeout 10 min, informational (not required). Runs: GREEN `34983809921` on the PR head; RED demo PR #195 (`34983897746`, failure, closed unmerged). Evidence: `.../QUEUED_PACKAGES_20260915/P027_OPSA_CI_20260915/` (`P027_OPSA_CI_EVIDENCE_20260915.md`, run JSONs, RED excerpt).
- Both PRs re-checked 2026-09-15 16:28Z: OPEN, every check SUCCESS, MERGEABLE, unmerged.

## Subject (read-only worktrees; verify before reading anything else)
- `C:/tmp/P027_CI_20260915`, HEAD must be `a33258367739400017c11a7e498030238f9b1564` (`git -c safe.directory=* -C C:/tmp/P027_CI_20260915 rev-parse HEAD`).
- `C:/tmp/P027_OPSA_CI_20260915`, HEAD must be `63b7bbe0` (full SHA from `git -c safe.directory=* -C C:/tmp/P027_OPSA_CI_20260915 rev-parse HEAD`; write it in your report and compare with `gh pr view 194 --repo bsemaay-tech/mtc-command-center --json headRefOid`).
- NO other git command in either worktree — never status/diff-with-working-tree/add/commit/checkout. Use `git -c safe.directory=* show <rev>:<path>` and `git -c safe.directory=* diff fcac0ac6 <sha> -- .github/workflows` for byte comparisons only, and say so. If a HEAD differs, STOP and write `VERDICT: BLOCK` with the observed HEAD.
- Do not run any workflow, do not push, do not open/close/comment on PRs, do not touch the ruleset. `gh` GET only; every `gh` call you make goes in your report with its output.

## What you must decide (T1 — the workflow package tier)
1. **Least privilege and secrets:** each job's `permissions` (must be `contents: read` or narrower), `persist-credentials: false` on every checkout, no secret referenced, no `pull_request_target`, no `schedule:` (P0-27 non-goal), no deploy/host/venue contact, no artifact upload of anything sensitive.
2. **Pinning:** action versions (`actions/checkout`, `actions/setup-python`) — tag vs SHA pin, and whether the repository's other workflows (`ci.yml`, `pine-alert-guard`) use the same convention (consistency is the bar, not perfection; a stricter pin is a NIT unless a policy line requires it — cite the line if you claim it does); Python `3.12` pin; Ruff `0.16.4` by version (Gemini F-01: no `--require-hashes` — decide whether that is REQUIRED for an informational job or a NIT; `contracts/constraints.txt` at `a3325836` carries `ruff==0.16.4`, verify with `git show`).
3. **Correctness of what runs:** the commands under each job run the tests they claim (`test_opsa` = 33 unittest tests on master bytes; the three P0-30 checkers; the contracts tests) with the right working directory and interpreter; the Windows `core.longpaths` step is BEFORE checkout and is the reason `55ab90b8` failed (read the RED log excerpt); `fail-fast`/`continue-on-error` semantics — an informational job must still FAIL visibly when its tests fail (the RED demos prove it: reproduce the claim from the run JSONs, do not take it from the evidence prose).
4. **Blast radius on `master`:** the ruleset's required checks stay exactly two; these jobs are informational; a red informational job cannot block a merge — say so explicitly, and say whether the evidence documents claim otherwise anywhere (they must not).
5. **Records honesty:** the acceptance packet, the OPS-A CI evidence note and the CI_POLICY paragraph vs the bytes and the GitHub state you read (run conclusions, PR states, ruleset). Any number you cannot reproduce is NOT VERIFIED, named.
6. **Merge order and conflicts:** both PRs touch `.github/workflows/` from the same base; state whether they conflict (`git diff fcac0ac6 a3325836 -- .github/workflows` vs `git diff fcac0ac6 63b7bbe0 -- .github/workflows` touch different files?) and the recommended merge order.

## Report format
Markdown with: verified identities (COMPUTED: both HEADs, sha256 of `git show a3325836:.github/workflows/research-gates.yml` and `git show 63b7bbe0:.github/workflows/opsa-tests.yml`); a per-item table for items 1-6 per PR (item | PR #193 | PR #194 | evidence file:line or `gh` output); findings numbered with severity REQUIRED / NIT and exact file:line; NOT VERIFIED (non-empty — you cannot execute the workflows); per-PR final lines exactly `VERDICT #193: PASS` / `PASS-WITH-NITS` / `REQUEST_CHANGES` / `BLOCK` and `VERDICT #194: …`; then one line `T1 MERGE RECOMMENDATION: <order or none>`. A REQUIRED finding on either PR → that PR's verdict is REQUEST_CHANGES with the exact change.

Written by Claude Opus 5 Lead (session 5, `03c6c8`) 2026-09-15 18:4xZ under the owner's Wednesday-Opus word; the Lead is the author of both PRs and is not a reviewer here.
