# T0 review brief — WP-P0-12 funding intake adapter — the NON-ACCEPTING half (real capture → export-tool shapes with UNRESOLVED labels), candidate `4c802e9b68539c2ed0a422dc34efcb0e55ae3796` (exact `claude-opus-5` xhigh; Wednesday 2026-09-16 slot, OPTIONAL lane — only if allowance remains after lanes 1-5; also the source brief for the Friday Sol lane)

You are a fresh, independent T0 reviewer. Your exact role (model and effort) is fixed by the launcher that started you; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory (scratch copies under `C:\tmp\OPUS_P012INTAKE_SCRATCH` only). The candidate is LEAD-AUTHORED (Claude Opus 5 Lead, disclosed); the Lead never accepts its own code — you are the first exact flagship read.

## Context (settled facts; verify identities, do not re-decide)
- Package WP-P0-12 (funding evidence). Base `origin/master` `fcac0ac6`. Branch `feature/p012-funding-intake-adapter-20260915`: `004a0711` (adapter + 7 tests; Lead-built under `OD-20260915-BUILD-ABC-1` item b) → `65c4bc40` (test-only: no-wall-clock assertion) → `4c802e9b` (test-only: unaligned END arm). Two new files: `IBKR_PAPER_BRIDGE/tools/funding_intake_adapter.py`, `IBKR_PAPER_BRIDGE/tests/test_funding_intake_adapter.py`; `export_mtc_funding.py` untouched.
- What it claims: `export_mtc_funding.py` has NO production mode (`ACCEPTED_EVIDENCE_KINDS = ("SYNTHETIC_FIXTURE",)`, refusal `CANDIDATE_PRODUCTION_EVIDENCE_UNAVAILABLE`); the adapter turns the Path 1 capture r1 derived view into the tool's packet/retained-row shapes, labels every unfillable field `UNRESOLVED:D-n` (six decisions D-1..D-6 in `P012_FUNDING_INTAKE_DESIGN_20260915.md`; owner packet `P012_INTAKE_DECISIONS_OWNER_PACKET_20260915.md`), fabricates nothing (no oracle price back-derivation), writes the short address only, write-once outputs.
- Reviews so far: Gemini 3.8 detection ×4 (two voided-but-complete, attempt 3 partial, attempt 4 COUNTED PASS-WITH-NITS, `fabricated_fields: []`; `QUEUED_PACKAGES_20260915/P012_FUNDING_INTAKE_20260915/GEMINI/`); Lead verification + fabrication-fence RED arm + wall-clock RED arm. Owner Q3 "Wait" and gate order Q6 stand; nothing here admits production. Lead = author, never acceptor.

## Subject (read-only worktree; verify before reading anything else)
- `C:/tmp/P012_INTAKE_20260915`, HEAD must be `4c802e9b68539c2ed0a422dc34efcb0e55ae3796` (`git -c safe.directory=* -C C:/tmp/P012_INTAKE_20260915 rev-parse HEAD`; NO other git command — never status/diff-with-working-tree/add/commit/checkout; `git -c safe.directory=* show <rev>:<path>` and `git -c safe.directory=* diff fcac0ac6 4c802e9b -- <paths>` for byte comparisons only, and say so). If HEAD differs, STOP and write `VERDICT: BLOCK` with the observed HEAD.
- Tests: from the worktree root, Bridge interpreter `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe -m pytest IBKR_PAPER_BRIDGE/tests/test_funding_intake_adapter.py -q -p no:cacheprovider --basetemp C:/bt_opus_intake` (expect 7 passed). The r1 capture directory is `C:/tmp/CLAUDE_P0_RUN_20260913/P012_PATH1_REAL_CAPTURE_20260915/r1/` (read-only; the launcher grants it); if it is not readable, say NOT VERIFIED for item 4 rather than inventing a capture.
- Records: `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/P012_FUNDING_INTAKE_20260915/` (verify every closure against bytes, never against a report's description).

## What you must decide
1. **Fabrication audit:** for every key of `intake_r1/binding_packet_draft.json` classify COPIED / TOOL-RULE / DERIVED-LABELLED / UNRESOLVED with the source line; any unlabelled computed value = REQUIRED (the back-derived oracle price ≈ 78 758 must appear nowhere).
2. **Design-note honesty:** verify against `export_mtc_funding.py` bytes that no `--mode` exists and only `SYNTHETIC_FIXTURE` is accepted; verify D-1..D-6 map to real gaps in the tool's closed key domains.
3. **Leakage:** short address only in every output; stdlib-only imports; no env read; no network — prove with the test that pins it and one mutant of your own.
4. **Determinism and write-once:** reproduce the r1 dry run yourself into a scratch dir (`python IBKR_PAPER_BRIDGE/tools/funding_intake_adapter.py --run-dir <r1 capture dir from the records> --out C:/tmp/OPUS_P012INTAKE_SCRATCH/r1`) and compare digests with `intake_r1/*.sha256`; run it twice and show the write-once refusal (exit 3).
5. **Completeness rule (D-6):** the proposed witness (hour-aligned window + two byte-identical passes) — is it labelled as proposed everywhere it appears; does the venue's +31..+60 ms stamp offset make the half-open end boundary a real gap (the design note's option (i))?
6. **Scope:** exactly two new files; full Bridge suite green with them (`LEAD_PYTEST_full_bridge.txt`); re-run the focused tests yourself.

## Report format
Markdown with: verified identities (COMPUTED HEAD, sha256 of both files); fabrication table; design-note table; leakage proof; determinism/write-once transcript; D-6 statement; findings REQUIRED/NIT; NOT VERIFIED; final line exactly `VERDICT: PASS` / `VERDICT: PASS-WITH-NITS` / `VERDICT: REQUEST_CHANGES` / `VERDICT: BLOCK`.

Written by Claude Opus 5 Lead (session 5, `03c6c8`) 2026-09-15 20:2xZ; the Lead is the author of the candidate and is not a reviewer here.
