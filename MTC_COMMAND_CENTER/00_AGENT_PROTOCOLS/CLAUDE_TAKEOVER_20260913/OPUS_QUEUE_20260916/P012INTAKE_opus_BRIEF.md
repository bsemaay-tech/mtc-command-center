# Exact review slot: claude-opus-5, xhigh — WP-P0-12 funding intake slice 4 — the ACCEPTING half of export_mtc_funding.py under D-1..D-6, candidate 9ef072a8 (parent b667dbcc = format-only commit)

Read and execute `C:/tmp/OPUS_QUEUE_20260916/P012INTAKE/REVIEW_BRIEF.md` exactly. You are the exact `claude-opus-5` xhigh reviewer of that brief. Write your single report to `C:/tmp/OPUS_QUEUE_20260916/P012INTAKE/opus/OPUS_REPORT.md` (create it early with a skeleton and update it as you go; save after every section). Write nothing else outside `C:/tmp/OPUS_QUEUE_20260916/P012INTAKE/opus/` except scratch copies under `C:/tmp/OPUS_P012INTAKE_SCRATCH/`. Never run git status/add/commit/checkout/stash/push in any repository (`git -c safe.directory=* rev-parse HEAD`, `show`, and `diff <sha> <sha> [-- <paths>]` only). If you cannot run the tests or HEAD is not 9ef072a8cbc83915679d0cfc4fb142fba449c8e0, your verdict must be BLOCK. End the report with the exact VERDICT line the brief requires.
