# T0 review brief — WP-P0-26 completion-marker repair, candidate `6ac9cfb7b923e8d86b8546f7c0117fbb0553d0a3` (exact `claude-opus-5` xhigh; Wednesday 2026-09-16 slot, lane 4 — runs only if allowance remains after lanes 1-3)

You are a fresh, independent T0 reviewer. Your exact role (model and effort) is fixed by the launcher that started you; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory (scratch copies for RED arms go under the scratch dir your launcher names).

## Context (settled facts; verify identities, do not re-decide)
- Package WP-P0-26 = the OPS-A operational tools (`MTC_COMMAND_CENTER/tools/opsa/`: `backup.py`, `restore.py`, `opsa_common.py`, `watchdog.py`, `heartbeat.py`, tests `test_opsa.py`; unittest, not pytest). Controlling contract: `MASTER_WORK_PACKAGE_AND_PARALLEL_DELIVERY_PLAN_2026-08-22.md:577-588` (automated second-location copy of every evidence store; no-delete guarantee).
- Authority for this slice: the scope packet `C:/tmp/P026_LOCAL_SCOPE_DECISION_20260907.md` (rev 2; copy at `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/P026_LOCAL_SCOPE_DECISION_20260907.md`) — §2 five MODIFY files / 0 ADD, §4 behaviour ceiling, §5 tests, §6 exclusions, §7 stop rule — and the owner's word "A" of 2026-09-15 (`OD-20260915-P026-REPAIR-LEAD-1` in `C:/CT13/DECISIONS.md`): the Lead builds the repair now instead of waiting for Codex (Sep 19). The watchdog half of the packet (§3 second and third defects) had already landed on master in `53d33dbc` with its three tests; this candidate touches the backup/restore half only.
- **The candidate is LEAD-AUTHORED (Claude Opus 5 Lead, disclosed).** One commit `6ac9cfb7` on `feature/p026-completion-marker-20260915` from `origin/master` `fcac0ac6`; 4 files modified (`backup.py`, `restore.py`, `opsa_common.py`, `test_opsa.py`), 0 added, `watchdog.py` untouched. The Lead never accepts its own code; you and exact Sol (Sep 19) are the flagship slots; Gemini corroboration is pending/recorded under the records directory.
- **Known blocker, disclosed by the Lead (records §4):** the repo-root P0-30 adapter `p030_closed_partition_backup_adapter.py` (merged to master 2026-09-13, after the scope packet) restores through `restore.py` from an isolated snapshot root it builds itself (`_isolated_restore_inputs`) without the two new completion files, so `check_p030_closed_partition_backup_adapter.py` exits 1 on the candidate (`Ran 44 tests`, `FAILED (failures=7, errors=2)`; 13 `run_not_complete` refusals — 12 `COMPLETE.json absent`, 1 `per-run manifest file records differ from the global manifest` in `test_shared_archive_reversion_mutant_is_detected`, whose expected error text no longer matches although the mutant is still refused) and 0 on the base. The Lead stopped at the packet's file ceiling and asked the owner: (A) extend the slice by that one adapter file (a further commit on this branch — then the launcher pin and this brief are updated by the Lead before you run), or (B) park the candidate for a P0-30 lane. Do NOT treat the adapter failure as a new discovery; DO judge whether the candidate's contract change is the right one and whether anything ELSE on master calls `restore.py` or reads `runs/<id>/` and would break.
- Lead verification records: `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/P026_REPAIR_20260915/` (`LEAD_VERIFICATION_P026_REPAIR.md`, `LEAD_UNITTEST_312.txt` = `Ran 37 tests … OK`, `LEAD_RED_ARMS_PREFIX_BACKUP_RESTORE.txt` = `FAILED (failures=5, errors=4)` on the pre-fix modules, `LEAD_RUFF.txt`, `LEAD_GUARD.txt`, the two adapter-check outputs, `BASE_SHA256SUMS_fcac0ac6.txt`) and the decision packet `../P026_DEPLOYMENT_AND_DRILL_DECISION_PACKET_20260915.md` (owner-facing; not part of the code review).

## Subject (read-only worktree; verify before reading anything else)
- `C:/tmp/P026_REPAIR_20260915`, HEAD must be `6ac9cfb7b923e8d86b8546f7c0117fbb0553d0a3` (`git -c safe.directory=* -C C:/tmp/P026_REPAIR_20260915 rev-parse HEAD`; NO other git command — never status/diff/add/commit/checkout; `git -c safe.directory=* show <rev>:<path>` and `git -c safe.directory=* diff fcac0ac6 6ac9cfb7 -- MTC_COMMAND_CENTER/tools/opsa` for byte comparisons only, and say so). If HEAD differs, STOP and write `VERDICT: BLOCK` with the observed HEAD.
- Pinned interpreter: `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (3.12.12). Tests: from `C:/tmp/P026_REPAIR_20260915/MTC_COMMAND_CENTER/tools/opsa` run `python -m unittest test_opsa -v` (expect 37 OK). Adapter checks from the worktree root: `python check_p030_opsa_heartbeat_adapter.py` (expect exit 0) and `python check_p030_closed_partition_backup_adapter.py` (expect exit 1 on `6ac9cfb7`: 44 tests, 7 failures + 2 errors, 13 `run_not_complete` refusals; expect exit 0 if the Lead's brief update says option A landed). Ruff 0.16.4 is in the same venv (`python -m ruff check --select E9,F821,F811,F401,F841 MTC_COMMAND_CENTER/tools/opsa`).
- Authority boundaries: nothing outside `MTC_COMMAND_CENTER/tools/opsa/` may have changed (plus, under option A only, the one adapter file at the repo root); no new file; no environment/credential read, no network, no SSH/SFTP, no scheduling, no host contact, no deletion primitive anywhere in the tools (`NoDeleteGuaranteeTests`).

## What you must decide (T0 — restore is the tool that would rebuild an evidence store after a loss)
1. **Packet conformance, clause by clause** (§4 "Completed-run manifests and explicit-run restore" — per-run immutable `RUN_MANIFEST.jsonl` + `COMPLETE.json` written only after every readback hash passes; a partial/interrupted run has NO marker; never rewritten; restore consumes an explicit completed run id and fails closed: no run without matching marker + manifest, no execution on hash mismatch; `--latest` removed; restore without a run id errors; additive only — every other interface unchanged): IMPLEMENTED-AS-WRITTEN / PARTIAL / BROADER THAN THE PACKET, quoting the packet clause and the code (file:line). "Atomically" in the packet vs `write_once_bytes` (`open(..., "xb")` + flush + fsync, no tmp/rename): say whether the guarantee actually obtained (exclusive create; no partial marker can be mistaken for a complete one) meets the clause, and what a crash between the manifest write and the marker write leaves behind.
2. **Fail-open hunt** on `backup.write_completion_evidence` / `restore.verify_completion_evidence` / `opsa_common.load_complete_marker` / `select_run`: every path where a run can be restored WITHOUT a genuine completion — marker for another run id copied in, marker whose `run_manifest_sha256` matches a manifest that lists fewer files than the global manifest, a `RUN_MANIFEST.jsonl` edited after the marker was written, an errors>0 run that still gets a marker, a marker written by a dry run, a `runs/<id>/` directory created by hand, a restore root copied without `runs/`, a global-manifest record set that disagrees with the per-run manifest, a run id that matches by prefix, a `COMPLETE.json` with `readback` not `all_match`. Design and run your own RED arms (scratch copy of the module + the suite's fixtures): at least (a) interrupted run → restore refuses under `--run <id>` with `run_not_complete` rc 3; (b) marker digest tamper → refused; (c) the pre-fix modules (`git show fcac0ac6:MTC_COMMAND_CENTER/tools/opsa/backup.py`, `…/restore.py`) with `--latest` selecting a partial run — reproduce the ORIGINAL defect the packet §3 names; (d) an errors>0 run leaves no marker.
3. **D026 evidence:** for every new refusal, the test that shows RED on the pre-fix behaviour; state which of the new tests pass on the OLD modules for the wrong reason (the Lead's RED run shows 5 failures + 4 errors — check the errors are the NEW arms failing for the right reason, not import errors masking a fence).
4. **Contract-change blast radius:** who else on master calls `restore.py`, `select_run`, or reads `runs/<run_id>/` — the P0-30 adapter is known; find any other consumer (grep the worktree, read-only) and say whether the stricter contract breaks it. Say plainly whether option A (adapter carries the completion files into its isolated root) or option B is the technically sound resolution, and whether the marker's digest binding survives the adapter's copy.
5. **Scope and protected paths:** `git diff fcac0ac6 6ac9cfb7 --stat` touches exactly the four files (or five under option A); nothing under `02_MTC_BACKTEST`, `07_ADAPTERS`, `01_PINE`, `MTC_V2`; no file added; global `manifest.jsonl` record shapes unchanged; heartbeat emission unchanged; `backup.py` CLI unchanged.
6. **Lead authorship:** say whether it changes your verdict or needs anything beyond your and Sol's review.
7. **Report honesty:** `LEAD_VERIFICATION_P026_REPAIR.md` vs the diff; the counts (37 / 5+4 / 44: 7+2 on 13 refusals) reproduced; interpreter statements correct.

## Report format
Markdown with: verified identities (COMPUTED: HEAD, sha256 of the four subject files); per-clause conformance table; RED/GREEN table per new refusal (test | RED on pre-fix? | GREEN now); your own RED arms with commands, cwd and exact observed output; the consumer list from item 4; findings numbered with severity REQUIRED / NIT and exact file:line; NOT VERIFIED (non-empty); final line exactly `VERDICT: PASS` or `VERDICT: PASS-WITH-NITS` or `VERDICT: REQUEST_CHANGES` or `VERDICT: BLOCK` (BLOCK if you could not execute the mandated commands or HEAD drifted). Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. Your report alone accepts nothing; the Lead assembles the roster (you + exact Sol + Gemini + Lead ✔ as author-disclosed verifier, never as acceptor).
