# Paste-ready start prompt — P0-26 PACKAGE session (model **Claude Sonnet 5**; project `C:\LAB\Tradingview_LAB_CLEAN`). T0 repair round 1 for the exact-Sol REQUIRED finding on the OPS-A restore gate.

You are the **P0-26 PACKAGE session**: you repair `MTC_COMMAND_CENTER/tools/opsa/` (backup / restore / opsa_common / test_opsa) for the Sol finding REQUIRED-1 and the small carried remarks, nothing else. Owner = Barış (English; UTC+3). The Lead (Fable 5.1) coordinates and merges.

**Read first, nothing else by default:** `C:/tmp/CLAUDE_P0_RUN_20260913/SESSION_PROMPT_P012_PACKAGE_20260919.md` sections "Binding rules", "Before touching anything", "Routes you may use", "Token discipline" (binding); `C:/tmp/SOL_QUEUE_20260919/P026/sol/SOL_T0_REPORT.md` (REQUIRED-1, NIT-1, NIT-2; the reviewer's probe `dir_record_mismatch_probe.py` under its scratch `C:/tmp/SOL_P026_SCRATCH/`); `C:/tmp/OPUS_QUEUE_20260916/P026/LEAD_ADJUDICATION_P026_SOL_T0.md` (the first, procedural read) and `LEAD_ADJUDICATION_P026_NIT_T0.md` (Opus NITs 1/2/3/5 of the slice read); CT13 `DECISIONS.md` rows `OD-20260919-P026-EVID-B-1`, `OD-20260918-P026-N2-DOC-1`, `OD-20260914-P030-P026-CONFIG-1` (300 s).

**Worktree and pin:** `C:/tmp/P026_REPAIR_20260915` @ `2e03a669`, porcelain empty (stop and ask otherwise). Interpreter: `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (3.12.12); tests from `MTC_COMMAND_CENTER/tools/opsa` as the brief mandates (`test_opsa.py` unittest, plus the P0-30 adapter checker `check_p030_closed_partition_backup_adapter.py` from the worktree root - both must stay GREEN); Ruff via `uvx ruff@0.16.8` or `C:/tmp/wp_p0_04_tooling_20260825/Scripts/ruff.exe` (the pinned venv has no Ruff). Records: `C:/tmp/CLAUDE_P0_RUN_20260913/P026_NIT_SLICE_20260918/REPAIR_R1_20260919/`. Queue-log prefix `P026SESSION:`. Commit author `Claude Sonnet 5 <noreply@anthropic.com>`.

**Do:**
1. **REQUIRED-1** (`restore.py:97-103` compares only `file` records; `:50-68` selects `file` AND `dir` records; `:245-251` recreates directories from the GLOBAL manifest after the gate passed): make the completion gate compare EVERY operational record restore consumes - `file` and `dir`, with type-appropriate canonical fields (dir: `rel` at least) - between the digest-bound per-run `RUN_MANIFEST.jsonl` and the global `manifest.jsonl` BEFORE any restore action; a disagreement is `run_not_complete` / rc 3 with nothing restored. Test-first: reproduce the reviewer's arm (backup a store with an empty directory, forge only the global `dir.rel`, restore → today rc 0 and the forged dir is created) as a RED test on the `2e03a669` bytes, GREEN after; a second arm for a forged `dir` record ADDED only globally; mutant = drop the dir comparison → both tests fail.
2. **NIT-1**: `backup.py:15-19` / `opsa_common.py:41-43` prose promising "neither completion artifact remains" after an interruption → state the truth (the per-run manifest may remain without `COMPLETE.json`; restore refuses it). **NIT-2**: `RESTORE_DRILL_EVIDENCE.md:152` still shows `--latest` - fix the line and re-apply the dated note that `P26-EVID B` moved to this slice (this IS the follow-up slice the row named; record `OD-20260919-P026-EVID-B-1` in the commit message). Opus slice-read NITs 1 (fence the four sole guards `opsa_common.py:213-214`, `restore.py:92-96`, `:111-112` - one test each), 2 (non-UTF-8 manifest bytes → `run_not_complete`, not `UnicodeDecodeError`), 3 (one unmarked `--latest` in section C4), 5 (README `:74` and `watchdog.py:38` `--silence-seconds 900` → 300).
3. GREEN: `test_opsa.py` (39 + your arms), adapter checker 46 OK; ruff per-rule parity by `--statistics`; guard PASS; commit(s), push. `LEAD_VERIFICATION_P026_R1.md` (interpreter per arm).
4. Roster on the final bytes: Gemini delta (root `P026_R1_GEMINI`, PRE `2e03a669`, recipe `P012_S16_REVIEWS_20260913/P026_NIT_GEMINI/`), exact-Opus re-read (lane `P026`: re-pin `opus/run.ps1` - tell the Lead first -, addendum in `REVIEW_BRIEF.md`; Pro or MAX profile per the Lead), exact-Sol (`make_sol_lane.ps1 -Pkg P026 -Account <as assigned>`; the brief's Ruff addendum stands). Adjudicate each (grep every citation, reproduce one arm, worktree clean); CT13 records; `MERGE_READINESS_P026.md`; message the Lead. **No PR, no merge** (the Lead merges under the standing delegation).

Questions → `C:/tmp/CLAUDE_P0_RUN_20260913/P026_NIT_SLICE_20260918/REPAIR_R1_20260919/QUESTIONS_FOR_LEAD.md` + `SendMessage` to the Lead; then stop.
