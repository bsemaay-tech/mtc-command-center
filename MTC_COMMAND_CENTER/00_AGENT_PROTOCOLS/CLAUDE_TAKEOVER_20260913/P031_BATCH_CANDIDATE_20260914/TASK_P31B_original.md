# TASK P31B - WP-P0-31 engineering batch implementing the owner's recorded lifecycle answers (owner: "P031 batch GO", `OD-20260914-P031-BATCH-GO-1`; answers `OD-20260914-P031-LIFECYCLE-1`) -> ONE new exact candidate + REPORT

You are a BUILDER (gpt-5.5 high, Codex Plus). Not a reviewer, not the Lead. Worktree: `C:/tmp/P031_M1_20260913` (branch `feature/p031-m1-20260913-refresh`, reviewed HEAD `c76043b92c70c9f79a6d06630c0896ebe73e68a1`, frozen base `62a42514…`). Read first: `MTC_COMMAND_CENTER/11_TRIAGE/P031_M1_SCOPE_AND_STATUS.md`, the ledger `MTC_COMMAND_CENTER/03_QUANTLENS/tools/p031_lifecycle_ledger.py`, its tests `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p031_lifecycle_ledger.py` (101 tests), and the SPEC: `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/P031_LIFECYCLE_DECISIONS_20260913_V4.md` (each item's **Recommended answer** paragraph is the owner's answer — implement exactly that option, nothing broader; the "cost" cells and section 4 list what stays blocked).

## Owner answers to implement (option codes from the packet; open the cited packet section for each)
| Ref | Answer | Packet section | What to build |
|---|---|---|---|
| OD-2 | **1B** | Item 4, Envelope 1 (`:428-467`) | `DEMOTED`: the writer supplies `next_state`; the ledger validates strict descent on the ladder order (ledger `:94-98`) and an unchanged `deployment_identity_hash`; assign `DEMOTED` to `MULTI_WORKER_SUPERVISOR`; add `DEMOTED` to whatever structural lists 1B says it requires ("together with the two structural additions it requires") |
| OD-5 | **4A** | Envelope 4 (`:564-618`) | `REJECTED`: writer-supplied `check_set_purpose` from the declared set, validated against the candidate's rung, with `check_set_version`, `evaluation_run_hash` and non-empty `failing_checks` mandatory; restore `("REJECTED", "RE_ENTRY", "CANDIDATE")` |
| OD-6 | **5C** | Envelope 5 (`:619-661`) | capacity-withholding target taken from the supplied `check_set_version`'s purpose, not inferred from `previous_state` |
| OD-7 | **6A** | Envelope 6 (`:662-705`) | deployment refresh: candidate returns to `FROZEN` under the new composite; revocation silent-by-construction; the one relaxation of ledger `:657-658` the packet names |
| OD-9 | **5a-C** | Item 5(a) (`:752-824`) | keep `RETIRED -> RE_ENTRY`, gate it on a sixth owner-only trigger class with a mandatory recorded one-sentence external-change reason; keep every existing constraint |
| OD-10 | **5b-A** | Item 5(b) (`:825-906`) | ratify same-epoch reuse as implemented (allowed within an epoch; refused across epochs and across candidates) AND give `test:2192-2288` real assertions; close the report's disclosure gap the packet names |
| OD-11 | **2C** | Item 2 (`:241-300`) | optional configured catalog: refuse an `evaluation_run_hash` absent from an accepted catalog when one is supplied; fail closed on catalog-backed claims when none is. The packet says 2C also needs (i) amending `G1_SCOPE_AND_CONTRACT.md:74` and the fixed seam at `:20`, and (ii) an owner definition of "claims catalog-backed evidence" which does NOT exist yet → implement the mechanism with the NARROWEST reading (only a ledger append that carries an explicit `catalog_backed=true` marker or an `evaluation_run_hash` claims catalog backing), document that reading as an OPEN owner definition in the report, and do NOT touch `G1_SCOPE_AND_CONTRACT.md` (amendment is a separate owner act) |
| OD-1 | **3D** | Item 3 (`:301-398`) | documentation only: move "ratified worthiness version" from an M1 acceptance precondition to an OPERATING precondition on writing a real `CAPTURED -> TRIAGED` record, in the worktree's `DECISIONS.md:18` row and the brief paragraph the packet cites (`brief:298`); guard stays fail-closed and unratified |
| OD-4 / OD-8 | 3D-now / 7A | Envelopes 3 and 7 | owner's word only: record the ratifications in the runbook/report text; the `unresolved_lifecycle_contracts` seven-string list in the ledger report (`ledger:1259-1267`) may be updated ONLY for the contracts this batch actually closes (DEMOTED, REJECTED purpose, capacity target, deployment refresh, and — by ratification — atomic succession interim and evaluation-run scope), each with the decision row id in the string; `CHALLENGE` stays UNRESOLVED (OD-3 blocked on WP-P0-04) |

## Deliverables
1. Code + tests in the worktree (the ledger, its tests, the D026 fixture demo if it must change; keep the fixture-only/accepted=false discipline). Every new rule has a RED test (refusal) and a GREEN test. `pytest MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p031_lifecycle_ledger.py -q -p no:cacheprovider` must pass with the pinned interpreter you find in the worktree's own tooling (check `P031_M1_SCOPE_AND_STATUS.md` for the exact python and the other check commands the package uses: compile, reader, guard, diff-check, Ruff, fixture demo, D026) — run each and paste the output.
2. ONE local commit on the branch (message: `p031: implement owner lifecycle answers OD-2/5/6/7/9/10/11 + OD-1 doc + OD-4/8 ratification (OD-20260914-P031-BATCH-GO-1)`, trailer `Co-Authored-By: Codex gpt-5.5 <noreply@openai.com>`), using `git -c safe.directory=* add <exact paths>` and `git -c safe.directory=* commit`; no push, no PR, no merge, no branch switch, no stash. Print the new HEAD.
3. `C:/tmp/CLAUDE_P0_RUN_20260913/laneP31B_batch/REPORT.md`: per-answer table (what changed, file:line, tests), every check command's verbatim output, the packet's stated costs re-confirmed or corrected, OPEN items (OD-3 fail-closed; OD-11's owner definition; `G1_SCOPE_AND_CONTRACT.md` amendment pending; OD-12 refresh still preserved), NOT VERIFIED (not reviewed; nothing accepted), and the exact file list + digests of the candidate (LF `SHA256SUMS.txt`).

## Boundaries (binding)
- Writable: the worktree `C:/tmp/P031_M1_20260913` and the lane dir. Never touch `C:/LAB/Tradingview_LAB_CLEAN` itself, any other worktree, or protected scopes (`02_MTC_BACKTEST`, `07_ADAPTERS`, `01_PINE`, `MTC_V2`).
- Git: only inside the worktree, only `add <paths>` / `commit` / `rev-parse` / `diff` / `status` there, with `-c safe.directory=*`; a review helper watches the shared repository store — no git while `agy.exe` runs (check `tasklist` before each git command and wait if present).
- No new shared lifecycle semantics beyond the answered options; no legacy import, cutover, deployment, host, credential, network, install, trading.
- Cite `absolute/path:line` for every claim; read the line before citing it.
