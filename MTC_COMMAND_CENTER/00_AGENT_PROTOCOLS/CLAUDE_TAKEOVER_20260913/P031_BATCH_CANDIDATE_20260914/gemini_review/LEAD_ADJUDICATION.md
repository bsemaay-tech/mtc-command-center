# LEAD_ADJUDICATION — P031_BATCH_GEMINI (gemini-3.8-flash-high detection review of the WP-P0-31 batch candidate 96af3eb6) — 2026-09-14 17:15Z

Conversation 0e705b39-5203-4dbd-a0cb-2e15243739f9, 1358 s (22.6 min — just inside the 1500 s ceiling), wrapper-validated SUCCESS, sentinel present; 37+ native views incl. the full 1428-line diff, the packet excerpt and the candidate ledger at cited lines.

Verdict **REQUEST_CHANGES** — 2 CORRECTION + 3 NIT: J-01 no RED test for either `ACCEPTED_EVALUATION_CATALOG_INVALID` branch; J-02 no RED test for `CATALOG_BACKED_INVALID` (non-bool); J-03 (NIT by the reviewer, **REQUIRED by the Lead**) `catalog_backed=True` with `evaluation_run_hash=None` passes both catalog checks when a catalog is configured — a claim of catalog backing without a hash to verify is a fail-open path in the very rule OD-11 asked to be fail-closed; J-04 batch REPORT cites `:803` (deployment-refresh code) for DEMOTED's identity check, actual lines `:824-825`; J-05 `DEPLOYMENT_REFRESH_IDENTITY_INVALID` RED coverage only for the same-identity branch. Per-item classification: all OD items IMPLEMENTED-AS-ANSWERED (no BROADER THAN ANSWERED); scope confined to the ledger, its tests, one DECISIONS row and one brief paragraph; `CHALLENGE` still UNRESOLVED; `G1_SCOPE_AND_CONTRACT.md` untouched.

Disposition: candidate 96af3eb6 stays NONACCEPTED; correction lane **P31FIX** (Codex Plus gpt-5.5, `laneP31FIX_build/TASK.md`) queued third in `codex_reset_queue_r6.ps1` (after Sol R6 and P1CAP; pool reset 20:09Z): J-01/J-02/J-05 RED tests, J-03 new refusal `CATALOG_BACKED_WITHOUT_EVALUATION_HASH`, J-04 corrected citation in the disposition. Then the T0 roster on the corrected candidate (Sol + Gemini delta now; exact Opus after the Wednesday reset). Adjudicated by Claude Opus 5 Lead (b9df29).
