# WP-P0-31 M1 — owner lifecycle-answer batch candidate `96af3eb61bfdf23f3315ca1d984f19407c02b9d5` — 2026-09-14

Authorization: `OD-20260914-P031-BATCH-GO-1` implementing `OD-20260914-P031-LIFECYCLE-1` (OD-1..OD-12 = packet recommendations). Worktree `C:/tmp/P031_M1_20260913`, branch `feature/p031-m1-20260913-refresh`, reviewed base `c76043b9`, candidate **`96af3eb6`** (backup-pushed under the owner's Q4 YES; no PR, no merge).

Build: lane P31B (Codex Plus gpt-5.5, 10:46-10:55Z, cut by the pool cap after 96 commands, uncommitted) + lane P31B-CONT (15:22-15:40Z; inventoried the uncommitted diff first — `INVENTORY.md` — then finished every item; `PROGRESS.md`, `REPORT.md`). Files: `03_QUANTLENS/tools/p031_lifecycle_ledger.py` (+247/−? see diff), `tests/test_p031_lifecycle_ledger.py`, worktree `DECISIONS.md:18` (OD-1 wording), `11_TRIAGE/MASTER_ARCHITECTURE_AND_IMPLEMENTATION_BRIEF_2026-08-21.md:298` (OD-1 wording). 4 files, +718/−133.

The Codex sandbox could not create the shared index lock (`.git/worktrees/P031_M1_20260913/index.lock: Permission denied`) and had no Python 3.12 (`py -3.12` absent; it verified on 3.14). The Lead inspected the diff (DEMOTED + `LADDER_RANK` strict descent; `REJECTED_PURPOSES_BY_RUNG`; `OWNER_EXTERNAL_CHANGE` + `_is_one_sentence`; optional `accepted_evaluation_catalog` with fail-closed `catalog_backed`; `_resolve_check_set_purpose`), re-ran every check on the pinned **Python 3.12.12**: `pytest … test_p031_lifecycle_ledger.py -q -p no:cacheprovider` → **105 passed, 1 skipped, 157 subtests**; `py_compile` OK; shared contracts **50 passed**; **Ruff 0.16.4** `--select E9,F821,F811` clean; `git diff --check` clean; repo guard **PASS**; then committed the four exact paths (task files excluded).

Status: candidate built, Lead-verified, **NOT reviewed, NOT accepted**. Open (from the packet's own cost cells): OD-3 CHALLENGE stays fail-closed (blocked on WP-P0-04); OD-11 owner definition of "claims catalog-backed evidence" open (narrowest reading implemented); `G1_SCOPE_AND_CONTRACT.md` amendment is a separate owner act; OD-12 refresh preserved. M1 acceptance still needs exact accepted P0-04/P0-13 provenance, the owner-ratified worthiness check-set version (now an OPERATING precondition for real `CAPTURED -> TRIAGED` writes, per OD-1 = 3D), and the full T0 roster: Sol (Codex Plus, after the 20:09Z reset), Gemini corroboration, exact Opus after the Claude Pro weekly reset (this week's slot went to P020 per `OD-20260914-P020-OPUS6-1`).
