# TASK P1CAP - build the READ-ONLY own-account evidence capture tool for WP-P0-12 "Path 1" (owner packet `P012_PATH1_REAL_CAPTURE_PACKET_20260914.md` section 5) -> new tool + tests + REPORT, ONE local commit

> PREPARED 2026-09-14 15:0xZ by the Lead; **RELEASED 15:08Z — owner answered "P1-6 yes" (~15:04Z)**. Worktree: `C:/tmp/P1CAP_20260914` (created by the Lead from current `origin/master`, branch `feature/p012-path1-capture-20260914`).

You are a BUILDER (gpt-5.5 high, Codex Plus). Not a reviewer, not the Lead. Read first: `IBKR_PAPER_BRIDGE/bridge/broker/hyperliquid.py` (the accepted broker: how it calls `Info.user_fills_by_time` and `Info.user_funding_history`, the 2000-element cap, `MAINNET_API_URL`/`TESTNET_API_URL` selection at ~`:2180`), `IBKR_PAPER_BRIDGE/tools/export_mtc_funding.py` module docstring (lines 1-80: what a production-class funding candidate needs), and the intake rules copied to the lane dir as `REAL_OBSERVATION_INTAKE.md`. Cite `absolute/path:line` for every claim; read the line before citing it.

## What to build (narrow)
1. `IBKR_PAPER_BRIDGE/tools/capture_own_account_evidence.py` — a CLI that, given `--network mainnet|testnet --address 0x<40 hex> --coin BTC --start <UTC ISO> --end <UTC ISO> --out <dir>`, performs READ-ONLY Info API queries through the same SDK classes the broker uses (`hyperliquid.info.Info` with the constants module's base URL for the chosen network; no `Exchange`, no key, no signing; refuse if any env var `HL_API_WALLET_KEY` is set — the tool must not even be able to see a key): (a) `user_fills_by_time(address, start_ms, end_ms)` (page until complete; refuse `CAPTURE_REFUSED_TRUNCATED` if a page hits the documented cap — 2000 for fills `HL_FILLS_PAGE_LIMIT` `:1579`, 500 for funding history `HL_INFO_PAGE_LIMIT` `:1584/:1904` — without a following short page); (b) `user_funding_history(address, start_ms, end_ms)`; (c) the account state (`clearinghouseState`-equivalent SDK call the broker uses for `account()`/`positions()`). Every response is stored as IMMUTABLE ORIGINAL BYTES (`*.json` exactly as received, before parsing) with a `.sha256` sidecar and an entry in `CAPTURE_MANIFEST.json` {request (endpoint, body), response sha256, byte length, wall-clock UTC start/end, base_url, sdk_version, tool sha256}. Then re-query (a) and (b) once more and refuse `CAPTURE_REFUSED_REQUERY_MISMATCH` unless the set of fill identities (`tid` or `hash+oid+time`) and funding identities (`hash`+`time`) is identical; record both passes.
2. Per-fill EXTRACTION view (derived, clearly labelled DERIVED, never replacing the bytes): for each fill in the window, one JSON object holding exactly the fields the intake names (`coin`, `fee`, `feeToken`, `time`, `tid` or `hash`+`oid`, `closedPnl` if present, `crossed`/`side`/`px`/`sz` as native strings) plus `capture_sha256` (of the response bytes it came from) and a JSON pointer to the element — so a reviewer can re-locate every value in the original bytes. Same for funding events.
3. Ownership-evidence slot: the tool accepts `--ownership-signature <file>` containing an EIP-191 `personal_sign` signature over a message the tool prints with `--print-ownership-message <run_id>`; it VERIFIES the signature recovers the given address (use `eth_account` already in the lock; no network) and records the result; missing signature = recorded as `OWNERSHIP_EVIDENCE: NOT_PROVIDED`, never invented.
3b. Ownership-signing page `IBKR_PAPER_BRIDGE/tools/path1_sign_ownership.html`: ONE self-contained HTML file (no external scripts, no network, no framework) that, when opened from disk in a browser with a wallet extension, shows the fixed message text for the given address/run_id (query string or two inputs), calls `window.ethereum.request({method:'personal_sign', ...})`, and displays the returned signature for copy-paste; it must never request accounts beyond `eth_requestAccounts`, never send a transaction, never contact any URL. Keep it under 120 lines with comments a reviewer can read in five minutes.
4. Tests `IBKR_PAPER_BRIDGE/tests/test_capture_own_account_evidence.py` with recorded fixtures (NO network in tests; monkeypatch the SDK calls): GREEN on a consistent fixture (manifest, sidecars, extraction, re-query identity); RED arms: truncated page, re-query mismatch, tampered stored bytes vs sidecar, non-2xx/exception, address malformed, a key present in the environment, testnet/mainnet URL mix-up (assert the base URL recorded equals the constant for the requested network). Run the existing Bridge test suite unchanged and paste the count.
5. `REPORT.md` in the lane dir: design conformance table (packet section 5 promise -> file:line), refusal codes, verbatim test output, NOT VERIFIED (no network call was made; nothing accepted; the file-source adapter into `export_mtc_funding.py --mode PRODUCTION` is NOT built — describe the narrowest adapter you would propose, with the exact schema-v10 retained-payload rows it would need, so the reviewers can decide), `SHA256SUMS.txt` (LF).
6. ONE local commit on the branch (message `p012: add read-only own-account evidence capture tool (Path 1)`, trailer `Co-Authored-By: Codex gpt-5.5 <noreply@openai.com>`), exact paths only; no push/PR/merge/stash. Print the new HEAD.

## Boundaries (binding)
- Writable: the worktree and the lane dir `C:/tmp/CLAUDE_P0_RUN_20260913/laneP1CAP_build`. Never touch `C:/LAB/Tradingview_LAB_CLEAN` itself, other worktrees, protected scopes (`02_MTC_BACKTEST`, `07_ADAPTERS`, `01_PINE`, `MTC_V2`), the broker module, `bridge/app.py`, systemd/deploy files, or `export_mtc_funding.py`.
- NO network calls, no credentials, no exchange writes, no key material anywhere (tests must prove the key-refusal). The tool is read-only by construction: it must import nothing from `hyperliquid.exchange`.
- Git only inside the worktree (`-c safe.directory=*`; `add <paths>`, `commit`, `rev-parse`, `diff`, `status`), never while `agy.exe` runs (check `tasklist` first).
- Codex Plus only; do not delegate or call other providers. If the repository disagrees with this brief, the repository wins — record the discrepancy and stop on that item.
