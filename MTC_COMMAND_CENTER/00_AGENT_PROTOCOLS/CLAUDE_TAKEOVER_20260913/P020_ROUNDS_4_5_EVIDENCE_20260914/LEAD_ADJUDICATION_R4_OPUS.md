# LEAD_ADJUDICATION_R4_OPUS — exact claude-opus-5 xhigh T0 round 4 of P020 preselection V1.4 — 2026-09-14 05:48Z

Lane P20O4P on Claude Pro (owner D4 YES = OD-20260914-P020-OPUS4-1), 05:28:34Z-05:45:29Z, 76 turns, exit 0, report `opus/OPUS_T0_REPORT.md` (539 lines, 40 064 B). Pro seven-day utilization 0.76 at start (allowed_warning).

**VERDICT: PASS-WITH-NITS. 0 REQUIRED, 4 NITs.** Identities COMPUTED and MATCH (frozen f839c960…, procedure c2d43525…, tests a1c9a534…, HEAD b9b72f85…, manifest 712ddc27…); 90 tests observed; two byte-identical freezes equal to the lane digest; mandated refusal paths observed; 10 RED arms incl. a live gate-equivalence probe against the real `_execute_trial` (8/8 agree), a frozen-file swap after the token read (execution stays on the accepted bytes), a plan mutation after preflight (all 60 `_profile` calls see the preflight object), a read counter (1 plan read + 1 read per dataset), commit-failure arms (both files ABORTED), a hostile trade container (only `len` touched), seven drift arms, five selection-mutation arms, a hand-written overlapping window (refused). Closure table: R1-R6, N-1..N-5, S1-S5, NIT-1/4, T1-T4 all CLOSED with V1.4 file:line (T3 "CLOSED for the three mandated paths"); no selection rule changed V1.1→V1.4.

Lead check of the NITs against the bytes:
| NIT | Opus claim | Lead check | Disposition |
|---|---|---|---|
| N4-1 | prereg §(d)/(e)/(g) body text carries V1.3 line numbers (`:231` cites `verify_frozen_identity` as `:456-522`, actual `:456-526`; `:233` `dataset_lines` `:839-852`, actual `:858-870`; `frame_from_lines`, `calibration_frame`, `trade_bearing_gate` likewise off by ~19 lines) | CONFIRMED (`grep -n "^def"`: 456 / 843 / 858 / 873 / 909) | NIT — documentation renumbering; carry into the next repair batch, does not block the roster |
| N4-2 | `commit` never truncates after write (`:559-563`), so a concurrent outside write leaves residue past the payload (ARM 6d: rc=0, file unparseable, recorded `calibration_sha256` no longer matches) | CONFIRMED (no `truncate` in `commit:557-566`); same observation as Grok GK20B/GK20C NIT and Opus round-3 NIT-B | NIT — one-line hardening (`handle.truncate()` after each flush); tool never writes concurrently; damage detectable |
| N4-3 | a fault inside `abort` itself can leave a reserved file with no ABORTED record; runbook `:172` / prereg `:489-493` word the ABORTED guarantee unconditionally | CONFIRMED by reading `abort:572-586` (per-handle try/finally, no per-handle except); same residual Grok noted as untested | NIT — one documentation sentence (+ optional per-handle try/except); one-shot property intact (both paths exist → ALREADY_RUN) |
| N4-4 | `"procedure": "PRESELECTION_PREREG_V1.md"` paired with `procedure_sha256` = digest of `preselect_profile.py` (`FROZEN:383-384`); the prereg document is not digest-bound | CONFIRMED (`:383-384`); unchanged from round-3 NIT-A | NIT — naming/binding clarity; the executable procedure IS bound; carry forward |

Roster for V1.4 after this record: Lead REPRODUCED; Grok GK20C NITS; Gemini 3.7 PASS (SATISFIED); **Opus round 4 PASS-WITH-NITS**; Sol round 4 RUNNING. If Sol accepts, the roster is complete and `p020_eligibility_run_v14.ps1` may be started by the Lead (its gate re-checks every verdict). The four NITs are recorded as residuals to fold into the next reviewed change (with the derivation-tool repair), not as a reason to re-freeze before the one-shot: none changes a selection rule, exposes an economic value, or opens a second-selection path (Opus §7 first paragraph). Adjudicated by Claude Opus 5 Lead (session 2c48d1).
