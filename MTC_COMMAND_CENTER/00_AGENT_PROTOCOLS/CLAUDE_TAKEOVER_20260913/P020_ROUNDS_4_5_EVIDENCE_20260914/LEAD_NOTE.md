# WP-P0-20 preselection — rounds 4 and 5 evidence (2026-09-14) — Lead recording note (08:20Z)

State at recording: **V1.5 frozen `b75489841a096f6f7271b12880c10cc318cfc25c8f19f7dedc8711baaefdc17b`; roster 4/5 accepting** (Lead REPRODUCED + end-to-end synthetic smoke; Grok pre-screen NITS; Gemini 3.7 PASS; exact Opus round 5 PASS-WITH-NITS); exact Sol round 5 queued for the Codex Plus reset. No one-shot has run on V1.5. The package remains NONACCEPTED (reporter 8/16).

What happened today (chronological, each file here is the primary record):
1. V1.4 round 4 — full roster accepting (Lead, Grok, Gemini, Opus PWN, Sol PWN): `LEAD_REPRODUCTION_V14.md`, `GROK_V14_PRESCREEN.md`, `LEAD_ADJUDICATION_G37_V14.md`, `LEAD_ADJUDICATION_R4_OPUS.md`, `LEAD_ADJUDICATION_R4_SOL.md`.
2. The V1.4 one-shot (token f839c960…) ABORTED at its first calibration cell: the synthetic instrument record ended exactly where the 15m calibration window began — an input precondition every reviewer had listed under NOT VERIFIED. Nothing economic was computed. `LEAD_TERMINAL_ELIG14.md`; ABORTED files archived (never deleted) under the owner's reset ruling: `ELIG14_ABORTED_ARCHIVE_NOTE.md`.
3. Owner decisions (DECISIONS.md rows): D6 A + end date 2026-05-01 (`OD-20260914-P020-RECORD-V2-1`), D5 A driver family generalization (`OD-20260914-P020-DRIVER-FAMILIES-1`), one more exact Opus review, B YES bounded measurement after eligibility (`OD-20260914-P020-MEASURE-1`).
4. V1.5 build (`REPORT_R6.md`; Codex Plus builder cut by the pool cap after the code was complete, completed by a Grok lane: `P20R6C_COMPLETION_REPORT.md`): instrument record V2, plan/driver re-pin, freeze-time record-interval check + real-stack smoke test, six round-4 NIT repairs, acyclic driver family check + tests, derivation-tool single-read repair (`GROK_DERIVE_REPORT.md` / `LEAD_ADJUDICATION_GKPD.md`). Package digests: `PACKAGE_SHA256SUMS_V15.txt`.
5. Round 5 (`REVIEW_BRIEF.md` — every reviewer must exercise the real-stack input preconditions): `LEAD_REPRODUCTION_V15.md` (incl. the addendum: real driver + real engine + real V2 records on a synthetic in-range frame → gate PASSED; control before the record start → recorded BLOCKED), `GROK_V15_PRESCREEN.md` + `LEAD_ADJUDICATION_GK20D.md` (NITS), `LEAD_ADJUDICATION_G37_V15.md` (PASS), `LEAD_ADJUDICATION_R5_OPUS.md` (PASS-WITH-NITS; item 7 40/40).

Residuals carried (all NIT, none blocking, for the next reviewed change): cost/funding records and plan/runbook interval TEXT still say 2025-09-22T08:00Z (inert; only the instrument interval is enforced — proven by the smoke); stale driver/prereg line citations after D5; `abort` swallows its own I/O error; `open("xb")` is a create not a lock; packaged smoke test weaker than the freeze check; family-set tests ROOT-dependent; error wording; banner; dead parameter. Lesson recorded in the Lead memory: a roster whose members share one NOT VERIFIED line has zero coverage of it — real-stack smoke arms are now mandatory in every T0 brief and were added to the tool at freeze time.

19 files recorded. Nothing here is acceptance; the one-shot and the (now owner-authorized) bounded measurement run only after Sol round 5 accepts.
