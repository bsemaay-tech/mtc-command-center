# T0 review brief (ROUND 5) — WP-P0-20 preregistered benchmark profile-selection procedure, frozen V1.5 (2026-09-14)

You are a fresh, independent T0 reviewer. Your exact role (model and effort) is fixed by the launcher that started you; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory.

## Context of this round
Round 4 (V1.4) was accepted by the full roster (Opus PASS-WITH-NITS N4-1..N4-4, Sol PASS-WITH-NITS 1-5, Gemini PASS, Grok NITS, Lead REPRODUCED). The one-shot then ABORTED at its FIRST calibration cell with `InstrumentRecordRefusal: REFUSED_INSTRUMENT_RECORD_OUT_OF_RANGE`: the pinned synthetic instrument record ended `2025-09-22T08:00:00Z`, exactly the first timestamp of the 15m calibration window (row 2049) — an input precondition every reviewer had listed under NOT VERIFIED ("real profile stack not exercised"). Nothing economic was computed (`_profile` raises before `build_signals`); the shot was reset by owner ruling `OD-20260914-P020-RECORD-V2-1` (ABORTED files archived under `C:/tmp/CLAUDE_P0_RUN_20260913/P020_ELIGIBILITY_RUN_V14/ABORTED_ARCHIVE/`; Lead diagnosis `.../LEAD_TERMINAL_ELIG14.md`). A builder lane (Codex gpt-5.5) then produced V1.5: instrument record V2 (`SYNTH-P020-BOUNDED-INSTRUMENT-V2.json`, `end_exclusive` 2026-05-01T00:00:00Z, owner-ratified), plan/driver re-pin, a freeze-time record-interval check (`PRESELECT_REFUSED_RECORD_INTERVAL`) + a real-stack smoke test, the six round-4 NIT residuals, the owner-authorized driver family-set generalization (`OD-20260914-P020-DRIVER-FAMILIES-1`, acyclic: derived plans carry `derivation.family_order`), and the derivation-tool single-read repair; report `C:/tmp/P020_PRESELECT_20260913/REPORT_R6.md`. **You review V1.5 from scratch, verify every prior finding is still closed, AND — new, mandatory — exercise the real stack's input preconditions yourself (see item 7).**
PRIOR CONTEXT (rounds 1-4): Round 1 (V1.1): both flagships REQUEST_CHANGES (R1–R6, `C:/tmp/P020_PRESELECT_T0_20260913/LEAD_ADJUDICATION_R1.md`). Round 2 (V1.2): Opus PASS-WITH-NITS, Sol REQUEST_CHANGES (S1–S5, `C:/tmp/P020_PRESELECT_T0_R2_20260913/LEAD_ADJUDICATION_R2_SOL.md`). Round 3 (V1.3): Opus PASS-WITH-NITS (`C:/tmp/P020_PRESELECT_T0_R3_20260913/opus/OPUS_T0_REPORT.md`, NIT-A/B/C), Sol REQUEST_CHANGES (`.../sol/SOL_T0_REPORT.md`, findings T1–T4; reconciled in `.../LEAD_ADJUDICATION_R3_SOL.md`). A bounded builder lane (Codex gpt-5.5, no reviewer role) repaired T1–T4 and re-froze as V1.4; its report is `C:/tmp/P020_PRESELECT_20260913/REPORT_R5.md`. (historical)

## Subject (frozen; verify identities before reading anything else)
- Package directory (read-only for you): `C:/tmp/P020_PRESELECT_20260913/`
  - `PRESELECTION_PREREG_V1.md` — artifact version V1.5 (V1, V1.1, V1.2, V1.3, V1.4 superseded)
  - `PRESELECTION_FROZEN.json` — SHA-256 `b75489841a096f6f7271b12880c10cc318cfc25c8f19f7dedc8711baaefdc17b` (verify by hashing; refuse if different)
  - `preselect_profile.py`, `tests/test_preselect.py`, `PRESELECT_RUNBOOK.md`, `REPORT.md`, `REPORT_R2.md`, `REPORT_R3.md`, `REPORT_R4.md`, `REPORT_R5.md`, `REPORT_R6.md`, `SHA256SUMS.txt` (LF; verify every digest against the files)
- Repository worktree (read-only): `C:/P020_IMPL_20260912`, HEAD must be `b9b72f858dc830a9389517f79da5ea3c1fa6122c`. Check it with `git -c safe.directory=* -C C:/P020_IMPL_20260912 rev-parse HEAD` (the command-local `-c safe.directory=*` is allowed because a sandbox may see the worktree as "dubious ownership"; write no git config). Do NOT run git status/diff/add/commit/checkout — a review helper watches the shared repository store.
- Reference gate and driver (CHANGED in V1.5 under D5 A): `C:/tmp/P020_LEAD_20260912/benchmark/run_bounded_benchmark.py` — `_execute_trial`, `_reserve_output_root`, `_validate_plan` (family-set check), `RECORD_PATHS` (instrument V2). Records: `SYNTH-P020-BOUNDED-INSTRUMENT-V2.json` (+ V1 kept as history), `BENCHMARK_PLAN.json` (re-pinned). Derivation tool: `C:/tmp/P020_PLAN_DERIVE_20260913/` (single-read repair, re-pin).
- Pinned interpreter: `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (Python 3.12.12).
- Frozen data: `C:/tmp/P020_DERIVED_20260912/selected_manifest.json` (SHA-256 `712ddc2745c7fec8b3ab52e0e07d42a26d8d2a216ba0ea638192e795aa0b0e9a`) and the five BTCUSDT CSVs it names.
- Authority: `C:/tmp/CLAUDE_TAKEOVER_20260913/P020_HANDOFF.md` lines 53-70 (owner-approved procedure shape); owner decisions D1 Option A (asymmetric 1D window rows 2049-2409 with explicit limitation; "do not change selection rules after seeing results") and D-P20-2 (acceptance reporter `check_p020_acceptance.py` unchanged), quoted verbatim in `REPORT_R2.md` section 0.

## What you must decide (T0 — this artifact controls economic-path evidence)
1. Preregistration correctness (same as round 1): nine OHLC-only GEN_* families / 359 records; ordering by (grid size, strategy_id) and canonical JSON bytes; five timeframes; prefixes 512/1024/2048; calibration windows disjoint from rows 1..2048 (15m/1h/2h/4h 2049-4096; 1D 2049-2409); binary predicate; lexicographically-first complete matching of five distinct families; one-shot 15-trial check; `BENCHMARK_PROFILE_BLOCKED` on any failure. Confirm NO selection rule changed between V1.1 and V1.4 (diff the prereg sections (a)-(c),(g) and the frozen `family_order`, `selection_rule`, `calibration_windows`, `measurement_inputs` against the V1.1 values recorded in `REPORT_R3.md` and the V1.3 values in `REPORT_R4.md`; they must be identical — only `artifact_version`, `procedure_sha256` and the T1–T4 enforcement/docs may differ).
2. Closure of every prior finding (each verified against bytes, with file:line):
   R1/R5 — one shared binary gate (`trade_bearing_gate`) used by both stages of the single transaction, clause-for-clause identical to `_execute_trial:459-493` (schema, null-OHLC, `.copy()`, `before_ohlc` mutation check, `simulate_slice` call signature, `num_trades <= 0 or not event_list or len(event_list) != trade_count`, projection_source check); NO float conversion, serialization, hashing, storage or return of any R value / `stats` field other than `num_trades` / `trade_series`.
   R4 — frames built from isolated window/prefix bytes (`io.StringIO`), each dataset file read once; prereg/runbook wording matches the mechanism.
   R2 — the selection about to be written is re-derived from the in-memory binary matrix and refused on mismatch; records `calibration_sha256`.
   R3 — no overwrite path (`PRESELECT_REFUSED_ALREADY_RUN` via exclusive create).
   R6 — HEAD, `procedure_sha256`, source pins, plan, compatibility, manifest and dataset digests re-verified against FROZEN values before the driver loads; `--freeze` records the observed HEAD.
   N-1..N-5 — runbook refusal table complete; warmup citation lines; LF SHA256SUMS; REPORT.md banner.
   S1 — single `--oneshot` transaction: calibration, matching from the IN-MEMORY matrix, 15 locked trials, both result files written only at the end; no `--calibrate`/`--eligibility` modes.
   S2 — atomic exclusive-create reservation (`open(path, "xb")`) of BOTH output paths after preflight and before the first simulation; handles kept; ABORTED records on later failure; never deleted.
   S3 — each dataset file read exactly once (preflight bytes reused by the frame builders).
   S4 — gate uses `len(trades)` only; unsized series refused; never iterates or materializes R values.
   S5 — runbook distinguishes preflight refusals (nothing written) from recorded BLOCKED/ABORTED outcomes.
   NIT-1 — `assert_disjoint` enforced inside `calibration_frame`. NIT-4 — `procedure_sha256` recorded by `--freeze` (computed last) and re-checked before the driver loads.
   **T1 (round 3, Sol)** — the token is compared against the digest of the SAME bytes that are parsed and executed: exactly one `read_bytes()` of `PRESELECTION_FROZEN.json` on the execution path, `sha256` of that buffer, `json.loads` of that buffer, and that accepted digest carried into BOTH output records; the frozen file is never re-opened or re-hashed after the token check.
   **T2 (round 3, Sol)** — the plan verified in preflight is the plan OBJECT executed: no `load_plan()` / `open` / `read_text` / `read_bytes` of the plan on the execution path after `verify_frozen_identity`; `_profile` receives the verified object.
   **T3 (round 3, Sol)** — a failure during the final two-output commit (first write, second write, flush) leaves BOTH reserved files as `ABORTED` records; handles stay open until commit succeeds; nothing is deleted; no mixed pair (one real payload, one ABORTED) is possible on those paths.
   **T4 (round 3, Sol NIT)** — runbook and prereg split outcomes by phase: before any reservation / partial reservation failure / post-both-reservations transaction failure, with the exact file residue for each.
   Opus round-3 NIT-A/B/C — state whether each is unchanged, addressed, or moot in V1.4 (they were not REQUIRED).
3. Leakage: can any economic outcome influence or be exposed by selection? Can the selection change after results are visible? Can execution run without the digest-bound token? Design and run RED arms that attack the NEW code: e.g. swap the frozen file between the token read and execution (monkeypatched `read_bytes` or a replacement `reserve_outputs`) — execution and both output digests must stay bound to the accepted bytes; mutate `BENCHMARK_PLAN.json` after preflight — `_profile` must see the preflight object; make the second `write` raise, make `flush` raise — both files must end `ABORTED`; an instrumented trade container whose iterator raises must never be iterated; a tampered `preselect_profile.py` byte must refuse `PRESELECT_REFUSED_SOURCE_DRIFT` via `procedure_sha256`; a `read_bytes` counter must show one read per dataset; a gate equivalence probe against `_execute_trial` with doubles.
4. Identity and drift: recompute every frozen hash yourself; the token binds to the exact frozen bytes.
5. Tests: `python -m pytest tests -q -p no:cacheprovider` with the pinned interpreter in the package directory (expected count: as stated in `REPORT_R6.md`; state what you observed); state what you observed. Reproduce `--freeze` yourself in a scratch copy (use command-local `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0=C:/P020_IMPL_20260912` if `git rev-parse` refuses with "dubious ownership"; write no git config) and confirm the digest above; confirm `--oneshot` without a token refuses and `--oneshot` with the dead V1.3 token `2e67f3f4ea357b4b974313ddd4a427564860e5618f24d48758f07b599da4b614` refuses with `PRESELECT_REFUSED_T0_TOKEN_MISMATCH`. Do NOT pass the real V1.5 token to `--oneshot`; do NOT run `run_bounded_benchmark.py --run`.
6. Limitation honesty: the 1D limitation (361 rows, 161 usable after 200-bar warmup) still recorded accurately, labelled accepted by owner decision D1 Option A, printed on every freeze.
7. **Real-stack input preconditions (NEW, mandatory — the round-4 miss):** with the pinned interpreter and the REAL pinned records, load `EconomicRecords.from_record_paths(...)` from `mtc_v2.core.economics` (worktree `C:/P020_IMPL_20260912/MTC_COMMAND_CENTER/01_MTC_PROJECT/00_PYTHON`) and call `records.instrument.for_evaluation(ts, records.runtime_instrument_config or {})` for the FIRST and LAST timestamp of every calibration window and every measurement prefix, read from the frozen manifest's CSVs (pure calls; no `_profile`, no `simulate_slice`, no token) — all ten windows/prefixes must be accepted; then list every other precondition the engine enforces on its inputs that you can find in `_build_profile`/`_records`/`_profile` (record digests, symbol scope, `test_only`, runtime config) and show each holds for the frozen inputs. Verify the tool's own freeze-time check (`PRESELECT_REFUSED_RECORD_INTERVAL`) refuses when a window is moved outside the interval (RED arm on a scratch copy). State explicitly whether any input precondition remains unexercised.
8. **D5 driver change:** `_validate_plan` must accept the current hand-written plan (original five families) unchanged, accept a derived plan carrying `derivation.family_order` with any five distinct families from it, and refuse a family outside `family_order`, a duplicate, and wrong sizes; it must NOT read `PRESELECTION_FROZEN.json` (acyclic). No other driver semantics may change (diff against the V1.4-pinned driver digest `4b293de6…`).
9. **Derivation tool:** single read per input (hash the parsed buffer), `derivation.*_sha256` describe the parsed bytes (probe a swap), `EXPECTED_FROZEN_SHA256` = the V1.5 digest, `derivation.family_order` emitted, refusals, tests.

## Known supplemental observations (not verdicts; verify or contradict them)
- Round-4 NIT residuals now claimed repaired in V1.5: N4-1/Sol-5 line citations, N4-2/Sol-2 commit truncate, N4-3/Sol-3 per-handle abort recovery + doc, N4-4/Sol-1 `prereg_sha256`/`runbook_sha256` + `procedure_code_sha256`, Sol-4 runbook `-p no:cacheprovider`. Verify each in bytes.
- Grok pre-screen of V1.5: `C:/tmp/CLAUDE_P0_RUN_20260913/laneGK20D_grok_v15/GROK_V15_REPORT.md` (if present when you start).
- Grok detection pre-screen of V1.4 (`C:/tmp/CLAUDE_P0_RUN_20260913/laneGK20C_grok_v14/GROK_V14_REPORT.md`): T1–T4 and all earlier findings CLOSED; one NIT unchanged from V1.3 — `open(path, "xb")` is an exclusive create, not an exclusive lock, so a concurrent shared `open(path, "w")` can write into the reserved empty file before the final commit (Windows; not a second-run path in the tool). It also notes an untested nested-failure path: if `ReservedOutputs.abort` itself raises mid-loop, `commit`'s `finally` may close remaining handles without an `ABORTED` record.
- Lead reproduction (`C:/tmp/P020_PRESELECT_T0_R4_20260913/LEAD_REPRODUCTION_V14.md`): 90 tests; deterministic freeze equal to the lane digest; refusal paths observed.

## Report format
Markdown with: verified identities (COMPUTED); a closure table for R1–R6 / N-1..N-5 / S1–S5 / NIT-1 / NIT-4 / T1–T4 / N4-1..N4-4 / Sol-4 / the new interval check / D5 / derivation tool (CLOSED / NOT CLOSED with file:line evidence); findings numbered with severity REQUIRED / NIT and exact file:line; the commands you ran with cwd and exact observed output (RED arms included); a NOT VERIFIED section (non-empty); and a final line exactly `VERDICT: PASS` or `VERDICT: PASS-WITH-NITS` or `VERDICT: REQUEST_CHANGES` or `VERDICT: BLOCK` (BLOCK if you could not execute the mandated commands). Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. The V1.5 token `b75489841a096f6f7271b12880c10cc318cfc25c8f19f7dedc8711baaefdc17b` must not be used to authorize execution on the strength of your report alone; the Lead assembles the roster.
