# LEAD_ADJUDICATION_R4_SOL — exact gpt-5.6-sol xhigh T0 round 4 of P020 preselection V1.4 — 2026-09-14 06:00Z

Lane P20SOL4 on Codex Plus `secondary` (owner H YES pattern; pool had credit after the Sep 14 reset), 05:20:22Z-05:55:53Z, exit 0, 240+ commands, thread 01a09e5b…. The sandbox (`-C <scratch>`, workspace-write) refused every write to the lane directory, so the reviewer left its finished report at `C:/tmp/P020_T0R4_SCRATCH_20260914/SOL_T0_REPORT.md` and quoted its digest in `SOL_LAST_MESSAGE.md`; the Lead copied it byte-for-byte to `sol/SOL_T0_REPORT.md` (sha256 c61038fc55c1e950b44aff66a5706657d4bdd0913a4e67c124e32c88ec402d44 before and after). Launcher defect noted for round 5+: add the lane dir as a writable root or run with cwd = lane dir.

**VERDICT: PASS-WITH-NITS. 0 REQUIRED, 5 NITs.** Identities COMPUTED (all pinned sources hash-verified); 90/90 tests; scratch `--freeze` byte-identical to the reviewed artifact; both mandated refusals observed; independent probes: token-byte binding (frozen swap), verified-plan reuse, one read per dataset, source drift, selection re-derivation, shared-write residue, a double-fault arm (abort-write failure). Closure table: 22 rows, R1-R6 / N-1..N-5 / S1-S5 / NIT-1/4 / T1-T4 all CLOSED with V1.4 file:line (T1 :786-809 + :1062/1078/1106/1135; T2 :489-526 + :1072-1078 + :1093/1126; T3 :557-570 + :572-586; T4 prereg :510-516/:541-553 + runbook :164-180). Selection rules unchanged.

NITs vs the Opus round-4 NITs (Lead cross-check):
| Sol NIT | Opus equivalent | Lead disposition |
|---|---|---|
| 1 token does not bind prereg/runbook bytes (`FROZEN:383-384`; `build_freeze :752-756`) | N4-4 | CONFIRMED; residual (documentation/binding hardening: freeze `prereg_sha256`/`runbook_sha256`, rename code digest) |
| 2 `open('xb')` reserves existence only; commit does not seek/truncate (`:549-555`, `:557-563`); 177 trailing bytes observed | N4-2 (and Grok GK20B/C NIT) | CONFIRMED; residual (one-line `truncate()`; not a second-run path) |
| 3 a second failure inside `abort()` stops recovery of later handles (`:572-586`, commit finally `:567-570`) | N4-3 | CONFIRMED; residual (per-handle best-effort recovery + one doc sentence); both paths still exist → re-run refused |
| 4 runbook test command `pytest tests -q` (`PRESELECT_RUNBOOK.md:31-35`) may write `.pytest_cache` while claiming "writes nothing" | — (new) | CONFIRMED by reading the runbook line; residual (publish `-p no:cacheprovider`) |
| 5 prereg prose cites V1.3 line numbers (`:229-242`, `:367-393`) | N4-1 | CONFIRMED; residual (renumber) |

**Roster for V1.4 is COMPLETE and ACCEPTING:** Lead reproduction REPRODUCED; Grok GK20C NITS (0 REQUIRED); Gemini 3.7 PASS (SATISFIED); exact claude-opus-5 round 4 PASS-WITH-NITS; exact gpt-5.6-sol round 4 PASS-WITH-NITS. Both exact flagships accept the same frozen bytes f839c960…. The six distinct residual NITs (N4-1..N4-4 + Sol 4; Sol 1/2/3/5 coincide with Opus) are recorded for the next reviewed change; none changes a selection rule, exposes an economic value or opens a second-selection path. Next step under owner decision D1 Option A ("Re-freeze and complete the required review before execution") and OD-20260913-P020-BENCH-1: the Lead starts `p020_eligibility_run_v14.ps1` (one-shot; its gate re-checks every verdict above). Owner B NO still holds: no bounded measurement after the eligibility result. Adjudicated by Claude Opus 5 Lead (session 2c48d1).
