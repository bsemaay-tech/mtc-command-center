# LEAD_REPRODUCTION_V15 - WP-P0-20 preselection procedure V1.5 (independent Lead reproduction, T0 round 5 roster member)

Performed by Claude Opus 5 Lead (session 2c48d1), 2026-09-14 07:3xZ, in a fresh scratch copy C:/tmp/P020_PRESELECT_LEAD_REPRO_V15_20260913/ (copied from the package after lane P20R6 (Codex Plus, cut by the pool's usage limit after the code was complete) and lane P20R6C (Grok completion: verification runs, REPORT_R6, digests; no code changes)). No Git command other than `git rev-parse HEAD` (command-local safe.directory env; no config written); no canonical-root write; no calibration/eligibility execution (refusal paths and pure calls only); no agy.exe running.

| Step | Command | Observed | Status |
|---|---|---|---|
| 0 | git -C C:/P020_IMPL_20260912 rev-parse HEAD | b9b72f858dc830a9389517f79da5ea3c1fa6122c | EQUAL to pin |
| S | SHA256SUMS.txt checked against the repro copy | 11/11 OK | EQUAL |
| 1 | pinned venv python -m pytest tests -q -p no:cacheprovider | 96 passed in 2.70s (90 in V1.4; +6 for the interval check, smoke arm, NIT repairs) | EQUAL to REPORT_R6 |
| 2 | --freeze twice in the scratch copy | sha256 b75489841a096f6f7271b12880c10cc318cfc25c8f19f7dedc8711baaefdc17b both runs; lane artifact identical | deterministic, EQUAL |
| 2b | LIMITATION line | SHORT_CALIBRATION_WINDOW_1D 1D rows 2049-2409 (361 rows, 161 usable after 200-bar warmup) -- accepted by owner decision D1 Option A | unchanged since V1.1 |
| 4/5 | --oneshot without token; with the dead V1.4 token f839c960... | exit 2 both (NO_T0_REVIEW / T0_TOKEN_MISMATCH per the completion report's captured messages); 0 output files created | refusals work |
| R | RED arm — interval boundary probes on the V2 interval read through the plan pin: 2026-05-01T00:00:00Z REFUSE, 2026-04-30T23:59:59Z OK, 2019-09-07T23:59:59Z REFUSE, 2025-09-22T08:00:00Z OK | all as expected (PRESELECT_REFUSED_RECORD_INTERVAL) | new check works |
| R2 | RED arm — plan pin swapped to the V1 record, `verify_frozen_record_interval(frozen, plan_v1, {})` | PRESELECT_REFUSED_RECORD_INTERVAL: instrument interval drift | fail-closed on record drift |
| P | **Real-stack precondition arm** (the round-4 miss): `EconomicRecords.from_record_paths` over V2 + cost + funding, `for_evaluation(first_ts/last_ts)` for every calibration window (10) and every measurement prefix entry (30) | 40/40 accepted; V1 control refuses 2025-09-22T08:00:00Z | inputs satisfy the engine precondition |
| Δ | frozen V1.4 → V1.5 key diff | differing: artifact_version, supersedes, execution_identity_recheck, instrument_record_interval (new), prereg_sha256/runbook_sha256/procedure_code_sha256 (new; procedure_sha256 removed), source_pins (BENCHMARK_PLAN.json, run_bounded_benchmark.py re-pinned), calibration_windows/measurement_inputs (only the added first_ts/last_ts). family_order, selection_rule, timeframe_order, window rows+hashes, prefix hashes UNCHANGED | no selection-rule change |

Structural checks (grep on the repro copy = lane bytes): interval check defined at :185-240 (instrument_record_interval / assert_instrument_interval / verify_frozen_record_interval), called from build_freeze (:733) and verify_frozen_identity (:628) before reservation; commit truncates after each write (ReservedOutputs.commit); abort has per-handle try/except/finally; freeze records procedure_code_sha256/prereg_sha256/runbook_sha256 (:875-877) and verify_frozen_identity re-checks them (:580-585); runbook test command carries -p no:cacheprovider (:33). Driver: `_validate_plan` keeps the original five-name check for plans without a `derivation` block and checks `derivation.family_order` otherwise (acyclic); benchmark tests 5 passed (completion report). Derivation tool: `load_json_once` single read, `derivation.family_order` emitted, `EXPECTED_FROZEN_SHA256` = V1.5, 15 tests.

NOT VERIFIED by this reproduction: correctness on real datasets (not executed); reviewer verdicts for round 5 (pending: Grok GK20D, Gemini 3.7 V15, Sol R5 on Codex Plus after the 10:04Z reset, exact Opus R5 on Claude Pro under OD-20260914-P020-RECORD-V2-1); whether any engine precondition beyond the instrument interval and the record digests/symbol scope is enforced at first `_profile` (the round-5 briefs ask every reviewer to enumerate them).

Lead reproduction result: REPRODUCED. One roster member only; authorizes no execution. V1.5 token b75489841a096f6f7271b12880c10cc318cfc25c8f19f7dedc8711baaefdc17b stays unused.

## Addendum 08:05Z — real-stack END-TO-END smoke on a synthetic frame (Lead)
Purpose: the round-4 miss was an input precondition enforced deep in the engine; pure `for_evaluation` calls cover the instrument interval but not a full `_profile → build_signals → simulate_slice` pass with the real economics (which calls `instrument.for_evaluation` per market event, `mtc_v2/core/economics.py:427`). So the Lead ran `trade_bearing_gate` with the REAL driver (`load_driver()`), the REAL engine modules (`driver._load_engine()`), the REAL pinned records (V2 instrument, cost, funding via the plan) on a SYNTHETIC 600-bar 15m random-walk frame (schema `timestamp_utc,open,high,low,close`, built through `frame_from_lines`) — no real dataset row, no token, nothing written to the package.
- Timestamps 2025-10-01T00:00Z.. (inside the 15m calibration window's date range; outside the old V1 interval): **GATE PASSED** (trade-bearing on that synthetic walk) — the whole real stack accepts the V2 interval, including the per-event economics path.
- Control, timestamps 2019-09-01.. (before the record start): `BenchmarkProfileBlocked: unsupported P020 profile construction: REFUSED_INSTRUMENT_RECORD_OUT_OF_RANGE …` — in V1.5 the driver wraps the record refusal into a recorded BLOCKED outcome instead of the V1.4 raw abort.
- Consequence for Grok NIT 2 (cost/funding records and the plan's `p020_profile.records.effective_interval` text still say 2025-09-22T08:00Z): not enforced by any engine path (grep: only `instrument.py` checks an interval; smoke passed with those records unchanged) → documentation residual only.
