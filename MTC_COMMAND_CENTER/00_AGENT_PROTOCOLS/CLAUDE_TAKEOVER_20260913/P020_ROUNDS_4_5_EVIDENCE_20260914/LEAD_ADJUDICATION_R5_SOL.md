# LEAD_ADJUDICATION_R5_SOL — exact gpt-5.6-sol xhigh T0 round 5 of P020 preselection V1.5 — 2026-09-14 10:35Z

Lane P20SOL5 on Codex Plus `secondary` (queue-fired at the 10:06Z pool reset; `--add-dir` lane dir so the report landed in place), 10:06:51Z-10:30:45Z, exit 0, report sha256 dc130cc1e9167370… (374 lines).

**VERDICT: PASS-WITH-NITS. 0 REQUIRED, 5 NITs** (1 `xb` create-not-lock; 2 ABORTED-recovery failure suppression vs the runbook's absolute promise; 3 stale live navigation ranges; 4 cost/funding/common interval TEXT still at the former prefix horizon; 5 historical banner not advanced to R6) — all coincide with Opus N5-1..N5-6 / Grok NITs; documentary or fail-closed; none lets an economic value select a family or permits a second selection. Package 96/96, driver 5/5 + current plan validates, derivation 15/15, scratch freeze byte-identical, both unauthorized `--oneshot` arms refused with no outputs. **Item 7 done:** real V2/V1/V1 records loaded through `EconomicRecords.from_record_paths`; 40/40 `for_evaluation` calls accepted at both bounds of all windows/prefixes (table in the report); digests/symbol/scope equal the plan pins; remaining dynamic coverage honestly bounded ("later owner-policy/profile-construction checks inside `_build_profile` remain dynamically unexercised" — the Lead's end-to-end synthetic smoke covered that path: gate PASSED with the real engine).

**Roster for V1.5 is COMPLETE and ACCEPTING:** Lead REPRODUCED (+ end-to-end smoke); Grok GK20D NITS; Gemini 3.7 PASS (SATISFIED); exact Opus round 5 PASS-WITH-NITS; exact Sol round 5 PASS-WITH-NITS. Both exact flagships accept the same bytes b7548984…. Residual NITs recorded for the next reviewed change. The Lead now starts `p020_eligibility_run_v15.ps1` (its gate re-checks every verdict; shot reset per OD-20260914-P020-RECORD-V2-1). Adjudicated by Claude Opus 5 Lead (session 2c48d1).
