You are gemini-3.8-flash-high acting as the independent read-only DELTA reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the O9FIX commit on the WP-P0-30 Shape-B archive exporter: `daf6a43b` → HEAD (the SHA in `subject/COMMIT_HEAD.txt`), written by the Claude Opus 5 Lead as the disclosed builder (owner "all", `OD-20260915-BUILD-ABC-1` item a; the Lead may not accept its own code) to close your predecessor's five NITs K-01..K-05 on `daf6a43b` (`sources/GEMINI_REPORT_daf6a43b_K01_K05.md`). Your job: does the delta close each NIT by the bytes, does it open anything new, does the evidence hold, does it stay inside the brief's ceiling (exactly two files: the exporter and its checker; the collector, the adapter and the contracts module untouched; no design change).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P030_O9FIX_20260915
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DELTA_STAT_daf6a43b_HEAD.txt, subject/COMMIT_HEAD.txt, subject/DELTA_daf6a43b_HEAD.diff (complete), subject/p030_archive_exporter_HEAD.py (complete; 340 lines → three ranges), subject/check_p030_archive_exporter_HEAD.py (complete; 466 lines → four ranges), sources/GEMINI_REPORT_daf6a43b_K01_K05.md (your predecessor's findings K-01..K-05 — sections (c) at minimum; read complete), sources/TASK_O9FIX.md (the brief), sources/DISPOSITION_O9FIX.md (the builder's claims), sources/LEAD_check_p030_archive_exporter.txt, sources/LEAD_RED_ARM_new_checker_old_exporter.txt, sources/LEAD_RUFF_O9FIX.txt, sources/LEAD_check_p030_closed_partition_backup_adapter_TAIL.txt, sources/DECISIONS_rows_P030.md, sources/p030_closed_partition_backup_adapter_HEAD.py (lines 120-160 at minimum: the whole-second timestamp rule K-03 cites), sources/p030_market_data_contracts_HEAD.py (lines 330-380 at minimum: `dataset_content_hash` and its refusals for K-02). OPTIONAL: sources/LEAD_check_market_data_collector.txt, sources/LEAD_check_p030_market_data_contracts.txt, sources/SHA256SUMS.txt, sources/LEAD_GUARD_O9FIX.txt.

TASK. (1) K-01: confirm `_reject_nonfinite_numbers` is now inside a guard that maps `ValueError` to `ExportRefused("source_line_invalid")`, and that the three fixture arms (`1e999`, `NaN` literal, nested `-1e999`) each reach that refusal — say which arm goes through `parse_constant` and which through the post-parse walk. (2) K-02: confirm the `dataset_content_hash(_descriptor(rows), rows)` call is wrapped so `ContractRefused` becomes `ExportRefused("contract_refused")`, that `_descriptor`'s own `mixed_dataset_descriptor` refusal still surfaces with its own code (it is raised inside the same `try`), and that the two fixture arms (duplicate rows; `bar_close_time == bar_open_time`) hit the contracts module's checks named in the test regexes (`duplicate observation_id`; `must be after bar_open_time`) — cite the contracts lines. (3) K-03: confirm `_utc_z` refuses `parsed.microsecond != 0` with code `invalid_timestamp`, matching the adapter's rule (cite the adapter line), and that the test's six arms are correctly classified (two whole-second refusals; four "UTC Z timestamp" refusals — including `2026-09-14T00:00:00` without `Z` and an int). (4) K-04: verify the 16/16 coverage table in the DISPOSITION against the checker: for every `ExportRefused` code raised anywhere in the exporter, name the test that asserts `caught.exception.code == <code>`; flag any code with no such assertion and any test whose assertion could pass for the wrong reason (e.g. the `short_read` second arm's `Path.stat` stub, the `target_unwritable`/`receipt_unwritable` `Path.open` stubs — do they exercise the branches they claim, and do they restore the real `open` for every other path?). (5) K-05: confirm the DISPOSITION corrects the REPORT citation without editing `REPORT.md`, and that its HEAD line numbers are right (`p030_archive_exporter_HEAD.py:223` for the `DATASET_ROW_FIELDS` return; checker `:151-153` for the preservation loop). (6) RED arm: `LEAD_RED_ARM_new_checker_old_exporter.txt` shows the new checker against the OLD exporter failing exactly on K-01 (ERROR `ValueError: non-finite JSON number`), K-02 (ERROR raw `ContractRefused`) and K-03 (FAIL `ExportRefused not raised`) with 17 others passing — consistent with the delta? (7) Scope and evidence honesty: the stat shows exactly two files; the counts (20 tests OK; 44 adapter tests OK; exit 0 on the collector and contracts checks) match the output tails; the builder's note that `ruff format` was deliberately not applied (the base files were not format-clean) — is that acceptable for this lane, or a NIT? (8) New findings: anything the delta opens (e.g. a refusal code that changed meaning, a message regex in a test that is looser than it should be, a wrapped exception that hides a programming error).

OUTPUT (under 2500 words): (a) finding-closure table K-01..K-05 (CLOSED / PARTIAL / OPEN with citations); (b) the 16-code coverage check as a table (code → test → verified / not); (c) new findings numbered with severity REQUIRED / NIT and file:line (packet-relative); (d) EXACT read coverage with ranges and continuations; (e) nonempty NOT VERIFIED (no execution); (f) one fenced JSON object {"part":"P030_O9FIX_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","closed":[...],"findings":[...],"required_scope_unread":[...]}. Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. End with the helper's GEMINI_READ_ONLY_OK sentinel.
