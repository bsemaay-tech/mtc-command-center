You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-30 archive-exporter **T0 repair round 1**: commit `0be0a8aa` on `feature/p030-integrated-20260913` (parent `53b43c21` = a format-only `ruff format` reflow; grandparent `153edee9` = the candidate the exact-Opus reviewer read this morning and returned REQUEST_CHANGES on, with two REQUIRED findings). The Claude Opus 5 Lead wrote the repair as the same implementer as O9FIX (disclosed); the Lead may not accept its own code. Your job is adversarial along four axes: (a) **closure** — does the repair close finding 1 (a refusal after the target was created left a consumable / truncated partition that the backup adapter accepted) and finding 2 (`source_root` containment fail-open: `..` traversal and junction/symlink components accepted, receipt `source_path` lying) BY THE BYTES, with a failing input exhibited for each in the checker? (b) **new holes** — did the staging/publish sequence introduce a new way to leave consumable bytes, a delete code path (forbidden by `opsa_common.py`), a TOCTOU that publishes over an existing file, or a refusal code/class change that breaks the 16-code table and the K-04 count? (c) **fence integrity** — were the carried tests weakened (e.g. the tightened `assertFalse(target.exists())` arms, the K-06 split), and is the format-only commit really reflow-only? (d) **record honesty** — does `LEAD_VERIFICATION_P030_R1.md` claim anything the bytes or the evidence files do not show (RED 7/25, GREEN 25/25, Ruff 0 new, guard PASS, the residual window stated)?

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P030_R1_20260916
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_semantic_53b43c21_0be0a8aa.patch (complete), subject/p030_archive_exporter.py (complete, ~420 lines → three ranges), subject/check_p030_archive_exporter.py (complete, ~780 lines → six ranges), subject/LEAD_VERIFICATION_P030_R1.md (complete), subject/COMMITS.txt, subject/BLOB_OIDS_HEAD.txt, sources/OPUS_T0_REPORT_attempt1_153edee9.md lines 1-60 and 216-300 (identities + the two REQUIRED findings) and 367-417 (NOT VERIFIED + verdict), sources/p030_archive_exporter_153edee9_pre_repair.py lines 110-130 and 280-340 (the pre-repair containment and write paths), sources/p030_closed_partition_backup_adapter_HEAD.py lines 140-180 and 220-250 (the adapter's `_canonical_relative_posix`, `_refuse_source_links` and how `capture_stable_prefix` applies them), sources/opsa_common_HEAD.py lines 1-40 (the no-delete invariant), every sources/LEAD_*.txt (complete). Skim subject/DIFF_format_only_153edee9_53b43c21.patch (at least 2 ranges) and state whether you saw any non-reflow change.

TASK.
1. **Finding 1 closure.** Trace the new write/verify/publish sequence line by line: staging names, `xb` + flush + fsync, re-read against the staging file, receipt written to its staging name, publish order (receipt first, then target), the existence re-checks before each `os.replace`, and every refusal path — for each refusal path state exactly which files can exist afterwards and under which names. Confirm: nothing under the target's own name on any refusal; the leftover is never a partition name; no delete anywhere (`os.remove`/`unlink`/`rmtree`/`shutil.move` absent); the residual window (receipt published, target publish fails) is stated in the code comment and in the record. Name the checker tests that exhibit the failing inputs (mid-write ENOSPC, receipt-write refusal, verify refusal, target-publish refusal, staging leftover → `target_exists`).
2. **Finding 2 closure.** Trace `_source_rel`: the canonical-relative-POSIX rule (compare character by character with the adapter's `_canonical_relative_posix` — list any input the adapter refuses that the exporter accepts, or vice versa), the link/junction walk (compare with `_refuse_source_links` — same walk to the filesystem root?), and the resolved-form check (non-strict resolve — is a missing leaf handled as the record says, and can a non-strict resolve let a dangling link through?). Name the tests (traversal, junction; adapter parity asserted in both).
3. **Codes and counts.** Confirm no refusal code was added or renamed (16 codes), that each existing negative test still targets its code, and that the K-06 split gives one exact message per arm.
4. **Fence integrity.** The two tightened tests (`test_target_and_receipt_unwritable_refusals`, `test_target_reread_byte_identity_refusal`) — do they now assert MORE, never less? Any test deleted or weakened in the patch?
5. **NIT 5 (`exporter_sha256` LF-normalised).** Is the receipt value now the sha256 of the LF bytes on any checkout, and does the test really force a CRLF read (mock scope)? Any way the digest could differ between checkouts still?
6. **Format-only commit.** From the ranges you read: reflow only?
7. **Record honesty.** Check every number in `LEAD_VERIFICATION_P030_R1.md` §3 against the `LEAD_*.txt` files (RED 7 of 25; which 7; GREEN 25 OK; Ruff findings; guard PASS).

OUTPUT (under 2500 words): (a) the refusal-path table (path → files left → names); (b) the exporter-vs-adapter rule comparison table; (c) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) — a REQUIRED finding means REQUEST_CHANGES; (d) EXACT read coverage with ranges and continuations; (e) nonempty NOT VERIFIED (no execution); (f) one fenced JSON object {"part":"P030_R1_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","finding_1":"CLOSED|OPEN|PARTIAL","finding_2":"CLOSED|OPEN|PARTIAL","new_holes":[...],"fence_changes":[...],"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
