You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the Lead-written WP-P0-21 S1 "catalogue-only" slice: commit 701c5ddd on `feature/p021-s1-policy-v1-numbers-20260915` from master `fcac0ac6`, three files under `MTC_COMMAND_CENTER/03_QUANTLENS/tools/` (`p021_readiness_rules.py`, `tests/test_p021_eligibility.py`, `tests/test_strategy_type_policy_set.py`). The Claude Opus 5 Lead wrote it under the owner's word "recommended" (`OD-20260915-P021-S2-RECOMMENDED-1`, answering `P021_OPTIONS_PACKET_S2_20260915.md`); the Lead may not accept its own code. The slice is meant to fold four owner decisions into the rule catalogue and NOTHING else: `gap_ratio_max = 0.0001` on `m2_missing_bar_ratio` (B-02 closed), the B-17 formula pin, the B-13 `ds-v1` digest pin, and the B-05 divergence metric M-C; the hashed policy-set artifact (`strategy_type_policy_set.py`, accepted fixture `shared.gap_ratio_max = None`) must be untouched, and every check must still refuse readiness. Your job is adversarial along three axes: (a) fidelity — does every value/pin in the code equal the owner's recorded words and the packet's text (cite the packet section and the DECISIONS row)? (b) overclaim — could any change make a check look ready, weaken `validate_catalog`, or let the catalogue drift from the accepted policy-set artifact without a stated reason? (c) test honesty — do the updated tests still fence readiness (every test's setUp/tearDown), and are the Lead's RED arms what they claim?

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P021_S1_20260915
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DELTA_fcac0ac6_HEAD.diff (complete), subject/DELTA_STAT_fcac0ac6_HEAD.txt, subject/COMMIT_HEAD.txt, subject/p021_readiness_rules_HEAD.py (complete; ~470 lines → four ranges), subject/test_p021_eligibility_HEAD.py (lines 1-110 — the fence), subject/test_strategy_type_policy_set_HEAD.py (complete), sources/p021_readiness_rules_fcac0ac6.py (lines 40-75 and 160-240 — the policy set, the open/closed numbers and the rules before the slice), sources/strategy_type_policy_set_fcac0ac6.py (lines 20-60 — the shared keys), sources/data_gap_ratio_fcac0ac6.py (the module docstring and the m2 definition — find it with a grep, then view the range), sources/P021_OPTIONS_PACKET_S2_20260915.md (complete), sources/P021_S1_CATALOGUE_SLICE_BRIEF_20260915.md (complete), sources/DECISIONS_rows_P021.md, and every sources/LEAD_*.txt / LEAD_VERIFICATION_P021_S1.md / DIFF_STAT file.

TASK.
1. **Fidelity table.** For each of the four decisions (B-02 value, B-17 pin, B-13 pin, B-05 metric) give: the owner's recorded word (DECISIONS row), the packet text that defined the option, the exact code line(s) in `p021_readiness_rules_HEAD.py` that encode it, and EQUAL / DIFFERENT. A DIFFERENT is REQUIRED. Check especially that `gap_ratio_max` is `0.0001` (not 0.001 or 0.0), that the formula id names `m2_missing_bar_ratio` and `data_gap_ratio.py` and matches what `data_gap_ratio.py` actually computes for m2 (numerator/denominator/span semantics), that the dataset-hash pin says `ds-v1` and nothing equates it with the P0-20 or P0-30 hashes, and that the M-C definition text records BOTH failure modes as the packet's option M-C describes.
2. **Fence integrity.** Show from the bytes that after the slice: every rule still carries `accepted_corrected_engine.B01` in `missing_rules`; `validate_catalog` still raises for a rule with no missing number and no missing rule; `readiness_record()["ready"]` cannot be True; `EXPECTED_OPEN_NUMBER_NAMES` is exactly the three divergence numbers; `self_check()`'s modified-copy arm still detects a removed refusal wiring (now on `divergence_tolerance`). Any weakening is REQUIRED.
3. **Policy-set artifact.** Confirm `strategy_type_policy_set.py` is NOT in the diff and that the test change in `test_strategy_type_policy_set_HEAD.py` only re-pins the open-number set and adds a comment; state whether the catalogue value (0.0001) vs the artifact's `shared.gap_ratio_max = None` creates a contradiction a consumer could act on, and whether the slice's `s2_scope` text in `POLICY_SET` states that gap honestly.
4. **Tests and RED arms.** Read the updated fence assertions and say whether they would fail on the old catalogue (RED 1 claims 47 failed / 5 passed) and whether the old tests would fail on the new catalogue (RED 2, same numbers); read RED 3 and say whether the mutant is the right mutant (a rule with `missing_rules=()`). Name any assertion that is vacuous.
5. **Scope.** The diff must touch exactly the three files; no protected path; nothing under `02_MTC_BACKTEST`, `07_ADAPTERS`, `01_PINE`, `MTC_V2`. The Ruff note says one F401 (`json` unused in the policy-set test) pre-exists on master — confirm from the fcac0ac6 test bytes in the diff context or say NOT VERIFIED.
6. **EXACT read coverage** (file, ranges, continuations) and a non-empty NOT VERIFIED (you cannot execute).

Output: Markdown with the fidelity table (task 1), the fence table (task 2), the artifact statement (3), the test/RED statement (4), the scope statement (5), the coverage list (6), findings numbered with severity REQUIRED / NIT and exact file:line, then EXACTLY this fenced JSON block and the sentinel line:
```json
{"part": "P021_S1_GEMINI", "verdict": "PASS|PASS-WITH-NITS|REQUEST_CHANGES|BLOCK", "evidence_class": "SUPPLEMENTAL_UNEXECUTED", "fidelity": {"B-02": "EQUAL|DIFFERENT", "B-17": "EQUAL|DIFFERENT", "B-13": "EQUAL|DIFFERENT", "B-05": "EQUAL|DIFFERENT"}, "findings": [{"id": "...", "severity": "REQUIRED|NIT", "path": "file:line", "description": "..."}], "required_scope_unread": []}
```
GEMINI_READ_ONLY_OK
