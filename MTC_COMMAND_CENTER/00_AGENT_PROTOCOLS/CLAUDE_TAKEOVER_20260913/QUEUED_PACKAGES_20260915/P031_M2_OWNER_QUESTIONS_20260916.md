# WP-P0-31 Milestone 2 — seven new one-liners for the owner (from the design half you allowed with `I A`) — 2026-09-16 11:3x UTC+3

Plain language. What the measurement found: the mapping table the August fold wrote (old 7-stage ladder → new states) does not match the words that are actually in the registry today. Of 63 strategies, only 3 carry a value the table knows; the other 60 say `RESEARCH_BATCH`, `READY_FOR_DETERMINISTIC_REVIEW`, `READY_FOR_PYTHON_PROTOTYPE` or `TRIAGED` — words the table never mentions. Under your rule Q5 ("unmappable → UNKNOWN, listed") the import as ruled would create 3 real seeds and 60 UNKNOWN blockers. Nothing is broken — the table was written from the rules document, not from the data. You decide how to extend it; I do not guess. Every default below = "nothing moves".

| # | Question | Words | What each does (measured) |
|---|---|---|---|
| Q7 | Extend the mapping table to today's words? | **`Q7 A`** = adopt the proposed extension (`RESEARCH_BATCH`→CAPTURED, `TRIAGED`/`READY_FOR_*`→TRIAGED, producer-spec `RESEARCH_GRADE`→CAPTURED, `FORWARD_PAPER_CANDIDATE`→CAPTURED, each with a `legacy:*` tag) · `Q7 B` = same but `FORWARD_PAPER_CANDIDATE` stays UNKNOWN · `Q7 C` = no extension | A: 235 seeds, 28 conflicts (resolved to the lower state, both values kept), 0 UNKNOWN · B: 17 targets blocked for Milestone 3 · C: 3 seeds, 60 blockers. Note: `FORWARD_PAPER_CANDIDATE` is the label the August audit found was minted from classification alone — that is why A maps it to CAPTURED, never CANDIDATE |
| Q8 | Which id becomes the ledger's `candidate_id`? | **`Q8 A`** = registry strategies keep `STG###`; triage candidates keep their `QLR_…` id; producer-spec files join the registry through their folder code (their own `candidate_id` kept as an alias); advisory verdicts attach only to an existing record; variants stay unseeded until they have a family id | The stores use four different id systems. Triage `Stg001` and registry `STG001` differ only by case but are DIFFERENT things (a YouTube intake transcript vs the ADA two-candle strategy) — a careless join would merge them. A keeps every item separate |
| Q9 | Triage: what counts as a "recorded decision"? | **`Q9 A`** = none is recorded → all 172 candidates seed as CAPTURED (the fold's own fallback) · `Q9 B` = treat "Build promotion packet / Run PineTS parity / Start forward paper-trade" (12 rows) as an extract decision → TRIAGED · `Q9 C` = those 12 UNKNOWN | The store has no decision field; B reads a recommendation as a decision |
| Q10 | 43 advisory verdicts whose strategy exists nowhere else | **`Q10 A`** = listed as "tag without a home", no record · `Q10 B` = create a CAPTURED record for each | B invents 43 candidates from an advisory file |
| Q11 | The nine diagnostic labels (`INSUFFICIENT_TRADES` etc.) live in scorecards, not in the five stores | **`Q11 A`** = out of Milestone 2 (scorecards are not migrated; only 3 of the 9 labels occur in data) · `Q11 B` = in scope, design a scorecard→strategy join | A keeps M2 to the five stores |
| Q12 | When two writers disagree, "lower state" means which order? | **`Q12 A`** = REJECTED < PARKED < CAPTURED < TRIAGED < CANDIDATE | Used in the dry run; any other order changes who wins |
| Q13 | Let a seed enter the ledger directly at its mapped state? | **`Q13 A`** = allow a `SEED_IMPORT` initial event at CAPTURED / TRIAGED / CANDIDATE / PARKED / REJECTED, marked `MIGRATED_2026`, counting toward no gate (the schema check widened from `FIXTURE` only) | Today the ledger admits a candidate only at CAPTURED; without this the 3 CANDIDATE seeds cannot be written |

Bold = the reading I would take if asked; none of it moves without your word. The build itself still waits for Milestone 1's acceptance (your Q6 = B). Records: `P031_M2_SOURCE_CENSUS_20260916.md`, `P031_M2_MAPPING_DRY_RUN_20260916.md`, `P031_M2_FIXTURE_DESIGN_20260916.md` (CT13 `QUEUED_PACKAGES_20260915/P031_M2_DESIGN_20260916/`).
