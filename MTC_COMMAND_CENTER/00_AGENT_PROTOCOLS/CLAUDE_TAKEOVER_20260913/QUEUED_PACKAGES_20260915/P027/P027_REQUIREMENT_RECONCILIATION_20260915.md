# WP-P0-27 (OPS-C, repo-root CI) — requirement-by-requirement reconciliation against actual evidence — 2026-09-15

Prepared by the Claude Opus 5 Lead (session 743291) on the owner's 2026-09-12 queue decision (Codex ad-hoc note `20260912-215227-p027-queued-preserve-existing-ci.md`: "preserve existing CI; assess unfinished P0-27 checks individually for possible deferral"). Facts below were read from the repository and GitHub on 2026-09-15 08:00–08:40Z; earlier package records (`11_TRIAGE/WP_P0_27_CI_HOME_2026-08-25/`) are cited where they still hold. This is an assessment; it changes no workflow, no setting and accepts nothing.

## 1. Current state (verified today)
- `origin/master` `fcac0ac6` carries three root workflows, byte-identical in CT13: `ci.yml` (job `bridge` → check context **`Bridge suite (Python 3.12)`**: ubuntu-24.04, hash-locked `requirements.lock`, `compileall`, `pytest IBKR_PAPER_BRIDGE/tests`, failure annotation + step summary "master must not remain red"), `research-gates.yml` (job `checkers`: eight root `check_*.py` gates + `generate_index.py --check`, plus the report-only `check_p020_acceptance.py` summary), `pine-defang-guard.yml` (check **`pine-alert-guard`**: `check_no_pine_alerts.py`, Python 3.11, runs on every push and PR). No `secrets.*`, no `schedule:`, no self-hosted runner in any workflow.
- Ruleset **21444962 "Protect master – required CI"**, enforcement `active`, target branch: `deletion` blocked, `non_fast_forward` blocked, `required_status_checks` = `Bridge suite (Python 3.12)` + `pine-alert-guard` (integration 15368 = GitHub Actions), `strict_required_status_checks_policy: true`; `bypass_actors` empty per `CI_POLICY.md`. Note: `CI_POLICY.md` (2026-08-25) recorded ONE required context (`Bridge suite (Python 3.12)`) and explicitly said `pine-alert-guard` was not yet required; today's ruleset query shows TWO — `pine-alert-guard` was added to the rule after WP-P0-23 delivered it (the progressive policy working as written).
- Run history: every `ci.yml` run on `master` since the first run (2026-09-05) is `success`; across the last 200 CI runs there is **no `failure` conclusion at all** — only `cancelled` runs from the concurrency group on superseded pushes. Last master run: PR #191 merge `fcac0ac6`, Bridge suite 88 s.
- **Until today no run had ever turned RED.** The deliberate-failure demonstration planned in `RED_GREEN_PLAN.md` §3 (branch `demo/wp-p0-27-red-20260825`) was never executed: no such branch or PR exists. → Executed today as PR #192 (section 3).

## 2. Requirement reconciliation (plan §WP-P0-27, lines 590-602 of the master plan, plus the two amendments)
| # | Requirement (plan text, condensed) | Status | Evidence / gap |
|---|---|---|---|
| R1 | Repo-root `.github/workflows/` on GitHub-hosted runners, no secrets, no venue contact | **IMPLEMENTED + VERIFIED** | three workflows; `permissions: contents: read`; no `secrets.` reference; runs on ubuntu |
| R2 | Day-one job: Bridge suite **plus light lint** | **PARTIAL** | Bridge suite ✔ (`pytest IBKR_PAPER_BRIDGE/tests`, hash-locked deps, `compileall`). "Light lint" = `compileall` only; no Ruff step in CI although Ruff 0.16.4 is the local standard (`--select E9,F821,F811`). Deferrable: add one `ruff check --select E9,F821,F811 IBKR_PAPER_BRIDGE` step (workflow-only change, T1). |
| R3 | Checks required on PRs into master | **IMPLEMENTED + VERIFIED** | ruleset 21444962 active, two required contexts, strict up-to-date policy; first protected merge PR #127 (`CI_POLICY.md`); every merge since went through it (PRs #169-#173, #186-#191). |
| R4 | "Direct Lead pushes remain allowed" | **DEVIATES — RECONCILED BY OWNER DECISION** | Direct push to master is rejected (GH013; `CI_POLICY.md` §Live protection); `OD-20260826-4` ("force-push/deletion blocked and no bypass users") and `OD-20260826-6` supersede the plan sentence. Master is PR-only. No action. |
| R5 | A red master run notifies immediately; master never stays red (fix forward or revert) | **IMPLEMENTED; RED path PROBE-VERIFIED (draft PR #192, run 34946092493 — never on master); e-mail delivery OWNER-CONFIRMED only (chat "yes" 2026-09-15 ~08:32Z; no message artifact captured — Gemini F-01/F-02 of the counted corroboration; relabelled 10:25Z from "IMPLEMENTED + VERIFIED")** | `ci.yml` failure step emits `::error` + step summary; GitHub's native Actions failure e-mail is the day-one channel (`CI_POLICY.md` §Day-one check). Master has never been red, so the path never fired. The PR #192 red run is the first real exercise; **e-mail receipt is owner-side evidence** (section 3, owner question). |
| R6 | D026: a deliberately broken test is shown to turn the run RED and trigger the notification | **MISSING → EXECUTED TODAY** | PR #192 `demo/wp-p0-27-red-20260915` (`test_ci_red_probe.py`, `assert False`): see section 3 for the run URL, the failing step, and the merge-blocked state. |
| R7 | Required-check setting on PRs demonstrated | **VERIFIED** | ruleset query + PR #192 `mergeStateStatus` (section 3) + every merged PR since #127 |
| R8 | Guards plug in as their packages deliver: WP-P0-23 no-`alert(` guard | **IMPLEMENTED + REQUIRED** | `pine-defang-guard.yml` → `pine-alert-guard` required |
| R9 | WP-P0-10 golden suite in CI | **NOT IN CI (dependent)** | no golden-suite step; `research-gates.yml` runs the research checkers, not the WP-P0-10 promotion diagnostics suite; add when WP-P0-10's owner names the executable check. |
| R10 | WP-P0-21 admission fixtures in CI | **PARTIAL / dependent** | `check_market_data_collector.py` (P0-21/P0-30 collector) runs in `research-gates`; the three newer root checkers `check_p030_closed_partition_backup_adapter.py`, `check_p030_market_data_contracts.py`, `check_p030_opsa_heartbeat_adapter.py` (merged 2026-09-13) are **not** in any workflow. Cheap addition (three `run:` lines) once P0-30 accepts them. |
| R11 | §9.6 parity set in CI | **NOT IN CI (dependent)** | parity package not delivered; nothing to plug in. |
| R12 | Contract tests in CI | **MISSING (cheap)** | `MTC_COMMAND_CENTER/contracts/tests` (50 tests; the Lead runs them by hand for every P031 commit) is in no workflow. One `pytest MTC_COMMAND_CENTER/contracts/tests -q` step in `research-gates` would close it. |
| R13 | Two inert `02_MTC_BACKTEST/.github/workflows/{parity,tests}.yml` recorded as inert and retired, unported | **RECORDED, NOT RETIRED** | still present; never run (not at repo root). Deletion touches protected `02_MTC_BACKTEST` → owner-gated; harmless meanwhile. Defer to the Q5 engine retirement. |
| R14 | Safety-ops amendment: restore-proof freshness, drill currency, credential-expiry, monitoring-health checks | **DEPENDENT on WP-P0-26** | P0-26's completion-marker/restore repair is unbuilt (checked today: `tools/opsa/backup.py` still has only the append-only manifest); nothing exists to check. Register rows stay `UNKNOWN`. |
| R15 | Delivery-doctrine guards: block concurrent writers on the shared claim; block cleanup when ownership/dependency/purpose is UNKNOWN (D026 each) | **MISSING (planned, not built)** | `AGENTS.md` states it ("WP-P0-27's mechanical claim/liveness check is planned, not built"); depends on WP-P0-05's claim record shape. Candidate for a small, separately scoped lane (script + RED/GREEN), not for this assessment. |
| R16 | Progressive activation, no big-bang | **IMPLEMENTED** | two required contexts; `Research gates` runs but is not required (its checkers are package-owned; making it required is a later owner step per `CI_POLICY.md` §Progressive policy). |
| R17 | No self-hosted runner, no scheduled data jobs, no ported retired workflows | **VERIFIED** | grep: no `self-hosted`, no `schedule:`; inert workflows unported. |
| R18 | Rollback = delete the workflow files | **VALID** | nothing else depends on them. |
| R19 | Acceptance-gate clause 4: WP-P0-10 and WP-P0-23 may not claim continuous protection before P0-27 is accepted | **HOLDS today (to be re-checked at acceptance)** | no merged record found today that describes OPS-C as "built/running/protecting"; WP-P0-23's guard IS required in the ruleset, which is the plan's intended progression, not a protection claim by P0-23; the Lead re-greps `11_TRIAGE`/registers for "continuous protection" claims at the acceptance step. |
| R20 | Failure notification also wired to the WP-P0-26 paging channel "when it lands" (plan line 593) | **DEPENDENT on WP-P0-26** | no paging channel exists yet (P0-26 decision packet §2 D-C); GitHub e-mail is the day-one channel and was confirmed by the owner today. |

## 3. D026 red demonstration executed today (PR #192)
Executed 08:16-08:23Z — full record in `RED_DEMO_EVIDENCE_20260915.md` beside this file. Summary: draft PR #192 from `demo/wp-p0-27-red-20260915` (master + one `assert False` test) → run 34946092493 **FAILURE** at `Run Bridge test suite` naming `test_ci_red_probe`; `gh pr view` → `mergeStateStatus: BLOCKED` with `Bridge suite (Python 3.12): FAILURE`; probe removed (`179890c5`) → run 34946405229 **success** → `mergeStateStatus: CLEAN`; PR closed unmerged, remote and local branch deleted, worktree removed. Master untouched. E-mail receipt confirmed by the owner (~08:32Z, "yes") — the demonstration is complete on both halves.

## 4. Recommendations per remaining item (complete before the milestone vs explicit deferral)
| Item | Recommendation | Consequence if deferred | Completion condition |
|---|---|---|---|
| R6/R5 red demo + notification | DONE today (PR #192); owner confirms the failure e-mail once | none | owner's one-line "email received / not received" |
| R12 contracts tests in CI | **complete now** (workflow-only, T1; one step) | P031's contract fence relies on the Lead's manual run | PR with the step + one green run + a RED arm (a temporarily failing contract test) |
| R10 three P030 checkers in CI | complete **after P030 accepts** | none today | P030 acceptance |
| R2 Ruff step | complete with R12 (same PR) | lint drift found only by the Lead | same PR |
| R9, R11 | **defer** (dependent on WP-P0-10 / parity deliveries) | none | owning package names the check |
| R13 inert workflows | **defer** to the engine retirement (protected scope) | none | owner's Q5 retirement step |
| R14 safety-ops checks | **defer** until WP-P0-26 lands | register rows stay UNKNOWN (already true) | P0-26 acceptance |
| R15 delivery guards | **defer**, separately scoped lane after P0-26 (or earlier if a writer collision recurs) | manual claim discipline continues | script + D026 evidence + T1 review |
| R16 make `Research gates` required | defer until its checkers are package-accepted | none | owner adds the context to 21444962 |

**P0-27 acceptance call:** with R6 executed and R5 confirmed by the owner, the plan's four-part acceptance gate (green root run ✔, red demo ✔, required-check demonstrated ✔, no continuous-protection claims by P0-10/P0-23 before acceptance — P0-23's guard IS required, which is the plan's intended progression) is satisfiable; the remaining items are progressive-activation work by design, not acceptance blockers. The Lead recommends the owner accept WP-P0-27 at the **day-one scope** (R1-R8, R16-R18) explicitly, with R9-R15 recorded as the progressive backlog above. Acceptance of WP-P0-27 is a **T1** call: it rests on the flagship verdict already recorded for the workflow files (`CI_POLICY.md` / lane reports, 2026-08-25) plus the evidence above; this assessment itself is documentary (T2) and its Gemini read-only corroboration does not substitute for that T1 verdict. No acceptance is claimed here.

## 5. NOT VERIFIED
- E-mail delivery of the failure notification (owner-side; owner-confirmed in chat, no immutable artifact such as the message headers is on record yet — capturing one needs the owner's word, since it means reading his mailbox).
- Whether `pine-alert-guard` would block a PR (it is required, but no red run of it has ever happened; the guard's own RED/GREEN evidence belongs to WP-P0-23).
- Runner-side secrets/settings beyond what the API exposes.
