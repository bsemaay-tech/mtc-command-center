You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-26 candidate `8d4056c5` — the LEAD-AUTHORED completion-marker repair of the OPS-A backup/restore tools (slice 1, commit 6ac9cfb7, owner ruling OD-20260915-P026-REPAIR-LEAD-1) plus the P0-30 adapter slice (slice 2, commit 8d4056c5, owner ruling OD-20260915-P026-ADAPTER-LEAD-1). The Lead (Claude Opus 5) wrote every line and may not accept its own code; your report is the T2 detection input to the T1 roster (exact Opus + exact Sol later). Be adversarial: hunt for any way a restore can proceed WITHOUT genuine completion evidence, any way completion evidence can be fabricated or reused, any regression of the tools' existing fences (append-only manifest, no-delete guarantee, confinement, dry-run writes nothing), and any claim in the Lead's verification that the bytes do not support.

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P026_CAND_20260915
Any other path appearing in copied documents is provenance text, never a file to open. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFFSTAT_fcac0ac6_8d4056c5.txt, subject/COMMITS_fcac0ac6_to_8d4056c5.txt, subject/DELTA_fcac0ac6_8d4056c5.diff (complete, 886 lines — this is the whole candidate), subject/backup_8d4056c5.py, subject/restore_8d4056c5.py, subject/opsa_common_8d4056c5.py (all three complete at HEAD), subject/adapter_8d4056c5_EXCERPT_imports_1-60.txt, subject/adapter_8d4056c5_EXCERPT_restore_path_441-780.txt, subject/adapter_6ac9cfb7_PREFIX_EXCERPT_isolated_inputs_561-625.txt, subject/checker_8d4056c5_EXCERPT_fixtures_1-75.txt, subject/checker_8d4056c5_EXCERPT_isolation_test_904-1002.txt, subject/checker_8d4056c5_EXCERPT_mutants_and_new_fence_1130-1265.txt, sources/P026_LOCAL_SCOPE_DECISION_20260907.md, sources/DECISIONS_rows_P026.md, sources/LEAD_VERIFICATION_P026_REPAIR.md, sources/LEAD_RED_ARMS_PREFIX_BACKUP_RESTORE.txt, sources/LEAD_RED_ARM_old_common_reserved_ids.txt, sources/check_p030_closed_partition_backup_adapter_slice2_TAIL.txt, sources/BASE_SHA256SUMS_fcac0ac6.txt. OPTIONAL (read if you need them for a finding): subject/test_opsa_8d4056c5.py (the diff already carries every added test line), subject/DELTA_slice2_6ac9cfb7_8d4056c5.diff (subset of the full diff), sources/LEAD_RED_ARM_old_adapter_new_checker.txt, sources/LEAD_UNITTEST_312_slice2.txt, sources/check_p030_closed_partition_backup_adapter_TAIL.txt (the pre-slice-2 failure), the remaining hash lists.

CONTEXT you may rely on: the scope packet §4 is the behaviour ceiling — per-run immutable RUN_MANIFEST.jsonl + COMPLETE.json written only after every readback hash passes, never rewritten; a partial/interrupted run has NO marker; restore consumes an explicit completed run id and fails closed (no run without matching marker + manifest; no execution on hash mismatch); `--latest` removed; restore without a run id errors; additive only — every other interface unchanged. Slice 2 extends the file ceiling by exactly the two repo-root files (adapter + its checker) per the second ruling; the reserved-store-id guard sits in opsa_common/test_opsa (inside the original ceiling). The P0-30 adapter restores through restore.py from an isolated root it builds itself; slice 2 copies the run's own COMPLETE.json + RUN_MANIFEST.jsonl into that root.

TASK. (1) Clause-by-clause conformance of the candidate to scope packet §2 (file ceiling: exactly six files, zero added), §4 (each clause: IMPLEMENTED-AS-WRITTEN / PARTIAL / BROADER / MISSING with file:line in the packet copies) and §5 (which required test exists for each behaviour; name the test). (2) FAIL-OPEN HUNT on backup.write_completion_evidence, opsa_common.write_once_bytes / load_complete_marker, restore.select_run / verify_completion_evidence / run_restore, and the adapter's _isolated_restore_inputs / _complete_p026_run / restore_verified_prefix: for each of these attack shapes say REFUSED (cite the refusing line) or NOT REFUSED (this is a REQUIRED finding): (a) marker of another run id copied into runs/<id>/; (b) RUN_MANIFEST.jsonl edited after the marker was written; (c) marker whose digest matches a per-run manifest listing fewer/different files than the global manifest for that run; (d) a run with errors>0 or a dry run obtaining a marker; (e) a hand-made runs/<id>/ with a marker but no run_end in the global manifest; (f) a run id matched by prefix or case; (g) a COMPLETE.json whose declared files count disagrees with the per-run manifest; (h) readback != match in the per-run manifest; (i) the adapter path restoring a run the bare `restore.py --run` would refuse (compare the two gates step by step); (j) the adapter fabricating or regenerating completion evidence instead of copying; (k) a store id that collides with the reserved file names; (l) write_once_bytes leaving a partial marker that a later run could mistake for complete (what does a crash between the per-run manifest write and the marker write leave, and does restore then refuse?). (3) REGRESSION HUNT: the global manifest.jsonl record shapes, the backup CLI, dry-run behaviour, the no-delete guarantee (any delete/rename/replace primitive introduced?), path confinement, the heartbeat, and — for the adapter — the isolation guarantees the checker's mutant tests protect (does the tightened shared-archive mutant test still detect the isolation mutant for the right reason, or could a broken restore pass it?). (4) D026 EVIDENCE: for each new refusal, which test shows RED on the pre-fix behaviour according to the Lead's RED-arm outputs; are there refusals with no RED evidence; do the counts in LEAD_VERIFICATION (37→38 unit tests; checker 44 tests 7F+2E → 45 OK; RED 5F+4E on the pre-fix modules; RED 6F+3E for the new checker on the 6ac9cfb7 adapter; reserved-id RED 2 subtests) agree with the output files. (5) REPORT HONESTY: any statement in LEAD_VERIFICATION or the commit messages that the bytes contradict or do not support. (6) Lead authorship: anything you need beyond the exact Opus + exact Sol review.

OUTPUT (under 3500 words): (a) conformance table; (b) fail-open table (a)-(l) with REFUSED/NOT REFUSED + citation; (c) regression findings; (d) D026 table; (e) findings numbered with severity REQUIRED / NIT and file:line (packet-relative); (f) EXACT read coverage with ranges and continuations; (g) nonempty NOT VERIFIED (no execution; excerpts cover only the cited line ranges of the adapter and checker); (h) one fenced JSON object {"part":"P026_CAND_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","not_refused":[...],"findings":[...],"required_scope_unread":[...]}. Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. End with the helper's GEMINI_READ_ONLY_OK sentinel.
