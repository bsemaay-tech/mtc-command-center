# LEAD_ADJUDICATION — P026_CAND_GEMINI (gemini-3.8-flash-high DETECTION review of the WP-P0-26 candidate `8d4056c5`) — 2026-09-15 11:40Z

**Attempt 1 COUNTED** (chain `gemini_retry_chain_v2.py`; probe OK 11:08Z; full call 11:08:53Z–11:23:42Z, 889 s wall / 874 s model; `launch.exit` `exit=0 actual_exit=0`; envelope `status: SUCCESS`, conversation `070eb88b-c6ea-4c7f-9d10-4511f89017ee`; `GEMINI_READ_ONLY_OK` sentinel present; usage input 557 036 / output 47 472 / thinking 33 839; `REVIEW.log` sha256 `18a59299…`; response 26 252 chars sha256 `4a7a2aeb…`).

**Native-read audit (`NATIVE_READ_AUDIT.json`):** 38 native reads; every one of the 20 REQUIRED files read COMPLETE (the 886-line diff in six ranges); five OPTIONAL packet files also read. **One read OUTSIDE the packet:** idx 2, `C:/LAB/Tradingview_LAB_CLEAN/AGENTS.md` (the repository's router file, opened before the packet — the harness's own instruction file; no account data, no subject bytes). Recorded as a boundary breach of the prompt ("Read ONLY files inside this packet"); the verdict is not voided by it because it neither adds subject material nor touches evidence, but it counts against the route's discipline in the ledger.

## Verdict: REQUEST_CHANGES — 1 REQUIRED, 2 NITs; `not_refused: ["e"]`
Fail-open table: 11 of 12 attack shapes REFUSED with correct citations (the Lead re-grepped (a)-(d), (f)-(l): all lines exist and say what the report says). Conformance: all §4 clauses IMPLEMENTED-AS-WRITTEN; §2 six files, zero added; §5 tests present. Regression: none found (manifest record shapes, CLI, dry-run, no-delete, confinement, heartbeat, mutant test reasoning all checked). D026 table: every refusal has RED evidence; all counts in `LEAD_VERIFICATION` reproduced from the output files ("exact match").

| # | Gemini finding | Lead check against the bytes | Disposition |
|---|---|---|---|
| **F-01 REQUIRED** | `restore.py:71-95,193-215` — `verify_completion_evidence` never checks that the global manifest holds a `run_end` for the run with `status == "ok"` and `errors == []`; `run_restore` consults `run_end` only on the zero-file path. A hand-made COMPLETE.json + RUN_MANIFEST.jsonl for an interrupted run (run_start + file records, run_end partial or absent) that matches the global file records passes the gate → partial run restored rc 0. The adapter enforces this (`_complete_p026_run`), the bare tool does not. | **CONFIRMED** — reproduced by the new test `test_hand_made_completion_evidence_cannot_revive_a_run_the_tool_did_not_close` on the `8d4056c5` restore.py: FAILED (3 subtests restored/verified a run the tool never closed). Shape (e) of the fail-open list was genuinely open. | **FIXED in `d81b07f6`** (slice 3): the gate requires exactly one `run_end` for the run with status ok, no errors, and `files` equal to the marker's; refusal `run_not_complete` with a `run_end` detail; the partial-manifest test now meets this gate first and keeps its zero-file fence via a tool-closed empty run; the adapter test that asserted the old fail-open as a precondition now expects rc 3. |
| F-02 NIT | `backup.py:101-112` — `run_end status: ok` appended before `write_completion_evidence`; a failed evidence write leaves "ok" in the append-only log (restore still fails closed). | CONFIRMED (ordering as described). | **FIXED in `d81b07f6`**: evidence written first; a failed write is appended to the run's errors and the run_end says partial. |
| F-03 NIT | `opsa_common.py:236-251` — `load_complete_marker` leaves `files`/readback validation to callers. | CONFIRMED (by design: `restore.verify_completion_evidence` is the gate). | **Documented in `d81b07f6`**: loader docstring states it is the pair-integrity half only and names the single gate; no second validation path was added (one gate, not two). |

## Consequence
Candidate moves from `8d4056c5` to **`d81b07f6`** (39 unit tests OK; adapter checker 45 OK; heartbeat check 0; Ruff clean; guard PASS; RED arm for the new fence: `FAILED (failures=3, errors=1)` on the slice-2 modules — `LEAD_RED_ARM_slice2_restore_backup_new_tests.txt`). A Gemini DELTA review of `8d4056c5..d81b07f6` follows (root `P026_DELTA_GEMINI`); the exact Opus lane 4 and exact Sol review the full candidate. The Lead never accepts its own code.

Recorded by Claude Opus 5 Lead (743291).
