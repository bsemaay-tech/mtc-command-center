You are gemini-3.8-flash-high acting as the independent read-only DELTA reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-26 slice-3 correction `d81b07f6` (vs `8d4056c5`), written by the Claude Opus 5 Lead (disclosed; the Lead may not accept its own code) to answer the counted Gemini DETECTION review of `8d4056c5` (REQUEST_CHANGES: F-01 REQUIRED — the bare restore accepted a hand-made COMPLETE.json + RUN_MANIFEST.jsonl pair for a run whose global manifest never carried a successful `run_end`; F-02 NIT — global `run_end status: ok` appended before the evidence was written; F-03 NIT — the marker loader leaves field validation to callers). Your job: does the delta close each finding by the bytes, does it open anything new, does the evidence hold, does it stay inside the packet's file ceiling and the "additive only" rule.

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P026_DELTA_20260915
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DELTA_fcac0ac6_d81b07f6_STAT.txt, subject/COMMIT_d81b07f6.txt, subject/DELTA_8d4056c5_d81b07f6.diff (complete, 265 lines), subject/restore_d81b07f6.py, subject/backup_d81b07f6.py, subject/opsa_common_d81b07f6.py (complete at HEAD), sources/GEMINI_DETECTION_REPORT_8d4056c5.md (your predecessor's report — sections (b), (e) at minimum; read complete), sources/LEAD_ADJUDICATION_of_the_detection_report.md, sources/LEAD_VERIFICATION_P026_REPAIR.md (the slice-3 section at its end at minimum; read complete), sources/LEAD_RED_ARM_slice2_restore_backup_new_tests.txt, sources/LEAD_UNITTEST_312_slice3_TAIL.txt, sources/check_p030_closed_partition_backup_adapter_slice3_TAIL.txt, sources/DECISIONS_rows_P026.md. OPTIONAL: the hash lists, LEAD_RUFF_slice3.txt, LEAD_GUARD_slice3.txt.

TASK. (1) F-01: quote the new rule in `restore.verify_completion_evidence` (exactly one `run_end` for the run; status ok; errors == []; files equal to the marker's) and re-run attack shape (e) mentally for both arms — an interrupted run (run_end partial with one error) and a crashed run (no run_end at all) — plus these variants: two run_end records for the run (one ok, one partial); a run_end ok whose `files` differs from the marker; a run_end ok with `errors` missing (key absent) or `errors: 0` (int, not list); a run_end for another run id; a global manifest with a malformed line among the run's records. For each: REFUSED (line) or NOT REFUSED. Then judge the new fence `test_hand_made_completion_evidence_cannot_revive_a_run_the_tool_did_not_close` in the diff: does it forge the pair the way an attacker would (digest-bound, matching global file records, readback match), does it cover both arms in both modes, is the RED arm evidence (`LEAD_RED_ARM_slice2_restore_backup_new_tests.txt`: 3 failures + 1 error on the slice-2 modules) consistent with what the old code would do; and does the reworked `test_partial_run_declaring_files_but_having_no_records_fails_closed` still prove the "nothing to verify" fence through a genuinely tool-closed zero-file run rather than through the new gate. (2) F-02: in `backup.run_backup`, confirm the evidence is now written before the terminal record, that a failed evidence write turns into an error of the run and a `partial` run_end, that dry-run still writes nothing, that the record shapes and the CLI are unchanged, and say what a crash BETWEEN the evidence write and the run_end append now leaves (marker present, no run_end) and whether restore refuses it (it must, via F-01). (3) F-03: is the docstring accurate — is `restore.verify_completion_evidence` really the only gate, and does the adapter path go through it (cite the adapter test change in the diff and the detection report's shape (i))? (4) Adapter checker change: the test `test_partial_run_that_p026_check_only_accepts_never_reaches_restore` now expects `RC_CHECK_FAILED` from the bare check-only; confirm this is the correct consequence of F-01 and that the adapter's own envelope fence is still asserted. (5) Scope: the delta touches five files, all inside the two rulings' ceiling; nothing added; no new primitive (delete/rename/replace); no change to the global manifest record shapes. (6) Report honesty: the counts 39 / 45 / RED 3F+1E in `LEAD_VERIFICATION` against the output tails.

NOTE for this re-run: a first attempt (11:35-11:44Z) was voided by a CLI 503 mid-run; its content is not shown to you and must not be assumed; the packet is unchanged. Review afresh. OUTPUT (under 2500 words): (a) finding-closure table F-01/F-02/F-03 (CLOSED / PARTIAL / OPEN with citations); (b) variant table for attack shape (e); (c) new findings numbered with severity REQUIRED / NIT and file:line (packet-relative); (d) EXACT read coverage with ranges and continuations; (e) nonempty NOT VERIFIED (no execution); (f) one fenced JSON object {"part":"P026_DELTA_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","closed":[...],"not_refused":[...],"findings":[...],"required_scope_unread":[...]}. Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. End with the helper's GEMINI_READ_ONLY_OK sentinel.
