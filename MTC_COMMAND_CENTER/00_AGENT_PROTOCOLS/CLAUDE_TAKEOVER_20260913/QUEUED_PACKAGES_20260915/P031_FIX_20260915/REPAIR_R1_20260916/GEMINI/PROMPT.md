You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-31 Milestone-1 **T0 repair round 1**: commit `e9e37aec` on `feature/p031-m1-20260913-refresh` = `48bd70de` (the candidate the exact-Opus reviewer read this afternoon and returned REQUEST_CHANGES on, one REQUIRED finding F-1) + ONE test, no ledger change. F-1: the OD-7 (6A) revocation guard `p031_lifecycle_ledger.py:832-833` (`DEPLOYMENT_IDENTITY_MISMATCH`) is the only line pair that stops an admission from re-using a deployment identity that a registrar refresh just revoked, and no test fenced it — the reviewer's mutant M-2 (guard removed) left all 109 tests green while the ledger re-admitted the revoked identity. The Claude Opus 5 Lead wrote the test (disclosed); the Lead may not accept its own code. Your job is adversarial along three axes: (a) **does the test fence the invariant** — old identity refused with the exact code, candidate still FROZEN, no admission record appended, new identity accepted AND recorded; could it pass for the wrong reason (e.g. refused by another guard earlier, such as `LADDER_IDENTITY_INCOMPLETE`, `PACKAGE_IDENTITY_MISMATCH`, `DEPLOYMENT_IDENTITY_RETIRED`, an envelope guard, or a transition rule), and would it really FAIL on the mutant (reason from the ledger code you read)? (b) **scope** — the diff must touch only the test file, +79/−0; (c) **record honesty** — the Lead's RED/GREEN files must say what the record says (RED: 1 failed / 109 passed, exactly the new test; GREEN: 110 passed).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P031_R1_20260916
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_48bd70de_e9e37aec.patch (complete), subject/DIFF_STAT_48bd70de_e9e37aec.txt, subject/COMMIT_e9e37aec.txt, subject/test_p031_lifecycle_ledger_HEAD_lines_2500-2680.py (complete — the existing refresh test and the new test in context), subject/test_p031_lifecycle_ledger_HEAD_lines_1-420_helpers.py (complete — `event`, `observed_state`, `append_frozen_fixture`, `append_authority_event`, `build_to_rung`), sources/p031_lifecycle_ledger_HEAD_lines_640-880.py (complete — the identity guards incl. `:832-833` = packet line 193-194), sources/OPUS_T0_REPORT_attempt1_48bd70de.md lines 334-417 (F-1 and the NITs' headings) and 597-611 (summary), every sources/LEAD_*.txt (complete).

TASK.
1. Trace the new test against the ledger code: at the moment of the OLD-identity admission, what is `current` (state FROZEN, package PACKAGE_A, deployment DEPLOYMENT_B after the refresh)? Walk the guards in order for a `SHADOW_ELIGIBLE` event from `ENVIRONMENT_ADMISSION_AUTHORITY` with `deployment_identity_hash = DEPLOYMENT_A`: which guard fires FIRST? It must be `:832-833` (`DEPLOYMENT_IDENTITY_MISMATCH`) — if any earlier guard (`LADDER_IDENTITY_INCOMPLETE`, `PACKAGE_IDENTITY_MISMATCH`, the `:675-686` deep-rung block, an envelope/transition rule) would fire first, the test passes for the wrong reason: say so as REQUIRED.
2. On mutant M-2 (the two lines removed), would the OLD-identity admission be ACCEPTED (walk the remaining guards: `DEPLOYMENT_IDENTITY_RETIRED` — is DEPLOYMENT_A retired by the refresh? read the refresh path `:804-818` and any retirement bookkeeping — and `DEPLOYMENT_IDENTITY_BOUND_TO_ANOTHER_CANDIDATE`)? If some other guard would still refuse it on the mutant, the test does not fence `:832-833` specifically: REQUIRED.
3. Does the test assert the whole invariant (four assertions named above) and does the "no admission record appended" assertion really check the replay (not just the current state)?
4. Scope and honesty: diff stat; the Lead's RED file shows exactly one failure and names it; GREEN 110.
5. NITs of the first read (N-1..N-10) are out of this commit's scope; if any of them is actually REQUIRED for M1 acceptance in your reading, say so, but do not re-litigate design notes.

OUTPUT (under 1800 words): (a) the guard-order walk for the OLD-identity admission on the candidate and on the mutant; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) — a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage; (d) nonempty NOT VERIFIED; (e) one fenced JSON object {"part":"P031_R1_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","finding_F1":"CLOSED|OPEN|PARTIAL","first_guard_to_fire":"...","mutant_would_accept":true|false,"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
