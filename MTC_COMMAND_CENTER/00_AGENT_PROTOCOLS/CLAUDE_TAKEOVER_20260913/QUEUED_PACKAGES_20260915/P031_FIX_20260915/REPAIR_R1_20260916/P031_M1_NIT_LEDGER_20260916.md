# WP-P0-31 M1 — NIT ledger from the first exact-Opus read of `48bd70de` (lane 1, 2026-09-16 15:09-15:36 UTC+3) — dispositions by the Lead, 16:0x UTC+3

The REQUIRED finding F-1 is repaired (`e9e37aec`, Gemini delta PASS). The ten NITs are not part of that commit; each gets a terminal disposition here so tonight's second exact-Opus read and Friday's Sol lane see them handled or acknowledged rather than rediscover them. Nothing here changes code on the branch; the M1 candidate stays `e9e37aec`.

| # | NIT (reviewer) | Lead reading | Disposition |
|---|---|---|---|
| N-1 | `DISPOSITION_P31FIX.md` row J-04 says the unchanged-DEMOTED identity is enforced by `:832-833`; in fact `:675-686` (`DEPLOYMENT_REFRESH_ENVELOPE_UNRESOLVED`) always precedes for DEMOTED | correct — a citation error in a record | **record fix** (this session): J-04 row wording corrected to name `:675-686` as the guard that fires, `:832-833` as the FROZEN-path guard (see `P031_FIX_20260915/DISPOSITION_P31FIX.md` addendum) |
| N-2 | `test_demoted_requires_strict_descent_and_keeps_deployment_identity` has no changed-identity arm; the invariant is fenced elsewhere | naming/locality, not a hole | **carried** to the next test-only slice (add one changed-identity arm to the demotion test); not required for M1 acceptance |
| N-3 | OD-6 (5C) made the replay-seam purpose equality unfalsifiable for `ADMISSION_WITHHELD_CAPACITY` (re-derived purpose = stored purpose) | true by construction of 5C; a design consequence the packet did not state | **carried as a design note** for the M1 record set; if the owner wants replay to re-derive the target from `previous_state`, that is an OD-6 amendment (owner) |
| N-4 | an ambiguous `check_set_version` (one version under two purposes) lets the writer name the withheld target; construction validates shape only | configuration-avoidable; a refusal at construction would close it | **carried** as a small engineering follow-up (refuse duplicate version strings across purposes at construction + test); not required for M1 acceptance |
| N-5 | `_is_one_sentence` accepts semicolon run-ons and refuses a sentence containing `v2.0` | true both ways | **owner question** (OD-9 wording): keep the naive rule, or accept decimals/abbreviations and refuse `;` runs — proposed rule: exactly one terminal `.!?` at the END, decimals inside allowed, `;` refused; to be put to the owner with the M1 acceptance packet |
| N-6 | the status report lists ratified contracts for OD-2/4/5/6/7/8/10 but not OD-9/OD-11 and never discloses the configured catalog | report completeness | **carried** as a small engineering follow-up (two report lines); not required for M1 acceptance |
| N-7 | `G1_SCOPE_AND_CONTRACT.md:20/24` still show the pre-batch constructor/append signatures; `:18` requires the file to be updated with the tests | true; the batch REPORT discloses it | **record fix owed on the P031 branch** (docs-only: the two signature lines + an amendment line citing OD-11) — queued as the next docs commit after tonight's read so the pin is not moved again today; Sol Friday reads the amended file |
| N-8 | dead row `"CAPTURED" -> {"worthiness"}` in `REJECTED_PURPOSES_BY_RUNG` (no CAPTURED→REJECTED arc) | harmless dead code | **carried** (remove or add the arc deliberately — owner/design choice: today CAPTURED can only be TRIAGED or DECLINED) |
| N-9 | the mandated contracts invocation fails from the worktree root (`mtc_contracts` import); passes from `MTC_COMMAND_CENTER/contracts` | record defect in the brief/lane instructions | **fixed in the lane 1 brief** (addendum note: run the contracts suite from `MTC_COMMAND_CENTER/contracts`); the Sol generator inherits the brief |
| N-10 | OD-7 × OD-10: after a refresh, the new composite's first admission may cite the same `evaluation_run_hash` that justified the revoked one (epochs turn only at RE_ENTRY) | an open lifecycle semantic the packet never raised | **owner question** for the M1 acceptance packet: should a refresh open a new evaluation epoch (fresh evidence required) — recommended reading: yes, but it is a semantic change and therefore the owner's |

Recorded by Claude Opus 5 Lead (session 6, `4a8233`).

## Addendum 2026-09-17 — NITs of the SECOND exact-Opus read (`e9e37aec`, `ATTEMPT2_REQUEST_CHANGES_e9e37aec/OPUS_T0_REPORT.md`)
The second read closed F-1, re-read N-1..N-10 against the bytes and re-raised none as REQUIRED ("the dispositions are fair"); it noted N-7 is still owed on the branch (as planned: docs commit after the reads) and used the N-9 correction. Its REQUIRED finding F-2 (two unfenced OD-7 refresh-path guards) is repaired as `61c56148` (see `REPAIR_R2_20260917/LEAD_VERIFICATION_P031_R2.md`). Three new NITs:

| # | NIT (reviewer) | Lead reading | Disposition |
|---|---|---|---|
| N-11 | `SHA256SUMS_P31FIX.txt` / `DISPOSITION_P31FIX.md` pin the ledger as `81eedf03…`, which is the hash of the CRLF-mixed checkout (1110 CRLF + 385 LF lines), not of the committed blob (`2bff9fcc…`, pure LF); the test file's two hashes agree because that checkout is pure LF | true; the owner's 08-29 ruling already says pin blob OIDs | **record fix**: blob OIDs recorded in the lane-1 addendum and in `LEAD_VERIFICATION_P031_R2.md` (ledger `7e7874770d…`, tests at `61c56148` `c72452f6…`); the `81eedf03…` line is annotated as a checkout hash wherever it is quoted from now on; no rewrite of the historical record |
| N-12 | the round-1 test's docstring says "Lane-3 exact-Opus review" (it was lane 1) and pins the guard as `:832-833` (drifts) | true both | **fixed in `61c56148`** (docstring: "Lane-1", guard named by its refusal code) |
| N-13 | OD-7 revocation is reversible: after A→B, a second refresh B→A is accepted and the candidate re-climbs under A (arm C5); nothing in the twelve answers says whether "revoked" may be left | true by construction of 6A ("revocation = absence of admission records for the new identity"); a design question | **owner question** with N-10 for the M1 acceptance packet: should a refresh permanently retire the previous identity (recommended reading: no code change until the owner rules; if "yes", the refresh path would add the old identity to `retired_deployments`, which is a semantic change) |

Recorded by Claude Opus 5 Lead (session 6, `4a8233`).
