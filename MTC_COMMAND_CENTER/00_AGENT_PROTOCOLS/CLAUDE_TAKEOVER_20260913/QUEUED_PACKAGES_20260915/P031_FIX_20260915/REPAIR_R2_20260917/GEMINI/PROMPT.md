You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-31 Milestone-1 **T0 repair round 2**: commit `61c56148` on `feature/p031-m1-20260913-refresh` = `e9e37aec` (the candidate the second exact-Opus reviewer read this morning and returned REQUEST_CHANGES on, one REQUIRED finding F-2) + TWO tests and one docstring fix, no ledger change. F-2: two of OD-7's refresh-path identity guards in the registrar FROZEN branch of `p031_lifecycle_ledger.py` — `:812-813` (`DEPLOYMENT_IDENTITY_RETIRED`, a refresh naming an identity this candidate already retired) and `:814-816` (`DEPLOYMENT_IDENTITY_BOUND_TO_ANOTHER_CANDIDATE`, a refresh naming an identity another candidate holds) — had no test: the reviewer's mutants (each guard removed) left all 110 tests green while the ledger accepted the refresh (its arms C1 and C2). The Claude Opus 5 Lead wrote the two tests (disclosed); the Lead may not accept its own code. Your job is adversarial along three axes: (a) **does each test fence its guard** — the refresh refused with the exact code, state / package / identity unchanged, the replay carrying no refresh record (for the foreign case BOTH candidates unchanged); could it pass for the wrong reason (refused earlier by `DEPLOYMENT_REFRESH_IDENTITY_INVALID` `:805-811`, a transition rule, a writer-class rule, `RETIRED_PACKAGE_IDENTITY` `:819-820`, or by the RE_ENTRY / re-freeze / re-admission steps themselves failing for an unrelated reason), and would it really FAIL on its mutant (reason from the ledger code you read: on the `:812-813` mutant, does `:814-816` rescue the retired case — `deployment_owners` maps DEPLOYMENT_A to the same candidate, so it must NOT; does `:819-820` rescue it — the candidate was re-frozen under PACKAGE_B while PACKAGE_A is the retired package, so it must NOT)? (b) **scope** — the diff must touch only the test file, +158/−2, and the −2 must be exactly the two docstring lines of the round-1 test (`Lane-3` → `Lane-1`; the `:832-833` line pin → the refusal-code name); (c) **record honesty** — the Lead's files must say what the record says (REPRO: both mutants 110 passed with no new tests; RED: each mutant 1 failed / 111 passed naming exactly its own test; GREEN: 112 passed, 1 skipped, 167 subtests; contracts 50 passed).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P031_R2_20260917
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_e9e37aec_61c56148.patch (complete), subject/DIFF_STAT_e9e37aec_61c56148.txt, subject/COMMIT_61c56148.txt, subject/test_p031_lifecycle_ledger_HEAD_lines_2580-2830.py (complete — the round-1 fence test with its corrected docstring and the two new tests in context), subject/test_p031_lifecycle_ledger_HEAD_lines_1-420_helpers.py (complete — `event`, `observed_state`, `append_frozen_fixture`, `append_authority_event`, `build_to_rung`), sources/p031_lifecycle_ledger_HEAD_lines_640-880.py (complete — the identity guards; packet lines 173-177 = ledger `:812-816`), sources/p031_lifecycle_ledger_HEAD_lines_1000-1140_replay_bookkeeping.py (complete — where `deployment_owners`, `retired_deployments` and `retired_packages` are filled), sources/OPUS_T0_REPORT_attempt2_e9e37aec.md lines 291-336 (the §5 mutation map) and 403-546 (F-2 and N-11..N-13) and 580-599 (summary), every sources/LEAD_*.txt (complete).

TASK.
1. Trace test 1 (`test_registrar_refresh_cannot_adopt_the_candidates_own_retired_identity`) step by step against the ledger code: SHADOW under DEPLOYMENT_A → RETIRED (supervisor, carries PACKAGE_A / DEPLOYMENT_A) → RE_ENTRY (registrar, OWNER_EXTERNAL_CHANGE, one sentence) → FROZEN under PACKAGE_B (no deployment) → SHADOW_ELIGIBLE under PACKAGE_B / DEPLOYMENT_B → registrar refresh SHADOW→FROZEN naming PACKAGE_B / DEPLOYMENT_A. Would every preparatory step be ACCEPTED on the candidate (name the guard that would refuse it if not)? At the refresh, what is `current` and which guard fires FIRST? It must be `:812-813`.
2. On the `:812-813` mutant, walk the remaining guards for that refresh (`:814-816` with `deployment_owners[DEPLOYMENT_A]`, `:819-820` with `retired_packages`) — would the refresh be ACCEPTED? If some other guard would still refuse it, the test does not fence `:812-813` specifically: REQUIRED.
3. Trace test 2 (`test_registrar_refresh_cannot_adopt_another_candidates_identity`): CANDIDATE_B at SHADOW under PACKAGE_B / DEPLOYMENT_B, CANDIDATE_A at SHADOW under PACKAGE_A / DEPLOYMENT_A, registrar refresh of A naming PACKAGE_A / DEPLOYMENT_B. Which guard fires first on the candidate (must be `:814-816`; `:812-813` must not, DEPLOYMENT_B is not retired); on the `:814-816` mutant would the refresh be ACCEPTED? Do two candidates in one ledger with the same fixture offsets collide on any sequencing / timestamp rule (read the helpers and the bookkeeping)?
4. Does each test assert the whole invariant (exact code; state, package and identity unchanged; replay carries only the genuine FROZEN records — test 1 asserts two, test 2 asserts both candidates' FROZEN records and nothing else)?
5. Scope and honesty as in (b) and (c).
6. NITs N-1..N-13 of the two exact-Opus reads are out of this commit's scope; if any is actually REQUIRED for M1 acceptance in your reading, say so, but do not re-litigate design notes.

OUTPUT (under 2000 words): (a) the guard-order walks for both refreshes on the candidate and on their mutants; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) — a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage; (d) nonempty NOT VERIFIED; (e) one fenced JSON object {"part":"P031_R2_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","finding_F2":"CLOSED|OPEN|PARTIAL","first_guard_to_fire_test1":"...","first_guard_to_fire_test2":"...","mutant_812_would_accept":true|false,"mutant_814_would_accept":true|false,"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
