# P0-12 capture-tool NIT slice - PREPARED, NOT APPLIED, NOT TESTED (2026-09-18 12:0x UTC+3; owner stopped new work Sat 2026-09-19)

`p1cap_nit_patch.py` is the Lead's draft patch for NITs 1-7 of the exact-Opus read of `af921d75` (`OPUS_QUEUE_20260916/P1CAP/LEAD_ADJUDICATION_P1CAP_T0.md`): NIT-1 verify_sidecars covers every sidecar + a recorded derived digest; NIT-2 a descending page is refused; NIT-3 account_state bytes recorded before the shape check; NIT-4 the real r2 capture bytes as a test fixture (`tests/fixtures/p012_path1_r2_capture/`, copied from the run root); NIT-5 a fill without tid is refused (not guessed); NIT-6 malformed signature / bad --verify-existing dir are named refusals (exit 2); NIT-7 the exact signed text + signature stored in the manifest (message format unchanged - changing it is the owner's call).
The worktree `C:/tmp/P1CAP_20260914` is still at `af921d75`, untouched. Session 7: read the patch, apply it, run the Bridge suite (pytest, ASCII basetemp outside C:/tmp, HL_* removed), ruff parity, RED arms on scratch copies UNDER THE PINNED INTERPRETER (see the P0-30 F-1 lesson), guard, commit, Gemini delta, exact-Opus re-read, Sol.
