You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of a Lead-written safety probe for WP-P0-29 DD-06: `IBKR_PAPER_BRIDGE/tools/dd06_agent_withdraw_probe.py` with its fixture tests and the owner-facing step packet. The Claude Opus 5 Lead wrote it under the owner's PREPARATION-only ruling `OD-20260915-P029-DD06-TESTNET-PROBE-PREP-1` (the ruling's text is in the packet); the Lead may not accept its own code; execution needs a second owner word and happens on Hyperliquid TESTNET from the KVM2-P4-03 environment. Your job is adversarial: find every way this probe could (a) touch mainnet or real funds, (b) leak or persist a private key or an unredacted address, (c) move testnet funds away from the account even when the venue does not refuse, (d) misclassify a venue answer (calling a transport failure a refusal, or an acceptance a refusal), (e) leave state behind (a resting order, an approved agent), or (f) tell the owner something the record cannot support.

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/DD06_PROBE_20260915
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/dd06_agent_withdraw_probe.py (complete; ~560 lines → four ranges), subject/test_dd06_agent_withdraw_probe.py (complete; ~350 lines → three ranges), subject/DD06_TESTNET_PROBE_STEP_PACKET_20260915.md (complete), subject/COMMIT_HEAD.txt, subject/DIFF_STAT_fcac0ac6_HEAD.txt, sources/SDK_0_24_0_SIGNATURES.txt (the SDK 0.24.0 signatures plus the source of `approve_agent`, `withdraw_from_bridge` and the HTTP error handler — the ground truth for what the probe calls), sources/bridge_settings_fcac0ac6.py (lines 60-120 at minimum: the credential resolver the probe reuses), sources/smoke_p0_fcac0ac6.py (lines 1-80 and 235-343: the precedent tool that ran on testnet on 2026-09-14), sources/SMOKE_P0_LEAD_NOTE_20260914.md (how the precedent was executed on the host), sources/DECISIONS_rows_DD06.md, sources/LEAD_PYTEST_focused_slice2.txt, sources/LEAD_PYTEST_full_bridge_slice2.txt, sources/LEAD_RUFF_slice2.txt, sources/LEAD_RED_ARM_m1.txt, sources/LEAD_RED_ARM_m2.txt, sources/LEAD_RED_ARM_m3.txt (classifier mutant), sources/LEAD_GUARD_slice2.txt, sources/RECOVERED_RESPONSE_attempt1_VOIDED.md (your predecessor's partial report on 71cde24e — check its four findings are closed by the slice-2 bytes; do not repeat them).

TASK.
1. **Mainnet / real-money reachability.** Trace every path by which `main()` could construct an `Exchange` or `Info` against a non-testnet URL: the `--network` argument, `TESTNET_URL` constant, environment variables, the SDK defaults (`Exchange(wallet, base_url=None, …)` defaults to MAINNET — confirm from the signatures file whether the probe always passes the testnet URL explicitly). State whether `HL_LIVE_ACK` handling is sufficient and whether any code path reads a mainnet credential. Grade REQUIRED if any path reaches mainnet.
2. **Key custody.** The key is read via `resolve_hyperliquid_credentials()`, wrapped into `Account.from_key`, then `del api_key`. Find every place the key or the `LocalAccount` could be serialized: `redact()` inputs, `record.step` payloads, exceptions' `str()`, the `approve_agent` return tuple (the SDK returns `(result, agent_key)` — confirm the probe discards element [1] before recording), `plan_text`, stderr messages. Is the 64-hex regex `(?i)(?:0x)?[0-9a-f]{64,}` enough (private keys are 64 hex; what about base64 or decimal forms — does any SDK error print the key in another form)? Is the final `write_record` guard (refuse to write if a 64-hex survives) sound, and can it itself fail open?
3. **Funds cannot leave.** The transfer arms send to `account_address` (the account's own master address). Judge, from the SDK source in the signatures file: (a) does `withdraw_from_bridge(amount, destination=own address)` mean a bridge withdrawal to the same address on Arbitrum — i.e. funds WOULD leave the venue account if the venue did not refuse? If so, the "moves nothing away from the account" claim in the docstring/packet is WRONG for that arm and must be corrected (a self-destination withdrawal still leaves the exchange); say what a safer arm would be (e.g. an amount below the venue minimum, or `destination` = a burn/unowned address is worse, or skip the withdraw arm's execution and keep only usdSend/spotSend to self). (b) `usd_transfer` / `spot_transfer` to self: same-account, but could the venue treat self-send as invalid for a reason unrelated to agent authority (then a REFUSED is not DD-06 evidence)? Recommend how the record should distinguish "refused because agent" from "refused because self-send" — e.g. by matching the venue's message — and whether the probe's current classification (any `status: err` / 4xx = REFUSED) overclaims.
4. **Classification honesty.** `classify_exception` keys on the exception class NAME (`ClientError`/`ServerError`); `classify_response` on `status`. Enumerate what a venue "must be user"/"does not exist" answer actually looks like in 0.24.0 (from `_handle_exception` and the smoke precedent's `_failure_data`): HTTP 200 with `{"status":"err","response":"…"}` or HTTP 4xx? Does the probe record enough of the message to let the Lead tell an authorization refusal from a validation refusal (min amount, unknown token, nonce)? Grade a NIT or REQUIRED.
5. **State left behind.** Control arm: a resting bid at 90 % of mid, smallest size ≥ $10.5 — can it fill (a 10 % flash move during the seconds it rests)? If it filled, what would the probe do (nothing — say so) and what should the packet tell the owner (flatten by hand)? Cancel failure path: `ABORTED_CONTROL_CANCEL_FAILED` with a loud finding — sufficient? `approveAgent` NOT refused: the venue would then hold an agent whose key was discarded — packet step 4 asks the owner to prune it; is that enough?
6. **Stop rule and tests.** Confirm `STOP_ON_NOT_REFUSED` stops the sequence after the first NOT_REFUSED fund-moving arm and that the RED arm `LEAD_RED_ARM_m1.txt` shows the fence failing when the rule is removed; confirm `LEAD_RED_ARM_m2.txt` shows both the redaction test and the write-once test failing when redaction is removed (the second because `write_record` refuses a surviving 64-hex); `LEAD_RED_ARM_m3.txt` shows the two classification tests failing when `classify_refusal_text` is mutated to return AUTHORIZATION for everything. List any test that passes on a wrong implementation for the wrong reason.
7. **Packet honesty.** Does the step packet describe exactly what the script does (arms, amounts, destinations, exit codes 0/2/3, skip of sub-account)? Does it overclaim what a refusal proves (mainnet parity)? Are the abort conditions complete?
8. **Scope.** `DIFF_STAT` must show exactly two new files under `IBKR_PAPER_BRIDGE/` (tools + tests) and nothing else; no protected path; the Bridge suite result in `LEAD_PYTEST_full_bridge_slice2.txt` must be green with the new tests included.

NOTE for this re-run: a first attempt (15:05-15:18Z) on the previous HEAD 71cde24e was voided by a CLI 503 after a partial report; the Lead applied its two REQUIRED findings and two NITs as slice 2 (HEAD 69377d6b: refusal-text classification AUTHORIZATION / VALIDATION / UNCLASSIFIED with only AUTHORIZATION counting; the withdraw3 wording now says funds WOULD LEAVE the venue if not refused; address guard at write time; packet abort condition for a filled control order). Review the NEW HEAD afresh; the packet is re-staged for it (new PACKET_SHA256SUMS.txt). Task 3(a)'s docstring critique may now be moot — say so if it is.

OUTPUT (under 2500 words): (a) findings numbered with severity REQUIRED / NIT and file:line (packet-relative); a REQUIRED finding means REQUEST_CHANGES; (b) a table of the eight tasks with your verdict per task; (c) EXACT read coverage with ranges and continuations; (d) nonempty NOT VERIFIED (no execution, no venue access, no host access); (e) one fenced JSON object {"part":"DD06_PROBE_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
