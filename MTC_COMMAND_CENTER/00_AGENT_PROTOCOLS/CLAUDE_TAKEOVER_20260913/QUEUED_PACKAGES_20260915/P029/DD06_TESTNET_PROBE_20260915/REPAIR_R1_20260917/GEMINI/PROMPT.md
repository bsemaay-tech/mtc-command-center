You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-29 DD-06 testnet probe **T0 repair round 1**: commit `a46b2a9d` on `feature/p029-dd06-testnet-probe-20260915` = `1af85067` (the slice the exact-Opus reviewer read this evening and returned REQUEST_CHANGES on, four REQUIRED findings) + a tool change and a test change (+349/−13, two files). Context you must weigh: the exact-Opus reviewer's own mutant (the `HL_LIVE_ACK` refusal removed) turned the fixture test that calls the real `main()` into ONE live TESTNET run using the credential in the workstation's user registry (control order placed and cancelled; fund arms refused; nothing moved; the incident record is in the packet). R-2 named the root cause: the suite's offline-ness rested on a single line. The Claude Opus 5 Lead built the slice and this repair (disclosed); the Lead may not accept its own code. Your job is adversarial along four axes: (a) **structural offline-ness** — read `main()`: is `refuse_without_run_token` called AFTER `refuse_unless_testnet` and BEFORE the `resolve_hyperliquid_credentials` call and BEFORE `Info(...)`/`Exchange(...)`? Read the `offline` fixture and `_NeverDial`: does EVERY `main()`-calling test use it; does it replace the resolver at the module the tool imports from (`bridge.settings.resolve_hyperliquid_credentials` — the tool does `from bridge.settings import resolve_hyperliquid_credentials` INSIDE `main`, so a module-attribute patch is picked up at call time; confirm by reading) and both SDK constructors at the modules the tool imports from (`hyperliquid.info.Info`, `hyperliquid.exchange.Exchange` — same inside-`main` import pattern); does it clear `HL_LIVE_ACK`, the token and the `HL_*` variables? Then reason: if a future edit deletes the token gate AND the live-ack refusal, can any test in this file reach a venue? (b) **the four REQUIRED closures** — R-1: are the four guards now fenced (`plan_text` pins the host + gates; the master-key test reaches exit 3 without constructing an SDK object; `write_record` refuses poisoned 64-hex and 0x40-hex; the token gate unit test)? Is `del api_key` now claimed anywhere as a control (it must not be)? R-3: `AUTHORIZATION_MARKERS` no longer contains a bare `"does not exist"`; `VALIDATION_MARKERS` does; the venue's "User or API Wallet ... does not exist" still classifies AUTHORIZATION via `"user or api wallet"`; the two new failing inputs are present. R-4: `_balances`/`_moved`, `S0_agent_balances`, the post-arm re-read of BOTH wallets, the three result labels and their finding texts; can `_moved` return True on a read failure (it must return None when nothing is readable); does the label logic prefer MASTER_FUNDS_MOVED over OWN when both changed (state which); is `agent_address` passed from `main` (`wallet.address`)? (c) **behaviour preservation** — the record's redaction and write-once guards unchanged; the stop rule still stops on ANY NOT_REFUSED arm; the r2/r3-shaped refusals ("Must deposit before performing actions") still classify UNCLASSIFIED; exit codes of `main` unchanged (0 only on `DD06_REFUSALS_OBSERVED`, 2 otherwise, 3 on refusal). (d) **record honesty** — the Lead's `LEAD_*.txt` files must say what the record says (RED: eight mutants each fail exactly their fence, `tripwire_hit=True` for the two removed-gate mutants; GREEN 25 passed; full Bridge suite 1623 passed / 1 skipped; guard PASS).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/DD06_R1_20260917
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_1af85067_a46b2a9d.patch (complete), subject/DIFF_STAT_1af85067_a46b2a9d.txt, subject/COMMIT_a46b2a9d.txt, subject/dd06_agent_withdraw_probe_HEAD_complete.py (complete — ~745 lines: the docstring, the constants and markers, `classify_refusal_text`, `refuse_unless_testnet`, `refuse_without_run_token`, `refuse_master_key`, `_attempt`, `_balances`, `_moved`, `run_probe`, `write_record`, `plan_text`, `main`), subject/test_dd06_agent_withdraw_probe_HEAD_complete.py (complete — ~607 lines), sources/OPUS_T0_REPORT_attempt1_1af85067.md lines 10-36 (the incident) and 185-229 (the findings), sources/INCIDENT_20260917_LANE7_DD06_UNINTENDED_TESTNET_RUN.md (complete), every sources/LEAD_*.txt (complete).

TASK.
1. Trace `main()` on the patched bytes with `--network testnet`, no `HL_LIVE_ACK`, no token: the exact line that refuses and the exit code; confirm no credential read and no SDK object before it. Then with the token set and a resolver that returns a master key: the exact line that refuses.
2. Trace the `offline` fixture: which attributes on which modules it replaces; whether the tool's inside-`main` imports pick them up; whether every `main()` test in the file uses it.
3. Trace `run_probe` on the `_BalancesInfo` + `_leaky_exchange` fixtures for `move="agent"` and `move="master"`: the S0 step data, the post-arm steps, `_moved` results, the label and the finding text. What happens when BOTH change?
4. R-3 and R-1 fences as in (b). Behaviour preservation as in (c). Honesty as in (d).
5. The first read's NITs N-3..N-9 are out of this commit's scope; if any is actually REQUIRED, say so, but do not re-litigate design notes.

OUTPUT (under 2000 words): (a) the traces; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) — a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage; (d) nonempty NOT VERIFIED; (e) one fenced JSON object {"part":"DD06_R1_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","r1":"CLOSED|OPEN|PARTIAL","r2":"CLOSED|OPEN|PARTIAL","r3":"CLOSED|OPEN|PARTIAL","r4":"CLOSED|OPEN|PARTIAL","suite_can_dial_after_two_gates_removed":true|false,"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
