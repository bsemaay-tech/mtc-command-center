# LEAD_VERIFICATION — WP-P0-29 DD-06 testnet probe, T0 repair round 1 (`a46b2a9d`) after the lane-7 exact-Opus read of `1af85067` returned REQUEST_CHANGES and disclosed an unintended testnet run — 2026-09-17 20:5x-22:0x UTC+3

**Trigger:** lane 7 (optional lane, run by hand 18:5x-19:04 UTC+3; launcher exit 0, 81 turns, 229-line report `OPUS_QUEUE_20260916/DD06/ATTEMPT1_REQUEST_CHANGES_1af85067/OPUS_T0_REPORT.md`) → **REQUEST_CHANGES**, four REQUIRED findings, nine NITs, and the §0 incident: the reviewer's mutant M8 (the `HL_LIVE_ACK` refusal removed) turned `test_main_refuses_mainnet_before_reading_any_credential` (which calls the real `main()` with nothing stubbed) into one live TESTNET run with the Windows-host agent-wallet credential from `HKCU\Environment` — a resting testnet BTC bid placed and cancelled, the fund arms refused ("Must deposit"), `DD06_INCONCLUSIVE`; nothing moved. Lead public reads at 19:05 confirm the venue clean (`INCIDENT_20260917_LANE7_DD06/`). The incident is R-2 demonstrated.
**Findings:** R-1 four safety guards unfenced (`TESTNET_URL`, the `refuse_master_key` call in `main`, `del api_key`, the `write_record` hex guards — mutants survive a green suite); **R-2** the suite calls the real `main()` and only one refusal keeps it offline; R-3 `"does not exist"` in `AUTHORIZATION_MARKERS` turns a missing sub-account into AUTHORIZATION-class DD-06 evidence, against the docstring and the packet; R-4 the `NOT_REFUSED` finding text asserts "for an agent wallet" without reading the agent's own balance (the r3 lesson). NITs N-1 dead `FUND_MOVING_ARMS`; N-2 `BaseException` swallows the operator's abort; N-3 `_resting_oid` regex matches a filled status; N-4 `_HEX40` needs the `0x` prefix; N-5 min-notional float edge; N-6 nothing binds execution to KVM2; N-7 `--include-usd-class-transfer` untested / not in the protocol; N-8 brief says "ran once" (three runs); N-9 arm order (owner's call).
**Role:** Lead = builder of the slice (`OD-20260915-P029-DD06-TESTNET-PROBE-PREP-1` and its slices 2-3) and of this repair (disclosed); T0 cap 3 rounds — round 1.

## Repair (`a46b2a9d`, +349/−13, two files)
- **R-2:** `RUN_TOKEN_ENV = "DD06_PROBE_RUN_TOKEN"`; `refuse_without_run_token(run_id, environ)` — the token must equal `--run-id`; `main()` calls it right after `refuse_unless_testnet` and BEFORE `resolve_hyperliquid_credentials` / `Info` / `Exchange`. Tests: an `offline` fixture replaces `bridge.settings.resolve_hyperliquid_credentials`, `hyperliquid.exchange.Exchange` and `hyperliquid.info.Info` with `_NeverDial` tripwires (the tool imports them inside `main`, so module-attribute patches are picked up at call time) and clears `HL_LIVE_ACK`, the token and `HL_*`; every `main()`-calling test uses it. Docstring and `plan_text` name both gates.
- **R-1:** `test_plan_names_the_testnet_host_and_the_gates` (pins the host literal and `TESTNET_URL`); `test_main_refuses_a_master_key_after_the_gates_and_before_any_sdk_object` (token set; the resolver returns a key that derives to the account address → exit 3, tripwires untouched); `test_write_record_refuses_surviving_hex` (64-hex and 0x40-hex poison → `ProbeRefused`, nothing written); `test_run_token_gate_is_a_unit_of_its_own`. `del api_key` kept, not claimed as a control.
- **R-3:** `AUTHORIZATION_MARKERS`: `"does not exist"` → `"user or api wallet"`; `VALIDATION_MARKERS` gains `"does not exist"`; docstring states the rule; two failing inputs in `test_refusal_text_classes` (sub-account / destination "does not exist" → VALIDATION; the venue's signer sentence → AUTHORIZATION).
- **R-4:** `_balances(info, address)` (perp accountValue + spot USDC total as strings, `None` where unreadable), `_moved(before, after)` (`None` when nothing readable), `redact_map`; `run_probe(..., agent_address=None)` records `S0_agent_balances`; after a `NOT_REFUSED` fund-moving arm it re-reads BOTH wallets (`post_<arm>_account_balances`, `post_<arm>_agent_balances`) and sets `DD06_FINDING_MASTER_FUNDS_MOVED` (account changed — the breach), else `DD06_FINDING_OWN_FUNDS_MOVED` (only the agent's own balance changed — the r3 shape), else `DD06_FINDING_NOT_REFUSED` with the readings; `main` passes `agent_address=wallet.address`. N-1: `FUND_MOVING_ARMS` used for the attribution branch, `usdClassTransfer` added. N-2: `KeyboardInterrupt`/`SystemExit` re-raised in `_attempt` and `_balances`.
Ruff: the 8 pre-existing findings only (my new S110/ISC004/I001/PLR0402 hits fixed); both files ruff-formatted (they were at HEAD).

## Evidence
| Arm | Result | File |
|---|---|---|
| RED M2 — `TESTNET_URL` → mainnet URL | 1 failed (`test_plan_names_the_testnet_host_and_the_gates`) / 24 passed | `LEAD_RED_DD06_R1_M2_testnet_url_to_mainnet.txt` |
| RED M3 — `refuse_master_key` call removed from `main` | 1 failed (the master-key test) — **tripwire hit** (`Info` must never be reached), no network | `LEAD_RED_DD06_R1_M3_master_key_call_removed.txt` |
| RED M4 — the 64-hex `write_record` guard removed | 1 failed (`test_write_record_refuses_surviving_hex`) | `LEAD_RED_DD06_R1_M4_hex64_guard_removed.txt` |
| RED M5 — the token gate removed from `main` | 1 failed (the token test) — **tripwire hit** (resolver), no network | `LEAD_RED_DD06_R1_M5_token_gate_removed_from_main.txt` |
| RED M8 — the `HL_LIVE_ACK` refusal removed (the incident's mutant) | 1 failed (`test_refuses_mainnet_and_live_ack`); no tripwire reached — the token gate stops `main` first | `LEAD_RED_DD06_R1_M8_live_ack_refusal_removed.txt` |
| RED R3 — `"does not exist"` back in AUTHORIZATION | 1 failed (`test_refusal_text_classes`) | `LEAD_RED_DD06_R1_R3_does_not_exist_back_in_authorization.txt` |
| RED R4 — `_moved` always False | 2 failed (the OWN / MASTER attribution cases) | `LEAD_RED_DD06_R1_R4_moved_always_false.txt` |
| RED N2 — the abort re-raise removed | 1 failed (`test_operator_abort_stops_the_sequence`) | `LEAD_RED_DD06_R1_N2_abort_swallowed.txt` |
| GREEN — the fixture suite, `HL_*` removed from the env, ASCII basetemp | **25 passed** (16 → 25) | `LEAD_PYTEST_GREEN_R1.txt` |
| Full Bridge suite (same env hygiene) | **1623 passed, 1 skipped, 0 failed** | `LEAD_FULL_BRIDGE_SUITE_R1.txt` |
| Guard | `RESULT: PASS`; staged exactly the two files | `LEAD_GUARD_R1.txt` |
| Gemini delta (`DD06_R1_GEMINI`, packet `DD06_R1_20260917`, 18 files incl. both complete files and the incident record) | COUNTED **PASS-WITH-NITS**, R-1..R-4 CLOSED, `suite_can_dial_after_two_gates_removed: false` (traced to the tripwire line), one NIT (a duplicate assertion, carried); 30 reads / 0 outside / 0 mismatches; 438 s | `GEMINI/` (+ `LEAD_ADJUDICATION.md` there) |
Every mutant run was a file swap of the tool inside the worktree with `HL_API_WALLET_KEY` / `HL_ACCOUNT_ADDRESS` removed from the child environment; the suite's tripwires make a dial impossible even with a gate removed — verified by the two `tripwire_hit=True` runs. No network call was made by the Lead's arms.

## Carried (not part of this commit)
N-3 (`_resting_oid` must require the `resting` key), N-4 (`_HEX40` bare 40-hex), N-5 (min-notional margin), N-7 (`usd_class_transfer` in the protocol + a test), N-8 (brief/record drift: three runs, not one — corrected in the lane addendum), **N-9 arm order → owner one-liner `DD06-ORDER A|B`** (A keep the packet's order withdraw3 first / B run the in-venue self-transfers first so a NOT_REFUSED trips the stop rule before the only arm that can move funds off the venue — rec. B, but it is the owner-approved order today). The `del api_key` line: kept, no claim.

## Roster
Lane 7 re-pinned to `a46b2a9d` with a binding safety rule in the brief addendum (scratch-copy mutants only; fixture suite only; never `main()`/`run_probe` with real SDK objects; no network); attempt 1 archived. The second exact-Opus read runs when the Pro window allows; Sol (Sat) generates from the pin that stands — the Sol generator README now carries the same rule. The Lead never accepts its own code.

Recorded by Claude Opus 5 Lead (session 6, `4a8233`).
