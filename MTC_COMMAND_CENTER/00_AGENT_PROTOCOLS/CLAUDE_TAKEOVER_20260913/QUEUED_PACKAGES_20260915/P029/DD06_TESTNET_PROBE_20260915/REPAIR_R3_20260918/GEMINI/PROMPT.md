You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-29 DD-06 testnet probe **T0 repair round 3**: commit `acd79b52` on `feature/p029-dd06-testnet-probe-20260915` = `2128352b` (round 2, which you reviewed as `DD06_R2_GEMINI`) + the round-3 change (+94/-18, two files). The third exact-Opus read of `2128352b` found one REQUIRED defect, **R-1**: `_paid` counted a key as `readable` per key, and the unified testnet account keeps every dollar in spot USDC while its perp `accountValue` is structurally `0.0` and always parses - so when the account's SPOT read failed after a fund-moving arm (the only balance that can pay on this account), the wallet still counted as measured, `_attribute` printed the flat words "account did not pay" with no UNREADABLE marker, and the OWN branch printed the exclusion "not the DD-06 breach" (sub-case R-1(b): with the account wholly unreadable the exclusion was still printed). The Lead's own unit test pinned that partial case as `False` under a name promising honesty. The T0 round cap (3) was reached; the owner authorized ONE extra round (`OD-20260918-DD06-CAP-A-1`). The Claude Opus 5 Lead built the slice and all three repairs (disclosed); the Lead may not accept its own code. Your job is adversarial along three axes: (a) **R-1 closure** - read `_paid` and `_attribute` in the patch and in the complete HEAD file: `_paid` must return True on any readable DECREASE of at least half the arm amount (a measured decrease is a fact even beside an unreadable key), False ONLY when every key parsed on both sides and none decreased, None when any key was unreadable or unparsable and no readable key decreased, None for an empty reading; `_attribute` must print the r3 exclusion ("not the DD-06 breach") only when `own_paid` is True AND `master_paid is False` (a complete, unchanged account), must yield the new label `DD06_FINDING_OWN_FUNDS_MOVED_MASTER_UNREADABLE` (no exclusion, the account named UNREADABLE) when `own_paid` is True and `master_paid is None`, and the NOT_REFUSED text must name an unmeasured wallet ("NOT measured (UNREADABLE): re-read it by hand"); check the decoy shape explicitly: before `{"accountValue": "0.0", "usdc_total": "983.987457"}`, after `{"accountValue": "0.0", "usdc_total": None}` -> `_paid` must be None, never False; (b) **the tests really pin it** - trace the three new parametrized cases (`agent-pays-master-spot-unreadable`, `master-pays-perp-unreadable`, `nothing-moves-master-spot-unreadable`), the changed expectation of `agent-pays-master-unreadable`, and the `_paid` unit assertions (partial -> None, decoy -> None, readable decrease beside an unreadable key -> True, "n/a" -> None, {} -> None); confirm the reviewer's own probe file `sources/reviewer_test_reviewer_partial3_on_2128352b.py` (which asserts the OLD behaviour and passed 3/3 on `2128352b`, see `sources/LEAD_REPRO_...txt`) can no longer pass on `acd79b52` - say which assertion in each of its three tests now fails and why; (c) **behaviour preservation and honesty** - the two execution gates, the offline test fixture with tripwires, redaction / write-once, the `NOT_REFUSED` stop rule, the ERROR branch (`DD06_INCONCLUSIVE_FUND_ARM_ERROR` + `SKIPPED_AFTER_ERROR`) and the round-2 attribution semantics for complete readings (r3 replay -> OWN, master decrease -> MASTER, tick -> NOT_REFUSED) are unchanged; the Lead's `LEAD_*.txt` must say what the record says (RED: readable-per-key restored -> 4 failed; MASTER_UNREADABLE branch removed -> 2 failed; unparsable counted as readable -> 1 failed; unmeasured note removed -> 1 failed; the reviewer's probe -> 3 failed on the patched code; GREEN 31 passed; full Bridge 1629 passed / 1 skipped; guard PASS).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/DD06_R3_20260918
Any other path appearing in copied documents is provenance text, never a file to open - that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_2128352b_acd79b52.patch (complete), subject/DIFF_STAT_2128352b_acd79b52.txt, subject/COMMIT_acd79b52.txt, subject/dd06_agent_withdraw_probe_HEAD_complete.py (complete - ~840 lines; at least the module docstring and `_balances`, `_paid`, `_attribute`, `_reread_both_wallets`, the arm loop), subject/test_dd06_agent_withdraw_probe_HEAD_complete.py (complete - ~750 lines; at least `_BalancesInfo` through `test_paid_is_directional_amount_aware_and_unreadable_honest`), sources/OPUS_T0_REPORT_attempt4_2128352b.md lines 227-304 (R-1) and 366-423 (NOT VERIFIED + verdict), sources/reviewer_test_reviewer_partial3_on_2128352b.py (complete), every sources/LEAD_*.txt (complete).

TASK.
1. Trace `test_not_refused_fund_arm_names_whose_funds_left[agent-pays-master-spot-unreadable]`: S0 readings, the post-arm readings (`usdc_total` None, `accountValue` "0.0"), `_paid` for the account (-> ?), for the agent (14.0 -> 13.0 vs threshold 0.5 -> ?), the label and the finding text; confirm neither "did not pay" (for the account) nor "not the DD-06 breach" can appear. Then `[master-pays-perp-unreadable]` and `[nothing-moves-master-spot-unreadable]`.
2. Trace the reviewer's probe on `acd79b52`: for each of its three tests name the first assertion that fails.
3. Adversarial: construct an input where the new `_paid` or `_attribute` is still wrong or misleading - e.g. both keys readable but one decreased by a tick and the other by the full amount; the agent partially unreadable while the master paid; `after` missing a key that `before` has; an `own_paid None` with `master_paid False` (what label and text?); say what the code does and whether it matters for the DD-06 conclusion.
4. Preservation and honesty as in (c). Scope: exactly two files.
5. The carried NITs (N-3..N-10, N-13..N-16 of the third read) are out of this commit's scope; if any is actually REQUIRED, say so, but do not re-litigate design notes.

OUTPUT (under 1800 words): (a) the traces; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) - a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage; (d) nonempty NOT VERIFIED; (e) one fenced JSON object {"part":"DD06_R3_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","r1":"CLOSED|OPEN|PARTIAL","partial_unreadable_master_can_read_as_did_not_pay":true|false,"exclusion_possible_without_complete_master_reading":true|false,"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
