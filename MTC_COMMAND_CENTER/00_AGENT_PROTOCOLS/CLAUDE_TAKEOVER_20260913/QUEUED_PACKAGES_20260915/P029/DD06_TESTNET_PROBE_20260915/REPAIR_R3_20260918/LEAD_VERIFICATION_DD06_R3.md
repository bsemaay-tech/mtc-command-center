# LEAD_VERIFICATION - DD-06 testnet probe, T0 repair round 3 (`acd79b52`) - the ONE extra round the owner authorized (`OD-20260918-DD06-CAP-A-1`, chat `A`, 2026-09-18 08:1x UTC+3)

**Trigger:** the third exact-Opus read of `2128352b` (attempt 4, 23:00-23:17 UTC+3 on 09-17; `OPUS_QUEUE_20260916/DD06/ATTEMPT4_REQUEST_CHANGES_2128352b/`; the safety rule held) returned **REQUEST_CHANGES** on one REQUIRED finding, R-1, reproduced by the Lead offline (`ADJUDICATION_R3_20260917/`): `_paid` counted a key as readable per key; the unified account keeps every dollar in spot USDC while its perp `accountValue` is structurally `0.0` and always parses, so a failed SPOT read after a fund-moving arm still counted as measured - "account did not pay", no UNREADABLE marker, and the OWN branch's exclusion "not the DD-06 breach" (also printed with the account wholly unreadable, R-1(b)). My own unit test pinned the partial case as `False`. The T0 round cap (D028: 3) was reached; the Lead's repair authority ended; the owner chose A (one extra round) over B (park for Sol) and C (accept with an operator rule).
**Role:** Lead = disclosed builder (as for rounds 1-2). Scope: exactly the two files.

## Repair (`acd79b52`, +94/-18; tool blob `798b2bb2`, tests blob `3a20f44d`)
- `_paid(before, after, amount)`: True on any readable DECREASE of at least half the arm amount (a measured decrease is a fact beside an unreadable key); False ONLY when every key parsed on both sides and none decreased; None when any key was unreadable (`None`) or unparsable (`InvalidOperation`/`ValueError`) and no readable key decreased; None for an empty reading. Docstring names the decoy shape.
- `_attribute`: the r3 exclusion ("not the DD-06 breach") only when `own_paid` and `master_paid is False` (complete, unchanged account; the sentence now says "fully read balances did not"); `own_paid` with `master_paid is None` -> new label **`DD06_FINDING_OWN_FUNDS_MOVED_MASTER_UNREADABLE`** ("the ACCOUNT was NOT measured (a balance read failed), so nothing is concluded about the master's funds ... re-read the account by hand"); NOT_REFUSED text appends "a wallet was NOT measured (UNREADABLE): re-read it by hand" when either side is None. MASTER branch unchanged. Module docstring updated. The ERROR branch inherits the new label through `_attribute` unchanged.
- Tests: new effects `_agent_pays_master_spot_unreadable` (the reviewer's shape), `_master_pays_perp_unreadable`, `_nothing_master_spot_unreadable`; parametrized cases 5 -> 8 (`agent-pays-master-unreadable` now expects MASTER_UNREADABLE and no "not the DD-06 breach"; `agent-pays-master-spot-unreadable` MASTER_UNREADABLE and no "did not pay"; `master-pays-perp-unreadable` MASTER + "falsified"; `nothing-moves-master-spot-unreadable` NOT_REFUSED + "NOT measured (UNREADABLE): re-read it by hand" and no "account did not pay"); `_paid` unit: partial -> None (flipped from False), decoy -> None for 6.0 and 1.0, readable decrease beside an unreadable key -> True, "n/a" -> None, {} -> None.
- Ruff: 7 findings (all pre-existing, same as `2128352b`); both files ruff-formatted (one reflow of the NOT_REFUSED concatenation); CRLF preserved.

## Evidence (`REPAIR_R3_20260918/`; every mutant on a scratch copy `C:/tmp/LEAD_DD06_SCRATCH_R3_RED/` built from the working files, fixture suite only, `HL_*`/`DD06_PROBE_RUN_TOKEN` removed, basetemps `C:/bt_s4/dd06r3*`; no network, `main()` never run)
| Arm | Result | File |
|---|---|---|
| CONTROL - the third reader's own probe `test_reviewer_partial3.py` (asserts the OLD behaviour; 3 passed on `2128352b`) run on the patched code | **3 failed** (result is MASTER_UNREADABLE; `_paid` decoy -> None; the "did not pay"/"not the breach" assertions fail) | `LEAD_RED_DD06_R3_CONTROL_reviewer_partial_probe_now_fails.txt` |
| R1a - readable-per-key accounting restored (round-2 `_paid`) | **4 failed** (`agent-pays-master-unreadable`, `agent-pays-master-spot-unreadable`, `nothing-moves-master-spot-unreadable`, the `_paid` unit) | `LEAD_RED_DD06_R3_R1a_readable_per_key_restored.txt` |
| R1b - the MASTER_UNREADABLE branch removed | **2 failed** (both unreadable-master cases: "not the DD-06 breach" printed) | `LEAD_RED_DD06_R3_R1b_master_unreadable_branch_removed.txt` |
| R1c - an unparsable string counted as readable | **1 failed** (the `_paid` unit, "n/a") | `LEAD_RED_DD06_R3_R1c_unparsable_counts_as_readable.txt` |
| R1d - the NOT_REFUSED unmeasured note removed (first run survived 31 passed -> the case's phrase was tightened to the note; D026) | **1 failed** (`nothing-moves-master-spot-unreadable`) | `LEAD_RED_DD06_R3_R1d_not_refused_unmeasured_note_removed.txt` |
| Baseline scratch, unmutated | 31 passed | `LEAD_RED_DD06_R3_BASELINE_scratch_unmutated.txt` |
| GREEN - the fixture suite in the worktree | **31 passed** (28 -> 31) | `LEAD_PYTEST_GREEN_R3.txt` |
| Full Bridge suite (same env hygiene, `C:/bt_s4/dd06r3full`) | **1629 passed, 1 skipped, 0 failed** (1626 -> 1629) | `LEAD_FULL_BRIDGE_SUITE_R3.txt` |
| Guard | `RESULT: PASS`; staged exactly the two files | `LEAD_GUARD_R3.txt` |
| Gemini delta `DD06_R3_GEMINI` (packet `DD06_R3_20260918`, 18 files; attempt 0 died in 6 s because the reused r2 runner hard-codes the R2 packet boundary - no Gemini call; runner cloned as `run_dd06_r3_g38.ps1`) | see `GEMINI/` and the queue log | `GEMINI/` |
The round-1/round-2 mutants (gates, tripwires, R3 markers, abort, the attribution arithmetic) were not re-run this round: none of their fences were touched (diff = `_paid` body, `_attribute` branches, docstrings, tests); the fourth reader is asked to confirm the complete-reading semantics unchanged (round-2 cases r3-replay / master-pays / mark-to-market-tick / nothing-moves still pass in GREEN).

## Carried (unchanged this round)
N-3 `_resting_oid`, N-4 `_HEX40`, N-5 min-notional, N-6 host binding, N-7 `usd_class_transfer` test, N-8 brief residue, N-9 arm order (owner `DD06-ORDER A|B`), N-10 `len(digits) <= 5`; new from the third read: N-13 a drift larger than half the arm amount counts as a payment, N-14 single post-arm snapshot, N-15 the ERROR finding embeds `_attribute`'s sentence, N-16 `ARM_AMOUNT_USDC` unpinned. The open venue question (withdraw3 from an agent with a FUNDED perp balance) stays open; the probe runs again only on a separate owner word.

## Roster
Lane 7 re-pinned to `acd79b52` (launcher HEAD pin, agent brief, REVIEW_BRIEF HEAD + addendum 4, queue row); attempt 4 archived. Fourth exact-Opus read on the fresh Pro window right after the Gemini delta; Sol (Sat) under the safety rule. A REQUEST_CHANGES on this round goes back to the owner, not to a Lead repair. The Lead never accepts its own code.

Recorded by Claude Opus 5 Lead (session 6, `4a8233`), 09:1x UTC+3.
