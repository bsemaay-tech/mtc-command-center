# WP-P0-22 (family-lineage and human-observation leakage control) — gap table: plan/brief contract vs merged bytes — 2026-09-15 night

Prepared by the Claude Opus 5 Lead (session 5, `03c6c8`). Read-only sources: plan block `MASTER_WORK_PACKAGE_AND_PARALLEL_DELIVERY_PLAN_2026-08-22.md:527-537`; brief §6.6 rules 6-8 (`MASTER_ARCHITECTURE_AND_IMPLEMENTATION_BRIEF_2026-08-21.md:1427-1447`); the frozen reduced-slice contract `11_TRIAGE/P022_RESEARCH_TRACKER_SCOPE.md` (status `IMPLEMENTATION_AUTHORIZED_LOCAL_SLICE`; scope commit `b69c6d97`, amendment `7778048f`); the reduced-scope proposal `C:/tmp/P022_SIMPLER_SCOPE_20260912/SCOPE_AND_ESTIMATE.md` (sha256 `618dfdb7…`); `P022_HANDOFF.md` (CT13) and `P022_UPGRADE_HANDOFF.md` (`C:/tmp/P022_LEAD_20260912`); the bytes on `origin/master` `fcac0ac6` (product PR #190 `a8aec656`, handoff PR #191). **Nothing here is acceptance; the merged slice is "acceptance of the reduced local milestone only, not full P0-22 package acceptance" (handoff, verbatim).**

## 0. Merged bytes
| Module | What it is | Size | Tests / reviews |
|---|---|---|---|
| `03_QUANTLENS/tools/p022_research_tracker.py` | single stdlib module, SQLite-backed local research tracker: `register` (version-1 record: local experiment id, half-open UTC period, hashed references for strategy/configuration/dataset/report, optional opaque family/parent/related links; idempotent identical registration; immutable integer revisions), `disclose` (resolve one report reference, verify digest, commit an append-only disclosure event, then emit the same buffer — commit-before-output; zero bytes on any failure), `status` (re-hash references; JSON/Markdown; always states `LOCAL_LOG_ONLY` + `ACCESS_COMPLETENESS_UNVERIFIED`; lists revisions, attempts, MATCH/MISMATCH/MISSING, whole-period exposure, overlapping periods, lineage links/conflicts, warnings; emits no untouched duration, clean-window certificate, acceptance receipt or `LIVE_CANDIDATE` eligibility) | 772 lines | `tests/test_p022_research_tracker.py` 27 tests (27/27 on 3.14 and 3.13; QuantLens discovery 83/83); reviewed candidate `1bdb7f8c`: exact Sol xhigh PASS, exact Opus xhigh PASS-WITH-NITS, Gemini 3.7 supplemental PASS; blobs byte-identical through merge |
| `11_TRIAGE/P022_RESEARCH_TRACKER_SCOPE.md` | the frozen reduced contract itself | — | — |
Not on master: any family resolver, lineage graph, observation ledger beyond the one disclosure command, untouched-window computation, `FAMILY_OBSERVED` marking, reader-path instrumentation, independent clock, protected history, or downstream gate.

## 1. Acceptance-gate gap table (plan "brief A-7d", five parts) — what the full package must show vs what exists
| A-7d part (plan line 533) | Brief rule | Merged slice provides | Gap | Depends on |
|---|---|---|---|---|
| (1) "The lineage graph resolves or explicitly marks `UNKNOWN`" — a resolved `family_id` per package | rule 6: `family_id` derived from source provenance, producer and parameter neighbourhood; `UNKNOWN` **blocks** `LIVE_CANDIDATE` | opaque `family_reference` / `parent_experiment_ids` / `related_experiment_ids` stored as given; conflicts across revisions reported; "unknown stays unknown, never inferred" | the **canonical family resolver** and the immutable family graph (registry + tag dictionary as seed; P0-13 trial catalog as input); the `UNKNOWN`-blocks-`LIVE_CANDIDATE` rule wired into the eligibility consumer (WP-V2A-10) | **WP-P0-13 acceptance** (the plan's dependency; the catalog writer slice is parked on P0-20 — see `P013_POST_P020_NOTE_20260915.md`); owner decision 98 (no arbitrary family bins) |
| (2) "the ledger is append-only and its enumeration is shown capable of finding an observation it was not told about" | rule 8: every human view and every optimizer read appends (who/what, when, `package_hash`, `family_id`, window) | one append-only event class (disclosure attempts of one CLI command) in application-local SQLite; the D026-style refusal tests prove commit-before-output and zero-bytes-on-failure for **that** path | **all other reader paths** (notebook, AI, file browser, export, raw/copied files) are unmonitored by design; the "finds an observation it was not told about" demonstration requires an observer independent of the tracker (owner decision 101: independent read observer/clock; 102: unlogged reads must ultimately be **prevented**, not accepted) | reader-path inventory + mediation design (T0 per the contract's retained tier); an independent protected service/clock boundary |
| (3) "the untouched window is computed from the ledger, not asserted" | rule 8 second half; rule 7 contamination intersection | deliberately **not computed**: the whole declared period is conservatively marked exposed once its report is disclosed (owner decision 99 accepts over-marking); no untouched duration is ever emitted | the untouched-window computation per candidate (intersection of observed windows with evidence windows, per family), consuming (1) and (2) | (1) + (2); WP-V2A-08 shadow windows to have something to compute over |
| (4) "an absent or incomplete ledger yields zero confirmation evidence" | rule 8 last sentence | honoured by construction: `status` never emits confirmation evidence; every output carries `LOCAL_LOG_ONLY` + `ACCESS_COMPLETENESS_UNVERIFIED` | none for the slice; the full package must keep this property when (3) exists (a computed window from an incomplete ledger must still be zero) — a fixture, not a feature | — |
| (5) "a D026 fixture in which a deliberately contaminated sibling is RED without the control and GREEN with it" | rule 7 (`FAMILY_OBSERVED`) | none (no sibling marking exists) | the contaminated-sibling fixture: sibling B's parameters chosen after A's window was read → without the control B looks clean (RED), with the control B carries `FAMILY_OBSERVED` and its overlapping portion is navigational-only (GREEN) | (1) + (2) + (3) |

## 2. Reduced-scope deferral table (from `SCOPE_AND_ESTIMATE.md` §"What makes this smaller") — carried verbatim as the backlog, with tonight's state
| Full mechanism | Reduced treatment on master | Owner decision it must still satisfy | State 2026-09-15 |
|---|---|---|---|
| Canonical family resolver + immutable family graph | opaque references; conservative warnings for declared relations | 98 (no arbitrary bins) | OPEN — blocked on P0-13 |
| Instrument all five reader/writer classes | one fixed research-review command (`disclose`) | 101/102 | OPEN — design not started |
| Fine-grained untouched-window computation | whole reviewed period marked exposed | 99 (over-marking accepted as interim) | OPEN |
| Independent read observer, clock, whole-window no-read attestations | explicitly missing; local UTC/sequence = bookkeeping | 101 | OPEN |
| Independent immutable anchoring, storage principals, retention | ordinary local transactional storage (local-owner-mutable, disclosed) | — | OPEN |
| Prevent every unlogged raw read/copy/export | deferred | **102 — "zero confirmation forever" was rejected by the owner as a final state** | OPEN (must not be re-litigated as acceptable) |
| Bind every displayed window to recorded evidence | same-buffer log-before-output for the one command | 103 | PARTIAL (one command) |
| Downstream promotion/admission integration | none; no accepting evidence emitted | D-15 / A-7d / A-18 | OPEN |
Carried review nit (Opus, non-material): related-reference exposure warnings are **not transitively propagated** in the reduced contract — a future slice should propagate along declared relations (rule 7 is transitive by family, not by declared pair).

## 3. What a next slice would contain (bounded; none started; order follows the dependencies)
| # | Slice | Kind / tier | Depends on | Est. |
|---|---|---|---|---|
| N1 | **Reader-path inventory (design, no code):** enumerate every path by which a human or a tool can read a shadow/research record today (dashboard, notebooks, exports, raw files, AI sessions, this tracker), classify each as mediated / unmediated, and propose the mediation (or prevention) per path against decisions 101/102 | docs T2 → becomes the T0 scope packet | nothing; the Lead can draft it | 3-4 h |
| N2 | **Family resolver contract:** the `family_id` derivation rule from `STRATEGY_RESEARCH_REGISTRY.json` + `TAG_DICTIONARY.json` seeds (§10.3 mechanism 2), the `UNKNOWN` rule, the migration of version-1 opaque references (map, never reinterpret — upgrade handoff) | design T2, then code T0 | **P0-13 accepted interface** (catalog identities are the graph's nodes) | design 3 h; code 2 Codex lanes |
| N3 | **Transitive warning propagation** (the Opus nit) inside the existing module + a RED/GREEN test | code T1 (same-scope defect repair per the contract's rule) | nothing | 1 lane |
| N4 | **Ledger v2 with an independent clock/observer boundary** (append-only receipts outside the local SQLite; who/what/when/hash/window per rule 8) | code T0 | N1 design; P0-26 evidence-store decisions (where receipts live; backup) | later |
| N5 | **Untouched-window computation + `FAMILY_OBSERVED` marking + the contaminated-sibling D026 fixture** | code T0 | N2 + N4; shadow windows (WP-V2A-08) | later phase |
| N6 | **Downstream gates** (WP-V2A-10 candidate eligibility consumes `UNKNOWN`/`FAMILY_OBSERVED`) | code T0 | N5 | later phase |

## 4. Not verified tonight / limits
- Test counts and the review verdicts are taken from `P022_HANDOFF.md` (2026-09-13) and the upgrade handoff; nothing was re-run tonight.
- Owner decisions 98-103 are cited through the reduced-scope proposal and the contract; their verbatim text lives in the P022 design draft v1.12 fold (`OVERNIGHT_PREPARATION_20260912/inputs/P022_DESIGN_DRAFT_V1.md`, lines 1123-1131 / 1196-1203 / …) which was not re-opened tonight — N1/N2 must quote them from the source before any scope is frozen.
