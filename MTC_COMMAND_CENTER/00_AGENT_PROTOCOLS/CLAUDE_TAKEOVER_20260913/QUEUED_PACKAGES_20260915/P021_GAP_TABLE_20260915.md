# WP-P0-21 (objective eligibility criteria + D026 fixtures) — gap table: design v1.5 vs merged bytes — 2026-09-15 night

Prepared by the Claude Opus 5 Lead (session 5, `03c6c8`). Read-only: design draft v1.5 (`C:/tmp/OC_P021_V15_VERIFY/P021_DESIGN_DRAFT_V1.md`, 2026-08-31, "DRAFT COMPLETE; BUILD BLOCKED", 17 blockers B-01..B-17 + D-01..D-03), the 2026-09-12 owner decision packet (`1 YES, 2 YES, 3 A, 4 YES, 5 YES, 6 A, 7 A`, recorded `OD-20260914-P021-P030-RECORD-1`), the 2026-09-07 policy decisions (`P021_DECISION_1 = A`, `P021_DECISION_2 = balanced`), and the bytes on `origin/master` `fcac0ac6` (integrated slice PR #188 `ca2f8a68`, closeout PR #189 `d90150ba`; ten reviewed P021 blobs, exact Sol affected-delta PASS; 103 tests post-merge per `P021_P030_HANDOFF.md`). **Nothing here is acceptance; the merged slice is a scoped implementation milestone (handoff §"Acceptance boundary"), and no real candidate has been evaluated.**

## 0. What is on master today (the "merged bytes")
| Module (path under `MTC_COMMAND_CENTER/`) | What it is | Lines | Tests |
|---|---|---|---|
| `03_QUANTLENS/tools/p021_readiness_rules.py` | rule catalogue: the seven `P021.*` checks with their limits, 12 CLOSED numbers (policy v1, PROVISIONAL, balanced profile), 4 OPEN numbers (B-02, B-06, B-07 ×2), `missing_rules` per check; `readiness_record()` refuses on every check; `--self-check` mutant fence; owner view | 422 | via `test_p021_eligibility.py` + `check_*`-style self-check |
| `03_QUANTLENS/tools/p021_evidence_contracts.py` | pure, fail-closed evidence contracts (no I/O, no evaluator): control inventory/manifest identity + `evaluate_control_evidence` (B08 state rule), `compare_lookahead` over the fixed CLOSED-bar union (P021-B12), `validate_closed_bar_runtime_receipt` (B16), versioned canonical preimages → bare sha256 (B19/B21), `require_ds_v1_digest` (`ds-v1`) | 567 | `tests/test_p021_eligibility.py` (46 tests) |
| `03_QUANTLENS/tools/data_gap_ratio.py` | fixed-step 24/7 timestamp-gap measurement (H1/M2 rule; measurement, not a threshold) | 116 | `tests/test_data_gap_ratio.py` |
| `03_QUANTLENS/tools/strategy_type_classifier.py`, `strategy_type_policy_set.py` | holding-hours classifier; policy-set v1 shape/version-hash validator (B-22 carrier) | 75 / 170 | `tests/test_strategy_type_classifier.py`, `tests/test_strategy_type_policy_set.py` |
| `01_MTC_PROJECT/tools/create_optimization_data_bundle.py` (+ its timeframe test) | B1 producer extension: required OHLCV incl. volume, closed-candle cutoff, structured `ds-v1` identity, 5m recognition, fail-closed unknown timeframe | 51 KB | `test_create_optimization_data_bundle_timeframes.py` |
| `check_market_data_collector.py` (repo root) | collector checker (P0-21/P0-30 shared), runs in CI `research-gates` | — | self-confirming |
Not on master: any eligibility **evaluator**, any real-candidate run, any PASS path, any `gap_ratio_max` / divergence value, any P0-12 kernel adapter.

## 1. Check-family gap table (design v1.5 catalogue → merged → gap → who closes it)
| Check | Design v1.5 says | Merged bytes provide | Gap (what a next slice would contain) | Blocked on / owner |
|---|---|---|---|---|
| `P021.DETERMINISTIC_REPLAY` | two clean-process runs; intent-stream sha256 equality binding; producers A/B in separate processes; stream canonicalization `PROVISIONAL-ON-P012` | catalogue rule (limits `run_count 2`, `hashes_must_equal`); refuses on `accepted_corrected_engine.B01`, `intent_stream_serialization.P012`; intent identity primitive (`intent_evidence_sha256`, B21) | the two-process launcher + kernel-output adapter + comparator; D026 fixture (one byte differs → FAIL) | **B-01** (P0-12 acceptance + interface freeze) — the plan's dependency; nothing to build before it |
| `P021.LOOKAHEAD_PREFIX` | per-decision equality full vs prefix; tolerance 0; decision domain = **B-12** open | `compare_lookahead` implements the owner-chosen domain (decision 1: fixed CLOSED-bar union of full + prefix decisions; one-sided mismatch fails; empty union blocks) with hostile-carrier refusals; catalogue still lists `decision_domain.B12` and `intent_hash_contract.B21` as missing rules | (a) **record hygiene**: the catalogue's `missing_rules` for this check name B12/B21 as open while the contract module implements the decided rule — either the catalogue drops them after the T0 roster accepts the contract slice, or the design v1.6 records B-12 as "decided 2026-09-12 (decision 1), contract implemented, roster pending"; (b) the prefix/full **producers** (P0-12 adapter) | B-01; roster of the contract slice (T0 per the packet: "can later govern admission identity") — **that roster has not run** (exact Sol delta PASS covered the integration; the packet asked for the full T0 roster before use) |
| `P021.REPAINT_CLOSED_BAR` | closed-bar flip 0; five-branch forming-bar rule; proof artifact = **B-16** open | `validate_closed_bar_runtime_receipt`: runtime-emitted immutable receipt bound to candidate/package/deployment/dataset/intent/runtime-code/instrument/timeframe/producer identities (decision 1 B16 shape); catalogue rule + `forming_bar_runtime_proof.B16` still missing | (a) the same record-hygiene item as above (B-16 shape decided + implemented, catalogue not updated); (b) the closed-bar replay producer + vector producer + five-branch comparator; D026 fixtures for branches 1, 3, 4 | B-01; roster of the contract slice |
| `P021.DATA_QUALITY` | exact-zero rules binding; `gap_ratio_max` **B-02** and formula **B-17** open; dataset-hash contract **B-13** open | catalogue rule (four binding limits; `gap_ratio_max` OPEN; missing `gap_ratio_formula.B17`, `dataset_hash_contract.B13`); `data_gap_ratio.py` measures gaps under the H1/M2 rule (a measurement, explicitly not the B-17 policy identity); `ds-v1` digest carrier + B1 producer emit dataset identity (`{contract:"ds-v1", digest}`) | (a) **B-02 owner value** — measure-first (decision 6 A: "keep unset; return an evidence-backed options packet after measurement") → the measurement exists in part (93-file scan: 4,477,626 rows, zero timestamp gaps; 17-file 5m scan: zero gaps) — an **options packet for `gap_ratio_max`** can now be written from those scans (Lead, documentary); (b) B-17: pin the formula identity from the H1/M2 rule already coded (one decision row, not code); (c) B-13: is `ds-v1` the dataset-hash contract? The packet forbids equating it to P020 `dataset_manifest_sha` — a decision row must say which digest satisfies `dataset_hash_required` for P021 | owner (B-02 value; B-17/B-13 pins) — **no P0-12 dependency for these three** |
| `P021.BASIC_FAILURE_FLOOR` | trade-count min **B-03**, single-loss multiple **B-04** open; zero rules binding; risk-unit source open | catalogue closes B-03 by policy v1 (`day 30 / swing 30 / position 12`, balanced profile; position extrapolated) and B-04's multiple (`1`) with `stop_loss_ceiling_equity_fraction 0.005` (decision 1 A); still missing `risk_unit_source.B04`, `strategy_type_policy_set.B22`; classifier + policy-set validator merged | (a) B-04 **risk-unit source** (which artifact carries intended risk-at-stop — P0-12 kernel output or the P0-20 allocator?) — decision row + adapter; (b) B-22: the policy set is validated but not **ratified as the check's input** — one decision row; (c) simulator evidence producer + expressibility oracle (P0-12) | B-01 for the producers; owner for B-04 source and B-22 |
| `P021.UNSIMULATED_CONTROLS` | conservation rule binding; **B-08** allowance matrix open (`null` ⇒ always BLOCKED); G19-F06 no manufactured manifest | `evaluate_control_evidence` implements decision 1's B08 rule (required-but-unsimulated ⇒ at most `SHADOW_ELIGIBLE`; informational controls only if openly manifested; independently computed manifest hash) + B19 control-hash contract; catalogue still lists `state_allowance_matrix.B08`, `control_hash_contract.B19` as missing | record hygiene (B-08/B-19 decided + implemented; catalogue not updated); the trace producer + inventory producer + registrar (P0-12/P0-20) | B-01; roster of the contract slice |
| `P021.BACKTEST_FORWARD_DIVERGENCE` | metric **B-05**, tolerance **B-06**, alignment/window/min-pairs **B-07** open; always BLOCKED | catalogue: three OPEN numbers + missing `divergence_metric.B05`, `divergence_alignment.B07`, `divergence_provenance.B20`; pairing key fixed by decision packet ("instrument, entry-bar timestamp and intent id"); forward periods closed (21/119/364 d) | measure-first per decision 6 A: nothing until shadow operation produces paired evidence (decision 3 A makes divergence required only from `TESTNET_PAPER_ELIGIBLE` upward); the **metric definition** (B-05) is a design question the Lead can draft as an options packet now (units: P021 N13 trade-performance divergence, distinct from P030 N8) | owner (B-05 options, then B-06/B-07 after measurement); WP-V2A-08 shadow windows for the data |

## 2. Blocker ledger B-01..B-22 (state tonight)
| Blocker | v1.5 state | State on 2026-09-15 | Evidence |
|---|---|---|---|
| B-01 P012 acceptance | OPEN | **OPEN** — P0-12 research/engineering scope accepted narrow (`OD-20260913-P012-SCOPE-1`); production admission not granted; kernel interfaces not frozen for consumers | handoff §2 P0-12 row |
| B-02 gap_ratio_max | OPEN | OPEN, measure-first (decision 6 A); measurements exist (two scans, zero gaps); **options packet of 2026-09-08 unanswered** (`P021_GAP_RATIO_MAX_POLICY_20260908.md`, recommended T = 0.0001 on `m2`) | catalogue `OPEN_NUMBERS` |
| B-03 min trade count | OPEN | CLOSED by policy v1 (30/30/12; PROVISIONAL, reversible) | `CLOSED_NUMBERS` |
| B-04 max single loss multiple | OPEN | value CLOSED (multiple 1; stop-loss ceiling 0.5 % equity = deployed `risk_pct_per_trade`); **risk-unit source still OPEN** | catalogue `missing_rules` |
| B-05 divergence metric | OPEN | OPEN | catalogue |
| B-06 divergence tolerance | OPEN | OPEN, measure-first | `OPEN_NUMBERS` |
| B-07 alignment window / min pairs | OPEN | OPEN, measure-first (pair key fixed) | `OPEN_NUMBERS` |
| B-08 unsimulated allowance matrix | OPEN | **DECIDED + IMPLEMENTED as contract** (decision 1); catalogue not updated; roster pending | `evaluate_control_evidence` |
| B-09 testnet external prerequisite ids | OPEN | OPEN (no decision found) | — |
| B-10 forward evidence sufficiency | OPEN | **partly CLOSED**: forward trade-count minima + periods + market-condition counts by policy v1 (balanced) | `CLOSED_NUMBERS` (7 forward numbers) |
| B-11 battery member pass rules | OPEN | OPEN (no decision found) | — |
| B-12 lookahead decision domain | OPEN | **DECIDED + IMPLEMENTED** (fixed closed-bar union; decision 1); catalogue not updated | `compare_lookahead` |
| B-13 dataset hash contract | OPEN | **PARTLY**: `ds-v1` carrier exists; which digest satisfies P021's `dataset_hash_required` is undecided (packet forbids equating with P020/P030 hashes) | `require_ds_v1_digest` |
| B-14 check-set applicability by state | OPEN | **DECIDED** (decision 3 A: all non-divergence checks for `SHADOW_ELIGIBLE`; all seven from `TESTNET_PAPER_ELIGIBLE`); not yet encoded in the catalogue (`required_check_ids_by_target_state` absent) | decision packet §3 |
| B-15 mixed-outcome precedence | OPEN | OPEN (no decision found; `FAIL > BLOCKED > PASS` still proposed) | — |
| B-16 forming-bar runtime proof | OPEN | **DECIDED + IMPLEMENTED** (runtime receipt shape); catalogue not updated | `validate_closed_bar_runtime_receipt` |
| B-17 gap-ratio formula | OPEN | OPEN as a policy identity; the H1/M2 formula is coded in `data_gap_ratio.py` — needs the pin, not code | catalogue `missing_rules` |
| B-19 control hash contract (post-v1.5) | — | IMPLEMENTED (versioned preimage → bare sha256) | contracts module |
| B-20 divergence provenance (post-v1.5) | — | OPEN | catalogue |
| B-21 intent hash contract (post-v1.5) | — | IMPLEMENTED | `intent_evidence_sha256` |
| B-22 strategy-type policy set (post-v1.5) | — | validator IMPLEMENTED; ratification as the check input OPEN | `strategy_type_policy_set.py` |
| D-01..D-03 | recorded | unchanged (documentary; D-03 brief-vs-plan carrier still open) | design |

## 3. What the next P0-21 slices would be (bounded; none started; each needs its own packet + word)
| # | Slice | Kind / tier | Depends on | Est. |
|---|---|---|---|---|
| S1 | **Design v1.6 + catalogue hygiene**: fold decisions 1/3 and the 09-07 policy into the design (B-08, B-12, B-14, B-16 → "decided + implemented, roster pending"; B-03/B-10 → policy v1); update `missing_rules` after the roster accepts the contract slice; encode B-14 as `required_check_ids_by_target_state` | docs T2 + a small T1 code change (catalogue) | the **T0 roster of the contract slice** (decision 1 called it T0; only an exact Sol delta PASS is on record) — Wed Opus is booked for four lanes; this would be a fifth or Friday Sol | 2-3 h Lead + 1 review |
| S2 | **Owner options packets for the measure-first numbers**: `gap_ratio_max` (B-02) — **an options packet already exists and is unanswered**: `C:/tmp/P021_GAP_RATIO_MAX_POLICY_20260908.md` (17 × 5m series, 4,105,966 bars, zero internal gaps; options Z 0.0 / **T 0.0001 recommended** / L 0.001 on `m2_missing_bar_ratio`; companion `gap_ratio_metric` pin; coverage as a separate rule) — no `P021_DECISION_3` answer is recorded anywhere (CT13 DECISIONS, takeover records, RUN_STATE grep tonight); the 09-12 packet's decision 6 A ("keep unset, measure-first") post-dates it. S2 = re-present that packet with the 93-file scan (4,477,626 rows, zero gaps) appended + B-17 formula pin (= the `m2` definition already coded) + B-13 digest pin; plus B-05 divergence metric options | docs T2 (Lead, no code) | nothing (measurements exist) | 1-2 h (gap packet mostly written) + 1-2 h (B-05) |
| S3 | **B-04 risk-unit source + B-22 policy ratification + B-09 / B-11 / B-15** decision rows | owner decisions (packet style) | S2 style; B-09/B-11 need the §6.5 wording read again | 1-2 h to draft |
| S4 | **Evaluator skeleton**: `evaluate_check(check_id, evidence, policy)` that consumes the contracts and returns the WP-P0-04 `EligibilityCheckResult` with `BLOCKED` on every missing rule; D026 fixtures for the exact-zero rules (duplicate timestamps, out-of-order, invalid OHLCV, unexpressible event, liquidation) | code T1 → T0 when it governs admission | S1 accepted; still refuses PASS until B-01 | Codex Sep 19+ (2 lanes) |
| S5 | **P0-12 adapters** (replay launcher, prefix producer, closed-bar producer, simulator trace, control inventory) | code T0 | **B-01** — not before P0-12 admission | later phase |

## 4. Not verified tonight / limits
- The 103-test count and the exact Sol delta PASS are taken from `P021_P030_HANDOFF.md` (2026-09-13); no test was re-run tonight (Gemini call in flight; no git). The module line counts are from the CT13 tree at `f1bbac40`.
- Whether decision 1's "T0 roster before use" was ever satisfied for the contract slice is **NOT established** by any record the Lead found tonight — recorded as the first item of S1, not asserted either way.
- The design v1.5 file lives only under `C:/tmp/OC_P021_V15_VERIFY/` (not in the repository); S1 should bring v1.6 into `11_TRIAGE/` so the catalogue can cite a repository path.

## Addendum 2026-09-15 18:4xZ — owner answered S2 "recommended" (`OD-20260915-P021-S2-RECOMMENDED-1`)
| Blocker | New state |
|---|---|
| B-02 `gap_ratio_max` | **CLOSED (policy v1, PROVISIONAL)**: `P021_DECISION_3 = T` → `0.0001` on `m2_missing_bar_ratio`; coverage stays a separate rule |
| B-17 formula identity | **CLOSED as a pin**: `m2_missing_bar_ratio@data_gap_ratio.py v1 (fixed-step 24/7, half-open span, leading/trailing absence excluded)` |
| B-13 dataset hash contract | **CLOSED as a pin**: the `ds-v1` digest of the scanned bundle satisfies `dataset_hash_required`; P0-20/P0-30 hashes stay separate fields |
| B-05 divergence metric | **CLOSED as a definition**: `P021_DIVERGENCE_METRIC = M-C`; B-06/B-07 stay measure-first |
Engineering follow-on (packet §5, part of S1): catalogue update (`OPEN_NUMBERS` → `CLOSED_NUMBERS` rows with policy-v1 provenance; `missing_rules` B-13/B-17 removed; `divergence_metric.B05` rule text) = one small T1 change with RED/GREEN on a feature branch; unmerged until a T1 verdict.
