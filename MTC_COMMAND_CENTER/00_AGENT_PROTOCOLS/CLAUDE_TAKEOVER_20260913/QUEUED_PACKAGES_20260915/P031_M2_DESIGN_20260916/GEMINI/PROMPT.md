You are gemini-3.8-flash-high acting as the independent read-only T2 documentary corroborator (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-31 Milestone-2 DESIGN HALF written by the Claude Opus 5 Lead on 2026-09-16 under the owner ruling `OD-20260916-P031-M2-Q1-Q6-1` (Q1 = A: source-row census on scratch copies of the five legacy stores, mapping-table dry application producing the reconciliation-report shape, D026 fixture designs; documentary + scratch only, no ledger write, no code on the P031 branch). The Lead's central claim is uncomfortable and must be checked hardest: **the live `STRATEGY_RESEARCH_REGISTRY.current_status` vocabulary is not table 4a's vocabulary — 60 of 63 values fall outside the ruled mapping, so the ruled import would produce 3 seeds, 0 conflicts and 60 `UNKNOWN` blockers** — and therefore the Lead puts seven new questions (Q7..Q13) to the owner instead of guessing a mapping. Your job is adversarial along four axes: (a) **arithmetic and provenance** — do the census numbers follow from the store bytes you can read, and are they pinned (sha256, blob OIDs, generated_at)? (b) **rule fidelity** — does `m2_dry_run.py` apply the fold's tables 4a/4b/4c and the owner's Q2 A / Q3 A / Q4 A / Q5 A exactly as written, with `UNKNOWN` never replaced by a guess, and is the what-if run unmistakably labelled as NOT ruled? (c) **inference creep** — does any statement, default, mapping or fixture decide something that belongs to the owner (the questions must be questions), or contradict the plan's non-goals (nothing edited/retired/deleted; M2 build waits for M1 acceptance, Q6 = B)? (d) **fixture honesty** — can each D026 fixture's RED arm really produce the stated wrong output, and does its GREEN observable discriminate?

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P031_M2_DESIGN_20260916
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/P031_M2_SOURCE_CENSUS_20260916.md, subject/P031_M2_MAPPING_DRY_RUN_20260916.md, subject/P031_M2_FIXTURE_DESIGN_20260916.md, subject/census.json, subject/m2_dry_run.py (complete; ~300 lines → two or three ranges), subject/m2_census.py (complete), subject/table_4a_prime_PROPOSED.json, subject/run_as_ruled_SUMMARY.json, subject/run_planted_SUMMARY.json, subject/run_whatif_4a_prime_SUMMARY.json, sources/FOLD_section4_to_6.md (complete), sources/DECISIONS_rows_P031.md (complete), sources/P031_M2_SCOPE_PACKET_DRAFT_20260915.md (complete), sources/producer_spec_promotion_status_listing.txt (complete, 63 lines), sources/p031_lifecycle_ledger_48bd70de_lines1-230.py (complete). SPOT CHECKS (ranges, not complete): sources/scratch_STRATEGY_RESEARCH_REGISTRY.json — locate and read the `current_status` of at least eight strategies spread across the file (state their `strategy_id` and value) and confirm the value vocabulary the census reports; sources/scratch_TRIAGE_CANDIDATE_REGISTRY.json — read the `summary` object and at least three candidate objects (confirm `stg_code` form `Stg###`, the absence of any decision field, the `eligible_for_retriage` flag); sources/scratch_AI_QUANTLENS_VERDICT_REGISTRY.json — at least three entries (confirm `decision` values and the id forms); sources/scratch_VARIANT_LOG_REGISTRY.json — at least three variants (confirm `promotable` and the absence of a strategy/family identity field).

TASK.
1. **Census arithmetic.** For each store, does the census's row count and value distribution agree with what you can read (producer_spec: count the 63 listing lines and the values; registry/triage/verdicts/variants: your spot checks must be consistent with the reported distributions; any value you find that the census does not list is a REQUIRED finding). Is every scratch copy pinned (sha256, blob OID at the named commit, generated_at)? Is the "fold said 44/63 and 172" comparison honest?
2. **The 60/63 claim.** From `FOLD_section4_to_6.md` table 4a list the seven old values; from your registry spot checks and the census confirm which live values are outside them; confirm the census's conclusion (3 in-table rows: two composites `PROMOTE_TO_FORWARD_PAPER_TRADE|PROMOTE_TO_PARITY_CANDIDATE` and one `PROMOTE_TO_FORWARD_PAPER_TRADE`) or refute it.
3. **Dry-run rule fidelity (read the code).** Trace `map_ladder` (Q3 A: highest-ranked component's row, all tags; any unmapped component → no state), the registry↔producer_spec join (folder `STG###`), the Q4 A branch (`migrated-conflict`, both values in provenance, `min` by `STATE_RANK`), the Q5-before-Q4 ordering (an unmappable side makes the row `UNKNOWN`), the triage rule (`decision_recorded = False` literal → all `CAPTURED`; is that the fold's letter, and is it disclosed as such?), the verdict rule (tag only on an existing host; orphans `NOT_SEEDED`), the variants rule, the balance/`--plant` logic (fresh enumeration vs census), and the `WHAT_IF_NOT_RULED` label path. Name any place where the code decides something the ruling does not (e.g. the `STATE_RANK` order, the extension rows' rank of -1) and check the Lead disclosed it as an owner question (Q12, Q7).
4. **Run summaries.** Do the three summaries' totals reconcile (as ruled: 175 seeds = 3 + 172; SEEDED 344 = 3 + 172 + 169 advisory tags; UNKNOWN 60; NOT_SEEDED 63 = 20 + 43; balanced true; planted: registry 64 vs 63, balanced false; what-if: 235 seeds, 28 conflicts all TRIAGED-vs-CAPTURED → CAPTURED, 0 UNKNOWN)? Are the report sha256s stated in the MAPPING document the ones in the summaries?
5. **Identity finding.** Check the census's claim that triage `stg_code` (`Stg###`) and registry `strategy_id` (`STG###`) collide only by case and are different items (the census names `Stg001` = a CANSLIM intake transcript vs `STG001` = `ql_alpha_ada_two_candle_sr_1h`; verify from the two stores), and that producer_spec `candidate_id` never equals a registry id while the folder code always does. Is the "exact, case-sensitive joins only; `stg_code` never an identity" rule the right consequence, and is it stated as a design rule rather than an owner decision (it should be a design rule)?
6. **Inference creep and non-goals.** List every sentence in the three documents that decides, recommends beyond "the Lead does not decide", or could be read as an authorization. The what-if extension table must be visibly NOT RULED everywhere it appears. The M2 build must be stated as waiting for M1 acceptance (Q6 = B). Nothing may say a store will be edited, retired or deleted.
7. **Fixtures.** For each of the five fixture designs: is the RED arm a real mutant with a stated wrong output, is the GREEN observable discriminating (not a count that cannot fail), and is fixture 4 (identity collision) and fixture 5 (idempotence) correctly marked as proposed additions beyond the plan's three?
8. **Questions to the owner (Q7..Q13).** Are they questions with options and consequences, each consequence traceable to a measured number; is any recommended answer smuggled in as a default; does Q13 (a `SEED_IMPORT` initial registrar transition; the schema `CHECK` widening) correctly reflect the M1 ledger bytes you read (`REGISTRAR_TRANSITIONS` admits `(None, "CAPTURED", "CAPTURED")` only; `source_kind CHECK = 'FIXTURE'`)?

OUTPUT (under 3000 words): (a) the census-arithmetic table (store → your reading vs the census); (b) the rule-fidelity table (rule → code lines → VERIFIED / DIVERGES / DECIDES-FOR-THE-OWNER); (c) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) — a REQUIRED finding means REQUEST_CHANGES; (d) EXACT read coverage with ranges and continuations; (e) nonempty NOT VERIFIED (no execution); (f) one fenced JSON object {"part":"P031_M2_DESIGN_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","census_60_of_63_claim":"CONFIRMED|REFUTED|NOT VERIFIED","inference_creep":[...],"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
