# WP-P0-13 — what flips when WP-P0-20 is accepted (refresh note on the dependency plan V5) — 2026-09-15 night

Prepared by the Claude Opus 5 Lead (session 5, `03c6c8`). Read-only against `P013_DEPENDENCY_RESOLUTION_PLAN_20260913_V5.md` (CT13; citation tokens as defined there) and the P0-20 state of 2026-09-15. **Authorizes nothing; starts nothing.** Its purpose: when the P0-20 acceptance act (V5 step **C-7**) happens, the Lead should not have to re-derive which P0-13 steps become startable and which stay blocked.

## 1. V5 phase state, refreshed (V5 was written 2026-09-13 morning)
| Phase / step | V5 mark | Tonight | Evidence |
|---|---|---|---|
| A-1 (D1 1D window) | OWNER DECISION PENDING | **ANSWERED** — Option A (asymmetric window), applied in the V1.1/V1.2 re-freeze | `OWNER_DECISIONS_PENDING.md` status 13:55Z 09-13 |
| A-2 (D-P20-1 scope row) | PENDING | **ANSWERED YES** → `OD-20260913-P020-SCOPE-1` | same; CT13 `c064acc6` |
| A-3 (D-P20-2 reporter criterion set) | PENDING | **ANSWERED** (recorded `OD-20260913-P020-BENCH-1`; reporter unchanged — `8/16 NOT ACCEPTABLE` still the printed line) | same |
| A-4 (D-P20-3 one-writer row) | NOT AUTHORIZED | **RECORDED** → `OD-20260913-P020-WRITER-1` (P0-20 sole writer of `identity.py` and `mega_walk_forward.py` while active) | same |
| B-1…B-7 (shared-file reconciliation: `DECISIONS.md`, merged `identity.py`, P013 rebase) | NOT AUTHORIZED | **VERIFIED NOT DONE (git, 14:38Z):** the P020 impl branch (`C:/P020_IMPL_20260912`, `feature/p020-successor-20260912`) is still at `b9b72f85`, 25 ahead of `origin/master`, clean; the last commit touching `contracts/mtc_contracts/identity.py` there is `930909a0` (2026-09-13, "bind benchmark metadata and type time exits") — no union merge after V5; P013 worktree `C:/tmp/P013_CONTRACT_V2_20260912` still at `da1fb184`. Phase B is independent of B-01 but is a stop condition for P013 (`H13:104`); it needs the P020 Lead as sole writer (`OD-20260913-P020-WRITER-1`) and its own T0 roster (B-5) — not tonight | git log on both worktrees |
| C-1 (name the two failing frozen probes) | NOT AUTHORIZED | not found in records — **verify** | — |
| C-2 (canonical-path migration + per-consumer audit) | NOT AUTHORIZED | not found in records — **verify** | — |
| C-3 (execute the preregistered benchmark) | NOT AUTHORIZED | **IN PROGRESS**: V1.6 preregistration frozen + roster + one-shot eligibility **ELIGIBLE** (09-14); derived measurement plan `d84b043a…` + tool `0bfd1891…` Sol PASS-WITH-NITS + Gemini PASS + Lead ALL EQUAL; **measurement HELD** by `OD-20260915-P020-MEASURE-GROK-1` until the Grok pre-screen (Grok back Thu 2026-09-18) → bounded measurement once → Gemini corroboration → exact Opus + Sol | handoff §2 P0-20 row |
| C-4 (P012 cost/full/production admission) | NOT AUTHORIZED; **not a B-01 prerequisite** | unchanged; P0-12 admission Q1..Q7 answered 09-14 (owner self-attestation), production admission not granted | handoff §2 P0-12 row |
| C-5 (fresh exact T0 roster on one settled head) | NOT AUTHORIZED | waits on C-1/C-2/C-3 at one head; Opus/Sol slots exist Wed/Fri but P0-20 is not in the Wednesday queue (measurement first) | Opus queue file |
| C-6 (re-attest 17/17 control coverage) | NOT AUTHORIZED | unchanged | — |
| **C-7 (the acceptance act)** | NOT AUTHORIZED | unchanged — **this is the B-01 gate act** | V5 §1.1 member 1 |
| D-1…D-9 | cannot begin before C-7 | unchanged | — |
| E (open after B-01): B-15 coordinate channel; B-04/B-12 producer adoption; B-07 engineering half; view-preimage ratification; authority beyond the bounded slice | open | unchanged | V5 Phase E |

## 2. What flips at C-7 (and only then)
| Becomes startable | Owner | Needs before dispatch | Artifact |
|---|---|---|---|
| **D-1** publish `P020AcceptedCatalogueDependencyV1` (twelve ordered members from **accepted** bytes) | P020 | C-7 head pinned | typed document + focused tests |
| **D-2** publish the canonical-path receipt schema + issuer id + verifier id + opaque `P020VerifiedCanonicalPathResult` behind `verify(receipt_bytes, completed_run_identity)` | P020 | C-7 | P020 tests: malformed / wrong-issuer / wrong-run / wrong-import-identity / nonaccepted-package receipts all refuse |
| **D-3** publish `TrialRejectionTaxonomyV1` frozen at the accepted head (closes P013 B-06 values) | P020 | C-7 | version-stamped taxonomy; `TrialGateAggregator` validates every code |
| **D-4** D026 RED/GREEN on the verifier (label-trusting copy accepts in RED; frozen verifier refuses in GREEN) | P020 + independent auditor | D-2 | RED/GREEN pair at one head |
| **D-5** P013 proof: no public constructor/caller path obtains `FullEvidenceSinkCapability` | P013 | D-1, D-2 | one P013 test (mechanism exists: sealed classifier `stages_conservation_identity.py:414-436`) |
| **D-6** P013 proof: the acceptance reader imports neither writer nor classifier and maps **all** verifier failures to `CANONICAL_RECEIPT_MISSING_OR_INVALID` | P013 | D-2 | one P013 test (import isolation is structural: `trial_catalog/__init__.py:14-18`) |
| **D-7** protected CI green + independent acceptance review at the frozen P020 identity | roster + CI | D-1…D-6 | pinned reports |
| **D-8** P013 design re-audit resolving every `PROVISIONAL-ON-P020` marker; re-pin `PKT13:30` (`556639f5…` → accepted head) and `PLAN:489` | P013 Lead | D-7 | dated re-audit record |
| **D-9** B-01 recorded closed; **full-writer package authorized separately by the owner** | P013 Lead / owner | D-8 | closure record naming the head + twelve digests |
**What does NOT flip at C-7:** Phase E (B-15 is "not P0-20-gated" and still open; B-04/B-12 producer adoption; B-07's three receipts; view-preimage ratification); the writer authorization itself (D-9 is a separate owner act); P0-31's provenance dependency on P0-13 (it needs the **consumed** interface, i.e. after D-9 and the writer slice — see `P031_M2_SCOPE_PACKET_DRAFT_20260915.md` §1 Track A); P0-21's B-01 (its own P0-12 dependency, unrelated to this one).

## 3. What the Lead can prepare before C-7 without authority (V5 §6.2 — none writes a repository byte)
| Item | State tonight | Night action |
|---|---|---|
| E-1 adapter-seam design note (six M-items + verifier signature) | not written | can be drafted from V5 §3.4 — **queued** (≈2 h) |
| E-2 merged-`identity.py` union specification + G-1…G-8 evidence contract | in V5 §2.2/§2.5 already; **blobs re-checked 14:39Z: P013 `1e9682ba`, P020 `fa57a771`, master base `75289fa6` — unchanged since V5** | E-2 is complete as written in V5; nothing to redo |
| E-3 documentation citation repairs (`PKT13:30` stale head; `PLAN:489` stale line; `HIST-2026-0045`) | not done | trivial note; **do in the morning summary** as a list, apply only under docs authority |
| E-4 `SimulatorClass` duplication question for the contract owner | not asked | one line in the morning asks (no rush) |
| E-5 B-15 joint-ratification request package | not written | draft from `DSGN:1708-1709`, `:1721-1723` — **queued** (≈1 h) |
| E-6 test plan for D-5/D-6 (not tests) | not written | **written below as the Codex brief skeleton** |

## 4. Pre-written brief skeleton — P013 Phase-D proofs D-5/D-6 (tests-only slice; DISPATCH ONLY after D-1/D-2 artifacts exist and the owner names the slice)
```
ROLE: implementer (Codex, via Invoke-CodexForClaude.ps1) under the Claude Lead; T0 package, tests-only slice; you accept nothing.
WORKTREE: <fresh worktree at the accepted P013 head after Phase-B rebase>; BASE: <sha>; P020 accepted head: <sha from C-7>.
WRITABLE: exactly two new test modules under MTC_COMMAND_CENTER/contracts/… (paths named by the Lead at dispatch) — NO product file, NO shared contract, NO P020 file.
TASK D-5: prove that no public constructor or caller path can obtain FullEvidenceSinkCapability:
  (a) enumerate every public symbol of the trial_catalog package (dir()) and assert none returns/exposes the capability;
  (b) attempt the three obvious forgeries (subclass, attribute injection, pickle round-trip) and assert each is refused by the sealed classifier (stages_conservation_identity.py:414-436);
  (c) RED arm: a scratch copy with the seal removed must let (b) succeed — record the RED output verbatim.
TASK D-6: prove the acceptance reader (a) imports neither the writer nor the classifier (assert on sys.modules after import + AST scan of trial_catalog/__init__.py:14-18 isolation), and (b) maps EVERY verifier failure class (malformed, wrong-issuer, wrong-run, wrong-import-identity, nonaccepted-package) to the single closed reason CANONICAL_RECEIPT_MISSING_OR_INVALID — one parametrized test per class, fed by the P020 D-2 refusal fixtures;
  RED arm: a scratch reader that passes a verifier detail string through must fail the single-reason assertion.
EVIDENCE: pinned interpreter 3.12.12; full contracts suite before/after (expect <N> passed → <N+k>); Ruff clean; git diff --check; repo guard PASS; RED outputs in files named LEAD_RED_ARM_D5.txt / LEAD_RED_ARM_D6.txt; SHA256SUMS of the two modules.
FORBIDDEN: any edit outside the two modules; any import of mega_walk_forward.py; any value for param_hash / preregistered_space_hash (Phase E, not this slice); any status label trusted from P020 (verify bytes only).
REPORT: LANE_REPORT.md (implemented / evidenced / neither per task), commands + cwd + exact output; no verdict.
```

## 5. Files
- This note: run root `P013_POST_P020_NOTE_20260915.md` → CT13 `QUEUED_PACKAGES_20260915/`.
- Source of truth for the sequence: `P013_DEPENDENCY_RESOLUTION_PLAN_20260913_V5.md` (unchanged tonight; this note does not supersede it).
