You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the Lead-written WP-P0-12 funding intake adapter: `IBKR_PAPER_BRIDGE/tools/funding_intake_adapter.py` with its fixture tests and the design note that scopes it. The Claude Opus 5 Lead wrote it under the owner's word "all" (`OD-20260915-BUILD-ABC-1` item b); the Lead may not accept its own code. The adapter is deliberately the NON-ACCEPTING half of an intake: it turns a real read-only capture (Path 1, `capture_own_account_evidence.py`) into the input shapes of `export_mtc_funding.py` and names, instead of filling, every field the captured bytes cannot supply. Your job is adversarial along three axes: (a) fabrication — does any output field carry a value that is not in the captured bytes or in a rule the export tool itself states? (b) overclaim — does any label, count or statement make the draft look like accepted or production evidence, or contradict the export tool's own limits and the owner's Q3 "Wait"? (c) leakage — does the full account address or anything credential-shaped reach an output?

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P012_INTAKE_20260915
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/funding_intake_adapter.py (complete; ~370 lines → three ranges), subject/test_funding_intake_adapter.py (complete; ~215 lines → two ranges), subject/P012_FUNDING_INTAKE_DESIGN_20260915.md (complete), subject/COMMIT_HEAD.txt, subject/DIFF_STAT_fcac0ac6_HEAD.txt, sources/export_mtc_funding_fcac0ac6.py (lines 1-215 — the docstring, the closed key domains and the evidence-kind constants — and lines 440-600 — coverage/retained-row/provenance validation), sources/capture_own_account_evidence_af921d75.py (lines 440-510 — `fill_derived` / `funding_derived`, the derived-view builders), sources/r1_DERIVED_EXTRACTION.json (complete), sources/r1_binding_packet_draft.json (complete), sources/r1_retained_rows_expected.json, sources/r1_intake_gap_report.json, sources/LEAD_PYTEST_focused.txt, sources/LEAD_RED_ARM_fabrication_fence.txt, sources/LEAD_INTAKE_r1_stdout.txt, sources/LEAD_RUFF.txt, sources/LEAD_GUARD.txt, sources/DECISIONS_rows_P012.md.

TASK.
1. **Fabrication audit, field by field.** For every key in `r1_binding_packet_draft.json` (bindings[*] and coverage), classify its value as: COPIED (verbatim from the derived view / manifest), TOOL-RULE (a constant or rule the export tool states — cite the export-tool line), DERIVED-LABELLED (computed but marked as a proposed rule, e.g. `funding_event_id`), or UNRESOLVED (`UNRESOLVED:D-n`). Flag as REQUIRED any value that is computed or guessed without a label — in particular confirm no oracle price appears anywhere (the back-derivation `usdc / (szi × rate)` ≈ 78 758.6 must not appear in any output), and that `positive_rate_payer = "LONG"` is exactly the export tool's `APPROVED_PAYER` constant.
2. **Design-note honesty.** The note claims the export tool has no `--mode` argument and only accepts `SYNTHETIC_FIXTURE` evidence, refusing everything else with `CANDIDATE_PRODUCTION_EVIDENCE_UNAVAILABLE`. Verify each of those claims against `export_mtc_funding_fcac0ac6.py` (cite lines). Verify the six decisions D-1..D-6 each map to a real gap in the tool's closed key domains (not to something the tool already answers).
3. **Completeness rule.** `completeness()` calls the window "complete" when both bounds are hour-aligned and the two funding passes are byte-identical. Is that rule sound as a *proposed* witness (D-6), and is it clearly labelled as the adapter's proposal rather than the tool's approved witness? Could a capture that misses a funding instant still pass it (e.g. a window that is hour-aligned but the venue stamped funding 41 ms past the hour at the window's end boundary — the r1 window ends 19:00:00Z exclusive; a funding at 19:00:00.041Z would fall outside — is that handled or at least stated)?
4. **Leakage.** The manifest carries the full mainnet address; the outputs must carry only the short form. Check every output and the test that asserts it. Check that no key-shaped value can enter (the adapter reads no credential — confirm by imports and code).
5. **Tests.** For each test, say what it proves and whether it could pass on a wrong implementation for the wrong reason. Confirm `LEAD_RED_ARM_fabrication_fence.txt` shows two tests failing when the mutant fills `oracle_price` by arithmetic.
6. **Scope.** `DIFF_STAT` must show exactly two new files under `IBKR_PAPER_BRIDGE/`; `export_mtc_funding.py` untouched; no protected path.
7. **What would make this the accepting half — and is any of it present?** List every change the export tool would need (new evidence kind, digest domain, `--mode`) and confirm none of it is in this diff.

OUTPUT (under 2500 words): (a) the field-by-field fabrication table; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative); a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage with ranges and continuations; (d) nonempty NOT VERIFIED (no execution); (e) one fenced JSON object {"part":"P012_INTAKE_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","fabricated_fields":[...],"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.

NOTE (attempt 2, HEAD 65c4bc40): since attempt 1 (HEAD 004a0711, voided by the CLI) only `subject/test_funding_intake_adapter.py` changed — the determinism test's no-wall-clock assertion was tightened per your NIT-01 (`sources/LEAD_RED_ARM_nit01_wall_clock.txt` shows the mutant it now catches). Re-check that arm; everything else is the same bytes.

NOTE (attempt 4, same HEAD 65c4bc40): attempt 3 (20:04-20:08Z, counted, PASS-WITH-NITS, fabricated_fields: []) could not open `sources/LEAD_RED_ARM_fabrication_fence.txt` because the file carried a non-ASCII character; every `sources/LEAD_*.txt` is now ASCII-sanitised (`?` for any non-ASCII byte). Read that file natively this time; everything else in this prompt stands.
