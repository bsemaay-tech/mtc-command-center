# WP-P0-12 — the six intake decisions as owner one-liners (D-1..D-6) — 2026-09-15 19:5xZ

Plain language. Each line: what it decides, the recommended word, what it does NOT do. Answer with the letter and the word (e.g. `D-1 R`). Every default = "nothing moves": the intake adapter stays `NONACCEPTING_INTAKE_DRAFT` and `export_mtc_funding.py` keeps refusing real captures with `CANDIDATE_PRODUCTION_EVIDENCE_UNAVAILABLE`. None of these words admits production, reorders the gate order (`OD-20260914-P012-ADMISSION-Q6`), or changes Q3 "Wait" — they only fix the SHAPE of the future production intake so it can be built and reviewed. The build after your words is a separate Lead slice (disclosed) plus the T1 roster.

| # | What it decides | Recommended word | What it does not do |
|---|---|---|---|
| D-1 | How a real funding row gets its fingerprint (`source_event_digest`): **R** = sha256 over the exact captured bytes of that row, tagged `HL_USERFUNDING_ROW_V1`, written as `<tag>:<hex>` | **`D-1 R`** | does not make any digest "accepted"; the tool still refuses until D-5 |
| D-2 | The event id rule: **R** = `hl-funding:<account-short>:<coin>:<time_ms>` (unique per account, coin and instant; stable across re-captures) | **`D-2 R`** | the venue's own hash is all zeros for funding — this id is ours, and it is labelled as such |
| D-3 | The event timestamp: **R** = keep the venue's stamp verbatim (it lands 31-60 ms past the hour) and add `interval_hour_utc` (floor to the hour) as the schedule key | **`D-3 R`** | never rounds the venue stamp away |
| D-4 | Oracle price: **R** = production requires an oracle capture at each funding instant; the back-derived price (`usdc / (size × rate)`) is allowed only as labelled research evidence, never admitted | **`D-4 R`** | no price is computed into the packet today (the adapter writes `UNRESOLVED:D-4`) |
| D-5 | Evidence kind for real read-only captures: **R** = admit `REAL_CAPTURE_READ_ONLY` only together with D-1 and a witness identity naming the ownership-signature record; until then `SYNTHETIC_FIXTURE` stays the only accepted kind | **`D-5 R`** | this is the gate that keeps real evidence out until the shape is reviewed |
| D-6 | The "whole interval captured" witness: **R** = two byte-identical funding passes + hour-aligned window, **plus** (i) a 1-second end-boundary tolerance for the venue's late stamps (the Gemini reviewer's point: a payment at `19:00:00.041Z` settles the 18-19 interval but falls outside a window ending `19:00:00Z`), and (ii) the expected-count check (every hour with an open position produced a payment) taken from the fills pass of the same capture — or declared out of scope in writing | **`D-6 R`** (i) adopted, (ii) as a separate coverage check | the adapter's current rule is labelled "proposed"; nothing asserts completeness for production today |

After all six words: one Lead slice adds the accepting half to `export_mtc_funding.py` behind the six rules (explicit `--evidence-kind` admission, never a `--mode` toggle), with RED/GREEN against the r1 capture; Gemini detection; T1 roster; then the production intake can be run on a real capture — still without admitting production (Q3 "Wait" stands until the owner says otherwise).

Records: design note `P012_FUNDING_INTAKE_DESIGN_20260915.md` (decision rows with reasons), adapter `65c4bc40`, r1 dry run `intake_r1/`, Gemini detection ×2 (`fabricated_fields: []`). Recorded by Claude Opus 5 Lead (session 5, `03c6c8`).
