# LEAD_VERIFICATION — WP-P0-12 funding intake, slice 4: the ACCEPTING half of `export_mtc_funding.py` (D-1..D-6) — 2026-09-16 (session 6, `4a8233`)

**Authority:** `OD-20260916-P012-INTAKE-D1-D6-R-1` (owner `D all R`, 08:05 UTC+3): "ONE Lead slice may add the accepting half to `export_mtc_funding.py` behind these six rules (explicit `--evidence-kind` admission, never a `--mode` toggle) with RED/GREEN against the r1 capture, disclosed, Gemini detection, T1 roster before merge. This row admits NO production evidence: `OD-20260914-P012-ADMISSION-Q3` "Wait" and the gate order Q6 stand."
**Role:** Lead = disclosed builder. The Lead never accepts its own code; the T1 roster (Wednesday optional lane 8 / Friday Sol) decides.
**Branch / worktree:** `feature/p012-funding-intake-adapter-20260915` in `C:/tmp/P012_INTAKE_20260915` (HEAD before this slice `4c802e9b`; base `origin/master` `fcac0ac6`).
**Stage:** `IBKR_PAPER_BRIDGE/` (primary path `IBKR_PAPER_BRIDGE/tools/export_mtc_funding.py`). Tier: T1 (non-economic offline tool; no runtime, no host, no key, no network). Protected-scope check: `tools/` and `tests/` only; no `bridge/` runtime module touched.

## 0. G1 scope contract (written before code, 09:5x UTC+3)
Allowed writes: `IBKR_PAPER_BRIDGE/tools/export_mtc_funding.py`, `IBKR_PAPER_BRIDGE/tools/funding_intake_adapter.py`, `IBKR_PAPER_BRIDGE/tests/test_mtc_funding_export_real_capture.py` (new), `IBKR_PAPER_BRIDGE/tests/test_funding_intake_adapter.py`. Forbidden: everything else (in particular `bridge/**`, `tests/test_mtc_funding_export.py` — its carried fences stay byte-identical).

Design constraints found in the repository before designing (repository wins over the prompt):
1. `tests/test_mtc_funding_export.py::test_the_module_exposes_no_production_switch` is a CARRIED FENCE pinning `ACCEPTED_EVIDENCE_KINDS == (SYNTHETIC_FIXTURE,)`; `test_two_retained_payments_produce_the_exact_candidate_bytes` pins the synthetic candidate BYTES. Therefore the real kind is NOT added to `ACCEPTED_EVIDENCE_KINDS` and the synthetic profile's candidate bytes do not change. The real kind is a second, explicitly declared **evidence profile**: the caller names it (`--evidence-kind REAL_CAPTURE_READ_ONLY` / keyword-only `evidence_kind=`), the packet must carry the same kind in `coverage.evidence_kind` and every `provenance.evidence_kind`, and a disagreement refuses (`CANDIDATE_EVIDENCE_KIND_MISMATCH`). The default profile stays SYNTHETIC — no toggle, no mode.
2. The tool joins Bridge-retained schema-v10 rows with a source-verified binding packet. No schema-v10 store carrying the owner's real events exists (KVM2 store is v4; the capture is not a Bridge store), so the CLI on the r1 capture still refuses at the snapshot step; the RED/GREEN against r1 runs through the pure function with fixture retained rows built from the r1 values (labelled) and through the adapter on the real r1 directory.
3. The r1 capture carries NO oracle read at the funding instants → under D-4 the honest outcome on r1 is a specific refusal `CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE`, not a candidate. A candidate is materialized only when a binding names an oracle capture by digest and pointer.
4. `Design defect patterns` applied: venue bytes are parsed (JSON element spans), never grepped (P5); identities are digests and re-derived ids, not labels (P8); every funding row inside the captured pass bytes gets a terminal disposition — inventoried, or the run refuses (P13); every new test has a RED arm (P10).

The six rules as the tool enforces them under the REAL profile (each names both producers and has a failing input in the tests):
| Rule | Producer A (packet) | Producer B (tool re-derivation) | Refusal |
|---|---|---|---|
| D-1 | `source_event_digest = HL_USERFUNDING_ROW_V1:<hex>` | sha256 over `coverage.source_witnesses[event_id]` (exact row bytes), which must be the exact byte span of element `i` of the funding pass bytes (`provenance.source_locator = <file>#/<i>`) | `CANDIDATE_BINDING_INVALID` |
| D-2 | `funding_event_id` | `hl-funding:<coverage.account_scope>:<row.delta.coin>:<row.time>` | `CANDIDATE_BINDING_INVALID` |
| D-3 | `event_timestamp`, `interval_hour_utc` (ninth binding key, REAL only) | `row.time` rendered `YYYY-MM-DDTHH:MM:SS.mmmZ` verbatim; floor-hour `YYYY-MM-DDTHH:00:00Z` | `CANDIDATE_BINDING_INVALID` |
| D-4 | `oracle_price_source` | must name an oracle capture `<sha256>#<json-pointer>`; `UNRESOLVED:*` or any `DERIVED*` label refuses; the oracle capture is NAMED, not re-opened (stated limitation) | `CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE` |
| D-5 | `coverage.witness_identity` | must carry `manifest_sha256=<64hex>`, `run <id>` and `ownership OWNERSHIP_EVIDENCE: VERIFIED <account_scope>` (the ownership-signature record of the capture manifest, named by digest) | `CANDIDATE_COVERAGE_INVALID` |
| D-6 | `coverage.funding_witness_passes` (>=2 identical hex blobs = the exact `userFunding` response bytes), `coverage.fills_witness` (exact `userFillsByTime` bytes), hour-aligned `interval_*` | passes byte-identical; `sha256(pass) == provenance.source_sha256`; in-interval iff `start <= stamp < end + 1 s`; grid = whole hours `start..end`; expected instants = grid hours where the fills-derived position is non-zero == observed instants; each row's `szi` == fills-derived position at its stamp; every row in the pass bytes inventoried | `CANDIDATE_COVERAGE_INVALID` |
Also under REAL: `raw_rate == row.delta.fundingRate` text; payer convention `LONG` cross-checked against the row's signs (`usdc` non-zero must have sign `-(sign szi * sign rate)`); `packet_version = REAL_CAPTURE_FUNDING_BINDING_PACKET_V1`; the candidate is `artifact_kind = REAL_CAPTURE_FUNDING_CANDIDATE_V1`, `synthetic_only: false`, `admission_status = REFUSED_REAL_CAPTURE_READ_ONLY_NOT_A_PRODUCTION_RECORD`, structurally non-consumable (no `schedule_id` / `events` / `settlement_currency` at the root), `production_mode` stays `UNAVAILABLE_PENDING_SOURCE_EVENT_DIGEST_DOMAIN`... NO: the digest domain IS now decided for the real kind, so the constant's name is stale prose; the value is kept verbatim because `test_no_literal_evidence_kind_unlocks_production` pins it and production admission is genuinely still unavailable (Q3). Recorded as a follow-up wording nit for the roster, not changed here.

Adapter (same branch, my own unmerged candidate): adds a fourth output `binding_packet_real_capture.json` (+ `.sha256`) in the accepting shape (D-1..D-6 applied; `oracle_*` stay `UNRESOLVED:D-4` on r1 because no oracle was captured), computed from the raw pass bytes named in the manifest (sha256 re-verified against `response_sha256`); the three draft outputs keep their bytes and label. `retained_rows_expected.json`'s `payload_digest` label is corrected from `UNRESOLVED:D-1` to `UNAVAILABLE:BRIDGE_OBSERVATION_REQUIRED` (the Bridge normalized digest is a Bridge observation, not the D-1 domain — the design note's D-1 row had conflated them; the owner's D-1 word names only `source_event_digest`).

## 1. What was built (09:0x-09:4x UTC+3; commit identities appended in §5)
| Item | Fact |
|---|---|
| `tools/export_mtc_funding.py` | second evidence profile `REAL_CAPTURE_READ_ONLY` (`_EvidenceProfile` table: kind, packet version `REAL_CAPTURE_FUNDING_BINDING_PACKET_V1`, artifact kind `REAL_CAPTURE_FUNDING_CANDIDATE_V1`, file names `funding_candidate_real_capture.json` + `.sha256`, admission status `REFUSED_REAL_CAPTURE_READ_ONLY_NOT_A_PRODUCTION_RECORD`, limitations, closed key sets `REAL_BINDING_KEYS` (= 8 + `interval_hour_utc`) and `REAL_COVERAGE_KEYS` (= 10 + `fills_witness`, `funding_witness_passes`), `synthetic_only=False`, 1-second end tolerance); keyword-only `evidence_kind=` on `build_funding_candidate` (default SYNTHETIC) and CLI `--evidence-kind {REAL_CAPTURE_READ_ONLY,SYNTHETIC_FIXTURE}` (no `--mode`); kind agreement (`CANDIDATE_EVIDENCE_KIND_MISMATCH`); D-1 digest form + exact-span re-hash; D-2 id re-derivation; D-3 verbatim stamp + hour key; D-4 oracle locator (`CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE`); D-5 witness-identity tokens; D-6 whole-hour window, >=2 byte-identical passes, pass digest = provenance `source_sha256`, every captured row inventoried (P13), fills-derived expected hours == observed hours, per-row `szi` == fills-derived position, contiguity of the fills history; raw_rate and payer-sign cross-checks; report carries `evidence_kind`. `ACCEPTED_EVIDENCE_KINDS` UNCHANGED `(SYNTHETIC_FIXTURE,)`; synthetic candidate bytes UNCHANGED (the pinned-bytes test passes). |
| `tools/funding_intake_adapter.py` | reads the raw response files named by the manifest when present (sha256 re-verified against `response_sha256`; mismatch refuses), parses the funding pass into exact element spans, emits the fourth output `binding_packet_real_capture.json` (3 closed keys; bindings 9 keys; coverage 12 keys; `oracle_*` = `UNRESOLVED:D-4`); gap report gains `real_capture_packet` (ruling, decisions applied, the D-4 residue, expected export-tool outcome `CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE`); `retained_rows_expected.json` `payload_digest` label corrected to `UNAVAILABLE:BRIDGE_OBSERVATION_REQUIRED` (was `UNRESOLVED:D-1` — a conflation of the Bridge normalized digest with the D-1 source domain in my own 09-15 design note; the owner's D-1 word names only `source_event_digest`). The three draft outputs keep their label. |
| `tests/test_mtc_funding_export_real_capture.py` (new, 57 tests) | explicit admission (never by default; mismatch both ways; unknown kinds unavailable; CLI choices, `--mode` refused); D-4 five refusals on r1-shaped bindings; the labelled candidate (root keys, no MTC selection key, 9-key bindings, `completion_check`, report fields, limitations name the oracle and Q3); determinism + capture-day-only instants; D-1 (re-serialization, tag forms, located span, conflation); D-2 (pass-level disposition names both identities; binding-level relocation); D-3 (four stamp/hour forms; ninth key required); raw_rate + payer sign; D-5 (five identity forms); D-6 (three unaligned windows; three pass-list forms; pass digest; 1-s tolerance admits the late stamp in a [15:00,18:00) window; >1 s refuses; open position with no payment names the missing hour; `szi` disagreement; fills gap; non-fill rows incl. `[]`; uninventoried captured row; foreign-coin row; empty inventory; closed witness shape); CLI stages the real candidate only under the named profile and refuses the same packet by default. Fixture material: the exact r1 funding pass bytes (433 B, zero hash, no address), venue-shaped fills with r1's sizes/times/`startPosition` and synthetic tx hashes, FIXTURE retained rows and a FIXTURE oracle locator — all labelled in the module docstring. |
| `tests/test_funding_intake_adapter.py` (+4 tests, 11 total) | real packet built under the ruled shape from raw bytes (closed key sets, D-1 digest of the exact span, D-2/D-3 values, witness identity tokens, no full address); the packet refuses under D-4 through the exporter and validates once a FIXTURE oracle capture is named, and is a kind mismatch under the default profile; absent raw bytes -> `real_packet` None + `NOT_BUILT`; tampered raw bytes refused on `response_sha256`; deterministic write-once outputs, capture-day-only instants. Two pins changed with reason: `payload_digest` `UNRESOLVED:D-1` -> `UNAVAILABLE:BRIDGE_OBSERVATION_REQUIRED` (lines 122, 142); `load_capture` now returns four values. |

## 2. RED / GREEN (all commands real; outputs in this folder)
| Arm | Command / result | File |
|---|---|---|
| RED, whole slice | new suite against the pre-slice exporter bytes (`git show 4c802e9b:.../export_mtc_funding.py` in a scratch copy): **57 failed** | `LEAD_RED_ARM_new_tests_vs_4c802e9b.txt` |
| RED, mutant M1 | `REAL_END_TOLERANCE_SECONDS = 0` -> 2 failed: the [15:00,18:00) tolerance test (the 18:00:00.060 stamp becomes out-of-interval) and the happy path (it pins `end_tolerance_seconds: 1` in `effective_interval`) | `LEAD_MUTANT_M1_tolerance.txt` |
| RED, mutant M2 | `szi` cross-check disabled -> 1 failed (`test_d6_a_row_position_that_disagrees_with_the_fills_refuses`) | `LEAD_MUTANT_M2_szi.txt` |
| RED, mutant M3 | D-1 compares the canonical re-serialization instead of the raw span -> 11 failed incl. `test_d1_digest_must_hash_the_exact_captured_row_bytes_not_a_reserialization` (sharpened after a first run showed it passing only because the other binding refused) | `LEAD_MUTANT_M3_d1_canonical.txt` |
| RED, mutant M4 | D-5 identity check dropped -> 5 failed (all five identity forms) | `LEAD_MUTANT_M4_d5.txt` |
| GREEN, focused | `tests/test_funding_intake_adapter.py tests/test_mtc_funding_export_real_capture.py tests/test_mtc_funding_export.py` -> **229 passed, 1 skipped** (`--basetemp C:/bt_s4/run6`, pinned Bridge 3.12.12) | re-run recorded in §5 after the commit |
| Ruff | `ruff format` (4 files) + `ruff check`: **5 findings, all pre-existing on the HEAD bytes** (UP035, 3x ISC004 in the synthetic limitations tuple, SIM101) — 0 new; the three files other than the exporter: `All checks passed!` | `LEAD_RUFF_s4.txt` |
| Real r1 dry run (adapter, read-only on the capture) | `funding_intake_adapter.py --run-dir .../P012_PATH1_REAL_CAPTURE_20260915/r1 --out .../intake_r1_s4` -> `events=2 fills=5 complete=True ... real_packet=BUILT real_packet_tool_outcome_expected=CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE`; capture dir tree-hash unchanged before/after; full owner address in 0 outputs (the two `0x`+40-hex hits are the venue's zero hash in the draft); real packet sha256 `f3bde0b7...` | `LEAD_INTAKE_r1_s4_stdout.txt`, `../intake_r1_s4/` |
| Real r1 dry run (exporter, pure function, FIXTURE retained rows) | REAL profile on the packet as emitted: **refused `CANDIDATE_ORACLE_EVIDENCE_UNAVAILABLE`** (D-4; r1 captured no oracle read) — D-1/D-2/D-3/D-5/D-6 all passed on the real bytes (expected hours {17:00, 18:00} == observed; opening position `0.0` from the real fills); default profile: `CANDIDATE_EVIDENCE_KIND_MISMATCH`; with a FIXTURE oracle locator: `REAL_CAPTURE_CANDIDATE_BUILT`, candidate sha256 `8eb1efe3...`, `synthetic_only false`, admission `REFUSED_REAL_CAPTURE_READ_ONLY_NOT_A_PRODUCTION_RECORD`, root keys carry no `events`/`schedule_id`/`settlement_currency` | `LEAD_EXPORTER_r1_real_packet.txt` |

## 3. Honest limits (for the roster)
- The CLI on r1 still refuses at the snapshot step: no schema-v10 store carries the owner's events (KVM2 is v4; the capture is not a Bridge store). The join with Bridge-retained rows is the Bridge's half and outside this slice; the tests use FIXTURE retained rows and say so.
- D-4 is a NAMING check (digest + JSON pointer of an oracle capture); the tool does not open the oracle capture and does not re-verify the price. Stated in `REAL_CAPTURE_EVIDENCE_LIMITATIONS`. The oracle capture tool does not exist yet (next engineering step under P0-12, owner word needed).
- D-5 is a NAMING check on the witness identity (run id, manifest digest, ownership record + scope); the manifest is not re-opened by the tool. The adapter computes the manifest digest from the file bytes it read.
- `PRODUCTION_MODE_UNAVAILABLE = "UNAVAILABLE_PENDING_SOURCE_EVENT_DIGEST_DOMAIN"` — the value is now stale prose for the real kind (its digest domain is ruled) but is kept verbatim: production admission is genuinely still unavailable (Q3 "Wait") and `test_no_literal_evidence_kind_unlocks_production` pins the constant. Wording follow-up for the roster, not changed here.
- Formatting: `ruff format` reflowed 74 pre-existing lines of `export_mtc_funding.py` (148 diff lines) with no semantic change. Committed SEPARATELY as a format-only commit (verify: `ruff format --check` on its parent fails, on the commit passes; `git diff <parent> <commit>` shows only reflow) so the slice commit's diff is the semantic change alone.
