You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-12 funding-intake slice-4 **T0 repair round 1**: commit `a871e429` on `feature/p012-funding-intake-adapter-20260915` = `9ef072a8` (the accepting half the exact-Opus reviewer read this afternoon and returned REQUEST_CHANGES on, one REQUIRED finding) + a small exporter change + two tests. REQUIRED-1: in `_real_completion` (D-6 ii) the accepting branch taken when the fills witness carries no fill for the symbol derives the position from the payments' own `szi`, yet the emitted `completion_check` said `"position_source": "coverage.fills_witness"` — a false statement about the artifact's own evidence source, on a path no test reached (the reviewer's three mutants — the no-fills branch returning 0, the label relabelled, `opening_position` always None — all survived the suite). The Claude Opus 5 Lead built the slice and this repair (disclosed); the Lead may not accept its own code. Your job is adversarial along three axes: (a) **does the repair make the statement true and fence it** — read the patched `_real_completion`: the payments position is resolved once before the grid walk; `position_source` is `REAL_POSITION_SOURCE_PAYMENTS` when and only when no fill for the symbol exists, else `REAL_POSITION_SOURCE_FILLS`; `REAL_CAPTURE_EVIDENCE_LIMITATIONS[3]` now states the second case; would each of the three mutants now fail at least one of the two new tests (reason from the test bodies and the code), and could either test pass for the wrong reason (e.g. the no-fills test refused earlier by another D-6 rule, or accepted through the fills path)? Check that the [17:00, 18:00) window with r1's two payments really reaches the no-fills branch: `_real_fills(b"[]", "BTC")` must return `(None, [])` and the other-coin fixture must be filtered out by symbol; (b) **behaviour preservation** — no refusal code or refusal message changed; the normal r1 path still reports `coverage.fills_witness` and `opening_position "0.0"`; the synthetic profile is untouched (the diff must not touch the synthetic candidate path); scope = exactly the two files (+94/−14); (c) **record honesty** — the Lead's `LEAD_*.txt` files must say what the record says (RED: mutant M1 → 2 failed/230 passed, M2 → 3 failed/229, M3 → 1 failed/231, each failure a new test; GREEN 232 passed / 1 skipped on the three modules; full Bridge suite 1669 passed / 1 skipped; guard PASS).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P012_INTAKE_R1_20260917
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_9ef072a8_a871e429.patch (complete), subject/DIFF_STAT_9ef072a8_a871e429.txt, subject/COMMIT_a871e429.txt, subject/export_mtc_funding_HEAD_lines_150-200_constants_limitations.py (complete), subject/export_mtc_funding_HEAD_lines_1140-1290_real_fills_completion.py (complete — `_real_fills` and the patched `_real_completion`), subject/test_real_capture_HEAD_lines_1-120_fixtures.py (complete — FUNDING_PASS, T1/T2 stamps 17:00:00.041Z / 18:00:00.060Z, FILLS_R1, `_fill`), subject/test_real_capture_HEAD_lines_195-275_helpers.py (complete — `real_coverage`, `r1_bindings`, `build`), subject/test_real_capture_HEAD_lines_890-975_new_tests.py (complete — the two new tests and their neighbours), sources/OPUS_REPORT_attempt1_9ef072a8.md lines 262-300 (REQUIRED-1) and 329-335 (summary), every sources/LEAD_*.txt (complete).

TASK.
1. Trace the no-fills test: with `fills=b"[]"` (and with the ETH-only fill), window [17:00, 18:00), r1 bindings and the r1 funding pass: `_real_fills` result; `payments_position`; the grid (which hours); `expected` vs `observed`; the per-row szi check; the emitted `completion_check`. Which assertion of the new test would fail on each of the three mutants (M1 no-fills → `Decimal(0)`; M2 label relabelled; M3 `opening_position` always None)?
2. Trace the normal-path test: `opening_position` must be the string of the first fill's `startPosition` (`"0.0"`) and `position_source` the fills label; which mutant does it kill?
3. Could the no-fills test be accepted through the fills path by mistake (e.g. the other-coin fixture not filtered)? Could it be refused before reaching `_real_completion` (kind, shape, D-1..D-5, whole-hour, passes, inventory)? If either, the test proves nothing: REQUIRED.
4. Behaviour preservation and scope as in (b); honesty as in (c).
5. The first read's NITs 1-8 are out of this commit's scope; if any is actually REQUIRED in your reading, say so, but do not re-litigate design notes.

OUTPUT (under 1800 words): (a) the two traces; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) — a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage; (d) nonempty NOT VERIFIED; (e) one fenced JSON object {"part":"P012_INTAKE_R1_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","required_1":"CLOSED|OPEN|PARTIAL","no_fills_branch_reached":true|false,"mutants_killed":{"M1":true|false,"M2":true|false,"M3":true|false},"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
