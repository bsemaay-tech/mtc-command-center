# LEAD_VERIFICATION — WP-P0-12 funding intake slice 4 (the accepting half), T0 repair round 1 (`a871e429`) after the lane-8 exact-Opus read of `9ef072a8` returned REQUEST_CHANGES — 2026-09-17 14:2x-14:5x UTC+3

**Trigger:** lane 8 (optional lane of the Wednesday queue, run by hand Thu 13:55-14:23 UTC+3 on the Pro window): exact `claude-opus-5` xhigh T0 read of `9ef072a8` → **REQUEST_CHANGES**, one REQUIRED finding + 8 NITs (`OPUS_QUEUE_20260916/P012INTAKE/ATTEMPT1_REQUEST_CHANGES_9ef072a8/OPUS_REPORT.md`, 335 lines; the reviewer hit the launcher's turn cap right after writing the complete report and its `VERDICT:` line — counted, like lane 3's attempt 3). Everything else VERIFIED by the reviewer: explicit admission with no toggle and intact fences; D-1..D-6 refuse all 17 of its own failing inputs; the 1-s tolerance reproduced; the r1 dry run reproduced byte for byte (refused at D-4 as recorded); the format-only parent AST-identical; scope and safety clean.
**REQUIRED-1:** `_real_completion` (D-6 ii) — when the fills witness carries no fill for the symbol the position is taken from the payments' own `szi`, yet the emitted `completion_check` said `position_source: "coverage.fills_witness"` (and `REAL_CAPTURE_EVIDENCE_LIMITATIONS[3]` claimed the fills-derived position unconditionally): a false statement about the artifact's own evidence source on an accepting branch that no test reached; the reviewer's three mutants of the block (no-fills branch → 0; the label relabelled; `opening_position` always None) survived the whole focused suite. Fix named as small: ~10 lines + two tests.
**Role:** Lead = builder of the slice and of this repair (disclosed; `OD-20260916-P012-INTAKE-D1-D6-R-1`); T0 cap 3 rounds — round 1.

## Lead reproduction (before writing anything)
`LEAD_REPRO_lane8_REQUIRED1.txt`: at `9ef072a8`, window [17:00Z, 18:00Z), the r1 bindings + funding pass and (a) `fills = b"[]"`, (b) an ETH-only fill → **both ACCEPTED** with `completion_check = {expected [17:00, 18:00], observed [17:00, 18:00], opening_position None, position_source 'coverage.fills_witness'}` — the false label, CONFIRMED. (With the full r1 window [15:00, 19:00) the same inputs are refused for the hour mismatch — the reviewer's five-payment construction and this two-payment window both reach the branch.)

## Repair (`a871e429`, +94/−14, two files)
- `tools/export_mtc_funding.py`: `REAL_POSITION_SOURCE_FILLS = "coverage.fills_witness"`, `REAL_POSITION_SOURCE_PAYMENTS = "captured funding rows: the fills witness carries no fill for the symbol, so the position is the constant szi the payments themselves report and the per-row szi cross-check is vacuous"`; `_real_completion` resolves `payments_position` once before the grid walk (same refusal, same message when the payments disagree), `position_at` returns it when set, else the fills-derived position; `completion_check.position_source` names the producer that was actually used; `REAL_CAPTURE_EVIDENCE_LIMITATIONS[3]` states the second case ("completion_check.position_source names which"). No refusal code or refusal message changed; the synthetic profile emits no completion check → its bytes are untouched (carried fences green).
- `tests/test_mtc_funding_export_real_capture.py`: `test_d6_the_completion_check_names_the_fills_witness_as_the_position_source` (normal r1 path: fills label, `opening_position == "0.0"`, expected hours) and `test_d6_without_a_fill_for_the_symbol_the_position_source_is_the_payments[empty-fills-witness|another-coin-only]` ([17:00, 18:00) window: accepted, payments label, `opening_position None`, expected == observed == [17:00, 18:00], the fills witness echoed, the limitation sentence present). Ruff-formatted (one blank line added by `ruff format`).

## Evidence
| Arm | Result | File |
|---|---|---|
| RED, mutant M1 (no-fills branch returns `Decimal(0)`) on the patched bytes | **2 failed / 230 passed / 1 skipped** — exactly the two parametrizations of the no-fills test | `LEAD_RED_lane8_R1_M1_no_fills_branch_returns_zero.txt` |
| RED, mutant M2 (`position_source` relabelled on both branches) | **3 failed / 229 passed / 1 skipped** — exactly the three new test cases | `LEAD_RED_lane8_R1_M2_position_source_relabelled.txt` |
| RED, mutant M3 (`opening_position` always None) | **1 failed / 231 passed / 1 skipped** — exactly the normal-path test | `LEAD_RED_lane8_R1_M3_opening_position_always_none.txt` |
| GREEN (three modules) | **232 passed, 1 skipped** | `LEAD_PYTEST_GREEN_R1.txt` |
| Full Bridge suite (ASCII basetemp `C:/bt_s4/p012_full`) | **1669 passed, 1 skipped, 0 failed** | `LEAD_FULL_BRIDGE_SUITE_R1.txt` |
| Ruff | `format --check` both files formatted; `check` 5 findings at HEAD → 5 at `a871e429` (0 new) | console |
| Guard | `RESULT: PASS`; staged exactly the two files | `LEAD_GUARD_R1.txt` |
| Gemini delta (`P012_INTAKE_R1_GEMINI`, packet `P012_INTAKE_R1_20260917`, 15 files) | COUNTED **PASS**, REQUIRED-1 CLOSED, no-fills branch reached, all three mutants killed, 0 findings; 19 reads / 0 outside / 0 mismatches; 88 s | `GEMINI/` (+ `LEAD_ADJUDICATION.md` there) |

## Carried NITs of the first read (not part of this commit)
NIT-1 stale `ACCEPTED_EVIDENCE_KINDS` message under the real profile; NIT-2 the tolerance boundary is `[end, end+1 s)` (docstring sentence); NIT-3 stale `production_mode` / `report_kind` / `synthetic_schedule_id` labels on a real run + the "FIXTURE retained rows" limit not in `REAL_CAPTURE_EVIDENCE_LIMITATIONS`; NIT-4 adapter `DECISIONS["D-1"]` gloss still mentions the payload-digest domain; NIT-5 the D-4 `"derived"` substring blacklist can only false-refuse (`#/derived/oraclePx`); NIT-6 the window is a payment-stamp window (accrual coverage `[start − 1 h, end)`) and the capture request does not mirror the +1 s tolerance — **owner sentence needed before the next capture is commissioned** (relevant to r2: window [07:00Z, 11:00Z) ends flat; payments at 08/09/10:00Z + 41-60 ms are inside); NIT-7 stale `UNRESOLVED:D-n` labels in the draft packet after the ruling; NIT-8 adapter `array_spans` trailing-byte check, float vs integer stamp arithmetic (0 disagreements in 14 450 stamps), the format-only commit's five trailing commas. All → one follow-up slice after the Sol read.

## Roster
Lane 8 re-pinned to `a871e429` (launcher, brief addendum 2026-09-17, agent brief, queue row, log); attempt 1 archived; attempt 2 (14:33) CAPPED at once by the Pro 5-hour limit (resets 18:00 UTC+3) → archived `ATTEMPT2_CAPPED_1133Z/`, alarm 18:01 to relaunch. Sol (Sat 2026-09-19) generates from the pin that stands. The Lead never accepts its own code.

Recorded by Claude Opus 5 Lead (session 6, `4a8233`).
