You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the Lead-written WP-P0-27 OPS-A tests CI workflow: PR #194, one new file `.github/workflows/opsa-tests.yml` at commit 63b7bbe0 (base master fcac0ac6). The Claude Opus 5 Lead wrote it under the owner's word "all" (`OD-20260915-BUILD-ABC-1` item c: "wire the OPS-A backup tests into CI as a second small PR"); the Lead may not accept its own change; per `CI_POLICY.md` a workflow file is audit tier T1 and the T1 verdict belongs to the Wednesday exact-Opus lane 5 (`OD-20260915-P027-T1-WEDOPUS-1`) — you are the detection reviewer before it. The job is INFORMATIONAL: the ruleset on master requires exactly `Bridge suite (Python 3.12)` + `pine-alert-guard` and this PR does not touch it. Your job is adversarial along three axes: (a) least privilege and blast radius — can this workflow write, deploy, contact a host or venue, read a secret, run on a schedule, or run untrusted PR code with write tokens? (b) correctness — does the job run the 33 `test_opsa` unittest cases on the right bytes with the right working directory, and is the Windows `core.longpaths` step placed before checkout as the RED evidence requires? (c) evidence honesty — do the run JSONs and the RED excerpt say what the evidence note claims (GREEN run 34983809921 success; RED demo PR #195 run 34983897746 failure; PR #194 OPEN, unmerged)?

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P027_OPSA_20260915
Any other path appearing in copied documents is provenance text, never a file to open — that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/opsa-tests_63b7bbe0.yml (complete), subject/DIFF_fcac0ac6_HEAD.diff (complete), subject/DIFF_STAT_fcac0ac6_HEAD.txt, subject/COMMIT_HEAD.txt, sources/P027_OPSA_CI_EVIDENCE_20260915.md (complete), sources/RED_RUN_34983897746_failed_excerpt.txt (complete), sources/opsa_tests_run_34983809921_GREEN.json, sources/opsa_tests_run_34983897746_RED.json, sources/pr194_state.json, sources/pr195_state_RED.json, sources/ci_fcac0ac6.yml (complete — the required workflow's conventions: permissions, action pins, checkout options), sources/research-gates_fcac0ac6.yml (lines 1-60 — the informational job convention on master), sources/test_opsa_fcac0ac6.py (lines 1-60 and the class/def names — count the tests: `grep def test_`), sources/CI_POLICY_CT13.md (lines 1-40 — tier and acceptance paragraph), sources/DECISIONS_rows_P027.md.

TASK.
1. **Least privilege table.** For the new job: `permissions`, `on:` triggers (no `schedule`, no `pull_request_target`, no `workflow_dispatch` with inputs that run code), `actions/checkout` options (`persist-credentials: false`; pin form vs `ci.yml`'s convention), `actions/setup-python` pin, `concurrency`, `timeout-minutes`, any `secrets.*`, any network step beyond the action pulls and pip. Each row: file:line, EQUAL/DIFFERENT to `ci.yml`'s convention, verdict. A write permission, a secret, a schedule or a host/venue contact = REQUIRED.
2. **Correctness of what runs.** The `run:` line vs the test module: `python -m unittest test_opsa -v` in `MTC_COMMAND_CENTER/tools/opsa` — count `def test_` in `test_opsa_fcac0ac6.py` and compare with the evidence note's "33 tests"; confirm `git config --system core.longpaths true` precedes checkout on `windows-2025`; confirm the job is NOT `continue-on-error` (an informational job must still go red when tests fail).
3. **Evidence honesty.** From the JSONs: run 34983809921 conclusion, head sha, workflow name; run 34983897746 conclusion; PR #194 state/mergeable/head; PR #195 state (closed) — do they match the evidence note line by line? Read the RED excerpt: is the failure a real test failure (which test, why) rather than an infrastructure error?
4. **Blast radius.** State that the ruleset's required checks are unchanged by this file (the file cannot change a ruleset) and that a red informational job cannot block a merge; say whether any copied document claims otherwise.
5. **Scope.** `DIFF_STAT` must show exactly one new file under `.github/workflows/`; nothing else.
6. **EXACT read coverage** (file, ranges, continuations) and a non-empty NOT VERIFIED (you cannot execute the workflow or query GitHub).

Output: Markdown with the least-privilege table (1), the correctness statement (2), the evidence table (3), the blast-radius statement (4), the scope statement (5), the coverage list (6), findings numbered with severity REQUIRED / NIT and exact file:line, then EXACTLY this fenced JSON block and the sentinel line:
```json
{"part": "P027_OPSA_GEMINI", "verdict": "PASS|PASS-WITH-NITS|REQUEST_CHANGES|BLOCK", "evidence_class": "SUPPLEMENTAL_UNEXECUTED", "test_count_in_module": 0, "findings": [{"id": "...", "severity": "REQUIRED|NIT", "path": "file:line", "description": "..."}], "required_scope_unread": []}
```
GEMINI_READ_ONLY_OK
