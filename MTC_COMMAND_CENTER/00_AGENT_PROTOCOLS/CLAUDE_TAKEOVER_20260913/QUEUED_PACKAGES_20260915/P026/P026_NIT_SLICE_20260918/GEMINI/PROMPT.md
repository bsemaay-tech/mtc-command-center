You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-26 OPS-A **NIT slice**: commit `505af399` on the P0-26 repair branch = `e114ed31` (the candidate the exact-Opus T0 read passed with NITs on 2026-09-17; you reviewed the candidate as `P026_CAND_GEMINI` / `P026_DELTA_GEMINI`) + the slice (+46/-6, five files). The Claude Opus 5 Lead built the slice (disclosed); the Lead may not accept its own code. The carried NITs and what the slice claims: **NIT-1** `restore.verify_completion_evidence` validated four pair-integrity fields (via `load_complete_marker`) and then printed `"completion_marker": "verified"` although the marker's own claims `readback` and `run_manifest` were never read - now both are checked LAST (after the pair, the global manifest and the per-run records, so the existing refusal reasons keep precedence): `readback` must be `"all_match"` and `run_manifest` must be `RUN_MANIFEST_NAME` (what `backup.py` writes); anything else is refused `run_not_complete` naming the value; the forged-evidence helper in `test_partial_run_declaring_files_but_having_no_records_fails_closed` now carries the tool's values so the older "nothing to verify" fence BEHIND the gate is still exercised. **NIT-2** (owner ruling `OD-20260918-P026-N2-DOC-1`: doc, not change) README states the O_EXCL-not-atomic marker rule (a torn/unreadable marker = not complete, re-backed-up under a new run id, data left in place, never restored from). **NIT-4** a dated note at the top of Part A of `RESTORE_DRILL_EVIDENCE.md` (contract changed: `--latest` removed, current-tool COMPLETE.json required, the recorded commands exit 2, how to reproduce). **NIT-5** `backup.py`'s dead `and status == "ok"` removed (status derives from errors alone). **NIT-3** (the P0-30 adapter checker's ASCII anchor) is fixed on the P0-30 branch and deliberately NOT touched here. Your job is adversarial along three axes: (a) **NIT-1 closed as claimed and placed correctly** - trace the gate order in `verify_completion_evidence` on HEAD: which refusal fires first for (i) a hand-made marker lacking both fields on a run whose global `run_end` is partial, (ii) the same on a run the tool closed successfully (`run_end` ok) - the tampered-marker test's two new arms, (iii) a marker with `readback: "all_match"` but `run_manifest: "RUN_MANIFEST.jsonl "` (trailing space), (iv) `readback` present but `True` (bool); confirm `RUN_MANIFEST_NAME` is imported from `opsa_common` and equals the name `backup.py` writes; say whether any restore path bypasses `verify_completion_evidence`; (b) **NIT-5 is really dead** - read `backup.py` around the return: `status` derives from `errors` alone, so `not errors and status == "ok"` == `not errors`; confirm the manifest `run_end` record still carries `status`; (c) **docs and honesty** - the README sentence and the evidence note must not over-claim (does the package really have no delete primitive? does "re-backed-up under a new run id" match the backup tool's behaviour - a new run id per run - or does it imply an automatic retry that does not exist?); the Lead's `LEAD_*.txt` must say what the record says (RED: readback claim unchecked -> 1 failed; run_manifest claim unchecked -> 1 failed; GREEN test_opsa 39; guard PASS); ruff findings per file identical to HEAD (2 / 4 / 7).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P026_NIT_20260918
Any other path appearing in copied documents is provenance text, never a file to open - that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_e114ed31_505af399.patch (complete), subject/DIFF_STAT_e114ed31_505af399.txt, subject/COMMIT_505af399.txt, subject/BLOB_OIDS_HEAD.txt, subject/restore.py_HEAD_complete.py (complete - ~330 lines), subject/backup.py_HEAD_complete.py (lines 200-300: run_backup's return and write_completion_evidence), subject/test_opsa.py_HEAD_complete.py (lines 330-420 the tampered-marker test, 440-520 the partial-run test), subject/README.md_HEAD_complete.md (lines 50-75), subject/RESTORE_DRILL_EVIDENCE.md_HEAD_complete.md (lines 1-40), sources/opsa_common_HEAD.py (lines 30-60 the names, and load_complete_marker complete), sources/LEAD_ADJUDICATION_P026_T0.md (the NIT table), every sources/LEAD_*.txt (complete).

TASK.
1. Trace the tampered-marker test's two new arms (falsified readback -> which message; falsified run_manifest -> which message; the marker restored afterwards) and the partial-run test's forged helper (why it now needs the two fields; which fence still refuses the empty run).
2. Adversarial: construct an input where NIT-1 is still wrong or misleading (cases iii/iv above; a marker whose `readback` is "all_match" but whose per-run file records carry `readback: "mismatch"` - which check fires; a marker with extra unknown fields).
3. NIT-5 and the two doc changes as in (b)/(c). Scope: exactly five files.
4. The remaining P0-26 NITs are out of scope; if any is actually REQUIRED, say so, but do not re-litigate design notes.

OUTPUT (under 1500 words): (a) the traces; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) - a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage; (d) nonempty NOT VERIFIED; (e) one fenced JSON object {"part":"P026_NIT_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","nit1":"CLOSED|OPEN|PARTIAL","nit2_doc":"HONEST|OVERCLAIMS","nit4_doc":"HONEST|OVERCLAIMS","nit5":"CLOSED|OPEN","findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
