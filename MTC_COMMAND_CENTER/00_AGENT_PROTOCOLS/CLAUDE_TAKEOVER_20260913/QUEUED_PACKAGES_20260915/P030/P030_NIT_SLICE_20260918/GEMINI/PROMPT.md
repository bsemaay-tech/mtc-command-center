You are gemini-3.8-flash-high acting as the independent read-only DETECTION reviewer (SUPPLEMENTAL_UNEXECUTED; you cannot execute anything) of the WP-P0-30 **NIT slice**: commit `d426e79f` on the P0-30 integration branch = `45a7f50e` (the candidate the exact-Opus T0 read passed with NITs on 2026-09-17, which you reviewed as `P030_R1_GEMINI` at its parent) + the slice (+157/-18, four files). The Claude Opus 5 Lead built the slice (disclosed); the Lead may not accept its own code. The five carried NITs and F8, and what the slice claims for each: **N1** `os.replace` could silently overwrite a target (or receipt) created between the exists-check and the move - new `_publish()`: on Windows `os.rename` refuses an existing destination (`FileExistsError`, nothing unlinked, the no-delete invariant holds), on POSIX the exists-check immediately before `os.replace` is the guard and the residual window is DOCUMENTED, not closed; a destination that appeared is refused as `target_exists` / `receipt_exists` naming the staging file that remains. **N2** the staging comment no longer claims a leftover partial is "never consumable" (its bytes may be a canonical prefix; the name is the only protection). **N3** the D-13 GREEN arm binds the adapter's capture to the export receipt (`prefix_sha256 == exported_sha256 == sha256(published bytes)`). **N4** a source line nested deeper than the interpreter's recursion limit is refused as `source_line_invalid` instead of escaping as `RecursionError` - NOTE: on the Lead's interpreter `json.loads` accepts 3000 levels and the recursive non-finite walk (`_reject_nonfinite_numbers`) is where the error arises, so the parser clause is defence in depth and the walk guard is the fence (the Lead's first parser-clause mutant SURVIVED - `sources/LEAD_RED_P030_N4_exporter_recursion_escapes.txt` - and was superseded by the walk-guard mutant `..._walk_guard_removed.txt`; say whether the record is honest about that); the same in both parse sites of the closed-partition backup adapter for parity. **N5 / F8** `check_p030_closed_partition_backup_adapter.py` built its config anchor with `json.dumps` default `ensure_ascii=True` while the config is written with `ensure_ascii=False`, so under a non-ASCII TEMP path the anchor never matched (`0 != 1`); anchor and replacement now use `ensure_ascii=False`; reproduced RED at HEAD under the long-form non-ASCII TEMP, GREEN after. Your job is adversarial along three axes: (a) **each NIT actually closed as claimed** - read `_publish` and both call sites (what happens on Windows when `final` exists; what happens on POSIX; is anything ever deleted or overwritten; is the receipt-first ordering preserved; does the new `FileExistsError` clause shadow or change the existing `OSError` -> `target_unwritable` path); the N3 assertion (does it bind what the NIT asked); the N4 guards (both files, both sites, both messages); the F8 anchor (is `replacement` built consistently with `anchor`; any other `json.dumps` default in that test that would still fail under a non-ASCII path); (b) **behaviour preservation** - the exporter's refusal codes, the staging/verify/publish order, the receipt fields and `exporter_sha256` LF-form, the adapter's canonical-prefix rules are unchanged; the adapter files were NOT ruff-reformatted (say whether the diff to them is only the semantic lines); ruff findings per file identical to HEAD (0 / 3 / 8 / 42 as the record says); (c) **honesty of the Lead's evidence** - the `LEAD_*.txt` must say what the record says (N1 mutant -> 2 failed; N3b receipt hash over other bytes -> the D-13 binding fails; exporter walk guard removed -> 1 error; adapter walk guard removed -> 1 error plus a scratch-only artefact `test_committed_configuration_is_explicitly_non_runnable` that also errors on the unmutated scratch baseline - say whether that artefact is explained; F8 RED at HEAD `0 != 1`; GREEN exporter checker 28, adapter checker 44 under both the ASCII and the non-ASCII TEMP; guard PASS).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P030_NIT_20260918
Any other path appearing in copied documents is provenance text, never a file to open - that includes AGENTS.md and every repository file outside the packet. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt. REQUIRED COMPLETE NATIVE READS: subject/DIFF_45a7f50e_d426e79f.patch (complete), subject/DIFF_STAT_45a7f50e_d426e79f.txt, subject/COMMIT_d426e79f.txt, subject/BLOB_OIDS_HEAD.txt, subject/p030_archive_exporter.py_HEAD_complete.py (complete - ~540 lines), subject/check_p030_archive_exporter.py_HEAD_complete.py (at least lines 1-140 and 330-420 and 680-760: the D-13 arm, the two new tests, the publish-failure test), subject/p030_closed_partition_backup_adapter.py_HEAD_complete.py (lines 90-130 and 180-215: both parse sites), subject/check_p030_closed_partition_backup_adapter.py_HEAD_complete.py (lines 140-200 and 820-870: the nesting arm and the F8 anchor), sources/LEAD_ADJUDICATION_P030_R1_T0.md (the NIT table), every sources/LEAD_*.txt (complete).

TASK.
1. Trace `test_publication_never_replaces_a_target_that_appeared_after_the_check` on Windows: the mocked `Path.exists`, the staging writes, the receipt publication, `_publish(staging_target, target)` -> `os.rename` -> `FileExistsError` -> which `ExportRefused` code and text; what is on disk afterwards (foreign target bytes intact, staging partial present, receipt published). Then say what the same code path does on POSIX.
2. Trace `test_pathologically_nested_line_is_a_refusal_not_a_crash` and the adapter's nesting arm: where the `RecursionError` arises on an interpreter whose JSON parser accepts the depth, and which guard converts it.
3. Adversarial: construct an input where the slice is wrong or misleading - e.g. a target that appears between the receipt publication and the target publication (what does the record say: receipt published, target refused - is that the documented residual window?); a `FileExistsError` raised by `os.rename` for a reason other than an existing destination; a non-dict top-level JSON nested 3000 deep; an F8-like `json.dumps` default elsewhere in the adapter checker.
4. Preservation and honesty as in (b)/(c). Scope: exactly four files.

OUTPUT (under 1800 words): (a) the traces; (b) findings numbered with severity REQUIRED / NIT and file:line (packet-relative) - a REQUIRED finding means REQUEST_CHANGES; (c) EXACT read coverage; (d) nonempty NOT VERIFIED; (e) one fenced JSON object {"part":"P030_NIT_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","n1":"CLOSED|OPEN|PARTIAL","n2":"CLOSED|OPEN|PARTIAL","n3":"CLOSED|OPEN|PARTIAL","n4":"CLOSED|OPEN|PARTIAL","f8":"CLOSED|OPEN|PARTIAL","posix_window_documented_not_closed":true|false,"findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
