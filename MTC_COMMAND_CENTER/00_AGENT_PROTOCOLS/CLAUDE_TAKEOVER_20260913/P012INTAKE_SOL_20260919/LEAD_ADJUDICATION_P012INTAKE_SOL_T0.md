# LEAD_ADJUDICATION — Sol (gpt-5.6-sol xhigh, Codex PRO `free`) T0 read of the WP-P0-12 intake NIT slice `8cbf4f1a` — 2026-09-19

**Lane:** generated by the Lead's automated chain (`SOL_QUEUE_20260919/make_sol_lane.ps1 -Pkg P012INTAKE -Account free`, from `OPUS_QUEUE_20260916/P012INTAKE/opus/run.ps1` — re-pinned by this session to the NIT slice for the lane-8 read); launched 09:07:21Z, `launch.exit` `exit=0`, 09:07:21Z-09:31:04Z (~24 min). Independent of the Opus read: the reviewer states it did not read any Opus report or Lead adjudication before writing its verdict.

**Verdict as returned:** `VERDICT: PASS-WITH-NITS` — **0 REQUIRED**. All ten claimed items independently VERIFIED with the reviewer's own failing inputs (D-1 canonical-reserialization digest, D-2 hour-based id, D-3 rounded stamp, D-4 `DERIVED_FROM_PAYMENT`, D-5 wrong-scope identity, D-6 single-pass), own start-limit mutant (`2 failed, 75 passed, 2 deselected` — exactly the two exclusion tests), own boundary sweep (`18:00:00.060Z` accepted, `18:00:01.060Z` refused), own real r1 dry run (draft outcome `CANDIDATE_EVIDENCE_KIND_MISMATCH`, real packet digest `f3bde0b7…` unchanged, capture directory hash unchanged, full address never emitted), and its own RED reproduction (`8 failed, 69 passed, 2 deselected` on `a871e429` bytes, restoring to `77 passed, 2 deselected`).

**Environment constraints disclosed by the reviewer (not defects):** its sandbox denied the brief's exact `--basetemp C:/bt_opus_intake_nit` path (its own scratch Git root has `.git` ancestry, tripping `CANDIDATE_STAGING_UNSAFE` for the same 35 CLI tests every time — [[bridge-pytest-staging-refuses-under-c-tmp]]) and could not install ruff 0.16.8 offline (network denied); it substituted an authorized scratch root and ruff 0.16.4 respectively and disclosed the exact substitution and count difference (5 findings reproduced on the two production files; the two test-file I001 findings are ruff-version-specific and were not expected to reproduce on 0.16.4). Accounted for, not a gap in the subject.

## Lead reproduction
| Check | Result |
|---|---|
| Citations spot-checked: `funding_intake_adapter.py:396-411` (`_export_tool_outcome_today` call site) | **EXACT** at `8cbf4f1a` |
| **NIT-3 reproduced independently**: replaced `_export_tool_outcome_today(packet)` with a hardcoded `{"refusal_code": "CANDIDATE_EVIDENCE_KIND_MISMATCH", ...}` (the currently-correct value, never calling the exporter) and re-ran `test_build_fills_only_what_the_bytes_supply_and_labels_the_rest` | **1 passed** — CONFIRMED: the test pins the output value only, not that the exporter is actually called. A regression that reverts to a hardcoded guess would pass silently. |
| Worktree porcelain after the lane (the brief grants the reviewer write access under `workspace-write`; it must leave no trace) | `git status --porcelain` **empty**, HEAD unchanged at `8cbf4f1a` |

## Sol's three NITs (own numbering; distinct from, and complementary to, the Opus report's B-J)
1. Shifted-start admission semantics are not fully reflected in public docstrings/output metadata: `build_funding_candidate`'s docstring (`:1491-1493`) still says `[start_inclusive, end_exclusive)`; `effective_interval` exposes only `end_tolerance_seconds` (no start-side field, even though the real value is now symmetric); the adapter's D-6 gloss still says "1-second end tolerance". Overlaps Opus's NIT-C/NIT-F — same underlying gap (prose/metadata lags the symmetric band), independently found.
2. `production_mode` still `UNAVAILABLE_PENDING_SOURCE_EVENT_DIGEST_DOMAIN` on an accepted real candidate although D-1's domain is now ruled. Same as Opus's carried NIT-J; pinned by an existing test, errs toward less admission — roster wording item.
3. **New, not found by Opus**: the NIT-A regression test (`tests/test_funding_intake_adapter.py:137-139`) asserts only the resulting refusal code, so a mutant that hardcodes the (currently correct) code without calling the exporter still passes. Independently reproduced above. Recommendation: assert the exporter was actually invoked (monkeypatch `exporter.build_funding_candidate` and check it was called with the draft's coverage, or assert on a distinguishable sentinel return).

No REQUIRED from either flagship. No safety, admission, or scope concern from either read. NIT-B (the D-6 completeness-grid/admission-band interaction, Opus-only) remains the standout item already relayed to the owner as `D6-GRID A|B` (non-blocking).

## Standing
`8cbf4f1a` roster complete: exact-Opus PASS-WITH-NITS (0 REQUIRED) + exact-Sol PASS-WITH-NITS (0 REQUIRED, different family) + Gemini PASS (COUNTED, all 10 items CLOSED) + this Lead reproduction. Ready for the owner's merge word per `OD-20260919-P012-MERGE-INTAKE-GO-1` (conditional: roster complete). The Lead never accepts its own code; this session is the disclosed builder.

Recorded by the P0-12 INTAKE session (Claude Sonnet 5).
