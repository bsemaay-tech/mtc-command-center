# Paste-ready start prompt — P0-12 INTAKE session (second, PARALLEL package session; model **Claude Sonnet 5**; project `C:\LAB\Tradingview_LAB_CLEAN`)

You are the **P0-12 INTAKE session**: you build and evidence ONLY task **T4** (the intake-adapter NIT slice) while a sibling session ("P0-12 package session") does T3 (the capture tool) in parallel. Owner = Barış (non-technical; English; times UTC+3). The Lead session (Fable 5.1) coordinates; you never touch the capture-tool worktree `C:/tmp/P1CAP_20260914` or any other package.

**Read first, nothing else by default:**
1. `C:/tmp/CLAUDE_P0_RUN_20260913/SESSION_PROMPT_P012_PACKAGE_20260919.md` — ONLY these sections, which bind you word for word: "Binding rules", "Before touching anything", "Routes you may use", "Token discipline". Skip its step list; your steps are below.
2. `C:/tmp/OPUS_QUEUE_20260916/P012INTAKE/LEAD_ADJUDICATION_P012INTAKE_R1_T0.md` (the NIT list you close: NITs 1-9 + NIT-A) and `LEAD_ADJUDICATION_P012INTAKE_T0.md` (the first read, for context on REQUIRED-1 already repaired as `a871e429`).
3. CT13 `C:/CT13/DECISIONS.md` row `OD-20260918-P012-D6START-B-1` (D6-START **B** = the funding window START is exclusive) — apply it in this slice.
4. `C:/tmp/CLAUDE_P0_RUN_20260913/P012_FUNDING_INTAKE_20260915/LEAD_VERIFICATION_P012_INTAKE_R1.md` and `LEAD_VERIFICATION_P012_INTAKE_S4.md` (how the accepting half was built and evidenced; D-1..D-6 vocabulary).
5. Memory lessons (read-only): `red-evidence-on-the-pinned-interpreter`, `bridge-pytest-staging-refuses-under-c-tmp`, `worktrees-share-git-gemini-watcher`.

**Your worktree and pin:** `C:/tmp/P012_INTAKE_20260915`, branch `feature/p012-funding-intake-adapter-20260915`, HEAD must be `a871e429` and `git status --porcelain` empty before you start (stop and ask otherwise). **Interpreter for everything:** `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe` (Python 3.12.12, eth_account, pytest). Bridge pytest: `-p no:cacheprovider --basetemp C:/bt_s4/p012intake_tmp`, every `HL_*` variable removed from the child env. Records go to `C:/tmp/CLAUDE_P0_RUN_20260913/P012_FUNDING_INTAKE_20260915/NIT_SLICE_20260919/`. Queue-log prefix: `P012INTAKE_SESSION:`.

**Coordination (both sessions share the accounts and the git folder):**
- Before EVERY git command: `tasklist | findstr agy` must be empty (a Gemini call from the sibling or the Lead dies otherwise). Before launching a Gemini chain, Pro lane or Codex lane: read the tail of `C:/tmp/OPUS_QUEUE_20260916/QUEUE_LOG.txt` and `C:/tmp/SOL_QUEUE_20260919/SOL_CHAIN_LOG.txt`; if another Pro lane or Codex lane on the same account is `launching` without `ended`, wait (poll every 5 min); one Pro lane and one Codex lane per account at a time, machine-wide. Codex accounts: the Lead's chain uses `free` today; you use `secondary` for your Sol lane (Codex Plus, reset 18:47) unless the Lead tells you `free` is free.
- Message the sibling (`SendMessage` to "P0-12 package session") when you start, when you launch a Gemini call, and when you finish; it does the same.

**Do, in order:**
1. **Build T4 test-first** on `a871e429`: for each NIT 1-9 and NIT-A in the adjudication, write the fence test first (RED on the `a871e429` bytes for every NIT that is a code change; a doc-only NIT needs no test), then the fix (GREEN). Apply D6-START B (window start exclusive) with its own test pair. Keep the carried fence `ACCEPTED_EVIDENCE_KINDS == (SYNTHETIC_FIXTURE,)` untouched (production admission is NOT granted). Run the adapter's own tests, then the full Bridge suite (~1630 passed expected). Mutants: one scratch copy per fenced NIT, the named test must fail. Ruff per-rule parity with HEAD (`uvx ruff@0.16.8 check --statistics <file>` — count with `--statistics`, never by grepping messages). Guard dry-run PASS, exact staged paths, commit (author `Claude Sonnet 5 <noreply@anthropic.com>`, trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`), push. Record `LEAD_VERIFICATION_P012_INTAKE_NIT.md` (what changed per NIT; evidence table; interpreter named per arm).
2. **Gemini delta**: clone the recipe of `C:/tmp/P012_S16_REVIEWS_20260913/P012_INTAKE_R1_GEMINI/` into a new root `P012_INTAKE_NIT_GEMINI` (HEAD_PIN = your sha; `PRE_STAGE.py` with PRE = `a871e429`, packet `P012_INTAKE_NIT_20260919`; PROMPT.md = delta review of each NIT + D6-START B + behaviour preservation + honesty of your `LEAD_*.txt`; HOLD_AUTHORIZATION.txt; RUNNER.txt → runner clone `run_p012_intake_nit_g38.ps1` with the new packet boundary). Launch `python C:/tmp/CLAUDE_P0_RUN_20260913/gemini_retry_chain_v2.py P012_INTAKE_NIT_GEMINI` in the background; no git and no worktree cwd until `launch.exit`. Extract the report with `python C:/tmp/CLAUDE_P0_RUN_20260913/adjudicate_part.py P012_INTAKE_NIT_GEMINI` (if it refuses because of its packet constant, extract the `response` field of the JSON envelope at the end of `REVIEW.log` yourself and run the native-read audit against your packet). COUNTED only with exit 0, the sentinel, 0 reads outside the packet. Write `LEAD_ADJUDICATION.md`.
3. **Exact-Opus re-read** (lane 8): re-pin `C:/tmp/OPUS_QUEUE_20260916/P012INTAKE/opus/run.ps1` to your sha, add an ADDENDUM to `P012INTAKE/REVIEW_BRIEF.md` (what the slice claims per NIT and D6-START B; what mutants the reviewer must run on the pinned interpreter), launch `run.ps1` in the background when no other Pro lane is running; on `launch.exit`: grep-verify every citation, reproduce one arm, archive `ATTEMPTn_<VERDICT>_<sha8>/`, write `P012INTAKE/LEAD_ADJUDICATION_P012INTAKE_NIT_T0.md`. `api_error_status 429` = five-hour cap → archive as CAPPED, retry after the window. REQUEST_CHANGES → repair (cap 3), Gemini delta, re-read.
4. **Sol on the final bytes**: `powershell -NoProfile -ExecutionPolicy Bypass -File C:/tmp/SOL_QUEUE_20260919/make_sol_lane.ps1 -Pkg P012INTAKE -Account secondary` (after 18:47, or `free` if the Lead says so), then its `run.ps1` by hand; after the lane `git status --porcelain` in the worktree must be empty; adjudicate the same way; record under CT13 `P012INTAKE_SOL_20260919/`.
5. **Records to CT13** (`C:/CT13`, exact paths, guard, commit `records(ct13): P0-12 intake session - ...`, push; never while `agy.exe` runs), then write `C:/tmp/CLAUDE_P0_RUN_20260913/P012_PACKAGE_SESSION/MERGE_READINESS_INTAKE.md` (roster table with hashes and verdict files) and **stop**. No PR, no merge — the owner's word `P012-MERGE intake go` is required.

Questions you cannot decide alone go to `C:/tmp/CLAUDE_P0_RUN_20260913/P012_PACKAGE_SESSION/QUESTIONS_FOR_LEAD_INTAKE.md` plus a `SendMessage` to the Lead ("P0 Claude Lead Session 7"); then stop.
