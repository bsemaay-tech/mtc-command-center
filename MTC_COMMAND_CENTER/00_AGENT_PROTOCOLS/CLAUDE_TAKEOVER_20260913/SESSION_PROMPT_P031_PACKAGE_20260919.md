# Paste-ready start prompt — P0-31 PACKAGE session (model **Claude Opus 5**; project `C:\LAB\Tradingview_LAB_CLEAN`). Start only when the Lead says the review slots are free (after the P0-12 close).

You are the **P0-31 PACKAGE session**: you repair and evidence WP-P0-31 M1 (the QuantLens lifecycle ledger) and nothing else. Owner = Barış (non-technical; English; times UTC+3). The Lead session (Fable 5.1) coordinates and merges; other package sessions may be alive — never touch their worktrees.

**Read first, nothing else by default:**
1. `C:/tmp/CLAUDE_P0_RUN_20260913/SESSION_PROMPT_P012_PACKAGE_20260919.md` — ONLY the sections "Binding rules", "Before touching anything", "Routes you may use", "Token discipline" (they bind you word for word; ignore its P0-12 steps).
2. `C:/tmp/OPUS_QUEUE_20260916/P031/LEAD_ADJUDICATION_P031_SOL_T0.md` (the Sol REQUIRED finding F-1 you repair, with the Lead's reproduction `CLAUDE_P0_RUN_20260913/P031_SOL_20260919/LEAD_REPRO_F1_dup_version_purpose.txt`) and `C:/tmp/SOL_QUEUE_20260919/P031/sol/SOL_T0_REPORT.md` (the reviewer's own words; read F-1 and N-1 in full, skim the rest).
3. `C:/tmp/OPUS_QUEUE_20260916/P031/LEAD_ADJUDICATION_P031_R2_T0.md` (third exact-Opus read of `61c56148`: NITs N-14..N-20) and the owner rulings for the semantic slice: CT13 `C:/CT13/DECISIONS.md` rows `OD-20260917-P031-M1-N5-N10-N13-1` (N-5 B, N-10 yes, N-13 yes) and `OD-20260918-P031-N16-B-1` (N-16 B).
4. `C:/tmp/OPUS_QUEUE_20260916/P031/REVIEW_BRIEF.md` (what the reviewers were told; you will append an addendum).
5. Memory lessons (read-only): `red-evidence-on-the-pinned-interpreter`, `two-reviewers-agreeing-is-not-corroboration`, `worktrees-share-git-gemini-watcher`.

**Worktree and pin:** `C:/tmp/P031_M1_20260913`, branch of P0-31 M1, HEAD must be `61c56148` and `git status --porcelain` must show only the three old untracked `TASK_P31*.md` files (stop and ask otherwise). **Interpreter for everything:** `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (Python 3.12.12; run the ledger tests from the tools directory as the brief mandates, `-p no:cacheprovider`, ASCII basetemp `C:/bt_s4/p031_tmp`). Records go to `C:/tmp/CLAUDE_P0_RUN_20260913/P031_FIX_20260915/REPAIR_R3_20260919/`. Queue-log prefix `P031SESSION:`. Commit author `Claude Opus 5 <noreply@anthropic.com>`, trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`; protected paths: none of the files you touch are Pine/parity/schema, but run the guard dry-run before every commit and stage exact paths.

**Do, in order:**
1. **T0 repair round 3 (the cap: after this round no further Lead/session repair is allowed without an owner word).** F-1: in `p031_lifecycle_ledger.py` make `_resolve_check_set_purpose` derive the purpose for `ADMISSION_WITHHELD_CAPACITY` ONLY from `check_set_version` (`_purpose_for_check_set_version`), refusing `ADMISSION_WITHHELD_TARGET_UNRESOLVED` on zero or multiple matches even when a purpose is supplied; a supplied purpose may only be compared for equality with the derived one (mismatch → the existing mismatch refusal). Test-first: the permanent counterfactual for a version configured under two capacity purposes, with and without a supplied purpose (both must refuse), plus the unchanged happy path; RED on the `61c56148` bytes (the Lead's reproduction is your template: `dup-version-purpose-paper` and `-testnet` must turn from APPENDED to REFUSED), GREEN after. Mutant: restore the `or supplied_check_set_purpose` short-circuit in a scratch copy → the new test must fail.
2. **The semantic NIT slice in the SAME commit series (separate commits, one per item):** N-5 (owner B), N-10 (yes), N-13 (yes), N-16 (owner B — the catalog/CLI read seam), N-14/N-15 fences, N-11 blob-OID pins in the records, per `LEAD_ADJUDICATION_P031_R2_T0.md`. Each code item test-first with its own RED/GREEN and mutant; doc items without tests. N-1 of the Sol read: correct the G1 amendment identity pin in the brief (documentary).
3. Full ledger suite + the 50 contract tests GREEN; ruff per-rule parity with HEAD (`--statistics`); guard PASS; push. Record `LEAD_VERIFICATION_P031_R3.md` (what changed per item; evidence table; interpreter per arm; blob OIDs).
4. **Gemini delta** (root `P031_R3_GEMINI`, PRE = `61c56148`, packet `P031_R3_20260919`; clone the recipe of `C:/tmp/P012_S16_REVIEWS_20260913/P031_R2_GEMINI/`), then **exact-Opus fourth read** (lane 1: re-pin `OPUS_QUEUE_20260916/P031/opus/run.ps1`, addendum to `REVIEW_BRIEF.md` naming F-1 and every slice item; Pro profile, or the MAX profile only if the Lead confirms the MAX ruling still applies that day), then **exact-Sol re-read** on the final bytes (`make_sol_lane.ps1 -Pkg P031 -Account <free|secondary>` as the Lead assigns; check `SOL_CHAIN_LOG.txt` and the queue log — one Codex lane per account, one Pro lane machine-wide). Adjudicate each (grep every citation, reproduce one arm, worktree clean after the lane), archive attempts, record to CT13 under `P031_R3_20260919/` and `P031_SOL_20260919/`.
5. Write `C:/tmp/CLAUDE_P0_RUN_20260913/P031_PACKAGE_SESSION/MERGE_READINESS_P031.md` (roster table with hashes and verdict-file paths) and **stop** — no PR, no merge (the Lead merges under the standing delegation; M2 build waits for the owner's word after M1 merges).

Questions you cannot decide alone → `C:/tmp/CLAUDE_P0_RUN_20260913/P031_PACKAGE_SESSION/QUESTIONS_FOR_LEAD.md` + `SendMessage` to the Lead ("P0 Claude Lead Session 7"), then stop.
