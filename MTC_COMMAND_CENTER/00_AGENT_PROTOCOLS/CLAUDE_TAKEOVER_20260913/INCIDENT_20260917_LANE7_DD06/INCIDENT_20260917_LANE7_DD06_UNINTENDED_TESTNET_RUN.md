# INCIDENT — 2026-09-17 18:56 UTC+3: an exact-Opus review lane caused one unintended live TESTNET run of the DD-06 probe

**What happened.** Lane 7 (`OPUS_QUEUE_20260916/DD06/opus/run.ps1`, exact `claude-opus-5` xhigh T0 review of the DD-06 probe slice `1af85067`, launched by the Lead 18:5x UTC+3) followed its brief's instruction to mutate the tool's safety controls in a scratch copy. Its mutant M8 removed the `HL_LIVE_ACK` refusal from `refuse_unless_testnet` (`tools/dd06_agent_withdraw_probe.py:214-217`). The fixture test `tests/test_dd06_agent_withdraw_probe.py:407-412` (`test_main_refuses_mainnet_before_reading_any_credential`) calls the real `probe.main(...)` with nothing stubbed; with the refusal gone, `main()` ran its real branch, resolved the Windows-host agent-wallet credential from `HKEY_CURRENT_USER\Environment` (`HL_API_WALLET_KEY`, `HL_ACCOUNT_ADDRESS`; `source: user_registry`) and executed the probe against the live TESTNET venue at 15:56:06-15:56:16Z (18:56 UTC+3). The reviewer disclosed it as §0 of its report and made no further network call.

**What the venue saw (from the reviewer's preserved record `C:/tmp/OPUS_DD06_SCRATCH/mut/unused/DD06_PROBE_RECORD.json`, sha256 `40ad6f32…`):** S0 reads of the master testnet account (two agents `MTC-bridge-test`, `kvm2-bridge`; perp 0.0; spot USDC 984.987457); S1 control order — a real resting testnet BTC bid 0.00016 @ 69,210.0 (oid 60364599563) placed and then CANCELLED (`{"statuses": ["success"]}`); withdraw3 6 USDC / usdSend 1 USDC / spotSend 1 USDC / approveAgent — all REFUSED by the venue ("Must deposit before performing actions") because the Windows-host agent wallet holds nothing; result `DD06_INCONCLUSIVE`.

**Damage assessment (Lead, public reads 19:05 UTC+3 = 16:05Z, `LEAD_PUBLIC_READS_testnet_after_incident.txt`):** TESTNET only (`TESTNET_URL` untouched; no mainnet contact); master testnet account `openOrders: []`, perp `accountValue 0.0`, no positions, spot USDC 984.987457 (unchanged since r3), no fills since 15:00Z; no agent approved (still the same two agents); no credential value printed or written (the record is address-free by design; the reviewer's stream and report were scanned: no key material); the subject worktree unmodified. **Nothing was lost or moved; no real money involved.**

**Rule breach, stated plainly:** the standing rule is "no host contact except an owner-worded testnet action". A testnet venue action happened without an owner word — not by the Lead's hand, but through a lane the Lead launched with a brief that asked for mutants of safety controls on a host whose user registry holds a live credential. The Lead did not foresee that mutating a *refusal* turns an offline fixture suite into a live run. That is on the Lead as much as on the reviewer.

**Root cause (= the reviewer's REQUIRED R-2):** the probe's offline-ness rested on a single line; the fixture suite calls the real `main()` with nothing stubbed; the credential is resolvable on this workstation (user registry), not only on KVM2.

**Immediate mitigations:**
1. Public reads done (above) — the venue is clean.
2. Code repair of R-2 as the disclosed builder (T0 repair round 1 on the DD06 branch): the `main()`-calling test stubs the credential resolver and both SDK constructors so the real branch can never dial from the suite; `main()` requires a second explicit execution token (`DD06_PROBE_RUN_TOKEN=<run-id>` in the environment) before constructing `Info`/`Exchange`, so no single-line change can arm the tool (also closes N-6).
3. Lane rule (recorded in the queue README, the Sol generator README and the memory lessons): **no review lane may mutate a venue-facing tool's refusals on a host that holds live credentials unless the tool's tests are structurally offline (stubbed resolver + SDK) — and never with `main()`-calling tests.** Until the repair is reviewed, the DD06 lane is NOT regenerated for Sol.
4. Recommendation to the owner (his environment, not the Lead's to change): keep the Windows-host agent wallet unfunded (it is), and consider moving `HL_API_WALLET_KEY` out of `HKCU\Environment` into a store the tools read only on explicit request — the key being process-readable by any lane is the exposure this incident demonstrates.

**Owner disclosure:** sent in chat 19:0x UTC+3 with this file; DECISIONS row not needed (no authority exercised or granted); recorded in RUN_STATE and the P0-29 DD-06 record set.

Recorded by Claude Opus 5 Lead (session 6, `4a8233`).
