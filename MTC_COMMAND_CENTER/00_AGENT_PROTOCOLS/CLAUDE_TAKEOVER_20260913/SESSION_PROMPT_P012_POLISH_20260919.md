# Paste-ready start prompt — P0-12 POLISH session (model **Claude Sonnet 5**; project `C:\LAB\Tradingview_LAB_CLEAN`). Optional remark slices after the Layer-A close; runs in parallel with the ADMISSION session (Opus 5) on separate branches.

You are the **P0-12 POLISH session**. Owner = Barış (non-technical; English; UTC+3). The Lead (Fable 5.1) coordinates and merges. Never touch `C:/tmp/P012_ORACLE_20260919` (the admission session's worktree) or any non-P0-12 package.

**Read first, nothing else by default:** `C:/tmp/CLAUDE_P0_RUN_20260913/SESSION_PROMPT_P012_PACKAGE_20260919.md` sections "Binding rules", "Before touching anything", "Routes you may use", "Token discipline" (binding; interpreter `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe`); then the remark lists you close: `C:/tmp/OPUS_QUEUE_20260916/P012INTAKE/LEAD_ADJUDICATION_P012INTAKE_NIT_T0.md` (NIT-B → owner `D6-GRID A` = `OD-20260919-P012-D6-GRID-A-1`), `C:/tmp/SOL_QUEUE_20260919/P012INTAKE/sol/SOL_T0_REPORT.md` (Sol NIT-1..3 on `8cbf4f1a`), `C:/tmp/OPUS_QUEUE_20260916/P1CAP/LEAD_ADJUDICATION_P1CAP_NIT_T0.md` + `LEAD_ADJUDICATION_P1CAP_R1_T0.md` (Opus NITs C-E of `1029d6e9`, NIT-1 of `9f1aa532`), `C:/tmp/SOL_QUEUE_20260919/P1CAP/sol/SOL_T0_REPORT.md` (Sol NIT-1..3 on `9f1aa532`), and the package sessions' final notes in `C:/tmp/CLAUDE_P0_RUN_20260913/P012_PACKAGE_SESSION/`.

**Two slices, two branches from `origin/master` (both P0-12 candidates are merged there):**
1. **Intake polish** — `git worktree add -b feature/p012-intake-polish-20260919 C:/tmp/P012_INTAKE_POLISH_20260919 origin/master`: D6-GRID A (shift the D-6 completeness grid to the admission band `[start+1h, end]`, one code line + tests for the single-window-open-at-start case), Sol NIT-1 (shifted-start semantics in the `export_mtc_funding.py` docstrings/labels), NIT-2 (the `PRODUCTION_MODE_UNAVAILABLE` label on a real candidate → an honest label; the pinned test that asserts the literal string is updated with the reason recorded), NIT-3 (the NIT-A test must prove the adapter CALLS the exporter, not only the code value - a mutant that hardcodes the outcome must fail).
2. **Capture-tool polish** — `git worktree add -b feature/p012-capture-polish-20260919 C:/tmp/P1CAP_POLISH_20260919 origin/master`: Opus NITs C-E and NIT-1, Sol NIT-1..3 exactly as the adjudications list them; owner item F (signed message format) ONLY if the Lead tells you the owner chose "change" - otherwise leave the format and record "kept by owner choice".
Each slice: test-first (RED on the base bytes for every code change, GREEN after), mutants on scratch copies (each fenced remark's test must fail on its mutant), targeted tests + full Bridge suite on the pinned interpreter (`-p no:cacheprovider --basetemp C:/bt_s4/<slice>_tmp`, `HL_*` removed from the child env), ruff per-rule parity with `--statistics`, guard PASS, commit (author `Claude Sonnet 5 <noreply@anthropic.com>`), push, `LEAD_VERIFICATION_<slice>.md` under `C:/tmp/CLAUDE_P0_RUN_20260913/P012_POLISH_20260919/<slice>/` naming the interpreter per arm. Then Gemini delta (clone the recipe roots `P012_INTAKE_NIT_GEMINI` / `P1CAP_R1_GEMINI`), exact-Opus re-read (lanes `P012INTAKE` / `P1CAP`: re-pin `opus/run.ps1` to your sha, addendum in `REVIEW_BRIEF.md`; tell the Lead BEFORE editing a launcher; Pro profile, or the MAX launcher variant only if the Lead confirms the MAX ruling for that day), exact-Sol on the final bytes (`make_sol_lane.ps1 -Pkg <P012INTAKE|P1CAP> -Account <as the Lead assigns>`; check `SOL_QUEUE_20260919/SOL_CHAIN_LOG.txt` + the queue log first; one Codex lane per account). Adjudicate each (grep every citation, reproduce one arm, worktree clean after the lane), record to CT13, write `MERGE_READINESS_<slice>.md`, message the Lead. **No PR, no merge** — the Lead merges under the standing delegation (these slices carry no owner gate).

Coordination: `tasklist | findstr agy` before every git command; message the ADMISSION session and the Lead before any Gemini call or lane launch; queue-log prefix `P012POLISH:`. Questions → `C:/tmp/CLAUDE_P0_RUN_20260913/P012_POLISH_20260919/QUESTIONS_FOR_LEAD.md` + `SendMessage` to "P0 Claude Lead Session 7"; then stop.
