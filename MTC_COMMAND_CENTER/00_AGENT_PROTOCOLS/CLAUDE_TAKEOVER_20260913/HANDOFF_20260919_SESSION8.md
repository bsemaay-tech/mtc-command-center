# HANDOFF — P0 Claude Lead, session 7 → session 8 — written Sat 2026-09-19 10:0x UTC+3 by session 7 (Fable 5.1 Lead `18c1b8`); lane-3 verdict and Sol results patched in below as they land

Supersedes `HANDOFF_20260919_SESSION7.md` (keep it: standing rules section 1 and the routes section 6 there are unchanged and still binding). Copies: this file, CT13 `MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/`. Owner-facing times UTC+3; the machine-stamped ledger is `C:/tmp/OPUS_QUEUE_20260916/QUEUE_LOG.txt` (trust it over prose times — session 6 and session 7 both over-estimated prose times once).

## 0. Read first
1. Memory `MEMORY.md` head, `p0-claude-lead-session-2026-09-19-session7`, `p0-claude-lead-session-2026-09-16-day`, and the lessons `red-evidence-on-the-pinned-interpreter`, `review-lane-mutant-armed-a-live-tool`, `partial-read-is-not-a-measurement`.
2. This file; then `RUN_STATE.md` tail (Session 7 section) and `QUEUE_LOG.txt` tail; then `C:/tmp/SOL_QUEUE_20260919/SOL_CHAIN_LOG.txt` (the Sol chain's own log).
3. `ListAgents` + mtimes before touching anything. A "busy" peer titled "Bridge work" was session 6 finishing its last write this morning — ask before taking over. The owner's Codex desktop app shows as `codex.exe`; it is not a lane.

## 1. New this session (Sat 09:22 → …)
- **Owner's new routing (Sat morning):** this chat = the LEAD session (Fable 5.1, Max, orchestration). Each package gets its OWN session on a cheaper model. Prompts and the model table are written: `SESSION_PROMPT_P012_PACKAGE_20260919.md` (Sonnet 5), `PACKAGE_SESSION_MODEL_ROUTING_20260919.md` (all packages), `P012_CLOSEOUT_PLAN_20260919.md` (what closes P0-12, tasks T1-T8, owner-only items). The P0-12 NIT slices (capture tool `P1CAP_NIT_SLICE_PREP_20260918/`, intake D6-START B) are therefore NOT the Lead's task any more — the package session does them. The Lead session coordinates: one Pro lane and one Codex lane at a time ACROSS sessions (check the queue log for an unmatched `launching`), adjudicates contradictions, keeps handoff/memory.
- **New routes (probed alive, quotas unmeasured):** Cline Pass (`cline -P cline-pass "<task>"`, `bsemaay` mailbox) and CodeBuddy Tencent `hy4-preview` 1M context (`codebuddy --model hy4-preview -y -p "<task>"`). Roles: builders / third-family detection reads / documentary lanes; never exact flagship slots; scratch cwd only until their FS-watcher behaviour on the shared `.git` is probed. TOOLBOX rows updated (kit `9dedabf`). Codex Plus/Pro weekly caps still closed until tonight 18:47 / 11:10; SuperGrok weekly limit exists (reset Fri); Claude Pro five-hour window resets 13:50 today; Claude Max = this session only.
- **P0-30 (lane 3) — NIT-slice repair round 2 = `b4df1413` (Opus + Gemini + Lead reproduction all in)** (pushed with the never-pushed `d426e79f`): F-1 derived per-layer nesting arms (`nesting_arms()` in both checkers, `_STACK_MARGIN = 64`), F-2 third adapter parse site guarded, F-3 receipt-side race arm, F-4 exact messages, F-5 docstring. Evidence `P030_NIT_SLICE_20260918/REPAIR_R2_20260919/LEAD_VERIFICATION_P030_NIT_R2.md`: nine mutants RED on BOTH the pinned 3.12 and 3.14, each exactly its own arm; GREEN 29/46 both, ASCII + non-ASCII TEMP; ruff parity; guard PASS. Gemini `P030_R2_GEMINI` **PASS COUNTED** (F-1..F-5 CLOSED; 74 reads / 41 files / 0 outside; cid `bab62220`). **Lane 3 attempt 5 = exact-Opus read of `b4df1413` (09:57-10:21): PASS-WITH-NITS, 0 REQUIRED** — F-1..F-5 CLOSED on the pinned 3.12 (eight reviewer mutants each break only their own arm); NITs 1-3 (junction-fixture decode noise in checker evidence; D-13 RED assertIn accepts two reasons; POSIX exists-check blind to a dangling link) carried to ONE post-Sol slice; NIT-4 (my record named E402, which ruff does not emit) corrected. `LEAD_ADJUDICATION_P030_R2_T0.md`; archived `ATTEMPT5_PASS_WITH_NITS_b4df1413/`. **Roster: Opus PASS-WITH-NITS + Gemini PASS + Lead reproduction → Sol tonight on `b4df1413`, then the merge PR.**
- **Lane 4 (P0-26 slice `505af399`)** was adjudicated by session 6 at 09:22 (PASS-WITH-NITS, 0 REQUIRED); owner ruled `P26-EVID B` at 10:3x (row `OD-20260919-P026-EVID-B-1`; docs-only commit `2e03a669`, lane 4 re-pinned; `505af399` had never been pushed — pushed now).
- CT13 waves: 25 `881702d8` (session 6), **26 `f598da72`** (this session: repair R2 records, Gemini R2 root, lane-3 re-pin, owner deliverables, queue log, run state).

## 1b. Sat afternoon (13:5x) — WP-P0-12 Layer A CLOSED; evening Sol chain running; P0-31 repair queued
- **P0-12 Layer A CLOSED 13:53** (`P012_CLOSEOUT_RECORD_20260919.md` FINAL): intake adapter `8cbf4f1a` (accepting half + NIT slice + D6-START B) merged as PR #196 → master `5214f169` (12:36); capture tool `9f1aa532` (NIT slice + Sol repair R-1/R-2) merged as PR #197 → master `9edf846b` (13:53). Each candidate: Gemini PASS + exact-Opus PASS-WITH-NITS + exact-Sol PASS-WITH-NITS + two reproductions, 0 REQUIRED on the final bytes. Owner rulings of the day: `P26-EVID B`, `P012-MERGE capture|intake go when roster complete`, `MAX-LANES today`, `INTAKE-ASIS` then `INTAKE-MERGE slice`, `D6-GRID A`, Codex audits unlimited (rows `OD-20260919-*`). Production admission unchanged (NOT granted).
- **Two package sessions did the work** (Sonnet 5): "P0-12 package session" (capture tool; T3 + Sol repair) and "Session prompt intake" (intake slice in the separate worktree `C:/tmp/P012_INTAKE_NIT_20260919`). Both told to write a final note under `P012_PACKAGE_SESSION/` and stay idle. Lesson: a package session re-pinned a SHARED launcher (`P012INTAKE/opus/run.ps1`) and the Lead chain generated its Sol lane from it — it worked out (the slice needed that read) but the rule is now: package sessions never edit a launcher the chain reads without telling the Lead first.
- **Carried P0-12 follow-ups (own rosters, not blocking):** next intake slice = `D6-GRID A` + Sol NITs 1-3 of `8cbf4f1a`; next capture slice = Opus NITs of `1029d6e9`/`9f1aa532` + Sol NIT-1 of `9f1aa532`; O-1 (`O-1 wait`), r3 real capture, Layer B items — owner-gated. `CT13-MERGE now|later` still open (default later).
- **Codex PRO (`free`) serves Sol** — confirmed by four lanes today. Evening chain re-armed 13:57 on `free` for P026 (`2e03a669`) → P021S1 (`eada65ed`) → P030 (`b4df1413`) → DD06 (`a46da0aa`, safety rule appended); finished lanes (P031, P1CAP, P012INTAKE) auto-skip. Adjudicate each as they land; `secondary` (Plus) opens 18:47 for a second pool.
- **P0-31**: Sol REQUEST_CHANGES on `61c56148` (F-1 CONFIRMED + reproduced: a writer-supplied `check_set_purpose` bypasses the version→purpose uniqueness rule; `OPUS_QUEUE_20260916/P031/LEAD_ADJUDICATION_P031_SOL_T0.md`, repro `P031_SOL_20260919/`). Repair = T0 round 3 (the cap) → **P0-31 package session (Opus 5)** with the semantic NIT slice: paste `SESSION_PROMPT_P031_PACKAGE_20260919.md` when the review slots are free. Not merge-ready until then.
- Interpreters: P0-12/Bridge = `C:/tmp/P012_FUNDING_PY312_20260911`; root checkers (P0-20/30/26/31) = the `P020_IMPL_20260912` venv. Routes: `route-lessons-2026-09-19` memory (Start-Job orphans a lane; a Codex cap is an ERROR event, never quoted text; Claude Pro 5h cap after ~3 lanes; MAX lanes worked under the ruling and one hit the Max session limit on a wrap-up turn — counted with stream evidence).

## 2. Package table (delta from the session-7 handoff; everything not listed is unchanged there)
| Lane | Package | Pin | Exact-Opus | Gemini | Next |
|---|---|---|---|---|---|
| 3 | P0-30 | `C:/tmp/P030_INTEGRATION_20260913` @ **`b4df1413`** | **PASS-WITH-NITS (attempt 5, 0 REQUIRED)** | R2 PASS | Sol tonight on `b4df1413` (chain position 6); then merge PR; NITs 1-3 of the fifth read = one follow-up slice after Sol |
| 2, 8 | **P0-12 MERGED**: capture `9f1aa532` (PR #197 → `9edf846b`), intake `8cbf4f1a` (PR #196 → `5214f169`) | done | done | Layer A closed; follow-up slices carried (D6-GRID A, NITs) |
| 1, 4, 6, 7 | P031 `61c56148`, **P026 `2e03a669`** (= `505af399` minus the evidence-doc note, per `P26-EVID B` = `OD-20260919-P026-EVID-B-1`; code blobs identical, Opus + Gemini carry), P021S1 `eada65ed`, DD06 `a46da0aa` | unchanged | PASS-WITH-NITS | PASS | Sol tonight (chain order P031 → P1CAP → P026 → P021S1 → P012INTAKE → P030 → DD06; DD06 brief gets the SAFETY RULE appended by the chain) |
| — | P0-20 record, P027CI retrospective | — | — | — | after the chain, by hand, if the Plus bucket allows |

## 3. The Sol chain (RE-ARMED 10:40 as pid 43936 for **11:11 local on the Codex PRO account `free`** — the owner said the Pro weekly bucket resets 11:10; the Plus pool `secondary` still resets 18:47 and is the fallback: `-StartAt <time> -Account secondary`; `C:/tmp/SOL_QUEUE_20260919/sol_chain_20260919.ps1`)
Sleeps until the start time, then per package: skip if `SKIP_<pkg>.txt` exists or `<pkg>/sol/launch.exit` exists; waits while `agy.exe` runs; GENERATES the lane from the CURRENT Wednesday pin (`make_sol_lane.ps1`, refuses on drift); appends the safety rule to the DD06 brief; launches `run.ps1`; logs to `SOL_CHAIN_LOG.txt`; **stops on a usage-cap signature** (Plus pool ≈ 3-4 lanes per 5-hour bucket) — relaunch by hand after the bucket: `powershell -NoProfile -ExecutionPolicy Bypass -File C:\tmp\SOL_QUEUE_20260919\sol_chain_20260919.ps1 -StartAt 2026-09-19T23:50:00` (lanes with `launch.exit` are skipped automatically). **A sleeping host kills the chain** — check `SOL_CHAIN_LOG.txt` first thing. After each lane: `git status --porcelain` in that worktree must be empty; adjudicate (grep every citation; reproduce one RED arm); archive under CT13 `<pkg>_SOL_20260919/`; queue-log row. Every Sol PASS + Opus PASS + Gemini + Lead reproduction on the same bytes = merge-ready → merge PRs need the owner's word for P0-12 (`P012-MERGE … go`); the others follow the standing delegation (PR-only serial merges, Bridge suite green).

## 4. Owner one-liners open / proposed
Open: none (`P26-EVID B` ruled 10:3x → `OD-20260919-P026-EVID-B-1`, applied as the docs-only commit `2e03a669`). Proposed (not urgent): `P012-MERGE capture go` / `P012-MERGE intake go` (after the package session's rosters complete), `CT13-MERGE now|later` (194-commit records branch → master), `O-1 build` (whenever he wants the oracle auto-fill). DD-06 probe run r4 still needs his separate word after the execution packet is re-issued (N-22).

## 4b. Interpreters (two pins, do not mix)
- **P0-12 / Bridge suite** (capture tool, intake adapter, `IBKR_PAPER_BRIDGE/tests`): `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe` (Python 3.12, eth_account + pytest) - the interpreter the P1CAP and P012INTAKE briefs pin at line 13 and session 6's `1619 passed` record used ("Bridge interpreter").
- **P0-20 / P0-30 / P0-26 root checkers**: `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (Python 3.12.12, no crypto packages).
- Never install into either; the session-7 handoff's section 6 wording "the pinned Bridge/P020 interpreter" was ambiguous and sent the P0-12 package session to the wrong venv on Sat 11:0x - corrected in the package prompt.

## 5. Lessons of the session (also in memory)
- Read the derived-depth / skip record BEFORE calling a GREEN run evidence: my first `nesting_arms()` re-probed the layer from a shallower frame and silently skipped the walk arms on both interpreters while the suites were green.
- Every guard output's `[unpushed]` line: `d426e79f` had never been pushed.
- Bash tool: quoted heredocs with an apostrophe fail; `printf` mangles `\t`/`\r` in Windows paths; `-File dir\x.ps1` loses backslashes — write scripts with the Write tool, run them.

Recorded by Claude Fable 5.1 Lead (session 7, `18c1b8`).
