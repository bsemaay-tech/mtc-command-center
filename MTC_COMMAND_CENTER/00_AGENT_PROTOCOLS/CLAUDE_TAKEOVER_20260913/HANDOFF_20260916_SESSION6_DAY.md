# HANDOFF — P0 Claude Lead, session 6 (day, 2026-09-16) — written 08:16 UTC+3 by session 5 (`03c6c8`) at 84 % context

Times for the owner: **UTC+3** (his rule, 2026-09-16); records may keep Z. English output. Plain language to the owner.

## 0. First 20 minutes (in order)
1. `ListAgents` + `tasklist` (`agy.exe` / `codex.exe` / `grok.exe` / `claude.exe`): session 5 (`tradingview-lab-clean-55`) is closing and will not write after this file; the owner's Codex desktop app-server (pid 32100) is idle, not a lane. Append your claim to `C:/tmp/CLAUDE_P0_RUN_20260913/OWNERSHIP_CLAIM.md`.
2. Read `MORNING_SUMMARY_20260916.md` (what the night produced; asks), then `HANDOFF_20260915_SESSION5_OVERNIGHT.md` §2 + §2-delta-1/2/3 + its NEXT SESSION block (per-package state), then `RUN_STATE.md` tail (the 08:05-08:15 UTC+3 lines), `OWNER_DECISIONS_PENDING.md` tail, `C:/CT13/DECISIONS.md` rows `OD-20260916-*`.
3. Governance as always: root `AGENTS.md`, `DECISIONS.md`, `CONTEXT_MAP.md`; stage `00_AGENT_PROTOCOLS/` five files; `mtc-repo-guard` before any repo action. Probe routes, never recall them.
4. No owner message is needed to start: every ask is answered except F (optional demo mail, default skip). One optional new ask you may put to him when convenient: **r3** — fund the `kvm2-bridge` agent wallet on the testnet faucet with a few test-USDC and re-run the DD-06 probe once, to prove that agent-signed withdraw/send act on the agent's own account (see `DD06_TESTNET_PROBE_20260915/LEAD_READING_r2_20260916.md`).

## 1. The day plan (owner asked "what is your plan / can we close packages" — answered: closing needs the flagships; Wednesday 23:00 UTC+3 Opus, Friday 21:47 UTC+3 Sol)
| When (UTC+3) | Do | Authority |
|---|---|---|
| now → ~12:00 | **P0-12 accepting-half slice** on `feature/p012-funding-intake-adapter-20260915` (worktree `C:/tmp/P012_INTAKE_20260915`, HEAD `4c802e9b`): implement D-1..D-6 in `export_mtc_funding.py` behind an explicit `--evidence-kind REAL_CAPTURE_READ_ONLY` admission (never a `--mode` toggle), `HL_USERFUNDING_ROW_V1` digest domain, `hl-funding:` ids, verbatim timestamps + `interval_hour_utc`, oracle requirement, witness identity naming the ownership-signature record, D-6 witness with 1-s end tolerance + fills-based expected count; RED/GREEN against the real r1 capture (`C:/tmp/CLAUDE_P0_RUN_20260913/P012_PATH1_REAL_CAPTURE_20260915/r1/`); full Bridge suite (`--basetemp C:/bt_*`, ASCII); Ruff; guard; Gemini detection root (ASCII lane logs); Lead = disclosed builder, never acceptor. **Still admits no production** (Q3 "Wait", gate order Q6). | `OD-20260916-P012-INTAKE-D1-D6-R-1` (packet text: "one Lead slice adds the accepting half") |
| ~12:00 → ~15:00 | **P0-31 M2 design half** (documents + scratch copies only, no ledger write, no code on the P031 branch): source-row census of the five stores, mapping-table dry application → reconciliation-report shape, three D026 fixture designs; `MIGRATED_2026` label, §4 composite fold, lower-state conflict rule, UNKNOWN listing per the answers | `OD-20260916-P031-M2-Q1-Q6-1` (Q1 A; build waits: Q6 B) |
| any time | records to CT13 after each block (`copy_night_records_to_ct13.py` in the session-5 scratchpad has 14 waves — write your own copy step, keep the FINAL mailbox-mask pass last); heartbeat RUN_STATE/LANE_TABLE every 2 h | standing |
| **23:00** (20:00Z) | `powershell -NoProfile -ExecutionPolicy Bypass -File C:\tmp\OPUS_QUEUE_20260916\run_queue.ps1` (lanes 1-3: P031 `48bd70de` → P1CAP `af921d75` → P030 `153edee9`), then BY HAND lane 4 `P026\opus\run.ps1` (`e114ed31`), lane 5 `P027CI\opus\run.ps1` (T1 of PRs #193/#194 = the merge gate), optional 6-8 (`P021S1` `7fecf204`, `DD06` `1af85067`, `P012INTAKE` — re-pin if the day's slice moved HEAD!). Never while `agy.exe` runs; one Pro lane at a time; adjudicate each (grep citations, reproduce one RED arm), record under CT13 `<pkg>_OPUS_20260916/` | `OD-20260915-P027-T1-WEDOPUS-1`; queue file `OPUS_QUEUE_20260916.md` |
| Thu 2026-09-18 | Grok pre-screen `laneGKDERIV_grok/run.ps1` → adjudicate → P0-20 measurement once | `OD-20260915-P020-MEASURE-GROK-1` |
| Fri 2026-09-19 21:47 | Sol lanes generated by `C:\tmp\SOL_QUEUE_20260919\make_sol_lane.ps1 -Pkg <…>` AFTER the Wednesday verdicts; Codex `secondary`; one at a time | queue README |

## 2. State you inherit (verify identities before acting)
- CT13 `feature/claude-takeover-20260913` @ `9ac6b89d` (pushed; 0 behind master `fcac0ac6`). Docs branch `docs/session-20260829-status` @ `6bd01789`.
- Lead-built candidates (pushed, unmerged, all with a counted Gemini read): P0-30 `153edee9`; P0-12 intake `4c802e9b`; DD-06 probe `1af85067`; P0-26 `e114ed31`; P0-21 S1 `7fecf204` (catalogue only — owner H); PRs #193 `a3325836` / #194 `63b7bbe0` (OPEN, MERGEABLE).
- DD-06: r1 aborted (tick size), r2 ran 08:12 UTC+3: control arm accepted; withdraw3/usdSend/spotSend/approveAgent all REFUSED "Must deposit before performing actions" → INCONCLUSIVE by the script's rule; Lead reading = the venue attributes those user-signed actions to the signer's own empty account (both agent wallets hold zero) — indirect evidence for the restriction; register stays BLOCK; r3 optional. Testnet clean (no orders; agents unchanged).
- P0-26 measurements: owner PC has ONE physical NVMe (55 GB free); daily all-store set 339 MiB; two sandbox-locked pytest dirs to exclude before a real run. Packet draft 2 + addenda.
- Owner words of the morning: `r2 go · H catalogue only · I A A A A A B · D all R` (rows `OD-20260916-*`). Open: F only (optional).

## 3. Rules (unchanged; the ones session 5 tripped on are starred)
No host contact except an owner-worded testnet action; no deploy; no trading; no mainnet; no spend beyond subscriptions; no merge of workflow files without the T1 verdict; no Claude lanes for audits. ★ No git of any kind while `agy.exe` runs (session 5 slipped once with a `rev-parse`; the Sol generator now refuses). ★ Lane evidence text files ASCII-only before Gemini staging. ★ Hash `git show` blob bytes in lane SHA256SUMS, not the CRLF checkout. ★ Fixture tests for venue-facing code need one real-shaped value. Guard PASS before every commit; exact staged paths; `HL_API_WALLET_KEY` never printed; short-form owner address in prose; never copy the owner's Gmail addresses; never run bare `codex`; the Lead never accepts its own code; Bridge pytest `--basetemp` ASCII and outside `C:/tmp`.

## 4. Where things are
Run root `C:/tmp/CLAUDE_P0_RUN_20260913/` (RUN_STATE, LANE_TABLE, OWNER_DECISIONS_PENDING, OWNERSHIP_CLAIM, `native_read_audit.py`, `gemini_retry_chain_v2.py`, `GEMINI_CHAIN_LOG.txt`); Gemini roots `C:/tmp/P012_S16_REVIEWS_20260913/<ROOT>/` (`PRE_STAGE.py`, `PROMPT.md`, `HOLD_AUTHORIZATION.txt`, `RUNNER.txt`, `HEAD_PIN.txt`); runners `C:/tmp/P012_S16_LAUNCH_PREP_20260913/run_<x>_g38.ps1`; packets `C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/`; queues `C:/tmp/OPUS_QUEUE_20260916/`, `C:/tmp/SOL_QUEUE_20260919/`; Grok lane `laneGKDERIV_grok/`; interpreters: Bridge `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe`, pinned 3.12.12 `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe`, Ruff `C:/tmp/wp_p0_04_tooling_20260825/Scripts/ruff.exe`; KVM2 = `baris@152.239.123.231`, key `~/.ssh/hostinger_kvm2`, smoke copy `/tmp/smoke_p0_20260914T140527Z/IBKR_PAPER_BRIDGE` (probe installed in its `tools/`).
Memory: `p0-claude-lead-session-2026-09-15-night`, `route-lessons-2026-09-15-night`, `owner-time-display-utc-plus-3` (all updated 05:00-05:15Z); MEMORY.md head points here after this file is written.
