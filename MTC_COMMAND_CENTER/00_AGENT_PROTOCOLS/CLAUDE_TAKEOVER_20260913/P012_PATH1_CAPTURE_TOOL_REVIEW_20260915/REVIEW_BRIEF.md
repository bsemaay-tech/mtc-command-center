# Review brief — WP-P0-12 Path 1 read-only own-account evidence capture tool, candidate `e77af1c8` (2026-09-15)

You are a fresh, independent DETECTION reviewer of a NEW tool (three new files, no existing code changed). Your exact role (model, effort, read-only or executing) is fixed by the launcher that started you. Output ONE report; write nothing else outside your own report directory.

## 1. Context (settled)
- WP-P0-12 production admission: the owner answered the risk packet Q1-Q7 on 2026-09-14 (`OD-20260914-P012-ADMISSION-Q1..Q7`; Q1 = the owner himself self-attests his account; Q3 "Wait" = no real fee tier is declared yet; production fee admission stays blocked until real evidence). "Path 1" = evidence from the owner's OWN Hyperliquid mainnet account: on 2026-09-14 16:03-16:14Z the owner placed three declared BTC fills (T2/T3/duplicate/T1-maker), held 0.00058 BTC across the 17:00Z and 18:00Z funding settlements and closed at 18:08Z (net +0.28 USDC) — his hands, his wallet, his clicks; the Lead only watched read-only. The commissioning text is `sources/P012_PATH1_REAL_CAPTURE_PACKET_20260914.md` section 5 (what the capture tool must promise) and the intake rules are `sources/REAL_OBSERVATION_INTAKE.md`.
- Lane P1CAP (Codex Plus gpt-5.5 high, 2026-09-14 20:11Z-20:34Z) built the tool per `sources/TASK_P1CAP.md`. The Lead verified and committed it as **`e77af1c8`** on `feature/p012-path1-capture-20260914` (base `fcac0ac6` = master after PR #191): full Bridge suite 1638 passed / 0 failed on Python 3.12 with a clean basetemp (`sources/LEAD_VERIFICATION_P1CAP.md`). NONACCEPTING: no review yet, **no network call has ever been made by this tool**, no real capture exists. This review is the first of the roster (Gemini 3.8 detection → exact Sol → exact Opus after the 2026-09-16 20:00Z reset).
- The Lead's own findings L-1..L-6 (`sources/LEAD_VERIFICATION_P1CAP.md`) are NOT verdicts: confirm, refute or extend each with file:line. They will feed the correction lane P1FIX together with your findings.

## 2. Subject (packet; verify identities first)
Packet root: `C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P1CAP_20260915/` — every file's sha256 is in `PACKET_SHA256SUMS.txt` (Lead-computed). Blob OIDs pinned in `sources/LEAD_VERIFICATION_P1CAP.md`.
- `subject/capture_own_account_evidence.py` (548 lines, sha256 `a1ef5ee1…`) — the CLI (`--network mainnet|testnet --address 0x… --coin BTC --start … --end … --out DIR [--ownership-signature FILE] [--run-id ID]`, `--print-ownership-message`).
- `subject/path1_sign_ownership.html` (58 lines, `b43dbe39…`) — the offline EIP-191 `personal_sign` page for the owner's wallet.
- `subject/test_capture_own_account_evidence.py` (224 lines, 10 tests, `d1d2ad20…`).
- `sources/REPORT_P1CAP.md` (lane report; two of its 22 line citations are off: tool `:23`→`:24`, `:125`→`:121-122`), `sources/SHA256SUMS_P1CAP.txt`, `sources/COMMIT_MSG_e77af1c8.txt`, `sources/TASK_P1CAP.md`, `sources/REAL_OBSERVATION_INTAKE.md`, `sources/P012_PATH1_REAL_CAPTURE_PACKET_20260914.md`.
- `sources/hyperliquid_sdk_info_excerpt.md` (the pinned SDK `Info` class: constructor, `user_state`, `user_fills_by_time`, `user_funding_history`), `sources/hyperliquid_sdk_api_excerpt.md` (`API.post` / `_handle_exception` that the tool overrides), `sources/hyperliquid_broker_excerpt.md` (the ACCEPTED Bridge broker's own use of the same calls: page limits `HL_FILLS_PAGE_LIMIT` 2000 / `HL_INFO_PAGE_LIMIT` 500, mainnet/testnet URL selection), `sources/export_mtc_funding_py_excerpt.md` (what a production-class funding candidate needs; the file-source adapter into it is NOT built).

## 3. What you must decide (detection; every finding with file:line)
1. **Read-only by construction.** No import of `hyperliquid.exchange`, no signing, no order/cancel/transfer path, no key: `HL_API_WALLET_KEY` in the environment must be a hard refusal before any network object is built (`:414`); the tool must be unable to place, modify or cancel anything even if misused. Any way to reach a write endpoint = REQUIRED.
2. **Bytes are original.** `CapturingInfo.post` (`:56-84`) overrides the SDK `API.post` (excerpt): same URL composition, same session/json/timeout semantics, captures `response.content` BEFORE `_handle_exception` and before JSON parsing; the stored `*.json` files and their `.sha256` sidecars are exactly those bytes; `CAPTURE_MANIFEST.json` records the ACTUAL posted payload (from the capture), base_url, sdk_version, tool sha256, wall-clock start/end, byte length, response sha256. Check `consume_capture` (`:140-145`): when the SDK object is a test double without `pop_capture`, the "raw" bytes are re-serialized parsed JSON — state whether a real run can ever take that path and whether the manifest says which path produced the bytes (Lead L-6).
3. **Pagination and truncation.** `paged_query` (`:247-302`): the SDK returns at most 2000 fills / 500 funding rows per call (broker excerpt); the tool pages by `cursor = page_max` (inclusive restart at the last row's time, dedup by identity) and stops when a page is shorter than the limit; refuses `CAPTURE_REFUSED_TRUNCATED` when the cursor stalls. Attack it: a window with more than `limit` rows sharing one millisecond; a last page of exactly `limit` rows; `end_ms` inclusive vs exclusive between the SDK (`endTime` inclusive per the docstring) and the tool's `--end`; rows outside the requested window returned by the API (does the tool filter or trust?). Is `request_body` (`:207-217`) guaranteed equal to what the SDK actually posts (`aggregate_by_time` flag, key names `startTime`/`endTime`, `user` vs `address`)?
4. **Identities.** `fill_identity` (`:175-195`): `tid` if present else `hash+oid+time` — can two distinct fills collide (same order, same ms, partial fills)? `funding_identity` (`:197-203`): `hash:time` only — two coins funded in the same block collide → `identity conflict` refusal (Lead L-3; latent for a single-coin account). Re-query pass: the identity SETS must be equal across the two passes (`CAPTURE_REFUSED_REQUERY_MISMATCH`) — is the comparison on sets of identities AND row equality, and are both passes' bytes recorded?
5. **Ownership evidence.** `ownership_message` (`:85-91`) fixed text binds address + run_id; `ownership_result` (`:389-401`) recovers the signer with `eth_account` `encode_defunct` (EIP-191) offline and records `OWNERSHIP_EVIDENCE: VERIFIED / MISMATCH / NOT_PROVIDED` — never invents; the signature is verified AFTER the five network passes (Lead L-4: a wrong signature leaves page files without a manifest) — REQUIRED or NIT? `--run-id` default: is it stable, unique, and printed before signing so the owner signs the same run_id the capture records?
6. **The HTML page** (`subject/path1_sign_ownership.html`): no external script, no URL, no network, no transaction; only `eth_requestAccounts` then `personal_sign` over EXACTLY the same message bytes the Python `ownership_message` produces (compare character by character, including line breaks and the address casing — checksum vs lowercase); displays the signature for copy-paste; reads address/run_id from the query string. Any divergence between the page's message and the tool's message = REQUIRED (the signature would verify MISMATCH).
7. **Write discipline.** `write_once` (`:112-119`): `exists()` then `write_bytes` (TOCTOU; `open(path, "xb")` is the exclusive form — Lead L-5); `verify_sidecars` (`:403-411`) re-hashes every stored file against its sidecar before the manifest is final; outputs go only under `--out`; nothing else on disk is touched; the tool never deletes.
8. **Errors.** `call_info` (`:147-163`): on an SDK exception the capture is popped and `raw` dropped — the error body is lost (Lead L-5) and the refusal code is generic `CAPTURE_REFUSED_QUERY_FAILED`; non-2xx from `_handle_exception` — are the bytes of the error response recorded before refusing?
9. **Tests** (`subject/test_capture_own_account_evidence.py`): map each RED arm the task demanded (truncated page, re-query mismatch, tampered stored bytes vs sidecar, non-2xx/exception, malformed address, key in environment, testnet/mainnet URL mix-up) to a test with file:line; state which arms are MISSING — the Lead found no RED test for a WRONG ownership signature (only the GREEN recovery at `:204`) and no multi-page success test (only the stall refusal at `:129`) (Lead L-2). Do the test doubles exercise `CapturingInfo.post` at all, or only the `consume_capture` fallback?
10. **Network selection.** `info_base_url` (`:121-123`): mainnet/testnet mapped to `constants.MAINNET_API_URL` / `TESTNET_API_URL` exactly as the broker does; the recorded base_url is asserted in a test.
11. **Lint** (Lead L-1): `F401` tests:5 unused `os`; `RUF100` tool:157; `F841` tool:160 unused `raw`; `ruff format --check` would reformat both files — NIT class unless one hides a defect.
12. **Scope boundary.** Nothing in `export_mtc_funding.py`, the broker, `bridge/app.py`, deploy files or protected scopes is touched (three new files only, per `sources/COMMIT_MSG_e77af1c8.txt`); the proposed file-source adapter (REPORT_P1CAP NOT VERIFIED section) is a proposal, not code — say whether the DERIVED extraction view carries every field the intake names (`coin`, `fee`, `feeToken`, `time`, `tid` or `hash`+`oid`, `closedPnl`, `crossed`/`side`/`px`/`sz` as native strings, `capture_sha256`, JSON pointer).

## 4. Report format
Markdown with: verified identities (COMPUTED for executing reviewers; QUOTED-and-compared for read-only reviewers); a promise-conformance table (packet section 5 promise → file:line → EQUAL / DIFFERENT / NOT FOUND); an L-1..L-6 table (CONFIRMED / REFUTED / EXTENDED with file:line); the RED-arm map of item 9; findings numbered with severity REQUIRED / NIT and exact file:line; the commands you ran with cwd and exact observed output (executing reviewers; read-only reviewers: exact read coverage with ranges and continuations); a NOT VERIFIED section (non-empty: no network call, no real capture, the owner's account state, the real API's pagination behaviour); and a final line exactly `VERDICT: PASS` or `VERDICT: PASS-WITH-NITS` or `VERDICT: REQUEST_CHANGES` or `VERDICT: BLOCK`. Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. Your report alone accepts nothing; the Lead assembles the roster.
