# LEAD_TERMINAL — P012_RISK_PACKET_GEMINI_B attempt 1 (gemini-3.8-flash-high DELTA audit of the P12FIX-corrected packet) — 2026-09-14 12:50Z

**Outcome: WRAPPER FAILURE after a completed model run — NOT counted as review coverage.** `launch.exit` = `exit=1 actual_exit=1 start=2026-09-14T12:35:05Z end=12:41:25Z`; `REVIEW.log` = `error=System.Management.Automation.RuntimeException: Gemini payload contains missing or unexpected members.` (wrapper `Invoke-GeminiProReadOnly.ps1` sha256 `66c749a0…`, `Assert-ExactJsonProperties … 'Gemini payload'` at line 742: the print-mode `result` object did not have exactly the six members `conversation_id, status, response, duration_seconds, num_turns, usage`). The wrapper discards the raw stdout on this throw, so the offending member is not recorded.

## What the CLI's own store shows (read-only recovery; `~/.gemini/antigravity-cli/`)
- Conversation `259ba939-5df3-404b-9082-3d01bda6d1e3`; 67 steps; 30 `view_file` calls, no other tool; every required file/range in PROMPT.md was read (packet 1-291 in two views; matrix 1-66; SIGNED 1-20; REVIEW_POLICY 1-80; HL setup 1-115; README 130-148; COMMANDS 96-105 + 258-266; master-plan excerpt; LEAD_NOTE; amendment 1-50/140-220/330-410 covering all 19 cited lines; PATH_D packet 1-258; DISPOSITION_FIX1; TASK; TASK_FIX1; prior report 309-389 + 424-446; FILE_MAP; both SHA256SUMS). First read = `C:/LAB/Tradingview_LAB_CLEAN/AGENTS.md` — instructed by the wrapper's own preamble ("Read and obey AGENTS.md"), not a boundary violation.
- Steps 1-5 are `ERROR_MESSAGE` records: `API error (attempt N): INTERNAL (code 500): Internal error encountered.` (12:35:54Z-12:36:42Z, backend-side, retried by the CLI; `cli.log` `run.go:389` "Run: attempt 1..5 failed"). The run then proceeded normally. **Most plausible cause of the envelope mismatch:** print mode's result builder (`printmode.buildResultOutput` / `hasRecordedErrorStep` / `failureFromTrajectory` in `agy.exe`) adds an omitempty error-class member when the trajectory holds recorded error steps; the 6-member schema the wrapper pins has no room for it. Same defect CLASS as the 2026-09-08 wrapper repair (a legal ERROR step discarded a completed review), one level up. Not proven — the raw envelope was not captured.
- Final model output (step 66, `transcript_full.jsonl`, 45,176 chars, ends with `GEMINI_READ_ONLY_OK`) recovered byte-exact to `CLI_TRANSCRIPT_RECOVERY/transcript_full.jsonl.final_response.md`. Content: verdict **PASS**; delta F-01..F-09 all **RESOLVED** with packet proof lines; Table A 10/10 and Table B 27/27 complete; citation table 115/115 EQUAL (0 WRONG LINE, 0 NOT FOUND); new findings **none**; NOT VERIFIED: unexecuted runtime, funding cadence not established, five residual obligations only counted, 24-27 grouped.
- The wrapper's integrity checks that run BEFORE the payload assertion (filesystem-change watcher, repository snapshot comparison, project-config hash, exit code 0) all passed — otherwise their own messages would have been thrown first.

## Disposition
- Evidence class for this attempt: **SUPPLEMENTAL / RECOVERED, not wrapper-validated**. It corroborates the Lead's mechanical check (`LEAD_CITATION_CHECK.md`: 235 citations, 177 quote-EQUAL, 57 table-cell citations with no quote to compare, 0 wrong) but does not by itself satisfy the audit slot.
- Attempt 2 launched as a new record (`P012_RISK_PACKET_GEMINI_B2`, same staged packet, same prompt except the part id) at 12:48Z.
- Route finding for the owner (decision, not executed): the wrapper should (1) persist the raw stdout next to the review on any post-run failure, and (2) tolerate an error-class member in the result envelope when `status` is `SUCCESS` and the sentinel is present, reporting it on stderr — the 2026-09-08 pattern. Until then a transient backend 500 can void a completed review.

Adjudicated by Claude Opus 5 Lead (session b9df29).
