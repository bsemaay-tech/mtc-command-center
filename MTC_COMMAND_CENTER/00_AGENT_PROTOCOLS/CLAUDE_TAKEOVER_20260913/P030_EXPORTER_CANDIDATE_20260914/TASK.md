# TASK O9EXP - implement the WP-P0-30 producer/exporter bridge, Shape B (owner: "O-9 implement GO", `OD-20260914-P030-EXPORTER-GO-1`; shape `OD-20260914-P030-BRIDGE-SHAPE-1`) -> new module + tests + D-13 GREEN half on fixtures + ONE local commit

You are a BUILDER (gpt-5.5 high, Codex Plus). Worktree: `C:/tmp/P030_INTEGRATION_20260913` (branch `feature/p030-integrated-20260913`, HEAD `f42fd5400b2c2ecb805644d406999cb55c8178c8`). Read first: the design choice `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/P030_BRIDGE_DESIGN_CHOICE_20260914.md` (Shape B section + comparison table), the gap facts in `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/P030_P026_CONFIG_DRILL_CHOICES_20260913_V3.md:498-517` and drill D-13 `:442-449`, then the code: `market_data_collector.py` (`MarketBar` :90-111, `MonthlyArchive._append` :294-300, `HashContract` :66-80), `p030_market_data_contracts.py` (`DATASET_ROW_FIELDS` :53-58, `producer_payload_hash()`, `observation_id()`, `dataset_content_hash()`, prefixes), `p030_closed_partition_backup_adapter.py` (`_prefix_facts` :197-213, `capture_stable_prefix` :229-290, `_STABLE_FIELDS` :35-46), and the three check suites (`check_market_data_collector.py`, `check_p030_market_data_contracts.py`, `check_p030_closed_partition_backup_adapter.py`).

## Build (Shape B — the collector stays byte-for-byte untouched)
1. New module `p030_archive_exporter.py` (same directory as the other P030 modules): a pure function `export_partition(source_jsonl: Path, target_jsonl: Path, *, source_root: Path) -> ExportReceipt` that reads ONE collector-written monthly partition once as bytes, parses each line, re-computes each row's `producer_payload_hash` and `observation_id` through `p030_market_data_contracts` (prefixes `p030payload-v1:` / `p030obs-v1:`), re-serializes each row with `json.dumps(..., sort_keys=True, separators=(",", ":"), ensure_ascii=False)` + `\n` in `DATASET_ROW_FIELDS` order semantics the contract requires, writes the target with exclusive create (`"xb"`, never overwrite), re-reads the target and refuses if its bytes differ from what was written, computes `dataset_content_hash()` over the exported rows, and writes a receipt `<target>.p030export.json` with: `schema: "p030.archive_export/v1"`, `source_path` (relative to `source_root`), `source_sha256` (of the exact source bytes read), `source_record_count`, `exporter_sha256` (sha256 of the exporter module's own bytes at run time), `exported_sha256`, `exported_record_count`, `dataset_content_hash`, `exported_at_utc` (caller-supplied, UTC Z), `identity_recomputed: true`. Refusals (typed exception `ExportRefused` with a code): non-canonical/unparseable source line; a row whose recomputed identities disagree with the row's own stored identities in a way the contract forbids (document which — the collector's bare hex ids are EXPECTED to differ from the prefixed ones; that is the point of the bridge; but symbol/interval/bar_open_time/payload fields must be preserved byte-for-byte); target exists; short/partial read; empty partition. Standard library only.
2. Tests `check_p030_archive_exporter.py` in the style of the existing check suites (deterministic fixtures, RED/GREEN): export of a collector-written fixture partition produces a target that `capture_stable_prefix` ACCEPTS (this is the D-13 GREEN half — build the fixture with `MonthlyArchive` and a fake bar or two exactly as `check_market_data_collector.py` does), receipt fields present and correct, byte-identity re-read refusal, target-exists refusal, malformed-line refusal, determinism (two exports byte-identical), exporter_sha256 equals the module file's digest, and the negative control: the RAW collector partition is still refused by `capture_stable_prefix` (D-13 RED half) — cite the exact refusal message.
3. Run every existing check suite in the worktree unchanged (collector, contracts, adapter) + the new one; paste outputs. The collector suite must show the collector untouched (no diff to `market_data_collector.py` — prove with `git -c safe.directory=* diff --stat`).
4. ONE local commit (message `p030: add Shape-B archive exporter (producer/exporter bridge, D-13 GREEN half) (OD-20260914-P030-EXPORTER-GO-1)`, trailer `Co-Authored-By: Codex gpt-5.5 <noreply@openai.com>`), exact paths only; no push/PR/merge/stash. Print the new HEAD.
5. `C:/tmp/CLAUDE_P0_RUN_20260913/laneO9EXP_build/REPORT.md`: design conformance table vs the Shape-B section (each promise → file:line), receipt schema, refusal codes, verbatim check outputs, the D-13 RED→GREEN pair with messages, NOT VERIFIED (not reviewed; no real archive; nothing accepted; acceptance edges unmoved), `SHA256SUMS.txt` (LF).

## Boundaries (binding)
- Writable: the worktree and the lane dir. Collector, contracts, adapter modules: read-only (a change to any of them is out of scope — STOP and report instead).
- Git only inside the worktree (`-c safe.directory=*`; add exact paths, commit, diff, status, rev-parse); no git while `agy.exe` is running. No network, install, host, credential, real archive/backup root, schedule, send.
- Cite `absolute/path:line` for every claim; read the line before citing it.
