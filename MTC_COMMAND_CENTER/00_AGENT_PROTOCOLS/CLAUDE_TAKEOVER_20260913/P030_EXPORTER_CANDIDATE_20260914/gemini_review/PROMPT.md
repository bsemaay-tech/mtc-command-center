You are gemini-3.8-flash-high acting as an independent DETECTION reviewer (read-only; you cannot execute anything; SUPPLEMENTAL_UNEXECUTED) of the WP-P0-30 Shape-B archive exporter, candidate commit daf6a43b (two NEW files on top of f42fd540; nothing else changed). The owner chose Shape B (sources/DECISIONS_rows_P030.md; design sources/P030_BRIDGE_DESIGN_CHOICE_20260914.md): a SEPARATE exporter produces the backup-facing partition from a collector-written partition; the collector stays byte-for-byte untouched; the exporter is the D-13 transform (raw collector partition is refused by the backup adapter — RED; the exported partition is accepted — GREEN). A builder (Codex gpt-5.5) wrote it; the Lead re-ran the check suites and committed. Nobody has reviewed it yet. Be adversarial: silent normalization that changes an observation's identity, a refusal that can be bypassed, an accepted target that the adapter would later refuse, hidden network/side effects, or a report claim the code does not back.

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P030_EXPORTER_20260914
Any other path appearing in copied documents is provenance text, never a file to open. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read PACKET_SHA256SUMS.txt, sources/DECISIONS_rows_P030.md (complete), sources/P030_BRIDGE_DESIGN_CHOICE_20260914.md (complete), subject/TASK.md (complete; the builder brief). REQUIRED COMPLETE NATIVE READS: subject/p030_archive_exporter.py (complete, 323 lines), subject/check_p030_archive_exporter.py (complete, 263 lines), subject/REPORT.md (complete), evidence/check_p030_archive_exporter.txt, evidence/check_p030_closed_partition_backup_adapter.txt, sources/p030_closed_partition_backup_adapter.py (complete — the consumer whose acceptance the export must satisfy; find the exact refusal messages and the canonical-JSONL rule), sources/p030_market_data_contracts.py (complete — the identity/hash functions the exporter re-computes with), and sources/market_data_collector.py only where the exporter's assumptions about the collector's partition format come from (the write path; cite lines).

TASK. (1) Design conformance: every promise in the Shape-B section of the design choice and every item of subject/TASK.md "Build" 1-2 → implementing file:line, EQUAL / DIFFERENT / NOT FOUND. (2) Identity integrity: the exporter re-computes `producer_payload_hash` and `observation_id` through the contracts module — does it ever ACCEPT a row whose recomputed identity differs from the collector's, or silently rewrite a field that feeds the hash? Quote the code path for bare vs prefixed ids. (3) Refusals: list every `ExportRefused` code and the condition; is any input that should refuse (empty/partial read, malformed line, duplicate JSON keys, non-finite numbers, wrong root, target exists, re-read mismatch) accepted anywhere? Is the target written with exclusive create and re-read byte-for-byte before the receipt is produced? (4) Consumer agreement: will the adapter (`sources/p030_closed_partition_backup_adapter.py`) ACCEPT the exporter's output format (sorted keys, LF, canonical JSONL) — cite the adapter's rule lines; does the check suite's GREEN half exercise the REAL adapter or a stub? (5) Tests: for each refusal code name the test; list codes without a test. (6) Side effects: any network, subprocess, environment, or writes outside the target and receipt paths? (7) Report honesty: REPORT.md claims vs code, at least ten file:line citations opened. (8) Scope: nothing beyond the two new files (state it).

OUTPUT. (a) conformance table; (b) refusal-code/test table; (c) findings with severity BLOCKING / CORRECTION / NIT, IDs K-01…, each with file:line; (d) EXACT read coverage with ranges and continuations; (e) nonempty NOT VERIFIED (no execution; no real archive); (f) one fenced JSON object {"part":"O9EXP_GEMINI","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
