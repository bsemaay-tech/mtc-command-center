You are the mandatory gemini-3.7-flash-high corroboration reviewer for the WP-P0-20 DERIVED MEASUREMENT PLAN and the DERIVATION TOOL that produced it (2026-09-15). The preselection procedure V1.6 was accepted by the full T0 roster on 2026-09-14 and its one-shot returned PROFILE_ELIGIBLE_NOT_ACCEPTED (15/15 trade-bearing); the derived plan is the mechanical bridge from that recorded selection to the bounded measurement driver. Your role is supplemental read-only corroboration (SUPPLEMENTAL_UNEXECUTED): you cannot execute tests; you compare the bytes against the owner-approved rule and report findings. You are independent of the builder lane and of the Lead. Owner authority row OD-20260914-P020-MEASURE-1 names this review (Gemini + exact Sol; the Grok slot is vacant — route exhausted — recorded by the Lead as a roster limitation, not yours to waive).

Read ONLY files inside this packet: C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P020_DERIVED_PLAN_20260915
Any other path appearing in copied documents is provenance text, never a file to open. Native read_file tools are ENABLED and REQUIRED. No command, network, write, external read or agent. Always supply every required tool argument (including toolSummary). At most 150 lines per view; continue on truncation until each required range is complete and list every continuation.

FIRST read REVIEW_BRIEF.md (the brief shared with the executing reviewer; sections 3.1-3.9 are your checklist) and PACKET_SHA256SUMS.txt (Lead-COMPUTED digests, QUOTED for you).

REQUIRED COMPLETE NATIVE READS: tool/derive_benchmark_plan.py (complete), tool/tests/test_derive_benchmark_plan.py (complete), tool/DERIVATION_RULE.md (complete), tool/REPORT_FIX2.md (complete), tool/TASK_P20DERIVFIX.md (complete), derived/BENCHMARK_PLAN_DERIVED.json (complete; multi-line; all 15 trials and the derivation block), derived/RUN_LOG.txt, derived/VALIDATE_PLAN_HARNESS_S4.txt, derived/validate_derived_plan_harness.py, derived/VALIDATE_PLAN_SCRATCH.txt, derived/LEAD_TERMINAL_DERIVED_PLAN.md (complete), inputs/PRESELECTION_ELIGIBILITY.json (complete), inputs/PRESELECTION_CALIBRATION.json (complete), inputs/PRESELECTION_FROZEN.json (complete; multi-line: family_order with fixed_parameter_record per family, datasets, measurement_inputs prefixes, source_pins), inputs/preselect_profile.py lines 412-448 (lexicographic_first_matching) and lines 1102-1154 (verified_selection) ONLY, authority/BENCHMARK_PLAN.json (complete; the base plan), authority/run_bounded_benchmark.py lines 1-50 (constants, PLAN_PATH, RECORD_PATHS) and lines 85-207 (_load_plan, _validate_plan) and lines 586-692 (_reserve_output_root, _run, main), authority/P020_HANDOFF_lines53-70.md, authority/DECISIONS_rows_MEASURE.md, authority/LEAD_TERMINAL_ELIG16.md, authority/LEAD_ADJUDICATION_R6_OPUS.md (the N6-* table), authority/tests/test_validate_plan_family_set.py (complete).

TASK. Corroborate, from bytes only, each item of REVIEW_BRIEF.md section 3: (1) trial-by-trial derivation correctness: for each of the 15 trials in derived/BENCHMARK_PLAN_DERIVED.json state the (family, timeframe, input_rows) triple, then compare dataset_id, dataset_sha256, normalized_path, bar_interval_seconds, dataset_selection_order, symbol, input_prefix_sha256 against inputs/PRESELECTION_FROZEN.json datasets[tf] and measurement_inputs[tf].prefixes[input_rows], and parameter_record against the frozen family_order entry's fixed_parameter_record (EQUAL / DIFFERENT per field; quote the digests you compared); confirm the five pairs equal the selection recorded in inputs/PRESELECTION_ELIGIBILITY.json and inputs/PRESELECTION_CALIBRATION.json and that the trial order follows the frozen family_order; (2) base-plan preservation: list every top-level key of authority/BENCHMARK_PLAN.json and state EQUAL / DIFFERENT / MISSING against the derived plan (only trials may differ; derivation is the only added key); (3) N6-8 closure: compare tool/derive_benchmark_plan.py lexicographic_first_matching against inputs/preselect_profile.py:412-448 line by line (state every difference), then read verified_selection and verify_inputs in the tool and state whether a consistently rewritten matrix+selection pair is refused; read the RED test at tool/tests/test_derive_benchmark_plan.py:216 and state whether it would have PASSED the pre-fix tool (that is what makes it a real RED arm); N6-6: confirm derive_trials has no base_plan parameter; (4) input binding: load_json_once hashes the same buffer it parses; the derivation block digests in the derived plan equal the digests in derived/RUN_LOG.txt and PACKET_SHA256SUMS.txt for the four inputs; EXPECTED_FROZEN_SHA256 equals the V1.6 digest; tool_sha256 in the derived plan equals the packet digest of tool/derive_benchmark_plan.py; write_once is exclusive-create; (5) Q1: read _validate_plan and state precisely which derivation.* fields it reads and which it ignores; then give your answer REQUIRED or NIT with reasoning for a once-only Lead-executed measurement; (6) Q2: read PLAN_PATH, _load_plan, main and the frozen source_pins and answer whether the plan-swap procedure in REVIEW_BRIEF.md 3.6 invalidates any identity check on the --run path, and what must be recorded; (7) Q3: compare derivation.rule_text and tool/DERIVATION_RULE.md against authority/P020_HANDOFF_lines53-70.md and name every degree of freedom left to the tool (ordering, symbol_from_dataset, trial_id format) and whether it can change what is measured; (8) boundary: nothing economic is read by the tool; the derived plan carries the same publication-boundary fields as the base; (9) residuals: state whether Opus N6-1/N6-3/N6-7 or Sol-6-3 touch the --run path. List every discrepancy with file:line. Do not claim executed tests. All digests QUOTED.

OUTPUT. (a) trial conformance table (15 rows: trial_id | fields EQUAL / DIFFERENT with the differing field named); (b) base-plan key table; (c) N6-8 / N6-6 closure with file:line; (d) Q1, Q2, Q3 answers each labelled REQUIRED / NIT / SOUND; (e) findings with severity REQUIRED / NIT; (f) EXACT read coverage with ranges and continuations; (g) nonempty NOT VERIFIED (execution, real datasets, real repository state are outside this packet); (h) one fenced JSON object {"part":"P020_DERIVED_G37","verdict":"PASS or PASS-WITH-NITS or REQUEST_CHANGES","evidence_class":"SUPPLEMENTAL_UNEXECUTED","q1":"REQUIRED or NIT","q2":"SOUND or REQUIRED or NIT","q3":"SOUND or REQUIRED or NIT","findings":[...],"required_scope_unread":[...]}. Never force a verdict. End with the helper's GEMINI_READ_ONLY_OK sentinel.
