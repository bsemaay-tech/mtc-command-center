# Lane GKDERIV - Grok detection pre-screen of the WP-P0-20 DERIVED measurement plan + derivation tool (tool-running; NOT a T0 slot)

You are grok-4.6, an independent detection auditor. Your report is supplemental: it cannot accept anything and does not replace the exact reviewers. Owner row `OD-20260914-P020-MEASURE-1` names this pre-screen as part of condition (c); owner row `OD-20260915-P020-MEASURE-GROK-1` ("no, wait for Grok (Sep 18)") makes the bounded measurement wait for it. Exact Sol (PASS-WITH-NITS, 0 REQUIRED) and Gemini 3.7 (PASS-WITH-NITS) already reviewed the same bytes on 2026-09-15; the Lead's own check is ALL EQUAL. Your job is to find what they missed.

## Subject (read-only; verify every digest before reading anything else)
- Derived plan `C:/tmp/CLAUDE_P0_RUN_20260913/P020_DERIVED_PLAN_20260914/BENCHMARK_PLAN_DERIVED.json` sha256 `d84b043a2b262d1c9a3c98f29f538eec169c05a0ecad0a97d801a01d9a7e2580`; its `RUN_LOG.txt`; the Lead harness `validate_derived_plan_harness.py` and outputs `VALIDATE_PLAN_HARNESS_S4.txt`, `LEAD_TRIAL_CHECK_S4.txt`.
- Derivation tool `C:/tmp/P020_PLAN_DERIVE_20260913/derive_benchmark_plan.py` sha256 `0bfd18915a0952cbf8bd1058b5d560a5cd30558a3f2f6d2ae6706947152289d7`, tests `tests/test_derive_benchmark_plan.py` (16), `DERIVATION_RULE.md`, `REPORT_FIX2.md`.
- Inputs `C:/tmp/P020_PRESELECT_20260913/`: `PRESELECTION_FROZEN.json` `cb75602080850873e28dc3aa268ad1072a0f1d50148d91ad75c9896cf37fb126` (V1.6), `PRESELECTION_ELIGIBILITY.json` `fc5f8895a38147cbc76d93560b61f33cdea3b8215088acfbe6f6e8014c7bc6c2` (status PROFILE_ELIGIBLE_NOT_ACCEPTED), `PRESELECTION_CALIBRATION.json` `808cbcb4ce529450e7e832baeffae00d00e64a57c456331936a75232c3299427`; `preselect_profile.py:412-443` (matching) and `:1102-1154` (verified_selection).
- Driver `C:/tmp/P020_LEAD_20260912/benchmark/run_bounded_benchmark.py` sha256 `3d4453cd23ad0f56ae0b34f94b21b7b1f3715f180091821b383369aa62fab324` (`_validate_plan` `:95-207`, `_run` `:596-668`, `main` `:671-688`), base plan `BENCHMARK_PLAN.json` `c6f07afd14301e640841e7f4ec95dfcef860df3a02e3ef3768c1513c517104c7`, records V3/cost/funding + sidecars.
- The two prior reviews for context (do not defer to them): `C:/tmp/P020_DERIVED_PLAN_REVIEW_20260915/sol/SOL_DERIVED_REPORT.md`, `C:/tmp/P012_S16_REVIEWS_20260913/P020_DERIVED_G37/REPORT_RESPONSE_UTF8.md`; the shared brief `C:/tmp/P020_DERIVED_PLAN_REVIEW_20260915/REVIEW_BRIEF.md` (items 3.1-3.9).
- Pinned interpreter `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (3.12.12).

## Boundary
Copy the tool dir and the derived-plan dir to `C:/tmp/GROK_SCRATCH_GKDERIV/` and work only there; the package, benchmark and derived directories are read-only. Run the tool tests with the pinned interpreter and `-p no:cacheprovider`; re-run the ORIGINAL tool file to a scratch output and compare the digest; run the harness on the real derived plan and on mutated scratch copies. Never run `run_bounded_benchmark.py --run`; never run `preselect_profile.py --oneshot` (the shot is spent); no git command of any kind; no network.

## Detect
(1) Trial-by-trial: every field of the 15 trials bound to the frozen bytes (datasets, prefixes, fixed parameter records) — find any field that is derived from something other than the frozen artifact. (2) Base-plan value preservation key by key. (3) Matching re-derivation: is `lexicographic_first_matching` a faithful transcription; can a rewritten matrix+selection PAIR still pass (design your own mutation beyond the packaged RED test); can a matrix with non-boolean truthy values (`1`, `"true"`) pass `is True`; can a selection that omits or duplicates a timeframe pass. (4) Input binding: single read per input; digests of the parsed buffer; `EXPECTED_FROZEN_SHA256`; `tool_sha256` self-hash — can a modified tool still emit the accepted plan bytes (if the self-hash is the only difference, what does that mean for the review chain)? (5) Driver: `_validate_plan` reads only `derivation.family_order` — build a plan with the right family_order but trials whose `parameter_record` or `input_prefix_sha256` do not match the frozen artifact and show whether the driver accepts it (it validates datasets and manifest, not prefixes/params against the frozen file) — then say what the launcher's whole-file digest assertion (`d84b043a…` before validate, before `--run`, after) does and does not close. (6) Plan swap: `PLAN_PATH` fixed; base kept as `BENCHMARK_PLAN_BASE_c6f07afd.json`; `SHA256SUMS_V16.txt` plan line swapped for the interval and restored; any identity check on the `--run` path that the swap breaks? (7) Boundary: nothing economic read by the tool; output contract unchanged. (8) Write three RED probes of your own and report their observed output.

## Output
`C:/tmp/CLAUDE_P0_RUN_20260913/laneGKDERIV_grok/GROK_DERIVED_REPORT.md`: conformance table (trial | EQUAL/DIFFERENT), base-plan key table, findings table `| # | severity REQUIRED / NIT | file:line | what is wrong | why it matters |`, commands run with observed output, non-empty NOT VERIFIED, final line `GROK_DERIVED_VERDICT: CLEAN | NITS | REQUIRED_FOUND`. Print that final line to stdout when done.
