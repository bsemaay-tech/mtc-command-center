# Review brief — WP-P0-20 derived measurement plan (from the V1.6 one-shot outputs) — 2026-09-15

You are a fresh, independent reviewer of the DERIVED MEASUREMENT PLAN and the DERIVATION TOOL that produced it. Your exact role (model, effort, read-only or executing) is fixed by the launcher that started you; do not delegate, resume any other session, or spawn agents. Output ONE report file named in your launch instruction; write nothing else outside your own report directory.

## 1. Context (settled; do not re-review)
- The preselection procedure frozen **V1.6** (`PRESELECTION_FROZEN.json` sha256 `cb75602080850873e28dc3aa268ad1072a0f1d50148d91ad75c9896cf37fb126`) was accepted by the full T0 roster on 2026-09-14: exact Opus round 6 PASS-WITH-NITS (0 REQUIRED, 9 NITs), exact Sol round 6 PASS-WITH-NITS (0 REQUIRED, 3 NITs), Gemini 3.7 SATISFIED, Lead REPRODUCED; the Grok pre-screen slot was vacant (route out for the week).
- The one-shot `--oneshot` run (2026-09-14 20:38Z, `authority/LEAD_TERMINAL_ELIG16.md`) returned **`PROFILE_ELIGIBLE_NOT_ACCEPTED`**: 15/15 trials trade-bearing. Recorded selection: 15m `GEN_TRIPLE_EMA_STACK`, 1h `GEN_STOCH_OVERSOLD_CROSS`, 2h `GEN_KELTNER_BREAKOUT`, 4h `GEN_MACD_BULL_CROSS`, 1D `GEN_DONCHIAN_BREAKOUT`. Outputs: `PRESELECTION_CALIBRATION.json` sha256 `808cbcb4ce529450e7e832baeffae00d00e64a57c456331936a75232c3299427`, `PRESELECTION_ELIGIBILITY.json` sha256 `fc5f8895a38147cbc76d93560b61f33cdea3b8215088acfbe6f6e8014c7bc6c2`.
- Opus round-6 findings on the derivation tool: **N6-8** (the tool did not re-derive the matching from the recorded binary matrix — a consistently rewritten output PAIR would pass) and **N6-6** (dead `base_plan` parameter). Lane P20DERIVFIX (Codex Plus gpt-5.5, 2026-09-14 20:52Z-20:56Z) fixed both: `tool/derive_benchmark_plan.py` sha256 `0bfd18915a0952cbf8bd1058b5d560a5cd30558a3f2f6d2ae6706947152289d7`, 16 tests, report `tool/REPORT_FIX2.md`. The Lead ACCEPTED the lane (`derived/LEAD_TERMINAL_DERIVED_PLAN.md` §1: byte-compared the transcribed matching function against `inputs/preselect_profile.py:412-443`; 16 passed on the pinned interpreter).
- The Lead then ran the tool ONCE on the real outputs (inputs hashed before the single read, `derived/RUN_LOG.txt`) → **`derived/BENCHMARK_PLAN_DERIVED.json` sha256 `d84b043a2b262d1c9a3c98f29f538eec169c05a0ecad0a97d801a01d9a7e2580`**, byte-identical on a second run; the real driver's `_validate_plan` accepted it (**PLAN_VALID**, `derived/VALIDATE_PLAN_HARNESS_S4.txt`, harness `derived/validate_derived_plan_harness.py`; the benchmark package was not touched).
- Owner authority for what comes next: `authority/DECISIONS_rows_MEASURE.md` row **`OD-20260914-P020-MEASURE-1`** ("1. B YES"): the bounded measurement (`run_bounded_benchmark.py --run`) may be executed by the Lead ONCE after (a) the roster accepted V1.6 [done], (b) the one-shot returned PROFILE_ELIGIBLE_NOT_ACCEPTED [done], (c) the derived plan passes `--validate-plan` [done via harness; will be repeated in place] AND the derivation-tool/derived-plan review — Grok pre-screen + exact Sol + Gemini; no further Opus. **This review is (c).** The Grok route answered `402 Grok Build usage balance exhausted` on 2026-09-15 05:25Z; that slot is vacant and is recorded by the Lead as a roster limitation for the owner — it is not yours to waive or to fill.
- The measurement output is a MEASUREMENT record only: not profitability, ranking, acceptance or production authority (`no_profitability_or_strategy_selection_results` / `not_representative_...` stay true).

## 2. Subject (packet; verify identities before reading anything else)
Packet root: `C:/LAB/Tradingview_LAB_CLEAN/_gemini_packets_20260913/P020_DERIVED_PLAN_20260915/` — every file's sha256 is in `PACKET_SHA256SUMS.txt` (Lead-computed).
- `tool/derive_benchmark_plan.py` (`0bfd1891…`), `tool/tests/test_derive_benchmark_plan.py` (`185af7b2…`), `tool/DERIVATION_RULE.md`, `tool/REPORT_FIX2.md`, `tool/REPORT_FIX1.md`, `tool/REPORT.md`, `tool/SHA256SUMS.txt`, `tool/TASK_P20DERIVFIX.md` (the correction brief).
- `derived/BENCHMARK_PLAN_DERIVED.json` (`d84b043a…`), `derived/RUN_LOG.txt`, `derived/VALIDATE_PLAN_HARNESS.txt` (session-3 inline harness output), `derived/VALIDATE_PLAN_HARNESS_S4.txt` + `derived/validate_derived_plan_harness.py` (session-4 repeat), `derived/VALIDATE_PLAN_SCRATCH.txt` (the expected refusal when the driver is copied to a scratch dir — record paths are pinned absolute), `derived/LEAD_TERMINAL_DERIVED_PLAN.md`.
- `inputs/PRESELECTION_FROZEN.json` (V1.6, `cb756020…`), `inputs/PRESELECTION_ELIGIBILITY.json` (`fc5f8895…`), `inputs/PRESELECTION_CALIBRATION.json` (`808cbcb4…`), `inputs/preselect_profile.py` (the accepted V1.6 procedure; only `lexicographic_first_matching` `:412-443` and `verified_selection` `:1102-1154` matter here), `inputs/PRESELECTION_PREREG_V1.md` (V1.6 prereg).
- `authority/BENCHMARK_PLAN.json` (the BASE plan, `c6f07afd14301e640841e7f4ec95dfcef860df3a02e3ef3768c1513c517104c7`), `authority/run_bounded_benchmark.py` (the driver, `3d4453cd23ad0f56ae0b34f94b21b7b1f3715f180091821b383369aa62fab324`; `_validate_plan` `:95-207`, `_reserve_output_root` `:586-594`, `_run` `:596-668`, `main` `:671-688`), `authority/BENCHMARK_RUNBOOK.md`, `authority/SHA256SUMS_V16.txt`, `authority/tests/test_validate_plan_family_set.py`, `authority/P020_HANDOFF_lines53-70.md` (owner-approved procedure shape), `authority/DECISIONS_rows_MEASURE.md` (owner rows MEASURE-1, RECORD-V3-1, OPUS6-1, DRIVER-FAMILIES-1), `authority/LEAD_TERMINAL_ELIG16.md`, `authority/LEAD_ADJUDICATION_R6_OPUS.md`, `authority/LEAD_ADJUDICATION_R6_SOL.md`.
Executing reviewers only: the live copies are `C:/tmp/P020_PLAN_DERIVE_20260913/` (tool), `C:/tmp/P020_PRESELECT_20260913/` (procedure + outputs), `C:/tmp/P020_LEAD_20260912/benchmark/` (driver + base plan + records), `C:/tmp/CLAUDE_P0_RUN_20260913/P020_DERIVED_PLAN_20260914/` (derived plan) — ALL READ-ONLY for you; pinned interpreter `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe` (Python 3.12.12).

## 3. What you must decide
1. **Derivation correctness (trial by trial).** The 15 trials are exactly the five recorded (family, timeframe) pairs × sizes 512/1024/2048, ordered by the FROZEN `family_order` (TRIPLE_EMA, STOCH, KELTNER, MACD, DONCHIAN → `p020-01`…`p020-15`), and for each trial `dataset_id`, `dataset_sha256`, `normalized_path`, `bar_interval_seconds`, `dataset_selection_order`, `symbol`, `input_rows`, `input_prefix_sha256` equal the frozen artifact's `datasets[tf]` / `measurement_inputs[tf].prefixes[size]`, and `parameter_record` equals the frozen family's `fixed_parameter_record` (deep-equal). State any field that differs or is not bound to the frozen bytes.
2. **Base-plan preservation.** Every top-level key of the derived plan other than `trials` and the added `derivation` is VALUE-equal to the base plan `c6f07afd…` (key by key; the rule is value-for-value, not byte-for-byte — say whether any key is missing, added or changed). In particular `execution_status` `UNEXECUTED`, `owner_authorization`, the two publication-boundary booleans, `trade_bearing_requirement` (fail-closed), `manifest`, `compatibility`, `implementation_sources`, `trial_policy`, `p020_profile` (records V3/cost/funding pins), `resource_measurement`, `repetition`, `output_contract`.
3. **Matching re-derivation (N6-8 closure).** `tool/derive_benchmark_plan.py` `lexicographic_first_matching` is a faithful transcription of `inputs/preselect_profile.py:412-443` (same search, same family order, same refusal semantics; only the exception class/code differs); `verified_selection` re-derives the selection from `eligibility_matrix_binary_only` (booleans `is True`), requires five distinct frozen families each marked eligible, and refuses `DERIVE_REFUSED_SELECTION_MISMATCH` when the recorded selection is not the lexicographically first matching; `verify_inputs` returns only the verified selection. The RED test `test_refuses_consistently_rewritten_matrix_and_selection` (`tool/tests/test_derive_benchmark_plan.py:216`) really exercises the pre-fix hole (a rewritten matrix+selection PAIR that passed every OLD check) — executing reviewers: run it, then neutralise the re-derivation (e.g. make `verified_selection` return `dict(recorded)` before comparing) in a scratch copy and show the test goes RED→GREEN→RED as expected.
4. **Input binding.** Each input is read exactly once and hashed from the same buffer that is parsed (`load_json_once`); `derivation.eligibility_sha256` / `calibration_sha256` / `frozen_sha256` / `base_plan_sha256` in the derived plan equal the digests in `derived/RUN_LOG.txt` and the packet digests; `EXPECTED_FROZEN_SHA256` is the V1.6 digest; the eligibility record's `frozen_sha256` and `calibration_sha256` cross-check the files; `status` must be `PROFILE_ELIGIBLE_NOT_ACCEPTED`; `derivation.family_order` lists the nine frozen ids in frozen order; `tool_sha256` equals the tool's own digest; `selection_rederived` is `true`; the output is created exclusively (`open(path, "xb")`), never overwritten. Executing reviewers: re-run the tool to a scratch output path and confirm sha256 `d84b043a…`.
5. **Driver acceptance and the unpinned digests (Q1).** `_validate_plan` accepts the derived plan because `derivation.family_order` carries the nine ids and the five selected families are distinct members of it (`authority/run_bounded_benchmark.py`, the D5 branch); it reads `derivation.family_order` ONLY — it does NOT verify `derivation.eligibility_sha256` / `calibration_sha256` / `frozen_sha256` against the recorded outputs, and nothing on the `--run` path reads `PRESELECTION_FROZEN.json` or the one-shot outputs. Today the binding chain is: the Lead's `RUN_LOG.txt` (input digests before the single read) + the derived plan's `derivation` block + the packet digests. **Question Q1: for a once-only, Lead-executed measurement under OD-20260914-P020-MEASURE-1, is that documentary chain sufficient, or must the driver pin the derivation digests before `--run`? Answer REQUIRED (blocks the measurement) or NIT (record and proceed), with your reasoning.** Executing reviewers: reproduce PLAN_VALID with `derived/validate_derived_plan_harness.py` (it imports the driver from its real directory and points `PLAN_PATH` at the derived file); then RED arms on scratch COPIES of the derived plan: a duplicate family, a family outside `family_order`, a wrong size (e.g. 4096), a trial with a tampered `dataset_sha256`, `execution_status` changed — each must refuse with the driver's own message. Do NOT modify the benchmark directory; do NOT run `--run`; do NOT run `--oneshot`.
6. **Plan-swap procedure (Q2).** `--run` reads `ROOT/BENCHMARK_PLAN.json` (module constant `PLAN_PATH`); there is no `--plan` argument. The Lead intends, right before the single measurement: keep the base plan as `BENCHMARK_PLAN_BASE_c6f07afd.json`, copy the derived plan over `BENCHMARK_PLAN.json`, refresh the plan line of `SHA256SUMS_V16.txt`, run `--validate-plan` in place (expect PLAN_VALID), then run `--run --output-root <new non-existing dir>` once under a launcher that records the sha256 of the plan, driver and records before and after and of every output. The FROZEN artifact's `source_pins` pin the BASE plan (`c6f07afd…`) — is any identity check on the MEASUREMENT path invalidated by the swap? (The preselection tool's `verify_frozen_identity` is on the `--oneshot` path, which is finished and must not run again — `PRESELECT_REFUSED_ALREADY_RUN`.) State whether the swap procedure is sound, what must be recorded, and whether the base plan should instead stay in place with the derived plan substituted only for the duration of the run.
7. **Rule transcription (Q3).** `derivation.rule_text` (`RULE_TEXT` in the tool) against the owner-approved shape in `authority/P020_HANDOFF_lines53-70.md` (five datasets/timeframes; three sizes 512/1024/2048; exactly 15 trials; five distinct families; the lexicographically first matching; "only if all 15 pass may the … measurement run start in a new non-existing external output root") and against `tool/DERIVATION_RULE.md`: is the derived plan the mechanical consequence of the frozen selection with NO choice left to the tool or the Lead? Name any degree of freedom (ordering, symbol derivation via `symbol_from_dataset`, trial-id format) and say whether it can influence what is measured.
8. **Boundary.** Nothing economic is read or compared by the tool (it reads the binary matrix, the selection, the frozen artifact and the base plan); the derived plan carries no result, ranking or PnL; the measurement driver's output contract (`_run`: interrupted/resumed vs reference journals, `semantic_comparison_timing_excluded`, `acceptance_verdict: NOT_EVALUATED`, `profitability_or_strategy_selection_result: NOT_PUBLISHED`) is unchanged by the derived plan.
9. **Residuals carried (state whether any touches the measurement path).** Opus N6-1/N6-7 and Sol-6-3 (documentation pins/annotations, deferred to the next re-freeze); Opus N6-3 (cost/funding records declare `end_exclusive = 2025-09-22T08:00Z`, before the 15m calibration window; inert at the pinned HEAD — an owner lever after the measurement); Opus N6-2 / Sol-6-1/6-2 (exclusive-create vs lock; abort recovery) — on the `--oneshot` path only.

## 4. Report format
Markdown with: verified identities (COMPUTED for executing reviewers; QUOTED-and-compared for read-only reviewers); a conformance table for items 1-2 (trial | frozen source | EQUAL/DIFFERENT; key | base value | EQUAL/DIFFERENT); a closure table for N6-8 and N6-6 (CLOSED / NOT CLOSED with file:line); answers to Q1, Q2, Q3 each labelled REQUIRED or NIT or SOUND; findings numbered with severity REQUIRED / NIT and exact file:line; the commands you ran with cwd and exact observed output (RED arms included; read-only reviewers: exact read coverage with ranges and continuations instead); a NOT VERIFIED section (non-empty); and a final line exactly `VERDICT: PASS` or `VERDICT: PASS-WITH-NITS` or `VERDICT: REQUEST_CHANGES` or `VERDICT: BLOCK` (BLOCK if an executing reviewer could not run the mandated commands). Never force a verdict; a REQUIRED finding means REQUEST_CHANGES. Your report alone does not authorize the measurement; the Lead assembles the roster and records the terminal.
