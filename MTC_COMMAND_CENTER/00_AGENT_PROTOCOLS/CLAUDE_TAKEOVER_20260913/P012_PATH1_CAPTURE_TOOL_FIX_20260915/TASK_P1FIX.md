# TASK P1FIX - close the Gemini detection findings F-1..F-11 and the Lead findings L-1..L-6 on the P012 Path 1 capture tool candidate e77af1c8 -> ONE local commit + DISPOSITION

You are a BUILDER (gpt-5.5 high, Codex Plus). Not a reviewer, not the Lead. Worktree `C:/tmp/P1CAP_20260914` (branch `feature/p012-path1-capture-20260914`, HEAD `e77af1c8`). Read first: `C:/tmp/CLAUDE_P0_RUN_20260913/laneP1FIX_build/GEMINI_REPORT.md` (the review; findings 1-11 with file:line), `C:/tmp/CLAUDE_P0_RUN_20260913/laneP1FIX_build/LEAD_VERIFICATION_P1CAP.md` (L-1..L-6), the three files you will change (`IBKR_PAPER_BRIDGE/tools/capture_own_account_evidence.py`, `IBKR_PAPER_BRIDGE/tools/path1_sign_ownership.html` only if F-1 needs it, `IBKR_PAPER_BRIDGE/tests/test_capture_own_account_evidence.py`), the SDK `Info`/`API` classes in `C:/tmp/P012_FUNDING_PY312_20260911/Lib/site-packages/hyperliquid/{info,api}.py` (read-only), and the intake rule `C:/tmp/CLAUDE_P0_RUN_20260913/laneP1FIX_build/REAL_OBSERVATION_INTAKE.md` line 31 (half-open interval `[start_inclusive, end_exclusive)`). Cite `absolute/path:line` for every claim; read the line before citing it.

## Fix exactly these (nothing broader; the tool stays read-only by construction — no `hyperliquid.exchange` import, no signing, no key)
- **F-1 (REQUIRED):** when `--ownership-signature` is given, `--run-id` is REQUIRED: refuse `CAPTURE_REFUSED_RUN_ID_REQUIRED` before any network object is built; never default the run_id in that case. Move the signature verification (`ownership_result`) to the START of `run_capture`, before `make_info` (closes L-4 / F-3): a MISMATCH refuses `CAPTURE_REFUSED_BAD_SIGNATURE` (or the existing code) with NO page file written; `NOT_PROVIDED` stays allowed and recorded. Tests: RED `--ownership-signature` without `--run-id` → refusal, no files; RED wrong signature (signed by another key) → refusal, no files (L-2 / F-9); GREEN correct signature + run_id → manifest `ownership_evidence` VERIFIED with the recovered address.
- **F-2 (REQUIRED):** window semantics = the intake's half-open `[start, end)`: in `paged_query` skip rows with `row_time(row) < start_ms or row_time(row) >= end_ms` for BOTH fills and funding, but REFUSE `CAPTURE_REFUSED_MALFORMED` ("row outside requested range") if the API returns a row earlier than the cursor start or later than `end_ms` by more than 0 (i.e. rows outside `[start_ms, end_ms]` inclusive are malformed; a row exactly at `end_ms` is legitimately returned by the inclusive API and is EXCLUDED by the half-open rule, not refused). Record `window.semantics: "half_open_start_inclusive_end_exclusive_ms"` and `window.start_ms`/`end_ms` in the manifest. Tests: RED a page containing a row at exactly `end_ms` → excluded from the derived view and from the identity set (state it in a comment); RED a row before `start_ms` → refusal; GREEN rows inside.
- **F-8 (NIT, do it):** a unit test that drives `CapturingInfo.post` itself with a fake `session` (object with `.post(url, json=, timeout=)` returning a response double with `.content` bytes, `.text`, `.status_code` 200 and, for the second arm, a non-2xx that makes `_handle_exception` raise): assert the capture holds the pre-parse bytes, the URL is `base_url + url_path`, and on non-2xx the bytes are captured BEFORE the exception.
- **F-6 / L-5 (NIT, do it):** in `call_info`, on exception keep the popped capture: record the error response bytes under `name=f"{kind}_{pass}_page{n}_ERROR"` with its sidecar (or return them to the caller for recording) so the error body is never dropped; refusal code stays `CAPTURE_REFUSED_QUERY_FAILED` with the HTTP status in the detail.
- **F-11 (NIT, do it):** re-query comparison on `{identity: row}` maps (content equality), not identity sets only; refusal `CAPTURE_REFUSED_REQUERY_MISMATCH` names the first differing identity. Test: RED same identities, one changed field → refusal.
- **F-4 / L-3 (NIT, do it):** `funding_identity` = `hash:time:coin` (coin from the row; refuse `CAPTURE_REFUSED_MALFORMED` if absent). Adjust the existing fixture/tests.
- **F-5 / L-5 (NIT, do it):** `write_once` uses `open(path, "xb")` (exclusive create); a pre-existing file refuses `CAPTURE_REFUSED_MALFORMED` ("output exists").
- **F-7 / L-6 (NIT, do it):** every manifest response entry records `raw_bytes_source`: `"http_response_content"` when the bytes came from `CapturingInfo.post`, `"reserialized_parsed_json_TEST_DOUBLE_ONLY"` when `consume_capture` fell back (and the real CLI path must never take the fallback — refuse `CAPTURE_REFUSED_MALFORMED` if `info` lacks `pop_capture` outside tests; simplest: `make_info` always returns `CapturingInfo`, and `consume_capture`'s fallback is used only by the test double).
- **F-9 / L-2 (NIT, do it):** multi-page SUCCESS test (two full pages + a short page, dedup at the boundary, identities complete).
- **F-10 / L-1 (NIT, do it):** Ruff clean for `F401`, `RUF100`, `F841` on both files (`C:/tmp/wp_p0_04_tooling_20260825/Scripts/ruff.exe check --select E9,F821,F811,F401,F841,RUF100`) and `ruff format` both files (say so if the format pass is unavailable in your sandbox; the Lead re-runs it).
- HTML page: change ONLY if F-1 requires the run_id to be mandatory in the page (it already reads `run_id` from the query string; keep the message byte-identical to `ownership_message`).

## Deliverables
1. Code + tests: `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe -m pytest IBKR_PAPER_BRIDGE/tests/test_capture_own_account_evidence.py -q -p no:cacheprovider --basetemp <your scratch OUTSIDE C:/tmp>` GREEN (if only Python 3.14 exists in your sandbox, say so; the Lead re-runs on 3.12); `py_compile` both Python files; Ruff as above.
2. `C:/tmp/CLAUDE_P0_RUN_20260913/laneP1FIX_build/DISPOSITION_P1FIX.md`: table F-1..F-11 / L-1..L-6 -> FIXED / NOT FIXED with file:line and test names; verbatim test output; `SHA256SUMS.txt` (LF) of the changed files.
3. ONE local commit on the branch (message `p012-path1: close Gemini F-1..F-11 and Lead L-1..L-6 on the capture tool (run-id required with signature, signature verified before network, half-open window, error bytes kept, content re-query, coin in funding identity, exclusive create, raw_bytes_source, RED arms, lint)`, trailer `Co-Authored-By: Codex gpt-5.5 <noreply@openai.com>`), exact paths only (`git -c safe.directory=* add <paths>`); if the sandbox cannot take the index lock, say so — the Lead commits. Print HEAD.

## Boundaries (binding)
- Writable: the worktree and the lane dir `C:/tmp/CLAUDE_P0_RUN_20260913/laneP1FIX_build`. Never touch `C:/LAB/Tradingview_LAB_CLEAN` itself, other worktrees, protected scopes (`02_MTC_BACKTEST`, `07_ADAPTERS`, `01_PINE`, `MTC_V2`), the broker module, `bridge/app.py`, deploy files, or `export_mtc_funding.py`.
- NO network calls, no credentials, no exchange writes, no key material anywhere; the tool must still import nothing from `hyperliquid.exchange` (the existing import test stays).
- Git only inside the worktree (`-c safe.directory=*`; `add <paths>`, `commit`, `rev-parse`, `diff`, `status`), never while `agy.exe` runs (check `tasklist 2>NUL | findstr /i agy.exe` first).
- Codex Plus only; no delegation. If the repository disagrees with this brief, the repository wins — record the discrepancy and stop on that item.
