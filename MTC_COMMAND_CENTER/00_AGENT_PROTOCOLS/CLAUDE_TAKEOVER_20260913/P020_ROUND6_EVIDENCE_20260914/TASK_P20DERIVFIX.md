# TASK P20DERIVFIX - close Opus round-6 NIT N6-8 (and N6-6) in the WP-P0-20 plan-derivation tool BEFORE any measurement plan is derived

> HOLD until the V1.6 one-shot has run (the tool consumes its outputs). Owner authority: `OD-20260914-P020-MEASURE-1` (B YES: bounded measurement after eligibility + derived-plan review) — the derived plan must be trustworthy first.

You are a BUILDER (gpt-5.5 high, Codex Plus). Not a reviewer, not the Lead. Writable: `C:/tmp/P020_PLAN_DERIVE_20260913/` and the lane dir `C:/tmp/CLAUDE_P0_RUN_20260913/laneP20DERIVFIX_build`. Read first: `C:/tmp/P020_PLAN_DERIVE_20260913/derive_benchmark_plan.py`, `DERIVATION_RULE.md`, `tests/`, and the frozen procedure's matching functions `C:/tmp/P020_PRESELECT_20260913/preselect_profile.py` `lexicographic_first_matching` (`:412`) and `verified_selection` (`:1102`) — read-only. The Opus finding (`C:/tmp/P020_PRESELECT_T0_R6_20260914/opus/OPUS_T0_REPORT.md`, N6-8): the tool checks that eligibility and calibration records agree, that the selection names five distinct frozen families and that digests chain, but never re-runs the matching over the recorded binary matrix — a consistently re-written output PAIR would pass.

## Fix exactly
1. **N6-8:** after the existing checks, re-derive the selection from the recorded calibration matrix (`eligibility_matrix_binary_only` / the calibration record's binary matrix — use the exact field names the frozen procedure writes; read them from `preselect_profile.py`, do not guess) with the SAME rule the procedure uses (`lexicographic_first_matching` over the frozen `family_order` and the five timeframes; import or transcribe it byte-for-byte and say which). Refuse with a new explicit code (e.g. `DERIVE_REFUSED_SELECTION_MISMATCH`) when the re-derived matching differs from the recorded selection, and record `derivation.selection_rederived: true` in the derived plan. RED test: a consistently rewritten output pair (matrix + selection edited together so the old checks pass) is refused; GREEN test: the fixture pair derived from the real rule passes unchanged.
2. **N6-6:** remove the dead `base_plan` parameter from `derive_trials` (or use it) — one change, test count unchanged or +1.
3. Do NOT change `EXPECTED_FROZEN_SHA256` (stays the V1.6 digest `cb75602080850873e28dc3aa268ad1072a0f1d50148d91ad75c9896cf37fb126`), the plan schema, sizes 512/1024/2048 or anything in the frozen package.
4. `REPORT_FIX2.md` in the tool dir: diff summary, verbatim `python -m pytest tests -q -p no:cacheprovider` (pinned interpreter `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe`), the RED/GREEN pair output, NOT VERIFIED (not reviewed; no derivation executed on real outputs), refreshed `SHA256SUMS.txt` (LF) and `DERIVATION_RULE.md` sentence for the new refusal.

## Boundaries (binding)
- Never run `--oneshot`, never touch `C:/tmp/P020_PRESELECT_20260913/`, `C:/tmp/P020_LEAD_20260912/`, `C:/LAB/Tradingview_LAB_CLEAN` or any worktree. No git at all. Codex Plus only; no delegation. Cite `absolute/path:line`; the repository wins over this brief.
