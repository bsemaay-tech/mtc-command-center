# TASK P20R7 - repair the WP-P0-20 preselection package after the V1.5 one-shot returned BENCHMARK_PROFILE_BLOCKED for a sizing artefact (owner decision D9 Option A) -> V1.6 + REPORT_R7

> PREPARED 2026-09-14 12:4xZ by the Lead; **RELEASED 13:02Z** — owner answered "1. D9-A" at ~12:59Z, recorded as `OD-20260914-P020-RECORD-V3-1` (CT13 DECISIONS.md). Exact Opus slot for the round-6 roster assigned by `OD-20260914-P020-OPUS6-1`.

You are a BUILDER (gpt-5.5, Codex Plus). Not a reviewer, not the Lead. Package directory (your only writable place besides the benchmark record/plan files named below): `C:/tmp/P020_PRESELECT_20260913/`. Read `PRESELECT_RUNBOOK.md`, `PRESELECTION_PREREG_V1.md`, `REPORT_R6.md`, `REPORT_FIX1.md`, `preselect_profile.py`, `tests/test_preselect.py` first.

## Why (verified facts; do not re-derive)
The V1.5 one-shot (token `b75489841a096f6f7271b12880c10cc318cfc25c8f19f7dedc8711baaefdc17b`) ran all 45 calibration cells and returned `BENCHMARK_PROFILE_BLOCKED: no complete matching of 5 distinct families to 5 timeframes` (record: `C:/tmp/CLAUDE_P0_RUN_20260913/P020_ELIGIBILITY_RUN_V15/LEAD_TERMINAL_ELIG15.md`). Owner-authorized diagnostics (D7-A, D8-A; counts only, identities withheld) showed: 8 of 9 families SIGNAL on 1h/2h/4h (dozens to 190 entries) but reach 0-1 fills; 15m has 4 fills; 1D is mostly short of signals (361-row window). Mechanism read from the pinned kernel: the frozen profile sizes by `RISK_AT_STOP` with `requested_risk_fraction = 0.0001` (owner policy `OD-20260912-P020-VALUES-1`) and floors the quantity to the instrument's `qty_step` (`mtc_v2/core/instrument.py:415-416` -> `rounding.py:139-145`, ROUND_DOWN); the synthetic instrument record V2 carries `quantity_step = 1` (one whole BTC contract at $50k-$126k), so the risk-derived quantity floors to 0 and no order is placed. The eligibility test therefore measured lot quantization, not the strategies. The venue-realistic BTC step on Hyperliquid is `quantity_step: 0.00001` (`03_PRODUCTION_CLOSURE_MATRIX.md:32`, row I-3). The owner chose D9 Option A: record V3 with `quantity_step = 0.00001`, everything else unchanged; the risk fraction policy is NOT changed.

## Deliverables
1. **Instrument record V3** — `C:/tmp/P020_LEAD_20260912/benchmark/SYNTH-P020-BOUNDED-INSTRUMENT-V3.json` (+ `.sha256` sidecar, same convention as V1/V2): byte-identical content to V2 (`1a1289641992d004977c274ef63c60e72f84319015d2a838f4574facf6db5a15`) except `record_id` = `SYNTH-P020-BOUNDED-INSTRUMENT-V3`, `quantity_step` = `0.00001` (JSON number, exactly as written here), and a new provenance entry `"quantity_step_correction": {"from": 1, "to": 0.00001, "reason": "the V1/V2 step of one whole contract floored every risk-sized order on 1h/2h/4h/1D to zero under the owner risk fraction 0.0001 (D8-A diagnostic, LEAD_TERMINAL_ELIG15.md); venue-realistic Hyperliquid BTC step per 03_PRODUCTION_CLOSURE_MATRIX.md:32 row I-3; owner ratification OD-20260914-P020-RECORD-V3-1 (chat: D9 A)", "ratified_by": "Baris Semaay", "ratified_at_utc": "2026-09-14T12:59:00Z"}`. `minimum_quantity` stays `0`, `minimum_notional` stays `0`, `price_tick`/`point_value`/`contract_multiplier` stay `1`, `effective_interval` stays as V2, `provenance_class` stays `SYNTHETIC_LABELLED`, `test_only` stays `true`. Do NOT touch V1 or V2 bytes.
2. **Plan re-pin** — `C:/tmp/P020_LEAD_20260912/benchmark/BENCHMARK_PLAN.json`: change ONLY the fields that name the V2 record (`p020_profile.records.instrument.path` / `.sha256` and any other field naming V2) to the V3 file and digest; run the driver's `--validate-plan` (identities only, no simulation) and paste the output. Update `run_bounded_benchmark.py` `RECORD_PATHS["instrument"]` to V3 — that file is pinned in `implementation_sources` and in the frozen `source_pins`, so its new digest flows into the freeze (expected; report both digests).
3. **Sizing-floor regression pair (synthetic, BTC price scale, no real data):** add `tests/test_sizing_floor.py` (or extend the existing end-to-end smoke test) that builds, for EACH of the five timeframes, a synthetic frame at BTC price scale ($60k-$120k) dated inside the record interval with the timeframe's typical stop distance under the frozen profile, runs it through the REAL driver/engine/gate (`trade_bearing_gate` path, real V3 record bytes), and asserts trade-bearing (GREEN); the SAME frames pinned to the V2 record must produce the blocked economic-path outcome (RED arm; assert the exact clause "no actual trade-bearing 2.1.0 successor result" or the BenchmarkProfileBlocked kind the code raises — read the code, do not guess). Both arms run in the package test suite. No `--oneshot`, no real dataset bytes in this test.
4. **Runbook + preregistration text**: `PRESELECT_RUNBOOK.md` — V1.5 token is dead (say so); the instrument record is V3; the shot count (V1.4 aborted on the record interval; V1.5 BLOCKED on the sizing artefact; the owner reset both by ruling). `PRESELECTION_PREREG_V1.md` §(d) (or wherever the instrument record is named) — pin V3 with its digest and one sentence on why (quantization artefact, D9 A); the selection rules in §(e)/(f)/(g), the family order, the windows and the prefixes are UNCHANGED — do not edit those sections.
5. **Re-freeze V1.6** twice (byte-identical), with the command-local `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0=C:/P020_IMPL_20260912` environment if `git rev-parse` reports "dubious ownership" (write no git config). Prove by diffing the frozen JSON against V1.5 `b7548984…` that ONLY these may differ: `artifact_version`, `procedure_code_sha256`/`prereg_sha256`/`runbook_sha256`, the instrument record pin/digest and `instrument_record_interval` (unchanged values expected), the `run_bounded_benchmark.py` pin, the `BENCHMARK_PLAN.json` pin, and any test-file pin. Calibration windows, measurement prefixes, canonical bytes, family order and selection rules must be byte-identical.
6. **Plan-derivation tool** (`C:/tmp/P020_PLAN_DERIVE_20260913/`): re-pin `EXPECTED_FROZEN_SHA256` to the V1.6 digest (run step 5 first), refresh `DERIVATION_RULE.md` citations to V1.6 line numbers, re-run its 15 tests and paste the output. No other change.
7. `REPORT_R7.md` (diff summary per item, tests output verbatim incl. the RED/GREEN sizing pair per timeframe, re-freeze output, digests table V2->V3 and V1.5->V1.6, NOT VERIFIED non-empty: not reviewed, not executed, the real 45-cell matrix and the 15 trials still unproven, the 1D window may still be short of signals for reasons unrelated to sizing) and `SHA256SUMS.txt` (LF) last.

## Boundaries (binding)
- Writable: the package dir; `C:/tmp/P020_LEAD_20260912/benchmark/` (record V3 + sidecar, `BENCHMARK_PLAN.json` pins, `run_bounded_benchmark.py` RECORD_PATHS only, `SHA256SUMS_V16.txt`, `BENCHMARK_RUNBOOK.md` one sentence, tests); `C:/tmp/P020_PLAN_DERIVE_20260913/`. Nothing else. Never delete anything; never touch V1/V2 record bytes; never change `requested_risk_fraction` or any owner-policy value.
- Never run `--oneshot` with any token; never run `run_bounded_benchmark.py --run`; no simulation on real dataset bytes (the regression pair uses synthetic frames only).
- No git command except `git -c safe.directory=* -C C:/P020_IMPL_20260912 rev-parse HEAD` (must be `b9b72f858dc830a9389517f79da5ea3c1fa6122c`). A review helper watches the shared repository store; check `tasklist` for `agy.exe` before that one command.
- Recorded owner routing: Codex Plus; do not delegate, spawn agents, or call other providers. Cite `absolute/path:line` for every claim; if the repository disagrees with this brief, the repository wins — record the discrepancy and stop on that item.
