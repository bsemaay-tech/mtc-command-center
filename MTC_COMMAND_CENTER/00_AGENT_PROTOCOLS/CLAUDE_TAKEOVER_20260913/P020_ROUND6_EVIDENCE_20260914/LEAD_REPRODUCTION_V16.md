# LEAD_REPRODUCTION_V16 - WP-P0-20 preselection procedure V1.6 (independent Lead reproduction, T0 round 6 roster member)

Performed by Claude Opus 5 Lead (session b9df29), 2026-09-14 16:13-16:16Z, in a fresh scratch copy `C:/tmp/P020_PRESELECT_LEAD_REPRO_V16_20260914/` (copied from the package after lane P20R7, Codex Plus gpt-5.5, 15:08-15:22Z, `REPORT_R7.md`). Pinned interpreter `C:/tmp/P020_IMPL_20260912/01a0924d-2c4b-7da1-99e1-24e2a7c7685c/.venv/Scripts/python.exe`. No Git command other than `git rev-parse HEAD` (command-local `safe.directory`). Raw output: `LEAD_REPRO_V16_RAW_OUTPUT.txt` (this directory).

| Step | Command | Observed | Status |
|---|---|---|---|
| 0 | `git -C C:/P020_IMPL_20260912 rev-parse HEAD` | `b9b72f858dc830a9389517f79da5ea3c1fa6122c` | EQUAL to pin |
| S | `SHA256SUMS.txt` (13 entries) against the repro copy | 13/13 OK | EQUAL |
| 1 | `python -m pytest tests -q -p no:cacheprovider` | `106 passed in 4.05s` (96 in V1.5 + 10 in `tests/test_sizing_floor.py`) | EQUAL to REPORT_R7 |
| 2 | `--freeze` twice in the scratch copy (command-local `GIT_CONFIG_*` safe.directory) | `cb75602080850873e28dc3aa268ad1072a0f1d50148d91ad75c9896cf37fb126` both runs = builder digest = package file; LIMITATION line `SHORT_CALIBRATION_WINDOW_1D … accepted by owner decision D1 Option A` still printed | deterministic, EQUAL |
| 3 | JSON diff record V3 vs V2 | top-level diffs exactly `record_id`, `quantity_step` (`1` → `1e-05`), `provenance` (only the added `quantity_step_correction`); `minimum_quantity` 0, `minimum_notional` 0, `effective_interval` 2019-09-08 → 2026-05-01 unchanged; V3 sha256 `69b6f246ad721ccfde23f87c0b21b6470b8fb75b88552bfad004ef8c69d557f9` = sidecar; V2 bytes unchanged (`1a128964…`) | D9-A implemented exactly |
| 4 | frozen key diff V1.5 (repro copy, `b7548984…`) → V1.6 | differing keys: `artifact_version`, `supersedes`, `instrument_record_interval` (digest), `prereg_sha256`, `procedure_code_sha256`, `runbook_sha256`, `source_pins` (only `BENCHMARK_PLAN.json`, `run_bounded_benchmark.py`); **`family_order`, `selection_rule`, `calibration_windows`, `measurement_inputs` EQUAL** | no selection-rule change |
| 6 | benchmark `pytest tests` in `C:/tmp/P020_LEAD_20260912/benchmark`; `run_bounded_benchmark.py --validate-plan` | `5 passed`; `PLAN_VALID: frozen manifest, selections, hashes, and 15-trial shape verified`; plan instrument path = `SYNTH-P020-BOUNDED-INSTRUMENT-V3.json` | EQUAL |
| 7 | derive tool `pytest tests -q -p no:cacheprovider` in `C:/tmp/P020_PLAN_DERIVE_20260913`; `EXPECTED_FROZEN_SHA256` | `15 passed`; pin = `cb756020…` = V1.6 (a bare `pytest -q` without the `tests` path hit a PermissionError on a sandbox-owned `tmp/pytest-of-*` directory left by the Codex lane — environment residue, not a package defect) | EQUAL |
| P | **Per-timeframe REAL-cell smoke** (disclosed precondition arm; `lead_smoke_v16_cells.py`; real `verify_frozen_identity` preflight → 19 pins OK, instrument pin V3; real driver/engine; `family_order[0]` = `GEN_TRIPLE_EMA_STACK` through `trade_bearing_gate` on each calibration window; no token, no outputs, no matching) | 15m TRADE-BEARING (0.03 s), 1h TRADE-BEARING, 2h TRADE-BEARING, 4h TRADE-BEARING, **1D BLOCKED** `no actual trade-bearing 2.1.0 successor result` | the sizing artefact is gone on real 1h/2h/4h data (0/9 families under V2 per D7-A); 1D stays the D1 weak spot for this family |

Disclosure: step P reveals the eligibility of ONE family (the first in the frozen order) on the five windows — five of the 45 cells. It was run as the precondition arm the V1.4/V1.5 lessons demand (a roster that never runs the real stack cannot certify executability), not as selection; no other cell, no matching and no trial was computed; nothing was written.

NOT VERIFIED by this reproduction: the full 45-cell matrix and the 15 trials (the one-shot, still unrun); whether 1D has ANY trade-bearing family under V3 (D8-A counted three 1D families with ≥1 signal under V2 and zero fills; V3 may or may not lift them) — if none, the one-shot returns `BENCHMARK_PROFILE_BLOCKED` again and the owner's D7 options B/C/D (drop 1D / longer 1D data / stop) return; reviewer verdicts for round 6 (Gemini 3.7 V16, Sol R6 on Codex Plus, exact Opus R6 on Claude Pro under `OD-20260914-P020-OPUS6-1`; Grok out).

Lead reproduction result: **REPRODUCED**. One roster member only; authorizes no execution. The V1.6 token `cb75602080850873e28dc3aa268ad1072a0f1d50148d91ad75c9896cf37fb126` stays unused until the roster accepts.
