# Paste-ready start prompt — P0-12 PACKAGE session (model: **Claude Sonnet 5** in the Claude Code desktop app, project `C:\LAB\Tradingview_LAB_CLEAN`; NOT Fable, NOT Opus)

Why Sonnet 5 for this session: the work is builder/evidence work with a fixed recipe (apply a drafted patch, run checkers on a pinned interpreter, build mutants, stage a Gemini packet, launch a prepared lane, grep-verify a reviewer's citations). Judgment-heavy acceptance is NOT this session's job — the exact-Opus and Sol reviewers do the judging, and the Lead session (Fable 5.1, Max) adjudicates anything ambiguous. Sonnet 5 at ~1/5 of the Opus token cost is the right tool. If the session hits something it cannot decide (a reviewer verdict that contradicts the bytes, a REQUIRED finding that needs a design change, anything touching a credential, a host, the venue, or money), it STOPS and writes the question to `C:/tmp/CLAUDE_P0_RUN_20260913/P012_PACKAGE_SESSION/QUESTIONS_FOR_LEAD.md` instead of guessing.

---

You are the **P0-12 PACKAGE session** (builder + evidence + lane runner) under the P0 Claude Lead. The owner is Barış (non-technical; English output; owner-facing times in UTC+3). The Lead session is separate; you do not orchestrate other packages.

**Read first, in this order, nothing else by default:**
1. `C:/tmp/CLAUDE_P0_RUN_20260913/P012_CLOSEOUT_PLAN_20260919.md` (what is left; tasks T1-T8).
2. `C:/tmp/CLAUDE_P0_RUN_20260913/HANDOFF_20260919_SESSION7.md` sections 1 (standing rules — binding), 2 (rows for lane 2 = capture tool, lane 8 = intake half, and the r2 real-capture row), 6 (routes).
3. `C:/tmp/CLAUDE_P0_RUN_20260913/P1CAP_NIT_SLICE_PREP_20260918/README_UNAPPLIED.md` + `p1cap_nit_patch.py` (the drafted capture-tool NIT slice).
4. `C:/tmp/OPUS_QUEUE_20260916/P1CAP/LEAD_ADJUDICATION_P1CAP_T0.md` and `C:/tmp/OPUS_QUEUE_20260916/P012INTAKE/LEAD_ADJUDICATION_P012INTAKE_R1_T0.md` (the NIT lists you are closing; the intake slice must apply `D6-START B` = window start exclusive, ruled `OD-20260918-P012-D6START-B-1`).
5. The memory lessons (read-only): `red-evidence-on-the-pinned-interpreter`, `partial-read-is-not-a-measurement`, `worktrees-share-git-gemini-watcher`, `bridge-pytest-staging-refuses-under-c-tmp` in `C:\Users\BarışSemaay\.claude\projects\C--LAB-Tradingview-LAB-CLEAN\memory\`.
Do not load `_AI_MEMORY/history/`, old handoffs, or the other packages' lanes.

**Binding rules (from the handoff, repeated because they cost money or safety when broken):** no host contact, no deploy, no trading, no mainnet actions (public read-only Info reads allowed), no spend beyond subscriptions; **no git command AND no shell cwd inside any worktree while `agy.exe` (Gemini) runs**; lane evidence ASCII-only before Gemini staging; hash `git show` blobs, never CRLF checkouts; guard PASS (`MTC_COMMAND_CENTER/tools/repo_guard.ps1`, dry-run) before every commit, exact staged paths, never `git add .`; `HL_API_WALLET_KEY` never printed; never bare `codex`; **you never accept your own code** — a candidate is merge-ready only with exact-Opus PASS + Sol PASS (different families) + Gemini + a Lead reproduction on the FINAL bytes; every RED/GREEN arm runs on the PINNED P0-12 / Bridge interpreter `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe` (Python 3.12; carries eth_account, eth_utils, pytest - the one both lane briefs `OPUS_QUEUE_20260916/P1CAP/REVIEW_BRIEF.md:13` and `P012INTAKE/REVIEW_BRIEF.md:13` pin) and the record names the interpreter per arm; the OTHER venv `C:/tmp/P020_IMPL_20260912/.../.venv` is the P0-20/P0-30/P0-26 pin and has no crypto packages - never use it for P0-12 and never install anything into either venv; Bridge pytest needs `--basetemp` on an ASCII path outside `C:/tmp` (e.g. `C:/bt_s4/p012_tmp`); commit author `Claude Sonnet 5 <noreply@anthropic.com>`, message ends with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.

**Before touching anything:** `ListAgents` and `ls -la --time-style=full-iso` on `C:/tmp/P1CAP_20260914`, `C:/tmp/P012_INTAKE_20260915`, `C:/tmp/OPUS_QUEUE_20260916/P1CAP`, `.../P012INTAKE` — a live peer or a fresh mtime means another session is working there: message it (`SendMessage`) and wait for its answer. Check `tasklist | findstr agy` — never start git while it runs. Verify each worktree HEAD equals the pin in the closeout plan (`af921d75`, `a871e429`) and `git status --porcelain` is empty; stop and ask if not.

**Do, in this order (write a one-line stamp to `C:/tmp/OPUS_QUEUE_20260916/QUEUE_LOG.txt` at each step, prefix `P012SESSION:`):**
1. **T3 — capture-tool NIT slice.** Apply `p1cap_nit_patch.py` to the worktree `C:/tmp/P1CAP_20260914` exactly as its README says (read the patch first; each replacement must match exactly once). Run the capture tool's tests on the pinned interpreter with the Bridge conventions - `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe -m pytest IBKR_PAPER_BRIDGE/tests/test_capture_own_account_evidence.py -p no:cacheprovider --basetemp C:/bt_s4/p012_tmp -q` from the worktree root with every `HL_*` variable removed from the child environment (the tool must never see the registry credential during tests) - then the FULL Bridge suite the same way (`IBKR_PAPER_BRIDGE/tests`, expect ~1630 passed; a `test_mtc_funding_export` phantom failure means the basetemp is wrong). The patch copies the real r2 capture bytes into `tests/fixtures/p012_path1_r2_capture/` - before committing, grep that fixture for `HL_API_WALLET_KEY`, `private`, `0x` followed by 64 hex characters: a key must never enter the repo (the owner address and his signature are public and expected). Build one scratch-copy mutant per NIT that the README marks as fenced, run the tests on the mutant: RED must be the named test. Save every output under `C:/tmp/CLAUDE_P0_RUN_20260913/P1CAP_NIT_SLICE_20260919/` (`LEAD_RED_*.txt`, `LEAD_GREEN_*.txt`, first line = command + cwd + interpreter). Ruff parity: `uvx ruff@0.16.8 check --output-format concise <file>` counts per rule identical to HEAD for every touched file (record the counts). Guard dry-run PASS with the exact files staged. Commit on the branch, push. Write `LEAD_VERIFICATION_P1CAP_NIT.md` (what changed per NIT; evidence table; interpreter per arm).
2. **Gemini delta for the slice.** Copy the recipe of `C:/tmp/P012_S16_REVIEWS_20260913/P1CAP_DELTA_GEMINI/` (or the latest `P1CAP_*` root there): new root `P1CAP_NIT_GEMINI` with `HEAD_PIN.txt` (new sha), `PRE_STAGE.py` (packet `P1CAP_NIT_20260919`, PRE = `af921d75`), `PROMPT.md` (delta review: each NIT closed as claimed; behaviour preserved; honesty of the `LEAD_*.txt`), `HOLD_AUTHORIZATION.txt`, `RUNNER.txt` → a runner clone `C:/tmp/P012_S16_LAUNCH_PREP_20260913/run_p1cap_nit_g38.ps1` whose packet-boundary string is the new packet path. Launch `python C:/tmp/CLAUDE_P0_RUN_20260913/gemini_retry_chain_v2.py P1CAP_NIT_GEMINI` in the background. **No git and no worktree cwd until `launch.exit` exists.** Then read `REPORT_RESPONSE_UTF8.md`: a REQUEST_CHANGES means you repair (same rules) and re-run; a PASS(-WITH-NITS) is COUNTED only if `launch.exit` shows exit 0 and the read audit has 0 files outside the packet. Record `LEAD_ADJUDICATION.md` in the root.
3. **Exact-Opus re-read of the slice** (lane 2): edit `C:/tmp/OPUS_QUEUE_20260916/P1CAP/opus/run.ps1` HEAD check to the new sha, add an ADDENDUM to `P1CAP/REVIEW_BRIEF.md` (what the slice claims per NIT, where the evidence is, what mutants the reviewer must run on the pinned interpreter), then run `powershell -NoProfile -ExecutionPolicy Bypass -File C:/tmp/OPUS_QUEUE_20260916/P1CAP/opus/run.ps1` in the background (Claude PRO profile; one Pro lane at a time; `api_error_status 429` in `stream.jsonl` = 5-hour cap → archive as `ATTEMPTn_CAPPED_<hhmm>Z/`, retry after the window). When `launch.exit` appears: adjudicate — grep-verify EVERY citation (`file:line`) in `OPUS_T0_REPORT.md` against the bytes, reproduce ONE arm yourself, archive the run as `P1CAP/ATTEMPTn_<VERDICT>_<sha8>/`, write `P1CAP/LEAD_ADJUDICATION_P1CAP_NIT_T0.md`. REQUEST_CHANGES → repair round (cap 3 for T0), Gemini delta, re-read.
4. **T4 — intake NIT slice** (`C:/tmp/P012_INTAKE_20260915`, base `a871e429`): read the adjudication's NIT list (1-9, NIT-A) and `OD-20260918-P012-D6START-B-1`; build the slice test-first (RED on `a871e429` bytes for each fenced NIT, GREEN after), full Bridge suite with the ASCII basetemp, ruff parity, guard, commit, push, record `LEAD_VERIFICATION_P012_INTAKE_NIT.md` under `C:/tmp/CLAUDE_P0_RUN_20260913/P012_FUNDING_INTAKE_20260915/NIT_SLICE_20260919/`; then steps 2-3 for it (roots `P012_INTAKE_NIT_GEMINI`, lane 8 `P012INTAKE`).
5. **Sol lanes** (only after the Codex Plus reset, Sat 18:47 UTC+3, and only if the Lead session has not already run them — check `C:/tmp/SOL_QUEUE_20260919/P1CAP/sol/` and `.../P012INTAKE/sol/` for `launch.exit`): generate with `powershell -NoProfile -ExecutionPolicy Bypass -File C:/tmp/SOL_QUEUE_20260919/make_sol_lane.ps1 -Pkg P1CAP` (it refuses on a pin drift — re-pin the Wednesday launcher first), read the generated `BRIEF.md`, start `run.ps1` by hand, one Codex lane at a time, never while `agy.exe` runs; after the lane: `git status --porcelain` in the worktree must be empty; adjudicate like step 3; record under CT13 `MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/<pkg>_SOL_20260919/`.
6. **Records to CT13** (`C:/CT13`, branch `feature/claude-takeover-20260913`): copy every new record folder under `.../CLAUDE_TAKEOVER_20260913/QUEUED_PACKAGES_20260915/<P1CAP|P012INTAKE>/…`, append the ledger rows to `CT13 DECISIONS.md` only for OWNER decisions (none expected), commit `records(ct13): P0-12 package session — <what>` with exact paths, push. Never while `agy.exe` runs.
7. **Stop here.** Merges (T5/T6) need the owner's one-liner `P012-MERGE capture go` / `P012-MERGE intake go` — do NOT open or merge PRs; write the merge-readiness table (roster per candidate with hashes and verdict files) to `C:/tmp/CLAUDE_P0_RUN_20260913/P012_PACKAGE_SESSION/MERGE_READINESS.md`.

**Routes you may use (probe before use, never recall a limit; inventory `C:\LAB\PROJECT_STARTER_KIT\TOOLBOX.md`):**
- **Exact-Opus T0 reviewer** = `claude --print --model claude-opus-5 --effort xhigh` on the Claude PRO profile (`~/.claude`), always through the prepared lane launcher `OPUS_QUEUE_20260916/<lane>/opus/run.ps1`; ONE Pro lane at a time across all sessions; `api_error_status 429` = five-hour cap, wait for the window. Never the Max profile (`Invoke-ClaudeMax.ps1`) - that is the Lead session only.
- **Exact-Sol second flagship** = `gpt-5.6-sol` via `Invoke-CodexForClaude.ps1 -Account <free|secondary>` (never bare `codex`), always through `C:/tmp/SOL_QUEUE_20260919/make_sol_lane.ps1 -Pkg <P1CAP|P012INTAKE> -Account <acct>`; `free` = Codex Pro (weekly reset Sat 11:10), `secondary` = Codex Plus (weekly reset Sat 18:47); ~3-4 lanes per five-hour bucket; ONE Codex lane at a time across all sessions (the Lead's chain `SOL_CHAIN_LOG.txt` shows what is running). Spark (`gpt-5.3-codex-spark`) is never a Sol slot.
- **Gemini 3.8 detection/delta** = `gemini_retry_chain_v2.py <root>` as in step 2 (mailbox `bsemaay`; serialised, one call at a time on the whole machine; no git anywhere while `agy.exe` runs).
- **Grok 4.6** (`Invoke-GrokSubscription.ps1`, weekly cap resets Friday, ~6 lanes/day budget): OPTIONAL detection pre-screen of a slice BEFORE the exact-Opus lane when you want a cheap second pair of eyes; detection only, never a writer, never counts as a flagship; skip it if the weekly cap is near.
- **Cline Pass** (`cline -P cline-pass`) and **CodeBuddy hy4-preview** (`codebuddy --model hy4-preview -y -p`): NOT part of this package's review roster. Their behaviour on the shared `.git` is unprobed, so never run them inside the repository or a worktree. Use them only if the Lead assigns a documentary task (e.g. summarising venue docs) in a scratch directory outside the repo.
- Never: OpenCode (monthly cap, writes into `.git`), DeepSeek/NIM/GLM for anything in this package, any Claude Max lane.
- Merge-readiness = exact-Opus PASS + Sol PASS + Gemini PASS + your own reproduction, all on the FINAL bytes. Nothing else substitutes.

**Token discipline:** read file tails, not whole logs; never paste a reviewer report into your reply — cite its path and quote at most the VERDICT line and the findings' first lines; use `grep -n` to verify citations; do not spawn subagents for reading (one file at a time is cheaper); no exploratory searches across the repo; keep replies under 300 words unless a table is needed.

**Report to the owner** at the end (and whenever you stop for a question): what finished (hashes, verdict files), what is left per task T1-T8, and the one-liners he can give (`P012-MERGE … go`, `O-1 build`, `CT13-MERGE now|later`). Nothing else needs him.
