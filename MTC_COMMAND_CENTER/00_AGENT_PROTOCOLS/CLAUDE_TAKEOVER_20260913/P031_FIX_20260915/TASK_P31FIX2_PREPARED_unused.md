# TASK P31FIX2 - add the permanent registrar-path RED test for CATALOG_BACKED_WITHOUT_EVALUATION_HASH (Gemini delta K-D-01 = Lead P31FIX-L-1) -> ONE local commit + DISPOSITION

You are a BUILDER (gpt-5.5 high, Codex Plus). Not a reviewer, not the Lead. Worktree `C:/tmp/P031_M1_20260913` (branch `feature/p031-m1-20260913-refresh`, HEAD `bd0d56d07eebf22ebbc1cdc96a27ddabeacd7acc`). Read first: `C:/tmp/CLAUDE_P0_RUN_20260913/laneP31FIX2_build/LEAD_registrar_fail_open_probe.py` (the Lead's probe that proved the edge: pre-fix ledger APPENDED, current ledger REFUSES), `C:/tmp/CLAUDE_P0_RUN_20260913/laneP31FIX2_build/LEAD_REGISTRAR_FAIL_OPEN_PROBE.txt`, and `MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p031_lifecycle_ledger.py` around `test_configured_catalog_refuses_absent_hash_and_accepts_present_hash` (`:2872-2944`) and `test_deployment_refresh_returns_to_frozen_under_new_composite` (`:2517-2585`). Cite `absolute/path:line` for every claim.

## Do exactly this
- Add ONE test method to `LifecycleLedgerTests`, e.g. `test_registrar_refresh_with_catalog_backed_claim_requires_evaluation_hash`: a ledger with `accepted_evaluation_catalog=(EVALUATION_A,)`; `append_frozen_fixture` + an ENVIRONMENT_ADMISSION_AUTHORITY `SHADOW_ELIGIBLE` event with `evaluation_run_hash=EVALUATION_A`, `catalog_backed=True` (state SHADOW); then `Registrar(ledger, "registrar-1").append(<deployment refresh FROZEN event, previous_state SHADOW, next_state FROZEN, new deployment identity>, catalog_backed=True)` must raise `ValueError` matching `CATALOG_BACKED_WITHOUT_EVALUATION_HASH`; then the same refresh event WITHOUT `catalog_backed` (or `catalog_backed=False`) must succeed and leave the candidate FROZEN with the new deployment identity (GREEN arm in the same test or a sibling). Mirror the Lead probe exactly; no new fixtures beyond what the file already has.
- Prove RED on the pre-fix code: copy the worktree's ledger at `96af3eb6` (`git -c safe.directory=* show 96af3eb6:MTC_COMMAND_CENTER/03_QUANTLENS/tools/p031_lifecycle_ledger.py`) into a scratch tree beside a copy of the NEW test file and the contracts package (the Lead's scratch layout `C:/tmp/P31FIX_RED_SCRATCH_20260915` shows the shape), run the single new test there: it must FAIL (the pre-fix ledger appends). Paste the output. Then run it in the worktree: PASS.
- No change to `p031_lifecycle_ledger.py`. No other test changes.

## Deliverables
1. `python -m pytest MTC_COMMAND_CENTER/03_QUANTLENS/tools/tests/test_p031_lifecycle_ledger.py -q -p no:cacheprovider` GREEN in the worktree (expect 109 passed / 1 skipped; state your interpreter — if only 3.14 exists, say so, the Lead re-runs on 3.12); `py_compile` the test file.
2. `C:/tmp/CLAUDE_P0_RUN_20260913/laneP31FIX2_build/DISPOSITION_P31FIX2.md`: K-D-01 -> FIXED with file:line and the test name; verbatim RED (pre-fix scratch) and GREEN (worktree) outputs; `SHA256SUMS.txt` (LF) of the test file.
3. ONE local commit on the branch (message `p031: add registrar-path RED test for CATALOG_BACKED_WITHOUT_EVALUATION_HASH (Gemini delta K-D-01)`, trailer `Co-Authored-By: Codex gpt-5.5 <noreply@openai.com>`) with `git -c safe.directory=* add <the one path>`; if the sandbox cannot take the index lock, say so — the Lead commits. Print HEAD.

## Boundaries (binding)
- Writable: the worktree and the lane dir `C:/tmp/CLAUDE_P0_RUN_20260913/laneP31FIX2_build` (+ your scratch under `C:/tmp/P31FIX2_SCRATCH`). Never touch `C:/LAB/Tradingview_LAB_CLEAN` itself, other worktrees, protected scopes, `DECISIONS.md`, the ledger module, or the brief.
- Git only inside the worktree, only `add <path>` / `commit` / `rev-parse` / `show` / `diff` / `status`, always with `-c safe.directory=*`, never while `agy.exe` runs (check `tasklist 2>NUL | findstr /i agy.exe` first).
- Codex Plus only; no delegation. If the repository disagrees with this brief, the repository wins — record the discrepancy and stop.
