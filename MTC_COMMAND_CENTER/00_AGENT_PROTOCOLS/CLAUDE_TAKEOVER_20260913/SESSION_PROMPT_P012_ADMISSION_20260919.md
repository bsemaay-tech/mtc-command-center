# Paste-ready start prompt — P0-12 PRODUCTION-ADMISSION session (model **Claude Opus 5**; project `C:\LAB\Tradingview_LAB_CLEAN`). Goal: take WP-P0-12 from "research/engineering complete" to PRODUCTION ADMISSION, with the owner's answers listed in section 0 already given.

You are the **P0-12 ADMISSION session**. Owner = Barış (non-technical; English; times UTC+3; he opens/closes positions and signs; you never trade, never touch a host, never read a credential). The Lead session (Fable 5.1) coordinates, adjudicates contradictions, merges. A sibling "P0-12 POLISH session" (Sonnet 5) may run the optional remark slices in parallel on other branches - never touch its worktrees.

## 0. Owner answers this session runs on (filled by the Lead 2026-09-19 15:4x from the owner's answers)
- `O-1 build`: **YES** (`OD-20260919-P012-O1-BUILD-1`) - build the oracle-capture tool + automatic D-4 fill; O-2 tolerance 0.05 %.
- Owner item F (signed ownership-message format): **keep** (`OD-20260919-P012-ITEM-F-KEEP-1`).
- Fee tier C-10: the owner placed SEVEN screenshots of his Hyperliquid account pages in `C:/tmp/CLAUDE_P0_RUN_20260913/OWNER INPUTS/` (15:32-15:34 UTC+3, `Ekran görüntüsü 2026-09-19 1532xx..1534xx.png`). Read them (image files), transcribe maker %, taker %, tier, 14-day volume and any other admission-relevant numbers into `P012_ADMISSION_20260919/OWNER_INPUTS_TRANSCRIPT.md` with the sha256 of each image; treat the transcription as owner-supplied evidence (Q3 `Wait` is thereby superseded ONLY if the screenshots show the fee tier; otherwise C-10 stays open and you say so).
- Instrument record I-4 human review: the owner reviews from a ONE-PAGE plain-language summary you write first (`P012_ADMISSION_20260919/I4_INSTRUMENT_SUMMARY_FOR_OWNER.md`: what the record says, each field's source, what could be wrong); the Lead relays it; his words `I-4 reviewed` close it → `human_reviewer` = owner with the date (`OD-20260919-P012-I4-METHOD-1`).
- r3 trade window: the owner OPENS the position NOW - Sat 2026-09-19 ~15:4x UTC+3, small BTC perp long (r2 shape: 0.0006 BTC, isolated 1x) on his own mainnet account - and holds it across the 16:00 and 17:00 UTC+3 funding hours, closing after 17:05; the Lead runs the public oracle bracket reader during the window (`P012_ORACLE_CAPTURE_20260919/`); the owner signs the ownership message afterwards (`OD-20260919-P012-R3-TRADE-1`). Exact open/close timestamps come from the venue fills, not from chat.
- KVM2 gate steps: **later** (`OD-20260919-P012-KVM2-LATER-1`) - admission is evidenced from Path 1 own-account captures; do not prepare KVM2 packets.
- Release word `P012-ADMIT go`: given at the END, when the Lead presents the packet; you never merge.
- `MAX-LANES`: EXTENDED for the whole admission push (`OD-20260919-MAX-LANES-EXTENDED-1`) - exact-Opus lanes may use the MAX profile via `make_max_launcher.py`, one lane per profile at a time, stamped `[MAX]`. The owner's target is to finish TODAY: work the phases back to back, run reviews in parallel across the Pro, MAX and Codex (free / secondary from 18:47) slots, and report progress to the Lead after each phase.

## 1. Read first, in this order, nothing else by default
1. `C:/tmp/CLAUDE_P0_RUN_20260913/SESSION_PROMPT_P012_PACKAGE_20260919.md` — ONLY "Binding rules", "Before touching anything", "Routes you may use", "Token discipline" (they bind you; the P0-12/Bridge interpreter is `C:/tmp/P012_FUNDING_PY312_20260911/Scripts/python.exe`).
2. `C:/tmp/CLAUDE_P0_RUN_20260913/P012_CLOSEOUT_RECORD_20260919.md` (what is already merged: capture tool `9f1aa532`, intake `8cbf4f1a`; the fence `ACCEPTED_EVIDENCE_KINDS == (SYNTHETIC_FIXTURE,)` still refuses every real capture).
3. CT13 `C:/CT13/MTC_COMMAND_CENTER/00_AGENT_PROTOCOLS/CLAUDE_TAKEOVER_20260913/P012_ACCEPTANCE_AMENDMENT_20260913.md` — the Class B table (rows OPEN-01..OPEN-10, I-4, C-10/C-12, F-13/F-15/F-23, SPECIALIST, MERGE-AUTH, OBLG-1..5) = the exact list production admission must close; and `C:/CT13/DECISIONS.md` rows `OD-20260914-P012-ADMISSION-Q1..Q7`, `OD-20260914-P012-PATH1-GO-1`, `OD-20260917-P012-ORACLE-O1-O3-1`, `OD-20260918-P012-D6START-B-1`, `OD-20260919-P012-*` (the owner's standing answers: he is the human reviewer; testnet counts for nothing; only venue-reported fees on authenticated fills are admitted; funding interval forward-only from 2026-09-12T11:00Z; gate order exactly; estimates never accepted).
4. `C:/tmp/CLAUDE_P0_RUN_20260913/P012_RISK_PACKET_OWNER_PRESENTATION_DRAFT.md`, `P012_ORACLE_CAPTURE_DESIGN_NOTE_20260916.md` (O-1..O-3 design), `P012_PATH1_REAL_CAPTURE_20260917/r2/` (the first real candidate; hand-filled D-4), `P012_ORACLE_CAPTURE_20260917/reads/` (42 public oracle reads, manifest), `P012_FUNDING_INTAKE_20260915/` (D-1..D-6 vocabulary).
5. The production closure matrix and record files the amendment cites (`03_PRODUCTION_CLOSURE_MATRIX.md`, `HYPERLIQUID-BTC-PERP-V1.4.json`, `FUNDING-RULES-V1.json`, `open_item_applicability.json`) — open each only when you work its row.

## 2. Work, in phases (stamp `P012ADMIT:` lines in `C:/tmp/OPUS_QUEUE_20260916/QUEUE_LOG.txt`; records under `C:/tmp/CLAUDE_P0_RUN_20260913/P012_ADMISSION_20260919/`)
**Phase 1 — O-1: oracle capture tool + automatic D-4.** New worktree from `origin/master` (`git worktree add -b feature/p012-oracle-capture-20260919 C:/tmp/P012_ORACLE_20260919 origin/master`). Build per the design note: a read-only tool that takes the public oracle/mark reads bracketing each funding hour (as the r2 manual reads did), writes them write-once with sha256 + manifest, and an adapter step that fills the D-4 locator `<sha256>#<pointer>` from those reads so `REAL_CAPTURE_READ_ONLY` candidates carry a tool-derived D-4 (tolerance O-2 = 0.05 %). Public Info reads only; no credential; offline fixtures for tests (record real read bytes from `P012_ORACLE_CAPTURE_20260917/reads/` as fixtures). Test-first, mutants, full Bridge suite, ruff parity, guard, commit, push. Roster: Gemini delta + exact-Opus + exact-Sol (one lane per account, coordinate via the queue log) → tell the Lead; the Lead merges.
**Phase 2 — r3 real capture (the owner's trade).** Before the window: give the Lead the exact runbook (what the owner does at open/close, the ownership message he signs, where the signature goes) 24 h ahead; run the oracle reader across the window; after the owner signs, run the capture tool at the merged sha (`CAPTURE_OK`/`VERIFY_OK`), then the intake with `--evidence-kind REAL_CAPTURE_READ_ONLY` → expect `REAL_CAPTURE_CANDIDATE_BUILT` with tool-filled D-4 and D-6 complete (window start exclusive, D6-GRID A). Record everything under `P012_PATH1_REAL_CAPTURE_<date>/r3/`. If N=3 authenticated fills are not yet reached (r2 had 2), ask the Lead for one more owner trade.
**Phase 3 — production closure matrix.** For every Class B row of the amendment, write one line: what closes it, the evidence file that closes it (or "OWNER" / "VENUE DOC" / "CANNOT-BY-DESIGN"), and its status after r3. Rows that need venue-document bytes (OPEN-01 instrument record, OPEN-03 fee schedule, OPEN-05 funding rules, the funding cadence): fetch the public Hyperliquid docs pages via a documentary route (WebFetch in your session, or a CodeBuddy/Cline lane in a scratch dir), store the bytes + sha256 + URL + date as `VENUE_DOC_<name>_<date>.md/.html`, and cite them; never paraphrase a venue fact without its bytes. Rows that need the owner (I-4 review, C-10 tier, SPECIALIST sign-off) take the section-0 answers. Produce `P012_ADMISSION_PACKET_V2.md`: per row - plain-language meaning, closing evidence, actor, recommended answer; end with the list of rows still open and why. Gemini review of the packet (documentary) + the Lead reads it.
**Phase 4 — the admission change.** Only when Phase 3 shows no open row except owner sign-offs: on a new branch, admit `REAL_CAPTURE_READ_ONLY` into `ACCEPTED_EVIDENCE_KINDS` behind the D-1..D-6 checks (and any row-specific gate the packet requires: e.g. funding interval start 2026-09-12T11:00Z, fee = venue-reported per fill only), with tests that a synthetic candidate is still accepted, a real candidate with tool-filled D-4 is accepted, and a hand-filled or out-of-interval one is refused. Full roster (Gemini + exact-Opus + exact-Sol + your reproduction). PR text names every closed row. **Do not merge**: the owner's release word `P012-ADMIT go` is given to the Lead, who merges.
**Phase 5 — close-out.** `P012_PRODUCTION_ADMISSION_RECORD_<date>.md`: the roster table, the matrix with every row closed or explicitly carried, the owner's words, the merge sha; register row; CT13 records; message the Lead.

## 3. Hard limits
No trading, no host/KVM2 action (if section 0 says KVM2 gates are in scope, you only PREPARE each step's exact packet; the owner presses the Toolkit buttons); no mainnet ACTION (public reads only); no credential; no estimate admitted as a fee; testnet evidence counts for nothing (Q2); every venue fact cited by bytes; one Pro lane and one Codex lane per account machine-wide; `tasklist | findstr agy` before any git; the Lead merges. Questions → `C:/tmp/CLAUDE_P0_RUN_20260913/P012_ADMISSION_20260919/QUESTIONS_FOR_LEAD.md` + `SendMessage` to "P0 Claude Lead Session 7"; then stop.

Report to the owner (through the Lead) in plain English after each phase: what closed, what is still open, what he must do next and when.
