# WP-P0-14 small reproducible read-only report — Lead recording note, 2026-09-14 06:58Z

Recorded under owner decision F YES (2026-09-13) with the scope fixed by `OD-20260914-P014-RO-1` (owner: "give recomended answers"): Q-A candidate families / lifecycle states / rejection reasons from the P031 fixture ledger; Q-B the P013 trial-catalog contract summary and whether any catalog data exists. The Minimum Explorer sequencing amendment is preserved (START_HERE.md:54); no package work, no Explorer implementation, no product code.

Chain: built on Codex Pro Spark (P14RO2, 4 min) → Grok detection audit 1 (`GROK_AUDIT_1.md`: 9 CORRECTION + 3 NIT — invented/wrong-line citations, paraphrased docstrings, 3-vs-7 unresolved contracts, unpinned interpreter, unpriced counts) → Spark correction died on its usage limit before editing → Grok correction 1 (`DISPOSITION_FIX1.md`, 12/12 FIXED) → Grok delta audit 2 (`GROK_AUDIT_2_DELTA.md`: 11/12 RESOLVED, event-count SQL cited a non-existent column, `:76`→`:75`) → Grok correction 2 (`DISPOSITION_FIX2.md`, 4/4 FIXED) → Grok delta audit 3 (`GROK_AUDIT_3_DELTA.md`: all closed, none REGRESSED, `GROK_P14C_VERDICT: CLEAN`).

Lead checks: reproducer run with the pinned interpreter is deterministic and matches the pasted block; the event-count query (`SELECT json_extract(canonical_event, '$.next_state') …`) returns CANDIDATE 1 / CAPTURED 1 / TRIAGED 1 on the copied ledger (read-only); `trial_catalog.py:75` EQUAL; files UTF-8 without BOM, LF; `SHA256SUMS.txt` OK. The copied inputs (fixture ledgers, `p031_lifecycle_ledger.py`, `trial_catalog.py`, tests, handoffs) stay in the scratch lane `C:/tmp/P014_RO_20260914/` and are named with digests in `SHA256SUMS.txt`.

Findings the report states (fixture data; nothing accepted): one fixture candidate `QLC-20260912-demo0001` in state `CANDIDATE` after CAPTURED → TRIAGED → CANDIDATE (3 events; `check_set_purpose` not persisted in this fixture evidence; `failing_checks` empty); unresolved lifecycle contracts: 3 in the older status snapshot, 7 encoded in the current reader source; the P013 trial-catalog contract exposes 19 public types with one refusal reason (`RESERVED_BOUNDARY_KEY`), 29 `def test_*` (40 with parametrization; suite not collected here), and NO catalog data file exists in the P013 worktree. Nothing here is a decision or acceptance.
