# DECISIONS

D026 | 2026-08-01 | **Minimum S3 scope defined by the Lead under Gate 1, because no S3 specification exists anywhere in the repository.** The 50-hour plan §16 names only "minimum S3 (initial liveness/lifecycle proof)". Searched `11_TRIAGE`, `_AI_MEMORY` and `IBKR_PAPER_BRIDGE/docs` — nothing further. Gate 1 is Lead-owned, so rather than block on an absent spec the Lead froze five items: S3-1 durable idempotent secret-safe deferral evidence; S3-2 explicit liveness in the existing status payload (minimum fields only, TS-P5-001 redesign stays deferred); S3-3 drain containment as a class; S3-4 queue-growth disposition where overflow must fail closed and never drop an event; S3-5 folding the carried Gate-5 nits. Out of scope: TS-P1-010+, EP-1's residual venue-side supersede window (Hyperliquid exposes no fencing token — a documented accepted limitation), any operations-dashboard or read-model redesign, any migration, any schema-default change. Recorded as an unattended decision in `11_TRIAGE/WPS_TSP1009B_S2_CLOSURE_RECORD_2026-07-31.md` §8.

D025 | 2026-08-01 | **DeepSeek V4 Flash and GLM-5.2 promoted to canonical Gate-5/Gate-6 auditors 3 and 4 (Barış).** Supersedes D024's advisory-only limit and the 50-hour plan §23c/§39-10 restriction **for audit authority only**; the plan document itself is frozen at `a07c90cc…` and is NOT edited. Canonical roster is now: `claude-opus-5` xhigh, `gpt-5.6-sol` xhigh, `cline-pass/deepseek-v4-flash` via Cline, and GLM-5.2 via the Z.AI Coding Plan route. Binding rules, written into `AGENTS.md` §CANONICAL AUDIT ROSTER: (1) an auditor that cannot execute the mandated suite must return BLOCK — non-execution is never acceptance; (2) a required finding from ANY canonical auditor is binding once the Lead reproduces it on real source, and an unreproduced finding is recorded as unreproduced with evidence rather than silently dropped; (3) acceptance requires accepting verdicts from both flagship auditors plus no unresolved reproduced required finding from any auditor; (4) rule 1 exists because GLM-5.2 once returned PASS-WITH-NITS on a commit carrying two severe defects while unable to run the suite at all. **Grants no implementation authority** — protected Bridge/core-runtime implementation stays with the flagship implementer. Authorizes no Git mutation, VPS/runtime, install, deploy, secret, broker/exchange, TESTNET, ARM, trading, network, reprovision, purchase, or mainnet action.

D024 | 2026-07-26 | **KVM2 documentation Cycle 4 authorized for this artifact only; GLM-5.2 is additional coder/auditor and Codex remains final authority.** Barış explicitly authorized continuation after Cycle 3 closed non-accepting at capped R3. Cycle 4/R1 may repair the KVM2 master, execution companion, audit prompt, superseded historical report banner, and this decision record. GLM-5.2 may act as the bounded coder and an additional independent advisory auditor; Codex independently verifies/classifies every GLM finding and remains lead/final acceptance authority. GLM does not replace any mandatory Gate 5 or Gate 6 auditor. For all future KVM2 Gate 5/Gate 6 work, the current canonical `AGENTS.md` roster governs and supersedes conflicting older KVM2 policy text: the required Claude auditor is exact `claude-opus-5` at effort `xhigh` — no implicit alias, no silent fallback, no `--resume`/`--continue` — and the required Codex auditor is exact `gpt-5.6-sol` with effort governed by the current `AGENTS.md`. This is task-specific documentation-write authority only, not standing authority for later document edits. Audit acceptance grants no edit or execution authority. This decision authorizes no Git mutation/publication, VPS/runtime access, installation, deployment, secret/account/credential provisioning, network/firewall/DNS/listener action, broker/exchange/TESTNET/ARM/trading action, reprovision, purchase, or mainnet/live action.

D023 | 2026-07-26 | **New KVM2 documentation audit cycle authorized; prior cap reset for this artifact; Cycle 3 is Codex-only.** Barış explicitly authorizes a new KVM2 documentation audit cycle, resets the three-round repair cap for this artifact only, and requires Codex-only repairs and fresh Codex audits with no Claude-credit use.
- Audit-cycle map (D023 ratifies the legacy labels and governs this reset):
  - Cycle 1: original rounds 1–3; closed non-accepting at the cap.
  - Cycle 2: R1 = round-5 focused repair; R2 = staged round-6/7 repair; R3 = final-repair-sync/slices; closed by the final non-accepting Opus verdict and STOP.
  - Cycle 3: owner-authorized by D023; R1 = fresh exact `gpt-5.6-sol`/`xhigh` `REQUEST_CHANGES`; R2 = `BLOCK: AUDITOR_TRANSPORT_UNAVAILABLE` after both frozen hashes were verified, before any content audit, with no content findings; current state is R3, Codex-only and the final permitted round under D023.
D023 waives the Claude auditor only for this Cycle-3 documentation repair/audit. Its fresh auditor must be exact `gpt-5.6-sol` at `xhigh`, ephemeral/read-only, with no fallback. The future lower-level bridge-candidate pre-deploy gate still requires fresh exact `claude-opus-5` at `xhigh`, no fallback and no resume/continue, on the exact repair diff/tests unless a later explicit owner decision amends that contract; D023 does not authorize invoking it now. This decision authorizes documentation work only: no Git mutation, runtime/VPS access, install, deploy, secret, broker/exchange, TESTNET, ARM, trading, network, reprovision, purchase, or mainnet/live action.

D022 | 2026-07-25 | **Claude canonical auditor upgraded to exact `claude-opus-5` at `xhigh` (Barış); D020 model clause superseded.** All other D020 audit rules remain binding: fresh independent sessions, no resume/continue from implementer context, no implicit alias or silent fallback, exact `gpt-5.6-sol` Codex roster/effort rules, standardized verdicts, and maximum three repair/re-audit rounds. Opus 5 currently has no available credits, so every audit requiring it remains BLOCKED/deferred until capacity returns; unavailability does not authorize Sonnet, Opus 4.8, or another fallback. This decision authorizes documentation alignment only and no implementation, merge, deploy, runtime, TESTNET, ARM, lab, network, reprovision, or mainnet action.

D021 | 2026-07-25 | **KVM2 lifecycle selected: bridge-first TESTNET host, then a controlled temporary AI laboratory; mainnet requires a clean-host fork.** Barış confirmed the Windows PC cannot remain continuously powered and wants the single Hostinger KVM2 to provide maximum useful value during the paper/TESTNET period. The bridge remains first priority and `11_TRIAGE/BRIDGE_VPS_DEPLOY_TASK_LIST_2026-07-25.md` remains authoritative/BLOCKED until repaired, independently accepted, and separately authorized. Only after accepted bridge-only stability may individually approved, isolated AI-lab workloads be admitted. Before any mainnet/live use, Barış will choose either (A) destructive reprovision of KVM2 from a trusted clean Ubuntu image into the trading-only profile, with credential rotation and verified-only restore, or (B) a separate clean trading VPS while KVM2 remains the lab. Removing agents or restoring a lab snapshot does not make the host clean. The clean rebuild kit is to be prepared now; this decision authorizes planning only and no install, deploy, secret, cutover, ARM, laboratory, network, reprovision, purchase, or mainnet action.

D020 | 2026-07-22 | **Audit model/effort/verdict/loop policy ratified (Barış).** Barış ratified the following binding rules for Gate 5 and Gate 6 audits: (1) Claude auditor must use exact model `claude-opus-4-8` at effort `xhigh`; no Sonnet, no implicit alias, no silent fallback — BLOCK if unavailable unless Barış explicitly waives. (2) Codex auditor must use exact model `gpt-5.6-sol`; effort `high` for ordinary G5; effort `xhigh` for G6/security/Pine/protected/architecture/re-audit after REQUEST_CHANGES or BLOCK — BLOCK if unavailable unless Barış explicitly waives. (3) Every audit session must be fresh and independent — no `--resume`/`--continue` from the implementer session; provide only scope contract, plan, diff/files, test evidence, repo rules. (4) Verdicts standardized to PASS / PASS-WITH-NITS / REQUEST_CHANGES / BLOCK; PASS-WITH-NITS is accepting with optional nits only and cannot contain a required repair; REQUEST_CHANGES requires a repair; BLOCK means workflow cannot safely continue. (5) Repair loop capped at 3 rounds per task; after the third non-accepting verdict stop and report to Barış. (6) Implementer is explicitly forbidden from commit/push/merge/rebase/destructive git — git sequencing is Lead-only after an accepting audit and only with authorization. Canonical roster defined in `AGENTS.md` §CANONICAL AUDIT ROSTER. This decision authorizes no commit, push, merge, deploy, or live action.

D019 | 2026-07-20 | **TS-P0 documentation closeout requested; reviewed docs commit/push completed; PR readiness assessed.** Barış instructed Codex to complete the remaining N3/N4/N5 closeout and the associated publication/readiness decisions. Codex completed N3/N4 locally in the pre-existing untracked main documentation set and committed the tracked contract markers plus N5 as `cfb08b81` on `feature/ts-p0-baseline`. PR #25 checks passed and it was marked ready for review. The instruction said to decide whether to merge/deploy, not explicitly to execute either destructive gate; therefore the earlier no-merge/no-deploy boundary was not silently overridden. Merge remains separately owner-gated. Deployment is additionally deferred because the PR is unmerged and runtime replacement would interrupt active Day 1 v2. No live/mainnet, threshold, strategy, or runtime authority is granted by this decision.

D018 | 2026-07-20 | **TS-P0 owner gates approved and Day 1 v2 explicitly authorized (Barış).** Barış approved the TS-P0 runtime-baseline hash scope, approved `RELEASE_EVIDENCE_CONTRACT.md`, and confirmed the sticky monitoring-window reset policy with the 300-second tolerance. He authorized push plus PR for exact audited commit `44338d61` on `feature/ts-p0-baseline`, but no merge or deploy. He separately authorized configuring the monitoring PC to remain awake, exactly one `MTC-Bridge-P2` task start, and exactly one paper/testnet ARM for Day 1 v2, with stop-on-failure and no retry, deploy, threshold, or strategy change. Codex executed those bounded actions: draft PR #25 opened; one task start and one ARM succeeded; run `paper-20260720090332` became ARMED at `2026-07-20T09:05:10Z`. This decision does not authorize PR merge, TS-P0 deployment, live/mainnet, or any further runtime mutation.

D017 | 2026-07-18 | **Interim TS-P1-007 funding exclusion ACCEPTED (Barış: "a").** Per Codex re-audit finding R-02 (`11_TRIAGE/CODEX_INTERIM_TSP1007_REAUDIT_2026-07-18.md`), no production path populates `fills.funding` — the Hyperliquid adapter maps `fee` only and no funding-ledger subscription exists. Barış explicitly accepts that the INTERIM daily-loss/consecutive-loss gate PnL is **gross − fees** (funding events not captured), as disclosed in `IBKR_PAPER_BRIDGE/docs/20_INTERIM_TSP1007_RISK_WIRING.md`. Real funding capture (subscription, signed attribution, day boundaries, broker cross-check) is deferred to TS-P1-005 / full TS-P1-007, which supersede the interim wiring. Revisit trigger: before any TS-P4-005 paper-evidence claim cites the daily-loss gate, or if observed funding costs become material relative to the 2% daily limit. This decision changes no thresholds, authorizes no deploy/push/ARM, and does not affect the unsigned live gate (live remains blocked).

D016 | 2026-07-18 | **ADR ratification (Barış): ADR-0018, 0019, 0020, 0021, 0022, 0023, 0024, 0025, 0027 ACCEPTED.** **ADDENDUM (same day, after explicit discussion):** Barış also ratified **ADR-0026, ADR-0028, and ADR-0029** ("ratify the last three"), following Fable's recommendation to keep all three unchanged. All twelve new ADRs (0018–0029) are now Accepted. ADR-0029 acceptance covers the promotion-gate FRAMEWORK only: `LIVE_TRADING_GATE.md` remains unsigned, live/mainnet remains blocked, no operational gate is signed by this ratification, and LLM/dashboard authority boundaries (0026/0028) are unchanged in substance — they are now binding owner decisions rather than proposals. Barış explicitly approved these nine records in writing on 2026-07-18; statuses flipped Proposed→Accepted in `09_DOCS/ADR/` citing this entry. Qualifications preserved from the ADR texts: ADR-0020 accepts the hybrid validation-tier DIRECTION — the hftbacktest Hyperliquid coverage audit (TS-P3-004) and current-engine mapping remain required evidence before integration; ADR-0024 accepts the storage-separation DIRECTION only — no database migration until the TS-P2-006 benchmark decides keep-vs-migrate; ADR-0018 + ADR-0025 resolve the build-versus-borrow route, so **TS-P0-004 is DECIDED: continue the existing Python system, build core risk/order/reconciliation internally, borrow bounded tools behind owned interfaces.** ADR-0026 (LLM safety boundary), ADR-0028 (read-only-first dashboard), and ADR-0029 (paper-to-live gates) remain Proposed pending separate discussion — their safety boundaries stay enforced by standing rules regardless (unsigned `LIVE_TRADING_GATE.md`, advisory-only LLM policy, read-only MTC dashboard). Live/mainnet remains blocked. This decision authorizes no code, runtime, threshold, or execution change.

D015 | 2026-07-05 | **Faz 3b Stage-1 discovery sweep APPROVED (Barış: "hepsini onaylıyorum yap").** Approves the pre-registered design `00_AGENT_PROTOCOLS/FAZ3B_STAGE1_SWEEP_PREREG_2026-07-05.md` AS AMENDED through Codex Gate-5 (report `11_TRIAGE/CODEX_GATE5_REPORT_FAZ3B_STAGE1_2026-07-05.md`, verdict APPROVE-WITH-CHANGES, all 5 edits applied in `571e5c02`; baseline comparability audited in `949f1875`). Authorizes, in order: (1) `MEGA_GRID_STRIDE` capped floor-selector engine knob + `grid_stride` row field + parity-harness assert-then-strip (self-parity must stay byte-identical, goldens NOT recaptured); (2) 1-cell smoke test; (3) the 980-job Stage-1 run (US equities 7 symbols, 10m 3 new modes + 1h 4 modes, stride-3, research_robust tier via union-adjusted DSR) under supervisor/watchdog with the pre-registered STOP rules. Same approval also covers: PR #15 merge to master, and untracked-file triage (commit USER_INTAKE 6 + 11_TRIAGE 11 + overnight ps1 2; delete `_tmp_native_us_equities_10m_audit_2026-06-28/`). Stage-2 confirmation remains separately gated (own pre-registration required BEFORE that run). Nothing from Stage-1 is promotable.

D014 | 2026-07-05 | **Faz 3b engine diff LANDED — Barış approved commit `cb8bf5a3` ("onaylıyorum") after adversarial Gate-5 audit (Claude Fable, verdict PASS WITH NITS).** Audit evidence: scope clean (only `mega_walk_forward.py` simulate_slice+plumbing, sanctioned harness edit, new tests; no GRIDS/gate/threshold/Pine/parity/MTC_V2 change); goldens NOT recaptured (single capture commit `75da649c`); `faz3b_self_parity.py --verify` re-run PASS (42 rows byte-identical, sha256 be8561ff…); 6/6 unit tests green; py_compile clean. Three NITS logged for Stage-1 pre-registration (close before any sweep): (1) short-path trail/channel branches untested; (2) NA slice (num_trades=-1) aggregation not wired end-to-end in `_worker_impl` fold means; (3) checkpoint key widened to 4-tuple — pre-Faz3b checkpoints re-run (wasteful, not wrong). **This decision approves the DIFF ONLY. No sweep is approved — Stage-1 remains a separate written gate per D013 (single-asset-class subset, trimmed grids, research_robust tier, micro-price exclusion).**

D013 | 2026-07-04 | **Faz 3b approved (Barış, exact sentence): swept `exit_mode` in `simulate_slice` (`fixed_2R` default, mandatory self-parity gate, two-stage DSR discipline) + items 2-3-4 package (micro-price exclusion from pooled leaderboards; two-tier label `research_robust` MIN_TRADES≥30 ∧ DSR≥0.50 vs unchanged `robust_final`; single-asset-class Stage-1 subsets).** Authorizes IMPLEMENTATION + SELF-PARITY REGRESSION ONLY — any sweep run remains separately approval-gated. Scope: `00_AGENT_PROTOCOLS/FAZ3B_EXIT_SWEEP_SCOPE.md`. `exit_mode` is NOT added to GRIDS (separate sweep axis; default trial counts and DSR unchanged). Golden baselines captured from the CURRENT engine BEFORE any edit; new engine at `fixed_2R` must match byte-identically or STOP. `research_robust` is a Stage-1 filter only and is never promotable.

D012 | 2026-07-02 | **First STG002 SYSTEM_TEST_ONLY local replay completed successfully.** Baris approved exactly one local replay run under Step 9.1. Codex ran `run_local_replay(...)` once into `MTC_COMMAND_CENTER/03_QUANTLENS/system_test/stg002_system_test_replay_20260702T171958Z/`. Result: `status=OK`, expected `888` (`ENTRY=444`, `EXIT=444`), received `888` (`accepted=888`, `duplicates=0`, `rejected=0`), simulated fills `888`, round trips `444`, unexplained `0`. This is systems-plumbing evidence only: not strategy approval, not paper-trading approval, not live-trading approval, not promotion evidence, and not profitability evidence. It does not approve schemas, broker/exchange/testnet, TradingView, WunderTrading, Pine, parity, `MTC_V2`, or real-money work.

D011 | 2026-07-02 | **SYSTEM_TEST_ONLY run output root is ignored.** Baris approved the pre-run readiness patch for the STG002 vertical slice. `.gitignore` now ignores `MTC_COMMAND_CENTER/03_QUANTLENS/system_test/` so local replay artifacts cannot accidentally enter Git. This decision does not approve a replay run, broker/exchange/testnet, TradingView, WunderTrading, schema writes, Pine, parity, `MTC_V2`, or real-money work. The first replay still requires the separate Step 9.1 approval sentence.

D010 | 2026-07-02 | **SYSTEM_TEST_ONLY vertical slice benchmark selected: STG002 / `QL_ALPHA_LINK_8EMA_1H`.** Baris approved Gate V0 planning and then approved STG002 as the benchmark. This is a systems-plumbing benchmark only, not strategy approval, paper approval, live approval, promotion evidence, or profitability evidence. Route remains Python-source-of-truth, localhost/fake-money first. No code, schema write, testnet, broker, TradingView, WunderTrading, Pine, parity, or live/paper execution action is authorized by this decision. Next allowed work is a draft implementation plan and review prompt only, unless Baris gives a separate explicit implementation approval.

D009-revised | 2026-06-07 | **scipy.sparse/stats hang: OpenBLAS 0.3.30 thread init deadlock on Haswell CPU.** Root cause revised 2026-06-07 (DeepSeek v4 Pro recovery session): the hang is NOT only about MSYS2 PATH entries. Deep diagnostic showed PATH has zero MSYS2 entries in PowerShell tool. The scipy base module imports fine; the hang is specifically in C extension modules (`_sparsetools.pyd`, `_stats.pyd`) that link against OpenBLAS 0.3.30 bundled with scipy 1.17.1. OpenBLAS config: `DYNAMIC_ARCH NO_AFFINITY Haswell MAX_THREADS=24`. Setting `OPENBLAS_NUM_THREADS=1` does NOT prevent the hang — the deadlock occurs during thread-pool initialization, before env vars are read. numpy.linalg works (uses separate OpenBLAS DLL). **Fix:** `_scipy_shim.py` — pure-Python `norm.ppf()`/`norm.cdf()` via Acklam algorithm (error < 1.15e-9). Intercepts `scipy.stats` before any C extension loads via `sys.modules` pre-seeding. Auto-injected by `run_python_clean.py`. All 425-cell sweep + CPCV + PBO + alpha ran successfully with shim. **Validation:** `norm.ppf(0.975)=1.95996` (true=1.95996), `norm.cdf(0)=0.5`. Multi-worker sweep (4 workers) stable. Full 59-strategy sweep now safe.

D009 | 2026-06-07 | ~~SUPERSEDED by D009-revised~~ **scipy import hang: MSYS2 PATH entries cause wrong DLL loading.** Root cause confirmed 2026-06-07: `import scipy` hangs because MSYS2 prepends its directories to PATH (e.g., `C:\Users\BarışSemaay\bin`, `/mingw64/bin`). When Python searches for BLAS/LAPACK DLLs, it finds MSYS2-built incompatible versions before the correct Windows scipy DLLs → DLL initialization deadlock. Confirmed via: `echo $MSYSTEM` shows `MINGW64` inside `powershell.exe -NoProfile -Command ...` launched from bash — the MSYS2 env IS inherited by ALL child processes (bash, powershell tool, cmd.exe, etc.). **Fix:** Strip MSYS2 PATH entries before launching any Python that imports scipy. Tool: `run_python_clean.py` (no scipy imports itself; strips MSYS2 vars+PATH from env before subprocess). Usage: `python run_python_clean.py target_script.py [args...]`. Also works for multiprocessing Pool workers (they inherit the cleaned env from their parent). **Scripts updated:** `sweep_new_only_2026-06-07.sh`, `overnight_remaining_2026-06-07.sh` (Phase 1, 2a CPCV, 2f alpha). **Does NOT fix:** Only clearing env vars (Python needs PATH to find itself); only removing `MSYSTEM` var (PATH still has wrong DLLs). **MSYS2 vars to strip:** `MSYSTEM`, `CYGWIN`, `MANPATH`, `MINGW_PREFIX`, etc. **MSYS2 PATH markers to strip:** paths containing `msys`, `mingw`, `/usr/bin`, `/usr/local/bin`.

D008 | 2026-06-07 | **PBO MemoryError — dual fix required (script cap + code early-exit).** Root cause: `probabilistic_pbo.py` with `--max-combinations 0` enumerates `itertools.combinations(range(n_splits), half)` — C(44, 22) ≈ 7.3 billion combinations for n_groups=10 (44 usable splits). **Bug:** the truncation `combos = combos[:max_combinations]` ran AFTER building the full 7.3B list — so even `--max-combinations 100000` still OOMed (second crash: Windows pagefile exhausted). **Fix 1 (code):** `probabilistic_pbo.py` line 62-68 changed to break-early: `if max_combinations > 0 and len(combos) >= max_combinations: break` inside the generator loop. **Fix 2 (scripts):** all overnight scripts must pass `--max-combinations 100000` (never 0). Patched: `probabilistic_pbo.py`, `overnight_batch_023_034_2026-06-07.sh`, `heavy_night_2026-06-05.sh`. 100k samples is statistically sufficient — logit-lambda PBO distribution stabilises well under 50k.

D007 | 2026-06-04 | SP-004 rubric D1-D6 sign-off (Barış). **D1**: Gate 1B = **/100, PASS ≥75** (uniform with Gates 1/2/3; CONDITIONAL 60-74, FAIL <60) — changed from the /50+PASS≥40 draft; criteria rescaled ×2. **D2**: PBO ≥0.5 → Gate 2 `OVERFIT_SUSPECT`, blocks promotion, keeps idea (accepted). **D3**: parity is **ADVISORY, not a hard gate** — mismatch → `PARITY_WARNING` + revisit note, does NOT block promotion; pure-Python strategies → `N_A` (Barış delegated; rationale: Pine layer may be retired for direct Python/Binance API execution). **D4**: Gate 3 reads separate `production_readiness_artifact_v1`, N/A until it exists (accepted). **D5**: numeric bands deferred to Phase 1.5 from real distributions (confirmed). **D6**: AI-drafts English thesis title, Barış overrides (accepted). Rubric `12_STRATEGY_EVALUATION_RUBRIC.md` updated to match; unblocks Phase 2 scoring lock. Follow-up: `evaluation_artifact_v1.schema.json` move `parity_gate` out of `hard_flags` → advisory `flags.parity_status` (Phase 1).

D006 | 2026-06-04 | AUDIT-008 (Barış): rolling-fold OOS overlap fix = **disjoint OOS via `step = test_size`** in `mega_walk_forward.py:604` (currently `step = remaining//(NUM_FOLDS-1)` = 0.10·span = half of test_size → structural 50% OOS overlap). Consequence: only 2 folds fit instead of 3 (f=2 runs past span_end), but they are fully independent. Downstream PASS logic already uses dynamic `n_folds=len(test_rets)` so it adapts. **Threshold rider (Claude-recommended, Barış to confirm at impl):** with n_folds=2 the existing `pos >= ceil(n_folds/2)` makes PASS = 1/2 OOS positive (weak); tighten PASS to `pos == n_folds` (ALL OOS folds positive) to keep the bar meaningful. Rolling-fold is a coarse screen; CPCV (D-AUDIT-002) + PBO (AUDIT-005) remain the rigorous gate. Existing 149 robust-PASS (DSR-unconfirmed) must be re-run under the new geometry before DSR-lock.

D005 | 2026-06-04 | AUDIT-009 (Barış): the 4 opening-range strategies (`QL_CONNELL_EVENT_DRIVEN_GAP_5M`, `QL_AVWAP_BRIAN_INTRADAY_OR_5M`, `QL_EPISODIC_PIVOT_CHRISTIAN_5M`, `QL_OPEN_RANGE_5PCT_STOP_CHRISTIAN_5M`) are **US-equity-session-only**. `bars_per_day=78` is correct for equity; do NOT generalize to crypto. On 24/7 crypto data they must be skipped with an explicit `INSUFFICIENT_DATA`/`N_A` note (opening-range has no meaning without a session open) — not run against an arbitrary UTC-midnight day boundary.

D004 | 2026-06-04 | AUDIT-001 (Barış): `QL_QTREND_V2_STRONG_ADX` intent = **strong trend**. Flip `overnight_v2_runner.py:594` from `adx_14 < adx_threshold` to `adx_14 >= adx_threshold`. Keep the strategy name (now consistent with the `strong_buy` gate already in the signal).

D003 | 2026-06-04 | MTC-Engine Validation is an additive shortlist-only funnel stage. Reuse existing `MTCRunner` with `src/config/profiles/light_risk.py`; implement one manual `SignalPlugin` adapter per promoted producer; use standalone Pine producer adapters for raw-signal parity only; do not edit `MTC_V2.pine` until final gated integration.

D001 | Phase 1 | `MTC_COMMAND_CENTER` legacy source is canonical for clean command center copy.
D002 | 2026-05-31 | Adopt internal CLI layer (`mtc_cli/`) as agent-native tool surface; reject CLI-Anything (wrong target — wraps GUI binaries, brittle generator, no fit). Dashboard buttons wrap CLI 1:1, no business logic in dashboard. CLI becomes sole programmatic writer for `_AI_MEMORY/` canonical files (automates Gate 7). Tracked as SP-001 in `NEXT_STEPS.md`. Start at Gate 1, re-check repo state at scaffold time before any write.
