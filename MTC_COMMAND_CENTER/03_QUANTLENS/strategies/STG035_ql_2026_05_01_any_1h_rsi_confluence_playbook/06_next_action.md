# Next Action

## Recommended Next Step
Decompose the RSI playbook into one deterministic prototype variant.

## Suggested First Variant
Use RSI `50` pullback plus `SMA(50)` confluence on `1h`, with stop beyond the moving average/support area and fixed `2R` target.

## Stop Condition
Do not run a broad 15-variant test until each setup and confluence rule is deterministic and lookahead-safe.
