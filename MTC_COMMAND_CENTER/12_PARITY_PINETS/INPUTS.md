# PineTS parity inputs

- Exact corpus/case IDs, Pine implementation/version, Python implementation/version, PineTS version,
  generation date, input data, oracle identity, and tolerance.
- Frozen pre-change golden and source commits; never use an unpinned working tree as the oracle.
- Relevant case fixtures only; pre-extract bounded samples from large corpora.
- Root `DECISIONS.md` and exact owner authorization for protected edits.

If any identity field is unknown, inventory it and block repository-wide conclusions.
