# PineTS parity outputs

- Per-corpus inventory and per-case result with implementation identities and tolerances.
- Executed, skipped, missing, reused, and not-comparable counts kept separate.
- Exact mismatches with minimal reproducible inputs; no silent golden/tolerance rewrite.
- RED/GREEN closure evidence and current-only state in `HANDOFF.md`.

Use scoped language: a corpus result proves only its recorded implementations, data, cases, oracle,
and date—not repository-wide parity.
