# Scoped implementation — G3/G4

Builder: start from the Lead's scope/plan/QA brief and selected INPUTS/TESTS. Confirm exact
whitelist and ownership. Preserve existing behavior outside the approved slice, foreign edits,
architecture, dependencies and protected boundaries. Minimal change; no speculative features.

Use meaningful red/green evidence when fixing behavior; independent expected results must not
come solely from your implementation. Exercise the complete scoped working path, not just each
layer separately. Follow prompt 05 and preserve exact failed/successful command output.

Bounded mechanical work uses the cheapest capable authorized route under the governance stage;
record provider/model, cost/entitlement check, exact paths, budget, fallback and stop. Inspect
delegated results yourself. A failed cheap route is not approval for a protected/model substitution.

Ordinary forward local commits of authorized owned scope are permitted under
`../../../00_AGENT_PROTOCOLS/AUTONOMY_AUTHORIZATION.md`, subject to explicit task restrictions,
exact staging and normal guard/hooks. Push/PR/merge and destructive Git retain separate gates.
Coordinate path ownership before another writer starts; never overwrite or silently transfer
foreign dirty work. Self-QA and a commit are not independent acceptance.

Report actual changed paths, outcome, command/cwd/environment, expected/observed results,
limits and out-of-scope discoveries. Supply factual HANDOFF inputs even if review is pending.
