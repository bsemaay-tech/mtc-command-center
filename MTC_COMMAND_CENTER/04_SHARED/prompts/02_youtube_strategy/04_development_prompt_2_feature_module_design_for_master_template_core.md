# 04 Development Prompt 2 Feature Module Design For Master Template Core 
DEVELOPMENT PROMPT 2 â€“ Feature Module Design for MASTER_TEMPLATE_CORE

SHORT DESCRIPTION
You will read:
- A TECHNICAL STRATEGY REPORT (generated by another LLM from a YouTube strategy video),
- Optionally, some original TradingView Pine Script code for that strategy.

Your job is to design a NEW MODULE for MASTER_TEMPLATE_CORE (either a FILTER, a MAIN SIGNAL
module, or an SL/TP/RISK module) and to produce a structured report that VS Code can use later
to implement the changes.

Several different LLMs will run this same prompt. I will merge your reports, so the output
must be highly structured and deterministic.

ROLE
You are an expert Pine Script v5 STRATEGY ARCHITECT working on a reusable template called
MASTER_TEMPLATE_CORE.

GLOBAL RULES
- First line of your answer MUST be:
  Model: <your model name here>
- Do NOT rewrite the entire MASTER_TEMPLATE_CORE script.
- Do NOT change the core engine architecture:
  - Risk engine (position sizing, daily loss limit, max trades per day, leverage cap),
  - SL/TP engine and Multi-TP structure,
  - JSON alert structure (fields and format),
  - Global filters framework (unless the feature is explicitly a new filter).
- Your job is to propose a CLEAN, MODULAR extension that:
  - Plugs into designated sections,
  - Is controlled by new `use_*` inputs,
  - Leaves behavior IDENTICAL when the new feature is turned OFF.

INPUTS I WILL PROVIDE (BEFORE THIS PROMPT)
1) FEATURE_TYPE:
   - One of: "FILTER", "MAIN_SIGNAL", "SL_TP_RISK".
2) A TECHNICAL REPORT from Prompt 1A / 1B / 1C describing:
   - Strategy type, rules, and a MASTER_TEMPLATE_CORE mapping.
3) (Optional) The original Pine Script strategy or indicator code
   used in the YouTube video (for reference only).

DO NOT assume these inputs are inside this prompt text; I will paste them separately.

YOUR GOALS

1) UNDERSTAND THE FEATURE REQUEST
   - Read the technical report carefully.
   - From the report, identify EXACTLY what new functionality
     should be added to MASTER_TEMPLATE_CORE:
       - If FEATURE_TYPE = FILTER â†’ a new filter module.
       - If FEATURE_TYPE = MAIN_SIGNAL â†’ a new Signal Plugin module.
       - If FEATURE_TYPE = SL_TP_RISK â†’ a new exit or risk mode.

2) PRESERVE MASTER_TEMPLATE_CORE ARCHITECTURE
   - Respect the idea that:
     - The SIGNAL PLUGIN produces:
       - `longSignal_raw`, `shortSignal_raw`
       - possibly `longSignal`, `shortSignal`, then edge triggers.
     - The ENGINE handles:
       - Entries/exits,
       - SL/TP,
       - Risk and daily limits,
       - JSON alerts.
   - New functionality must:
     - Be gated by a new boolean input, e.g. `use_new_filter_X`,
     - Default to OFF,
     - When OFF, the template behaves EXACTLY as before
       (no extra trades, no changed exits).

3) PRODUCE A STRUCTURED DESIGN + CODE PLAN
   Your output must have these sections:

   1) FEATURE SUMMARY
      - 3â€“8 bullet points:
        - What this module does,
        - Whether itâ€™s a FILTER, MAIN SIGNAL, or SL/TP/RISK component,
        - High-level logic.

   2) MASTER_TEMPLATE_CORE INTEGRATION PLAN
      - 2.1) New Inputs
           - List all new `input.*` parameters:
             - Exact variable name,
             - Type (bool/int/float/string),
             - Default value,
             - Group (e.g. grpFilter, grpSignal, grpSLTP, grpRisk, etc.),
             - Short description.

      - 2.2) New Internal Variables / Functions
           - List new helper functions or internal series you will introduce.
           - For each:
             - Name,
             - Purpose,
             - Where it will live (section 2: common functions? section 4: signal plugin? etc.).

      - 2.3) Public API Touch Points
           - Describe how the module affects the PUBLIC API:
             - If FILTER:
               - Provide `filterLongOk_new` / `filterShortOk_new`
               - Explain how they combine with `allowLong` / `allowShort`.
             - If MAIN_SIGNAL:
               - How to set `longSignal_raw` / `shortSignal_raw`.
               - How to produce `longSignal` / `shortSignal` and `longEdge` / `shortEdge`.
             - If SL_TP_RISK:
               - Which parts of SL/TP/BE/trailing/risk engine it augments,
               - How it is toggled (e.g. `use_special_SL_mode`),
               - How to ensure existing modes remain intact.

      - 2.4) Default Behavior Guarantee
           - Explicitly state:
             - Which input(s) default to OFF.
             - Confirm that when these are OFF, behavior is identical
               to the current MASTER_TEMPLATE_CORE.

   3) CODE CHANGE PLAN (FOR VS CODE)
      Write a precise plan that VS Code Chat can apply later.

      For each change, use this bullet format:

      - CHANGE #N â€“ <Short Title>
        - Type: ADD / MODIFY / REMOVE
        - Location:
          - Section name (e.g. "Section 1 â€“ Inputs", "Section 2 â€“ Common Functions",
            "Section 3 â€“ Filters", "Section 4 â€“ Signal Plugin", "Section 5 â€“ Engine",
            "Section 6 â€“ Visualization").
          - Rough anchor (e.g. â€œnear f_ma_filter() definitionâ€ or
            â€œbelow existing Stochastic filter blockâ€).
        - Description:
          - What will be added/modified/removed.
        - Code Snippet (if applicable):
          - Provide a self-contained Pine Script snippet that can be pasted
            with minimal adjustment.
        - Notes:
          - Any dependencies (e.g. must be combined with CHANGE #2 first).

   4) COMPATIBILITY & RISKS
      - Explain any potential pitfalls:
        - Interaction with existing filters,
        - Edge cases (low volatility, low volume, HTF gaps, etc.).
      - State explicitly:
        - Which parts are SAFE (architecture-preserving),
        - Which parts, if any, are RISKY or ambiguous and might need extra review.

   5) OPTIONAL VISUALIZATION
      - If the module benefits from new plots/shapes:
        - Propose minimal plotting changes.
        - Make them controlled by existing or new `show_*` inputs.

RULES
- Do NOT change existing JSON alert structure (fields, version, template_ver).
- Do NOT modify risk engine core logic, unless FEATURE_TYPE = SL_TP_RISK and change is explicitly scoped.
- Even then, risk engine changes must be:
  - Optional (guarded by a `use_*` input),
  - Backward compatible when OFF.
- Keep the report highly structured, clean, and deterministic so I can:
  - Run this prompt on ~6 LLMs,
  - Then merge their outputs into one consolidated design for VS Code.
